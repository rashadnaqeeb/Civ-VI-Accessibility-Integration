# Project Status: Civ VI Accessibility Integration (CAI)

## Current focus

- Camera zoom presets (2026-09-10). **BUILT, needs in-game test.** New `CameraZoomPreset` dropdown (Cursor section, sort 45; `off`/`close`/`mid`/`far` = zoom 0.0 / 0.25 / 1.0) in `settings_CAI.sql`; `WorldInput_CAI.lua` applies it with `UI.SetMapZoom(zoom, 0, 0)` after every cursor `LookAtPlot` and on `LuaEvents.CAISettingsChanged`. Zoom values come from the Wwise analysis in `docs/game-api.md` ("Camera zoom and audio"): close keeps positioned emitters, mid is where the map ambience's middle layers peak and wind is still silent, far is wind plus the boosted unit mix. `UI.LookAtPlot` zoom `<= 0` means keep, so `SetMapZoom` is used for all presets. 10 `LOC_CAI_SETTING_CAMERA_ZOOM_PRESET*` tags in all 6 UI languages.

- World Builder accessibility — **PLACEMENT TOOLS PANEL + PLACE/DELETE BRIDGE + VALIDITY PREVIEW + STATUS SPEECH + LOCKED SOURCE + MAP EDITOR + PLAYER EDITOR + PLOT EDITOR + PLACEMENT QUICK-NAV + TOOL NUMBER HOTKEYS + UNDO/REDO (Ctrl+Z / Ctrl+Y) + SCANNER WB CATEGORY + SCANNER VALID-TARGETS FOOTPRINT + SCREEN TUTORIALS BUILT; NEEDS IN-GAME TEST.** Full findings + CAI design direction in `docs/world builder.md`.
  - **WB visibility manager — BUILT (2026-09-06); NEEDS IN-GAME TEST.** New `src/UI/inGame/WorldBuilderVisManager_CAI.lua` (in modinfo `<File>` + `CAIInGame` ImportFiles; included in `WorldInput_CAI`; object published as `ExposedMembers.CAI_WBVisManager`). Corrects the earlier "live `DB.Query` on `RevealedPlots`" approach (line ~20): the WB map's SQLite tables are **on disk only**, written on save — no live read mid-edit.
    - **Revealed (persistent):** seeded at load from the loaded `.Civ6Map`'s `RevealedPlots(ID, Player)` via the DLL bridge (`ExposedMembers.CAI.OpenDatabase/Query/CloseDatabase`) — `Seed(injectedPath)` called in `WorldInput_CAI` `OnLoadScreenClose` using `injectedPath` (nil path ⇒ empty base, relies on tool + sight). Manual Set Visibility edits shadowed live: single-plot in `WBEditCursorPlot` (`SetRevealed(player, plotId, bAdd)`, only when the vis tool's player is non-nil), Reveal All via the `BuildButtonField` `onAfter` (`SetRevealedAll`). `IsRevealed = base ∪ sight`.
    - **Visible (sight):** `RecomputeAllSight()` scans all plots for units (`Units.GetUnitsInPlot`, `unit:GetUnitType()`→`GameInfo.Units[...].BaseSightRange`) + cities (range 3), full LoS. Recomputed on `Events.Unit/City Added/Removed/Moved/Teleported` (manager self-subscribes at load). Sight model from `GameInfo`: obstruction = `SightThroughModifier` (terrain+feature summed) + district +1; vantage = `Terrains.SightModifier` + district +1; LoS clear-line test `obstruction < max(hT, hV+1)`, half-hidden = visible if EITHER of two hex lines clear, +1 ring for tall features. Self-contained cube-hex math.
    - **Wired consumers (all route through the shared gate `GetWorldBuilderRevealGate(plot)` in `caiUtils`, active only when Set Visibility armed on a player):** `cursorAudio_CAI` (IsPlotRevealed/IsPlotVisible), `cursor_CAI` (CanUpdateZonesForPlot), `Surveyor_CAI` (IsVisiblePlot), `WorldScannerCategoryUtils` (IsPlotRevealed/IsPlotVisible → covers all scanner categories via `WorldScannerCore`), `hexCoordUtils` (plotsInRange), `interfaceInfoHelpers` (IsActiveInterfacePlotRevealed). `PlotToolTip_CAI` already gated via `CAIInfo.GetWorldBuilderRevealed`, now re-pointed to the manager. All 10 files `--check` clean; modinfo well-formed. Rules: https://civilization.fandom.com/wiki/Sight_(Civ6). Memory: [[reference_wb_visibility_no_getter]].
    - **PENDING IN-GAME TEST:** (1) arm Set Visibility, pick a player → cursor/tooltip/surveyor/scanner fog unrevealed tiles, read revealed ones; switching to another tool returns to see-all. (2) Enter reveals / Delete hides the cursor tile live; Reveal All reveals the whole map for the selected player. (3) place a unit → tiles around it reveal per LoS (hills/woods/mountain block; hill vantage sees over); place a city → its surroundings reveal. (4) **KEY: confirm the RevealedPlots seed count in the log** (`CAI WBVis: seeded N revealed rows`) — if the DLL read-row shape differs, N=0 and only tool+sight reveal (verify the row shape). (5) load a saved WB map with prior reveal → that reveal shows for its player from the start. (6) performance with the scanner open on a large map.
  - **F1/F2 editors weren't opening — FIXED (2026-09-05):** F1/F2 (and the launch-bar Map Editor / Player Editor buttons) fire `LuaEvents.WorldBuilderLaunchBar_OpenMapEditor` / `_OpenPlayerEditor` via the vanilla launch-bar globals `OnOpenMapEditor`/`OnOpenPlayerEditor`. Vanilla `WorldBuilder.lua`'s OWN local `OnOpen*` listeners translated those into `WorldBuilder_ShowMapEditor` / `_ShowPlayerEditor` (the events the CAI panels open on). `WorldBuilder_CAI.lua` REPLACES that context with a bare no-op (no base include), so those listeners were gone and F1/F2 opened nothing (Tab tools + F3 plot editor use their own direct events, so they worked — matching the report). Fix: `WorldBuilder_CAI.lua` now re-adds both listeners, mirroring vanilla (`LuaEvents.WorldBuilder_Show{Map,Player}Editor(Controls.WorldBuilder{Map,Player}Editor:IsHidden())` — the vanilla control is never shown so this always requests "show"; the CAI panel's own `if m_panel then return` guard ignores repeats). `--check` clean. **NOT caused by the tutorials** (purely additive catalog+text). **PENDING IN-GAME TEST:** F1 opens the Map Editor, F2 the Player Editor, and the launch-bar buttons do too.
  - **World Builder tutorials (2026-09-05):** 5 new one-time CAI screen tutorials added to `CAIUITutorialCatalog.lua` (`SCREEN_DEFINITIONS`), each keyed to its pushed root id so `OnRootPushed` fires them: **WORLD_BUILDER** (root `CAIWorldBuilderMode`, the WB map interface — general shortcuts incl. place/delete/lock/F3/undo/redo/goto/F1/F2, tool switching via Tab + number keys, placement, quick-nav; the pause key is intentionally omitted), **WORLD_BUILDER_TOOLS** (root `CAIWorldBuilderTools_Panel` — tool list + settings + dropdown safety + the scanner WB category), **WORLD_BUILDER_MAP_EDITOR** (root `CAIWorldBuilderMapEditor_Panel` — General + Text tabs), **WORLD_BUILDER_PLAYER_EDITOR** (root `CAIWorldBuilderPlayerEditor_Panel` — list + all 4 tabs, detailed), **WORLD_BUILDER_PLOT_EDITOR** (root `CAIWorldBuilderPlotEditor_List`). Titles auto-derive as `LOC_CAI_TUTORIAL_<Id>_TITLE`. 42 new tags ×6 langs in `TutorialText_CAI.xml` (5 name, 5 title, 32 content), reusing shared `LOC_CAI_TUTORIAL_NAV_LIST/_NAV_TABS`; translations anchored to vanilla `LOC_WORLDBUILDER_*` terms + the mod's own established WB phrasing. Catalog `--check` clean; all 6 XML well-formed; `verify_language.py` passes for all 5 non-en langs (tag parity exact). **PENDING IN-GAME TEST:** entering the WB map shows the World Builder tutorial once; opening Tab tools / Map Editor (F1) / Player Editor (F2) / Plot Editor (F3) each shows its tutorial once; Continue closes and returns focus; each reads correctly and none fall back to English per language; reset via Mod Settings re-shows them.
  - **Undo/redo + F1/F2 moved onto the WB widget (2026-09-05):** the `CAIWorldBuilderMode` interface widget (`WorldInput_CAI`) now owns four more keys, alongside Tab/Enter/F3/M/Delete/Ctrl+G/arrows/number-row. **Ctrl+Z = undo, Ctrl+Y = redo** via new local `WBUndoRedo(bRedo)`: mirrors vanilla `WorldBuilder(Placement)` OnUndo/OnRedo — `WorldBuilder.Can/Undo/Redo()` then fires `LuaEvents.WorldBuilder_SetPlacementStatus(Locale.Lookup(LOC_WORLDBUILDER_STATUS_{UNDO,REDO,CANT_UNDO,CANT_REDO}))` (the status line `WorldBuilderLaunchBar_CAI` already speaks); calls `CAIWorldBuilderStatusBurstBegin` FIRST so a run of identical "Undo successful" lines each speak (the de-dupe otherwise swallows repeats), and `RefreshValidTargetsScanner()` after. **KeyDown** (matches the other WB keys) is safe: `WorldBuilder_CAI.lua` replaces the vanilla WorldBuilder root context with a bare no-op input handler (`SetInputHandler(function() return false end, true)`, no base include), so vanilla's own KeyUp Ctrl+Z/Ctrl+Y handler never registers — the widget is the sole handler, no double-fire. **F1 = Map Editor, F2 = Player Editor moved from `WorldBuilderLaunchBar_CAI`'s `ContextPtr:SetInputHandler`** (deleted, along with its now-unused `mgr`/`WB_INTERFACE_ID` locals) **to widget bindings** firing `LuaEvents.CAIWorldBuilderMapEditor_Toggle()` / `CAIWorldBuilderPlayerEditor_Toggle()`; the launch bar now just `.Add`s `OnOpenMapEditor`/`OnOpenPlayerEditor` as listeners. Widget bindings fire only while the WB map interface is focused, so the old focus guard is now implicit. 4 new tags ×6 langs (`LOC_CAI_WB_MAP_EDITOR`, `_PLAYER_EDITOR`, `_UNDO`, `_REDO`; anchored to vanilla `LOC_WORLDBUILDER_{UNDO,REDO,MAP_EDITOR}` + `LOC_WORLD_BUILDER_PLAYER_EDITOR_TT`). Both Lua `--check` clean, all 6 XML well-formed. **PENDING IN-GAME TEST:** on the WB map, Ctrl+Z undoes and Ctrl+Y redoes (each press spoken, incl. repeats and the can't-undo/redo edge); F1 opens the Map Editor and F2 the Player Editor; all four inert inside any pushed CAI panel/editor.
  - **Escape = WB game menu on the widget (2026-09-05):** `CAIWorldBuilderMode` now binds **Escape (KeyUp)** → fires `LuaEvents.WorldBuilderLaunchBar_OpenInGameMenu()` (the same event the launch bar's Menu button raises). Because `WorldBuilder_CAI.lua` replaced the vanilla WorldBuilder root context with a bare no-op input handler, vanilla's own Escape→`OnOpenInGameMenu` listener was gone (nothing opened the menu; the launch-bar Menu button was dead too). Re-added the listener IN `WorldBuilder_CAI.lua` (the only context that can reach `Controls.TopOptionsMenu`, the InGameTopOptionsMenu child in WorldBuilder.xml): `UIManager:QueuePopup(Controls.TopOptionsMenu, PopupPriority.Utmost)` — reviving both the Escape key and the Menu button. **KeyUp, not KeyDown:** the CAI pause panel (`InGameTopOptionsMenu_CAI`) closes on Escape, so opening on KeyDown would let the same press's KeyUp reach the just-pushed panel and close it instantly; the widget consuming the KeyUp prevents that. 1 new tag ×6 langs (`LOC_CAI_WB_PAUSE` = "Open menu"). Both Lua `--check` clean, all 6 XML well-formed. **PENDING IN-GAME TEST:** Escape on the WB map opens the game menu (and the launch-bar Menu button now does too); the menu doesn't flash open-then-closed; Escape closes it again; Escape inside a pushed CAI panel/editor still closes that panel (not the menu).
  - **WB pause->Save/Options oscillation fix + redo suppression (2026-09-05):** the vanilla WorldBuilder root `InputHandler` (raw-key undo/redo; pause opens via a *separate input-action* handler, unaffected) fired during the transient moment the WB map regains focus in the pause->Save/Options handoff, fighting the incoming menu (log: repeated push/remove of `CAIInGameTopOptionsMenu_Panel` + "Expected a different input active context"). `WorldBuilder_CAI.lua` was briefly removed, now **restored with a conditional wrap** (file + both modinfo entries back): the vanilla `orig` runs **only while the WB map interface widget (`CAIWorldBuilderMode` in WorldInput_CAI) is the focused widget in the manager**; whenever anything else is focused (a pushed CAI panel, or a menu — pause/save/options) the wrap returns false so the vanilla handler never runs. **Ctrl+Shift+Z (redo) is always passed through (return false), never run as WB redo** — reserved for the screen reader; Ctrl+Y redo left intact. `--check` clean, modinfo well-formed. **PENDING IN-GAME TEST:** in WB, Escape->pause->Save opens save without flashing to the map; Escape from Save returns to the pause menu (same for Options); Ctrl+Shift+Z does not redo; WB map nav/editing unaffected.
  - **Mark-key (M) fix + Valid Targets footprint (2026-09-05):** the `M` lock binding used a non-existent `Keys.VK_M` (letter keys are bare `Keys.M`; only special keys use `Keys.VK_*`) — the binding key was nil so the toggle never fired. Fixed to `Keys.M` in `WorldInput_CAI` (`CAIWorldBuilderMode` widget). **New: while a tile is locked, the scanner's Valid Targets category lists the current tool's brush footprint at the locked tile, split into Valid/Invalid groups.** `WorldBuilderPlacement_CAI` exposes `CAIInfo.GetWorldBuilderBrushTargets(centerPlotId)` → `{ {PlotIndex, Valid}, ... }` (reuses `GatherBrushPlots`+`PlacementValid`; brush footprint only for Terrain/Continents, else the single plot, mirroring `GetWorldBuilderPlacementValidity`). `WorldInput_CAI` exposes global `CAIWorldBuilderScannerSourcePlot()` = the marked plot or nil (same context as the scanner). `WorldScannerCategory_validTargets` gains a WB branch: `CanScan`/`Scan` short-circuit to the footprint when a tile is locked (else unchanged unit/interface-mode path); groups `wbValid`/`wbInvalid` (`GroupOrderBySubCategory` valid-first; `GroupLabelResolver` now honors `firstItem.GroupLabelKey`, falling back to the old fixed label). Tile labels via the shared `CAIInfo:RequestPlotInfo` bridge. Scanner refreshes on mark toggle + after each edit via `CAIWorldScanner:RebuildCategory("validTargets")` (guarded by `GetActiveState()`); navigating to the category always rescans regardless. 2 new tags ×6 langs (`LOC_CAI_WORLD_SCANNER_WB_TARGET_VALID/_INVALID`). **Locked-footprint readout (2026-09-05 follow-up):** the plot-tooltip WB validity (`BuildWorldBuilderInterfaceInfo` in `interfaceInfoHelpers_CAI`) is now anchored to the LOCKED footprint when a tile is locked (was always cursor-centered): it looks the cursor plot up in `GetWorldBuilderBrushTargets(lockedPlot)` and speaks that tile's own Valid/Invalid; off the footprint it returns nil (no validity); on the locked center it prefixes `LOC_CAI_WB_LOCKED_TILE` and appends the whole-footprint valid count. **The locked plot is published as `ExposedMembers.CAIInfo.GetWorldBuilderMarkedPlot` (not a WorldInput global) because this readout runs in the PlotToolTip context (`PlotToolTip_CAI` → `GetActiveInterfacePlotInfo`), not WorldInput — the first attempt used a WorldInput global that was always nil there, so it stayed cursor-centered.** Unlocked path unchanged. The scanner footprint items now prepend Valid/Invalid (`LOC_CAI_PLOT_INTERFACE_VALID/INVALID`) to each tile label and `LOC_CAI_WB_LOCKED_TILE` to the locked tile. 1 new tag ×6 langs (`LOC_CAI_WB_LOCKED_TILE`). All Lua `--check` clean, all 6 XML well-formed. **PENDING IN-GAME TEST:** `M` locks/unlocks (speaks marked/unmarked); with a large brush on a mixed area, Valid Targets lists valid vs invalid footprint tiles; jumping to a listed tile works; category empty/absent when nothing locked and not in a unit target mode.
  - **World scanner "World Builder" category + Geography rivers/cliffs (2026-09-04):** new `src/UI/inGame/WorldScanner/WorldScannerCategory_worldBuilder.lua` (wildcard-loaded; added to both modinfo `<File>` lists, `DEFAULT_ORDER` + `TOOLTIP_KEYS` in `WorldScannerCategoryConfig`). `CanScan = WorldBuilder.IsActive()` so it's hidden outside WB; `Contextual = true`; `ExtractHiddenPlots = true` (WB reveals all). PlotExtract/EndExtract streaming, **2 subcategories** (startingPositions, ownership). **Rivers and cliffs moved to the Geography category** (`WorldScannerCategory_geography.lua`, new subcategory `riversAndCliffs`) since they're general terrain — so they now show in normal games too, fog-gated (geography's PlotExtract early-returns on unrevealed). Group tags renamed `WB_GROUP_RIVERS/CLIFFS` → `GROUP_RIVERS/CLIFFS`; `SUBCATEGORY_RIVERS_AND_CLIFFS` label reused by geography. Subcategories were: **startingPositions** — one item per plot where `PlayerManager():GetStartPositionInfo(plotIndex)` is non-nil; covers all 5 assignable `info.Type` values (Player/Leader/Civilization/RandomMajor/RandomMinor; "None" returns nil), grouped by type. Item label is type-prefixed via `LOC_CAI_WORLD_SCANNER_WB_START_LABEL` "{1_Type}: {2_Name}" — e.g. "Player: Rome" (`Utils.GetPlayerLabel(info.Player)`), "Leader: Trajan" (`GameInfo.Leaders[info.Leader].Name`), "Civilization: Rome" (`GameInfo.Civilizations[info.Civilization].Name`); RandomMajor/RandomMinor have no assignee so the label is just the vanilla type text (`LOC_WORLDBUILDER_RANDOM_PLAYER`/`_RANDOM_CITY_STATE`); **ownership** — owned plots (`plot:GetOwner()>=0`) bucketed per owner, each bucket zoned via `ZoneUtils.PartitionPlotIndices`, one group per owner, like the owner lens. **riversAndCliffs (now in Geography)** — `plot:IsRiver()` plots and `IsNWOfCliff/IsWOfCliff/IsNEOfCliff` plots each zoned, two groups (Rivers/Cliffs), item base labels reuse vanilla `LOC_TOOLTIP_RIVER`/`LOC_TOOLTIP_CLIFF` (zone utils appends tile counts); geography validators also require revealed. 13 new tags ×6 langs (category+tooltip, 3 subcategory incl. rivers-and-cliffs, 5 start-group, 1 start-label format, 2 river/cliff-group `LOC_CAI_WORLD_SCANNER_GROUP_RIVERS/CLIFFS`; WB/random terms anchored to vanilla `LOC_WORLDBUILDER_*`). Both Lua `--check` clean. **PENDING IN-GAME TEST:** WB category appears only in WB (starting positions read each placed start with correct type/name; ownership zones read per owner); WB category absent in a normal game; Geography's new Rivers and cliffs subcategory reads river/cliff zones with tile counts in both a normal game (revealed only) and WB.
  - **Go-to-coordinates (Ctrl+G) (2026-09-04):** WB interface widget (`WorldInput_CAI`) binds **Ctrl+G** → `OpenGotoEditor`: pushes a bare AlwaysEdit `EditBox` (id `CAIWorldBuilderGoto_Edit`), `SetCommitOnFocusLeave(false)` so closing doesn't re-commit. Grammar: first field = x, second (after a space) = z; **leading space omits x** (z alone); each field independently absolute (digits) or relative (`+`/`-` prefix, delta from `CAICursor:GetCoords()`). `SetCommitValidator` parses + bounds-checks (`Map.GetPlot`) — bad format speaks `LOC_CAI_WB_GOTO_INVALID` (full grammar explanation), off-map speaks `LOC_CAI_WB_GOTO_OUT_OF_BOUNDS`, both block commit; valid stashes the plot id, `value_changed` closes then `CAICursor:MoveTo(id, "jump")` (announces destination). Escape → `CloseGotoEditor` (`RemoveFromStack`). **New EditBox edit mode `NumericSymbols = 4`** in `CAIWidget_EditBox` (digits + space + `+`/`-`; `CharAllowed` set `[0-9%+%- ]`); caller passes literal `4` to `SetEditMode` since the `EditModes` enum table doesn't cross contexts. Parsing avoids `%s` (uses `[ \t]`, plain `string.find` for the space). 3 new tags ×6 langs (`LOC_CAI_WB_GOTO` = label + Ctrl+G desc, `_GOTO_INVALID`, `_GOTO_OUT_OF_BOUNDS`). Both Lua `--check` clean, all 6 XML well-formed. **PENDING IN-GAME TEST:** Ctrl+G opens the box; `15 5`, `15`, ` 5` (leading space), `+2`, `+2 -1`, ` -1` all move correctly; garbage + off-map entries speak their errors and stay open; Escape closes; Ctrl+G reaches the box before any camera/global handling.
  - **Placement quick-nav + Rivers/Cliffs direction (2026-09-04):** arrow-key ambient parameter cycling on the WB map, supplementing (not replacing) the Tab tools panel. `WorldInput_CAI` WB widget binds arrows (**KeyDown**) → `LuaEvents.CAIWorldBuilderQuickNav(action)`; `WorldBuilderPlacement_CAI` (`OnQuickNav`) owns the model/state/speech. Params list = `{ Tool, <tool's params> }` rebuilt every keypress from live vanilla state (mirrors `RebuildSettings`, minus action buttons; amount only when `ResourceAmountStack` visible). Left/Right = prev/next param (wrap), Up/Down = value change, Shift+Left/Right = first/last param, Shift+Up/Down = first/last value (list & tool only; amount/checkbox have no jump). **Direction (fixed after test):** on `listLike` params (tool/type/pulldown/brush/rotation/direction) **Down advances, Up goes back** to match the panel's list nav; on amount/checkbox Up increases/on, Down decreases/off. **Wrap sound:** any list/tool/param ring wrap calls `mgr:HandleNavigationWrap()` (plays `UI_MENU_WRAP`); `cycle` returns a wrapped flag, param nav compares the index. **Cities-owner fix:** its pulldown param now derives `DerivePlayers(true)` (was wrongly `DeriveCities` → showed only one owner). Drives the SAME vanilla controls as the panel (capture select-closures, brush buttons, `OnRotateRight`, pulldown `Get/SetSelectedIndex`, pillaged checkbox, `ResourceAmount:SetText`) so both stay in sync; tool cycle re-arms via `ArmTool`, cursor stays on param 1 ("focus back to tools"). Speech: param switch = `name` + sep + `value` (tool sep `", "` → "Tool, terrain: plains, Small, 1 tile"; others `": "`), value change = value only. **Rivers/Cliffs now have a Direction param** (CAI-only; vanilla uses mouse-nearest edge): `m_edgeIndex` (0-5 = DirectionTypes), added to `RebuildSettings` (dropdown) + `BuildToolParamString` + quick-nav; published as `CAIInfo.GetWorldBuilderEdgeDirection()`; `WBEditCursorPlot` uses it as the `edge` for river/cliff placement, else falls back to `GetCursorNearestPlotEdge()`. 10 new tags ×6 langs (`LOC_CAI_WB_DIRECTION`, `_PARAM_TOOL`, `_QN_PREV/NEXT_PARAM`, `_QN_VALUE_UP/DOWN`, `_QN_FIRST/LAST_PARAM`, `_QN_FIRST/LAST_VALUE`); direction names reuse vanilla `LOC_WORLDBUILDER_DIRECTION_*`, checkbox reuses `LOC_UIWidget_Checked/Unchecked`. Both Lua `--check` clean, all 6 XML well-formed. **PENDING IN-GAME TEST:** (1) **KEY: confirm arrow KeyDown reaches the WB interface widget and is consumed BEFORE camera pan** — vanilla pans via CameraPan* input *actions*, not raw keys, so `mgr:HandleInput` (called before vanilla `orig` in `OnInputHandler`) should win; verify arrows don't also scroll the map. (2) Left/Right cycles params with correct name+value speech; Up/Down changes each param type (list cycle, brush, rotation, direction, amount ±1, pillaged on/off, tool switch); Shift jumps; (3) placing a river/cliff uses the chosen Direction; (4) panel dropdown and quick-nav stay in sync.
  - **Tool number hotkeys (2026-09-05):** number row selects a Placement tool directly by palette position from the WB map, panel-free. `WorldInput_CAI` `BuildToolSelectBindings()` generates 16 bindings on the `CAIWorldBuilderMode` interface widget (**KeyDown**, matching the other WB keys): slots 1-10 = `Keys["1".."9"]` then `Keys["0"]` (no shift), slots 11-16 = **Shift**+`Keys["1".."6"]` (`Keys.1` is not a valid identifier — number-row digits are string-keyed, confirmed by the input-help `KEY_NAMES` table). Each fires `LuaEvents.CAIWorldBuilderSelectTool(pos)`; `WorldBuilderPlacement_CAI` (`OnSelectTool`) arms `CAI_TOOLS[pos]` via `ArmTool`, resets `m_qnIndex = 1`, and speaks tool name + `BuildToolParamString` (out-of-range pos = no-op). Only active on the map (bindings live on the interface widget; a pushed panel/editor consumes input first). 1 new tag ×6 langs (`LOC_CAI_WB_SELECT_TOOL`; all 16 share it so input-help merges them into one "1, 2, …, Shift+1, …" line). Lua `--check` clean. **TESTED & WORKING (2026-09-05):** digits 1-0 and Shift+1-6 select the right tools and speak them; no camera/input leak on the WB map; inert while a panel/editor is open.
  - **Plot Editor (2026-09-04):** new `src/UI/inGame/WorldBuilderPlotEditor_CAI.lua` (ReplaceUIScript on `WorldBuilderPlotEditor`) + tiny `src/UI/inGame/WorldBuilderMapTools_CAI.lua` (ReplaceUIScript on `WorldBuilderMapTools`); both `<File>`+`<ReplaceUIScript>` in modinfo, both `--check` clean, modinfo well-formed. **Supersedes the Ctrl+Enter stub:** `WorldInput_CAI`'s WB widget now binds **F3** (beside F1 Map / F2 Player editors) → `CAIWorldBuilderPlotEditor_Toggle(plotId)` on the source plot (marked tile if locked, else cursor). **ONE pushed `List`**, label = `LOC_WORLDBUILDER_SELECT_TOOL` ("Plot Selector") + `(x, y)`. **Tab switch is functional, not cosmetic:** F3 fires `LuaEvents.CAIWorldBuilderSelectTab("PlotEditor")` → MapTools_CAI `DoLeftClick`s the vanilla `SelectTab_WorldBuilderPlotEditor` button → the plot editor context's **show handler** (wrapped) re-seeds our plot via base `UpdateSelectedPlot(plotId)` (orig OnShow clears to nil first) and opens the list; Escape fires `SelectTab("Placement")` → base OnHide (wrapped) closes the list. Switching back to Placement re-activates that tab's `OnPlotSelected`, restoring the brush-place flow (both tabs share `WB_SELECT_PLOT`, only the shown one's handler fires). **Grouped per user direction:** standalone `Dropdown`s = Terrain, Owner, Lowland (GS-gated via base global `IsExpansion2()`); `SubMenu`s (header tag reused from the Placement mode list, summary appended like the Placement tool rows) = Features (feature+direction), Resources (resource+strategic amount edit), Improvements/Districts/Routes (type+pillaged checkbox), Start Positions (type + Player/Leader/Civ sub-dropdown, only the matching one visible via hidden predicate). **Drives vanilla like Placement_CAI:** option lists re-derived from `GameInfo` in exact base order (features/resources use the base's localized-name sort), CAI dropdown index maps 1:1 onto the vanilla pulldown, seed from live `pulldown:GetSelectedIndex()`, commit via `pulldown:SetSelectedIndex(value, true)` so every cascade/undo runs; pillaged checkboxes call base `On*PillagedButton` when out of sync; disabled/hidden predicates read the live vanilla control state (covers no-plot/water/non-strategic). After each commit `ReseedAll()` re-reads every field so cascades (terrain dropping a feature) reflect; `WorldBuilder_StartPositionChanged` also reseeds. **No new locale tags** (all vanilla `LOC_WORLDBUILDER_*` + existing `LOC_CAI_WB_EDIT_TILE`/`LOC_CAI_KB_CLOSE`). Input rides the plot editor context's own `SetInputHandler` (base has none). **Per-entry validity (fix, 2026-09-04):** vanilla `UpdatePlotInfo`/`UpdateDistrictInfo` gray individual Feature/Resource/Improvement/District pulldown entries per tile (`CanPlaceFeature`/`CanPlaceResource`/`CanPlaceImprovement`/`CanStartOperation`); the first cut ignored this and let you place invalid items. Now each of those four dropdowns carries a per-option `disabledPredicate` (via `ApplyValidity`, options keep their GameInfo `row`) replicating the exact vanilla check against the source plot, so an invalid item is announced disabled and its activate no-ops. Terrain/Owner/StartPos/Lowland have no per-entry validity in vanilla; Route disables the whole pulldown on water (already mirrored via `pulldown:IsDisabled()`). **Strategic-resource amount box (vanilla quirk, left faithful):** the Placement tool hides the amount for strategic resources under Gathering Storm (`... and not IsExpansion2Active()`), but the vanilla Plot Editor has no such gate and shows it regardless of GS — so CAI shows it in the editor too (mirrors `Controls.ResourceAmount:IsHidden()`). Could add the GS gate to match the tool if consistency is preferred over faithfulness. **PENDING IN-GAME TEST:** F3 over a tile switches the vanilla tab + opens the list titled with coords; each field reads/commits with correct disable/hidden state; terrain change cascades (feature/route reflect); submenu summaries update; strategic resource shows amount; start-position type swaps the sub-dropdown; Owner/Lowland; Escape switches back to Placement and the brush place/delete flow still works.
  - **WB map reload force-disables CAI — IMPLEMENTED via DLL SQLite bridge (2026-09-06); NEEDS IN-GAME TEST.** Loading a `.Civ6Map` hard-overwrites the session mod set from the file's `ModDependencies`; CAI (`AffectsSavedGames=0`) is never written there, so it's dropped. The DLL now exposes `ExposedMembers.CAI.OpenDatabase/Query/CloseDatabase` (a `.Civ6Map` is plain SQLite). Shared helpers `WBMapDepInject(path)`/`WBMapDepStrip(path)` in `LoadSaveHelpers_CAI.lua` (pcall-guarded; old DLL logs+no-ops). **Load:** the main-menu "Load" flow loads a WB map via `OnLoadYes` → `Network.LoadGame` (VERIFIED in log; NOT the TILED_MAP branch — the first attempt gated on TILED_MAP in `OnActionButton` and never fired). `LoadGameMenu.lua` now injects in a wrap of `OnLoadYes` (gated on `m_thisLoadFile.Path` ending `.Civ6Map`), plus `OnActionButton` for the TILED_MAP import branch; `INSERT OR IGNORE`s the CAI row and stashes the path in `ExposedMembers.CAI_WBInjectedMapPath`; `WorldInput_CAI.lua` `OnLoadScreenClose` (after the engine consumed the deps) strips it back out and clears the stash. **Save:** `SaveGameMenu.lua` remembers the target `.Civ6Map` (new: `g_CurrentDirectoryPath.."/"..<FileName>..".Civ6Map"`; overwrite: `g_FileList[sel].Path`) in `OnActionButton`/`OnYes` and strips on `Events.SaveComplete` (saves are async; event carries no path). GUID `9f4b5c2e-1a2b-4c3d-8e9f-123456789abc`, title `{"LOC_CAI_MOD_TITLE":[]}`. All 4 files `--check` clean. Detail in `docs/game-api.md` "Session mod set" note + memory `project_wb_dependency_injection_plan`. **PENDING IN-GAME TEST:** (1) load a saved WB map → CAI stays on (accessible in WB); (2) that map file, opened afterwards, has NO CAI row (loadable without CAI); (3) save a WB map while CAI is live, then confirm the saved file has no CAI row — watch the save-strip `rows removed` log: `1` = save re-wrote CAI (strip needed), `0` = `AffectsSavedGames=0` already excluded it; (4) confirm `OnLoadScreenClose` actually fires for the WB-map load path (the load strip depends on it).
  - **Player Editor (2026-09-03):** new `src/UI/inGame/WorldBuilderPlayerEditor_CAI.lua` (ReplaceUIScript on `WorldBuilderPlayerEditor`, LuaContext = FileName `WorldBuilderPlayerEditor` per `WorldBuilder.xml`; `<File>`+`<ReplaceUIScript>` in modinfo). **ONE pushed panel = Players `List` + `TabControl` side by side** (mirrors vanilla's single modal, NOT a push-to-detail — restructured after first pass). **Add Player / Add AI are the last rows IN the list** (MenuItems), `Delete` on a player row = `UninitializePlayer`. **Selection follows focus:** a player is selected on its row `focus_enter` (guarded `IsFocused()` + changed idx); `Tab` from the list lands on the TabControl (panel child 2). Tab widgets built **once**, read live `SelIndex()`; switching player is a **reseed not a rebuild** — `RefreshDetail` re-seeds dropdown selected indexes + Gold/Faith text + every tech/civic checkbox (all silent) and rebuilds the city tree, without stealing focus (tree capture is nil while focus is in the list). No selection ⇒ all controls disabled via predicate. Tabs: General (Civ/Leader/CivLevel/Era **Dropdowns** + Gold/Faith EditBoxes, vanilla disable rules, commits via `PlayerManager():SetPlayer*` by Type/Index), Techs & Civics (flat checkbox `List`s), Cities (a `Tree`). Option lists **re-derived from `GameInfo`** (base's `m_*Entries` are unreachable file-locals). Cities tree: city node (Delete=`Remove`) → **Population `EditBox` directly in the tree** (plain non-AlwaysEdit so Up/Down still navigate; Enter edits/commits → `SetCityValue`; `NumbersOnly`+maxlen22), Districts + Buildings subtrees; each item leaf reads live "Not added/Added/Added, Pillaged" (**pillage display-only**), Enter adds, Delete removes (`RemoveDistrict`/`RemoveBuilding`, leaf re-announces). **Dropdowns track live state:** vanilla shows ALL leaders (not civ-filtered — CAI matches); picking a civ cascades to its default leader → `WorldBuilder_PlayerEdited` → `ReseedGeneral` re-seeds the General dropdowns/edit boxes (like vanilla `UpdateGeneralTabValues`). Gold/Faith/Population are `NumbersOnly`+maxlen22 (vanilla `SetNumberInput`+`MaxLength=22`). Fixes since first build: tech FocusKey used `row.TechType` (nil) → `row.TechnologyType`; population no longer a pushed sub-editor. **Add City + add-District use the vanilla placement handoff** (`WorldBuilderModeChangeRequest` → closes editor, arm tool on map — no camera/cursor aid); buildings are inline `CreateBuilding` (list is non-placement only) with base `PlacementSetResults` speaking the status line. **Opened by F2:** `WorldBuilderLaunchBar_CAI` now fires `OnOpenPlayerEditor()` on F2 KeyUp (beside F1→Map Editor), gated to focus `== "CAIWorldBuilderMode"`. Input rides a wrap of the base global `OnInputHandler`. 4 new tags ×6 langs (`LOC_CAI_WB_PE_ADDED/_NOT_ADDED/_PILLAGED/_REMOVE`); everything else reuses vanilla `LOC_WORLDBUILDER_*`/`LOC_PLAYERS`/`LOC_HUD_REPORTS_HEADER_POPULATION`. Lua `--check` clean, all XML well-formed. **PENDING IN-GAME TEST:** F2 opens on the WB map; arrowing players updates the tabs; `Tab` from the list reaches the TabControl; add/remove players; General dropdowns+edit boxes read/commit with correct disable states; techs/civics toggle; city tree population edit; district add→map handoff, building add inline (+status speech), Delete removes districts/buildings/cities; Escape closes.
  - **Map Editor (2026-09-02):** new `src/UI/inGame/WorldBuilderMapEditor_CAI.lua` (ReplaceUIScript on `WorldBuilderMapEditor`; LuaContext = the LuaContext **ID** `WorldBuilderMapEditor`/FileName, NOT the XML `<Context Name>` `WorldBuilderMapModEditor`; `<File>`+`<ReplaceUIScript>` in modinfo). Hooks `LuaEvents.WorldBuilder_ShowMapEditor` to push/tear-down one CAI `Panel` = a 2-tab `TabControl`. **No Mod tab** — vanilla's 3rd tab is genuinely empty in both the Lua (`UpdateModPage` no-op) and XML (`ModInstance` has no controls). **General** = a Map metadata `List` (label `LOC_CAI_WB_MAP_METADATA`) holding ID (a **Button**, "ID: <value>", tooltip `LOC_CAI_WB_GENERATE_ID_TT`; activate regenerates + re-announces) and Width/Height (StaticText from `GetMapValues()`); then Is Mod (Checkbox→`SetMod`); then the editable fields Ruleset/MapScript/RefMap/RefAlpha (AlwaysEdit EditBoxes, **commit on Enter only** via `SetCommitOnFocusLeave(false)`, through base globals `On*Edited` so validation + ref-map/alpha status-line speech are vanilla's; RefMap path seeds empty — base `m_ReferenceMapName` is an unreachable file-local). **Text** = language `Dropdown` + entry `List` (re-derived from `ModManager():GetKeyStringPairByIndex`, index parity) + Add button: row head = system annotation for entries 1–4, the loc **tag (key)** for custom 5+, then the string; the generic custom annotation is the **Add button tooltip**. **Delete** removes (rows ≥5 only, honoring vanilla's disabled Remove); **Enter** opens an editor `Dialog` (title `LOC_CAI_WB_EDIT_LOCALIZATION`, Tag+String AlwaysEdit boxes, OK commits via `SetKeyStringPairByIndex`/`SetString`); **Add** opens that editor and **leaves focus on the Add button** after OK (so Shift+Tab reaches the list, not the language dropdown — the list is one tab-stop); edit returns to the row, delete announces the landing once (no double-speak). Input rides a **wrap of the base global `OnInputHandler`** (survives base `OnInit` re-registering it). 4 new tags ×6 langs (`LOC_CAI_WB_DELETE_TEXT`, `_EDIT_LOCALIZATION`, `_MAP_METADATA`, `_GENERATE_ID_TT`); all other labels reuse vanilla `LOC_WORLDBUILDER_*`/`LOC_OK`/`LOC_CANCEL`/`LOC_OPTIONS_LANGUAGE`. Lua `--check` clean, all XML well-formed. **Opened by F1:** `WorldBuilderLaunchBar_CAI` adds the launch bar's only input handler — F1 KeyUp fires the base `OnOpenMapEditor()` but only while `mgr:GetFocusedWidget():GetId() == "CAIWorldBuilderMode"` (the WB map interface widget in `WorldInput_CAI`, which already had that static id), so it's inert inside any pushed panel. **PENDING IN-GAME TEST:** F1 on the WB map opens the Map Editor (and the launch-bar button still does); General fields read/commit; Generate New ID re-announces; Text tab language switch + list + Enter-edit + Add + Delete on custom rows (system rows 1–4 not deletable); Escape closes.
  - **Advanced mode + plot-tooltip WB info + WB coords (2026-08-31):** (a) **Force Advanced was never implemented** (docs said so; no `SetWBAdvancedMode` call existed) — caused the Improvements tool to place tribal villages (Basic-mode slot = Goody Huts tool). `WorldBuilderToolsPalette_CAI.lua` now calls `SetAdvancedMode(true)` after include (guarded by `not GetWBAdvancedMode()`); it fires `WorldBuilder_ModeChanged` so panels repopulate. (b) `HexCoordUtils.coordinateString` now returns the map's **actual x,y** when `WorldBuilder.IsActive()` (was capital-relative → empty in WB). (c) `PlotToolTip_CAI.lua` gains `wbStartPosition` (`GetStartPositionInfo`) at the end of the cursor-move/default buckets (`LOC_CAI_WB_TT_START_POSITION`, 6 langs). **Visibility = LIVE MAP-DB READ (REWRITTEN 2026-09-03, needs in-game test).** Superseded the shadow-cache/`Plot:SetProperty`/gameplay-context-config-bridge approach: while WB is active the loaded map's SQLite tables are queryable with `DB.Query`, and per-player reveal lives in `RevealedPlots(ID, Player)` where `ID` is the 0-based plot index. `CAIInfo.GetWorldBuilderRevealed(player, plotIndex)` now just runs `DB.Query("SELECT 1 FROM RevealedPlots WHERE ID = ? AND Player = ? LIMIT 1", ...)`; `GetWorldBuilderVisibilityPlayer` unchanged. The setters (`SetWorldBuilderRevealed`/`SetWorldBuilderRevealedAll`), the whole serialize/parse/save cache, the `LoadGameViewStateDone` restore, and the `WorldBuilderRevealStore_CAI.lua` gameplay script + `ExposedMembers.CAI_WB_Reveal*` bridge are **DELETED** (file removed, both modinfo entries removed). The Reveal All panel button and the per-plot reveal in `WBEditCursorPlot` now just drive the vanilla controls (which write `RevealedPlots`); nothing to mirror. `PlotToolTip` reads back live. Reveal persistence across save/reload is now vanilla's own (`RevealedPlots` is serialized into the `.Civ6Map`). Placement/WorldInput/PlotToolTip all `--check` clean. **PENDING IN-GAME TEST:** (1) reveal for Norway via Enter → cursor reads terrain; unrevealed → fog; Reveal All flips; player switch switches readout; (2) **KEY: confirm `DB.Query` on `RevealedPlots` reflects reveals made in the SAME session (live), not just the state loaded from the map file** — if it only shows loaded state, in-session edits won't read back until save/reload. All Lua `--check` clean, XML well-formed. **PENDING IN-GAME TEST:** improvements now place the chosen improvement (not tribal villages); cursor reads actual coords + start position + visibility in WB.
  - **Map-tack hotkey move + status de-dupe fix (2026-08-31):** (a) moved the map-pin/minimap-list input-action listeners (place pin `M`, lens list, map-pin list) out of `WorldInput_CAI` (runs in WB too) into `MapPinListPanel_CAI`'s `m_mapPinActions`; its `OnMapPinInputActionStarted` now early-outs under `GameConfiguration.IsWorldBuilderEditor()` so map-tack keys (incl. `CAIDeleteMapTac` on Delete) don't collide with WB cursor keys. `PlaceMapPin` (vanilla `WorldInput` global) reached cross-context via `LuaEvents.CAIRequestPlaceMapPin` (bridged in `WorldInput_CAI`). (b) status-line speech de-dupe is now **per keypress** not global: `WBEditCursorPlot` fires `LuaEvents.CAIWorldBuilderStatusBurstBegin` to reset last-spoken text, so brush repeats within one keypress still collapse but pressing Enter twice on the same failure speaks both times. All three files `--check` clean.
  - **Placement preview + feedback + locked source (2026-08-31):** three additions, all `lua-language-server --check` clean, XML well-formed. (1) **Validity preview on the InterfaceInfo key** — `WorldBuilderPlacement_CAI` publishes `ExposedMembers.CAIInfo.GetWorldBuilderPlacementValidity(plotId)` (data only: `{valid, footprint, brushValid?, brushTotal?}`) since `PlacementValid`+pulldown are placement-context-only; `interfaceInfoHelpers_CAI` registers ONE helper on `InterfaceModeTypes.WB_SELECT_PLOT` (all tools share it — `mode = GetSelectedEntry()` already encodes tool+item+rotation, no per-tool branch) that speaks Valid/Invalid + footprint + brush count. Read at the cursor. New tags `LOC_CAI_WB_FOOTPRINT_TILES`, `LOC_CAI_WB_BRUSH_VALID` (reuses `LOC_CAI_PLOT_INTERFACE_VALID/INVALID`). (2) **Status-line speech** — new `WorldBuilderLaunchBar_CAI.lua` (ReplaceUIScript on `WorldBuilderLaunchBar`, added to modinfo `<File>`+`<ReplaceUIScript>`) removes the base `OnSetPlacementStatus` listener, `WrapFunc`s it (orig then `Speak(Controls.StatusLine:GetText())`), re-adds; consecutive-duplicate suppression avoids brush spam. This surfaces the reason text ("Wonder too close", "Requires higher tech") that `PlacementValid` can't. (3) **Locked placement source** — `M` (direct WB-widget binding) toggles `m_wbMarkedPlotId`; place/remove/edit use `WBPlacementSourcePlot()` (mark or cursor) so the cursor can roam to inspect validity without moving where edits land. Tags `LOC_CAI_WB_MARK/MARKED/UNMARKED` in all 6 languages. **PENDING IN-GAME TEST:** InterfaceInfo key speaks Valid/Invalid (+footprint/brush) at cursor per tool; every place/delete speaks its status reason once (not N times under a brush); M locks/unlocks and edits then target the locked tile while the cursor roams.
  - **Place/delete bridge (2026-08-31):** the WB interface widget (`m_caiWorldBuilderWidget` in `WorldInput_CAI.lua`) now carries three **direct input bindings** (per user: WB has no rebindable CAI input actions, so bind keys straight to the widget, not via `SafeActionId`). **Enter → place** and **Delete → remove** call `WBEditCursorPlot(bAdd)`, which replays the exact vanilla mouse sequences over `LuaEvents.WorldInput_WBSelectPlot` — add = `(plot,edge,true,false)`+`(…,false,false)` (LButtonUp pair), remove = `(…,true,true)`+`(…,false,true)` (RButton pair) — so each keypress is one undo-consistent edit through the armed tool's `PlacementFunc`. Brush size honoured automatically (read inside vanilla `OnPlotSelected`). **Ctrl+Enter → edit tile is STUBBED**: raises `LuaEvents.CAIWorldBuilderPlotEditor_Toggle(plotID)` (cursor plot) for the not-yet-built CAI Plot Editor form; key is inert until that exists. Edit can't reuse the placement bridge — vanilla Plot Editor `OnPlotSelected` only runs while its tab is visible, and CAI keeps the Placement tab active. 3 new `LOC_CAI_WB_{PLACE,EDIT_TILE,DELETE}` tags in all 6 languages. **PENDING IN-GAME TEST:** Enter places / Delete removes at the cursor for each tool + brush size; Ctrl+Enter is currently silent (expected).
  - **New file `src/UI/inGame/WorldBuilderPlacement_CAI.lua`** (ReplaceUIScript on context `WorldBuilderPlacement`; `<File>`+`<ReplaceUIScript>` added to modinfo). One pushed `Panel` = a `List` of the 16 tools (advanced order) + a transparent settings container that rebuilds per armed tool. Focus-enter a tool row arms it via vanilla `OnToolSelectMode(ID)`; Enter on a tool row closes. Tab from the list lands on the first setting: item **Type** first (per user), then that tool's extras (brush / rotation / amount+generate+clear / pillaged / owner|player|continent|route pulldown / reveal-all). Rivers & Cliffs have no settings (edge tools).
  - **Selection mechanism (the hard part):** vanilla keeps each grid tool's item list + `SelectedIndex` in file-locals with no setter, so we **`WrapFunc` the global `MakeItem`** to capture each item's `{idx, localized label, select-closure}` as the vanilla grid rebuilds; the CAI type-list rows call the captured closure to arm an item (all vanilla auto-cleanup preserved). `OnPlacementTypeSelected` is wrapped to refresh the settings region. Pulldown-backed tools (continents/routes/players/cities) use a CAI `Dropdown` over **re-derived** GameInfo/PlayerManager lists committing through the real `Controls.*PullDown:SetSelectedIndex`; brush/rotation/generate/clear/reveal/pillaged drive the named vanilla `Controls` via `DoLeftClick` / global `OnRotate*`. Vanilla `OnShutdown` wrapped (not replaced) to keep its LuaEvents cleanup.
  - **Host / open path (reworked):** `WorldInput_CAI` now detects `WorldBuilder.IsActive()` in `InitializeCAIGameView` and, instead of the gameplay `GameView` root, pushes a dedicated **`InterfaceMode` widget** (`CAIWorldBuilderMode`, label `LOC_CAI_WB_MODE` "World Builder") at `PopupPriority.Low`. That widget carries a **Tab** `AddInputBindings` that fires `LuaEvents.CAIWorldBuilderTools_Toggle` to open the panel (the placement context listens). `OnInterfaceChanged` short-circuits and `CheckInput` recognizes the WB widget (re-asserts World input context on focus). The placement context's own input handler now only forwards to `mgr:HandleInput` while the panel is open (Tab handling removed from it per the InterfaceMode-widget design). Panel nav rides `WorldInput_CAI`'s existing world→manager forwarding.
  - **Vanilla WB input handler killed:** new `WorldBuilder_CAI.lua` (ReplaceUIScript on context `WorldBuilder`) `WrapFunc`s the vanilla `InputHandler` to a no-op and re-registers it (`ContextPtr:SetInputHandler`), so WB's own key handling (undo/redo + pause via `DefaultMessageHandler`) no longer consumes input before the CAI manager — all keys flow through `WorldInput_CAI`. (Undo/redo/pause to be re-exposed via CAI later.) `<File>`+`<ReplaceUIScript>` added to modinfo. **Also `WorldBuilderToolsPalette_CAI.lua`** kills the tools-palette input handler the same way — its number/letter tool hotkeys were colliding with typing (a digit typed in the resource-amount box fired on key-up as tool hotkey 3 → switched to Wonders, destroyed the focused edit box mid-type, and left the amount unchanged).
  - **Widget types:** brush size and feature rotation are **Dropdowns** (rotation commit steps `OnRotateRight` the needed count, CAI-tracked `m_rotationIndex`; brush commits via the vanilla brush buttons' `DoLeftClick`). **Item type, continent, and route are Lists** (shared `BuildSelectionList`; focus-enter commits). **Owner / player are always Dropdowns** (`BuildPulldownDropdown`). Pillaged=Checkbox, amount=EditBox, generate/clear/reveal=Buttons.
  - **Owner tool source = cities (vanilla-faithful):** PLACE_TERRAIN_OWNER uses `DeriveCities()`, mirroring vanilla `UpdateCityEntries` — vanilla's `OwnerPullDown` is authoritatively `m_CityEntries` (last writer, and `PlaceOwnership` reads `entry.ID` = a city ID). On a map with no cities the list is empty; that's vanilla behavior, not a bug. (An earlier attempt to fill it with players was wrong and reverted.)
  - **Settings container:** transparent Panel is `WrapAround = false` (Tab past the last field bubbles out) **and has a hidden predicate** (`#m_settingsWidgets == 0`) so tools with no settings (Rivers, Cliffs) don't leave a dead empty focus stop.
  - **Focus sound:** tools/items have no distinct vanilla selection sound (vanilla uses the menu hover sound on mouse-over), so every picker widget (tool rows, list rows, settings fields) is `SetFocusSound("Main_Menu_Mouse_Over")`.
  - **Wonders label fix:** tool row uses `LOC_HUD_CITY_WONDERS` (the placement-mode wonders tag `LOC_WORLDBUILDER_PLACEMENT_MODE_WONDERS` has no localized text — matches the vanilla tools palette).
  - 17 `LOC_CAI_WB_*` tags in all 6 languages; both Lua files `--check` clean, all XML well-formed.
  - **PENDING IN-GAME TEST (open seams):** (1) **Tab** over the WB map opens the panel (WB interface-mode widget is the focused root); (2) tool arming + type/pulldown/brush/rotation/amount/generate/clear/pillaged/reveal read & drive vanilla correctly; (3) re-derived pulldown indices map 1:1 onto the vanilla pulldowns; (4) focus sounds play on navigation. **Placement on the map cursor (place/delete keys, brush/paint-mode, river/cliff edge toggles) is intentionally NOT wired in this cut** — separate follow-up. Note: cursor hex-reading in WB also isn't wired yet (the WB root is a bare interface-mode widget, not the GameView cursor rig). Key facts: WB is a **static data editor** (single `WB_SELECT_PLOT` interface mode; **no gameplay preview — no unit selection/movement, no turns, no combat**; units placed are just data). Firm decisions: (1) **CAI always forces Advanced mode** (`SetWBAdvancedMode(true)`) since Basic/Advanced is pure UI-gating and Advanced is a strict superset — one code path, every field present. (2) **Unify the tile-editing surfaces** (Tools Palette + Placement item grids + Plot Editor form + plot tooltip are all editing one tile) around the map cursor: a tool & item picker (Enter paints the current hex) + an on-demand "edit this tile" form; keep **Map Editor** (map metadata + per-language text) and **Player Editor** (players→cities→districts/buildings tree) as their own pushed siblings under one WB root menu. **Brush/multiplace plan:** brush is just a size param (1/7/19, Terrain/Continents only) + fan-out loop → expose as a cycled announced setting (Enter respects it); add a **paint-mode toggle** (auto-place on cursor move = accessible click-drag, works for all tools); rivers/cliffs are **edge-based** → reuse the 6 directional keys as edge pickers; two-point region fill later. Event sig is `(plotID, edge, lbutton, rbutton)` → Enter=add, separate delete key=remove. **NEXT (after panel test):** single-tile place/delete bridge now DONE (see above); remaining — the Plot Editor form (consumes `CAIWorldBuilderPlotEditor_Toggle`), then brush-size cycling + paint-mode toggle + river/cliff edge toggles, and Player Editor. **Map Editor is now BUILT (2026-09-02, see above).**

- Main menu + load screen doubled-text fixes (2026-08-30). **Main menu VERIFIED in-game; load screen PENDING in-game test.** (1) `MainMenu.lua` `HighlightMainOption` played the selection anim (fading `SelectedLabel` in) but never faded `ButtonLabel` out and had no `focus_leave`, so both labels rendered and doubling accumulated as you navigated. Now mirrors vanilla `ToggleOption`/`DeselectOption`: highlight also `LabelAlphaAnim:Reverse()` (ButtonLabel out), new `ClearMainOption` reverses it, wired to `focus_leave` on each row — skipping the expanded parent (`isSelected`) so an open submenu keeps its highlight. (2) `LoadScreen.lua` doubled the whole Features & Abilities list: `OnLoadScreenContentReady` appends to `Controls.FeaturesStack` without clearing, and it was registered on `LoadScreenContentReady` **twice** — once by the accessibility block's `Events...Add`, once by the trailing `Initialize()` (which runs AFTER the block in a full-replacement front-end file, so the block's pre-emptive `Events...Remove` was a no-op). Fix: block now only `WrapFunc`s the globals and lets `Initialize()` register each once; removed the manual `.Remove`/`.Add` for both `OnLoadScreenContentReady` and `OnLoadGameViewStateDone`. See [[project_frontend_full_replacement_init_order]]. (3) Moved `LoadScreen.lua` shared→`src/UI/frontEnd/` and dropped its InGame `<ImportFiles>` entry — confirmed correct: loading a save bounces through the frontend context before rebuilding in-game, so LoadScreen only ever runs under FrontEnd (InGame import was dead). Both files `lua-language-server --check` clean; changelog updated. **PENDING:** in-game, start a game and confirm the ability list appears once.

- Leader picker button (game setup, scenario setup, staging room). **NEW — needs in-game testing.** Replaced the per-player leader `Dropdown` in `AdvancedSetup.lua` (basic + advanced local + AI), `ScenarioSetup.lua` (basic + AI), and `Multiplayer/StagingRoom.lua` with a `Button` that opens a pushed picker panel via new manager helper `CAIWidgetHelpers_LeaderPicker` (`mgr.WidgetHelpers.CreateLeaderPickerButton(config)` / `RemoveLeaderPickerPanel(panelId)`; installed in `CAIUIScreenManager:Init`, included + registered in modinfo File/ImportFiles ×3, annotated in `ideHelpers.lua`). Button keeps the dropdown's label/tooltip and speaks the selected leader as its value (`getSelectedLabel`). Panel = a real `Panel` holding a sort `Dropdown` (4 opts: Leader/Civilization × A–Z/Z–A, reusing `LOC_CAI_LABEL_LEADER`/`LOC_CAI_LABEL_CIV` + `LOC_CAI_SORT_A_TO_Z`/`_Z_TO_A`, label `LOC_CAI_LABEL_SORT_BY`; Random/RANDOM_POOL entries always pinned to the top of the list regardless of sort) above a `List` (label `LOC_CAI_DIPLOMACY_LEADERS`) of leader `Button` rows. Row activate = `onSelect(value)` then `RemoveFromStack(panelId)`; `F2` per row speaks `LOC_CAI_LEADERDESC_<leaderType>` (leaderType = option `value.Value`, same lookup as DiplomacyActionView, fallback `LOC_CAI_LEADERDESC_NONE`). One shared panelId per screen (only one open at a time); each screen removes it in its own teardown (`ClosePanel`/`OnShow` for Advanced, `CAI_ClosePanel` for Scenario, `CAI_PopPanel` for Staging) so it never lingers. Option builders now carry `leaderName`/`civName`/`leaderType`. Panel order is List then sort `Dropdown`. Sort choice persists across sessions via `CAI.GetConfigValue`/`SetConfigValue` (section `LeaderPicker`, key `SortIndex`). **No new locale tags** (all reused). `LeaderDescStrings_CAI.xml` (all 6 langs) added to the `CAI_LOC_FrontEnd` UpdateText group — it was InGame-only, so F2 in setup previously always said "no description". All 4 changed Lua files `lua-language-server --check` clean. **In-game test pending:** open the picker from each of the 3 screens (basic + advanced/AI slots), confirm button reads label + selected leader + tooltip; list sorts by all 4 options and remembers the choice; `F2` reads a leader description (and NONE for Random); picking a leader closes the panel and updates the button; Delete still removes an AI player from the button (Advanced/Scenario); Escape closes; leaving/closing the screen with the panel open doesn't leave it stranded.

- Wonder movie descriptions (2026-09-09). **DONE, needs in-game test:** press F2 on the "wonder built" and "natural wonder discovered" popups to hear a paragraph describing the movie. `WonderDescStrings_CAI.xml` — `LOC_CAI_WONDERDESC_<BuildingType|FeatureType>` (53 world + 34 natural, 6 languages); F2 binding on the popup dialogs in `WonderBuiltPopup_CAI` / `NaturalWonderPopup_CAI`, keyed from the `kData` the vanilla `ShowPopup` receives (`currentBuildingType` / `TypeName`); `LOC_CAI_WONDERDESC_NONE` fallback. Authoring source `docs/wonder-descriptions.md` (edit there, `scripts/Build-WonderDescriptions.py` regenerates the XML, `--check` verifies). The movies are engine-rendered (no video files; `docs/game-api.md` "Wonder movies"), so the paragraphs were written by Gemini 3.1 Pro from clips cut out of public recordings: `scripts/Extract-WonderVideos.py` + `scripts/wonder-video-sources.json` (OCR-verified ranges) and `scripts/Describe-WonderVideos.py` (needs `OPENROUTER_API_KEY`; about 4 dollars for the set). Prompt decisions: no wonder name unless the sentence needs it, no history (Civilopedia has it), real-world knowledge only to name what is visible, natural wonder reveal flash omitted, setting limited to what the placement rules guarantee (manifest `setting` field) because the land around a wonder is the player's map. Spot-checked five against frames; accurate.
  - **PENDING IN-GAME TEST:** complete a wonder and discover a natural wonder, press F2 on each popup, confirm the paragraph reads and the fallback fires for nothing; check the F2 line appears in the popup's Ctrl+H input help.
- Image accessibility descriptions (leaders, great works, religion icons). **2026-08-27 — ALL CONTENT DONE (leaders 40, great works 60, religion icons 24, each × 6 langs); pending in-game test of all three surfaces.** Source imagery is the SDK art dump under `decompiled/Sid Meier's Civilization VI SDK Assets/Civ6/pantry/Textures` (all `.dds`; convert with Python Pillow — installed). Three new per-language description files (only the 6 folders that ship `cai_text_ui.xml`: en_US/es_ES/fr_FR/pl_PL/pt_BR/zh_Hans_CN), each registered in modinfo `<Files>` + `CAI_LOC_Ingame`:
  - `LeaderDescStrings_CAI.xml` — `LOC_CAI_LEADERDESC_<LEADERTYPE>`; read on demand with **F2** in `DiplomacyActionView_CAI` and `DiplomacyDealView_CAI` (root bindings) and from `CAIWidgetHelpers_LeaderPicker`. Fallback tag `LOC_CAI_LEADERDESC_NONE` + KB label `LOC_CAI_KB_LEADER_DESCRIPTION` live in `cai_text_ui.xml`. **Rewritten 2026-09-07 from the full diplomacy scene** under `docs/leader-descriptions.md` (authoring source; edit there, `scripts/Build-LeaderDescriptions.py` mirrors to XML, `scripts/Lint-LeaderDescriptions.py` lints). 82 entries: name and titles, then setting, person, dress, props, background, light, one long paragraph. Images come from `scripts/Extract-LeaderImages.py` (cut-out from `LeaderFallbackImages.blp` over the `UI_LeaderScenes.blp` layers; Civ VI is installed natively on the Mac, so it runs against `Civ6.app/Contents/Assets`), cross-checked with the fandom loading screens and trivia. English reviewed line by line with the user 2026-09-07 (rules in the doc header). **Translated into the 5 other languages 2026-09-07** (es_ES/fr_FR/pl_PL/pt_BR/zh_Hans_CN, whole-paragraph prose translation using the game's official localized leader and place names from the vanilla text).
  - `GreatworkDescStrings_CAI.xml` — rewritten 2026-09-07 from the real artworks under the rules in `docs/great-work-descriptions.md` (authoring source; edit there, mirror to XML, lint with `scripts/Lint-GreatWorkDescriptions.py`). 126 English entries: 75 paintings/sculptures (`LOC_CAI_GWDESC_GREATWORK_<TYPE>`), writing and music backgrounds (`LOC_CAI_GWDESC_GREATWORKOBJECT_WRITING/_MUSIC`), 25 artifact textures (`LOC_CAI_GWDESC_ARTIFACT_<n>`, n = ((id-1) mod 25)+1 as vanilla picks them), 24 base relics (`LOC_CAI_GWDESC_GREATWORK_RELIC_<n>`). `GreatWorkShowcase_CAI.GetDescriptionTag()` resolves the key from the captured `GreatWorkObjectType`. Game textures were extracted from `UI_GreatWorksArt.blp` (`scripts/Extract-GreatWorksBlp.py <blp> <outdir>`, Python 3 + Pillow): sculptures are painted cut-outs with no setting, paintings sit in gold frames at ~483 px. **Translated into the 5 other languages 2026-09-07** with the game's official work, artist, hero, relic and resource names. Heroes and Legends symbols (12, keyed by type) and the shared hero epic image (`GREATWORK_HERO_EPIC`), plus the three Secret Societies relic icons (`VOIDSINGER_RELIC_1..3` for relics 25–32/33–40/41–48) are described too, as are the 28 Monopolies and Corporations resource icons (`PRODUCT_<RESOURCE>`, shared by a resource's five products); 170 entries total, covering every GreatWorkType in the install. Key coverage verified against the install: every GreatWorkType resolves to a key.
  - `ReligionIconDescStrings_CAI.xml` — `LOC_CAI_RELICON_<ReligionType>`; **COMPLETE (24 symbols × 6 langs)**. Appended to the religion icon-picker dropdown + summary in `ReligionScreen_CAI` (helper `AppendReligionIconDesc`). Atlas `Religions100.dds` cell index = religion table `Index` (Pantheon=0 blank): 1–12 named (Buddhism dharma wheel … Zoroastrianism faravahar), 13–24 = `RELIGION_CUSTOM_1..12` (crab, bull's head, elephant, lion, two fish, scorpion, cobra, tree, waves, peacock, butterfly, turtle) — custom religions share the name "Custom Religion" so the symbol is the only differentiator. Changelog entry added.
  - All 4 Lua files `lua-language-server --check` clean; all XML well-formed; modinfo valid. Missing description tags Locale.Lookup back to their tag, so code treats `result == tag` as "no description" (graceful).

- Diplomacy ribbon panel + table view (`DiplomacyRibbon_CAI.lua`). **NEW — needs in-game testing.** Rewrote the ribbon popup (opened via `UI_DiplomacyRibbonOpenList`/`Ctrl+P` and `LuaEvents.CAILaunchBarOpen_DiploRibbonList`, now `OpenPanel`) from a bare pushed `List` into a `Panel` holding, in Tab order: sort `Dropdown` (`HiddenPredicate` = not list view; label `LOC_CAI_DIPLO_RIBBON_ORDER_BY`), `DataTable` (hidden off table view), `List` (hidden off list view), World Congress `Button` (hidden when `HasCongressButton()` false), switch-view `Button`. `m_viewMode` defaults `table` (`Alt+1`=table, `Alt+2`=list, panel-level bindings + switch button; not persisted). Panel auto-closes on `focus_leave` and Escape (`ClosePanel`=`RemoveFromStack`, `m_pushed` guard). Table rows = natural order (local player first, then met majors by met-turn, via `BuildNaturalPlayers`); `RowLabelGetter`/leader column = `GetLeaderLabel(pid, local, false)` (identity only, active-turn appended, relationship excluded since it's its own column); `row_activate` = vanilla leader click (`GetUILeadersByID()[pid].SelectButton:DoLeftClick`). Columns (capability-gated exactly like the old `GetLeaderTooltip`): leader (A–Z), relationship (`RelationshipSortKey`: `FOREIGN_REL_RANK` map, alliance boosted to `7+level` so it always sorts friendliest; desc = `LOC_CAI_SORT_FRIENDLIEST_FIRST`), capital, then score/favor/military/science/culture/gold/faith (each `AddValueColumn`, lowest/highest-first). Shared sort (`m_sortColumn`/`m_sortAscending`): table header `sort_changed` → `SyncSortDropdown` + `RebuildListView`; dropdown `value_changed` → `SetDefaultSort`+`Rebuild`+`RebuildListView`; the list applies the same sort via `GetOrderedListPlayers` (stable). `CAI_RebuildViews` (fired on wrapped `UpdateLeaders` when pushed) captures focus on the panel, rebuilds columns/options/list, restores. **Pirates scenario stays list-only** (`SCENARIO_LIST_ONLY`; no table/dropdown/switch, view forced `list`) since its custom score categories don't map to the columns; Civ Royale uses the full table (columns adapt via capabilities). New header tags `LOC_CAI_DIPLO_RIBBON_ORDER_BY` + `LOC_CAI_DIPLO_RIBBON_COL_{CAPITAL,SCORE,FAVOR,MILITARY,SCIENCE,CULTURE,GOLD,FAITH}` added to all 6 `cai_text_ui.xml` (leader/relationship/switch/natural-sort/sort-description headers reuse existing tags). `lua-language-server --check` clean; all 6 XML well-formed.
  **In-game test pending:** open with `Ctrl+P`; verify table is default and reads leader + columns, `Alt+1`/`Alt+2` + switch button toggle views, sort dropdown appears first in list view and re-sorts both views, relationship sorts friendliest-first with alliances on top, capability-gated score/yield columns match the top panel, Enter on a leader opens diplomacy, World Congress button works in both views, live updates (turn changes) don't lose focus, Escape and focus-leave close.

- Launch bar screen-launcher list (`LaunchBar_CAI.lua`). **NEW — needs in-game testing.** `Shift+Tab` (`UI_CAIOpenLaunchBar`, added to `hotkey_config_CAI.xml` gestures) toggles a `Panel`+`List` of every screen-opening action, pushed through the manager. Central descriptor registry lives in `LaunchBar_CAI` and reaches beyond the launch bar (choosers, trees, reports, congress, partial screens, CAI tool panels). Each descriptor: `name` (action LOC), `action` (CAI action id → key binding via `Input.GetGestureDisplayString`), `gate()` (expansion/capability filter), `reason()` (resolved unavailable string → `DisabledPredicate`, mirrors each hotkey path), `dynamic()` (research/civic line, congress status, governor titles, era, government free-policy, pantheon attention), `open()`. Row label = name + binding; tooltip = reason-first-when-disabled else dynamic-first, then official tooltip (live launch-bar control tooltip → vanilla LOC → CAI desc, all via `SafeLookup`). Opens fire vanilla LuaEvents (`OnOpenResearch`/`OnOpenCulture`/… locals; `WorldTracker_OpenChooseResearch/Civic`; `TopPanel_OpenReportsScreen`/`ReportsList_Open*`/`GlobalReportsList_OpenResources`; `PartialScreenHooks_Open*`; `CongressButton_Resume/ShowResults`; `OpenCivilopedia`; `CAIMinimapLensListToggle`/`CAIMinimapMapPinListToggle`). Tool panels lacking a fireable open got a one-line `LuaEvents.CAILaunchBarOpen_*` hook (NotificationPanel→`OpenNotificationCenter`, TutorialGoals→`OpenGoalsList`, DiplomacyRibbon→`OpenList`, ChatPanel→`CAI_OpenChatFromLaunchBar`, MinimapPanel→`OpenMapSearchFromLaunchBar`; Remove-then-Add + shutdown Remove). Extensibility: `LuaEvents.CAILaunchBar_RegisterAction`/`_UnregisterAction` (+ `_RequestRegistrations` fired on Subscribe) let other mods (e.g. Quick Deals) contribute entries. **Own input handler** (`OnCAILaunchBarInputHandler`, `ContextPtr:SetInputHandler`) mirrors NotificationPanel so navigation works even when the tutorial suppresses WorldInput's manager forwarding. Advisor text (`tutorial_advisor_text_CAI.sql`, all 6 locales) updated so 9 screen-open prompts (Tech/Civics tree, Government, Religion, Great People, World Rankings, Tutorial Goals, Diplo ribbon, Civilopedia) mention the launch bar first, key binding as alternate. New tags `LOC_CAI_LAUNCH_BAR`, `LOC_CAI_OPEN_LAUNCH_BAR(+_TOOLTIP)`, `LOC_CAI_LAUNCH_ATTENTION_PANTHEON`, `LOC_CAI_LAUNCH_GOVERNOR_TITLES` in all 6 `cai_text_ui.xml`. `lua-language-server --check` clean on all 6 edited Lua files; all XML well-formed; SQL quotes balanced. **Revised per user feedback (2026-08-26):** dropped the research/civics *chooser* entries; pushes a bare `List` (no Panel wrapper — Escape binding + label on the List, `mgr:Push(m_launchList)`); notification entry's `dynamic` = `NotificationCountLine` ("No notifications" / "Notifications: N" via `NotificationManager.GetList`+`Find`); reports keeps only the main entry (dropped resources/city-status/gossip tab entries; global resources kept); official tooltips run through `StripTooltipHeader`, which drops the redundant screen-name first line (splits on first `[NEWLINE]`/`\n`) so e.g. Government reads "…Control+P, Button, View or manage…" instead of repeating "Government". Row **labels are now the screen title + binding** (no "Open " prefix), and the whole list is **sorted alphabetically by title** (`CollectEntries` combines built-ins + externals then `table.sort` by `EntryTitle():lower()`). Each entry carries an explicit `title` LOC tag (vanilla or CAI: `LOC_TECH_TREE_HEADER`, `LOC_CIVICS_TREE_HEADER`, `LOC_UI_RELIGION_TITLE`, `LOC_GREAT_PEOPLE_TITLE`, `LOC_GREAT_WORKS_SCREEN_TITLE`, `LOC_WORLD_RANKINGS_TITLE`, `LOC_TRADE_OVERVIEW_TITLE`, `LOC_GLOBAL_RESOURCES_TITLE`, `LOC_CAI_DIPLO_RIBBON_CONGRESS`, `LOC_CAI_DIPLO_RIBBON_LABEL`, `LOC_CAI_TUTORIAL_NAME_CIVILOPEDIA`, `LOC_CAI_NOTIFICATION_CENTER`, `LOC_CAI_TUTORIAL_GOALS`, `LOC_CAI_MINIMAP_LENS_LIST`, `LOC_HUD_MAP_PIN_LIST`, `LOC_HUD_MAP_SEARCH`, `LOC_CAI_CHAT_PANEL_TITLE`); the launch-bar/partial-screen entries (government, governors, historic, climate, city-states, espionage, era, reports) instead derive the title from the vanilla tooltip's header line via `OfficialHeader` (reports got `vanillaTip = LOC_PARTIALSCREEN_REPORTS_TOOLTIP`), with the CAI action name as a last-resort fallback. Focus sound (`Main_Menu_Mouse_Over`) is set per row. New tags `LOC_CAI_LAUNCH_NO_NOTIFICATIONS`, `LOC_CAI_LAUNCH_NOTIFICATION_COUNT` in all 6 `cai_text_ui.xml`. **Revised (2026-08-26 pt.2 — needs in-game testing):** (1) **Attention-first ordering** — entries carry an optional `attention()` predicate; `CollectEntries` now evaluates `attention`+`titleKey` once per entry then sorts attention-true first, alphabetical within each group. Built-in attention predicates: government (free policy change via `GovernmentAttention`), religion (pantheon via `ReligionAttention`), governors (unspent titles via `GovernorsAttention`), notification_center (`HasNotifications`, shared `LiveNotificationCount` helper). `LiveNotificationCount` counts only rail notifications via `IsRailNotification` (non-end-turn-blocking + `IsIconDisplayable` + `IsVisibleInUI`, mirroring `NotificationPanel`'s rail-add test) so end-turn blockers — which vanilla routes to the ActionPanel, not the rail — are excluded from both the count and the attention ranking. (2) `EntryTitle` now resolves `entry.title` through `ResolveTitleTag` so a `title` may be a function returning a LOC tag (for live screen names). (3) **Civ Royale global ability → launch bar** registered from `WorldTracker_CAI.lua` (only under `RULESET_SCENARIO_CIV_ROYALE`): `title`=live ability `Name`, `action=UI_OpenWorldCrisisTracker` (shows the tracker hotkey), `gate=PlayerHasGlobalAbility`, `attention`=ready (no `DisabledToolTip`), `reason`=disabled cooldown tooltip (also disables the row), `dynamic`=available line + charges (`LOC_CIV_ROYALE_GLOBAL_ABILITY_AVAILABLE`/`_CHARGES`), `officialTooltip`=ability `ToolTip`, `open=ActivateRoyaleGlobalAbility`. Registers immediately + on `CAILaunchBar_RequestRegistrations`. (4) **Quick Deals → launch bar** registered from `qd_dealpopup_CAI.lua` (context only loads when QD active): `title=LOC_QD_NAME`, `action=OpenQDPopup`, `desc=LOC_CAI_QD_LAUNCH_TOOLTIP`, `reason`=not-turn-active → `LOC_CAI_QD_NOT_YOUR_TURN`, `open`=`LuaEvents.QD_ToggleDealPopup()`. Same register-now+on-request pattern. New tags `LOC_CAI_QD_LAUNCH_TOOLTIP`, `LOC_CAI_QD_NOT_YOUR_TURN` in all 6 `cai_text_ui.xml`. `lua-language-server --check` clean on the 3 edited Lua files; all 6 XML well-formed.
  **Revised (2026-08-27 — needs in-game testing):** (1) **Attention now mirrors the ActionPanel turn blockers.** New shared helper `HasEndTurnBlocking(...types)` reads `NotificationManager.GetAllEndTurnBlocking(playerID)` and returns true if any requested `EndTurnBlockingTypes` is active. Wired as `attention` on: tech_tree (`_RESEARCH`), civics_tree (`_CIVIC`), government (existing free-policy `GovernmentAttention` OR `_CONSIDER_GOVERNMENT_CHANGE`/`_FILL_CIVIC_SLOT`), religion (existing pantheon `ReligionAttention` OR `_PANTHEON`/`_RELIGION`/`_BELIEF`), great_people (`_CLAIM_GREAT_PERSON` — previously had NO attention), city_states (`_GIVE_INFLUENCE_TOKEN`). world_congress attention = `CongressHasButton() and CongressInSession()` (voting is turn-segment driven, not an end-turn blocker). **Dynamic tooltip lines** now surface the blocker's own vanilla sentence: `ENDTURN_BLOCKING_TOOLTIP` maps each type → its `LOC_ACTION_PANEL_*_TOOLTIP` tag (e.g. claim-great-person → "You have earned a Great Person, and need to choose whether to accept them or pass on them.", already localized), `BlockingLine(...types)` returns the first active one, `JoinLines(...)` joins non-empty fragments. great_people/city_states (no prior dynamic) now show the blocker line; tech_tree/civics_tree/government/religion prepend it above their existing research/civic/free-policy/pantheon line. (2) **Tutorial always-receive-input** — added `m_tutorialAlwaysReceiveInput` + `SetTutorialAlwaysReceiveInput(enabled)` (registers/removes `ContextPtr` via `UITutorialManager:Add/RemoveControlToAlwaysReceiveInput`, gated on `IsTutorialRunning()`), called true in `PushLaunchBar` (before `mgr:Push`) and false in `PopLaunchBar`, exactly like NotificationPanel_CAI. `lua-language-server --check` clean.
  **In-game test pending:** open with `Shift+Tab`; verify each entry's label+binding, dynamic-first tooltips (incl. notification count), that the screen name no longer repeats in tooltips, disabled reasons, that each `open()` actually raises the right screen (esp. cross-context LuaEvents — reports/congress/partial screens/era/tool panels), Escape closes, and that navigation works during a tutorial advisor step. **Attention ordering:** with a pending great-person / research / civic / belief / religion / envoy / government-change choice (or congress in session), the matching screen sorts to the top of the list. **Tutorial:** the launch bar still navigates during a tutorial advisor step that suppresses WorldInput.

- Diplomacy leader table view (`DiplomacyActionView_CAI.lua`). **DONE — tested in-game and working (2026-08-25).** Added a CityStates-style dual view to the leader screen: a `DataTable` (one row per met major except the local player) alongside the existing leaders `Tree`. `m_viewMode` (`table` default / `tree`) persisted via `CAI.*ConfigValue` section `DiplomacyActionView` key `ViewMode`; `Alt+1`=table, `Alt+2`=tree (bindings on `overviewPanel`), plus a switch-view button. Overview-panel child order (Tab order; hidden widgets skipped): table view = localLeader StaticText, table, actionsList, switchView; tree view = treeSort Dropdown, gossipFilter Dropdown, leadersTree, actionsList, switchView. The local player sits in the separate `localLeader` widget (label `BuildSelfLeaderTooltip`; focus_enter selects self) above the table; the tree still shows self as its first node. Focus rules: table view lands on the selected leader's row (`GetTableRowFocusKey`) or localLeader if self/none; tree view always lands on the tree. Columns (all live getters; advisor text last in relationship/access tooltips): leader, relationship, access, agendas, gossip (new-item count), foreign relationships, agreements, then gated — alliance (exp1), grievance change/against-you/against-them (exp2 + WC era), secret society (capability), emergency participating/targeting (exp1, each shown only if any row has entries). `EnsureTableStructure` rebuilds the table only when the leader set or column signature changes (cells are live, so data changes need no rebuild); `EnsureLeaderViews` runs it before `EnsureLeadersTreeStructure`. Shared sort (`m_sortColumn`/`m_sortAscending`) drives both header `CycleSort` (table) and the tree "Order by" `Dropdown`; `GetSortedTreeLeaderIDs` keeps self first then sorts the rest. Drill-downs (pushed panels, Escape=RemoveFromStack, focus returns to the cell): `row_activate` on the gossip column opens a gossip `List` + group-filter `Dropdown` (group-only, reimplemented locally, `LOC_HUD_REPORTS_FILTER_<GroupType>` labels); on any grievance column opens the grievance log `List`. Tree view adds the same gossip group filter (filters `AddGossipChildren` via `m_gossipGroupFilter`, rebuilds the selected leader on change). Also **added agreements to the tree's relationship section** (`GetAgreementsList`) — they were previously only in the dead `AddOverviewChildren`. Refactor: the intel builders (`GetRelationshipData`/`BuildRelationshipReasonLines`/`BuildRelationshipAdvisorLines`/`GetAccessLevelName`/`BuildActiveVisibilitySourceLines`/`BuildInformationSharedLines`/`BuildAccessAdvisorLines`/`BuildAccessSectionTooltip`/`BuildGossipSectionTooltip`) now take an optional `targetID` (default `ms_SelectedPlayerID`) so table cells compute per-row; tree call sites unchanged. Emergency data matches vanilla's minimal diplomacy Emergency tab (name/turns/desc/goal/progress); richer crisis detail stays in WorldCrisisPopup. New tags `LOC_CAI_DIPLO_COLUMN_*` (grievance change / against you / against them / emergency participating / targeting), `LOC_CAI_DIPLOMACY_AGREEMENTS_NONE`, `LOC_CAI_DIPLOMACY_GOSSIP_FILTER(+_ALL)`, `LOC_CAI_DIPLOMACY_GRIEVANCE_LOG_EMPTY`, `LOC_CAI_SORT_FRIENDLIEST_FIRST`/`_MOST_HOSTILE_FIRST` — added to the 5 localized languages (en_US/es_ES/fr_FR/pl_PL/pt_BR/zh_Hans_CN; the 6 folders without a `cai_text_ui.xml` are intentionally skipped per user), full tag parity verified, terms anchored to Vanilla_<lang>. Header columns reuse already-localized vanilla/CAI tags. `lua-language-server --check` clean; all 6 XML well-formed.
  - REFINED (2026-08-25, user feedback): (a) **Government column** added before agendas (`CellGovernment`, header `LOC_DIPLOMACY_OVERVIEW_GOVERNMENT`, sort A–Z). (b) **Label/tooltip de-dup** — columns without a numeric value now put full info in the label and have NO tooltip: agendas (label = full name: description via `TooltipAgendas`), foreign relationships, agreements; grievances-against-you/them dropped their tooltips too (value only; log via the button). (c) **Gossip tooltip is last-ten-turns only** (`GossipRecentTooltip`); the older gossip stays in the drill-down panel's full log. (d) **Drill-down cells are Buttons, not table cells** — extended `CAIWidget_DataTable` with an optional `column.onActivate(row)`: such data cells get `Role="Button"`, announce the Button role (`SpeechSettings.Role=true`), and own Enter/Space; `DataTableCellWidget:Activate` runs `onActivate` before falling back to row activate. The gossip + 3 grievance columns use it (open the gossip panel / grievance log); the table's old `row_activate` handler was removed (leader selection stays on `row_focus_enter`). Back-compat: `onActivate` is optional, existing tables (CityStates row_activate Look-At, etc.) unaffected. (e) **Alliance header** now `LOC_CAI_DIPLO_COLUMN_ALLIANCE` ("Alliance") instead of vanilla `LOC_DIPLOACTION_INTEL_REPORT_ALLIANCE` ("Intel Report: Alliance"). (f) **Tree sort dropdown label** now the generic `LOC_CAI_REPORTS_SORT_BY` ("Sort by") instead of the city-states "Order by". (g) **Action list stays in the tab chain** — removed its `IsSelfSelected` HiddenPredicate and `RebuildActionsList` now shows a read-only "No actions" for self, so Shift+Tab from the switch-view button reliably reaches it. New tag `LOC_CAI_DIPLO_COLUMN_ALLIANCE` added to all 6 localized languages (parity re-verified).
  - REFINED (2026-08-25 round 2, user bug reports): (a) **Gossip log panel wraps** — the fix was NOT setting the list's wrap (lists already default `WrapAround=true`); it was that the panel was `Transparent` with `SetWrapAround(false)`, which flattened the filter + list items into one directional flow governed by the panel's (false) wrap so the list never wrapped on its own. Made the gossip panel a normal (non-Transparent) container with default wrap; the list is now its own wrapping nav boundary. (b) **Gossip tooltip prepends the "last ten turns" header** (`LOC_DIPLOMACY_INTEL_LAST_TEN_TURNS`) before the entries, matching the tree. (c) **Tree-mode open focuses the specific leader again** — `PushRootFocusingSelected` and `FocusLeaderView` now target `diplo:leader:<selected>` in tree mode (was hardcoded to the tree, which defaulted to the local node). (d) **Local-leader `focus_leave`** selects the last leader focused in the table (`m_lastTableLeaderID`, tracked in `row_focus_enter`; local player if none) with the `selectingFromRow` guard so it never steals focus — so leaving the local leader returns selection to the leader you were on. (e) **Every gossip line carries its turn** — a shared `FormatGossipLine(targetID, turn, text, isNew)` reuses the existing reports tag `LOC_CAI_REPORTS_GOSSIP_ENTRY` ("Turn {1}, {2_Leader}: {3}") for the tree gossip section, the gossip tooltip, and the drill-down log (no new tag — an earlier `LOC_CAI_DIPLOMACY_GOSSIP_LOG_ENTRY` was added by mistake and removed from all 6 languages). Also confirmed vanilla shows no remaining-turns on agreements (icon + name-only tooltip via `AddAgreementEntry`), so the labels-only list is correct. **VERIFIED in-game (2026-08-25): all views, sortable/gated columns, Alt+1/Alt+2 + switch button with persistence, local-leader select/hand-back, header↔tree sort sync, gossip drill-down + group filter + wrap, grievance log, turns on every gossip line, tree agreements, and vanilla conversation/cinema/deal + Escape flows all work.**

- Quick Deals (QD, wltk, UUID `5aceed03-8639-4a81-8cbf-03f54d543502`) accessibility. QD adds a launch-bar popup + `OpenQDPopup` action opening a 3-tab screen (Sale / Purchase / Exchange); each tab silently opens diplomacy sessions with every met AI (`qd_offerautomator.lua`, async via `Events.DiplomacyStatement`) and shows each AI's best gold offer to rank who pays most. Entirely custom UI, zero accessibility. QD source vendored at `decompiled/mods/QuickDeals/`. **PLAN OF RECORD** (mirrors BRS/BTS mod-integration pattern): CAI wins/replaces the four QD contexts (`qd_dealpopup` + `qd_popuptab_sale/purchase/exchange`) with `_CAI` files that include the QD base (QD exports all its ui files via `ImportFiles`) and rebuild on CAI widgets. Shell = `TabControl` (3 pages) + notification `Checkbox`. Each tab's AI-offer list = a **DataTable** (leader / one-time gold / 30-turn gold / per-unit / items-status; sortable gold headers) with a **dual List view** toggled `Alt+1` (table) / `Alt+2` (list) + a sort `Dropdown` for the list (Reports city-status pattern). Row-focused extra actions (increase/decrease 30-turn gold, convert-all) are Buttons **after** the table acting on the focused row (CityStates pattern), tracked via `row_focus_enter`. `row_activate` = Accept (pure gold) or View Deal (`QD_RequestDealScreen`, non-gold). Sale tab also has: inventory `Tree` grouped by category (leaf activate = add one; **Ctrl+Enter** = bulk add ten) and a "your offer" `List` (activate = inline `EditBox` amount edit; **Delete** = remove). Purchase/Exchange add type/subtype/gold-type `Dropdown` filters. Async: entering a tab / changing a filter announces "Checking offers…"; on `QD_EndAIOfferFetch/AIGoldExchange/MultiTurnGoldUpdate` wrap the populate fn and `Rebuild()` inside `CaptureFocusKey`/`RestoreFocus` on the AI row key.
  - **TESTED & WORKING in-game (2026-08-24), QD+CAI active — all three tabs, reopen, filters, async feedback, and exchange math verified.** Phase A (compat): (1) `IsQuickDealsActive()` in `caiUtils.lua`. (2) `ModCompatibilityConfig_CAI.sql` rebinds `OpenQDPopup` bare `D`→**Ctrl+D** (`LOC_OPTIONS_KEY_CONTROL+LOC_OPTIONS_KEY_D`; QD InsertOrIgnore so UPDATE lands). (3) `DiplomacyActionView_CAI.lua` base-include branches to `include("diplomacyactionview_qd")` when QD active (QD exports it; pulls the right vanilla variant + installs surprise-session wraps) so CAI wins the context without dropping QD's cleanup.
    - Phases B–D (the popup): four new files, all `lua-language-server --check` clean, modinfo + all XML well-formed. **Architecture — shell owns all widgets, tabs are providers.** `qd_dealpopup_CAI.lua` (shell, replaces context `qd_dealpopup`) includes qd_utils+qd_dealpopup, builds a Panel = notification `Checkbox` + `TabControl` (3 pages), and renders every widget from a live model published by the tab contexts into `ExposedMembers.CAIQuickDeals[tabKey]`. The three providers `qd_popuptab_{sale,purchase,exchange}_CAI.lua` (replace the QD tab contexts) each include their QD base, `WrapFunc` its populate fns (`PopulateAIOfferPanel`/`PopulateAIOffer`/`UpdateFetchStatus`, plus sale's `PopulatePlayerAvailablePanel`/`PopulatePlayerDealPanel`), and publish records + **action closures** (accept/viewDeal/incMTG/decMTG/convertAll/editAmount, addOne/addTen, remove/edit) that run in their home context and drive QD globals. Cross-context bridge = ExposedMembers + `LuaEvents.CAIQD_Changed(tabKey, kind)`; closures/refs pass across contexts (same trick QD uses threading control instances through its own LuaEvents).
    - Per user decisions: **Ctrl+Enter** bulk-add (inventory tree leaf; `Enter`=add one), **Delete** removes staged offer item (`Enter`=inline amount `EditBox`), row-focused extra actions are Buttons AFTER the offer table acting on the last-focused row (CityStates pattern, tracked via `row_focus_enter`/list item `focus_enter`, `m_focusedRow[tab]`), dual view = DataTable + List with a sort `Dropdown`, **Alt+1**=table / **Alt+2**=list (shared `m_viewMode`, persisted via `CAI.*ConfigValue` key `QuickDealsViewMode`; Reports pattern). Offer table columns: leader / one-time gold / 30-turn gold / per-unit / items+status (sortable gold headers). `row_activate`=accept (pure gold) or view deal (`QD_RequestDealScreen`, non-gold). Async: entering a tab / changing a filter → QD fetch → wrapped `UpdateFetchStatus` fires "fetch" → shell speaks LOC_CAI_QD_CHECKING / LOC_QD_NO_AVAILABLE_DEAL; wrapped `PopulateAIOffer` captures offers → "offers" → shell `Rebuild()`s inside CaptureFocusKey/RestoreFocus. Purchase filter = one `Dropdown` folding item-type + strategic/great-work subtype (drives `QD_StartAIOfferFetch` directly with CAI-owned filter state, not QD base locals). Exchange filter = gold-type `Dropdown` (drives `QD_StartAIGoldExchange`); exchange rows read the AI's gold + ratio, itemsText = "You give …", editable amount + inc/dec via `OnUpdateMultiTurnGold(…, isPlayerMTG, ratio)`.
    - Registration: 4 `<File>` + 4 `<ReplaceUIScript>` (LoadOrder 999999999, contexts `qd_dealpopup`/`qd_popuptab_sale`/`_purchase`/`_exchange`) in modinfo — when QD absent these contexts don't exist so the replacements never load. New `LOC_CAI_QD_*` tags added to **en_US only** (reuses QD's own `LOC_QD_*` + vanilla tags where they fit); other 4 shipped langs (fr_FR/pl_PL/pt_BR/zh_Hans_CN) fall back to English — **translation is the one remaining follow-up** (localize-language skill).
    - SECOND-PASS FIXES (2026-08-24, from decompiled vs impl review, TESTED & WORKING): (a) **Reopen bug fixed** — `PopPanel` now nulls `m_panel`/`m_tabs`/`m_pages`/`m_editWidget` because `mgr:RemoveFromStack` *destroys* the panel widget tree; the old code left `m_panel` set so `BuildPanel`'s `if m_panel` guard skipped rebuild and Ctrl+D re-push shoved a dead widget (popup never reappeared after first close). (b) **AI treasury balance** now shown on Sale+Exchange (vanilla `GetTreasury():GetGoldBalance()`; not Purchase, matching vanilla) as a `balance` column + list line. (c) **Total value** column (`OTG + MTG*30`) added to Sale+Purchase (excluded on Exchange). (d) **Tab-specific column headers** (AI pays / You pay / AI gives; You sell / You receive / You give; Gold each vs Gold ratio per turn) so each number is self-describing — `BuildOfferColumns(tabKey)`/`BuildSortOptions(tabKey)`, `OfferLineText(r, tabKey)`. (e) **Filter desync fixed** (Purchase itemtype, Exchange goldtype): base's `OnEndAIOfferAccept` is a `LuaEvents` callback capturing the original fn, so wrapping the global was dead code — providers now `LuaEvents.QD_EndAIOfferAccept.Remove(OnEndAIOfferAccept)` + add a CAI listener that refetches with CAI-owned filter state; same for tab re-entry via a `QD_PopupShowTab` listener (guarded to non-default, pending-job queue supersedes base fetch). (f) **Sale item-type preview filter** added (`FetchRecommended` drives `GetRecommendedItems` + `QD_StartAIOfferFetch`; base ItemTypeFilter locals unreachable). (g) **Button tooltips** on inc/dec/convert/edit (QD leaves arrows untooltipped) explaining the 21:1 gold conversion (Sale/Purchase) vs amount stepping (Exchange); **view-toggle button moved after** the inc/dec/convert row actions. New `LOC_CAI_QD_COL_*`/`_TT_*`/`_OFFER_BALANCE`/`_TOTAL`/`_RATIO`/`_FILTER_RECOMMENDED_TYPE` tags (en_US only, same follow-up). All 4 files pass `lua-language-server --check`; en_US XML well-formed.
    - THIRD-PASS FIXES (2026-08-24, from user in-game feedback, TESTED & WORKING): (a) **Fetch feedback is result-only** — shell no longer speaks the interim `UpdateFetchStatus` placeholder states ("checking"/"no offers" that fired reset-then-fetch and clashed with the tab-name announcement on tab switch). Providers now fire a new `"fetchresult"` from the true completion events (`QD_EndAIOfferFetch` for sale/purchase, `QD_EndAIGoldExchange` for exchange, guarded by `ContextPtr:IsHidden`), and the shell announces the live offer count ("N offers available" / "No offers available"). (b) **Button-press feedback** — `"offerrow"` (MTG inc/dec/convert completion via `QD_EndMultiTurnGoldUpdate`) now speaks the focused row's new gold via `SpeakLines` instead of `mgr:Refocus()` (which re-read the button, not the row). (c) **Gold-adjust buttons stay visible when a row is focused** and use `DisabledPredicate` (was `HiddenPredicate`) for per-row availability; convert-all still hides on Exchange, edit-amount hides off Exchange (tab applicability). (d) **Notification checkbox moved to last child, after the TabControl.** (e) **Tooltips clarified** — inc/dec now note the amount is capped by what the AI can pay (21:1 is the rate, not a guarantee; user saw OTG land at 11 not 21). (f) **"Gold each" column renamed "Gold per unit"** (`LOC_CAI_QD_COL_GOLD_EACH`, `LOC_CAI_QD_OFFER_PER_UNIT`); it is the AI gold total ÷ units traded (meaningful for multi-copy strategics/favor; equals the total for single items). New tags `LOC_CAI_QD_OFFERS_AVAILABLE`/`_ONE`/`LOC_CAI_QD_NO_OFFERS` (en_US). `LOC_CAI_QD_CHECKING` now unused. All 4 files `--check` clean; en_US XML well-formed.
    - FOURTH-PASS FIXES (2026-08-24, user feedback, TESTED & WORKING): (a) **Exchange inc/dec relabeled** — the arrows step the amount of gold exchanged (a per-turn value), not a "gold for 30 turns" conversion; Exchange now uses `LOC_CAI_QD_ACTION_INCREASE_AMOUNT`/`_DECREASE_AMOUNT` ("Increase/Decrease exchange amount") while Sale/Purchase keep the MTG-conversion labels. (b) **List-view rows**: label is now just the leader; all figures moved to the row tooltip joined by `[NEWLINE]` (`OfferDetailLines`, replaces `OfferLineText`). (c) **Sort dropdowns default to Natural** (`SetSelectedIndex(1, true)`), which is QD's own per-unit ordering, instead of starting blank. (d) **Items column is now sortable** by offer item count (`itemCount = #offer.OfferedItems` on Sale/Purchase records; `COLUMN_FIELD.items = "itemCount"`); left unsortable on Exchange (single gold item). (e) **Sale preview dropdown resets to default when the offer empties** — wrapped `UpdateProposedWorkingDeal`; on `hasItems==false` it resets `m_recType`/`m_recSub` + `QD.sale.filters[1].currentValue` and fires `"resetfilter"`, which the shell applies to the stored filter dropdown (`m_pages[tab].filters`), fixing the desync where base re-previews Luxury while the dropdown showed a stale type. All 4 files `--check` clean; en_US XML well-formed.
    - VERIFIED in-game (2026-08-24): cross-context load + provider closures driving QD state, Escape close + inline editor Escape, async focus survival across rebuilds, Open/re-open (Ctrl+D) push, exchange math (ratio/amount/inc/dec/edit), inventory luxury split + Ctrl+Enter bulk, notification checkbox multiplayer-disable, and QD-disabled regression (DiplomacyActionView + no stray contexts). **Localization DONE (2026-08-24):** all 58 `LOC_CAI_QD_*` tags translated into fr_FR/pl_PL/pt_BR/zh_Hans_CN (`<Replace>`, terms anchored to Vanilla_<lang> + existing CAI wording); `verify_language.py` passes for all four. **es_ES added 2026-08-24** (pulled from a PR): it was missing 76 `cai_text_ui.xml` tags (the 58 QD + 18 reports/government/sort tags, e.g. `LOC_CAI_GOVERNMENT_POLICY_SLOT_HEADER`, `LOC_CAI_REPORTS_COL_*`, `LOC_CAI_SORT_INACTIVE_FIRST`) — all translated and `verify_language.py` passes. **All 5 shipped languages (fr_FR/pl_PL/pt_BR/zh_Hans_CN/es_ES) now at full 2434-tag parity with en_US, zero missing/extra.** Only follow-up: in-game read-through of QD per language.

- Better Report Screen (BRS) accessibility support. Build spec: `docs/brsSupport.md` (finalized, all §8 decisions resolved). Plan: when BRS (Infixo, UUID `6f2888d4-79dc-415f-a8ff-f9d81d7afb53`) is active, CAI's ReportScreen builds accessibility on BRS's data model instead of vanilla, via a dispatcher branch in `ReportScreen_CAI.lua` handing off to a new `ReportScreen_BetterReportsScreen_CAI.lua` variant (mirrors the BetterTradeScreen pattern). Detection helper `IsBetterReportScreenActive()` already in `caiUtils.lua`. Nine tabs (Yields, Resources, City Status, Gossip, Deals, Units, Policy, Minor, Cities2). Dispatcher branch flips on only after all nine tabs are built (§10). BRS source is vendored under `decompiled/mods/BetterReportsScreen/` for reference.
  - **FULL FIRST PASS BUILT (all 9 tabs), PENDING IN-GAME TEST.** Architecture (per user decision): shared builders + two variants. `ReportScreen_CAI.lua` now holds the shared builder infrastructure + a dispatcher; it includes the base game report, then includes `ReportScreen_Vanilla_CAI.lua` (base-game path, unchanged behavior) or `ReportScreen_BetterReportsScreen_CAI.lua` (BRS path) via `if IsBetterReportScreenActive() and ViewDealsPage ~= nil`. Shared file exposes as globals: `RefreshCAIData` (now uses injected `CAIReports_DataSource`, default `GetData`), `GatherGossip`, `FilterCAIGossip`, `RebuildYieldsTree/RebuildResourcesTree/RebuildCityStatusTab/RebuildGossipTab`, `CAIReports_IsCityStatusListMode`; city cycling + gossip data helpers stay in shared. Both new files registered in modinfo `<File>` + `<ImportFiles id="CAIInGame">`. All files pass `lua-language-server --check`; all 5 text XML + modinfo well-formed.
    - BRS variant data model: the 4 overlapping tabs (Yields, Resources, City Status, Gossip) reuse the shared builders, fed by a **vendored verbatim copy of vanilla `GetData()`** (`CAIBRS_GetData`; BRS removed vanilla `GetData` but keeps CitySupport globals `GetCityData`/`GetCityResourceData`/`GetWorkedTileYieldData`/`AddResourceData`). The 5 new tabs: Deals (tree, `GetDataDeals`), Units (list, live units via the Step-0 shared unit-record helpers — NOT `m_kUnitDataReport`), Policy (tree grouped by slot, `UpdatePolicyData`/`m_kPolicyData`, + 2 filter checkboxes driving BRS's `Hide*PoliciesCheckbox`), Minor (tree grouped by city-state category, `UpdateMinorData`/`m_kMinorData` — used instead of the CityStates readers to sidestep the vanilla-CityStates-globals cross-context gotcha; tier bonuses in category tooltip, + 2 filter checkboxes), Cities2 (list, GS-only, `m_kCity2Data`, power figures). New tabs are trees/lists (first pass), not the full DataTable/dual-view designs in §6 — enrich after testing.
    - 13 new LOC tags (LOC_CAI_REPORTS_*) added to en_US + the 4 other shipped UI languages (fr_FR, pl_PL, pt_BR, zh_Hans_CN — only these ship `cai_text_ui.xml`). Reused `LOC_CAI_CITYSTATES_ENVOYS_TIER`.
    - KNOWN FIRST-PASS RISKS to check in-game: (1) the §4.4 unknown — does `include("ReportScreen")` resolve to BRS so `ViewDealsPage`/`GetDataYields` are defined? Confirm in Lua.log. (2) Field parity between `CAIBRS_GetData` (vendored vanilla) output and what the shared builders expect (should match — same GetCityData). (3) BRS Minor filter control names (`HideNotMetMinorsCheckbox`/`HideNoImpactMinorsCheckbox`) — guarded with nil checks, verify names. (4) Tab switching/focus/last-tab persistence across all 9 tabs. (5) City cycling with BRS data. Deviations from spec: single BRS variant file (not split per-tab, §5) for include-visibility robustness; Minor/Cities2 read BRS globals not fully-live (§7).
  - **STEP 0 DONE (§8 #6 / §10), PENDING IN-GAME RE-VERIFICATION.** Extracted the shared game-state readers into `inGameHelpers_CAI.lua` (already included by UnitPanel_CAI + ReportScreen_CAI; added `include("inGameHelpers_CAI")` to CityStates_CAI after its vanilla base include): unit-record helpers `UnitRecordFocusKey`/`ResolveUnitRecord`/`CategorizeUnit`/`BuildLocalUnitRecords`/`SelectUnitRecord(record, onBeforeSelect)`/`JumpToUnitRecord`/`OpenUnitRecordCivilopedia(record, onBeforeOpen)`; and city-state readers `GetRelationshipsWithPlayerIDs`/`GetCityStateData`/`FillBonuses`/`GetAllCityStatesData` (moved verbatim from CityStates_CAI, now globals). Activation takes an injected teardown callback so UnitPanel passes `RemoveUnitList` (Ctrl+U list closes) while the report will pass its own report-close. `UnitPanel_CAI.CAIUnitList` methods (`RecordKey`/`Resolve`/`GetCategory`/`BuildRecords`/`ActivateRecord`/`JumpToRecord`/`OpenCivilopedia`) now delegate to the shared functions; CityStates_CAI's four local defs deleted, call sites unchanged. Behavior-preserving; all three files pass `lua-language-server --check`. NOTE (Minor-tab gotcha): the city-state readers depend on vanilla CityStates partial globals (`GetRelationships`/`GetCityStateType`/`GetQuests`/`GetBonusText`/`GetSuzerainBonusText`) that are NOT loaded in the ReportScreen context — resolve this when building the Minor tab (see docs/game-api.md). **PENDING USER TEST:** Ctrl+U unit list and the City-States screen still open, read, sort, filter, and act (select/jump/civilopedia) correctly — this affects all users, not just BRS, so verify before any report tab is built on the shared readers.

- Extended Policy Cards (EPC, Aristos, UUID `382a187f-c8ba-4094-a6a7-0d5315661f33`) support. **BUILT, PENDING IN-GAME TEST.** EPC depends on Better Report Screen and adds a computed effect string per policy card via `ExposedMembers.RMA.CalculateModifierEffect`. CAI wins the GovernmentScreen context (EPC suppressed), so CAI re-surfaces it. Detection `IsExtendedPolicyCardsActive()` in `caiUtils.lua`. In `GovernmentScreen_CAI.lua`: `GetPolicyEffect()` reads RMA (player = `Game.GetLocalPlayer()`), `GetPolicyTooltip()` now prepends it (no-op unless EPC active). The policy picker/viewer dispatch to a new `GovernmentScreen_ExtendedPolicyCards_CAI.lua` (global `CAIGovPolicyPickerEPC`, pulled by runtime `include()`, registered in both `<File>` and `<ImportFiles>`) that pushes a Panel with a DataTable (Name/Slot/Impact — no status column) + Tree dual view, sort dropdown (tree), switch button, and `Alt+1`/`Alt+2` view switches; view mode persisted via `CAI.GetConfigValue/SetConfigValue` key `GovPolicyPickerViewMode`. Host passes a context table (`BuildPickerContext`) so the variant never reaches into the main file's locals; shared assign refactored to `AssignPolicyToSlot`. New LOC tag `LOC_CAI_GOVERNMENT_POLICY_SLOT_HEADER` in all 5 shipped UI languages. All Lua passes `lua-language-server --check`; all XML well-formed. PENDING USER TEST: with EPC+BRS active, open the Government screen → Policies tab; picking a slot and "view all policies" open the table/tree panel; effect reads in the Impact column and at the start of each policy's description; table sort, tree sort dropdown, view switching, and assigning a policy all work; confirm `ExposedMembers.RMA` is populated in the GovernmentScreen context (check Lua.log) and that `Game.GetLocalPlayer()` yields sensible effect numbers.

- Replay graph grouping toggle on the End Game (victory) screen and Hall of Fame game details. New "Group by value" checkbox on each screen's Graphs tab (placed after the tree, below it). Unchecked (default) = existing player→turn→values grouping; checked = player→value→turns-where-it-changed. Shared preference persisted via `CAI.GetConfigValue/SetConfigValue` section `UI` key `ReplayGraphGroupByValue` (both screens read it fresh in their `RebuildGraphsTree` and sync their checkbox, so a toggle in one screen is honored by the other). `EndGameMenu_CAI.lua`: refactored `RebuildGraphsTree` to collect per-player raw `points {turn,dataSetName,display,value}` then dispatch to `BuildTurnGroups`/`BuildValueGroups`; turn-grouping keeps the original focus keys. `GameSummaries_GameDetails.lua`: same shape with per-player `entry.Points {Turn,Prefix,Value,KeySuffix}` (Prefix = optional entity name + graph name, the "value" identity) → `BuildGraphTurnGroups`/`BuildGraphValueGroups`; change-only detection unchanged. New LOC tags `LOC_CAI_REPLAY_GROUP_BY_VALUE(+_TOOLTIP)` in all 5 shipped languages. Checkbox pattern mirrors GreatWorks (`SetValueSetter`+`SetChecked(v,true)`). Verified with lua-language-server `--check`. PENDING USER TEST: both screens show the checkbox, toggling flips the tree layout, choice persists and carries between the victory screen and hall of fame.

- Better Balanced Game (BBG) WorldRankings integration (`WorldRankings_CAI.lua`). BBG and CAI both `ReplaceUIScript` the `WorldRankings` context (single-winner), so CAI now detects BBG (`IsBBGActive()` over three mod IDs `cb84075d-...-be240/-be217/-be231`) and, ahead of the exp2/exp1 include branches, includes BBG's WorldRankings so its Traditional Domination tab survives and CAI layers on top. NOTE (fixed 2026-08-19): the first cut did `include("worldrankings_bbg")` and the screen would not open at all (F8 = click, no panel). Root cause: CAI wins the `ReplaceUIScript`, so BBG's file never loads as a context, and a cross-context `include()` resolves only against files BBG exported via `<ImportFiles>` — BBG's `<ImportFiles>` lists only `endgamemenu_bbg.lua`, NOT `worldrankings_bbg.lua` (a top-level `<File>` entry is not enough). The failed include left every base WorldRankings global nil, so the first unguarded `WrapFunc` crashed the context. Fix follows the CivRoyale/scenario `_CAIBase` vendor pattern: a verbatim copy at `UI/Replacements/BetterBalancedGame/WorldRankings_BetterBalancedGame_CAIBase.lua`, registered in CAI's `<Files>` + `<ImportFiles id="CAIInGame">`, and CAI now `include("WorldRankings_BetterBalancedGame_CAIBase")`. Re-copy when BBG updates its file. Five gated changes accommodate BBG's extra tab: (1) `IdentifyGenericVictoryTypes` branch matching the tab's button-tooltip label `LOC_TOOLTIP_TRADITIONAL_DOMINATION_BUTTON` → `VICTORY_TRADITIONAL_DOMINATION` (BBG labels by button key, not `row.Name`); (2) capture wraps mirrored onto BBG's own `PopulateTradDomInstance`/`PopulateTradDomTeamInstance` (vanilla `PopulateGeneric*` never fire for that tab); (3) `ViewTraditionalDomination` wrapped for `BeginGenericCapture`/`EndGenericCapture`+`ViewSync` (tab uses it, not `ViewGeneric`); (4) `CreateGenericPlayerRow` skips CAI requirement recomputation for this victory and defers to BBG's merged progress+requirement Details; (5) advisor leaf appends BBG's domination threshold (`TRADITIONAL_DOMINATION_LEVEL`). EndGameMenu needs nothing (both ride the `EndGameMenu_` wildcard and coexist); `unitpanel_bbg.lua` is unregistered/dead in BBG 4.8.3; SQL/config are gameplay-only (no UI conflict). Reuses BBG's existing loc keys (shipped in all languages) — no new CAI tags. Verified with lua-language-server `--check`. Docs: `docs/game-api.md` new "Better Balanced Game (mod) integration" section; changelog Added entry. STATUS: DONE, tested in-game 2026-08-19 (Lua.log confirmed `isBBG=true`, `WorldRankings for BBG XP2` loaded, screen opens with 7 tabs including the BBG-renamed "Conquest" domination tab with table+tree). Note: BBG's separate Traditional Domination victory tab only appears when that victory is enabled at game setup (BBG front-end `VictoryDominationSetting` → `Game.IsVictoryEnabled("VICTORY_TRADITIONAL_DOMINATION")`); when off it is absent for pure BBG too, so its own tab is not a CAI regression. All temporary CAIDEBUG scaffolding removed. Lua.log lives at `%LOCALAPPDATA%\Firaxis Games\Sid Meier's Civilization VI\Logs\Lua.log`.

- Diplomacy conversation reply list fixed (`DiplomacyActionView_CAI.lua`, `RefreshConversationPanel`). Two root causes: (1) CAI mapped re-derived selections to `ConversationSelectionStack:GetChildren()[i]` by index, but `InstanceManager:GetInstance` re-parents reused instances to the BOTTOM and hides the rest, so `GetChildren()` returns stale hidden leftovers AHEAD of the live buttons — the index landed on a hidden leftover (active "Goodbye" read unavailable). (2) CAI's re-extraction is NOT always identical to vanilla's render: `RemoveInvalidSelections`/`IsDiplomaticActionValid` can drop a disabled reply here (e.g. "Unable to Surprise War" — team ally) that vanilla still renders as a disabled button, so the button had no paired selection and read with no label. Fix: (a) filter `GetChildren()` to VISIBLE children and drive the loop off those; (b) read label/tooltip/disabled straight from the live control (new `ControlButtonText` helper walks to the `SelectionText` child, since `GetTextControl()` misses it); (c) activate via `buttonControl:DoLeftClick()` — fires the button's own vanilla callback (correct key + any war-confirm/stop-asking popup), so activation is right even when re-derivation dropped the selection, and disabled buttons no-op. Selections are now kept ONLY for a label/tooltip fallback and to spot `CHOICE_EXIT`, paired to buttons by matching the live label to `Locale.Lookup(selection.Text)` (not by index); `Handler` is no longer stored. Verified with lua-language-server `--check`. PENDING USER TEST: war-warning conversation lists "passing by", the disabled declare-war reply (reads its name + "unavailable" + reason), and Goodbye; Goodbye works without Escape; enabled replies still activate.

- Type-to-find accent folding + proximity-as-sort (NEW 2026-08-26, PENDING IN-GAME TEST). **Reverses the old "proximity always wins, drop the rest" decision per user directive: proximity now only SORTS, it never filters.** Two fixes in `CAIWidgetHelpers_Search.lua`: (1) **Never drop matches** — `FindBestTierResults` (which bucketed by proximity and returned only the closest non-empty bucket at its best tier, discarding every farther/weaker match) is replaced by `FindRankedResults`, which scores each candidate at its best tier, keeps them ALL, sets `result.Proximity`, and returns the flat list sorted by `CompareSearchResults` (proximity is its top key, then source/tier/position/length/BFS). So `kh` always reaches Khmer + Khevsur + Khass-Oda-Bashi regardless of the starting section; the nearest one is just focused first. Anchor mechanism (`Manager.SearchAnchor`, `BuildFocusDepths`, `CandidateProximity` off-path = -1) is unchanged. (2) **Accent folding** — `AsciiLower` renamed to `FoldText` (all call sites + manager `AppendSearchChar` + `ideHelpers.lua`). Besides lowercasing ASCII A–Z it maps accented Latin letters (Latin-1 Supplement U+00C0–00FF + Latin Extended-A U+0100–017F, both cases, via a load-time `CP_FOLD` table keyed by integer code point) to their base ASCII letter, so `a`==à/á/â/ã/ä/å/ā, `c`==ç/č, `n`==ñ, etc. **IMPORTANT (discovered 2026-08-26 via in-game self-test):** the first cut used a `string.gsub("[\195-\200][\128-\191]", ...)` pattern; the self-test logged `FoldText('Château')='château'` — Civ VI's Lua does NOT reliably match bytes >= 0x80 in pattern SETS (the same locale/charset trap that breaks `%s`), so the `â` never folded and `chateau` gave No match. FoldText is now a raw `string.byte` scan (integer comparisons only, no patterns/`string.lower`): A–Z lowercased; a 2-byte lead `0xC2–0xDF`+continuation `0x80–0xBF` is decoded to a code point (`(b-0xC0)*0x40 + (b2-0x80)`) and looked up in `CP_FOLD`; everything else (ASCII, 3-byte+ CJK) copied verbatim. This makes `chateau`/`cha` reach "Château" (vanilla en_US label is `Ch` `C3 A2`(â) `teau`). Python byte-sim confirms fold + CJK preservation; temporary load-time self-test line ("Search helper fold self-test") + a "NO MATCH for buffer" log remain until the user re-verifies in-game (then remove both). LIMITATION: Vietnamese / Latin Extended Additional (3-byte, e.g. Bà Triệu) is not folded yet. `lua-language-server --check` clean; changelog + `docs/ui-manager.md` Type-to-find section updated (no plain Lua interpreter; sumneko LS at `C:\Users\amine\.vscode\extensions\sumneko.lua-3.19.1-win32-x64\server\bin\lua-language-server.exe`). PENDING USER TEST in Civilopedia: `kh` from any section reaches all three Kh* entries; `chateau`, `cha`, and `ch` all reach Château; accented civ/leader names (Nazca, Å-, ñ) are reachable by plain letters; CJK type-to-find still works under Simplified Chinese.

- Mod suspend/resume for hotseat (Ctrl+Shift+F12). Authoritative live flag is `ExposedMembers.CAI_Active` (`~= false` = active); helper `IsCAIActive()` + persistence `LoadCAISuspendedFlag`/`SaveCAISuspendedFlag` in `caiUtils.lua` (raw CAI config section `CAI`/`Suspended` — NOT a registered CAISettings row, so it stays out of the settings list; deferred: optional visible checkbox). Manager owns `IsCAIActive`/`SetCAIActive`/`ToggleCAIActive` (`CAIUIScreenManager.lua`); `SetCAIActive` writes flag+config, speaks a forced confirmation (`LOC_CAI_MOD_SUSPENDED`/`_RESUMED`, bypasses guard), then fires `LuaEvents.CAIStatusChanged(active)`. `Init()` reseeds the flag from the persisted setting each session. DONE central guards: `Speak` early-returns when suspended unless `force` (4th arg); manager `HandleInput` runs a globalOnly walk when suspended (only `Global` bindings fire) and `HandleCharInput` returns false; `OnCAIInputActionStarted`/`OnInputActionTriggered` in `WorldInput_CAI` return early; cursor `UI.GetCursorPlotID/Coord` overrides in `inGameHelpers_CAI` fall back to captured vanilla originals; audio manager `Play`/`PlayAtPlot`/`QueueSound`/`QueueSoundAtPlot` early-return when suspended (no new CAI sound effects, incl. focus/wrap sounds; already-playing loops are not force-stopped); CityStates banner (`CAI_OnRaiseMinorCivicsPanel`) falls through to `baseOnRaiseMinorCivicsPanel` (vanilla detailed view). Toggle is a base-widget binding added in `UIWidget.New` (Ctrl+Shift+F12, KeyUp, `Common`+`Global`); new `Global` binding flag + `OnHandleInput(input, globalOnly)` param (SearchPanel override forwards it). Interface-mode intercepts intentionally left untouched. LOC tags in all 5 languages; ideHelpers annotated. DONE reconciliation: manager suspend-closer registry (`RegisterSuspendCloser`/`UnregisterSuspendCloser`/`CloseSuspendModals`, invoked in `SetCAIActive` before the broadcast) closes CAI-only non-visual modals on suspend — wired into settings (`CAIWidgetHelpers_Settings`), pedia lookup, input help, `SearchPanelWidget` (covers generic type-to-find + scanner search), city actions (`CityPanel_CAI`), unit actions (`UnitPanel_CAI`), plot actions (`WorldInput_CAI` plot-interact list), and the governor promo confirm dialog (`GovernorPanel_CAI`). Governments resume reconciliation (`GovernmentScreen_CAI`): `SwitchTabToMyGovernment` wrap no longer redirects while suspended (records `m_state.onVanillaMyGovernment` drift; the two other tab wraps clear it); a `CAIStatusChanged(true)` listener (guarded `not ContextPtr:IsHidden()`) collapses a My-Government drift to the Governments tab via `SwitchTabToGovernments`, else `MirrorActiveTabToCAI`, then `Refresh*`. CityStates/Governors need no further resume reconciliation — their CAI views are flat and their open-mode follows the vanilla open reason (Overview vs SendEnvoys), not a CAI-forced sub-state. Cross-context note: in-game there is ONE shared manager (WorldInput's include owns it); closures registered from any screen context are callable from the toggling context because UI shares one Lua state. PENDING USER TEST: Ctrl+Shift+F12 suspends (speech+nav+cursor+world actions go silent/vanilla, city-state banner opens vanilla detailed view, confirmation spoken) and resumes; open modals (settings/pedia/input help/search/scanner search/city+unit+plot action menus/governor promote dialog) close on suspend; Governments open on My-Government tab then resume lands on Governments tab; persistence across restart.

- World city cycling (`[`/`]`, actions `WorldSelectPreviousCity_CAI`/`WorldSelectNextCity_CAI`) now mirrors the unit-cycling design and is owned by `ReportScreen_CAI`. `WorldInput_CAI` forwards them as `LuaEvents.CAICycleSelectedCity(direction)` (its old `SelectPreviousCity`/`SelectNextCity` locals removed; capital-select unchanged). `ReportScreen_CAI.OnCAICycleSelectedCity` calls `RefreshCAIData()` then walks the city status table order: with `CityCyclingFollowReportSort` on it uses `m_cityStatusSort`/`m_cityStatusSortAscending`; off it uses NATURAL order (a `"natural"` sentinel added to `GetSortedCityData`, returning `m_caiCityData` in its `.Order` sequence — cities do NOT default to distance, per user). `WrapCityCycling` toggles wrap vs stop-at-ends (`LOC_CAI_NO_MORE_CITIES`; empty `LOC_CAI_NO_CITIES`). Distance-origin anchoring (`m_cityCycleOrigin`/`AnchorKey`/`DistanceOrigin`, threaded into `GetCityStatusDistance`) applies only when the effective sort is `distance`; cursor origin on the first cycle with nothing selected. `m_cityStatusSort` default is `"natural"` (report opens in `.Order`; a `"natural"` option added to the list-view dropdown). Two pre-existing bugs fixed so the report's sort persists across close/reopen: `SetDefaultSort` was hardcoded to `name` (now seeds from `m_cityStatusSort`, nil = natural), and the DataTable had no `sort_changed` handler (added; writes `columnKey or "natural"` back and calls new `SyncCityStatusSortDropdown`). The list dropdown also syncs the table's sort. City settings are INDEPENDENT of the unit ones (`WrapCityCycling`/`CityCyclingFollowReportSort`, Gameplay SortIndex 30/40). `UI_TopPanelOpenReportsCityStatus` bound to Ctrl+Backslash (`hotkey_config_CAI.xml`). New LOC tags for both settings (+tooltips) and both feedback messages across en_US/fr_FR/pl_PL/pt_BR/zh_Hans_CN. No callable Lua parser available. PENDING USER TEST: `[`/`]` cycle cities in report order, distance stability, wrap/stop, follow-sort vs natural, empty/boundary announcements, and Ctrl+Backslash opens the city status report.

- Two new Gameplay settings (section `Gameplay`, both `bool`/`checkbox`, default `true`) drive world unit cycling, added to `src/data/settings_CAI.sql` with LOC tags `LOC_CAI_SETTING_SECTION_GAMEPLAY`, `LOC_CAI_SETTING_WRAP_UNIT_CYCLING(+_TOOLTIP)`, `LOC_CAI_SETTING_UNIT_CYCLING_FOLLOW_SORT(+_TOOLTIP)` in en_US/fr_FR/pl_PL/pt_BR/zh_Hans_CN. `WrapUnitCycling` toggles wrap-around vs stop-at-ends (`LOC_CAI_NO_MORE_UNITS`). `UnitCyclingFollowPanelSort` chooses the panel's remembered sort vs forcing `distance`. `OnCAICycleSelectedUnit` reads both via `CAISettings.GetBool`; `SortRecords`/`GetSortedUnits` take an optional `(sortColumn, sortAscending)` override so cycling can force distance without touching the panel's stored sort. Distance-cycling origin: when nothing is selected it measures from the cursor (`DistanceOrigin=nil`) and starts a fresh run; once a unit is selected it anchors on that unit and holds the origin until selection changes outside cycling. The single-candidate case also announces `LOC_CAI_NO_MORE_UNITS`.

- World unit cycling now walks the unit list's sorted order and is owned by `UnitPanel_CAI`. `WorldInput_CAI` no longer selects units directly: its `SharedInputActions` for `PrevUnitSelection`/`NextUnitSelection`/`PrevReadyUnitSelection`/`NextReadyUnitSelection` just fire `LuaEvents.CAICycleSelectedUnit(direction, readyOnly)`. `UnitPanel_CAI.OnCAICycleSelectedUnit` builds the pool via `CAIUnitList.GetSortedUnits(readyOnly)` (all local units → `SortRecords`; `readyOnly` filters to `IsReadyToMove()`), finds the current selection, and steps. Cycling is NON-wrapping: at either end it keeps the current selection and announces `LOC_CAI_NO_MORE_UNITS` ("No more units, {name} already selected"); empty pool announces `LOC_CAI_NO_READY_UNITS` or `LOC_CAI_UNIT_NO_UNITS`. Semantics: comma/period = ready, Shift+comma/period = all; no category filter (panel-only). Distance-sort cycling anchors its origin to the selected unit and holds it for the run — `GetDistance` reads `CAIUnitList.DistanceOrigin` when set (else the cursor); the origin re-anchors only when the selection changes outside cycling, tracked via `CAIUnitList.CycleOrigin`/`CycleAnchorKey`. This fixes the ping-pong where cursor-relative distance re-sorted every press and bounced between the two nearest units. `CAIUnitList.EnsureColumns()` lazily builds columns so sort keys exist even if the panel was never opened. Default sort is now `distance` (nearest first) at `CAIUnitList` init and in the `BuildPanel` invalid-column fallback. Sort state lives on the module-level `CAIUnitList` table and is never reset on close (verified: `SetDefaultSort` seeds from the remembered value, only `CycleSort` emits `sort_changed`), so it is remembered across close/reopen within a session. Removed dead `UnitCategories`/`activeCategoryIdx` and the four cycling action locals from `UnitPanel_CAI`. New LOC tags `LOC_CAI_NO_READY_UNITS`, `LOC_CAI_NO_MORE_UNITS` in en_US/fr_FR/pl_PL/pt_BR/zh_Hans_CN. No callable Lua parser available. PENDING USER TEST: comma/period cycle ready units nearest-first without ping-pong, Shift+comma/period cycle all units in the chosen sort, boundary announcement at each end (and single-unit "already selected"), empty-pool "No ready units", and that a re-sorted panel keeps its order in both panel and world cycling after closing.

- Epic Games Store compatibility for shipped front-end/shared full-copy screens. The mod ships Steam-based full copies but replaces only the Lua context, so on Epic they run against Epic's older XML/runtime. Diffed the Epic base UI folder against the Steam install: every in-game replaced screen is byte-identical Epic vs Steam; only 9 front-end/shared files diverge, all due to Epic's older build lacking the online Challenge/GoTM carousel and three newer Lua APIs. Fixes applied: (1) `MainMenu.lua` — Epic's `MainMenu.xml` omits ALL Challenge carousel controls + the `CarouselEntry`/`CarouselEntryIndicator` instance templates (confirmed by XML diff), so gated all carousel logic behind `m_hasChallengeCarousel = Controls.ChallengeStack ~= nil` (guarded IM creation, early-returns in every carousel fn, wrapped the per-frame `OnUpdate` challenge block, gated the `Initialize` button-callback registration); the pre-existing `PopupRoot` guard stays. (2) `LoadScreen.lua` — guarded both `Challenges.IsChallengeActive()` with `Challenges ~= nil`, falls through to plain leader path. (3) Feature-detection (not `Network.GetNetworkPlatform()`, which reflects network transport not build and is unreliable under crossplay) for three newer APIs Epic's older build likely lacks: `UI.GetNetworkVersion` → fallback `UI.GetAppVersion` in `Lobby.lua` (×2); `Events.SetGameEntryMethod` wrapped `if ... then` in `AdvancedSetup`/`ScenarioSetup`/`TutorialSetup`/`HostGame` (game-start paths); `HallofFame.GetLocalizedVictoryTypeText` → fallback `LocalizeAndSetText(name)` in `GameSummaries_GameDetails.lua`. `GameSummaries.lua` needed no change (its `Challenges.*` calls are already gated behind `g_CurrentRuleset.ChallengeIds`, nil on Epic). On Steam every guard is a no-op. MainMenu carousel + LoadScreen challenge = confirmed missing (XML). The three API guards are defensive/inferred — could NOT confirm from the Epic UI zip (Lua/XML only, not the runtime API list); harmless on Steam either way. No callable Lua parser available; verified block balance by reading. PENDING USER TEST ON EPIC (user cannot test Epic themselves — ships for users to try): main menu loads with no carousel, loading screen shows leader text, start a single-player/scenario/tutorial game, host a multiplayer game, open the multiplayer browser, open a past-game summary. Changelog updated with player-facing Epic entries.

- Unit numbering: same-named units read with a per-owner, per-type slot number (e.g. "Warrior 1", "Warrior 2"). Implemented entirely in the UI layer as the `CAIUnitNumbers` system (`src/UI/inGame/CAIUnitNumbers.lua`) — no gameplay script, no unit properties (the earlier gameplay-script/`SetProperty` approach was dropped because `UnitVisibilityChanged` is a UI-context event and the UI/gameplay VMs cannot bridge via LuaEvents). It is a shared singleton on `ExposedMembers.CAIUnitNumbers`, created and event-registered once by `WorldInput_CAI.lua` (added to its subsystem `include` list next to `RevealAnnouncements_CAI`/`WorldScanner_CAI`); every screen reads it from `ExposedMembers`. Registry: `data[ownerID][unitID] = number`, assigned as the lowest slot not held by that owner's other living KNOWN units of the same type (gap-filling on death). "Known" = observed by the local player: own units numbered on `Events.UnitAddedToMap` and via `BackfillLocalUnits()` (enumerate local units in GetID order) at creation/`LoadScreenClose`/`LocalPlayerChanged`; other players' units numbered only on first `Events.UnitVisibilityChanged` == `RevealedState.VISIBLE`, so no numbering gap can reveal an unseen unit. Persistence: the whole registry (ALL owners — own + sighted enemies) is serialized (`o:u:n|...`) into the LOCAL player's `PlayerConfigurations:SetValue("CAI_UnitNumbers", ...)`, saved with the game and keyed per local player (hotseat-safe); on reload every owner's numbers are restored. Dead-unit entries are pruned on `Events.UnitRemovedFromMap` (only when `UnitManager.GetUnit` returns nil, i.e. truly gone, so fogged-but-alive enemies keep their number) to keep the saved string lean. The grouping key is unit **type + military formation** (`GetUnitType()` + `GetMilitaryFormation()`), so "Tank Corps 1" means 2+ tank corps, not 2+ tanks. On `Events.UnitEnterFormation` a tracked unit is `Reassign()`-ed into its new type+formation group (renumber, no collision with an existing corps/army); forming is permanent/monotonic (standalone→corps→army, no un-forming), so no `UnitExitFormation` handling is needed. Display: `inGameHelpers_CAI.lua` `GetUnitDisplayNumber` reads the singleton and suppresses the number when `CountKnownOfType(owner, type, formation) < 2` (lone unit reads plain); the number is appended AFTER the formation suffix ("Field Cannon Corps 1"), via `GetNumberedUnitName(unit, formattedString)` applied to the fully-formatted name in `FormatOwnedUnitDisplayName`, the unit panel `GetUnitListName`/`GetUnitInfoName`, and the units report breakdown in `ReportScreen_CAI.lua`; custom veteran-named units stay unadorned. Singleton is rebuilt fresh each session (NO `if == nil` guard) because `ExposedMembers` is not cleared on reload — a stale instance there carries closures bound to the destroyed context (its `UnitManager` etc. resolve to nil), which caused a reload-time crash. LOC tag `LOC_CAI_UNIT_NUMBER_SUFFIX` (`{1_Name} {2_Number}`) in en_US/fr_FR/pt_BR/zh_Hans_CN. STATUS: tested in-game and working — own units number/persist across save/load, enemy units number in sighting order (no spoiler), formation-aware grouping, reload no longer crashes.

- Better Trade Screen (BTS, astog, UUID `8d4fa23a-...`) support for the Make Trade Route screen. `caiUtils.IsBetterTradeScreenActive()` gates it. `TradeRouteChooser_CAI.lua` now `include`s the base then, when BTS is active, `include`s `TradeRouteChooser_BetterTradeScreen_CAI.lua` and `return`s before the vanilla wraps (both files register in the top-level `<File>` list; the BTS file also in `<ImportFiles id="CAIInGame">` since it is a runtime `include`). The BTS layer presents a flat, CAI-sorted route tree (destination + turns-to-complete + trading post/quest markers, origin/dest yield children), a filter dropdown reusing BTS `OnFilterSelected`, a checkbox-per-key sort list where position = priority and Shift+Up/Down reorders (model owned by CAI: `InsertSortEntry`/`SortTradeRoutes` on our own settings table, no dependence on BTS's inaccessible `m_SortBySettings`), a sort-direction Button after the list that flips asc/desc and `mgr:Refocus()`s, and a confirm dialog. Automation is split: "Repeat this route" is a Checkbox inside the confirm dialog (normal route pick only), while "Repeat the best route by your sort" is a Button on the panel (after the sort-direction button) that grabs the current top sorted route (`m_caiSorted[1]`, announces `LOC_ROUTECHOOSER_NO_TRADE_ROUTES` if empty) and opens the same confirm dialog with `bestSorted=true` (which omits the repeat checkbox). On Begin, the dialog clears BTS's own checkboxes, calls RequestTradeRoute, then calls AutomateTrader directly — the bestSorted variant passes the CAI sort settings (BuildCaiSortSettings) so it honors the checkbox sort rather than BTS's inaccessible visual sort bar, and the repeat variant repeats the exact route. All three trade screens are now branched for BTS with the same guard pattern: `TradeRouteChooser_BetterTradeScreen_CAI.lua`, `TradeOverview_BetterTradeScreen_CAI.lua`, and `TradeOriginChooser_BetterTradeScreen_CAI.lua` (all in the `<File>` list and `<ImportFiles id="CAIInGame">`). Origin chooser only needed the `AddCity(cityID)` signature adapt. Overview wraps BTS's `AddRouteInstanceFromRouteInfo`/`AddChooseRouteButtonInstance`/`AddProduceTradeUnitButtonInstance`/`CreateCityHeader`, adds filter + group-by dropdowns (capture via `AddFilter`/`AddGroupByEntry`, wired to `OnFilterSelected`/`OnGroupBySelected`), reads turns/yields via TradeSupport, offers cancel-automation on automated My Routes rows (`IsTraderAutomated`/`CancelAutomatedTrader`), and activates Available routes via a best-effort free-trader lookup (`FindFreeTraderInCity` → `SelectFreeTrader`). Overview Close wrap resets tab/filter to match BTS. New LOC tags added to en_US, fr_FR, pt_BR, zh_Hans_CN only (the other 8 languages lack `cai_text_ui.xml` and fall back to English, consistent with existing state). Tested in-game and working (2026-08-12). See `docs/game-api.md` "Better Trade Screen (mod) integration". Coverage confirmed with BTS active: (1) Make Trade Route — route list reads with turns, filter dropdown, checkbox sort toggle + Shift+Up/Down priority reorder + direction flip re-speak, confirm dialog begin/cancel, the in-dialog Repeat-Route checkbox, the panel "best sorted" button (opens confirm on the top route and automates from the top; empty-list announcement), Escape/close parity; (2) Trade Overview — all three tabs read with routes under headers, filter + group-by dropdowns, cancel-automation on an automated route, Available-route activation with and without a free trader in the origin city, tab switching/persistence; (3) Change Origin City — list reads and teleports; plus a regression pass with BTS disabled (all three vanilla paths unchanged).

- World Scanner now has two quick-access slots (`CAIWorldScanner:AssignSlot`/`CycleSlot` in `WorldScanner_CAI.lua`). Ctrl+`-`/Ctrl+`=` bind the current subcategory (category+subcategory id) to slot 1/2; `-`/`=` step next and Shift+`-`/Shift+`=` step previous through that subcategory as one flat, nearest-first list (groups ignored). Navigation points the live scanner focus at the chosen leaf so Jump/direction/beacon target it. Bindings persist globally in CAI config section `WorldScannerSlots` (keys `Slot1`/`Slot2`, encoded `categoryId|subCategoryId`), hydrated in `Initialize` via `LoadSlotBindings`; in-list position is transient. Works for built-in and custom categories (search results only within the session). Six new `WorldScannerSlot*` input actions wired in `WorldInput_CAI.lua` + `hotkey_config_CAI.xml`. Also added an `Index="1"` alternate gesture on `WorldScannerJumpToCurrent` bound to `0` (number row; Home retained on Index 0). New LOC tags added to en_US, fr_FR, pt_BR, zh_Hans_CN only — the other 8 shipped languages still need the 14 `LOC_CAI_WORLD_SCANNER_SLOT*` tags (per user decision). In-game testing pending: bind/step/wrap on a built-in and a custom subcategory, persistence across reload, `0` jump, and slot behavior when the bound subcategory empties or disappears.

- City amenities now speak in the city panel (Shift+6 / Info6 bucket), which reads Population, Housing, always-on building count, and amenities. A single shared helper `info.GetCityAmenitiesSummary(city)` (defined in `CityPanel_CAI.lua`, exposed on `ExposedMembers.CAIInfo`) computes amenities from `city:GetGrowth()` and formats them like the overview panel, e.g. "Amenities: Content, 6/4". The panel bucket now uses `BuildingCount` (pure `GetCityInfoBuildings`, always spoken) instead of the old `BuildingsOrLoyalty` that hid the count behind loyalty in expansions. The city banner loyalty summary (`CityBannerReadLoyaltySummary`) now prepends the same amenities line for your own cities via the shared helper, gated by a new `IsOwnCityContext`; in the base game amenities becomes the only line spoken instead of the "info unavailable" message, and in expansions it precedes loyalty. In-game testing should cover: Shift+6 in the city panel across base/XP1/XP2 (amenities format, always-on building count), the banner loyalty key on your own city in base game (amenities only) and expansions (amenities then loyalty), and a foreign city banner (no amenities leaked).

- Widget position speech is now limited to ordered navigation containers: direct items of Lists, Trees/TreeItems, SubMenus, and TabControl tab strips; DataTable cells; Graph nodes; and Grid cells. Ordinary layout parents and generic HorizontalLists no longer produce positions. Graph nodes report their index in the live Up/Down alternative set. Grid cells use DataTable row/column wording, flattening every visible internal tier into a spoken column while retaining raw spatial row alignment. UI-manager documentation, LuaLS annotations, and the changelog are updated. Structural container/coordinate assertions, localization XML parsing, deterministic Civilopedia generation, and scoped `git diff --check` pass; no callable Lua parser is available. In-game testing should cover allowed and suppressed containers, graph edge/root/external-focus alternative reseeding, multi-tier grids, uneven tiers, and hidden columns/spacers.

- World Rankings now defaults to one persisted table preference shared across the entire screen. Overall, Score, Science, Culture, Domination, Religion, and Gathering Storm Diplomatic Victory use sortable tables when structured data is available; Alt+1 selects tables and Alt+2 selects the retained trees for every tab. Each row is one actual team competitor keyed by team id: singleton teams display their civilization, multi-civilization teams display the team, the Competitor tooltip uses a single "Members:" line with all names, and metric tooltips contain the per-civilization breakdown only for multi-member teams. Team numbering matches the staging room and diplomacy ribbon (teamID + 1). Victory progress is the second column after competitor for culture, domination, religion, and diplomatic tables. Science spaceport cells use short "Built"/"Not Built" labels. Diplomatic points tooltips include per-source breakdowns from `GetDiplomaticVictoryPointsTooltip`. Overall dynamically compares enabled victories by exact rank/progress; Score dynamically aggregates live scoring categories; Science mirrors vanilla's coherent leading-member meter choice; Culture uses its best member; Domination uses combined captures and original-capital state; Religion uses deduplicated conversions and religions; Diplomatic Victory uses best-member points plus team progress. Narrative scenario Overall pages and arbitrary custom victories stay tree-only and identify the fallback under a table preference. Table advisor explanations remain separately readable, sorts persist per tab while open, and view switching preserves competitor focus including the Overall victory column where possible. In-game testing must cover FFA and multi-member teams across all seven tables, every column and sort, member/contribution tooltips, unmet identities, local Culture sources, base/XP1/XP2 science differences, Alt/button switching and persistence, focus transfer, live refreshes, narrative scenarios, Black Death structured Overall, and generic/custom tree fallback.

- Shared dialogs now bind Home and End to the first and last visible controls. Under the standard dialog layout, Home enters the first content row and End enters the transparent action row at its final button; descendant-specific Home/End behavior still takes precedence. Input Help, the general tutorial, Civilopedia, README, UI-manager documentation, localization, and changelog use general player-facing first/last-control wording. Localization XML parsing, deterministic Civilopedia generation, binding/label assertions, and scoped `git diff --check` pass; no callable Lua parser is available. In-game testing should cover Home and End from content and action buttons, a dialog with hidden content/actions, and a dialog containing a List or EditBox to confirm its specialized Home/End behavior remains local.

- The former tooltip-only section viewer is now the Focused Widget Reader. Every focused widget caches its eligible speech content in canonical `BuildSpeech()` order while excluding role, state, and position; on-demand reading bypasses normal speech suppression for the included content. Hard `[NEWLINE]` boundaries keep each element in a separate reader section. Alt+Up/Down move to and read the previous/next section, while Alt+Home/End jump to and read the first/last section. The old `StaticText` exception is removed. Direct comma-separated `table.concat` calls in 40 UI screen contexts now use `[NEWLINE]`; UI-manager composition and utility/helper/logic/category modules remain unchanged except for the Focused Widget Reader itself. Helper/module names, cached fields, input-help localization, README, UI-manager/API docs, Main Menu tutorial, Firaxis tutorial advisor text, Civilopedia localization/source generation, and changelog use the new name and behavior. Player-facing instructions explain that the reader lets players hear detailed information about the focused control one section at a time. The interrupted first bulk pass was fully cleaned up before the screen-scoped conversion. XML parsing, deterministic Civilopedia generation, exact reader/UI-manager scope assertions, malformed/unquoted-token scans, exhaustive screen-separator audit, and repository-wide `git diff --check` pass; no callable Lua parser is available. In-game testing should cover buttons, value widgets, table cells, containers, StaticText, and representative multi-item screen text with tooltip speech settings both enabled and disabled.

- Technology and Civics Trees now include a third Graph view backed by the first-class `GraphWidget`. The initial in-game switch froze because a GraphGroup hidden predicate called `GetVisibleChildren()` while each child visibility check recursively queried that same parent; the group predicate is removed, and graph builders create groups only when they contain nodes. The widget derives the active node from the UI manager's focus path; Right/Left follow dependents/prerequisites, Up/Down traverse the relationship set produced by the last edge move, Home/End select its bounds, and Ctrl+Home/Ctrl+End select graph roots. Matching Civ V, a Left move that lands on a root changes the alternative set to all roots; non-root ascents retain all prerequisites of the departed dependent. Both screens expose a Grid, Graph, Tree View dropdown and matching Alt+1, Alt+2, Alt+3 shortcuts, persist their selections independently as `UI/TechTreeViewMode` and `UI/CivicsTreeViewMode`, use the same main-list label for all three view containers, and reuse their live node actions, filters, search, unlock details, era grouping, and focus-key restoration. Tutorials, Civilopedia source/generated data, UI-manager/API docs, localization, and the changelog are updated. Static validation passes; immediate in-game retesting is pending for opening Graph without a freeze, saved-view reopen behavior, the Pottery/Writing root-swap and Mining/Masonry/Construction multi-parent routes, dropdown/shortcut switching and focus retention, search/filter rebuilds, actions, base/expansion rulesets, and shuffled/scenario trees.

- Heroes Recall with Faith failed its first in-game test because the CAI parent popup tried to call `RecallHero(...)` from the separate child-panel Lua context. The accessible action now reproduces vanilla's live Faith purchase request with the hero unit, standard formation, and freshly resolved origin city. In-game retesting must confirm the purchase succeeds and the Great People panel closes.

- The Heroes tab of Great People now defaults to a sortable ten-column table: Name, Status, Lifespan, Movement, Strength, Ranged strength, Attack range, Charges, Passive abilities, and Commands. Stats come from the official era-aware `GetHeroUnitStats()` helper; resolved numeric zeroes remain visible, while unavailable stats sort nil-last. Passive/command cells include every localized name and description and sort by count. Status sorting now uses five semantic bands: discovered/unclaimed, yours/alive, yours/deceased, another player's/alive, and another player's/deceased; reverse sorting reverses those bands, and the shared table/list descriptions state the actual endpoints. Alt+1 selects the table, Alt+2 selects the retained detailed list, and `UI/GreatPeopleHeroesViewMode` remembers the view. List mode has one sort dropdown generated from the table columns and no filter; both views share the selected sort and stable hero-class focus. Enter/Space retains Look At hero/origin-city behavior, Shift+Enter opens Civilopedia, and Recall with Faith remains contextual after the view. Exact column/order, table rebuild, list-only sort/no-filter, shared metadata, view persistence/bindings, action/focus, explicit comparator, localization XML/coverage, deterministic tutorial generation, and scoped `git diff --check` assertions pass; no callable Lua parser is available. In-game testing should cover all ten columns and sorts—especially both five-band Status directions—zero/nil stats, every hero status, passive/command text, list tooltip/dropdown, Alt switching and persistence, notification-directed focus, Look At, Civilopedia, and recall.

- Global Resources now defaults to a sortable table with Resource, Class, Total amount, and one live amount column per local or met major civilization; absent ownership is shown and sorted as zero. The missing initial `DataTable:Rebuild()` found by the first in-game test is fixed, so headers and rows are now constructed when the report opens. Player cells retain the Mercantilism-gated Monopolies control detail, and Enter/Space in a player column uses vanilla leader activation to open Diplomacy. Alt+1 selects the table, Alt+2 selects the retained class/resource/owner tree, and `UI/GlobalResourcePopupViewMode` remembers the view. Tree mode begins with one Order resources by dropdown offering natural order, both name directions, lowest/highest Total amount, and lowest/highest amount for each current player column; its Total amount option shares the table header's localization. The former vanilla Sort By and Order Civilizations By mirrors are removed. Localization XML parsing/coverage, deterministic tutorial generation, structural assertions, and scoped `git diff --check` pass; no callable Lua parser is available. In-game retesting should cover initial table population, table headers and zeroes, dynamic known-player columns, diplomacy activation, monopoly/control details, dropdown options and sorting, view persistence/focus, and base versus Monopolies rulesets.

- The Governors screen now defaults to a five-column sortable table: Name, Status, the vanilla establishment-speed statistic, the vanilla identity-pressure statistic, and Earned promotions. The two stat headers read the same live vanilla control tooltips used by tree rows, falling back to `LOC_GOVERNOR_TRANSITION_STRENGTH_TOOLTIP` and `LOC_GOVERNOR_IDENTITY_PRESSURE_TOOLTIP`; their sort-dropdown labels inherit that exact text. Only the Name cell tooltip carries the governor's title, description, and base ability, so moving across other columns does not repeat it; earned promotions begins with the non-base earned count and follows it with each promotion name and description. Status distinguishes established city, establishing city and remaining turns, needs assignment, neutralization and remaining turns, appointed Secret Society governors, appointment availability, and unavailability. Alt+1 selects the table, Alt+2 selects the retained detailed tree, and `UI/GovernorPanelViewMode` remembers the view. Tree mode begins with an Order governors by dropdown offering natural order and both directions for every column; it shares the table-header sort, including semantic active/unavailable Status order and nil-last Secret Society establishment/identity-pressure values. Opening now explicitly focuses the active table or tree, so remembered tree mode starts in the governor tree rather than its preceding sort dropdown. Both views keep the same title-count label, stable governor-index focus, promotion grid/list updates, and vanilla appoint/assign/reassign behavior. In-game retesting should cover both expansions, Secret Societies, World Congress read-only mode, every status, all sorts and nil-last behavior, live vanilla stat-header wording, Name-only tooltip speech, promotion counts/details, row activation, initial table/tree focus, dropdown focus, Alt switching, view persistence, and focus preservation through appointment, assignment, neutralization, and promotion refreshes.

- Every sortable `DataTable` column now declares semantic descriptions for both directions. Active headers across Governors, Great People, City-States, City Status, and Units state the actual order—such as A to Z, nearest first, ready first, active first, strongest first, lowest progress first, or most first—instead of generic ascending/descending wording. The Governor, Great People, City-States, and Units alternate-view dropdowns build from the same column metadata; the fixed-direction City Status list dropdown now states its actual direction explicitly. Five-table metadata coverage, four shared-dropdown consumers, the fixed-direction City Status options, removal of stale generic labels, Name-only Governor tooltip assignment, localization XML parsing/coverage, deterministic tutorial generation, and scoped `git diff --check` pass. In-game testing should cover every header cycle and dropdown option, including natural order, dynamic Great Person/City-State columns, nil-last values, and the absence of Governor description speech outside Name.

- Great People now defaults to a sortable progress table with civilizations as stable player-id rows and one dynamic class-id column per current timeline entry. Great Person headers expose name, class, era, passive/active effects, charges, usage requirements, and the local earn restriction; biography remains only in its existing read-only edit box. Cells compare that civilization's current points, recruit cost, and per-turn rate, while vanilla's anonymous unmet-player labels are preserved. Enter/Space and Delete bubble from data cells to table-level column-directed Recruit/progress and Pass actions; sortable headers retain their own activation. Focusing a Great Person column updates the existing contextual buttons and biography. Alt+1 selects the table, Alt+2 selects the retained detailed tree, and `UI/GreatPeopleViewMode` remembers the choice. The tree's Order Civilizations by dropdown exposes natural order, an explicit Civilization name sort, and ascending/descending options named by Great Person class rather than the detailed person header; both views share the selected sort. The original table/tree and revised dropdown labels, focus, ordering, directions, view switching, and dynamic-candidate refresh passed in-game testing.

- Units List retest found severe Charges-sort lag, a mislabeled combined strength value under Melee Strength, labeled health text instead of a bare value, and blank zero-valued stats. The table now has 15 exact columns: Name, Direction/Distance, Activity, current Health, Current moves, Maximum moves, Melee strength, Ranged strength, Bombard strength, Religious strength, Anti-air strength, Range, Charges, Promotions, and Adjacent enemies. Every resolved numeric zero is displayed. The strength design is verified against Civ V Access's Military Overview: that table independently reads/sorts raw base combat and ranged strength and renders zero, never a sum or maximum; Civ VI applies the same rule to its five distinct native strength getters. Activity sorting uses Civ V's three-band shape but keeps its rank consistent with Civ VI's displayed value: Ready is band 1; Embarked, Automated, Healing, Sleeping, Skip Turn, Fortified, and queued Moving are band 5; Not Ready is band 9. The visible fallback and rank both use `IsReadyToMove()`, so active-route traders, assigned spies, zero-movement units with remaining actions, and other nontraditional units cannot receive a hidden rank that contradicts Ready/Not Ready. Sentry, Intercept, and generic `ACTIVITY_OPERATION` do not create undisclosed active-band distinctions. Activity returns the scalar numeric key `band * 100 + localized status ordinal`, guaranteeing contiguous bands and statuses through both existing comparators; the failed intermediate compound Lua-table key is removed. Moving is the only new general activity-status localization. Sentry/Intercept are not spoken because vanilla references their labels only in a commented debug block, and the invented Busy status is removed. The shared helper covers summaries, Shift+3, flat-list tooltips, and the table. Shared summaries order combat stats and adjacent enemies before the Promotions bucket's level, experience, and promotions. Charge sorting uses direct live charge APIs instead of heavyweight `ReadUnitData`; list sorting decorates one key per record; only the active view rebuilds, and table sorts no longer rebuild the hidden list. Trade/spy database flags accept both Civ VI boolean encodings, and stale removed sort keys fall back to Name. The outer panel is labeled Units through vanilla `LOC_TECH_FILTER_UNITS`; the table and list labels resolve live to the selected category filter. The table still follows Reports City Status and City-States cell speech: Name is only the unit name, Direction alone has a numeric-distance tooltip, and only flat-list items use the shared summary tooltip. Category filtering, Alt+1/Alt+2 views, actions, separate move sorts, stats-only summaries, and Shift+0 adjacent enemies remain. Scalar-key/state/readiness assertions, verified localization coverage, exact-file LuaLS, and scoped `git diff --check` pass. Full in-game retesting must cover active-route traders, spies, nontraditional-unit activity bands, both Activity sort directions, Charges/Activity/Adjacent-enemies performance, Moving transitions, category-dependent view labels, summary order, all 15 columns and sorts, numeric zeroes, filters, focus, and rulesets.

- Shared `DataTable` navigation now binds Shift+Home and Shift+End to the first and last visible column on the focused row, including headers, while preserving plain Home/End vertical behavior. Static validation passes; in-game testing is pending on header and data rows, including edge columns.

- `DataTable` activation belongs to its transparent, localized `TableRow` container. Registering `row_activate` before `Rebuild()` gives that row Enter/Space bindings which receive bubbled input from every cell; the obsolete per-column `activatable`/`role` fields and `cell_activate` event are removed. Sortable headers and actionable data remain `TableCell` widgets instead of becoming Buttons. City Status's Name cell exposes its exact composite row label, so Up/Down deduplicates the cell value and speaks the city row label once. The user confirmed Enter activation works in-game. Cell/row role localization, row-owned/direct-cell activation, listener-gated row bindings, header-owned sorting, Name getter/row-label equality, stale-API removal, tutorial XML/generation, and scoped `git diff --check` assertions pass. Remaining in-game checks are Space activation from several columns, cell/row identification in Input Help, header sorting, and Name-column vertical speech.

- Shared `DataTable` ascending sorting is fixed. The comparator now branches explicitly between `< 0` and `> 0`; the former compact boolean expression returned true for both unequal directions while ascending and violated `table.sort`'s ordering contract. The four-case direction truth table, stale-expression assertion, localization XML, and scoped `git diff --check` pass. In-game retesting should cover ascending and descending numeric and localized-string columns in City Status and City-States, equal-value stability, nil-last behavior, and the natural-order third cycle.

- Reports City Status now defaults to a sortable `DataTable`, with the prior detailed city-button list on Alt+2 and the table on Alt+1. `UI/ReportsCityStatusViewMode` remembers the view with invalid values falling back to table. Fixed columns compare name, cursor-relative direction/distance, population, housing, growth, amenities, happiness, war weariness, defense, and damage; expansions add loyalty, and every live `GameInfo.Yields()` entry adds a yield column. Row labels append Under Siege only when active plus the expansion governor's assigned/traveling state or no-governor state, replacing the redundant Status and Governor columns. Distance is the second column, uses four-coordinate `Map.GetPlotDistance(...)` for sorting, and reads direction through `CAIHexCoordUtils.directionString(...)`; the list dropdown also supports nearest-first Distance. The row-owned action lets Enter/Space from any cell activate the city, matching the list's Enter behavior; view switching preserves the selected city, and the detailed tooltip/sort yield work remains. Localization/tutorial XML, deterministic Civilopedia generation, setting/default/validation, exact table/dropdown order, four-coordinate distance, nearest-first list sort, view/binding/focus, dynamic-yield, and scoped `git diff --check` assertions pass; no callable Lua parser is available. In-game testing must cover both views, persistence, all columns and header cycles, row-label status/governor wording, distance from several cursor positions, Alt switching/focus preservation, city activation, every list sort, base/XP1/XP2 conditional columns, and refresh after city changes.

- Production Panel false retention after successful ordinary district/wonder placement is fixed. Placement-exit reconciliation now removes the retained CAI root synchronously when vanilla's ProductionPanel context is already hidden; only the still-visible animation case uses the existing deferred update. The open handshake remains unchanged and waits for vanilla's final `ProductionPanel_ListModeChanged(...)`, ensuring the correct tab is established before CAI pushes. A complete base/Babylon close-path audit confirms that every visual close routes through vanilla `Close()` and its central `ProductionPanel_Close` event; purchased-district buttons also call `Close()` after entering placement and are covered. The separate context-shutdown path now unregisters CAI listeners and removes the CAI root without using show/hide handlers. Targeted lifecycle/shutdown assertions and scoped `git diff --check` pass. In-game retesting should complete an ordinary district, wonder, and purchased district after waiting for the visual close, cancel each placement, and complete placement from Queue/Manager flows to confirm close parity, legitimate reopen behavior, and captured-row focus.

- Navigation-cursor scoping now applies only to city management. District and wonder placement neither lock cursor movement nor move the cursor to the selected city center; their existing target navigation, validation, and information speech remain unchanged. Focused structural and whitespace validation pass; in-game testing should enter both placement modes from a remote cursor position, confirm the position is preserved, inspect and jump outside the selected city's area, place from an existing valid-target route, and confirm city management remains bounded and recenters when entered from outside its envelope.

- World Scanner region entries now use one shared tile-count policy: ordinary zones append only their currently explored member count, unexplored components append their unexplored count, and the fully disclosed Pirates and Civ Royale/Red Death overlay regions append explored out of total. Geography applies explored count before same-label direction disambiguation and never exposes a hidden landmass/ocean total. Static localization, mode-coverage, geography-ordering, stale-tag, and scoped whitespace validation pass; in-game testing should compare partially explored geography, unexplored components, ordinary lens regions, and partially explored Pirates/Red Death overlay zones.

- Navigation-cursor directional stepping now announces the localized "Edge of map" message when `Map.GetAdjacentPlot(...)` returns nil at a non-wrapping map boundary. The cursor stays on its current plot and does not emit a false moved event. Localization XML, single-tag/branch assertions, scoped `git diff --check`, and the documented vanilla nil-check pattern pass; in-game testing should step into each reachable map edge and confirm wrapping maps still cross their wrapped axis normally.

- City-States defaults to one sortable comparison table followed directly by the envoy/action controls; Alt+2 retains the original expandable tree and Alt+1 returns to the remembered table view. Tree view begins with an Order city-states by dropdown generated from every current fixed and dynamic table column, with natural order plus ascending and descending options; table headers and the dropdown share one sort, and disappearing dynamic columns fall back to natural order. Fixed columns cover city-state, type, Your relationship, Envoys with all 1/3/6/Suzerain bonus breakdowns, current suzerain, Active quests with count plus description-only entries, and unified Foreign relationships as the final fixed column before dynamic influence. Quest titles and rewards remain in the alternate tree. The table's Suzerain breakdown uses the complete vanilla text without appending the separate tree-only unique-bonus/resource synthesis, preventing repeated unique bonuses and resource lists. Foreign relationships combines every met non-local major and city-state, lists all at-war entries first with alphabetical ordering inside both groups, excludes the selected city-state itself, and sorts rows by visible relationship count. Known-major and anonymous-unmet influence columns remain dynamic. Envoys and quests retain numeric sorting, and table data-cell Shift+Left/Right and Ctrl+Enter retain their envoy actions. Exact-file LuaLS, tree-sort wiring/comparator/dynamic-option assertions, XML parsing, localization uniqueness, tutorial generation, and scoped whitespace checks pass. In-game retesting must cover dropdown labels/options/focus, every fixed and dynamic sort in both directions, table/dropdown synchronization, dynamic-column fallback, complete cell wording and war-first ordering, description-only quest cells, nonduplicated Suzerain details, local/self/unmet relationship exclusion, bonus activation changes, quest refresh, table/tree switching, envoy controls, focus restoration, type-to-find, and base/XP1/XP2/scenario rulesets.
- Tutorial Setup now omits its accessible Back action, activates both leader choices through the live vanilla controls' `DoLeftClick()`, and uses the End Game Menu cinematic title/skip localization for a dedicated intro-movie panel whose Skip action clicks the vanilla movie container. Structural assertions and targeted `git diff --check` pass; in-game validation is pending.
- Active English Firaxis tutorial advisor messages and callout bodies use cumulative CAI-specific instructions. The in-game localization SQL overrides 104 active `AdvisorText.xml` body tags while retaining vanilla headers and gameplay explanations. Information bindings are spread across the lesson where each applies, including city Production (`Shift+2`), border growth (`Shift+4`), population (`Shift+6`), and tile movement cost (`X`); required Choose Production routing uses the sole-blocker `Ctrl+Space` path, and the first directed exploration move no longer suggests quick movement. The Scanner lesson now follows the Barbarian attack and teaches only hierarchy navigation, `End` direction/distance, and `Home` cursor jump. Tutorial history remains cumulative, while the contextual Tutorial Scanner category separately exposes only the active world callout's header and plot and clears it at item transitions. The Culture icon recognizes adjacent “Cultural” as an alias, preventing “Culture Cultural growth.” Civics correctly introduces the default era/tier grid and alternate tree before Technology reuses that layout and introduces tooltip reading. SQL execution, all 104 original-tag resolutions, localization XML, targeted LuaLS, scanner lifecycle assertions, mod registration, and `git diff --check` pass. Player wording and in-game Scanner testing remain pending.
- Notification Center and Tutorial Goals no longer set `InputContext.Shell` directly; the UI manager owns context switching. Notification Center now explicitly includes `Civ6Common`, providing the context-local `IsTutorialRunning()` used by its temporary tutorial always-receive registration instead of crashing before `mgr:Push`. Targeted LuaLS, include-order/no-direct-context assertions, and `git diff --check` pass. Tutorial Goals still receives its hotkey but returns without building after load because its CAI mirror is empty; vanilla restores private goal state from `UI.GetGameParameters():Get("TutorialGoals")`, while CAI only listens for new goal events. The Goals fix must reconstruct that persisted state and may also need tutorial always-receive registration once the list can push. Notification Center needs an in-game opening/input/close retest.
- Tutorial Warrior selection locking is disabled: `TutorialUIRoot_CAI` clears any restored private lock and makes `LockUnit` a no-op. Vanilla performs `TutorialCheck("UnitSelectionChanged")` before its forced deselection, while separate operation and map-movement restrictions still constrain the unit, so this removes the formation-step comma/period skips without advancing tutorial items early. Targeted LuaLS, sole-call/event-order/restriction assertions, and `git diff --check` pass. Needs in-game validation through the fortify, formation, and second-city steps.
- The intended Religion Screen close step now receives input. `FIRST_PANTHEON_I` temporarily registers `/InGame/ReligionScreen` as always receiving tutorial input, allowing its enabled `ModalControls` Escape path to raise the expected panel-closed event; the shared detailed-item cleanup removes the registration immediately afterward. The complete active scenario was audited after removing Firaxis's commented-out examples: all seven Production chooser items are already registered, the Civics and Government special cases remain covered, Trade Route Chooser enables its root, and the user confirms Research, Technology, and Civics interaction routes already work without broader registration. No other active screen item has this uncovered mismatch. Targeted LuaLS, active-item coverage, permission/context/cleanup assertions, and `git diff --check` pass. Focused in-game validation is pending at the end of the first-pantheon sequence.
- City Change Production no longer bypasses the tutorial rail. CAI previously raised `CityPanel_ProductionOpen` directly whenever the live `ChangeProductionCheck` was visible/enabled, but Firaxis's overlay blocks input without changing those control properties. The closed-panel path now also requires the published `ChangeProductionCheck` permission and announces the existing tutorial opening restriction when denied. Intended city-production steps explicitly enable that hash, while Action Panel blocker flows use their separate vanilla path. If Production is already visible, the same action applies the shared screen-close policy and explains a denied close. Targeted LuaLS, scenario-route/order assertions, and `git diff --check` pass. Focused in-game testing should try the City Change Production action/list entry during an unrelated instruction, each intended city-production step, an Action Panel production blocker, and free roam.
- Unit actions and movement no longer use detailed tutorial `EnabledControls` as an allow-list. The Unit Action List and direct unit-action hotkeys rely only on vanilla's live `action.Disabled`; quick movement and final Move To confirmation read the same serialized `UnitActionRestrictions` written by Firaxis `DisableUnitAction`. `FOUND_FIRST_CITY` now disables `UNITOPERATION_MOVE_TO` specifically for `UNIT_SETTLER` on activation and re-enables it on deactivation, preventing only the initial Settler from leaving its required founding plot. The former completion-event movement inference is removed. Targeted LuaLS, all-three-path restriction assertions, lifecycle checks, and `git diff --check` pass. Focused in-game testing should confirm all three paths are blocked for the first Settler, then usable for every Scout, Warrior, Builder, and later Settler instruction.
- Tutorial screen closing now follows one shared phase-aware policy. Research/Civics Choosers, Government, Production, World Rankings, Great People, Diplomacy, Trade Route Chooser, the Technology/Civics Trees, and Pantheon/Religion block full-screen Escape while an instruction owns them and announce that the tutorial is preventing the screen from closing. The dedicated close message replaces the incorrect opening-hotkey failure text. The policy reads vanilla's enabled controls, so the intended `TechTreeModal`, `CivicsTreeModal`, and `ModalControls` close steps work without item-ID exceptions, and every screen closes normally when `ActiveItemId` is nil during free roam. Nested Escape operations such as filters, pickers, dialogs, conversations, and type-to-find run before the full-screen gate. Production's visual close button is also gated, while valid production selection retains its normal programmatic close. Targeted LuaLS for all affected files, localization XML parsing, close-gate announcement coverage, and `git diff --check` pass. Focused in-game testing should attempt premature and expected Escape on each tutorial-opened screen, confirm the close-block explanation, then verify normal closing during free roam.
- Tutorial callout/dialog routing is implemented. Native callouts inside opened screens become accessible header/body dialogs with vanilla OK and enter Tutorial history silently. Runtime retesting showed recursive context discovery classified HUD pointers as screen controls, so native IDs now authoritatively identify `TutFoundCityAction`, `TutOpenProduction`, all `TutSelectEndTurnAction*` controls, LaunchBar/TopPanel/PartialScreenHooks pointers, and UnitPanel action pointers as ordinary spoken HUD messages. Advisor and callout Enter leakage is handled by Enter down/up focus ownership only for focused paths with a matching CAI Enter binding. This rejects duplicate key-up delivery after synchronous focus changes without globally consuming raw Enter releases used by `InputActionTriggered`; the earlier global rule prevented Move To destination confirmation. Focus transitions and widget destruction cancel ownership when its leaf leaves the active path, covering vanilla contexts such as Tutorial Setup that consume the movie-skip key-up before CAI receives it. Dialog presentation and detailed input registration remain synchronous. Callouts still wait until delayed screens such as Pottery ResearchChooser have pushed, and world-anchor text/location still suppresses an exact same-text control candidate. Targeted LuaLS, input-route assertions, and `git diff --check` pass. Focused in-game testing should verify warrior Move To destination confirmation, the first leader-button Enter after skipping the Tutorial Setup movie, Alt+M immediately after advisor dismissal, repeated Enter presses, settler selection and Escape in the world, Mining/Pottery screen dialogs surviving advisor Enter, dialog OK returning focus without activating the screen, HUD callouts remaining non-dialog messages, `CITY_DEFENSE_B/C` speaking once with location, and production OK no longer collapsing its first category.
- Tutorial permissions distinguish advisor, detailed, and free-roam phases for CAI-only screen-opening and production paths. An active non-detailed advisor blocks those bypasses; no active item allows them normally even though the tutorial remains running. City Details checks `ToggleOverviewPanel` in both its City Panel action-map entry and central show boundary, preventing the uncloseable out-of-rail panel while allowing it in free roam. Top Panel gates every Reports-opening action with `LaunchBar_Hook_Reports`, and ReportScreen repeats the check centrally and clears denied pending focus. Non-opening readouts remain available. Ordinary unit actions and movement no longer use this enabled-control policy. Targeted LuaLS, phase/gate assertions, and `git diff --check` pass; test City Details and every Reports shortcut during an advisor, a detailed step, and post-chain free roam.
- The first tutorial playthrough's Production regression is fixed and the second-city retest passed. The installed scenario's missing warrior-action cleanup is also repaired in `TutorialUIRoot_CAI`: its `FOUND_SECOND_CITY_D` activation hook calls vanilla `EnableUnitAction` for warrior Move, Heal, and Cancel after the scenario unlocks units. This updates persistent tutorial restriction state and the final Unit Panel action records while preserving tutorial-wide Skip Turn, Delete, and automation restrictions. Targeted LuaLS, hook/action invariants, and `git diff --check` pass; retest the warrior after accepting the second-city/free-roam advisor.
- Hall of Fame return-state synchronization is fixed. Opening Game Details hides the parent; vanilla return still selects ruleset 1, rebuilds data, selects Overview, clears the selected history game, and preserves its sort function/direction. The retained CAI panel now follows every corresponding vanilla refresh: ruleset options and live selection, active tab, sort selection, Overview tree, and History list. Dropdown callbacks are registered only once during construction. Details now removes its CAI root with silent next-root restoration so the synchronous vanilla Overview reset becomes the first announced return focus instead of briefly announcing the obsolete History position. The matching tutorial and generated Civilopedia article describe the refreshed default-ruleset/Overview return and preserved sort method. Structural/tutorial validation, the single scoped silent-removal caller, manager annotations, and `git diff --check` pass. Focused in-game retesting should use a non-default ruleset and sort, then confirm Escape announces Overview directly.
- Hall of Fame game-detail Graphs remain eagerly constructed and must never use lazy expansion. Their tree is now player -> turn containing at least one change -> values changed on that turn. Each dataset contributes its first non-nil value and later values only when unequal to its last recorded value, omitting carry-forward repetitions; city/religion ownership and entity naming remain intact. Read-only database analysis showed the largest stored game falling from tens of thousands of eligible samples to about 11,000 actual changes across roughly 1,500 player-turns. The matching tutorial and generated Civilopedia article are updated; eager/no-lazy and change-comparison assertions, all 204 tutorial references, XML parsing, deterministic generation, and `git diff --check` pass. In-game retesting must confirm that the formerly freezing History entry now opens responsively and that only changed turns/messages appear.
- The Global Resources tutorial now documents the implemented leader-row action: Enter calls vanilla `OnLeaderClicked`, closes Global Resources, and opens the Diplomacy view for that known leader. Its generated Civilopedia article has the same five ordered paragraphs; all 204 tutorial localization references resolve without duplicates, both source XML files and the generated XML parse, implementation-path assertions and deterministic generation pass, and `git diff --check` passes. Focused in-game wording review is pending.
- The Espionage Chooser tutorial has been rewritten from `EspionageChooser_CAI.lua` and the installed vanilla chooser. It now treats destination Tree and mission List as mutually exclusive modes; documents city labels/timing/districts and read-only mission previews; distinguishes CAI's placement confirmation, reselect, travel, and the same-current-city Counterspy transition; and explains Gain Sources, enabled/disabled offensive missions, the separate vanilla mission-confirmation popup, and immediate Counterspy assignment. All 203 catalog localization references resolve without duplicates; the generated article has the expected nine ordered paragraphs, obsolete text is absent, implementation-path assertions pass, both XML files parse, and `git diff --check` passes. Focused in-game wording review is pending.
- Reports tutorial routing now consistently uses the stable `CAIReports_Panel` root ID in panel construction, stack lookup/removal, and the tutorial catalog; the stable `CAIReports_Tabs` ID is also passed directly at construction. `CAIRPT_` remains only for transient child IDs. Static validation passed, and the user confirmed the Reports tutorial now opens in-game.
- The Main Menu tutorial's first paragraph now welcomes the player, explains that tutorials introduce accessible screens when first encountered, and identifies both the dialog checkbox and Mod Settings as ways to disable them. The localized row is confirmed as the first tutorial and Civilopedia paragraph; XML parsing and `git diff --check` pass.
- World Congress contextual tutorials now provide stage-by-stage instructions for ordinary resolutions, active special-session votes, and selecting a future emergency proposal, including exact row order, outcome/target behavior, Favor costs, blockers, review, and submission. Religion Setup guidance now covers founding and enhancement identity, belief-picking, validation, and confirmation while explicitly routing pantheon choice to the standalone chooser. Generated Civilopedia data is deterministic; both XML files parse, all 199 catalog localization references resolve, the expected 6/5/4 World Congress and 10 Religion paragraphs are present, stale tutorial tags are absent, and `git diff --check` passes. Focused in-game wording review is pending.
- Frontend Civilopedia feasibility is documented. The stock screen is instantiated only by the in-game root, and its schema/data live in the gameplay database rather than the frontend configuration database. A small transplant is therefore not viable. A frontend-owned browser is possible; a limited version serving CAI's custom `Simple` articles is practical, while a complete ruleset- and mod-aware encyclopedia would require a substantial mirrored/custom data layer and cannot naturally know the final gameplay database before game creation.
- The event-driven mod tutorial framework passed its initial focused in-game test, but the first broad catalog review found sparse and inaccurate screen descriptions plus a Production Queue focus race. Every cataloged screen has now been re-audited against its CAI implementation. Tutorials describe the actual control order, child hierarchy, alternate grid/tree presentations, contextual actions, and screen-specific bindings; notable corrections cover Mods rows and saved mod groups, Technology/Civics Tree queue and search bindings, persistent Governor promotion grid/list views, deals, diplomacy, and Unit Promotions. Production Queue and World Congress contextual checks are deferred until the next context update so they open after tab/body focus settles. XML parsing, all 190 catalog localization references, tag uniqueness, mod registration, 36 unique screen definitions, targeted LuaLS, lifecycle assertions, and `git diff --check` pass. Focused in-game retesting of descriptions and the deferred Production Queue/World Congress triggers is pending.
- The revised tutorial catalog and deferred Production Queue focus behavior passed the user's in-game retest. Civilopedia data defines a dedicated `Accessibility Mod` section at sort index -10, immediately before Basic Concepts. Its 57 pages comprise the expanded Main Menu tutorial as an introduction, a 13-chapter key-binding reference, a search/type-ahead reference, a Gameplay group with 11 implementation-audited articles and 58 titled chapters matching the README Gameplay structure, a Screen Tutorials group with all 36 screen and 4 contextual tutorials, and a UI Widgets group with three inheritance-family articles. Gameplay now documents every accessible lens individually, the deliberate omission of the redundant Empire lens, reveal-announcement timing and settings, and player-facing end-turn behavior without referring to an Action Panel. The key reference explains remapping and gives every configured input-action category its own defaults chapter; in-game chat is categorized under Online. Leaf, Value, and Container articles use custom data layouts with the stock `Simple` renderer; their 19 titled chapters retain the implementation-audited widget text, with Menu Item removed at the user's direction. Abstract bases and the internal HorizontalList tab-strip implementation are also excluded. The shared Main Menu/Civilopedia introduction explains nested focus paths and panels; Edit Box distinguishes always-active and activation-required fields using the exact live tooltip hints; Dialog and Panel descriptions are corrected; and both front-end summary tutorials use the player-facing Hall of Fame and Hall of Fame game details names. The user's broad in-game Civilopedia review found only that the reveal introduction did not identify its timing and settings; that prose is corrected. XML parsing, complete localization resolution, group/page order, all gameplay and key-reference chapter-to-layout mappings, empty-chapter checks, mod registration, deterministic generation, exact widget-chapter membership, chat-category ownership, reveal-setting coverage, stale developer wording checks, and `git diff --check` pass. A focused in-game retest of the revised Reveal announcements text and Online chat category is pending.
- Era Complete/advisor overlap is diagnosed. XP1/XP2 keeps Era Complete locked at High priority while AdvisorPopup uses the higher Tutorial priority. Lowering or replacing an advisor briefly re-shows the still-queued era context, whose vanilla `OnShow()` always restarts `UI_Era_Change` and resets/replays `EraPopupAnimation`; immediately raised queued tutorial advisors repeat that priority alternation. Existing `Lua.log` confirms repeated animation starts interleaved with advisor remove/push pairs under one era lock. The latest tutorial session also has one independent load-time runtime error at `EraCompletePopup_CAI.lua:42`: `ContextPtr:RemoveInputHandler()` is not a ContextPtr API (only `SetInputHandler` is documented). No fix has been implemented.
- End Game Menu Replay is now a three-level Tree: player, non-empty turn, then every recorded replay dataset/value for that turn. It uses the complete coalesced turn range, preserves scenario dataset filters and fractional precision, omits players/turns without values, and intentionally omits vanilla's player checkboxes because they only hide sighted graph lines. Stable focus keys and subtree capture/restore are in place. Static validation passes; focused in-game testing is pending across ordinary games, sparse data, fractional values, and Royale/Pirates dataset filters. Smaller End Game semantic gaps remain the unannounced local-winner marker/leader portrait identity and team chat's color-only channel identity.
- Government-lens scanner results now use government type as their scanner group while retaining government + owner + purchasing city as the authoritative zone identity. This makes group navigation cycle government types without merging adjacent territory belonging to different civilizations or cities. Targeted LuaLS, grouping/zone-identity assertions, and `git diff --check` pass; focused in-game testing is pending.
- Tourism-lens accessibility has been redesigned around the vanilla banner contract. The Active Lens scanner now has one `Tourism` subcategory, one item per local-player tourism banner, and High/Medium/Low groups matching Firaxis's thresholds. Scanner and Space summaries say tourism value, strength, then international tourists without a banner header; the explicit lens-information action then reads the complete live `GetTourismTooltipAt()` breakdown or says that the tile has no banner. Targeted LuaLS, XML/localization uniqueness, canonical-item/tooltip-path assertions, and `git diff --check` pass; focused in-game testing is pending.
- At the user's direction on 2026-08-12, every pending in-game test, retest, regression, scenario matrix, and optional fixture check — including the Better Trade Screen integration across all three trade screens and the World Scanner quick-access slots — is complete and passed. No tests remain pending; all pending/retest wording elsewhere in this file is inactive.
- At the user's direction on 2026-07-26, every pending in-game test, retest, regression, scenario matrix, and optional fixture check is complete and passed. No tests remain pending; all older pending/retest wording in the compressed history is inactive.
- Credits accessibility is complete and passed in-game at the user's direction on 2026-07-26. The exact-body full front-end replacement provides a live package Dropdown and searchable one-level Tree; only outer sections expand, and each role plus its contributors is one StaticText block. Whitespace-only spacer records are ignored. Outside Tree focus, the visual roll narrates each visible source line at screen center; Space pauses/resumes with a click and localized state feedback. Escape closes, with no accessible Back widget. No Credits tests remain pending.
- Frontend Mods / Additional Content accessibility is complete and passed in-game at the user's direction on 2026-07-25. The full vanilla-copy replacement uses one post-`PostInit` open chokepoint; installed mod rows provide sighted selection, expandable details, enable/disable and complete failure feedback, native name/teaser search, concise sort/filter/group controls, and bulk actions. Tabs click their live vanilla controls. Subscriptions use a non-expandable name-only List with status/date tooltips, Update/View/Unsubscribe actions, one-press optimistic removal with stale Steam-result suppression, and Browse Workshop. The group-name dialog drops native edit-box focus. Vanilla `Ownership` is the official DLC entitlement boolean, and only true Dependencies—not References or Blocks—appear. Vanilla parity, lifecycle, control order, search, localization/XML, registration, and `git diff --check` pass. No Mods-screen tests remain pending.
- The accessible Main Menu Multiplayer submenu now omits the Civ Royale and Pirates matchmaking/Play controls because matchmaking disables CAI. Each scenario is represented only by its direct Help button, labeled from the live `View a quick tutorial on...` tooltip. Both front-end tutorials now hide Previous on their final page, relabel Next as Close, and close without raising matchmaking. Static verification is complete; focused Main Menu and final-page testing is pending.
- Pirates scenario accessibility is implemented. All overlapping CAI contexts compose the official scenario scripts; the unexported Minimap script has a collision-safe normalized copy. The accessible layer now covers the three-slot relic screen and relic popup, all shared/faction unit actions and six target modes, live Crew values, naval-repair filtering, Crew Morale on T, pirate-only ribbon scores, official treasure/infamous/fleet plot details, known map zones and relic sensor targets in World Scanner, Tavern/Sack banner status, pause-menu How to Play, the in-game Expansion Intro, and the separate four-page Main Menu Pirates Intro. Main Menu matchmaking is intentionally unavailable because it disables CAI. Authored Lua files pass targeted LuaLS; modinfo/localization XML, localization uniqueness, Minimap parity, front-end vanilla-body parity, and `git diff --check` pass. Focused multiplayer/Hotseat and shared-context regression testing is pending.
- First Pirates in-game results found three output bugs, now corrected: Dowsing Rod used the UI-invalid two-index distance overload and announced a plot-sized number; ribbon player names skipped `Locale.Lookup`; and T reused the icon-bearing Crew Morale title. Sensor rows now identify their source ship and use four-coordinate distance. At the user's direction, T says only `Crew morale`, `{balance} gold`, `{net} per turn`, then preserves the original scenario status/mutiny string. Targeted LuaLS/XML/static validation passes; focused retesting is pending.
- Second Pirates results exposed a target-cache race and two consequences of scenario-specific location storage. Shore Party's mode-enter callback fills `g_targetPlots` after CAI can cache an empty table, so plot detail said invalid while the later scanner/click path was valid; Pirates target lookups now rebuild from the live table. Search-zone membership now uses four-coordinate distance, restoring infamous-pirate regions to the Pirates scanner. New Infamous Pirate notifications intentionally have no standard location and carry a zone ID instead; their custom camera activation now also moves the CAI cursor to the live zone center. Treasure-fleet paths are confirmed to be projected routes from active colonial fleets to the Europe exit; the route is public even when its ships are not visible.
- Pirates scanner presentation now has exactly Treasure and Enemy activity subcategories. Scanner items use clear search-area, fleet-path, treasure-location, and relic-signal names instead of scenario flavor lines. Cursor-follow and B geography put those clear names before fog status, while official flavor remains additional plot detail; CAI supplies four-coordinate fallback membership when Firaxis's tooltip wrapper misses a hidden treasure-search plot. The default category order places Red Death and Pirates immediately after Geography; persisted custom ordering changes only through reset or Move to default position.
- Copied Firaxis replacements omitted from their source DLC imports are now organized under `UI/Replacements/<source DLC>`: `Byzantium_Gaul`, `BlackDeathScenario`, and `CivRoyaleScenario`. The five normalized-exact Royale copies no longer sit beside authored in-game CAI files. All eight files exist only in their DLC folder and have exactly one top-level plus one criteria-scoped import registration; modinfo XML, old-path absence, normalized Royale/Firaxis parity, and `git diff --check` pass.
- Red Death End Game observer re-entry now wraps the final context `OnShow` path. The scenario's observer branch bypasses `BASE_OnShow` and `ShowComplete`, so the previous CAI hook never repushed the panel destroyed by Observe. A visible Royale observer now restores always-receive-input state and rebuilds/pushes CAI exactly when no panel is built. Targeted LuaLS, lifecycle assertions, and `git diff --check` pass; retest Observe through the final result.
- Red Death B geography speech now has deliberately asymmetric live detail: safe-zone plots append the cursor-relative direction to the current zone center and the official shrink countdown/minimum-size status; ordinary Red Death appends the official fallout category and damage used by T; outside plots say only `Outside of safe zone`. Automatic cursor transition speech remains the concise three-state form, and the compact cursor-move plot tooltip identifies the exact center plot as `Safe zone center`. The World Scanner adds the same center as a navigable/beacon/auto-move group under Safe zone. Supply drops, ruins, and raider outposts are removed from the Royale-specific cursor/scanner object source because ordinary tribal-village handling already exposes them. Targeted LuaLS, XML, de-duplication/nesting assertions, and `git diff --check` pass; in-game testing is pending.
- The occasional centered mechanical cue between Royale turns is the vanilla `UI_Royale_Ring_Warning`: the notification wrapper assigns it only to Safe Zone Appeared and Safe Zone Changed, emitted when a new safe-zone phase begins. It is not the ordinary every-turn Red Death contraction or a remote global-ability cue.
- World Scanner zones are implemented as a first-class item type. Connected Geography land/ocean, Terrain unexplored/mountain, and active Continent/Political/Government/Power-range lens plots now collapse into six-neighbor regions. Unexplored regions match Civ V presentation under Terrain/Base and refresh their live hidden-tile count; the separate Unexplored subcategory is removed. Terrain also exposes every revealed `plot:IsFreshWater()` tile independently under a Fresh water subcategory, including mountains and feature plots. Build/resort distance uses each zone's nearest member from the captured origin; the current-item boundary re-resolves from the live navigation cursor before speech, beacon, auto-move, End, or jump, refreshes zone labels, and prunes members whose reveal/classification changed. The contract is retained by All, subcategory, custom, and search leaves. Gathering Storm uses authoritative named `Territories` for mountain ranges and composes connected ocean-zone labels from revealed official ocean names, falling back to sea names when no ocean territory is present. Base game, Rise and Fall, scenarios, and rulesets without that API use generic range/ocean labels. Landmasses use only revealed continent composition, `Home landmass` for the capital/first-city component, home-relative direction for duplicate labels, and numbering only as a final collision fallback. Geography defaults immediately after Terrain, including migration of saved layouts that do not yet contain Geography. XML, registration, targeted Lua Language Server, and static architecture checks pass.
- At the user's direction on 2026-07-24, all pending in-game tests, retests, regressions, and optional fixture checks are closed as successful; older pending-test wording in compressed history is not active work. World Scanner terrain emits every revealed mountain exactly once as a generic Mountains item under Elevation, preventing base terrain, features, or terrain variants from splitting or duplicating the Mountains group. Category reset confirmation matches delete confirmation: it is a trapped Dialog child of the current owning root, captures and restores the management control, and never pushes a stack root. Targeted Lua Language Server and lifecycle/grouping invariants pass, as does `git diff --check`. README documents the complete player workflow for category types, ordering, visibility, contextual settings, custom sources and name terms, deletion, reset, persistence, and focus return.
- Surveyor terrain now follows the Civ V terrain-shape model with Civ VI-specific data. Hills normalize through `GameInfo.TerrainClass_Terrains()` to their flat base terrain and add the generic `Hills` bucket; ordinary mountains emit only `Mountains`; natural wonders stand alone. Regular feature suppression is derived from `GameInfo.Feature_ValidTerrains()`: omit only a base class or non-flat elevation shared by every valid terrain for that feature. This removes redundant facts for oasis, marsh, typed floodplains, reef, and volcano while retaining useful distinctions for forest, jungle, ice, volcanic soil, and geothermal fissures; missing validity data suppresses nothing. Every revealed `plot:IsFreshWater()` result also increments a localized Fresh Water bucket independently of terrain suppression. Changelog and API notes are updated. Targeted Lua Language Server, official terrain-class/feature-implication assertions, structural checks, and `git diff --check` pass; combined in-game testing is pending.
- Surveyor appeal counting is implemented. `Ctrl+Shift+X` / `Ctrl+Shift+Numpad 2` counts revealed non-water plots within the current radius using vanilla's live Breathtaking, Charming, Average, Uninviting, and Disgusting bands. Bands sort by descending tile count with localized name as the tie-breaker; empty bands are omitted, unexplored tiles retain the shared suffix, and rulesets without the Appeal lens capability announce that appeal is unavailable. Vanilla API, hotkey, localization, README, and changelog updates are complete. XML parsing, localization uniqueness, gesture-conflict, count-order, handler-wiring, targeted Lua Language Server, and `git diff --check` validation pass; in-game testing is pending.
- Espionage Popup now builds and pushes its accessible dialog after vanilla refresh even when a Tutorial-priority advisor keeps the native Low-priority context hidden. The CAI manager retains the dialog below the advisor and promotes it naturally after the advisor queue clears. `Open()` remains the sole normal post-refresh build point, preserving the earlier duplicate/placeholder fix. Static verification passes; focused advisor-overlap testing is pending.

- Climate Screen vanilla parity and location work passed in-game at the user's direction and is closed. Event History uses vanilla's `CurrentLocation` visibility gate while displaying and jumping to `StartLocation`; sea-level turns announce the official recent Polar Ice and Sea Level update alerts; affected cities identify their owning civilizations; and CO2 contributors identify leaders plus civilizations without exposing unmet identities. Revealed non-global current events append their live navigation-cursor-relative direction and use Enter to jump to `CurrentLocation`; visible located Event History rows do the same for `StartLocation`. Both jumps keep the panel open. The Gran Colombia/Maya wrapper intentionally remains Apocalypse-only because its forest-fire localization tags are unavailable to the in-game locale database outside that mode and would be spoken raw; do not generalize this include. Targeted LuaLS, structural, localization, and diff checks also pass.
- GovernmentScreen anarchy investigation confirms a vanilla false-positive warning: activating the already-active government still calls `GetAnarchyTurns(...)` and can show the previously-adopted-government warning, but the engine treats the same-government request as no change. During actual anarchy, `GetCurrentGovernment()` returns `-1`; the P-key culture summary now checks `IsInAnarchy()` first and says `Government, Anarchy (N turns)` instead of incorrectly emitting the pre-Code-of-Laws unlock text. Static verification passes; in-game retest is pending.
- GovernmentScreen parity follow-up is implemented. Governments is now a flat List of button rows rather than a tree; each numbered row label includes its government tier, preferring XP1+ explicit tier data and falling back to vanilla's total-slot grouping in base game, while Chiefdom remains unnumbered. Each row's tooltip contains slot mix, deduplicated government bonuses, XP2 stats without an empty paragraph element, locked prerequisite with live current-civic percentage, and base-only selected-government heritage/carryover values, with Shift+Enter Civilopedia access. Base legacy output prefers the detailed formula instead of repeating its short description or raw flat value; every accumulated heritage entry identifies the effect, percentage, and source government. Expansion output uses vanilla's Major/Minor labels and omits the disabled legacy-progression section. Policy picker/catalog labels append localized `New` for policies unlocked by the completed civic and for XP1 Dark-Age policies on the official era-start condition; Dark and Golden eligibility remain in policy tooltips. Confirm Policies always uses vanilla's confirm label; while disabled, its tooltip enumerates every empty slot by category-local ordinal or explains that no policy changes were made when all slots are filled. Opening the policy picker plays vanilla's card-take cue; selection or Escape plays the card-drop cue, while Delete retains vanilla double-click-removal behavior without drag sounds. Pirates uses its own three-slot relic-only presentation described above. Targeted LuaLS, localization XML/tag uniqueness, old-tag/symbol and structure assertions, and `git diff --check` pass; in-game testing is pending.
- Great Works Overview/Showcase parity and grouping tests passed in-game at the user's direction. Slot-owning building instances now append their live direction from the navigation cursor in both hierarchies: the building row under city grouping and the city-named instance row under building grouping. Enter on either instance jumps the cursor to the building plot without closing Great Works or toggling that row; Left/Right retain expansion and collapse. Targeted Lua Language Server, hierarchy/activation assertions, and `git diff --check` pass; a focused in-game location/jump test is pending.
- World Scanner search now mirrors Civ V Access. Ctrl+F pushes the edit box immediately with no rebuild or indexing. Enter closes it, rebuilds fresh scanner data, and directly matches item names through the UI manager's newly public six-tier type-ahead classifier; there is no Firaxis `Search.*` context. Identical names are grouped into nearest-first instances, groups sort by match tier then distance, and `All` plus the original scanner categories are exposed as result subcategories. Collection walks each built-in category's authoritative implicit `All` branch once, including disabled categories, so custom projections never duplicate search results. Page Up/Page Down history remains. Ordinary tokens form one Civ V-style ranked phrase; `--term` tokens exclude names through the same matcher. There is no multi-term AND intersection or result cap. The full in-game search matrix passed at the user's direction on 2026-07-23.
- World Scanner category management and custom projection pass the initial in-game retest. The first child-Settings attempt attached the Tree directly and manipulated private root focus caches. The second correctly adopted the Input Help/Civilopedia child-Panel structure, but its close and scanner-handoff APIs used the manager's default nonrecursive `GetWidgetById`, which searches stack roots only. Because Settings was now a child, every lookup returned nil: Escape returned without closing and scanner management received no parent and refused to open. Escape now captures and closes its Panel directly like the working helpers; cross-context Settings APIs rediscover that Panel with `GetWidgetById(id, true)`. Settings otherwise follows the proven helper pattern: a transparent, wrapping, input-trapping Panel attaches to the current root, contains and directly focuses the settings Tree, and restores the previously focused widget on close. Scanner management attaches its own wrapping, input-trapping Panel to the same root, inherits that saved widget, prepares focus, then destroys Settings without restoring over the handoff. Custom delete and reset confirmations now attach as trapped Dialog children of that same root rather than pushing; cancel/Escape restores the captured management control. Delete confirmation rebuilds and focuses the surviving next/previous category, while reset rebuilds the default layout and returns to Reset. Term additions append the remove control, clear the editor silently, and retain the normal commit announcement. In-game testing showed generic root capture/restore could still leave Include/Exclude when a focused term disappeared; removal now explicitly focuses the next term, previous final term, or Add term inside the same submenu. Visible custom default names now use the first unused `Custom N` among live names instead of exposing the persisted monotonic internal id, so deleted or renamed numbers are reusable across restarts. Every management category label now appends its live Enabled or Disabled state. Custom row tooltips resolve live into Sources, Included keywords, and Excluded keywords, naming all or selected subcategories and saying None for empty sections. Player-facing descriptions use tile and categorized, and Valid targets/Active lens include concrete availability examples. Districts and constructed wonders remain merged under stable `districts`, with persisted `wonders` ids migrated. Targeted LuaLS, attached-delete/reset lifecycle, cancel/confirm focus return, and `git diff --check` previously passed.
- Reports consolidation now opens and pushes correctly from F2, while Ctrl+Y focuses Empire Economy. The first maintenance merge rendered blank values because named `{Value}` localization keys were called positionally; all player-yield aggregate labels now use Firaxis' named parameter table. Science, Culture, Gold income, Faith, and Tourism city contributions expand by city and into available city yield tooltips; Gold deal categories expand into individual deals. Tourism enriches its existing authoritative Cities row and identifies only the residual not attributed by Reports as `Other city tourism`. Gold expenses preserve every engine tooltip category and amount; Cities, Units, and WMDs only receive matching Reports children, with no manual category reconstruction or synthetic maintenance remainder. Maintained buildings are enumerated directly from each city's owned `GameInfo.Buildings` records because Gathering Storm's Reports records returned zero maintenance in the live case. Unit expense types expand into individual live units and retain Corps/Army/Fleet/Armada suffixes. Every concrete city, district, building, wonder, maintenance-instance, unit, and concrete district/building source inside a city-yield tooltip appends its live navigation-cursor-relative location. Parsed tooltip component rows are matched by their actual Buildings/Districts ancestor plus localized component name, avoiding both the failed mixed-parameter localization reconstruction and false matches such as Campus Research Grants. Activation closes Reports before jumping to the component's current plot so destination speech is not interrupted; actionable tree leaves consistently use `TreeItem`, while read-only leaves remain `StaticText`. Aggregate type headers remain expandable because they can represent several plots. Empire Economy's collapsed tooltip compactly reports Favor balance, available Envoys, and active/capacity trade routes in the same name/value style as its yields; detailed rates and influence progress remain in the children. Summary and expanded Science/Culture/Gold/Faith rates share one-decimal rounding. Arbitrary tooltip indentation is preserved. Trade routes remain last and non-expandable; trade, envoy, and Favor help text is retained. The Reports tutorial now matches this hierarchy: gold deals are under Empire Economy and resource trades are import/export sources in Resources. Retest is pending.
- Strategic-resource merge analysis from live Coal data confirms that the flow APIs add authoritative aggregates while Report entries add named city detail. Coal reports `+16` extracted versus five named `+3` city rows (`+15`), exposing an unattributed `+1`; City-State `+3`, Units `-2`, and Power `-3` are exact duplicates. Proposed merge: retain accumulation/consumption totals, nest named Report entries beneath their matching flow category, add a localized miscellaneous residual for unmatched aggregate amounts, suppress exact duplicate flow rows, and keep only genuinely unmapped Report entries in a fallback branch.
- Cultural World Rankings now nests multi-player teams, sorts teams by their best member's progress, sorts team children by individual progress, and leaves singleton players at the top level. Player tooltips contain live domestic tourists and positive turns-to-victory estimates. Only the local-player row expands, including beneath a team; its flat source-civilization leaves show live tourists visiting us plus the vanilla tourism-per-turn, lifetime-tourism, and modifier breakdown, while unmet identities remain masked. The advisor includes vanilla's victory, domestic-tourist, and visiting-tourist explanations. Gathering Storm dominance summaries and total Tourism/Culture rates remain on vanilla Overall rather than Culture. Lua Language Server and targeted structure checks pass; in-game confirmation is pending.
- World Rankings Overall now expands each ordinary victory into ranked teams/singletons and expands multi-player teams into member rows. Ordering mirrors vanilla's victory progress and average primary/secondary tiebreakers, then generic score and team id. Rows expose place, normalized progress, multiplayer human names, and local player/team indicators; known-player tooltips expose primary, distinct secondary, and additional summaries, while unmet details remain masked. Scenario-specific Overall branches remain authoritative. Lua Language Server, localization XML/tag uniqueness, hierarchy/data/masking invariants, and `git diff --check` pass; in-game confirmation is pending.
- Final non-scenario World Rankings parity pass is implemented. Score prepends the configured maximum-turn condition to its advisor. Science milestone tooltips expose Spaceport, prerequisite technology, and individual-project status; progress denominators follow vanilla's early-versus-late milestone rules; Gathering Storm's advisor includes the final light-year requirement and its progress row appears only after launch. Detailed tabs announce `You`, `Your team`, and known multiplayer human names, and Religion uses vanilla's full conversion-summary wording. Lua Language Server reports no problems; in-game confirmation is pending.
- Civilopedia governor-title prerequisite/progression rows and unit-promotion prerequisite rows are confirmed to originate as vanilla `AddLabel(...)` text with no page target, unlike technology/civic `AddIconLabel(...)` rows. CAI therefore renders them as static text by design; the destination pages exist, so adding accessible jumps is a possible CAI enhancement rather than a search-data fix.
- Ctrl+I Civilopedia lookup now processes icons and removes balanced parenthetical spans before comma-or-colon term collection and every vanilla-context search, skips terms that normalize to empty, and validates each native type-ahead candidate against the authoritative localized title returned by the game's search. Colon-prefixed labels such as technology and civic completion popups therefore expose their article title. Comparison applies the same parenthetical removal while result display retains the full title. Only processed, locale-case-folded exact title equality is retained, so partial-title and alias matches are dropped. Parenthetical query/comparison/display invariants passed previously; colon tokenization and `git diff --check` pass statically, with focused in-game confirmation pending.
- Type-to-find interaction handling passed its in-game retest. Successful widget interactions clear the query silently through the base event system, while focus leave, Backspace, Escape, ranked Up/Down, wrap-around, and timeout behavior remain unchanged. A new default-enabled UI setting includes tooltips in type-to-find: the matcher preserves the strongest label tier, appends the strongest tooltip-only tier, excludes label-matching widgets from tooltip duplication, and always ranks tooltip-only results after label results. All type-to-find settings are grouped at the bottom of the UI section. Targeted Lua Language Server, settings-SQL execution/order/default, localization XML/tag uniqueness, source-ranking invariants, and scoped `git diff --check` validation pass. Focused in-game confirmation of tooltip matching, ordering, disabling, and settings order is pending.
- Notification Center now opens as a panel with Notifications and Message buffer tabs. The buffer page orders list, category dropdown, then Copy buffer; preserves its filter across close/reopen; defaults first list entry to the newest match; formats rows as message, live scanner-style relative location, and added turn; copies only the selected filter as message plus turn without location; and presents location-bearing entries as buttons that close the panel and jump the navigation cursor. Manual buffer reads now append the same live location string after message text. The list remains focus-stable during live refresh. Lua Language Server, localization XML, API/structure/lifecycle invariants, and `git diff --check` pass; in-game confirmation is pending.
- Mod raw-audio gain now follows the live game master-volume setting while preserving per-tag, per-sound, and positional-distance gains. Lua Language Server, gain-composition/polling invariants, and `git diff --check` pass; in-game confirmation is pending.
- World scanner religious-unit ownership categories now follow theological combat: foreign units remain neutral for a Religious Alliance or the local civilization's majority religion, while every other foreign religious unit is categorized as an enemy even during diplomatic peace. Lua Language Server, API/call-site/category-precedence invariants, and `git diff --check` pass; in-game confirmation is pending.
- A default-enabled `AnnounceUnownedCombatResults` checkbox is added under Events. UnitPanel combat results continue to announce all locally involved combat plus visible non-local combat by default; disabling it suppresses results unless the attacker, defender, interceptor, or anti-air component belongs to the local player. SQL execution/default/category checks, localization XML validation, Lua Language Server, ownership/visibility wiring invariants, and `git diff --check` pass; in-game confirmation is pending.
- Unit-list leaf rows now bind Ctrl+Enter to jump the navigation cursor to the row's live unit plot without selecting it or closing the list. Plain Enter retains selection. Lua/localization/static verification passes; in-game confirmation is pending.
- Unit Captured is now wrapped with a CAI input handler. Single-player retains the vanilla popup, while multiplayer adds the localized capture message to the notification buffer only when the local player captures another player's unit. Modinfo XML parsing, registration/gate/message invariants, and `git diff --check` pass; in-game confirmation is pending.
- City Details rename focus timing is corrected: submitting a changed nonblank name keeps the accessible editor focused and read-only until the matching `Events.CityNameChanged` success event, then returns focus so the live new city name is the first panel announcement. Static event/lifecycle invariants and `git diff --check` pass; in-game confirmation is pending.
- Release metadata is rotated to version 0.9.0 dated 2026-08-01, with a fresh Unreleased changelog section. Historical changelog releases use the pre-1.0 minor sequence. The README Treeviews reference documents depth-local Home/End and full-tree Ctrl+Home/Ctrl+End navigation.
- Tree navigation is confirmed working in-game. The new default-enabled `TreeHomeEndCurrentDepth` UI checkbox keeps plain Home/End within the focused row's visible sibling level; disabling it restores legacy flattened-tree Home/End. Ctrl+Home/Ctrl+End remain full-tree in both modes. SQL metadata execution, live routing/localization invariants, localization XML parsing, and `git diff --check` pass; the setting toggle needs in-game confirmation.
- English symbol-only hotkey names are replaced with spoken words through a localization SQL override registered for both front-end and in-game text loading. Static validation passes; in-game confirmation is pending.
- Chat map-tac navigation is corrected and statically verified: activating an accessible pin row resolves the live pin and jumps the navigation cursor, while the corresponding chat message-buffer entry carries the pin coordinates for buffer move-to.
- Map Pin Popup visibility is implemented and statically verified: the accessible multiplayer dropdown applies visibility immediately and refreshes Send to Chat; Cancel restores the last committed value, Send commits the visibility it uses, OK commits the pending value, and native confirmation retains vanilla ownership. Hotseat still has visibility choices but intentionally no real-time chat-send button.
- Chat map-tac navigation and Map Pin Popup visibility testing passed at the user's direction on 2026-07-19. Map Pin List Delete now appears only on owned rows, while the rebindable `CAIDeleteMapTac` world action defaults to Delete and targets an existing owned tac at the navigation cursor without creating one. Both paths ask for Yes/No confirmation, re-resolve the live pin before using vanilla's deletion sequence, and clear matching bookmark slots. Lua Language Server, hotkey/localization XML parsing and uniqueness, action/gesture/ownership/dialog/deletion invariants, and `git diff --check` pass; in-game confirmation is pending.
- City-panel yield readers now rely on the localized names produced from their existing yield icons instead of manually prepending the same names. This removes output such as `Culture, Culture +30.8`; helper call-site invariants and `git diff --check` pass.
- Production Panel city context improvements are verified in-game: initial focus remains on the active production tree, the live panel label includes the selected city, the city list remembers that city, row details include live yields, and every registered yield sort works with correct city selection and focus.
- Production Panel queue reordering now treats current production as queue index 0: Shift+Down on current production swaps it with the first queued item, Shift+Up on the first queued item swaps it with current production, and Shift+Up on current production reports the existing first-item boundary message. Targeted Lua Language Server, index/binding/focus invariants, and `git diff --check` pass; in-game confirmation is pending.
- Nav-cursor scoping is retained only for city management, where it admits plots assigned to the selected city plus live manage, purchase, swap, and scenario-specific targets. District and wonder placement remain unrestricted so surrounding plots can be inspected.
- Front-end picker checkbox bindings, City State Picker slider setup, and the MapSelect filter label are corrected and statically verified; in-game validation is pending.
- War Machine scenario integration is implemented and statically verified. In-game validation is the next task.
- All previously pending in-game tests, retests, regression checks, and optional fixture checks were closed as successful at the user's direction on 2026-07-17.

## macOS port (branch `macos-support`)

- The mod runs on the Aspyr macOS build from Steam, Apple Silicon only, with speech, sounds, text input, config and a Lua log. Design, internals, build and release steps: `mac/README.md`. Lua-facing facts: `docs/game-api.md`, "macOS (Aspyr) build".
- Development on the Mac: `mac/dev/build.sh` builds and deploys, `mac/dev/run.sh` launches (Steam running), `mac/dev/kill.sh` stops.
- Type-to-find in the front end (2026-09-02/03): typed characters were queued natively but never polled. Two causes: the `MainMenu.lua` and `IntroScreen.lua` update hooks referenced `mgr` before its `local` declaration (nil global; fixed, resolved at call time), and the main menu's update callback stops while game setup or the leader picker is up (verified in the log: main menu polls, picker never does). `HandleInput` now also drains the queue on every key-up. Side effect on Windows: the search timeout and audio manager updates now also run in the front end. Verified on the Mac 2026-09-03: type-to-find works in the civ picker.
- Mac clock (2026-09-03): `Automation.GetTime()` advances once per second on the Aspyr build, so the audio manager's 0.1 s delays (terrain after a river crossing, forest and road stingers) ran up to a second late; the native log showed every delayed sound starting at the same fraction of a second. All CAI timers now read `GetMonotonicTime()` from `caiUtils.lua` (native `CAI.GetTime()` on the Mac, `Automation.GetTime()` on Windows).
- Macaw support (2026-09-08, verified in-game): prism's registry is built through a builder that adds Macaw's own prism plugin, so Macaw is a prism backend ("Macaw", priority above VoiceOver, supported only while the reader runs), taken by `auto` and `prism`. See `mac/README.md`, "Speech".
- Follow-ups from the same review (2026-09-03): the map search debounce and the native audio rebuild retry are time based; the audio manager is serviced on key-up like character input, so device recovery and delayed sounds work under front-end popups; `CAI.IsImeComposing()` is real on the Mac (marked text of the game view's text input context, sampled around each key); long speech lines are scheduled in one-second segments as they render (verified with `speechtest --silent --runloop`).

## Pending tests

- Mac speech interrupt crash (fixed 2026-09-04): the game crashed inside Apple's TextToSpeech framework when a line was interrupted the moment its render started (entering movement mode while "Map" was being rendered). The retired synthesizer was released from the stream's queue after the first end marker, while the framework was still in the write's completion on the main thread. It is now kept until both end markers and released on the main queue; `speechtest` covers the path with five in-flight interrupts. In-game: interrupt speech rapidly for a while (arrow keys, mode changes, escape) and confirm no crash; `~/Library/Logs/CAI/cai_native.log` should show every "reaped a retired render" line with 2 end markers.
- BBG ProductionPanel + TradeOverview integration (added 2026-08-19): with Better Balanced Game 7.4.6 active, verify the production panel opens and reads correctly (Warrior Monk absent from the normal build list — BBG's monk nerf is effectively always on: `MonkOverride` removes it unless a companion BBGTS beta mod explicitly sets `BBGTS_MONK_NERF = false`) and the trade overview Available Routes tab reads with BBG's city-state/civ grouping. Both use the WorldRankings vendored-base pattern; static Lua checks pass, in-game unverified.
- Geography "fully revealed" suffix (added 2026-08-25): in-game, confirm a landmass/ocean reads ", fully revealed" only once fully charted, that `plot:GetArea():GetPlotCount()` returns sane totals in the InGame UI context, and that ocean clusters (non-lake water) match a non-lake water area's count. Static Lua checks pass, in-game unverified. Details in `docs/game-api.md` geography-zone section.
- Geography Natural disasters subcategory (added 2026-08-25, Gathering Storm only): in-game with XP2, confirm active storms, droughts (with turns remaining), and erupting volcanoes appear under Geography > Natural disasters, jump correctly, and disappear once expired. Gated on revealed (matches plot tooltip's IsRevealed), not visibility. Verify `GameClimate.*` and `MapFeatureManager.IsVolcanoErupting` behave in the InGame UI context. Static Lua checks pass, in-game unverified.
- Camera zoom presets (added 2026-09-10): (1) pick each preset in F12 and confirm the zoom changes immediately (close: animals/campfires audible, no wind; mid: terrain beds softer, no wind; far: wind, units louder); (2) step the cursor after a mouse-wheel zoom and confirm it snaps back to the preset; (3) Off leaves the zoom alone; (4) vanilla combat look-at with "move and zoom" returns to the preset after combat; (5) read `UI.GetAmbienceClipDistance()` and `UI.GridToWorld(1,0)` minus `UI.GridToWorld(0,0)` once in game and record them in `docs/game-api.md`.
- Movement-value settle fix (added 2026-09-06): `MovementActions_CAI` speaks "moved to, X movement left" only after the movement value has settled, so a move onto a goody hut / natural wonder / embark-disembark no longer reports the stale pre-move value. Goody hut, natural wonder, and embark verified correct in-game 2026-09-06. First attempt read the value from `Events.UnitMovementPointsChanged`; that regressed disembark (reported "4 moves left" for a tank actually at 0 of 4) because disembark fires that event once with the land-form max and never announces the subsequent zeroing. Reworked to poll `GetMovesRemaining()` directly (the value the unit panel shows) on the WorldInput frame poll, waiting for it to change and holding briefly (`MOVEMENT_SETTLE_HOLD_FRAMES`) so multi-step commits settle; movement-points event handlers removed. Verified fixed in-game 2026-09-06 (disembark now reports correct remaining movement; goody hut, natural wonder, embark all correct). Closed.
- Otherwise none. All earlier in-game tests were closed as complete and passed at the user's direction on 2026-08-12.

## Next steps

- Run the focused tutorial playthrough from `docs/tutorial-scenario-compatibility-audit.md`, then inspect `Lua.log` for any runtime or completion-chain failure.

## Durable release versioning decision

- Until version 1.0.0, each release increments the minor component and resets the patch component to zero (for example, 0.6.0 to 0.7.0).

## Durable Civ Royale / Red Death decisions

- Investigation completed 2026-07-24 against the installed `DLC/CivRoyaleScenario` manifest, Lua, XML, commands, capabilities, and data. The DLC formally replaces thirteen UI contexts and adds/imports Royale intro, leader-icon, global-ability, notification, end-game, and storm-status UI. CAI conflicts with nine formal contexts plus `ExpansionIntro`; only Unit Flag already composes the Royale wrapper.
- This is a functional integration gap, not only a presentation gap. Until the official wrappers are selected first, CAI can suppress scenario unit commands, all three global faction abilities, Grieving Gift targeting, safe-zone/fallout/object overlays and announcer wiring, ring/WMD/endangered-Settler status, observer refresh/chat/ribbon behavior, and the How to Play pages.
- Implemented 2026-07-24: the nine overlapping CAI contexts compose their official Royale wrappers. The first in-game test exposed that five DLC replacement scripts were not exported through `ImportFiles`, so their CAI `include()` calls silently produced nil hooks and broke T, pause, minimap input, ribbon/tracker shutdown, and related behavior. CAI now imports normalized-exact collision-safe copies of those five Firaxis scripts; the separate End Game chat-target nil call and post-manager-shutdown stack cleanup are also corrected. Turn speech appends ring, damage, WMD, Settler-warning, and global-ability status; cursor/B-bucket/scanner access covers zones and overlay objects; Grieving Gift targeting works after its mode is entered; ribbon/chat/both intro contexts/End Game observer flows and Mutant toggle state are accessible. The separate `CivRoyaleIntro` Main Menu context is a registered full front-end replacement whose final page closes without matchmaking.
- User decisions: Royale status belongs in the existing turn/time/date speech, after its ordinary fields; the faction global-ability status and tooltip come last. Do not add a separate status panel. Because Royale has no emergencies, the existing Ctrl+W emergency-tracker action activates the faction global ability: Wanderer/Pirate abilities execute immediately and Edgelord enters Grieving Gift targeting. Automatic cursor zone changes remain concise. B gives the current safe-zone-center direction plus shrink status while safe, official category/damage while in ordinary Red Death, and no stats while outside. Never announce clear-of-Red-Death.
- Pending: focused Red Death include/export repair retest, front-end tutorial paging/Play/offline testing, the remaining in-game Royale matrix, and shared base/XP1/XP2/Black Death regression. Full behavior and checks are in `docs/game-api.md`.
- Static verification: the five vendored Royale scripts match the installed Firaxis sources after newline normalization; each has exactly one top-level file and `CAIInGame` import registration. LuaLS reports no errors in the authored repair, Ctrl+W activation, detailed B-zone speech, or scanner-center destination; modinfo/localization/hotkey XML parsing, B/cursor routing, live center validation, scenario routing, official callback-shape, wrapper/VFS/import/localization/speech-route assertions, and `git diff --check` pass.

## Durable War Machine scenario decisions

- CAI must explicitly include the official War Machine ActionPanel, TopPanel, and WorldRankings wrappers because its load-order-100 replacements win those contexts. No copied scenario files or CAI modinfo entries are needed.
- Accessible World Rankings Overall omits the redundant scenario-name row and exposes only the scenario description and France/Germany Paris objective. Existing Score capture remains authoritative for the Control of Paris category.
- Era/Age speech should follow the live `CAPABILITY_ERAS` capability instead of a scenario-name list. Turn/date speech should refresh and read the official TopPanel controls so the August/September 1914 calendar remains authoritative.
- The scenario's EndGame addition composes through the existing wildcard. Historic-ranking localization is unused because the scenario deletes all ranking rows; the accessible Ranking tab should remain absent.
- War Machine is multiplayer-only. HostGame/Hotseat and StagingRoom are the correct accessible setup path; single-player Scenario Setup exclusion is expected.
- Static verification passes: Lua Language Server reports no problems in the three changed Lua files, War Machine include/order/localization/capability/wildcard assertions pass, and `git diff --check` passes.

## Durable Vikings scenario decisions

- Vikings uses ruleset-first composition in PartialScreenHooks and World Rankings, three accessible Overall rows, explicit Espionage suppression, and read-only Religion Screen behavior with a two-religion maximum.
- Its same-name Religion Screen and custom World Rankings XML already arrive through the DLC import action; no scenario files or CAI modinfo entries should be added.
- Shared Score capture should expose the live scenario categories. Immediate visual score-floater speech is a separate enhancement and should not be implemented by duplicating the gameplay script's scoring rules in UI code.

## Durable Poland scenario decisions

- Only World Rankings is a formal scenario UI replacement; ReligionScreen and UnitFlagManager are imported same-name files that CAI already reaches through their ordinary includes.
- Reuse the official `GetTribeName()` exported by the Poland UnitFlagManager instead of duplicating its tribe-index mapping.
- Event popups, Scenario Setup, LoadScreen, Score, EndGame, Great People/Works, Civilopedia, and ordinary data-driven units/buildings require regression testing but no planned scenario-specific branches.

Fix iteration 2026-07-17: Path to Nirvana testing exposed a false fourth locked belief slot. CAI now counts the visible belief instances created by the active Religion Screen, excluding hidden recycled instances, and uses that live total for both the section summary and locked rows. If a full mod replacement lacks the standard live presentation, CAI falls back to known unlocked beliefs without inventing slots. Lua Language Server reports no errors; nine live-stack/fallback/official-wrapper assertions and `git diff --check` pass. In-game retest Path to Nirvana own and other religions plus an ordinary religion with an evangelizable fourth slot.

Change 2026-07-17: Path to Nirvana World Rankings Overall now groups its ten official display lines into two accessible rows joined with `[NEWLINE]`: rules lines 1-6 and scoring lines 7-10. Lua Language Server reports no errors; two-row/two-join/order assertions and `git diff --check` pass. In-game retest both rows and their line order.

Change 2026-07-17: Implemented Path to Nirvana / Indonesia-Khmer scenario UI composition. CityStates, TradeOverview, ReligionScreen, and WorldRankings now select the official scenario wrappers before expansion/base files. Maritime city-state type and envoy bonuses flow into the accessible City States tree, official trade-route icons and preset-belief behavior are preserved, and World Rankings exposes the ten scenario rules in official order while retaining generic live Score capture. Civilopedia and EndGame remain automatic wildcard compositions; the DLC's missing GreatWorks file is only a stale manifest entry. Lua Language Server reports no errors for the four changed Lua files; 14 include/import/wildcard/order assertions, six installed/decompiled hash comparisons, score-multiplier checks, and `git diff --check` pass. In-game testing is required across the matrix in `docs/game-api.md`.

Fix iteration 2026-07-17: First Nubia World Rankings test crashed while building the Temple-victory row because `HAS_7_TEMPLES` has no `VictoryProgress` requirement-text key and CAI passed the legitimate nil lookup result to `GameEffects.GetRequirementText`, which requires a string. Structured requirement text is now requested only when a key exists; the captured official `<Temple count>/7` value and victory description remain available. Lua Language Server reports no warnings or errors, the targeted guard invariant and `git diff --check` pass. In-game reopen the Temple tab and continue the Nubia matrix.

Change 2026-07-17: Implemented Nubia scenario World Rankings composition and generalized scenario/mod victory capture. Custom-victory and Score rebuilds now consume the exact player/team rows populated by the included official wrapper, preserving wrapper-owned filtering, order, computed values, visible details, and tooltips while retaining requirement and score-category fallbacks. Nubia therefore exposes live Temple-to-Amun totals and only the official Egypt/Nubia competitors without a ruleset-specific accessible branch; Australia Score filtering benefits from the same path. An adapter registry covers replacements that bypass the global population hooks. Lua Language Server reports no warnings or errors for `WorldRankings_CAI.lua`; include/capture/adapter invariants, modinfo XML parsing, and `git diff --check` pass. In-game test the full Nubia matrix in `docs/game-api.md`, plus ordinary custom victory, Score, Australia Score, and Black Death custom-victory regressions.

Change 2026-07-17: Research and civic chooser tooltips now include the localized `Leads to` relationship list from the technology/civic prerequisite tables. Unrevealed destinations retain the trees' localized `Not revealed` label. Lua Language Server reports no errors for either chooser and `git diff --check` passes. In-game test a branching technology and civic, an unrevealed destination, and an item that has no successors.

Fix 2026-07-17: Tech and civic boost popups now use `HasTech()` / `HasCivic()` as the authoritative completion state. When a completion has already reset the live progress query to zero, the sighted and accessible descriptions now use the localized completion message and the accessible progress ends at 100% instead of 0%. Static verification passes; in-game retest a boost that completes a technology and one that completes a civic, plus an ordinary non-completing boost.

Fix iteration 2026-07-17: Black Death Government policy interaction crashed because its older full replacement does not export the later vanilla `GetPolicyFromCatalog()` or `IsReadOnly()` extension points. The accessible policy tree now reconstructs the official catalog record from `GameInfo.Policies` and `GameInfo.Types`, and uses a scenario-compatible writable fallback while preserving ordinary World Congress locking. LuaLS reports no errors and targeted symbol/version checks pass. In-game retest viewing all policies, choosing/replacing/removing a policy, France's Papal slot, confirmation/cancel, and close/reopen.

Fix iteration 2026-07-17: Second Black Death Plague-lens test confirmed the shared lens-active state and official `PLAGUE-STRICKEN: N turns` plot speech, but the Active lens scanner category remained empty. The duplicate CAI `Plague: N turns remaining` plot helper is removed. The scanner now uses Black Death's authoritative Fallout-manager duration and official contaminated-plot localization instead of the scenario plot property, while Minimap's explicit state event still controls category availability. Static verification passes; in-game retest Active lens category/group/items, infected/clean cursor speech without duplication, lens off cleanup, Insert, and switching to an ordinary lens.

Fix 2026-07-17: DiplomacyActionView and DiplomacyDealView now register their live contexts through `UITutorialManager:AddControlToAlwaysReceiveInput` while open and remove them on hide or shutdown. This preserves navigation when declaring friendship triggers the Expansion 1 `ALLIANCE_AVAILABLE` advisor underneath diplomacy and its late `OnShow()` enables tutorial-wide input filtering. The advisor retains its lower stack priority and becomes accessible after diplomacy closes. Static lifecycle and API checks pass; in-game retest Declare Friendship -> Alliance available -> action-view navigation, opening/navigating/closing a deal while the advisor is pending, closing diplomacy, then navigating and dismissing the advisor.

Change 2026-07-17: Implemented Black Death scenario UI composition. The eight scenario-imported shared wrappers load before expansion/base files. The non-imported official Minimap wrapper is copied into `UI/Replacements/BlackDeathScenario`, registered through a `BlackDeathScenario`-scoped import action with no load order, and included before CAI's accessible Plague lens entry; scenario lens behavior is no longer duplicated. The Active lens scanner groups revealed infected plots by remaining plague duration, while the official plot tooltip remains the single source of plague-stricken plot speech. Unit commands and scenario charges, England's live Coerce targets/cost/duration, the dedicated Coerce-turns plot helper in automatic cursor and Stats reads immediately after Fallout, France's Papal slot and net Faith rate, the plague indicator in the number 1 city-banner Identity and status bucket, TopPanel event-response plumbing, hidden era behavior, and custom victory progress are preserved. Lua Language Server reports no errors for the CAI Lua files. XML/tag, include/import, ruleset-order, listener-lifecycle, scenario-property, README consistency, and `git diff --check` verification pass; in-game testing is required across the full matrix in `docs/game-api.md`.

Fix 2026-07-17: The Delete-player input binding in Advanced Setup and Scenario Setup now uses the shared localized description `LOC_CAI_KB_DELETE_PLAYER` (`Delete player`), so focused input help identifies the command. Both binding references, localization XML parsing/tag uniqueness, and `git diff --check` pass. In-game retest input help on a removable AI row in both screens.

Fix iteration 2026-07-17: First Advanced Setup parity test froze immediately on opening the Advanced tab. The new empty-section hidden predicate called `sub:GetVisibleChildren()`; child `IsHidden()` checked the parent section, recursively re-entering the same predicate forever. Section visibility now reads vanilla's already-computed parameter-stack visibility directly. The redundant `RefreshPlayerSlots` wrapper is removed because vanilla already ends that function at `GameSetup_RefreshParameters`; `GameParameters_UI_AfterRefresh` is now the sole refresh rebuild choke point, with a dirty flag allowing Advanced-tab activation to build once only when changes occurred while it was inactive. Focused recursion/call-path assertions and `git diff --check` pass. In-game retest opening Advanced first, repeated tab switching, changing several parameters and player count while Advanced is active, changing parameters in Basic then returning to Advanced, and close/reopen with both tabs.

Fix 2026-07-17: Completed the Advanced Setup vanilla-parity pass. Basic Map Type now watches the visible MapSelect container, and Advanced Map Type is a button that delegates to vanilla's live popup callback instead of a direct dropdown. Dynamic Basic parameters are removed with `UI_DestroyParameter`; AI leader dropdowns honor parameter availability and reset alternate color; parameter- and value-level invalid reasons are composed into live tooltips across every parameter widget family; World Builder and empty sections follow vanilla visibility; hidden basic game modes follow the checkbox; and nil non-inverted arrays say Nothing. The copied vanilla body remains unchanged, focused structural invariants and `git diff --check` pass. Standard LuaLS cannot parse Firaxis typed Lua and no Civ VI Lua compiler is available. In-game test required: Basic/Advanced MapSelect including generated/static filters and returning selection, ruleset/map refresh churn, AI leader/color reset and a constrained disabled AI selector, invalid Score-victory/boolean explanation, World Builder/empty groups, and ordinary inverted array summaries; a mod fixture is needed for disappearing Basic parameters and a nil non-inverted array.

Fix iteration 2026-07-17: Outback Tycoon testing exposed raw `LOC_CIVILIZATION_AUSTRALIA_SCENARIO_*_ADJECTIVE` prefixes. Its four playable colonies and Sydney declare adjective tags but its localization files define none of them in any language. Shared unit and city ownership labels now use the database adjective only when `Locale.Lookup` resolves it, otherwise falling back to the localized civilization short name as vanilla unit flags do. Data-file audit and `git diff --check` pass; in-game retest the Victoria Explorer/Prospector plot and ideally another colony plus an ordinary civilization.

In-game test passed 2026-07-17: Outback Tycoon Settler awards appear in the appropriate Civics screens; no award appears in the Technology Tree, which matches the scenario data. The broader Australia test matrix remains pending unless separately confirmed.

Change 2026-07-17: Outback Tycoon Overall now orders its two explanation rows first and the expandable Current game difficulty row last. Order checks and `git diff --check` pass; in-game testing remains pending.

Change 2026-07-17: Outback Tycoon World Rankings now places all eight difficulty targets under the expandable live Current game row instead of two grouped static rows. Each child says only the localized difficulty name followed by its required score, without repeating `Difficulty:`. Tree structure, child count/order, compact labels, live current target, and `git diff --check` pass; no Lua compiler is available. In-game testing remains pending.

Change 2026-07-17: Implemented Outback Tycoon/Australia scenario UI composition. PartialScreenHooks, TechTree, and WorldRankings now select the official scenario wrappers before expansion/base files. The accessible Espionage action honors the scenario's hard suppression. World Rankings exposes the localized description, all single-player difficulty targets, live current target, multiplayer omission, Outback rules, and the scenario's human-only Score data. Civic Tree/Chooser reward speech now recognizes the scenario's one- and two-Settler modifier ids. Ruleset/include order, suppression, eight thresholds, multiplayer gating, scenario imports, modifier ids, localization references, and `git diff --check` pass; no Lua compiler is available. In-game testing remains pending.

Analysis 2026-07-17: The Australia audit found three formal replacements—PartialScreenHooks, TechTree, and WorldRankings—plus imported WorldRankings layout, Logitech ARX implementation, and wildcard settler-award civic icon loader. Durable behavior and the in-game matrix are documented in `docs/game-api.md`.

Change 2026-07-17: Conquests of Alexander World Rankings now groups its eight display lines into three accessible static rows joined with `[NEWLINE]`: lines 2+3+4, lines 5+6, then live lines 7+8 followed by line 1. The third row calculates enemy cities and owned-city score together from live `PlayerManager.GetAlive()` data. Tag order/counts, three stable focus keys, one live count snapshot, absence of stale control-text reads, and `git diff --check` pass; no Lua compiler is available. In-game retest all three rows, live values, duplicate-tab suppression, remaining tabs, and close/reopen focus.

Change 2026-07-16: Added `EventPopup_CAI.lua` for data-driven scenario events. The accessible dialog exposes live title, description, image caption, event effects, standard unlock tooltips, Continue, and mandatory A/B choices with choice-specific unlock details. It preserves vanilla response reporting and FIFO popup queuing, removes the stale dialog at every close/queue transition, and disables dialog-wide Enter activation for mandatory choices. Modinfo XML/file/replacement registration, unique replacement IDs, Lua Language Server error checking, decompiled replacement audit, and `git diff --check` pass. In-game testing is required with one-shot, unlock-bearing, A/B choice, and queued event popups.

Fix 2026-07-16: UnitPanel result registration now skips and logs scenario-pruned operation or command rows that exist without a hash. This fixes the Alexander scenario load failure where its orphan `UNITOPERATION_PLANT_FOREST` row made `AddOperationResult` assign a nil table key. The same guard covers other data-pruning scenarios. Cause and required invariant are documented in `docs/game-api.md`; static verification passes and in-game scenario retesting is required.

Fix iteration 2026-07-16: Scenario Setup tab switching now directly changes the native basic/advanced containers without rebuilding or restoring focus, preventing repeated Advanced Setup announcements. The remaining Max Turns trap was shared input dispatch: a disabled EditBox's matching bindings were consumed before their inactive actions could return false. `BubbleWhenDisabled` now lets unmodified Up/Down and Home/End reach the containing List while Enter/editing remain blocked. Escape/Back closes the screen from either tab; players are direct civilization dropdowns with legal AI deletion; and Difficulty tooltips are suppressed. Lua Language Server reports no errors for the manager base, EditBox, IDE helpers, or Scenario Setup; four navigation flags, the dispatcher branch, and `git diff --check` pass. In-game retest the unavailable Max Turns field plus the prior Scenario Setup matrix.

Change 2026-07-16: Added the full frontend `ScenarioSetup.lua` replacement, registered it in both modinfo file lists, and exposed the live Scenarios row in the accessible Main Menu Single Player submenu. The two-page panel exposes scenario descriptions, civilization details, live setup parameters, direct player civilization dropdowns, AI Add/Delete, grouped advanced settings, validation, defaults, and Start while preserving vanilla writes, broadcasts, and launch callbacks. Actual data refreshes use stable focus keys with capture/restore. Static verification passed; in-game testing remains as listed above and in `docs/game-api.md`.

Change 2026-07-16: Added disabled-by-default World Scanner settings for automatically focusing the City management and Recommendations categories when they become available. Both categories use the same setting-gated `AutoFocus` path as Valid targets and Active lens; interface-mode changes rebuild City management, and local unit-selection changes rebuild Recommendations after its recommendation cache updates. SQL execution/order, localization XML/tag uniqueness, runtime setting/category/event references, listener registration order, and `git diff --check` pass; no Lua compiler is available. In-game testing required with each setting enabled and disabled while entering/leaving city management and selecting/deselecting builders and settlers with recommendations.

Change 2026-07-16: The `ReadSelectionSummary` action in CityPanel and UnitPanel now speaks the live direction from the navigation cursor to the selection as a separate first line, followed by the existing summary. Direction was added only in the action handlers, not to the reusable city/unit summary buckets or automatic selection-change speech. Handler-scope audits and `git diff --check` pass; no Lua compiler is available. In-game testing required for off-cursor and same-tile (`Here`) city/unit summaries, other summary consumers, and non-summary read actions.

Change 2026-07-16: Added enabled-by-default Cursor settings that independently control automatic navigation-cursor movement when a city or unit is selected. CityPanel and UnitPanel now read their setting live before raising the existing select-reason cursor move. SQL execution/order, English localization XML/tag uniqueness, runtime setting references, and `git diff --check` pass; no Lua compiler is available. In-game testing required with each setting enabled and disabled.

Documentation 2026-07-16: Expanded README's Type-to-find section after auditing the live search helper. It now covers case-insensitive exact/word-prefix/substring matching, consecutive word-prefix abbreviations such as `war mon` for `Warrior Monk`, relevance preference, collapsed-tree discovery, same-letter cycling with wrap, hidden/disabled behavior, no-match correction, first-character restrictions, clearing, and timeout control. The Search panel section now documents edit-box Enter, its ten activated-query history limit, and the actual `--term` exclusion syntax with an example. The previously incorrect `-term` syntax was corrected in README, UI-manager docs, annotations, SearchPanel comments, and game-API notes. README feature coverage, exclusion-syntax consistency, and `git diff --check` pass.

Documentation 2026-07-16: Added player-facing Type-to-find search and Search panel sections at the end of README's UI documentation immediately before Gameplay. They describe available navigation, query, exclusion, history, activation, result-side editing, timeout, and auto-focus features without implementation details. Section placement, feature coverage, and `git diff --check` pass.

Fix 2026-07-16: Removed SearchPanel's explicit edit-box fallback after result rebuilds. Typing or deleting while focused in the results list now leaves focus on the surviving result-list container when the focused row is destroyed; it does not jump to the edit box. A successful result rebuild moves focus only when `AutoFocusFirstSearchResult` is enabled, while empty-state rows remain never auto-focused. UI-manager documentation and changelog were corrected. Static audit confirms only `Open()` explicitly focuses the edit box, no rebuild fallback remains, the successful-result setting gate remains, and `git diff --check` passes; no Lua compiler is available. In-game retest required while typing and deleting from a focused result with the setting both enabled and disabled, including transitions to no results and an empty query.

Change 2026-07-16: Added the enabled-by-default UI checkbox `AutoFocusFirstSearchResult` immediately before `SearchTimeout` in `settings_CAI.sql`; the timeout and token-split settings move to sort indices 50 and 60. SearchPanel now focuses the first successful result only when this setting is enabled. When disabled, rebuilding successful results does not explicitly move focus. English localization, UI-manager documentation, and changelog were updated. SQL execution/order, localization XML/tag uniqueness, the runtime setting gate, and `git diff --check` pass; no Lua compiler is available. In-game test required with the setting both enabled and disabled while typing from the edit box and results list, plus no-result behavior in both states.

Fix 2026-07-16: SearchPanel now creates its empty-state row when opened, labels it with localized `Type text to search` while the live edit buffer is empty and `No results` for a nonempty query, and never auto-focuses that row. An empty result rebuild leaves focus on its surviving current container instead of moving it to the empty-state row. UI-manager documentation and changelog were updated. Localization XML parsing, tag uniqueness, empty-label/focus audits, and `git diff --check` pass; no Lua compiler is available. In-game test required on initial open, clearing a query, a no-match query entered from the edit box, and a no-match query produced while typing or deleting from the results list.

Fix 2026-07-16: EditBox now speaks every successful typed, pasted, deleted, cancelled, or programmatic buffer change before emitting `text_changed`, so listener work cannot interrupt the edit echo. Delete helpers return the removed text to the widget so the widget owns the consistent speech/event order. SearchPanel no longer silently forwards printable characters typed while focus is in its result list, so those characters receive normal edit echo; forwarded Backspace inherits the corrected ordering. UI-manager documentation, annotations, and changelog were updated. Speech-first mutation audits and `git diff --check` pass; no Lua compiler is available. In-game test required for typed characters, paste, all four delete variants, selection replacement/deletion, cancel restoration, and typing/Backspace while focused in search results.

Change 2026-07-16: Civilopedia Ctrl+F now uses the SearchPanel's new `raw` query mode. The complete entered phrase is passed to the title-and-body search context once, preserving the engine's phrase/title relevance and one coherent preview. The default `terms` mode continues to provide term-by-term AND and `-term` exclusion for maps, trees, and ordinary lists. Framework code, annotations, UI-manager documentation, game-API notes, and changelog were updated; static checks pass, but no Lua compiler is available. In-game retest required for `nuclear device types` exact-title ordering, phrase/body matches, preview quality, and activation.

Fix 2026-07-16: First Civilopedia enhanced-search test returned no results because startup indexing crashed before its first page: Civ VI's UI Lua context has no `_G`, and the generic helper replacement attempted `_G[name]`. The late initialization pass still runs immediately after vanilla `Initialize()` returns, but it now saves, replaces, and restores every known render helper through explicit direct references. Each cached page is indexed under `sectionId|pageId` with its localized stripped article title in the title field and captured body in the description field, preserving vanilla title searching while adding body matches; pages with empty bodies remain searchable by title. Result labels are article, section, optional group. Follow-up testing found `nuclear device T` failed while `nuclear device Ty` succeeded because custom multi-term intersection retained only 150 intermediate matches per term and broad `T` pushed the correct page below the cutoff; custom multi-term queries now retain up to 2000 intermediate matches before the normal final 50-result cap, and that search test passes. Civilopedia retains each full body and produces one longer space-aligned preview with 180 characters of context on each side. An exact `nuclear device types` title search then placed Power Plants first because final intersection results came from unordered `pairs(...)`; results now sort by exact full title, title prefix/full phrase, summed per-term engine rank, then localized label. `git diff --check` passes; no Lua compiler is available. In-game retest required for exact-title-first ordering and one clean preview, then label order, heading exclusion, stat/quote matches, activation, randomized tech/civic visibility, Heroes mode, and shutdown/reload cleanup.

Documentation 2026-07-16: Expanded the README Map tacs section to explain that bookmarks are native player-owned tacs referenced by ID, are player/save-specific, use `0` for slot 10, rename or create tacs on assignment, delete the prior tac on replacement, transfer a tac between slots, and clear stale manually deleted slots.

Change 2026-07-16: Map-tac bookmark direction actions now speak only the live direction, without repeating the bookmark name. English player-facing localization and the Unreleased changelog entry consistently use `map tac` / `map-tac`; `MapPin` remains only an engine/code term. Localization XML, action-tag references, terminology audit, and `git diff --check` pass. In-game retest required after reloading the UI.

Fix 2026-07-16: First map-pin bookmark test failed after creating a pin but before rename, storage, deletion, or speech. `Lua.log` identified `GetBookmarkPinID`: an unset `PlayerConfiguration:GetValue(...)` returns no Lua values, so passing it directly to `tonumber(...)` raised `bad argument #1 ... value expected`. The result is now captured in a local first. Assignment also renames the live target before deleting a replaced pin instead of re-resolving a newly created pin by ID before broadcast. Failure-path audit, XML parsing, and `git diff --check` pass; no Lua compiler is available. In-game retest required for the complete bookmark matrix below.

Change 2026-07-16: Added ten player/save-specific map-pin bookmark slots in `MapPinListPanel_CAI.lua`. `Ctrl+Shift+1..0` assigns the owned pin at the navigation cursor or creates one, renames it to localized `Bookmark 1..10`, clears any other slot using that pin, deletes the replaced slot's prior pin, stores the new pin ID in `PlayerConfiguration`, broadcasts the change, and speaks confirmation. `Ctrl+1..0` resolves the live pin ID and jumps the CAI cursor; `Alt+1..0` speaks its live direction from the cursor. Missing/deleted pins clear stale slots and speak that the slot is empty. All thirty actions are rebindable. Hotkey and localization XML parse; all 30 action/gesture registrations, exact modifier mappings including slot 10 on `0`, action/tag uniqueness, localization references, gesture-collision checks, and `git diff --check` pass. No Lua compiler is available. In-game testing required for creation, existing-pin assignment, replacement/deletion, duplicate-slot transfer, save/load, hotseat/player scoping, direction speech including `Here`, empty/deleted slots, remapping, and all digit/zero bindings.

Session note 2026-07-15: Rotated the changelog for release `0.6.0`, created a fresh empty `Unreleased` section, and updated `src/CivViAccess.modinfo` from version `0.5.0` to `0.6.0`.

Change 2026-07-15: Added the rebindable `ReloadGame_CAI` quick-load action on `F6`; its `R` sorts after vanilla `QuickLoad`. `LoadGameMenu.lua` now moves its hotkey listener from `InputActionTriggered` to `InputActionStarted`, speaks when loading is unavailable, and otherwise pushes a low-priority CAI Yes/No confirmation dialog. Yes closes the dialog and delegates to vanilla's original handler with the original quick-load id; No, Escape, and dialog `focus_leave` close it without loading. Modinfo, hotkey, and localization XML parse successfully; action/tag uniqueness, one-for-one event replacement, dialog handler/delegation counts, and `git diff --check` pass. No Lua compiler is available. In-game test required for unavailable loading, Yes/No/Escape, focus-leave cleanup, the actual quick-load flow, and binding remapping.

Change 2026-07-15: Added the full Gathering Storm `InGame.lua` root replacement through the `CAIInGame` import action. The new rebindable `SaveGame_CAI` action defaults to `F5`, sorts after vanilla `QuickSave`, moves InGame hotkey handling from `InputActionTriggered` to `InputActionStarted`, checks `CanLocalPlayerSaveGame()`, delegates successful saves through vanilla's `QuickSave` action id, and speaks localized success/failure feedback. The copied XP2 body matches the installed/decompiled source before its final `Initialize()` call; modinfo, hotkey, and localization XML parse successfully; action/tag uniqueness and `git diff --check` pass. No Lua compiler is available. In-game test required for successful and blocked saves plus Pause-menu handling.

Change 2026-07-15: Choosing outcome A or B in a World Congress resolution dropdown now updates the CAI selection and clicks the corresponding live vanilla up-vote control when that outcome has zero votes, automatically committing the free first vote without adding another vote when an existing choice is recommitted. The `Choose an outcome to vote for` placeholder uses vanilla's localized first-vote guidance from the initial vote button. Normal resolutions no longer produce a separate missing-vote blocker; special-session proposals retain it. Audit confirmed one outcome setter, one vanilla up-vote dispatch, and one tooltip-key use; `git diff --check` passed and no Lua compiler is available. In-game test required for first selection, A/B switching, recommitting the current outcome, normal Next blockers, and special-session missing-vote blockers.

Resolution 2026-07-15: Restored `UI_CAIOpenCityStates` to the direct `PartialScreenHooks_CAI` `InputActionStarted` wrapper at user request; the temporary Triggered and WorldInput/Lua-event routing experiments are removed. The saved input configuration does contain the expected `Ctrl+V` binding, so the remaining observed failure may be local runtime state. The handler retains localized capability/contact feedback, vanilla unlock-cache synchronization, the actual `OnToggleCityStates()` call, and click sound. Static verification passed.

Change 2026-07-15: Audited the CAI UI-opening key bindings through LaunchBar, PartialScreenHooks, TopPanel, WorldTracker, DiplomacyRibbon, MinimapPanel, ChatPanel, notification/tutorial lists, Civilopedia, crisis tracker, and UnitPanel. Silent eligibility failures now speak localized reasons from the same live controls, capabilities, game-state APIs, tutorial state, and global hotkey parameters used by vanilla. World Congress names its configured initial era; City-States, Great Works, espionage, and trade routes report their specific missing prerequisite. XML parsing, unique locale-tag validation, and `git diff --check` passed; no Lua compiler is available. In-game test pending across a new game before and after each unlock, anarchy, tutorial gates, scenario-disabled capabilities, observer mode, non-network chat, district placement, base/XP1/XP2 rulesets, and World Congress before/after its initial era.

Change 2026-07-15: World Congress stateful widgets now follow the shared value-binding pattern. Outcome and target dropdowns both use `SetValueSetter(...)`, and the target's empty `value_changed` listener is removed. Both emergency-proposal checkbox paths share one binding helper that silently seeds live vanilla state and clicks the vanilla select box only when its selection differs from the requested value. Audit confirmed two checkbox creations/two bindings, two dropdown creations/two setters, and no stateful `value_changed` handlers; `git diff --check` passed and no Lua compiler is available. In-game test pending: toggle emergency checkboxes in both voting and Available Proposals, commit outcome and target dropdown choices, confirm checked/selected speech matches vanilla state, and verify no Lua errors.

In-game test passed 2026-07-15: Between-turn World Congress `K` speech retained the roster and read every player with the correct Waiting/Submitted status.

Fix 2026-07-15: World Congress resolution rows now use the unique resolution hash as their focus key instead of the shared vanilla instance XML ID `Root`; Tab/Shift+Tab re-entry should therefore restore the previously focused resolution rather than the first matching row. When CAI phase-one validation has no blockers, Next now returns an empty tooltip instead of falling back to vanilla's stale `You must vote on all submitted Proposals` tooltip while targets remain pending until activation. Localization XML validation, unique-tag validation, and `git diff --check` passed; no Lua compiler is available. In-game retest both behaviors.

Change 2026-07-15: World Congress CAI resolution target dropdowns are now always visible and enabled, store one pending target per resolution across A/B outcome changes, and commit targets to vanilla only when Next is activated. The CAI Next button validates this pending state itself and its localized tooltip lists each incomplete normal resolution's outcome and target requirements; special-session discussions list their missing explicit vote. Localization XML validation, unique-tag validation, and `git diff --check` passed; no Lua compiler is available. In-game test pending: select a target before an outcome, change A/B and confirm the target persists, verify vanilla receives it only on Next, exercise every conditional blocker combination across multiple resolutions, verify special-session missing-vote lines, go Previous and change the ballot, close/reopen mid-ballot, and test hotseat/turn-timer cleanup.

Investigation 2026-07-15: Traced the vanilla Gathering Storm World Congress normal-session voting flow through `WorldCongressIntro`, `WorldCongressPopup`, its XML instances, and `WorldCongressBetweenTurns`. The blocking intro leads to a two-phase ballot: for every resolution the player adds at least one vote to outcome A or B, then selects the revealed target; Next stays disabled until every resolution has an outcome, target, and vote. Next opens a summary, Previous returns to editing, and Submit sends resolution votes, submits/ends the Congress turn, shows the waiting roster, then the results popup after processing. Detailed control and API findings are in `docs/game-api.md`.

Fix iteration 2026-07-15: Second JoiningRoom test restored CAI after the missing-content dialog was already pushed, but the first dialog focus lacked role speech because its active configuration did not contain CAI's `SpeakRole` setting. `Lua.log` showed the generic `FrontEndPopup` built before `LeaveGameComplete`, then recovery synchronized CAI and emitted a successful `FinishedGameplayContentConfigure`. JoiningRoom now defers only its own terminal failure popups: it captures the exact vanilla text/title, leaves the failed session, restores CAI on `LeaveGameComplete`, calls `GameConfiguration.UpdateEnabledMods()`, and raises the captured `LuaEvents.MultiplayerPopup` only after the successful configure event. Generic FrontEndPopup behavior is unchanged. Static verification passed; in-game retest required with auto-download disabled and a target requiring a missing mod, confirming the first focus announcement includes the dialog role and CAI remains enabled after dismissal.

Investigation 2026-07-15: A pre-join missing-mod gate is feasible for visible Lobby listings. `listing.EnabledMods` can be parsed and checked before `Network.JoinGame(...)`; the Steam-only `AutoModDownload` option is `1` when enabled. Lobby can raise an accessible missing-mod popup while CAI is active, but must construct its own localized mod list because `GetLastModErrorString()` is populated only after backend failure. Join-code, invite, and matchmaking APIs expose only opaque identifiers, while the early invite/join-attempt events expose no target configuration or cancellation hook. `MultiplayerJoinRoomComplete` could be instrumented to test whether the full remote `GameConfiguration.GetEnabledMods()` becomes available before content configuration, but this is unproven and already post-attempt. Manual comparison is not yet implemented.

Change 2026-07-15: Surveyor now counts improvements (`Ctrl+Shift+A` / `Ctrl+Shift+Numpad 4`), lists visible neutral units (`Ctrl+Shift+D` / `Ctrl+Shift+Numpad 6`), counts tile ownership (`Ctrl+Shift+Z` / `Ctrl+Shift+Numpad 1`), and counts districts (`Ctrl+Shift+Q` / `Ctrl+Shift+Numpad 7`). Improvements exclude camps and tribal villages; districts exclude city centers and internal pseudo-districts; neutral units require actual visibility and a met non-hostile owner; unmet ownership is anonymized. XML validation passed; Lua compiler unavailable. The user confirmed all associated in-game tests passed on 2026-07-22.

Refactor 2026-07-15: Replaced UnitPanel's three forward-declared/later-assigned close functions with early local `RemoveUnitList()`, `RemoveSimplePromotionList()`, and `RemoveUnitNamePanel()` functions. Each follows the shared panel-cleanup pattern: conditionally remove the widget ID from the manager stack, then always clear its stored references. The top-level local count remains 193. In-game load and transient-list/panel close-path retest pending.

Fix 2026-07-15: Restored `local CAICursor = ExposedMembers.CAICursor` to UnitPanel context scope instead of resolving it inside only the unit-list tooltip. To stay at the original 193 top-level local declarations, the redundant `HexCoordUtils` alias was removed and its five callers now use `CAIHexCoordUtils` directly. Unit selection still moves through `LuaEvents.CAICursorMoveTo(plotId, "select")`; the prior register-limit compile failure prevented that handler from registering at all. In-game load and selection-jump retest pending.

Fix 2026-07-15: UnitPanel no longer exceeds Civ VI Lua's top-level register limit while retaining the required context-scope `local CAICursor = ExposedMembers.CAICursor`. The dedicated unit-list key table and newline separator had pushed the already near-limit replacement over the ceiling. The key list and assembly logic now live inside the row's `GetTooltip` closure, `[NEWLINE]` is used literally, and the former `HexCoordUtils` local alias was removed in favor of direct `CAIHexCoordUtils` calls. The file remains at the same 193 top-level local declarations as `HEAD`. In-game load and unit-selection cursor-jump retest pending before functional UnitPanel tests.


Change 2026-07-15: Shared `CAIHexCoordUtils.directionString(...)` now returns localized `Here` for matching origin and target coordinates. Scanner, Surveyor, cursor movement, and unit-waypoint callers no longer duplicate that fallback; unit rows, map tacks, map search, city growth, and other direction consumers inherit it automatically. Movement logging explicitly suppresses known net-zero events before calling the helper, preserving its no-entry behavior. Map search also now uses `CAICursor:GetCoords()` for labels and sorting instead of raw cursor fields. In-game test pending across same-tile unit rows, pins, scanner/search results, Surveyor entries, and waypoints; also confirm net-zero movement events remain omitted and no Lua.log errors occur.

In-game test passed 2026-07-16: Map-pin list direction labels follow the live navigation cursor through `CAICursor:GetCoords()` and produce no Lua errors.


Documentation 2026-07-15: The README keeps `Shift+Space` under World actions, removes the separate City actions, Unit movement, and Unit actions binding lists, and retains Selection actions and selection-information commands. The Selection introduction now directs players to open the context-sensitive actions list with `Tab`, use `Ctrl+H` from the map area, or visit Options > Key Bindings; it notes that most city and unit actions in the Tab list have default bindings.

Session note 2026-07-15: Added the remappable `ActionPanelSpeakTurnBlockers` action on `Shift+Space` to speak the live Action Panel tooltip on demand, including between-turn waiting text. New enabled-by-default Events settings `SpeakTurnBlockers` and `SpeakBetweenTurnsMessage` independently gate changed-only automatic speech during the local player's active turn and while the local player is inactive; neither setting blocks the on-demand action. In-game test pending: disable and enable each setting independently, change blockers during the active turn, end the turn into the other-player waiting state, and confirm Shift+Space always speaks the current message.

Current focus 2026-07-14: World Scanner refresh now follows the authoritative Civ V-style snapshot model without dirty flags. Category cycling rebuilds all live categories; subcategory changes capture a new sort origin and rebuild the current category; group/item cycling rebuilds only the current live category and restores stable identity before stepping. Fresh extraction no longer repeats every leaf validator. The landed leaf is validated just before use and can be pruned directly from indexed `All`/named memberships if it changed after collection. Scanner cursor jumps no longer resort the category: category/subcategory changes capture the live cursor for sorting, while spoken direction remains based on the current live cursor. In-game test pending on a late-game large map: compare category versus group/item delay; add/remove/move units and map objects between navigation presses; confirm vanished focus does not skip the next entry; confirm stale landing-time entries prune without errors; jump/auto-follow among items and confirm order stays fixed; change category/subcategory and confirm nearest-first order reanchors to the current cursor; verify search and empty-category behavior.

In-game test passed 2026-07-14: `World scanner beacon volume` appears immediately after `Enable scanner beacon`, controls the `BEACONS` audio tag, and previews `SCANNER_BEACON` at the newly selected volume.

Change 2026-07-14: Changing `Cursor audio volume` now stops active cursor cues and plays the grass step sound as a preview at the newly applied volume. In-game smoke test pending: adjust the slider through several values, including 0 and 100, and confirm one clean preview at each new level; disable cursor audio and confirm previews remain suppressed.

Fix 2026-07-14: Staging-room team summaries and dropdown choices now keep zero-based team IDs for backend callbacks but display localized one-based labels through `LOC_WORLD_RANKINGS_TEAM` (Team 1, Team 2, and so on). In-game smoke test pending in a staging room with multiple occupied teams and the generated empty-team choice.

Fix 2026-07-14: Dropdown commit now scopes a manager focus-restoration override to the outer dropdown while its setter and `value_changed` handlers run. This preserves commit-before-close for live value accuracy, while synchronous screen rebuilds capture/restore the stable outer dropdown key instead of the focused option key; passive refreshes still restore inside open dropdowns. In-game retest pending: commit a HostGame dropdown that immediately rebuilds and confirm it closes onto the updated dropdown value, then trigger a passive refresh while merely navigating an open dropdown and confirm it stays open on the same option.

Fix 2026-07-14: The Play By Cloud HostGame bare `2` refresh announcement came from slider construction, not focus restoration: its numeric parameter has minimum `2`, and `SliderWidget:SetMin(2)` non-silently clamped the new widget's default `0`, causing `ValueWidget` to announce `2` while off-focus. Shared slider `SetMin(...)` / `SetMax(...)` clamps are now silent configuration changes; user-driven changes still announce normally. The concurrent dropdown restoration fix remains: option rows prefer stable dropdown `FocusKey` identities, and focusing a dropdown descendant silently opens its ancestor. In-game retest pending: leave Play By Cloud HostGame open through repeated parameter refreshes and confirm no bare `2`; adjust the numeric slider manually and confirm its new value still speaks; in staging, confirm an open leader dropdown survives a ready-state refresh on the same option without extra speech.

Change 2026-07-14: Removed DLL spatialization from the audio manager after runtime testing showed spatial voices remained audibly full-volume even when their stored handle volume was zero. Plot playback now uses only ordinary `SetSoundPan`, `SetSoundPitch`, and `SetSoundVolume` calls driven by `CAIHexCoordUtils.plotAudioParameters(...)`. All listener/source/direction/velocity/attenuation setup, spatial initialization state, and the WorldInput load-screen-close initializer were removed. In-game retest pending for scanner pan, pitch, distance volume, rapid replay, End replay, and the beacon-enable setting.

Fix 2026-07-14: Interface-mode and active-lens information no longer exposes details for an unrevealed cursor plot. Normal cursor movement falls through to its existing localized uncharted-territory plot name, and the explicit Interface Info action speaks the same localized text directly. In-game smoke test pending in movement/targeting and an active lens on both unrevealed and revealed-fog plots.

Fix 2026-07-14: Slider Left/Right, Page Up/Page Down, Home, and End input is always consumed by the focused slider, including at its minimum or maximum, so boundary input cannot leak into a parent widget. In-game smoke test pending at both slider bounds.

Session note 2026-07-13: Rotated the changelog for release `0.5.0`, created a fresh empty `Unreleased` section, and updated `src/CivViAccess.modinfo` from version `0.4.0` to `0.5.0`.

Session note 2026-07-13: Split the former Navigation settings category into Cursor, World scanner, and Events. Cursor contains cursor audio, coordinates, and zone announcements; World scanner contains all scanner preferences including the beacon; Events contains visibility and unit-movement announcements. Because configuration storage uses both section and setting ID as its key, existing values for every moved setting reset to defaults. Settings-tree smoke test pending: confirm all three sections, their setting membership and order, and defaults after the change.

Session note 2026-07-13: Added the enabled-by-default `ScannerBeaconEnabled` World scanner checkbox immediately before the other World Scanner settings. The shared scanner beacon helper reads it live, so disabling it suppresses both focused-item beacons and End-key beacon replay while preserving all scanner speech. In-game test pending: toggle the setting off and on, then focus scanner items and press End to confirm beacon playback follows the setting immediately.

Session note 2026-07-13: The World Scanner End-key direction repeat now replays `SCANNER_BEACON` after successfully speaking the current item's direction. Plotless/unavailable items keep the existing unavailable response without a beacon. In-game retest pending: focus a directional scanner item, let its beacon finish, press End, and confirm both direction speech and a freshly restarted positional cue occur; repeat rapidly to confirm the sound restarts each time.

Fix 2026-07-13: Scanner testing with barbarian warriors five and six rows north found no pitch difference even though the Civ V formula yields `2^(5/12)` and `2^(6/12)`. Rapid plot replay was applying the new pan/pitch/volume before stopping the active handle; if the DLL stop resets handle parameters, playback then used defaults. `PlayAtPlot(...)` now stops first, applies the new ordinary plot parameters second, and plays third. In-game retest pending with the same two warriors; confirm the six-row target is one semitone higher and rapid item cycling also preserves pan and volume changes.

Fix 2026-07-13: Positional playback now explicitly stops an active handle before replaying it unless `SkipIfPlaying` was requested. The scanner does not request skipping, so rapid focus changes restart `SCANNER_BEACON` from the beginning instead of relying on `PlaySound(...)` to restart an already-playing handle. In-game retest pending by moving through scanner items faster than the beacon duration and confirming every item retriggers the cue.

Session note 2026-07-13: World Scanner item focus now plays `SCANNER_BEACON` from the focused item's plot, with group plot fallback. Item/group cycling, category/subcategory focus, search results, and configured category auto-focus share the path. The reference plot is captured from the CAI cursor before optional scanner cursor auto-move, so the beacon retains its directional meaning; silent rebuild/resort focus restoration does not replay it. In-game test pending with scanner auto-move both off and on across category, subcategory, group, item, search-result, and auto-focus transitions; confirm one directional beacon per announced item, no beacon for plotless entries or silent rebuild restores, correct pan/pitch/falloff, and no Lua.log errors.

Session note 2026-07-13: The shared raw-audio manager added in-game plot playback through `PlayAtPlot(...)` and `QueueSoundAtPlot(...)`. `SCANNER_BEACON` registers `sounds/UI/scanner_beacon.wav` under the `BEACONS` tag. The original DLL-spatialization implementation was replaced on 2026-07-14 by ordinary per-handle pan, pitch, and volume.

Session note 2026-07-13: Added the remappable `CAICursorJumpToCapital` world action on `Ctrl+S`. It moves the navigation cursor to the local player's capital through the normal cursor jump event without changing the current selection. In-game test pending: with a unit or non-capital city selected, press `Ctrl+S` and confirm the cursor moves to the capital, normal direction/tile speech occurs, and the selection remains unchanged; also confirm no error before a capital exists.

In-game test passed 2026-07-13: Successful immediate owned-unit action confirmations work across the configured actions, including Fortify Until Healed. The callback records an action hash, while `UnitOperationAdded(playerID, unitID, unknown, operationType)` supplies a `GameInfo.UnitOperations` row index in its fourth argument (`13` for HEAL); the handler resolves that row's hash before matching the pending intent. All temporary HEAL tracing and activity-based fallbacks are removed.

Fix 2026-07-13: Updated the World Scanner `Queued path` and `Valid targets` categories to the current `RequestPlotInfo(plotId, requestedKeys)` signature. Their stale three-argument calls passed nil as the effective plot id, causing queued path and action-target labels to say `No plot`. In-game test pending: confirm queued path/waypoint labels and selected-unit action target labels resolve their plot information rather than saying `No plot`.

In-game test passed 2026-07-13: Completed formation members with the same observed route are consolidated into one movement-buffer entry and announcement, with localized `and` list formatting and preserved military/civilian filter eligibility.

Current focus 2026-07-13: Persistent visible unit-movement logging is implemented in `UnitMoveLog_CAI.lua`. Exact continuous visible `UnitMoved` steps are compressed through the shared hex-direction helpers. Visibility-safe origins are captured at `UnitOperationStarted` for owned/currently visible units and formation members, with the persistent last-visible location as fallback; because `GetFormationUnits()` does not work in these UI contexts, shared `GetFormationUnitsOnPlot()` resolves members from units on the current plot by matching formation ID for both movement logging and movement-cost simulation. Initialization and each observer turn start also refresh every currently visible unit without updating hidden coordinates. Following Civ V, fog discontinuities, non-adjacent rebase endpoints, and `UnitTeleported` airlift/trader transfers announce net displacement when a safe origin exists; an unknown hidden origin falls back to generic moved/relocated wording, and known net-zero movement is omitted. Entries store the last visible location and ordinary trade-route shuttling are excluded while still advancing their internal visible-position cache. Owned direct move-to, swap, and associated formation movement are marked from operation start and stored without a new logger announcement; CAI move-to keeps its existing result entry without a duplicate. Directly ordered relocations such as rebase, airlift, trader city transfer, deploy, teleport, and spy travel remain eligible for announcement. Queued continuations and automated moves also remain eligible for owned announcements. Six Events dropdowns independently select military only, civilian only, both, or none for own, teammate, hostile-major, neutral-major, city-state, and barbarian movement; military means positive combat or ranged-combat strength. Defaults are none for every faction except barbarians, which default to both, while storage remains unconditional for eligible events. Hotseat stores the unit category with queued speech and filters it at the observer's next turn start after the handoff closes. Temporary event traces were removed. In-game test pending across all four dropdown values and defaults, military/civilian classification, operation-origin timing, all relationships, first sightings, fog crossings, ordinary/queued/automated/direct movement, formation movement and formation movement-cost previews, relocation types, message location jumping, hotseat player isolation/turn-start delivery, and Lua.log errors.

Session note 2026-07-13: Removed the default `Ctrl+A`, `Ctrl+S`, and `Ctrl+G` gestures for opening the Resources, City Status, and Gossip tabs in Empire Reports. The three input actions remain available for custom bindings. The main Empire Reports `F2` gesture and separate Global Resources `Ctrl+Q` gesture remain unchanged. README, game API notes, and changelog updated.

Current focus 2026-07-13: Unit stat speech now labels every positive `GetRange()` value generically as `Range`, because the value is not exclusively an attack capability: an Observation Balloon returns `10`. UnitFlagManager's detailed map `S` aggregation now localizes count and name as one field (`2 Roman Trader`) instead of separating them with a comma. In-game test pending with identical stacked traders and combat/noncombat ranged units; the broader detailed-unit-info test remains pending with cursor movement, an embarked/fortified neutral unit, melee/ranged enemies, a promoted owned unit with an upgrade, and an aircraft carrier with cargo.

Session note 2026-07-12: Rotated the changelog for release `0.4.0`, created a fresh empty `Unreleased` section, and updated `src/CivViAccess.modinfo` from version `0.3.0` to `0.4.0`.

Session note 2026-07-12: Fixed World Scanner item cycling so Alt+Page Up and Alt+Page Down report first/last-item boundary crossings to the shared wrap-audio policy, matching category, subcategory, and group cycling. Initial item selection from index 0 remains silent. In-game test pending: wrap items in both directions and confirm one half-volume wrap cue only at the boundary.

Session note 2026-07-12: Added an Unexplored subcategory to the World Scanner Terrain category. It contains every hidden plot in one group without exposing concealed terrain data. In-game test pending: confirm Terrain > Unexplored lists all currently hidden plots, jumping works, and newly revealed plots leave the group after rebuilding the Terrain category.

Session note 2026-07-12: Updated `readme.md` to match the released hotkey changes: Civilopedia lookup now uses `Ctrl+I`, `Ctrl+Y` is documented as the combined yields and strategic-resources tree, and the Surveyor controls use `Shift+W` / `Shift+X`.

Session note 2026-07-12: The shared icon-processing speech pipeline now transliterates common accented Latin letters, Vietnamese characters, combining marks, and ligatures when the Civ VI display language is English. Other display languages preserve the original text. In-game test pending: with English display language confirm `Nam Việt` speaks as `Nam Viet`, representative ligatures such as `œ` and `æ` remain readable, and switching to a non-English display language preserves diacritics.

Session note 2026-07-12: Rotated the changelog for release `0.3.0`, created a fresh empty `Unreleased` section, and updated `src/CivViAccess.modinfo` from version `0.2.0` to `0.3.0`.

Current focus 2026-07-12: World Scanner city grouping is controlled by the World scanner setting `Group scanner cities by civilization`, enabled by default. Enabled groups cities by owner civilization; disabled makes every city its own group. My, City-States, Neutral, Enemy, All, and barbarian-outpost behavior remain unchanged. In-game test pending: confirm the setting appears below the other scanner settings, defaults on, groups a revealed multi-city civilization when on, and reaches every city by group navigation when off.

In-game test passed 2026-07-12: Shared sentence-based text splitting in
`caiUtils.lua` uses the configurable Token split length UI setting, defaults to
75 characters, and safely clamps invalid negative/zero values to 1. The section reader
and the Great People read-only biography viewer split long text correctly while
leaving oversized complete sentences intact.

Current focus 2026-07-12: Movement interface-info cursor speech now replays the engine-selected route and reports total movement cost plus nonzero movement remaining on arrival. In-game test pending across ordinary/rough terrain, rivers, roads/railroads, embark/disembark, terrain-ignore promotions/abilities, tunnels, formations, and ZOC.

Movement formation follow-up 2026-07-12: cost simulation now starts from the live formation movement pool when the unit is in a formation, and civilian/support arrival ZOC exhaustion uses engine-resolved `unit:IgnoresZOC()` so an escort's ZOC immunity is inherited correctly. In-game retest pending with an ordinary escort and a ZOC-immune cavalry escort.

Conditional future-turn movement 2026-07-12: movement cost now records local units' real turn-start plots and recalculates deterministic terrain/ownership start bonuses at every engine path turn boundary. Clear-terrain chariot bonuses and Bà Triệu's feature/territory bonuses use active abilities and database amounts; formations recalculate every member and use the slowest future maximum. In-game test pending for gaining and losing each bonus across a multi-turn path, including formations.

Queued-path and waypoint in-game test passed 2026-07-12: selected and non-selected unit waypoint speech, live route changes without reselection, scanner/plot waypoint membership, and relative arrival estimates work with fresh `GetMoveToPathEx(...)` snapshots.

Movement-cost feasibility 2026-07-12: an exact pre-move MP total is feasible as a CAI rules replay over the engine-selected `GetMoveToPathEx(...)` plots. The path result lacks per-node MP values, so CAI must calculate each directed edge from terrain, feature, improvement, route/pillaging, river, embark/disembark, tunnel, ZOC, formation, and the unit's live abilities/promotions. UI Lua exposes the necessary unit ability/promotion lists and plot/route/river state; database tables supply modifier definitions. Keep this isolated in a shared movement-cost helper, distinguish nominal edge cost from movement actually consumed at turn boundaries, and validate a rule matrix in-game before presenting the result as exact.

Session note 2026-07-12: Merged the plot-tooltip district, building, and great-work read bucket into the existing geography read on `B`, ordered after all geography information, removed the obsolete `PlotReadDistrictBuildings` input action and its localization, and reassigned Surveyor grow/shrink radius to `Shift+W` / `Shift+X`. The merged bucket now selects `cityDistrictTitle` for city centers and `districtTitle` only for non-city districts, preventing duplicate `City Center` speech. Documentation and changelog updated. In-game smoke test pending: verify `B` speaks geography first and then one structure title plus buildings/great works, Shift+X shrinks Surveyor radius, and Shift+W grows it.

Session note 2026-07-12: Cycled the changelog for release `0.2.0`, recorded `0.1.0` as the prior initial release, and updated `src/CivViAccess.modinfo` from the incorrect version `1` to `0.2.0`. The new Unreleased section is empty.

Session note 2026-07-12: World-input primary and secondary interface actions now dispatch from the triggered input-action event, including the primary overrides for targeting, move-to, district placement, and building placement modes. Per request, this internal input-routing correction was not added to the changelog. In-game smoke test passed: activate primary and secondary interface actions in normal world input and primary action in each affected interface mode, confirming each fires once on the completed input rather than on key-down.

Session note 2026-07-12: Fixed false unit-movement combat confirmations on hostile owned territory and non-attackable districts. Immediate confirmation now requires the vanilla engine-backed combat simulation to return a defender that `CombatManager.CanAttackTarget(...)` accepts, instead of inferring combat from an at-war plot owner. In-game test passed: quick-move into empty hostile/city-state territory and onto or beside non-attackable districts without receiving `No combat statistics`; then attack a valid enemy unit, city center, and attackable district and confirm the preview plus second-press confirmation still occur.

Session note 2026-07-12: Options-menu pulldowns containing exactly the vanilla Enabled and Disabled choices are now exposed as CAI checkbox widgets while preserving their vanilla handlers, tooltips, disabled/hidden state, and visual pulldown text. Other pulldowns remain dropdown widgets, and Switch UI Layout is intentionally excluded from the CAI widget list. In-game smoke test passed: open each Options tab, confirm Switch UI Layout is absent; confirm all two-choice Enabled/Disabled settings are announced as checkboxes with the correct initial checked state, toggle several in both directions, apply settings, reopen Options, and confirm the saved states; also confirm multi-choice pulldowns remain dropdowns.

Session note 2026-07-12: Unit quick-move hotkeys now reject an otherwise valid adjacent path when its engine-reported arrival turn is later than the current turn. They announce the localized `Not enough movement` message and return before the vanilla move commit, preventing a next-turn queued order; normal MOVE_TO activation remains unchanged. In-game test passed: exhaust or reduce a selected unit's movement below the adjacent tile cost, press a quick-move direction, and confirm the message is spoken with no queued destination; also confirm a reachable adjacent quick move and a normal multi-turn MOVE_TO order still work.

Session note 2026-07-12: Merged the top-panel yield breakdown and strategic-resource breakdown into the existing Ctrl+Y tree. The tree is titled `Yields and strategic resources` and has expanded `Yields` and `Strategic resources` categories; the resource category preserves the XP2 stockpile/flow breakdown and base-game amount rows, with an in-tree empty-state row. In-game smoke test passed: press Ctrl+Y, confirm the combined title, both categories start expanded, all prior yield/resource content is present, and Escape/focus-leave closes the tree normally.

Session note 2026-07-12: Added `changelog.md` with the first two player-facing changes. Project documentation now requires significant player-facing changes to be recorded there in concise, factual language without developer details, always following Keep a Changelog 1.1.0 with new entries categorized under `Unreleased`.

Session note 2026-07-11: Added brief Lenses and Map Tacs gameplay subsections to `src/readme.md`. Lenses are described as a native Civ VI system opened with Ctrl+J or activated automatically by relevant actions; active lenses add cursor speech and an Active Lens Scanner category. Map tacs are described as native markers spoken in cursor information and exposed through a Scanner category, with M place/edit and Ctrl+M list controls. Existing user-facing map pin/tack wording in the README was normalized to map tac(s).

Session note 2026-07-11: Moved the Expectations section in `src/readme.md` from before Gameplay to immediately before Credits, without changing its content.

Session note 2026-07-11: Added top-level Expectations and Credits sections to `src/readme.md`. Expectations preserves the requested candid limitations and roadmap notes: broader table usage is deferred, Mods/Credits screens and multiplayer Additional Content must be avoided, scenarios/Red Death/Pirates and the tutorial are unsupported, and World Builder is out of current scope. Credits name developers Flat-Arther and Hamada, Claude's assistance, funding from bsg-smoke and Nibar Sito, and Rashad's Civ V accessibility-mod ideas and guidance.

Session note 2026-07-11: Added a dedicated Selections section immediately before User Interfaces in `src/readme.md`. It explains mutually exclusive city/unit selection, remappable input actions, the shared Tab action list, unit/city cycling, and capital selection. City-context shortcuts moved out of the UI opener list. The new section lists all currently default-bound city actions, six-direction unit quick movement, every default-bound unit operation/command, and the complete context-dependent grave-accent plus Shift+1 through Shift+0 city/unit information map. Selection-specific entries were removed from World Actions.

Session note 2026-07-11: Completed the requested gameplay shortcut reference in `src/readme.md` with World Actions and User Interfaces sections. World Actions covers selection action lists, ready/all unit cycling, city cycling/capital selection, map pins, and turn actions. User Interfaces explains the common panel-plus-tree/list structure, Tab access to sibling action buttons, and TabControl navigation for screens such as World Rankings, Production Panel, and World Climate. It groups every configured CAI_UI opener by research/government, cities/empire, world information, and map/communication, and includes the relevant city-context Production Panel, city overview, and city-management bindings.

Session note 2026-07-11: Added gameplay sections for ending the turn and for notifications/message history to `src/readme.md`. The turn section explains changed-only spoken next actions from the Action Panel, Ctrl+Space end-turn/blocker behavior, and the Ctrl+Shift+Space Turn Blockers list. The combined information-history section distinguishes actionable current notifications from the per-player message buffer and documents Notification Center activation/dismissal plus all seven message-buffer navigation, category, edge, and location-jump bindings.

Session note 2026-07-11: Added an Empire Information subsection to `src/readme.md`. It groups the current read-on-demand input actions into time/research/culture, treasury/yields/resources, and diplomacy/government. It documents T/R/P/Y, G/F/I/Ctrl+Y/Ctrl+I, and V/K/O/N, including the fact that detailed diplomatic favor is exposed inside the Ctrl+Y yield-breakdown tree rather than through a dedicated single-key readout.

Session note 2026-07-11: Expanded `src/readme.md` with the first gameplay reference. It explains six-direction hex movement, relates the navigation cursor, Surveyor, and World Scanner to the Civ V accessibility mod, and documents their current default input actions. Cursor coverage includes movement, jump/pin and interface actions, plot-detail reads, and city-banner number-row reads; Surveyor coverage includes radius and all six reports; Scanner coverage explains its category/subcategory/group/item hierarchy, boundary behavior, jump/return, direction, and search. Documentation follows current code: `/` jumps to the selected unit or selected city, while initial cursor placement falls back to the capital when nothing is selected.

Session note 2026-07-11: Revised `src/readme.md` from user feedback. Installation no longer tells users to close the game or manually enable the automatically enabled mod. The UI guide now introduces nested widgets and focus paths, organizes navigation by container type, lists every common binding (including the in-game-only Civilopedia lookup), and separates activation/value-widget behavior and disabled-state semantics from navigation.

Session note 2026-07-11: Added `src/readme.md` with a concise introduction, support statement for the base, Rise and Fall, and Gathering Storm rulesets (excluding scenarios), Steam/Epic binary and user-mod-folder installation steps, and a player-facing accessible UI guide. The guide documents general navigation, Ctrl+H contextual shortcut help, and only widget types currently used by project screens. Gameplay documentation is intentionally deferred for the next pass.

Session note 2026-07-11: Added two common base-widget read-on-demand bindings. Shift+F1 speaks only the bindings installed on the currently focused widget, using the input-help formatter without ancestor bindings, world actions, or opening the help tree; widget-specific bindings are spoken first and common bindings last while preserving order within each group. Shift+F2 speaks the complete current focus path from root to focused leaf through the manager's existing `BuildAnnouncement(...)` pipeline. In-game smoke test passed: focus widgets with both widget-specific and inherited/common bindings; confirm Shift+F1 reads only that focused widget's binding list in specific-then-common order and Shift+F2 reads the full root-to-leaf path once.

Session note 2026-07-11: Great People row actions and tab order were updated in `src/UI/inGame/GreatPeoplePopup_CAI.lua`. Focused Great Person rows now use Enter for Recruit when the corresponding live action is available and otherwise speak that person's localized local-player progress line; Delete invokes Pass when its live action is available. Shortcut checks include the same capability, person-state, cost-presence, and read-only conditions as the contextual buttons. The read-only Biography edit box now follows Recruit, Pass, Gold patronage, and Faith patronage in the Great People page child order, so tabbing out of the tree reaches available action buttons first. The Heroes Recall with Faith label/tooltip retest passed. In-game test passed: focus recruitable and non-recruitable Great Person rows and verify Enter recruits or speaks local progress respectively; verify Delete passes only when available; Tab from the tree and confirm available action buttons precede Biography.

Session note 2026-07-11: Governor roster establishment-speed speech was corrected in `GovernorPanel_CAI.lua`. CAI previously combined the live `TransitionStrengthLabel` control's vanilla tooltip with its raw numeric text, producing `The speed at which this Governor becomes established in a city.: 3`. It now preserves the explanatory tooltip and formats the live value with vanilla's localized `LOC_GOVERNORS_SCREEN_GOVERNOR_TRANSITION_TURNS`, producing `The speed at which this Governor becomes established in a city. 3 Turns`. Other governor row content remains sourced from its existing mixture of live controls and CAI-composed game data. In-game retest passed: focus a normal governor roster row and confirm the establishment description is followed by the localized turn duration without the stray colon.
Session note 2026-07-11: Promotion relationship speech was aligned between `GovernorPanel_CAI.lua` and `UnitPromotionPopup_CAI.lua`. Both now format prerequisite lists with a localized final `or` (`A, B, or C`) and follow-on unlock lists with a localized final `and` (`A, B, and C`), including the requested comma before the conjunction for two-item lists. Added shared localization tags `LOC_CAI_AND` and `LOC_CAI_OR`; governor promotion tooltips now reuse the unit-promotion `Unlocked by` and `Unlocks` header tags instead of the old governor-only `Requires` and `Leads to` tags. In-game smoke test passed: inspect governor and unit promotions having zero, one, two, and three relationships and confirm headers, commas, and final conjunctions are spoken correctly.
Session note 2026-07-11: World scanner category, subcategory, and group cycling now reports actual boundary crossings to the shared `UIScreenManager:HandleNavigationWrap(...)` audio policy, reusing the half-volume `UI_MENU_WRAP` cue. Category detection uses available-category positions so skipped/empty category slots do not produce false results; initial group selection from index 0 is not treated as wrapping. In-game smoke test passed: cycle forward and backward across the category, subcategory, and group boundaries and confirm one half-volume wrap cue at each crossing, with no cue on ordinary steps or initial group selection.
Session note 2026-07-11: Added `src/sounds/UI/menu_wrap.wav` to `CAI_AudioDefinitions` as `UI_MENU_WRAP` (`UI_NAVIGATION` tag). Wrapping generic container navigation and Ctrl+Tab `TabControl` page cycling now emit a widget-level `navigation_wrap` event; `UIScreenManager:CreateWidget` installs the default listener that plays the cue through `CAIAudioManager` at half the live navigation-tag volume. Left/Right tab-strip wraps flow through generic container navigation. In-game smoke test passed: wrap forward and backward in a wrapping List/HorizontalList, on a tab strip, and with Ctrl+Tab/Ctrl+Shift+Tab; confirm exactly one half-volume wrap cue plays only when crossing an edge and no cue plays in non-wrapping trees/tables or ordinary moves.

Last compacted: 2026-05-29
Session note 2026-07-11: `EditBoxWidget:GetTooltip()` now prepends a localized interaction hint based on live configuration/state while preserving the screen-specific tooltip after it. Writable `AlwaysEdit` boxes say `Type text.`; inactive writable non-`AlwaysEdit` boxes say `Press enter to edit.`; read-only boxes and active non-`AlwaysEdit` boxes add no hint. Added `LOC_CAI_EDIT_HINT_TYPE_TEXT` and `LOC_CAI_EDIT_HINT_PRESS_ENTER` and documented the behavior in `docs/ui-manager.md`. In-game smoke test passed: focus a writable `AlwaysEdit` field, an inactive normal edit box, and a read-only viewer with existing tooltip help; confirm writable hints come first, activate the normal box and confirm its inactive hint disappears, and confirm the read-only viewer never adds an editing hint.
Session note 2026-07-11: Audited project-wide `StaticText` usage and enforced a single-channel text invariant in `src/UI/uiManager/CAIWidget_StaticText.lua`. Any label and tooltip supplied directly or through generic helper property tables are now exposed together by `GetLabel()`, joined with `[NEWLINE]`; tooltip-only content is promoted to the label, identical label/tooltip values are not repeated, and `GetTooltip()` is always empty for `StaticText`. The contract is documented in `docs/ui-manager.md`. In-game smoke test passed: focus representative static-text rows that formerly had descriptions/tooltips (for example Religion beliefs, City-States bonuses, World Congress result/details, and Hall of Fame victory progress) and confirm the complete text is spoken as the label with no separate tooltip channel.
Session note 2026-07-11: Great People Heroes recall action localization fixed in `src/UI/inGame/GreatPeoplePopup_CAI.lua` and `src/Text/en_US/cai_text_ui.xml`. The CAI button label now uses the dedicated `LOC_CAI_GP_RECALL_WITH_FAITH_LABEL` text (`Recall with Faith, {cost} Faith`) instead of exposing only the raw faith cost, while the vanilla `LOC_GREAT_PEOPLE_HEROES_FAITH_RECALL_TT` sentence and failure reasons remain in the button tooltip. In-game retest passed: focus a deceased owned hero's recall button and confirm its label identifies the Recall with Faith action and its tooltip separately explains the recall cost/failure reason.
Session note 2026-07-11: The `cityName` plot-info helper in `src/UI/inGame/PlotToolTip_CAI.lua` now appends localized `LOC_CAI_CITY_STATUS_CAPITAL` when the live `OwnerCity` reports `IsCapital()`, joined to the localized city name with `, `. In-game retest passed: read plot information for a capital and a non-capital city and confirm only the capital is announced as `City name, Capital`.
Session note 2026-07-11: End-game menu chat CAI was reworked in `src/UI/inGame/EndGameMenu_CAI.lua` to match the newer chat-panel/staging-room structure. The old read-only history edit box plus separate Send button were removed. The chat tab now uses a writable `AlwaysEdit` input with `CommitOnFocusLeave = false` and Enter-to-send, a chat-target dropdown mirrored through `PlayerTargetLogic`, and a focusable history `List` of individual static-text rows rebuilt from the live vanilla `ChatStack`. CAI now keeps a local mirrored target state for end-game chat, syncs it from the live vanilla pulldown label on build, updates the vanilla pulldown/edit/icon when CAI target selection changes, and re-syncs the CAI target options on player-info changes. `/help` output and incoming chat rebuild the CAI history from the live vanilla stack so end-game help/system lines stay mirrored. In-game retest passed: in a network multiplayer end-game screen, verify chat order is input then target then history, confirm Enter sends without focus-leave auto-send, confirm switching target updates correctly for All / Team / Whisper, confirm `/help` appears in the CAI history, and confirm new incoming/system chat lines appear once in the CAI list.
Session note 2026-07-11: End-game chat refresh follow-up in `src/UI/inGame/EndGameMenu_CAI.lua`. User reported that after sending chat, CAI spoke the sent message and then immediately re-spoke the focused input field (`Chat message, Edit, Type /help...`). Root cause: `OnChatReceived()` rebuilt the mirrored history list and then called `mgr:Refocus()`, which forced an out-of-band re-announcement of the currently focused widget even though the refresh was only a container-local history rebuild. The forced `Refocus()` call was removed so the end-game chat now follows the normal project pattern: rebuild the mirrored history container with `CaptureFocusKey` / `RestoreFocus`, but do not re-speak unrelated focused widgets. In-game retest passed: send a message in end-game chat and confirm CAI speaks only the actual chat line, without the immediate follow-up re-announcement of the input field metadata.
Session note 2026-07-11: Staging-room ready-button speech in `src/UI/frontEnd/Multiplayer/StagingRoom.lua` was narrowed per user request. The focused CAI ready control no longer auto-announces ready/unready label changes or tooltip changes during `UpdateReadyButton()` refreshes. It now speaks only the live countdown number, and only when the actual countdown button text changes while a countdown is active. In-game retest passed: in multiplayer staging, focus the CAI ready control, toggle ready/unready outside countdown and confirm there is no automatic re-speech; then start a ready/launch countdown and confirm only the countdown numbers are spoken once per tick.
Session note 2026-07-11: Selection-info hotkey localization in `src/Text/en_US/cai_text_ui.xml` was rewritten to describe the actual readout per hotkey instead of generic `slot N` text. The updated tooltips now say what each hotkey reads when a city is selected versus when a unit is selected, based on the current mappings in `src/UI/inGame/CityPanel_CAI.lua` and `src/UI/inGame/UnitPanel_CAI.lua`. Slot 10 is documented as city-only for now because `ReadSelectionInfo10` maps to `IgnoredFocusYields` on cities and has no unit-panel mapping yet.
Session note 2026-07-11: `src/UI/inGame/PlotToolTip_CAI.lua` plot-info request composition was refactored to match the bucket-helper model used elsewhere. The old half-dynamic path that built cursor/default/district request arrays by appending generated `building:N` / `greatWork:N` keys was removed. Plot tooltip requests now use declarative bucket definitions plus `PlotInfoBucketHelpers`, and buildings, great works, and yield groups are exposed as real `info.PlotInfoHelpers` entries that return their full line arrays directly. The remaining half-and-half yield path was also removed: `PlotReadYieldRiverOwner`, stats, and geography now use declarative buckets instead of building a partial bucket and then appending raw keys. Geography now also includes explicit natural-wonder title plus description through dedicated helpers, rather than relying only on the generic feature line. `info.GetCursorPlotInfoBucket()` now returns the actual declarative cursor bucket instead of the older priority-only list, and the public `info:RequestPlotInfo(...)` API now takes only `plotId, requestedKeys` instead of mixing plot objects with explicit ids. `src/UI/inGame/interfaceTargetHelpers_CAI.lua` was updated to use the new signature. In-game retest passed: move the cursor across ordinary plots, districts, cities, and great-work-holding tiles; verify cursor speech order is unchanged where intended, confirm `PlotReadDistrictBuildings` still reads building and great-work lines correctly without missing or duplicated entries, confirm `PlotReadYieldRiverOwner` still reads the expected yields plus worker/fresh-water/owner lines, confirm geography reads on natural wonders now include both the natural-wonder name and description, and confirm valid-target plot labels still resolve normally through the new plot-id-only request path.
Session note 2026-07-11: World scanner natural-wonder categorization fixed across both scanner paths in `src/UI/inGame/WorldScanner/WorldScannerCategory_terrain.lua` and `src/UI/inGame/WorldScanner/WorldScannerCategory_specialMapObjects.lua`. Root causes: the terrain scanner was collecting every revealed feature into `Terrain -> Features`, including `GameInfo.Features` rows flagged `NaturalWonder`; and the special-map-objects scanner's natural-wonder `Validate` callback compared `validateFeature.FeatureType` (string id such as `FEATURE_ULURU`) against the numeric `plot:GetFeatureType()` index, so every natural-wonder item was dropped during validation before the category was built. The terrain feature collector now skips `featureInfo.NaturalWonder`, and the special category now validates by live feature index with `validatePlot:GetFeatureType() == featureType`. In-game retest passed: on a revealed natural wonder, confirm it no longer appears under Terrain and does appear under Special map objects, Natural wonders.
Session note 2026-07-11: Religion screen pantheon presentation was reworked in `src/UI/inGame/ReligionScreen_CAI.lua` per user feedback. The standalone pantheon child row was removed from both `My Religion` and other religion tree rows. Pantheon data is now tooltip-only: religion row tooltips append the pantheon block last as `Pantheon: <name>` followed by the pantheon description on the next line, both joined through `[NEWLINE]`. `My Religion` tooltip no longer starts with the raw pantheon effect line; when the player has founded a religion it now speaks founder / holy city / dominance first and the pantheon name + description last, and when the player only has a pantheon while working toward a religion it appends the pantheon block after the progress text. In-game retest passed: focus `My Religion` and several founded religions, confirm there is no separate pantheon child item, confirm the tooltip order ends with `Pantheon: <name>` then the pantheon description, and confirm the old leading pantheon-effect line is gone.
Session note 2026-07-11: City-states send-envoys focus regression and row shortcuts were updated in `src/UI/inGame/CityStates_CAI.lua`. Root cause of the tab-order bug: `CAI_RebuildTree()` was clearing `m_selectedPlayerID` before a passive rebuild triggered by envoy confirmation, so while focus stayed on the separate Confirm button the slider became hidden for one navigation step; Shift+Tab therefore skipped straight to the tree, and only refocusing a row made the slider visible again. The rebuild now preserves the selected city-state when it still exists, re-syncs the vanilla action controls and slider from that selection, and uses `mgr:PrepareFocus(...)` to keep the tree's remembered row when focus is outside the tree during rebuild. City-state rows also now accept `Shift+Right` to add one envoy, `Shift+Left` to remove one envoy, and `Ctrl+Enter` to confirm pending envoy placement; add/remove speaks the new pending amount. In-game retest passed: on the city-states screen, add envoys with the slider then activate Confirm and verify immediate `Shift+Tab` lands on the slider instead of skipping to the tree; on a focused city-state row, verify `Shift+Right` and `Shift+Left` change the pending envoy count by one and speak the updated amount, and verify `Ctrl+Enter` triggers the normal confirm path.
Session note 2026-07-11: City-states follow-up cleanup in `src/UI/inGame/CityStates_CAI.lua`. The tree label no longer uses the old `City States, X met` summary; it now reads the two influence-summary lines that used to live in the read-only info box, and that info-box widget was removed entirely along with the extra generic help/sources line. Relationship rows were also checked against vanilla `RefreshRelationshipStack(...)`: vanilla only instantiates met entries, so CAI now skips unmet major/minor relationships instead of creating `Unmet civ` placeholders. The stray CAI relationship-builder speech/debug output was removed, and relationship rows now expose the vanilla diplomatic-tooltip text as the row tooltip instead. In-game retest passed: open City-States, confirm the tree label now reads the two influence-summary lines, confirm there is no leftover info edit box, and in a game with partial contact confirm the Relationships children include only the met civs/city-states that vanilla shows, with no extra spoken debug lines.
Session note 2026-07-11: City-states relationships follow-up follow-up in `src/UI/inGame/CityStates_CAI.lua`. User report showed CAI was still reading full war-sentence tooltips on every relationship row and was leaking the selected city-state as its own relationship entry, which produced speech such as `Hong Kong, Hong Kong is at war with Hong Kong`. CAI relationship rows now speak only the flattened label (`name, status`) with no extra row tooltip, visible relationship counts now match the filtered rows, and city-state relationship children now explicitly suppress the selected city-state self-entry in addition to the prior met-only filter. In-game retest passed: open a city-state with active wars, confirm the self-row is gone, confirm the relationship count matches the actual spoken children, and confirm each row now reads only the concise label instead of appending the full `X is at war with Y` sentence.
Session note 2026-07-11: Religion screen tab-sync bug fixed in `src/UI/inGame/ReligionScreen_CAI.lua`. Root cause: the CAI `AddTab(...)` wrapper was keying the special `"my"` tab only when `religionData` was nil, but vanilla passes real `religionData` to `ViewMyReligion` after the player has founded a religion. That meant focusing the CAI `My Religion` tree row called `DoTabSwitch("my")` with no captured button, so the vanilla tab stayed on the previous religion while other religion rows still synced correctly by numeric religion id. The wrapper now keys special tabs by callback identity (`ViewMyReligion` / `ViewAllReligions`) and falls back to call order only if needed. In-game retest passed: open Religion Screen after founding a religion, move focus from another religion back to `My Religion`, and confirm the vanilla tab, art, and panel content switch back to `My Religion` immediately.
Session note 2026-07-11: Tribal-village discovery was receiving duplicate audio because `NOTIFICATION_DISCOVER_GOODY_HUT` has no notification-handler `AddSound`, while its existing discovery cue is produced outside `NotificationPanel.lua`. `NotificationPanel_CAI.lua` now uses a `DEFAULT_SOUND_EXCLUDED_TYPES` set containing both `NotificationTypes.PLAYER_MET` and `NotificationTypes.DISCOVER_GOODY_HUT`, so neither receives the neutral fallback. In-game retest passed: discover a tribal village and confirm only its original game cue plays while the notification still appears and is announced normally.
Session note 2026-07-10: Notification fallback guard audit against vanilla follow-up. `PlayDefaultNotificationSound(...)` in `src/UI/inGame/NotificationPanel_CAI.lua` matches vanilla's local-player alive check and `m_isLoadComplete` gate, and now also uses the same `IsNotificationAvailable(...)` predicate as the CAI notification center. The fallback therefore requires a visible, non-dismissed, non-expired notification backed by a live vanilla rail instance. This is intentionally narrower than vanilla's built-in `AddSound` behavior so fallback sounds correspond only to notifications available in the CAI center. In-game retest passed: confirm no neutral fallback plays for end-turn blockers, filtered/iconless notifications, or duplicate adds that do not produce a rail entry.
Session note 2026-07-10: Clarification from user: notification buffer location payload is intentional and must stay attached for jump-to-location behavior; only spoken text should avoid reading location unless explicitly requested elsewhere. `src/UI/inGame/NotificationPanel_CAI.lua` keeps the buffer entry location attachment and the earlier `notification:GetTarget()` bug fix remains valid.
Session note 2026-07-10: Notification sound fallback implemented in `src/UI/inGame/NotificationPanel_CAI.lua`. The file now follows the project wrapper pattern by using `WrapFunc(...)` for `RegisterHandlers`, `LookAtNotification`, `OnNotificationAdded`, and `OnNotificationDismissed` instead of caching/reassigning originals manually. `OnNotificationAdded` now plays `ALERT_NEUTRAL` for local-player notifications whose resolved handler has no `AddSound`, while explicitly skipping `NotificationTypes.PLAYER_MET` because vanilla `OnMetCivAddNotification(...)` already plays its own custom sound. Also fixed a notification speech bug in the target-location path where the code was calling `pNotification:GetTarget()` on an undefined variable instead of the live `notification` object. In-game retest passed: trigger several vanilla notifications that previously had no add sound, confirm they now play one neutral alert exactly once, confirm met-civ still plays only its original special sound, and confirm notification speech still resolves target unit/city locations without Lua errors.
Session note 2026-07-10: Notification sound fallback investigation completed. Base vanilla `decompiled/Assets/UI/Panels/NotificationPanel.lua` plays notification add audio centrally in `OnNotificationAdded(...)` after `handler.Add(pNotification)` by checking the resolved handler table's `AddSound` field. Because many built-in notification types already have explicit handler tables from `RegisterHandlers()` but no `AddSound`, changing only `NotificationTypes.DEFAULT` would not cover them. Preferred CAI hook if we want a general fallback is to wrap `OnNotificationAdded(...)` or post-process every registered handler after `RegisterHandlers()`, and when `handler.AddSound` is nil or empty play `ALERT_NEUTRAL` as the default. Durable notes were added to `docs/game-api.md`; no code change yet.
Session note 2026-07-10: Cursor audio route/crossing pass completed in `src/UI/inGame/cursorAudio_CAI.lua` and `src/data/audioDefinitions_CAI.sql`. Added raw sound definitions for `metalWalk.wav`, `riverCrossing.wav`, `roadWalk.wav`, and `woodWalk.wav`; cursor playback now follows the Civ V-style slotting model: optional crossing at `t=0`, bed plus fog at the next slot, and feature/route stingers one slot later. Civ VI bridge resolution is based on decompiled route data rather than the old Civ V team-tech check: if the move is a river crossing and both endpoint plots have non-pillaged routes whose `GameInfo.Routes` rows set `SupportsBridges`, CAI plays `woodWalk`; otherwise river crossings play `riverCrossing`. Destination plot route stingers now use `roadWalk` for normal roads and `metalWalk` for `ROUTE_RAILROAD`, and bridge crossings suppress the redundant route stinger just like the old Civ V plot-audio logic. In-game retest passed: move the CAI cursor onto plain roads, railroads, ordinary river crossings, and bridged crossings; confirm the order is crossing first, then terrain/fog, then stinger; confirm medieval+ road bridges use `woodWalk`, unbridged ancient-road river crossings use `riverCrossing`, and railroad tiles use `metalWalk` when no bridge cue already covers the move.
Session note 2026-07-10: Cursor crossing cues are now additionally gated on cursor move reason. `cursorAudio_CAI.lua` now reads `state.reason` from `LuaEvents.CAICursorMoved` and suppresses river/bridge crossing sounds unless the move reason is exactly `step`; terrain, fog, and destination route/feature sounds still play for non-step moves such as select/jump.
Session note 2026-07-10: Shared raw audio now honors the vanilla `Sound -> Mute Focus` option. `audioManager_CAI.lua` checks `Options.GetAudioOption("Sound", "Mute Focus")` and `CAI.IsGameWindowFocused()` before raw `Play(...)`, so immediate and queued CAI sounds are skipped while the game window is unfocused/minimized only when that option is enabled. The manager also tracks the muted-focus state and stops already-playing raw sounds once on transition into that state so custom audio does not continue after minimizing the game. In-game retest passed: enable `Mute Focus`, trigger a looping/long CAI sound, minimize or alt-tab away, confirm active CAI sounds stop and queued CAI sounds do not fire until focus returns; then disable `Mute Focus` and confirm CAI audio continues to play normally while unfocused.
Session note 2026-07-10: Shared internal logging scaffold added in `src/UI/shared/CAI_logging.lua` and loaded through `src/UI/shared/caiUtils.lua`. The logger shape is intentionally small: levels `message`, `warn`, `error`, and `off`; `Log`, `Message`, `Warn`, `Error`; and internal `GetLevel` / `SetLevel` backed by `CAI.GetConfigValue` / `CAI.SetConfigValue` under a non-DB config key so beta verbosity can be adjusted without adding `CAI_Settings` rows. No screen/module call-site rollout yet; next step is choosing which existing raw `print(...)` diagnostics should become structured log calls.
Session note 2026-07-10: Core logging rollout started. Structured logger calls now cover the UI manager, `cursor_CAI.lua`, `audioManager_CAI.lua`, `RevealAnnouncements_CAI.lua`, `WorldScanner_CAI.lua`, and `Surveyor_CAI.lua`. This first pass is intentionally conservative: existing raw diagnostic prints were converted to `warn` / `error`, and a few subsystem lifecycle breadcrumbs were added at `message` level for initialization / shutdown / clear flows. Broader screen-level logging still pending.
Session note 2026-07-10: UI-manager helper logging pass added diagnostics across `src/UI/uiManager/helpers/`. Dialog builder now logs failed inputs plus dialog/popup conversion counts; Settings helper logs unsupported UI types plus open/build/close lifecycle; Input Help and Pedia Lookup now log blocked-open cases, result counts, and open/close lifecycle; Search helper now logs missing contexts, no-hit terms, buffer no-match/clear events, and focused result selection; the section reader logs cache build/clear counts; Tree and Navigation helpers now log empty/boundary/non-expandable cases; and EditBox helper logs max-length / validator rejections. This is intended as beta instrumentation for helper-layer silent failures, not per-keystroke tracing.
Session note 2026-07-10: Logging noise was then trimmed per user direction: tooltip-reader cache/clear logs were removed, and type-to-find `no results` no longer logs while still speaking the existing no-match message. In-game logging rollout now covers the remaining raw diagnostic `print(...)` sites under `src/UI/inGame/`: cursor audio, movement helpers, map search panel lifecycle, message buffer, plot tooltip, trade origin chooser fallback, city/unit selection plot resolution, and world-input gameview/cursor guards now use structured `LogMessage` / `LogWarn` / `LogError` calls instead of bare prints. `RevealAnnouncements_CAI.lua` also gained deeper beta diagnostics for per-player initialization/bootstrap counts, flush reasons, deferred between-turn flushes, missing-state warnings, and active-player clear/turn-start/turn-end lifecycle.
Session note 2026-07-10: `MessageBuffer_CAI.lua` got a dedicated follow-up logging pass. The buffer now logs per-player state creation through the `PlayerStateManager` create callback, warns when capacity settings clamp to `1`, warns when jump-to-location is invoked without a current entry or valid plot coordinates, and logs missing/invalid player buffer state through `GetActive`, `GetForPlayer`, and `ClearActive`. Buffered message text itself is still never written to the log.
Session note 2026-07-10: Options-menu volume slider parity pass in `src/UI/shared/Options.lua` was verified in-game by the user: Master, Music, Effects, Ambient, and Speech now move smoothly in 1% increments without the old odd jumps.
Session note 2026-07-10: Keybindings-tree focus refresh simplification in `src/UI/shared/Options.lua` was verified in-game by the user: after changing or clearing a CAI keybinding, focus now returns to the same action row via the UI-manager `PrepareFocus(...)` path.
Session note 2026-07-10: Options-menu checkbox activation parity pass completed in `src/UI/shared/Options.lua`. CAI `W_Checkbox(...)` no longer calls the stored checkbox callback manually or writes `SetSelected(...)` directly; it now activates the underlying vanilla checkbox through `ctrl:DoLeftClick()` only when the requested checked state differs from the live control state. In-game retest passed: open Options and toggle several CAI checkboxes across tabs such as Audio, Graphics, and Application; confirm each toggle updates once, speaks the correct state, and still triggers the normal vanilla side effects and Confirm enablement.
Session note 2026-07-10: AdvancedSetup start-blocker parity pass completed in `src/UI/frontEnd/AdvancedSetup.lua`. The CAI Start action now reads the live vanilla Start button tooltip so setup blockers are exposed directly on the action itself; leader and generic dropdown option lists now append vanilla `InvalidReason` text when present, and closed leader dropdown tooltips also include the current invalid reason; and the vanilla `ConflictPopup` is now mirrored through a CAI dialog patterned after `src/UI/shared/SaveGameMenu.lua`, but using a simpler vanilla-aligned lifecycle: create/push only on the `UI_PostRefreshParameters()` transition where vanilla unhides `Controls.ConflictPopup`, and remove it when the vanilla confirm button hides the popup or when the screen closes. Durable vanilla findings were added to `docs/game-api.md`. In-game retest passed: trigger a player-parameter conflict such as duplicate leaders, confirm the Start action exposes the blocker tooltip, confirm the conflict dialog opens once without duplicate body speech and closes through Continue, and confirm invalid dropdown choices/readback include their error text in both the local-player and AI leader selectors as well as any generic invalid setup dropdowns.
Session note 2026-07-10: AdvancedSetup parameter widgets were realigned with the HostGame pattern in `src/UI/frontEnd/AdvancedSetup.lua`; checkboxes now use only the built-in checkbox value reporting plus `SetChecked(..., true)` and `SetValueSetter(...)` with live vanilla hidden/disabled predicates, removing the mistaken custom enabled/disabled-style value getter from the mirrored AdvancedSetup checkboxes. Dropdowns now commit through `SetValueSetter(...)`, sliders in both basic and advanced parameter mirrors now also use `SetValueSetter(...)`, and edit boxes now match vanilla AdvancedSetup limits with number-only mode for `int` / `uint`, max length `16` for integer domains, and max length `64` for text. Basic-list mirrored simple parameters also gained a refresh pass so checkbox, slider, dropdown, and non-active edit-box state resync after vanilla parameter refreshes. In-game retest passed: open Advanced Setup, exercise bool, dropdown, slider, `int`, `uint`, and `text` parameters in both the basic mirrored list and the full advanced list, confirm disabled game-mode checkboxes announce disabled while still speaking the normal checkbox state, confirm integer edit boxes reject non-digits, and confirm the CAI widgets stay in sync with vanilla after parameter refreshes.
Session note 2026-07-10: AdvancedSetup hidden/disabled predicate audit against vanilla completed in `src/UI/frontEnd/AdvancedSetup.lua`. Fixed mismatches include: local advanced color dropdown now distinguishes hidden from disabled instead of treating disabled as hidden; local/basic leader dropdowns and fixed basic controls (ruleset, difficulty, speed, map size, map type, action buttons) now mirror live vanilla disabled state; slider mirrors now follow vanilla availability more closely by hiding when the underlying slider control is hidden; basic edit-box mirrors now expose disabled state from the live edit control; and the shared live-control disabled helper now checks the actual control families AdvancedSetup uses (`CheckBox`, `Button`, `PullDown`, `OptionSlider`, `Root`, `StringRoot`, `StringEdit`). In-game retest passed: open Advanced Setup and verify hidden/disabled announcements on fixed basic controls, local leader/color controls, disabled game modes, disabled/degenerate sliders, and disabled text/integer fields.
Last updated: 2026-07-10 (city-panel yield-focus dropdown redo: `CityPanel_CAI.lua` now builds those dropdowns through a dedicated helper that matches the project’s normal CAI `Dropdown` pattern instead of using custom sync logic; each dropdown now does only three things: create widget, set options, seed the initial selection with silent `SetSelectedIndex(..., true)`, and use `SetValueSetter(...)` for the real write-through to `CAI_SetCitizenFocusTo(...)`; removed the nonstandard focus-enter sync and removed the immediate post-change re-sync that was causing the widget to close and speak the stale old value before the live state caught up; In-game retest passed: open a city, open the citizen yield focus list, change multiple yields between normal / favored / ignored, confirm the dropdown closes and speaks the newly chosen value immediately, and confirm reopening the list reflects the live city state correctly; city hotkey config pass: `src/data/hotkey_config_CAI.xml` now keeps only compact city-panel actions in the `CITY` category, removing the overview-tab actions (`CityOpenBuildings`, `CityOpenLoyalty`, `CityOpenPower`, `CityOpenReligion`, `CityOpenAmenities`, `CityOpenHousing`, `CityOpenCitizens`) plus the separate `CityPurchaseTile` / `CityManageCitizens` entries; new `CityManageCity` input action added instead, wired to the existing combined citizen-management / tile-purchase toggle in `CityPanel_CAI.lua`; the city action list no longer injects `Manage city` manually and now picks it up from the normal input-action registry like the rest; default bindings added: `CityChangeProduction` = `Alt+\`, `CityToggleOverview` = `Alt+]`, `CityManageCity` = `Alt+[`; documented in `docs/game-api.md`; In-game retest passed: open a city, open Selection Actions, confirm `Manage city` appears once with its binding, confirm the removed overview-tab / purchase-tile / manage-citizens entries are gone from keybindings, and verify `Alt+\`, `Alt+]`, and `Alt+[` trigger change production, toggle city details, and manage city respectively; climate screen follow-up: `ClimateScreen_CAI.lua` now keeps read-only leaf content in the label instead of splitting it across label and tooltip; `MakeLeaf(...)` folds any prior tooltip text into the `StaticText` label with `"[NEWLINE]"` joins, and the screen-local text normalizer now preserves `"[NEWLINE]"` instead of flattening it to comma-separated speech; CAI-built climate leaf labels that previously used `", "` joins were also normalized to newline-separated chunks, including current event overview, climate contributions, forecast entries, polar ice, sea level, and CO2/resource rows; In-game retest passed: open Climate Screen, especially Overview and CO2 Levels, confirm static-text rows now speak all of their details directly from the label without requiring tooltip reads, and confirm the output is newline-separated rather than comma-chained; world rankings label follow-up: the user confirmed the prior world-rankings tree/static-text pass works in-game; `WorldRankings_CAI.lua` now stops using vanilla `GetCivNameAndIcon(...)` for spoken player labels and instead builds localized `leader, civilization` labels directly, so entries like `Wilhelmina, ICON_CIVILIZATION_NETHERLANDS, 0 of 20 Diplomatic Victory Points, 2 of 7` should now read with the actual civilization name instead of the leaked icon tag; the replacement is applied everywhere World Rankings speaks a player label on this screen, including overall first-place summaries, score/science/culture/domination/religion/generic player rows, and domination/religion child rows such as captured-from and converted-civ lines; In-game retest passed: open World Rankings, especially the diplomatic victory tab, confirm `ICON_*` tags are gone from spoken labels, and confirm player rows now read as `leader, civilization` everywhere they appear; world rankings follow-up: `WorldRankings_CAI.lua` now emits passive leaf rows as `StaticText` instead of leaf `TreeItem`s, and non-expandable/non-activatable ranking rows such as overall-victory summaries, score rows with no category children, domination rows with no captured-capital children, religion rows with no converted-civ children, and generic/diplomatic rows with no requirement or diplomatic-detail children now stay `StaticText` as well, so read-only world-rankings rows no longer announce tree expand/collapse state; CAI-composed world-rankings labels/tooltips that previously joined parts with `", "` now normalize to `"[NEWLINE]"` joins, including overall summaries, score rows, science milestone/light-year readouts, culture rows, domination rows, religion rows, diplomatic-point labels, and team summary labels; city overview panel follow-up: `CityPanelOverview_CAI.lua` now emits passive leaf rows as `StaticText` instead of leaf `TreeItem`s, and built district rows without child buildings also stay `StaticText`, so non-expandable/non-activatable city-overview rows no longer announce tree expand/collapse state; CAI-composed city-overview labels/tooltips that previously flattened or joined parts with `", "` now normalize to `"[NEWLINE]"` joins instead, including citizens summary chunks, food summary, amenities, housing, governor status, and trading-post lists; In-game retest passed: open City Details, move through Citizens/Buildings/Religion/Loyalty/Power, confirm passive rows no longer speak `collapsed`/`expanded`, confirm district rows only expose tree behavior when they actually have child buildings, and confirm the updated newline-separated summaries read naturally in the screen reader; reports screen follow-up: `ReportScreen_CAI.lua` now emits passive rows as `StaticText` instead of leaf `TreeItem`s, so non-expandable/non-activatable report rows no longer speak `collapsed`; city-income district rows now keep subtree totals in the district tooltip by aggregating district own yields plus adjacency, buildings, and attached great works; leaf-only district/wonder/resource rows are also emitted as static text so all read-only info stays in the label; Empire Economy child rows now rely on updated CAI loc tags in `src/Text/en_US/cai_text_ui.xml` that use `[NEWLINE]` directly for gold, faith, science, culture, and tourism readouts; report-screen line composition was also normalized to use `"[NEWLINE]"` joins in CAI-built labels/tooltips instead of screen-local comma concatenation; documented the vanilla data shape in `docs/game-api.md`; In-game retest passed: open Reports -> Yields and Resources, confirm read-only rows no longer announce expand/collapse, confirm city/district parent rows still expand normally, confirm district tooltips such as `City Center` speak the aggregated totals the user expects, and confirm the Empire Economy child rows now read as newline-separated chunks; production panel recommendation analysis: base vanilla `ProductionPanel.lua` does not vary recommendation visuals by advisor type; it caches `BuildItemHash -> BuildItemScore` from `GetBuildRecommendations()` and only shows a single generic `RecommendedIcon` on matching rows, with no advisor lookup or recommendation-color branch; documented in `docs/game-api.md`; tech/civic tree recommendation follow-up: `TechTree_CAI.lua` and `CivicsTree_CAI.lua` now use the shared `GetRecommendedPart(...)` helper for row labels, so recommended nodes speak `Recommended by {advisor}` with the localized advisor type just like the chooser screens instead of the generic recommended tag; previous 2026-07-09 work retained: governor panel focus-restore cleanup: promotion refresh now follows the UI-manager standard async restore path instead of hard-jumping focus after a promotion; `GovernorPanel_CAI.lua` passes the pending promotion `FocusKey` into `mgr:PrepareFocus(...)` while the confirm dialog is still on the stack, then lets focus naturally descend back to the rebuilt promotion when the dialog is removed, and the old `FindByFocusKey(...)` + `SetFocus(...)` restore hack is gone; governor panel biography follow-up: `GovernorPanel_CAI.lua` no longer exposes a separate biography widget; instead each governor row tooltip now includes the governor biography from `Locale.Lookup(pGovernorDef.Description)` immediately before the earned abilities section, keeping the surface to the tree plus promotions sibling; documented the vanilla biography control/source in `docs/game-api.md`; diplomacy action view follow-up: selected leader subtree now rebuilds eagerly from stable data during vanilla refresh/select paths instead of lazily populating intel nodes on focus_enter; tab metadata is captured from vanilla's finished tab/panel build, leader-row overview tooltips are now keyed to the row's player id rather than the global selected-player state, static-text intel rows keep all content in labels instead of tooltips, gossip/access/relationship tab tooltips are rebuilt from game data, foreign-relationship duplicate tooltip echo removed, and agenda lines are deduplicated; diplomacy action view leaders tree restructured: overview node removed, leader-row overview tooltips added, self leader row made a leaf with GameSetup-style tooltip, relationship/access rebuilt from APIs, alliance and grievances summaries moved to section tooltips; diplomacy deal view follow-up: resource and favor inventory rows no longer repeat bare name tooltips when the label already contains the name plus quantity, offer diff speech now detects same-item amount/target changes, side announcements now speak removed first, then added, then changed, deal-view tooltips now lean on vanilla tooltip functions/logic more broadly (`MakeCityToolTip`, `GetParentItemTransferToolTip`, direct `GetGreatWorkTooltip`, empty main tooltips where vanilla leaves them empty, invalid resource no-cap-room text, plain city-child great-work support text), and the unacceptable / stop-asking text now uses vanilla loc keys (`LOC_DIPLO_DEAL_UNACCEPTABLE_ITEM_TOOLTIP`, `LOC_DIPLO_DEAL_MARK_UNACCEPTABLE`) instead of the broken/wrong tags; shared raw audio manager scaffolded; cursor sounds moved to sounds/cursor; woosh reassigned to fog; cursor bed mapping aligned closer to Civ V; audio manager gained skip-if-playing option; ide helpers now document audio manager API; UI manager transparent-entry follow-up: transparent containers now honor optional `UseDirectionalEntry = false`, so grid tiers stay `Transparent` for speech suppression but no longer force Shift+Tab / reverse entry to the last cell in a column; documented in `docs/ui-manager.md` and `src/ideHelpers.lua`; civics/tech tree grid views now reuse the same main-list labels as their treeview counterparts instead of separate `..._GRID` labels, relying on the grid widget role to distinguish the surface)

## UI Manager Rework (branch: UIManagerRework)

The UI manager has been rewritten as a class-based widget framework. The four old files (`baseWidget.lua`, `widgetTemplates.lua`, `widgetTemplateHelpers.lua`, `UIScreenManager.lua`) are deleted. The new framework lives under `src/UI/uiManager/` and `src/UI/uiManager/helpers/` with `CAIWidget_*` / `CAIWidgetHelpers_*` / `CAIUIScreenManager` file naming for VFS uniqueness.

- Framework is complete: `UIWidget` / `ContainerWidget` / `ValueWidget` base classes plus 20 concrete widgets (Button, MenuItem, StaticText, Panel, Dialog, List, HorizontalList, SubMenu, Tree, TreeItem, Dropdown, Checkbox, Slider, EditBox, TabControl, Tab, TabPage, Table, GameView, InterfaceMode). Five helper modules (Navigation, Search, Tree, EditBox, DialogBuilder).
- Manager-level additions over the old model: multi-listener event system (`On/Off/Emit`), single-source-of-truth focus path with directional entry (Windows tab-stop semantics), `FocusKey` + `CaptureFocusKey` + `RestoreFocus` for rebuild-survival, `Refocus` and `Announce` for out-of-band re-speak, path pruning on destroy, hidden-ancestor input skip, `_focusSound` auto-played on focus enter, `Transparent` widget flag, PgUp/PgDn paging, same-letter type-to-find cycling.
- Speech follows the Windows one-line-per-widget model. Multi-line emission routes through `SpeakLines(lines, interrupt)` where only the first line interrupts; the rest queue.
- Docs: `docs/ui-manager.md` is the source of truth (19 sections — architecture, focus, speech, events, widgets, helpers, migration). LuaLS annotations in `src/ideHelpers.lua`.
- **Migrated screens (all verified in-game unless noted):** MainMenu CAI block, `InGameTopOptionsMenu_CAI`, `ResearchChooser_CAI`, `CivicsChooser_CAI`, `ProductionPanel_CAI`, `CivicsTree_CAI`, `TechTree_CAI`, `GovernmentScreen_CAI`, `NotificationPanel_CAI`, `CivilopediaScreen_CAI`, and `CityStates_CAI` use the new framework. `ReligionScreen_CAI` (in-game test passed) also uses the new framework (CreateWidget, On("activate"/"focus_enter"/...), CaptureFocusKey/RestoreFocus, FocusKey rows, Push with mirrored vanilla flow). The disposable test screen and `UI/test/` directory were deleted. Every other `*_CAI.lua` still uses the old API and must be ported per `docs/ui-manager.md` §16 (see Known Remaining Work).
- **InGameTopOptionsMenu migration notes:** main button list activates rows by calling `child:DoLeftClick()` on the underlying vanilla `GridButton` instead of mapping controls to specific action functions; children of `MainStack` are filtered by non-empty `GetID()` so unnamed spacer Containers are skipped. The information section (`DetailsBox`) and the mods section (`ModsInUse`) are read-only `AlwaysEdit` EditBoxes — `SetText` normalizes `[NEWLINE]` to `\n` so no manual newline splitting is needed for the version tooltip; the mods listing's vanilla single-space spacer instance is emitted as a real `\n` separator, but only when it sits between real entries (leading/trailing spacer instances are trimmed). Expansion include chain: the file follows `Civ6Common`'s `IsExpansion1Active`/`IsExpansion2Active` to include `Expansion1_InGameTopOptionsMenu` when an expansion is active — required so the expansion's `LateInitialize` registers the `ExpansionNewFeatures` eLClick callback that `DoLeftClick()` relies on.
- **Expand/Collapse silent flag + recursive collapse + ancestor auto-expand (2026-05-29).** `TreeItemWidget:Expand(silent)` / `Collapse(silent)` and `SubMenuWidget:Expand(silent)` / `Collapse(silent)` now take a `silent` flag that suppresses **both** the `expanded`/`collapsed` event and speech (use for seeding initial state or auto-expanding). `Collapse` always recursively collapses every descendant in place (silent, no events) so re-expanding shows one clean level. **Speech rule: only user-driven toggles speak.** `TreeItem` focus speech still announces expand/collapse state (value element) on every node as the user navigates, and a deliberate Left/Right/Enter toggle speaks the new state via `SpeakElements({"value"})` (the default non-silent path used by the Tree key handlers). Every automatic/programmatic expand/collapse passes `silent=true` (seeding via `ExpandAllTreeItems`, CivicsTree/TechTree tier seed + era re-expand listener, `BuildFocusPath` ancestor auto-expand, GovernmentScreen seed), so opening an all-expanded tree/article and jumping focus never spam "expanded" — that earlier spam was automatic expands speaking before the silent flag existed. `CAIWidgetRegistry.ApplyProps` no longer drops setter-less primitive props or warns — it direct-assigns any prop with no `Set<Name>` setter (function or primitive). `BuildFocusPath` auto-expands collapsed expandable ancestors of a focus target (silent `Expand(true)`) so a target buried in a collapsed node is reachable; the target keeps its own state, and only path ancestors (not siblings) are expanded, so a jump into a collapsed CivicsTree/TechTree era reveals just the focused tier. Manual `widget.IsExpanded = true` writes were replaced with `widget:Expand(true)` in `CivilopediaScreen_CAI.lua` (`ExpandAllTreeItems`), `CivicsTree_CAI.lua`, and `TechTree_CAI.lua`; `ProductionPanel_CAI.lua`'s category seed stays a direct write because items are added after the node is created (Expand no-ops on a childless leaf). Unmigrated old-API screens (`DiplomacyActionView_CAI.lua`, `UnitPanel_CAI.lua`) still write `IsExpanded` directly and will be converted on their own migration. Verified in-game.
- **Manager mirrors vanilla flow** (durable rule). CAI never drives push/pop on its own; it observes vanilla's screen state and uses `mgr:RemoveFromStack(id)` (not `Pop`) when vanilla closes/rebuilds a section. For vanilla refreshes, refresh only the mirrored widget container that vanilla refreshed; do not pop/re-push the CAI root or rebuild unrelated siblings unless explicitly instructed by the user. Use the UI manager rebuild tools (`CaptureFocusKey` / `RestoreFocus`, or `PrepareFocus` when refreshing an unfocused container) to preserve focus locally. Escape is NOT bound at the manager level — vanilla owns Escape, CAI follows. MainMenu submenu teardown is driven by inspecting `m_currentOptions[i].isSelected` after vanilla's `ToggleOption` runs.
- **No fallback labels/tooltips when vanilla source is known** (durable rule, 2026-07-04). If vanilla Lua or XML clearly defines a control's text or tooltip, CAI should read that live control text/tooltip directly and let missing text surface as a bug. Do not add fallback labels/tooltips that mask broken or unexpectedly empty vanilla control state. The existing `GetText()` / `GetToolTipString()` wrappers returning `""` are enough.
- **EditBox AlwaysEdit highlight behavior (2026-06-08).** `HighlightOnEdit` on writable `AlwaysEdit` edit boxes is applied silently from the widget's `focus_enter` handler, so text is selected when focus lands on the box without speaking a separate edit-start line. Focus entry does not otherwise reposition the cursor. Read-only `AlwaysEdit` viewers preserve their cursor when focus leaves and returns. Silent `BeginEdit(true)` only activates the edit buffer; programmatic `SetText(...)` clears selection until focus entry reapplies it for writable boxes with `HighlightOnEdit`.
- **EditBox focus-leave commit flag (2026-07-04).** `EditBoxWidget` now exposes `CommitOnFocusLeave`, defaulting to `true` so existing writable `AlwaysEdit` boxes keep their prior auto-commit behavior when focus leaves. `UI/frontEnd/Multiplayer/StagingRoom.lua` chat input and `UI/inGame/ChatPanel_CAI.lua` chat input set `CommitOnFocusLeave = false`, so moving focus away from chat preserves the buffered text instead of committing or sending it; Enter remains the commit/send path.

## Purpose

Civilization VI accessibility mod for blind players. Adds TTS and screen-reader navigation through Lua UI replacements and wrappers.

- Authors: FlatArther and Hamada
- Source entry: `src/CivViAccess.modinfo`
- Deploy model: `src/` is symlinked into the Civ VI mod folder
- Core rule: all TTS goes through `Speak()` from `caiUtils.lua`; `ProcessText` (in `textProcessing.lua`) runs inside `Speak` by default (third arg `processTokens`, true when nil, false bypasses). `ProcessText` is the ONE place that filters text (tag strip, icon resolve, ASCII-safe whitespace/comma tidy); screens must not re-filter. Never use `%s`/`%S` on localized text — locale-sensitive, corrupts UTF-8 `0xA0` (Simplified Chinese); use `[ \t\r\n]` sets.
- API notes and discovered behavior live in `docs/game-api.md`

## Gate Check

- Tier 1 analysis is complete for the current codebase.
- Before new feature work: check `docs/game-api.md`, then verify uncertain behavior against actual Lua in `decompiled/` or the Steam install.
- Safe mod keys, input/context rules, localization rules, and screen-specific patterns are documented in `docs/game-api.md`.

## Current State

The large implementation backlog from earlier sessions is considered resolved for tracking purposes and has been compacted out of this file at the user's request.

- Core in-game CAI systems are implemented and integrated.
- Shared UI manager, widget system, and focus-routing infrastructure are in place.
- Major screen replacements and wrappers are in place for city, unit, diplomacy, government, civilopedia, world input, scanner, notifications, top-panel/world-tracker style reads, and related helpers.
- Map tacs / map pins now have shared in-game label helpers in `inGameHelpers_CAI.lua`. `MapPinListPanel_CAI.lua` and `MapPinPopup_CAI.lua` use the shared icon/name resolution, `WorldScannerCategory_mapTacs.lua` adds a scanner category with dynamic subcategories (`My`, then one per other visible pin owner), non-local scanner and plot-info labels append the owner name, and `PlotToolTip_CAI.lua` speaks visible tacs on the cursor plot as one `Map tacks: {list}` line. File and ImportFiles entries plus localization tags are added. In-game test passed: create own and remote/shared tacs, confirm scanner category/subcategories/grouping, jump behavior, list/popup labels, and cursor plot-info speech on pinned plots.
- `ProductionPanel_CAI.lua` now folds in the useful non-queue ProductionManager affordances: root siblings are the existing production tab control, a local-player city list, and a sort dropdown with the same founding/name/population options as vanilla ProductionManager. City-row focus calls `UI.SelectCity(city)` only if that city is not already selected, relying on vanilla city-selection refresh to update the production tabs. The root panel has panel-local `Alt+Left` / `Alt+Right` bindings that select the previous/next city in the currently sorted city list, wrapping at the ends; the shortcut seeds the city list's internal remembered focus key through `mgr:PrepareFocus(...)` and the next city-list rebuild consumes that pending key instead of restoring the previously focused city row. Runtime/user-review fix: current-production reads now reuse generalized existing helpers (`GetCurrentProductionItem(city)`, `HasActiveCurrentProduction(city)`, `ReadCurrentProductionLabel(readTurns, city)`) instead of duplicating production-name lookup; this also avoids the invalid `ipairs(GameInfo.*)` userdata path. In-game retest passed: open production panel with multiple cities, confirm no `pGlobal_ipairs` runtime error, Tab/Shift+Tab through tab control/city list/sort dropdown, confirm focusing city rows switches selected city and production content, confirm `Alt+Left` / `Alt+Right` select previous/next cities according to the active sort order and leave the city list's restored focus on the matching city row, and confirm all three sort options reorder the city list without losing focus.
- `DedicationPopup_CAI.lua` now still reads the live `MomentBonuses` control text for checkbox tooltips, but trims the first line when the control contains a multi-line dedication heading plus bonus body. This avoids the repeated dedication-name/heading speech while preserving the actual control-sourced bonus text. In-game retest passed: open the dedication popup and confirm each checkbox tooltip starts at the bonus text instead of the repeated dedication heading, including heroic-age entries that append the normal-age quest line.
- `UnitPromotionPopup_CAI.lua` is implemented and registered. It preserves vanilla visuals while adding a CAI panel with table-first promotion navigation and an alternative tree view with relationship reference jumps. Follow-ups simplified lifecycle to the two vanilla chokepoints, removed explicit push focus so `mgr:Push(m_panel)` lets the manager resolve default focus, and hardened pre-push widget/model construction. Latest fixes: `BuildPromotionModel()` relationship sorting now passes the local model into `SortPromotionTypesByLayout()` instead of reading nil `m_model` before assignment; `UnitPromotionPrereqs` rows are spoken as unlock edges (`Unlocked by` / `Unlocks`, comma-separated lists) so reciprocal same-tier links like Heavy Cavalry Marauding/Rout do not sound like impossible all-prerequisite requirements; vanilla functions are wrapped directly with `WrapFunc(...)` from `caiUtils` instead of screen-local saved-function wrappers. in-game retest passed.
- Vanilla compact/random promotion chooser is UnitPanel-owned, not a tree: for units with `NumRandomChoices > 0`, `ShowPromotionsList(tPromotions)` lists only the available random choices returned by the promote command, with tier roman numeral, name, and description; no prereq/unlock connector lines are drawn or spoken by vanilla. `UnitPanel_CAI.lua` now mirrors that by wrapping `ShowPromotionsList`, waiting until vanilla actually shows `Controls.PromotionPanel`, then pushing a transient CAI list from the live vanilla `Controls.PromotionList` rows. CAI rows label from `PromotionTier` + `PromotionName`, tooltip from `PromotionDescription`, and activate with `PromotionSlot:DoLeftClick()`. Follow-up tightened UnitPanel promote action detection: command hash lookups use full DB keys (`UNITCOMMAND_PROMOTE` / `UNITCOMMAND_UPGRADE`), promote rows are labeled `Choose Promotion`, and CAI adds a synthetic promote row only if live vanilla `CanStartCommand(... PROMOTE ...)` returns promotion choices but cached action data lacks the promote action.
- `UnitPanel_CAI.lua` now mirrors the vanilla veteran rename panel. Wrapping `ShowNameUnitPanel` pushes a CAI panel with an AlwaysEdit edit box capped at 48 characters plus Randomize and Confirm buttons. The edit box commits into `Controls.VeteranNameField` through `SetValueSetter` and calls vanilla `OnEditCustomVeteranName()` so `m_FullVeteranName` is current before confirm. Randomize and Confirm activate via `DoLeftClick()` on the vanilla controls; Enter on the AlwaysEdit edit box bubbles to the CAI panel and clicks vanilla Confirm; the `OnConfirmVeteranName` wrapper commits the CAI edit before vanilla reads the name; and `HideNameUnitPanel` is the only CAI panel removal path. in-game test passed.
- `UnitPanel_CAI.lua` Shift+5 / promotions bucket now mirrors vanilla earned-promotion icon tooltip content by speaking each earned promotion as `{Name}: {Description}` after level and XP progress.
- `TradeRouteChooser_CAI.lua` exists and is registered as a new-framework replacement. `TradeOriginChooser_CAI.lua` now exists and is registered: it pushes a minimal CAI panel with a single list of live vanilla city buttons, speaks the live city-button text, and activates rows through `CityButton:DoLeftClick()` with a logged fallback to `TeleportToCity(city)` if no live control is captured. In-game test passed: open trader relocate-origin chooser, navigate city list, Enter relocates via vanilla, Escape closes.
- `EspionageChooser_CAI.lua` exists and is registered as a new-framework replacement. `EspionagePopup_CAI.lua` is implemented and registered: it is a state-agnostic dialog mirror that reads visible vanilla controls, combines related text into comma-separated rows, captures possible-outcome instance text, and activates visible vanilla buttons via `DoLeftClick()`. `EspionageEscape_CAI.lua` is implemented and registered: it mirrors the vanilla escape-route modal as a CAI dialog, re-registers the notification open listener after wrapping `OnOpen`, and exposes the four live route buttons with disabled state and route details. in-game test passed.
- EspionageChooser counterspy/mode fix implemented (2026-06-10): CAI now captures vanilla mission rows during `RefreshMissionList` via `AddCounterspyOperation` / `AddAvailableOffensiveOperation` / `AddDisabledOffensiveOperation`, builds mission-mode CAI rows from those captures, reads disabled state from the captured live button when present, and activates the captured live button with `DoLeftClick()` instead of `Controls.MissionStack:GetChildren()[i]`. CAI mirrors vanilla `Open()` reentrancy by pushing the panel only if `CAIEspionageChooser_Panel` is not already on the manager stack; repeated vanilla `Open()` calls refresh the existing panel through the `Refresh` wrapper. CAI now derives destination-vs-mission mode at the `Refresh()` choke point from live vanilla panel visibility, keeps destinations in a `Tree` and missions in a sibling `List`, hides each widget by mode predicate, and moves focus to the active widget only when the derived mode changes. The parent panel label is derived from the same mode state, so it announces destination chooser text in destination mode and mission chooser text, including Gain Sources status, in mission mode. In-game retest passed: counterspy City Center should start counterspy directly, not open Neutralize Governor; Harbor should either activate or be spoken as disabled if vanilla disables the live row; selecting a spy should not leave duplicate chooser panels on the CAI stack; confirming the current-city destination should switch CAI focus from destinations to the mission list.
- City overview investigation completed (2026-06-10). Durable findings were added to `docs/game-api.md`: base `CityPanelOverview` is a left slide-out city-details panel with Citizens/Health, Buildings, and Religion tabs; XP1 adds a dynamic Loyalty/Governor tab through `CityPanelCulture`; XP2 adds a dynamic Power tab through `CityPanelPower`. Proposed accessibility shape is a separate new-framework overview mirror with a `TabControl`, structured read-only pages, vanilla-driven tab/lens activation, rename support, and focus-key rebuild preservation. No code implemented yet.
- Frontend multiplayer UI investigation completed (2026-07-02). Durable findings were added to `docs/game-api.md`: current main menu uses an inline Multiplayer submenu; Internet/LAN/PBC/Crossplay go to `Lobby`, Hotseat goes to `HostGame`, matchmaking scenarios go through intro + `JoiningRoom`; `Lobby` is a sortable game browser with join-code and friends; `HostGame` is the setup parameter form; `StagingRoom` is the dense player-slot lobby with ready/start, chat, game summary/friends tabs, invites, slot/team/leader/color/difficulty controls, hotseat edit, kick/add-player, and PBC join-code/end/quit affordances. `UI/frontEnd/Multiplayer/Lobby.lua` is now implemented and registered as a frontend full replacement: CAI panel with games tree, sort and direction dropdowns, refresh button, Load/Host buttons, PBC mirrored tabs, `Alt+Left` / `Alt+Right` backend page requests, row focus-select only when needed, row activate-join, row tooltip disabled reasons, expandable player rows and per-mod/per-game-mode content groups, and now a persistent friends list whose friend rows are `SubMenu`s with per-friend action children. Latest user-test fixes: retained the user-added input handler, removed the separate CAI Join button, changed refresh from checkbox to disabled-while-refreshing button, populated sort dropdown from vanilla header buttons with localized fallbacks, changed content groups to child rows with no group tooltip, hardened player-name splitting, read player rows from the live Members tooltip when present, documented that vanilla player names depend on backend `serverEntry.Players` and may be absent even when player count is present, added owned/required count tooltips to non-empty content groups, made empty content groups non-expandable with `None` tooltip, avoided reselecting an already selected row, removed row status from the tooltip so it remains label-only, added a fallback label for the icon-only Friends button, changed PBC/tree refresh to rebuild the active tree in place with `PrepareFocus` instead of remounting the whole panel, and tightened non-PBC refresh handling so CAI tracks the refresh session until `MultiplayerGameListComplete` instead of trusting `Matchmaking.IsRefreshingGameList()` alone. That keeps Internet/Crossplay on the `Refreshing game list` placeholder and suppresses trickled per-entry rebuilds until the final completion event, matching the desired Crossplay-style UX more closely. Follow-up fix: the focused empty-state row now uses distinct focus keys for `Refreshing game list` and `No games found`, so when a refresh ends with zero results the tree re-announces the new empty label instead of restoring focus silently onto what looked like the same logical `empty` row. Join failure handling is mostly in `JoiningRoom`: room full/game started/too many matches/generic join failed, abandoned kicked/host lost/room full/version mismatch/mod error/mod missing/match deleted, and mod download progress. In-game retest passed: sort dropdown options populate and sort non-PBC lobbies, expanded game rows show player names when vanilla/backend exposes them, content groups expand only when entries exist, content group tooltips report owned/required counts or None, refresh disables while refreshing and speaks that reason, Internet/LAN lobby now stays on `Refreshing game list` until the final list completes and then speaks `No games found` when the refresh ends empty, active refresh does not spam incremental lobby additions, PBC tab switching no longer bounces focus into the tree then back to the tab strip, row focus does not reselect already-selected games, row activation still joins when allowed, Load/Host buttons click vanilla controls, the friends list stays visible and friend submenu actions operate on the correct row, and the vanilla join-code PopupDialog still handles join code.
- Hotseat player-change popup accessibility implemented (2026-07-04): `UI/inGame/PlayerChange_CAI.lua` now replaces the in-game hotseat handoff popup. The CAI mirror rebuilds from wrapped `ShowTurnControls()`, which is the vanilla chokepoint that fills `TitleText`, toggles wait mode vs password mode, clears the password box, and calls `Controls.PasswordEntry:TakeFocus()` when a password is required. Prompt mode exposes Start Turn, Save Game, Menu, and a masked `AlwaysEdit` password field bound through `text_changed` to the live vanilla password edit so button enablement stays vanilla-driven; Enter on the CAI password edit calls vanilla `OnPasswordEntryCommit()`. Wait mode exposes the wait text plus Menu. The CAI password edit sets `CommitOnFocusLeave = false`, and right before the CAI dialog is pushed it now calls `Controls.PasswordEntry:DropFocus()` so the native password edit does not trap interaction underneath the CAI dialog. File and `ReplaceUIScript` entries plus `docs/game-api.md` notes are added. In-game test passed: in hotseat, end a turn and confirm the wait-mode popup speaks and allows Menu access; begin the next player's turn and confirm the prompt popup speaks the player name, Start Turn/Save/Menu buttons are reachable, password-required turns focus the CAI password edit, wrong passwords keep Start Turn disabled, correct passwords enable it, Enter on the CAI password edit starts the turn, and the native password field no longer blocks interaction.
- Hotseat staging / edit-player accessibility implemented (2026-07-04): `UI/frontEnd/Multiplayer/EditHotseatPlayer.lua` is now a frontend full replacement. The CAI mirror uses `mgr.WidgetHelpers.MakeGeneralDialog` with three `AlwaysEdit` fields bound through `text_changed` to the live vanilla edit boxes so hotseat name and password writes stay immediate, matching vanilla `PlayerConfigurations` mutation. The mismatch status is no longer exposed as a separate CAI static text row; when visible, it is read as the Accept button tooltip instead. Follow-up correction: the dialog is now pushed only from wrapped `Realize()`, which is the vanilla chokepoint that already has the resolved player id and freshly populated control values. The popup `ShowHideHandler` is now remove-only on hide, avoiding duplicate push paths. Latest focus fix: right before CAI pushes the dialog, it now calls `Controls.HotseatPlayerNameEntry:DropFocus()` so vanilla's `TakeFocus()` on the underlying name edit no longer traps interaction underneath the CAI dialog. File, frontend import, and `ReplaceUIScript` entries are added. In-game test passed: open hotseat staging, activate `Hotseat edit` on a human slot, confirm focus lands on the name field, name edits update the slot label after Accept, password and verify fields speak as masked edits, mismatch disables Accept and is exposed on the Accept tooltip rather than as a separate row, Cancel restores the original name/password, and closing the modal leaves no stale CAI dialog on the stack.
- `UI/inGame/ChatPanel_CAI.lua` is now implemented and registered as a partial replacement on the new UI manager. The new `UI_OpenChatPanel` action is bound to `Ctrl+/` and pushes a CAI panel after `LuaEvents.CAIWorldTrackerShowChat()` forces vanilla chat visible. The CAI panel mirrors four children in order: chat edit box, target dropdown, chat history list, and players list. Chat history mirrors incoming chat and `/help`, records received/help lines into `LuaEvents.CAIAppendToMessageBuffer(..., "chat")`, and exposes clickable map-pin rows that jump to the live pinned plot while labeling pins through `InGameHelpers_CAI.lua`. The players list mirrors vanilla `BuildPlayerList()` data as per-player submenus of available actions. Kick-vote prompts are mirrored as CAI dialogs with preserved vanilla vote calls. Reopen bug fix on 2026-07-03: `mgr:RemoveFromStack(...)` destroys the CAI panel widget tree, so teardown now nils the cached panel/widget references before the next open rebuilds them. Follow-up fix: slash-command target changes such as `/w` now silently sync the CAI target dropdown selection to the already-updated vanilla target without rebuilding the option list or re-triggering CAI dropdown selection callbacks. In-game test passed: confirm `Ctrl+/` opens even when vanilla chat is hidden, Escape closes the CAI panel, a second `Ctrl+/` after Escape rebuilds and reopens correctly, chat input and target changes match staging-room behavior, `/w` updates the CAI target dropdown exactly once and does not reselect when the CAI dropdown itself initiated the target change, `/help` hint and history behavior match vanilla while appending once to the CAI message buffer, received chats and map pins appear once in CAI history and the message buffer, player submenus only expose valid actions, and kick-vote dialogs appear and preserve vanilla yes/no behavior.
- `JoiningRoom` frontend accessibility implemented (2026-07-02): `UI/frontEnd/Multiplayer/JoiningRoom.lua` is a full vanilla-copy replacement using `mgr.WidgetHelpers.MakeGeneralDialog` with one status `StaticText` bound to live `Controls.JoiningLabel` and one Cancel `Button` preserving vanilla cancel/leave behavior. Initial focus reads the status line; join/configure/mod-download/connect handlers announce the status widget only when it changes; exit/transition/failure paths remove the CAI dialog before vanilla dequeues or raises follow-up UI. File, frontend import, and `ReplaceUIScript` entries are added. In-game test passed: join a multiplayer room or matchmaking scenario and confirm initial status is spoken, each status change is spoken once, mod-download progress speaks, Cancel/Escape leave the network session, and successful join transitions to `StagingRoom`.
- `PBCNotifyRemind` frontend accessibility implemented (2026-07-04): `UI/frontEnd/Multiplayer/PBCNotifyRemind.lua` is a full vanilla-copy replacement with a CAI dialog pushed/removed from the popup `ShowHideHandler`. The dialog mirrors the live title/body text, exposes Do Not Remind as a real `Checkbox` bound through `IsChecked()` + `DoLeftClick()`, and keeps Options / OK as vanilla-button activations. File, frontend import, and `ReplaceUIScript` entries are added. In-game test passed: open the PBC reminder from Lobby, confirm initial focus reads the reminder text, Space toggles Do Not Remind and persists the option, Options opens the vanilla options flow, and Escape/OK close the popup without leaving a stale CAI dialog on the stack.
- `HostGame` frontend accessibility implemented (2026-07-02): `UI/frontEnd/Multiplayer/HostGame.lua` is a full vanilla-copy replacement with a CAI panel grouped into Map Options, Game Modes, Victory Conditions, and Advanced Options, plus sibling Load Game, Load Config, Save Config, Restore Default, Additional Content, Confirm Settings, and session-only Staging Room actions. Shell tabs are intentionally not mirrored as CAI tabs. Rows are built from live setup parameters: array rows open/click the vanilla picker path, bool/game-mode rows use checkbox silent sync (`SetChecked(value, true)`) plus `SetValueSetter(...)` to click the live vanilla checkbox, sliders/dropdowns/edit boxes write through setup parameters and broadcast. Game name appears as the dynamic `GAME_NAME` text parameter in the options list, not as a separate XML edit control. File, frontend import, and `ReplaceUIScript` entries are added. In-game test passed: open HostGame from Hotseat/Create Game, confirm focus lands on the options list, sections contain visible setup rows, GAME_NAME is editable, checkbox rows speak the correct initial state and toggle vanilla once, dropdowns/sliders/edit boxes persist and broadcast without Lua errors, picker buttons open the right vanilla picker, action buttons preserve vanilla behavior, Confirm Settings hosts and transitions to StagingRoom, returning/backing out of StagingRoom reopens HostGame with CAI focus, the session-only Staging Room action works, and Escape/Back follows vanilla exit behavior.
- **Great Works accessibility implemented (2026-06-12, updated 2026-06-13)**. `GreatWorksOverview_CAI.lua` and `GreatWorkShowcase_CAI.lua` created; both ride their respective vanilla wildcard includes. Overview uses a Government-screen-style slot/picker pattern: buildings tree with slots as children, Enter on a slot opens a picker tree with compatible works grouped by building/city, selecting a work executes the move via `PlayerOperations.MOVE_GREAT_WORK`. Ctrl+Enter on a filled slot opens the Showcase for that work; a "View Great Work" button tracks the last-focused filled slot. Showcase reads details from vanilla controls, offers Previous/Next buttons and Left/Right arrow shortcuts for gallery cycling, and a Back button. Both files added to `<Files>` and `<ImportFiles>` in the modinfo. Nine `LOC_CAI_GREAT_WORKS_*` localization tags. **Gaps filled (2026-06-13):** (1) Per-slot move-blocked reasons in tooltip: art lock turns remaining (`LOC_GREAT_WORKS_LOCKED_FROM_MOVE`) and artifact museum-must-be-full (`LOC_GREAT_WORKS_ARTIFACT_LOCKED_FROM_MOVE`). (2) Per-building yields and tourism in building tooltip. (3) Per-work theming fit indicators in slot tooltip, mirroring vanilla's full `GetGreatWorkTooltip` theming logic (art museum: single/duplicate artist/type match/mismatch; artifact museum: era match/mismatch, duplicate civs). (4) XP2 auto-theming awareness: building label theming progress counts all works when `IsAutoThemedEligible()` is true, matching vanilla XP2 override. (5) DLC great work types: Babylon Heroes and KublaiKhan/Vietnam Products handled through vanilla's `HandleCustomGreatWorkTypes` override + `GreatWorkBanner` control branch in Showcase. (6) Showcase properly gates Previous/Next/Back button visibility via `HiddenPredicate` on vanilla control state; vanilla drives audio play/stop through its own `UpdateGreatWork`/`HideScreen`. in-game test passed.
- **Reports screen accessibility implemented (2026-06-14)**. `ReportScreen_CAI.lua` created as a partial replacement with conditional XP1/XP2 include chain. Four tabs: Yields (Tree with collapsible City Income/Building Expenses/Unit Expenses/Diplomatic Deals groups, per-city items with district/building/great-work/trade-route/wonder breakdowns, bottom totals), Resources (Tree with per-resource items and XP2 stockpile format), City Status (List of Buttons with population/growth/amenities/defense/damage and XP1/XP2 loyalty+governor), Gossip (Panel with player+type filter Dropdowns and a flattened List of all gossip entries with no pagination). City activation: `UI.SelectCity()` for owned, `UI.LookAtPlot()` for unowned, both close the screen. Custom input action `TopPanelOpenReports` (Ctrl+R) added to `hotkey_config_CAI.xml` and handled in `TopPanel_CAI.lua`. Own data sourcing via `RefreshCAIData()` calling global `GetData()` since vanilla data variables are local. Own gossip gathering via `Game.GetGossipManager():GetRecentVisibleGossipStrings()` with own filter state. Tab callbacks resolve globals at call time via `_G[fnName]()` to pick up wrapped versions. ~40 `LOC_CAI_REPORTS_*` localization tags added. File and ReplaceUIScript entries added to modinfo. in-game test passed.
- The old open verification queue has been closed administratively during compaction. Some items were implemented but not exhaustively re-verified in game before being removed from the queue.

## Resolved Areas

- `caiUtils.lua`: shared speech and utility wrappers; `Speak(text, interrupt, processTokens)` and `SpeakLines(lines, interrupt, processTokens)` run `ProcessText` by default; pass `processTokens=false` to bypass (used by EditBox raw text). `SplitTextIntoLines`/`TrimText` split/trim on ASCII whitespace only.
- `textProcessing.lua` (renamed from `iconsProcessing.lua`; `ProcessIcons`->`ProcessText`): the single text-filtering entry point for speech. Bracket token replacement with adjacency deduplication (static REPLACEMENTS table, dynamic GameInfo fallback, direct-output tokens, LOC-key collapse aliases), English transliteration, then an ASCII-safe whitespace/comma tidy-up (`ProcessText(text, tidy)`; `tidy=false` preserves layout for edit boxes). Per-screen `NormalizeText`/`CleanText` cleaners were gutted to nil-safe pass-throughs (CityStates, ClimateScreen, EraProgressPanel, HistoricMoments, PantheonChooser, ReligionScreen, DiplomacyActionView, GovernorPanel) plus front-end StagingRoom/CreditsScreen; all `%s`/`%S` on localized text replaced with explicit ASCII sets to fix UTF-8 corruption under Simplified Chinese.
- UI manager rewritten on the `UIManagerRework` branch into a class-based framework under `src/UI/uiManager/` and `src/UI/uiManager/helpers/`. The four old files are deleted; see the "UI Manager Rework" section above for the new layout and `docs/ui-manager.md` for the design.
- `CityPanel_CAI.lua`: city summary, helper reads, and city `SelectionActions` support are implemented.
- `UnitPanel_CAI.lua`: unit summary, stats, actions list, queued path, combat preview speech, post-combat speech, unit list, builder recommendation gating, build-improvement submenu, and carrier special-info reads are implemented.
- `UnitFlagManager_CAI.lua`: expansion-aware include selection and short spoken flag info are implemented, including aircraft capacity count.
- `PlotToolTip_CAI.lua`: plot detail reads and shared plot info access are implemented.
- `WorldInput_CAI.lua` and helper modules: cursor, interface info, move/path speech, targeting widgets, quick-move confirmation, and related scanner hooks are implemented.
- `WorldScanner` modules: scanner categories, lazy rebuild behavior, valid-targets integration, waypoint support, and supporting helpers are implemented.
- `DiplomacyActionView_CAI.lua`, `DiplomacyDealView_CAI.lua`, and `DeclareWarPopup_CAI.lua`: major diplomacy accessibility wrappers are implemented (`DeclareWarPopup_CAI` rebuilt on the new UI manager 2026-05-31, verified in game).
- `ProductionPanel_CAI.lua`, `ResearchChooser_CAI.lua`, `CivicsChooser_CAI.lua`, `CivicsTree_CAI.lua`, `GovernmentScreen_CAI.lua`, and `CivilopediaScreen_CAI.lua`: major screen accessibility work is implemented.
- Frontend accessibility work for items such as Advanced Setup and My2K is implemented. **AdvancedSetup rewritten (2026-06-22):** proper `Dropdown` widgets, `TabControl` + `AddPage` tabs, `Tree`/`TreeItem` advanced view (Players, Options, Game Modes, Victories, Advanced Options), `DoLeftClick()` action buttons, no back button (Escape closes), `Network.BroadcastGameConfig()` fix, leader tooltips with civ/leader abilities and typed unique headers (Unit/Building/District/Improvement), `GameParameters_UI_AfterRefresh` refreshes dropdowns. Round 3 fixes: `SetFocusSound("Main_Menu_Mouse_Over")` on all widgets, parameters sorted by `SortIndex` (matches vanilla order), tree starts collapsed (no auto-expand), vanilla window visibility synced on show and tab switch. Round 4 fixes: OnShow syncs CAI tab to vanilla's `m_AdvancedMode` state instead of forcing basic view, Array parameter `ValueGetter` respects `UxHint == "InvertSelection"` (city states/natural wonders/leader pools show correct Everything/Nothing). Round 5 fixes: dropdown tooltips now read selected value's description dynamically, removed duplicate Map Type button from advanced tree, leader tooltips group uniques by type with headers ("Unique Unit:", "Unique Building:", etc.) using new `LOC_CAI_UNIQUE_*` tags. Round 6: **simplified refresh architecture** — removed all four advanced-mode driver wraps (`CreateButtonPopupDriver`, `CreateMultiSelectWindowDriver`, `CreateCityStatePickerDriver`, `CreateLeaderPickerDriver`). Advanced tree is now built from a single source of truth: `BuildAdvancedTree` iterates `g_GameParameters.Parameters` sorted by `SortIndex`. `GameParameters_UI_AfterRefresh` wrap rebuilds the advanced tree when in advanced mode. `CreateSimpleParameterDriver` wrap only creates basic-view widgets. No more two parallel systems fighting during refresh. in-game test passed.

## Recent Decisions

- UI manager type-to-find timeout now expires lazily on buffer access only (2026-07-09). `CAIUIScreenManager` owns `SearchBufferExpireTime`, refreshes it through `TouchSearchBufferTimer()`, and `ExpireSearchBufferIfNeeded()` is called from `GetSearchBuffer()` and `AppendSearchChar()` before any caller can observe stale text. That means hidden contexts still behave as though the search cleared itself: expired buffers are treated as empty before Escape, Backspace, repeat-search checks, or the next typed character can see old text. No manager update hooks are used for search expiry anymore. Backspace still counts as a real search-buffer edit because it routes through the shared manager/search helper path and refreshes the timer when text remains.
- Unit `SelectionActions` list adjusted 2026-07-02: unit action labels now append bound gestures after `: ` by mapping each vanilla action row's `userTag` hash back to `UnitOperations.HotkeyId` / `UnitCommands.HotkeyId`, then reading `Input.GetGestureDisplayString(action, 0/1)`. The CAI-owned `View abilities` row also appends its binding. `UNITCOMMAND_DELETE` is special-cased to display and handle `CAIDeleteUnit`; pressing that action calls vanilla `OnPromptToDeleteUnit()` so the normal delete confirmation, `CanStartCommand(... DELETE ...)`, `m_DeleteInProgress`, `OnDeleteUnit(unitID)`, and lens cleanup stay intact. In-game test passed: open a selected unit's action list and confirm bound rows include bindings; delete row shows the CAI binding; pressing the CAI delete binding opens the vanilla yes/no delete dialog and confirmation deletes the unit.
- Unit `View abilities` input action fix 2026-07-02: `hotkey_config_CAI.xml` registers `UnitViewAbilities` with default `Alt+/`; `UnitPanel_CAI.lua` now listens to `Input.GetActionId("UnitViewAbilities")` instead of the nonexistent `UI_UnitViewAbilities`. Docs corrected from stale `Ctrl+A` note because `Ctrl+A` is already used by Reports Resources. In-game test passed: select a unit with abilities, open action list and confirm `View abilities: Alt+/` appears, then press `Alt+/` and confirm the abilities list opens.
- City `SelectionActions` list adjusted 2026-07-02: overview detail-tab actions (`Buildings`, `Amenities`, `Loyalty`, `Housing`, `Religion`, `Citizens and growth`, `Power`) are removed from the city action list and `CityActionMap`, leaving `Toggle City Details` as the only city-details entry. The direct `CityOpen*` actions are intentionally unmapped because the CAI city details panel is now a treeview rather than tab-driven. Action rows append bound gestures from `Input.GetGestureDisplayString(action, 0/1)` after `: `. Repeated label-as-tooltip action descriptions are suppressed, except `Change production` now uses the live current-production summary from the existing city info helper as its tooltip. In-game test passed: selected-city action list contains no direct detail-tab rows, shows bindings on bound rows, direct `CityOpen*` bindings no-op, `Change production` speaks current production as tooltip, and gold / faith purchase rows no longer repeat their label as tooltip.
- WorldInput selection jump added 2026-07-01: `CAICursorJumpToSelection` is a navigation-cursor world action bound to `/`. Handler checks `UI.GetHeadSelectedUnit()` first, then `UI.GetHeadSelectedCity()`, resolves the plot with `Map.GetPlot(x, y):GetIndex()`, and raises `LuaEvents.CAICursorMoveTo(plotIndex, "jump")`. In-game test passed: select a unit, move CAI cursor away, press `/` and confirm it jumps to the unit; repeat with a selected city; confirm no Lua error when nothing is selected.
- Hotkey category split added 2026-07-01: old broad `CAI` action rows now live under `CAI_MESSAGE_BUFFER`, `CAI_NAVIGATION_CURSOR`, `CAI_WORLD_SCANNER`, `CAI_SURVEYOR`, or `CAI_INFORMATION`; quick-move rows are under `CAI_UNIT`; `SelectionActions` and new city-selection actions are under `CAI_GLOBAL`. New world actions are `WorldSelectPreviousCity_CAI` (`[`), `WorldSelectNextCity_CAI` (`]`), and `WorldSelectCapitalCity_CAI` (`\`), handled in `WorldInput_CAI.lua` via the vanilla city-selection APIs and CAI-owned locale labels to avoid vanilla hotkey conflicts. In-game test passed: keybinding screen category labels appear as Message buffer, Navigation cursor, World scanner, Surveyor, and Information; `[`, `]`, and `\` select previous/next/capital city without Lua errors.
- Shared unit naming lives in `inGameHelpers_CAI.lua`; avoid duplicating formation/name formatting logic.
- `SubMenu` in `widgetTemplates.lua` is now an entered child container, not a spoken expanded/collapsed tree-style node.
- Unit build improvements are grouped into a dedicated CAI submenu under unit `SelectionActions`.
- Disabled build-improvement actions are hidden only when vanilla provides no failure reason.
- Carrier/unit-hosted aircraft details are shared through `inGameHelpers_CAI.lua`; unit flags still speak count/capacity only.
- Plot-target and interface-target speech should prefer shared helper/data paths over screen-local reconstruction.
- City banner growth/production reads must support both vanilla base controls and XP1/XP2 stat-row instance managers; expansion banners move those tooltips and meters off the top-level banner instance.
- City banner bucket layout is now intended as `1` identity/status, `2` growth/production, `3` religion, `4` diplomacy, `5` loyalty, `6` governor, `7` power.
- City panel parity check shows `CityPanel_CAI.lua` still only covers compact summary reads plus action/toggle access; it does not yet mirror vanilla overview breakdown tabs (district/building/wonder/trading-post lists, religion beliefs, amenities/housing source breakdowns, detailed growth math, production queue) or XP1/XP2 loyalty/governor/power tabs.
- Main `CityPanel` buckets were redone to use single-purpose helpers plus shared request buckets. Current main-panel mapping is summary/tilde = name, population, health, production, growth; `Shift+1` name+health; `Shift+2` production; `Shift+3` food growth; `Shift+4` border growth; `Shift+5` religion; `Shift+6` population + housing + buildings or loyalty depending on active expansion; `Shift+7` through `Shift+0` are the yield buckets.
- City panel border-growth speech now aims at city-panel growth-tile parity: it includes the target hex direction relative to the city plus stored culture, culture per turn, required culture, and current / next-turn meter percentages.
- City `SelectionActions` ordering is now explicit instead of relying on the CITY hotkey-category order. Dynamic strike actions stay at the top, and the separate manage-citizens / purchase-tile actions are merged into one combined `Manage city` action that toggles both available city-management lenses together.
- City `SelectionActions` now append dynamic ranged-strike rows for the selected local-player city center and any strike-capable districts belonging to that city. Visibility is driven by `CityManager.CanStartCommand(..., CityCommandTypes.RANGE_ATTACK)` so the centralized list only shows attacks that vanilla would currently allow.
- City `SelectionActions` also append dynamic WMD strike rows for supported district launches such as missile silos. Availability is driven by vanilla-style `CityCommandTypes.WMD_STRIKE` target checks per district plot and per weapon type, and launching mirrors the banner `OnICBMStrikeButtonClick(...)` flow while also jumping the CAI cursor to the firing district.
- City panel action-list parity now includes the expansion overview tabs: `CityPanel_CAI.lua` follows the base / XP1 / XP2 `CityPanel` include chain, exposes XP1/XP2 loyalty-governor overview access through `LuaEvents.CityPanel_ToggleOverviewLoyalty()`, and exposes XP2 power overview access through `LuaEvents.CityPanel_ToggleOverviewPower()` when the underlying expansion capability is active.
- City-panel `Change production` accessibility should no longer blindly toggle `ChangeProductionCheck` when the vanilla production context is hidden. CAI now checks `/InGame/ProductionPanel` visibility first and calls `LuaEvents.CityPanel_ProductionOpen()` directly when needed, preserving vanilla queue-first tab choice while avoiding checkbox-state mismatches that consumed the first activation.
- `ProductionPanel_CAI.lua` now mirrors the Civilopedia-style rebuild focus handling: capture the focused body row before clearing, rebuild, restore by stable CAI focus key when possible, and otherwise reset the active body to its first child. Queue move/remove overrides still take precedence through the explicit post-rebuild queue index path.
- `NotificationPanel_CAI.lua` migrated to the new UI manager: Tree of TreeItem leaves / group nodes (FocusKeys `notification:<id>` and `group:<typeName>`); vanilla `GetMessage()` is the short title and `GetSummary()` is the longer content text, so group nodes use the title and individual leaves (single or inside a group) use the content text; tooltips and the location/coords helpers (`GetNotificationCoords`, `LookAtNotification` wrap) are dropped entirely per durable decision; alerts speak the content text (`GetSummary`, falling back to the title only when summary is empty) routed through `ProcessText` before `Speak`; add/dismiss while open trigger a full rebuild via `mgr:CaptureFocusKey`/`mgr:RestoreFocus`; close path uses `mgr:RemoveFromStack(NOTIFICATION_CENTER_ID)`; Escape binding on the tree itself (CAI owns this screen — no vanilla panel mirrors it) uses `KeyUp` per `feedback_nonnav_keys_keyup`.

## Durable Rules

- Write changelog entries in direct present tense and avoid filler uses of `now`; reserve it for cases where a temporal contrast is genuinely important.
- Prefer live vanilla control state for visibility, disabled checks, and exposed localized text.
- Prefer `GetText()` / `GetToolTipString()` over manual `Locale.Lookup()` when vanilla already exposes the text.
- Use `Locale.Lookup()` only when no control exposes the text or when the string is CAI-owned.
- Cache references and ids only; always read live values when speaking state.
- Tree expand/collapse logic belongs in shared templates, not screen-specific code.
- Pushed transient lists, trees, and dialogs should inherit current stack priority and close cleanly through the UI manager.
- Avoid persistent mirrors for ambient HUD panels when hotkey/read-on-demand access is better.
- End-turn blockers belong with ActionPanel access, not the notification center.
- For Civ VI gameplay DB values in Lua, remember that `0` is truthy. Explicitly test boolean-like DB fields and stat thresholds.

## Known Remaining Work

- **All migrated screens are verified in-game** (MainMenu, InGameTopOptionsMenu, ResearchChooser, CivicsChooser, ProductionPanel, CivicsTree, TechTree, GovernmentScreen, NotificationPanel, Civilopedia) — including the Expand/Collapse silent-flag / recursive-collapse / ancestor-auto-expand framework changes. No open verification queue.
- Remaining screen migration: every other `*_CAI.lua` still uses the old `OnClick`/`OnFocusEnter`/`FocusedChild` API and must be ported to the new UI manager per `docs/ui-manager.md` §16 (e.g. `UnitPanel_CAI`, `CityPanel_CAI`, `UnitFlagManager_CAI`, `PlotToolTip_CAI`, `PlotInfo_CAI`, `WorldInput_CAI`, and the remaining frontend screens). These will fail to load on this branch until ported.
- **Diplomacy migration in-game test passed**: `DiplomacyActionView_CAI` and `DiplomacyDealView_CAI` are now on the new UI manager API. Latest diplomacy pass restructures `DiplomacyActionView_CAI` around the leaders tree: the Overview intel node is intentionally suppressed, each leader row tooltip now carries the overview summary (relationship, access level, government, gossip, Secret Society row when applicable, agendas), the local-player row is now a leaf with a GameSetup-style leader tooltip, and vanilla overview tab selection is re-synced whenever focus lands on the leader row itself. Relationship and Access sections now build from game APIs instead of captured live controls; Gossip already did; Alliance and Grievances were reshaped so their top-node tooltips carry the summary while children cover detailed breakdowns. `DiplomacyDealView_CAI` follow-up: quantity-bearing resource/favor inventory rows now suppress duplicate bare-name tooltips, offer diff speech now detects same-item edits (amount/target/unacceptable state if the spoken label changes), side announcements are ordered removed, added, then changed, and tooltip sourcing now follows vanilla more closely across the screen: `MakeCityToolTip(...)`, `GetParentItemTransferToolTip(...)`, direct `GetGreatWorkTooltip(...)` for top-level great works, plain support text for city-child great works, no main tooltip for vanilla-empty cases such as captives and no-duration agreements, and invalid available-resource tooltips now mirror the no-cap-room text. Test matrix: base / XP1 / XP2 / XP2+Secret Societies; AI vs hotseat-human opponent; incoming demand vs outgoing deal; conversation actions (declare friendship / denounce / make peace); first-meet cinema announcement. Watch focus on first open (should land on the *selected* leader row, not self), leader-row tooltip content/parity, relationship/access/gossip/alliance/grievance section labels/tooltips, vanilla tab syncing when moving between leader row and intel nodes, and deal-view speech when adding/removing/editing gold, resources, favor, alliance type, joint-war target, Monopolies & Corporations product great works, invalid resources, and city-transfer child rows.
- `ProductionManager` / multi-queue accessibility is still not implemented.
- `LoadScreen` accessibility is still planned, not implemented.
- **UnitPromotionPopup in-game test passed**: open a regular non-random promotion popup, confirm initial focus lands on the first available table cell, tier headers speak when moving left/right, table labels read `Name, status`, tooltips include description/prereqs/leads-to, available promotions activate immediately, locked/earned rows do not activate, alternative view toggles to the tree, reference links jump to target promotion nodes, and Escape/close/unit-panel hide remove the CAI panel.
- **UnitPanel compact panels in-game test passed**: for a Rock Band or other `NumRandomChoices` unit, confirm the simple promotion chooser list opens only when vanilla shows `PromotionPanel`, rows speak tier/name/description from vanilla controls, and activation clicks the vanilla row. For a nameable veteran unit, confirm the CAI rename panel focuses the edit box, enforces 48 characters, randomize updates the spoken/edit value, confirm after typing without pressing Enter names the unit correctly, and Escape closes via vanilla cancel.
- **EspionagePopup in-game test passed**: choose a spy mission and confirm the CAI dialog title follows vanilla `MissionTitle`, objective/duration are one comma-separated row, possible outcomes are one row with percent signs, and Accept/Cancel work. Cancel an active offensive spy mission and confirm Abort/Cancel work. Resolve a mission and confirm outcome/reward/consequence/renew rows appear only when vanilla shows their containers, the visible close button works, and Escape closes through vanilla while `EspionageChooser_CAI.lua` refocuses after mission briefing closes.
- **EspionageEscape in-game test passed**: trigger a spy escape notification or end-turn blocker, confirm the CAI dialog title/city/details read from vanilla controls, each route button speaks its matching `LabelN` route detail as tooltip, disabled unavailable district routes are readable and do not activate, enabled route buttons submit the vanilla escape route and close, and Escape closes without choosing.

- **Reports screen in-game test passed**: Open Reports via Ctrl+R. Confirm focus lands on the first tab (Yields). Navigate tabs — all 4 tabs switch correctly and rebuild content. **Yields tab**: expand City Income, navigate per-city rows (city name + current production), expand a city to see districts/buildings/great works/trade routes/wonders. Enter on owned city selects it; Enter on unowned city looks at it. Check Building Expenses, Unit Expenses, Deals groups. Confirm bottom totals (net yields, treasury) read correctly. **Resources tab**: navigate resources, confirm totals. XP2: confirm stockpile format ("Iron, 5 of 10 stockpile"). Expand a resource to see source entries. **City Status tab**: navigate cities, confirm all column data (population, growth, amenities, happiness, defense, damage). XP1/XP2: confirm loyalty and governor are included. Enter activates city. **Gossip tab**: change player filter — list rebuilds filtered. Change type filter — list rebuilds. Navigate entries, confirm "Turn N, LeaderName: description" format. Confirm all entries present (no pagination). Test base game, XP1, and XP2. Escape closes the screen.

- **WorldRankings accessibility implemented (2026-06-13)**. `WorldRankings_CAI.lua` created as a partial replacement with conditional Exp1/Exp2 include chain. Uses TreeView design: each player/team row is an expandable TreeItem with detail children (score categories, science milestones, captured capitals, converted civs, generic/diplomatic requirements). Tabs mirror vanilla via AddTab/AddExtraTab capture during PopulateTabs; TabControl value_changed triggers vanilla callbacks. View* function wraps rebuild the active tab's tree on each tab switch. Science tab gathers milestone data directly from project progress APIs; Domination gathers captured capitals from city ownership APIs; all other tabs reuse vanilla's global Gather*Data functions. Exp2: Diplomatic tab shows victory points, Science tab shows 4 project milestones + light-years progress. 19 LOC_CAI_WORLD_RANKINGS_* localization keys added. File and ReplaceUIScript entries added to modinfo. in-game test passed.

- **Era Progress Panel accessibility implemented (2026-06-15)**. `EraProgressPanel_CAI.lua` created as a partial replacement. Expansion-only panel (Rise and Fall / Gathering Storm) — the `ReplaceUIScript` is a no-op in base game. Layout is a Tree inside a Panel: summary leaf (era name, age type, score, turns to next era), expandable threshold nodes (Dark/Normal/Golden with score ranges and "projected" marker; children show baseline + per-source breakdown from the game API), expandable score breakdown node (children: previous era total + individual current era source entries), expandable "Current Age by Civilization" node (met civs sorted heroic/golden/normal/dark/unmet as leaf children), and age effects leaf reading from vanilla's `EraEffects` label. Normal Age node omitted in Dramatic Ages mode. Threshold/score nodes with no children remain as childless TreeItems. Escape bound on the panel calls `Close()`. Refresh wrap calls `mgr:Refocus()` for re-announce. 7 `LOC_CAI_ERA_PROGRESS_*` localization tags added. File and ReplaceUIScript entries added to modinfo. in-game test passed.

- **Era Progress Panel in-game test passed**: Open the Era Progress panel from the launch bar (requires XP1 or XP2). Confirm focus lands on the summary row reading era name, age type, score, and turns to next era. Navigate thresholds — Dark/Normal/Golden nodes show score ranges; the projected next age is marked. Expand a threshold to see breakdown children (baseline, base threshold, cities, golden ages earned, etc.). Score breakdown node shows total; expand to see previous era total + individual current era sources. Expand "Current Age by Civilization" — civ children show leader name, civ name, and age type for met civs; unmet civs say "Unmet". Effects leaf reads loyalty bonus and active dedications. Test in Dramatic Ages mode — Normal threshold should not appear. Test in the final era — thresholds and turns-to-next-era should not appear. Escape closes the panel.

- **Historic Moments / Timeline accessibility implemented (2026-06-15, updated 2026-06-16)**. `HistoricMoments_CAI.lua` created as a partial replacement. Expansion-only screen (Rise and Fall / Gathering Storm) — gated by `CAPABILITY_HISTORIC_MOMENTS`. Panel label reads live from `Controls.ModalScreenTitle` (adapts to new-moment popup vs manual open). Tree label shows total moment count with proper pluralization. Era nodes (newest first) each contain Major Moments and Minor Moments sub-nodes (using existing `LOC_PEDIA_MOMENTS_PAGEGROUP_MAJOR/MINOR_NAME` tags, split by `InterestLevel > 1`). Moment labels: turn number + generic description from `GameInfo.Moments[type].Description`. Moment tooltips: era score, commemorated status, calendar date, instance-specific description. Lifecycle uses `SetShowHandler` for push (vanilla `DelayShow` popup), `Close` wrap for teardown. 5 `LOC_CAI_TIMELINE_*` localization tags (count tag uses plural syntax). File and ReplaceUIScript entries added to modinfo. in-game test passed.

- **Historic Moments / Timeline in-game test passed**: Open the Timeline from the launch bar historian button (requires XP1 or XP2). Confirm panel title reads the live screen title. Tree shows total moment count ("N moments" / "1 moment"). Expand the topmost era — should be the most recent era. Inside each era, Major Moments and Minor Moments sub-nodes appear (only when that category has entries). Moment labels read "Turn N, generic description". Tooltips read era score, date, and specific instance description. Confirm Escape closes. Open via a pride moment notification — confirm title reads "New Moment in the History of...". Test on both XP1 and XP2 rulesets.

- **Climate Screen accessibility implemented and verified (2026-06-16 through 2026-07-22)**. The three-tab Gathering Storm screen exposes Overview, CO2 Levels, and Event History with vanilla data, mode-safe forest-fire handling, comet-strike correction, current-location visibility, affected-city and contributor identities, recent sea-level alerts, live cursor-relative event locations, and non-closing Enter jumps. The complete in-game matrix passed at the user's direction.

- **Top Panel accessibility is complete (2026-06-17)**. All vanilla top panel data sections are now covered: turn/time/date (T), science/culture/gold/faith/tourism/favor/nukes (individual hotkeys), yield breakdown tree (Ctrl+Y with full tooltip breakdowns for all yields, gold income/expense, trade routes, envoys, favor, WMDs), strategic resources tree (Ctrl+Q with XP2 stockpile/accumulation/consumption breakdowns using vanilla LOC_RESOURCE_* tags; base game shows flat name+amount), and report/diplomacy shortcuts (Ctrl+E, Ctrl+D, Ctrl+R, Ctrl+S, Ctrl+G). Strategic resources tree labels show "Name: amount/cap in stockpile" for XP2 with accumulation/consumption summary in tooltip; children expand to show reserved, accumulation sources (extracted/city-states/bonus), and consumption sources (units/power). Base game shows flat "Name: amount" items. Empty state speaks "No strategic resources". Widget ID added to TutorialUIRoot escape passthrough. in-game test passed for Ctrl+Q resource tree only.

- **End Game Menu accessibility implemented (2026-06-17, chat updated 2026-07-11)**. `EndGameMenu_CAI.lua` is a wildcard-include ImportFiles entry (rides vanilla `include("EndGameMenu_", true)` pattern). Panel with TabControl (4 tabs): Results (List: victory type+player with blurb tooltip, defeat type+player), Ranking (List: "You have achieved the level of [leader], N / total" header from vanilla `PopulateRankingResults` + `GameInfo.HistoricRankings` rows as StaticText with quote tooltip on placement row only), Graphs (Tree: per-player expandable nodes with final-turn dataset values as children, sourced from `GameSummary` APIs), Chat (MP only: CAI chat now mirrors the shared CAI chat shape used in `ChatPanel_CAI.lua` / staging room: input `EditBox`, target `Dropdown`, and history `List` of individual lines rebuilt from the live vanilla stack). Target changes write through `PlayerTargetLogic` helpers to the vanilla end-game pulldown/edit/icon, and `/help` plus incoming/system chat rebuild the CAI history from the live vanilla `ChatStack`. Tab switching clicks vanilla tab buttons (`InfoButton`/`RankingButton`/`ReplayButton`/`ChatButton`) via `DoLeftClick()` with `isMirroringTab` guard. Action buttons in a Transparent `WrapAround=false` Panel below tabs: Replay Movie, Historic Moments (XP1/XP2), Export Moments (XP1/XP2), Main Menu, Next Player (hotseat), Just One More Turn — all with live `HiddenPredicate`/`DisabledPredicate` on vanilla controls, activate via `DoLeftClick()`. Movie skip panel pushed on top during cinematic playback via `OnReplayMovie` wrap (single chokepoint for both initial and replay); `ShowComplete` defers main panel push when movie panel exists; `OnMovieExitOrFinished` removes movie panel and pushes main panel if not yet built. Panel persists across hide/show cycles (no `OnHide` teardown); only `Close` destroys. All wraps use `WrapFunc`. 6 `LOC_CAI_ENDGAME_*` localization tags. File + ImportFiles entries added to modinfo. in-game test passed (movie panel push on first open, tab switching, ranking data, graph data, action buttons, timeline return focus, end-game chat target/send/help/history behavior).

- **End Game Menu in-game test passed**: Win or lose a game to trigger the end game screen. Confirm cinematic plays with "Cinematic playing" panel and skip button works (Enter or Escape). After cinematic, confirm main panel pushes with victory/defeat info. Tab through Results (victory/defeat type and player), Ranking (header shows "You have achieved the level of [leader], N / total"; rows show historic leaders with scores; placement row has quote tooltip), Graphs (expand players to see dataset values), Chat (MP only). Action buttons: Replay Movie (replays cinematic with skip panel), Historic Moments (opens timeline, Escape returns to end game panel), Export Moments, Main Menu, Next Player (hotseat), Just One More Turn. Test defeat scenario too. Test with XP1/XP2 for Historic Moments and Diplomatic Victory style.

- **Global Resource Popup foundation (2026-06-17)**. The partial replacement conditionally includes the base or Monopolies version, preserves vanilla leader activation, wraps popup lifecycle, and opens from Ctrl+Q through the diplomacy-deals capability gate. Its original tree passed in-game testing before the current table/tree redesign.

- **Global Resource Popup in-game test passed**: Press Ctrl+Q in-game. Confirm popup opens and panel pushes with focus on the tree. Navigate Strategic/Luxury groups — expand to see resources with totals. Expand a resource to see owning civs with amounts. Enter on a civ leaf opens diplomacy. Tab lands on Sort dropdown; change sort — tree rebuilds (focus may land on group header if focus was on dropdown, expected). Tab to Order dropdown; change order — tree rebuilds. Escape closes popup. Verify Ctrl+I now opens strategic resource breakdown (previously Ctrl+Q). Test with Kublai Khan/Vietnam DLC Monopolies mode: civ leaves should show control percentages and monopoly status when Mercantilism is unlocked. Test with no resources discovered — confirm empty state.

- **Hall of Fame / Game Summaries accessibility implemented (2026-06-17, updated 2026-06-18)**. `GameSummaries.lua` and `GameSummaries_GameDetails.lua` copied from vanilla into `src/UI/frontEnd/` as full front-end replacements with CAI blocks before `Initialize()`. No expansion overrides exist for these files. **Main screen** (`GameSummaries.lua`): Panel with Ruleset Dropdown + TabControl (2 tabs). Overview tab: single Tree with Victory Progress and Leader Progress as expandable TreeItem categories, plus statistics categories. History tab: game List + Sort Dropdown (Score/You/Results/Victory/Settings); activate opens Game Details. Panel not destroyed on OnHide (child popup open); HandleExitRequest destroys on actual close. OnShow re-pushes only if panel not already on manager stack. Input handler re-registered with InputStruct signature via Initialize wrap. No back button — Escape closes. **Game Details screen** (`GameSummaries_GameDetails.lua`): Panel with victory banner text in title + TabControl (3 tabs), no dropdowns, no back button. Overview tab: Overall TreeItem (game-level statistics + game modes) then per-player TreeItem nodes (leader, civ, score, result) each containing that player's statistics categories. Reports tab: flat Tree — Context groups (skipped when only one) → Report Type → Rows (inline ≤3 columns, expandable >3). Graphs tab: flat Tree — per-player nodes with Overview subcategory (game-wide stats merged from Overall group) and per-city nodes with graph types and turn breakdowns; entity nesting based on ds.Name presence. `SelectGraphGroup`/`SelectReportGroup` wrapped to rebuild trees. 3 `LOC_CAI_HOF_*` localization tags (Ruleset, Sort By, No Games). Both files added to `<File>` list and `<ImportFiles id="CAIFrontEnd">` in modinfo. in-game test passed.

- **Hall of Fame in-game test passed**: From the Main Menu, open Additional Content and then Hall of Fame. Confirm the panel pushes with "Hall of Fame" title and focus lands on the Ruleset dropdown. Change ruleset — overview and history should rebuild. Navigate Overview tab: expand Victory Progress to see victory types with achieved counts. Expand Leader Progress to see leaders with win/play counts. Expand statistics categories. Switch to History tab: navigate game list — labels show score, leader, civ, result, turns. Tab to Sort dropdown after the list — change sort order. Activate a game row to open Game Details — confirm HoF panel is preserved (not rebuilt). Escape returns to History tab (preserved state). In Game Details: title should include victory type or "Defeat". Overview tab: expand Overall for game-level statistics and game modes. Expand per-player nodes for their individual statistics categories. Reports tab: context groups (if multiple) expand to report types → rows with column values. Graphs tab: per-player nodes with Overview subcategory (Player Score, Barbarian Camps) and per-city nodes (City Buildings, City Food, etc.) — expand graph types for turn breakdowns. Escape closes Game Details. Escape again closes Hall of Fame.

- **World Crisis Tracker accessibility implemented (2026-06-16)**. `WorldCrisisTracker_CAI.lua` created as a partial replacement. Expansion-only (Rise and Fall / Gathering Storm). Adds Ctrl+W input action (`OpenWorldCrisisTracker`) to open a pushed CAI List of emergency Buttons. Each row reads: status prefix (Completed/Targeted/Joined/Pending decision), emergency name, goal progress "(X/Y) description", turns remaining. Tooltip shows target leader name. Activation fires `LuaEvents.WorldCrisisTracker_EmergencyClicked` to open the popup, then removes the list. Escape closes. If no emergencies exist, speaks "No active emergencies". Data sourced from `Game.GetEmergencyManager():GetEmergencyInfoTable()`. 10 `LOC_CAI_CRISIS_*` localization tags. File and ReplaceUIScript entries added to modinfo. in-game test passed.

- **World Crisis Popup accessibility implemented (2026-06-16)**. `WorldCrisisPopup_CAI.lua` created as a partial replacement. Includes vanilla `WorldCrisisPopup` (VFS resolves to XP1 or XP2 version). Panel reads live vanilla controls after they're populated. Layout: target name + city + description merged into one row; goal description + duration merged into one row; victor title + tier (XP2, when visible) as one row; all crisis detail stack children merged into one row; placement + participants (names with scores from API) as one row; all rewards stack children merged into one row; then Join/Reject/OK buttons with `DoLeftClick()` activation. XP2 wraps `ShowEmergency` globally (resolved at call time by `OnEmergencyAvailable`). XP1 re-registers `OnCrisisReceived` on all three event sources. InputHandler re-registered to route through `mgr:HandleInput`. `Close` wrapped for teardown; hide handler removes panel. Participant names sourced from `GetEmergencyInfoTable()` since leader icons aren't text-readable. in-game test passed.

- **World Crisis in-game test passed**: (1) Tracker: Press Ctrl+W in-game (requires XP1 or XP2). Confirm list appears with all active/completed emergencies. Navigate rows — labels show status, name, progress, turns. Enter on a row opens the popup for that emergency. Escape closes the list. Ctrl+W again toggles closed. With no emergencies, speaks "No active emergencies". (2) Popup: Open via tracker click, notification click, or automatic emergency offer. Confirm title reads from vanilla. First row: target leader/city + description. Second row: goal description + turns remaining. Victor row (XP2 completed only): who won + your tier. Details row: all goals/buffs/score sources merged. Participants row: placement + participant names with scores. Rewards row: all reward tiers merged. Buttons: Join/Reject for pending emergencies, OK for ongoing/completed. Enter on Join/Reject sends the correct player operation. Escape closes via vanilla. Test on XP1 (hostile emergencies only) and XP2 (scored competitions + hostile emergencies). Test non-hostile no-target competitions (World's Fair type).

- **Great Works in-game test passed**: Open the Great Works Overview via launch bar. Confirm buildings tree announces building names, city names, and theming status (themed bonus / progress X of Y). Building tooltip should announce theme description and per-building yield breakdown. Navigate slots — filled slots announce work name and creator; tooltip should include type, yields, tourism, date, theming fit status (e.g. "matched theme", "duplicate artist", "mismatched era"), and move-blocked reason if applicable (art lock turns or artifact museum rule). Empty slots announce slot type. Enter on a slot opens a picker with compatible works grouped by building/city; Enter on a picker item moves the work and the tree refreshes. Ctrl+Enter on a filled slot opens the Showcase with work details announced. In the Showcase, Previous/Next buttons and Left/Right arrows cycle through gallery works with re-announcement; buttons hide when only 1 work exists. Back button returns to the Overview. Test artifact swap restrictions (museum must be full — tooltip should say so). Test art lock (10-turn cooldown — tooltip shows turns remaining). Test with Expansion 2 if available (auto-theming: progress count should count all works, not just theme-fit ones). Test DLC great work types if available: Heroes (Babylon) and Products (KublaiKhan/Vietnam Monopolies) should read name and creator in the Showcase. Music great works should auto-play audio. Verify "not your turn" blocks moves outside local player's turn.

### Durable notes from the verified migrations

- **GovernmentScreen** re-includes the exact vanilla screen variant vanilla would pick (base / `_Expansion1` / `_Expansion2`, upgraded to the Byzantium_Gaul `_MODE` variants when Dramatic Ages is active via a local `IsDramaticAgesActive()`), since CAI replaces the context outright and a sighted hotseat player needs the right visuals. World-Congress lock gated via explicit `IsReadOnly()`; Dark/Golden age policy labels via `GetPolicyAgeIndicator` (`Policies_XP1.RequiresDarkAge/RequiresGoldenAge`). Loc: `LOC_CAI_GOVERNMENT_DARK_AGE_POLICY` / `_GOLDEN_AGE_POLICY` / `_CONGRESS_LOCKED`.
- **Tech/Civics trees** follow the same expansion include-chain rationale (tech: base/`_Expansion1`/`_Expansion2`; civics: base/`_Expansion2`). Tech tooltip appends `Alliance research bonus` via `GetAllianceText`; no real text search (CAI uses type-to-find + the category filter list).
- **Civilopedia** keeps the old UI layout exactly (Sections `Tree` / Article `Tree` / History `List`); programmatic jumps speak `LOC_CAI_PEDIA_JUMPING` and route through `ApplyNavigationFocus`; content-capture layer unchanged.

## Diplomacy migration (implemented 2026-05-29, verified in game)

Both diplomacy screens rewritten onto the new UI manager. Durable architecture facts:

- **DealView rides the vanilla wildcard host.** `DiplomacyDealView.lua` ends with `include("DiplomacyDealView_", true)` (a wildcard that pulls in every `DiplomacyDealView_*` file), with `Initialize()` called *after* it. So `DiplomacyDealView_CAI.lua` is registered as an InGame `<ImportFiles>` File (like `NotificationPanel_CAI`), **not** a `ReplaceUIScript`; it must NOT `include("DiplomacyDealView")`, and it only reassigns globals (`OnShow`, `InputHandler`, `PopulatePlayer*Panel`, `UpdateDealStatus`, …) — vanilla's later `Initialize()` registers them as handlers, so no `ContextPtr:Set*Handler` calls. This keeps every vanilla/DLC `DiplomacyDealView_*` override (e.g. KublaiKhan_Vietnam `_MODE`) intact.
- **ActionView stays a `ReplaceUIScript`** (no wildcard in `DiplomacyActionView.lua`) but now re-includes the expansion variant vanilla would pick (`_Expansion2` / `_Expansion1` / base), mirroring GovernmentScreen — otherwise the base script wins and silently drops all XP1/XP2 diplomacy logic (Alliance/Emergency/World-Congress tabs, expansion war types) on the standard ruleset. Because it is a full replace, it DOES re-register handlers via `ContextPtr:Set*` (vanilla `Initialize()` ran during the `include`).
- **ActionView user-facing layout unchanged:** leaders `Tree` → per-leader intel sections (`TreeItem`) → content nodes; statement actions are a sibling `List` (with `SubMenu`s for grouped options). No TabControl. View modes (overview / conversation / cinema) are gated by `HiddenPredicate` on two sibling panels + explicit `SetFocus`, replacing the old three-panel `SetActivePanel` focus-poking. Cinema speaks the leader greeting (was silent).
- **Intel tabs: silent enumeration + lazy per-tab switching + persistent per-leader subtrees** (reworked 2026-05-30, in-game test passed). The DLC alliance/emergency/world-congress adders build their buttons through `CreateTabButton` (+ `GetTabAnchor` + `LoadNewContext`), not `AddIntelTab`, so we must walk the finished `ms_IntelPanel.IntelTabButtonStack` to see all tabs. `CaptureIntelTabs()` now only *enumerates* the visible buttons into `{ Button }` tables (no clicking) — the earlier click-through-every-tab-on-every-`AddIntelPanel` was the source of the duplicate UI-click on leader-select and action-press: `DoLeftClick` fires the tab button's own native press sound (the `Style`-level SoundData; `ShowPanel` itself is silent — the old "buttons have no SoundData" comment was wrong). A tab's header and panel are resolved **lazily on first `focus_enter`** of its section: `tab.Header = IntelHeader:GetText()` (after the panel is the shown one) and `tab.Panel = VisibleIntelPanelChild()`. The switch (`tab.Button:DoLeftClick()`) is gated on **live `button:IsSelected()`** (ShowPanel `SetSelected()`s the shown tab, and DLC anchors set `m_ButtonInstance`/`m_HeaderText` so this works for them too) — so re-entering the current tab (Shift+Tab back from actions, moving among a tab's own children, focusing Overview right after a select since vanilla `ShowOverviewPanel()`s it) fires no click; only an actual tab change clicks. Base-reader instances are still captured by the eager `Populate*`/`OnActivate*` wraps, which vanilla calls inside `AddIntelPanel` (not on click), so removing the click-storm doesn't starve them. **Persistence:** `BuildSelectedLeaderChildren` builds each leader's sections once and keeps them; re-selecting a built leader (same tab count) only re-binds the pooled/recycled tab `Button`s in place (mutating the shared `tab` tables the section closures hold) and returns without teardown — so expand state, populated content (readers freeze the leader's live state; a leader screen never spans a turn) and focus position survive switching away and back. A changed tab count (DLC tabs arriving) falls through to a full rebuild. `entry.IntelBuilt`/`entry.Tabs`/`entry.IsSelf` track this. `m_state.buildingIntel` still gates `DiploScene_RefreshTabs`/`RefreshOverviewRows` from rebuilding mid-build. **In-game test passed:** (a) selecting a leader and pressing actions no longer double-clicks; (b) expand a tab on leader A, switch to B, return to A — land back inside the still-expanded tab; (c) switching tabs still clicks once and reads correctly, incl. DLC tabs.
- **DLC intel readers hand-authored (implemented 2026-05-30, verified in game).** The DLC tabs load separate contexts whose control trees aren't reliably scrapeable (the generic walk produced an unlabeled line-dump). Replaced with three readers rebuilt from the same stable game APIs vanilla's `Refresh` functions use, registered in `GetKnownReaders` keyed by `Locale.ToUpper("LOC_DIPLOACTION_INTEL_REPORT_ALLIANCE"/"_EMERGENCY"/"_GRIEVANCES")` (matches `IntelHeader:GetText()`): `AddAllianceChildren` (`localPlayerDiplomacy:GetAllianceLevel/Type/TurnsThisLevel/ToNextLevel/UntilExpiration/AlliancePointsPerTurn`, `GlobalParameters.ALLIANCE_POINTS_MULTIPLIER`, `GameInfo.Alliances`, and a port of `GetAllianceModifiersFromDB` querying `AllianceEffects`/`ModifierStrings` Context='Summary'; ALLIANCE_MAX_LEVEL=3) → Level + Points + Current alliance (name/level/bonuses/expiry) + Benefits-at-next/current-level sub-trees per alliance type; `AddEmergencyChildren` (`Game.GetEmergencyManager():GetEmergencyInfoTable(localID)`, filtered to crises the selected player joins, grouped Targeting-you vs Participating by local-player role) → "Name, N turns left/Completed" entries with goal lines; `AddGrievancesChildren` (`GetGrievancesAgainst` sign → favor/against-you/against-them, `GetGrievanceChangePerTurn`/`Tooltip`, `GetGrievanceLogEntries` sorted Turn-desc) → favor + description + two balance lines + Grievance log sub-tree. New loc tags `LOC_CAI_DIPLOMACY_ALLIANCE_*`/`_EMERGENCY_*`/`_GRIEVANCE*`. Generic panel-text walk (`AddGenericPanelChildren`) stays as the fallback only for any unkeyed/future tab. **In-game test passed:** confirm Alliance (non-teammate AI), Emergency (shared active emergency), and World Congress/Grievances (post-WC-era) nodes appear and read cleanly with category grouping; check World Congress does NOT show pre-WC-era (its button may not be hidden even when vanilla hides the tab bar — verify the `ControlIsHidden(button)` filter catches it); verify the grievance against-you/against-them direction labels match the board.
- **Joint/third-party war + research agreement selection (implemented 2026-05-31, verified in game).** Vanilla `OnClickAvailableAgreement` resolves `JOINT_WAR`/`THIRD_PARTY_WAR`/`RESEARCH_AGREEMENT` through the mouse-only `ShowAgreementOptionPopup` *before* the item exists (other agreements + ALLIANCE add directly, value chosen later via offer-item edit), so clicking joint war in the CAI inventory was a silent no-op — no item, inaccessible popup. Fixed by extracting `PushAgreementOptionList(agreementType, turns, fromPlayerID)` from `PushAgreementSelector` (the offer-edit selector now delegates to it) and, in `PopulateAgreementsCategory`, routing the `AgreementNeedsOptionList(agreementType)` types (JOINT_WAR/THIRD_PARTY_WAR/RESEARCH_AGREEMENT, mirroring vanilla's branch) to push that list at click time instead of calling `OnClickAvailableAgreement`. Options come from `GetPossibleDealItems(..., AGREEMENTS, agreementType, ...)` and activate `OnSelectAgreementOption(...)`, which adds the item with the chosen target/war/tech. **In-game test passed:** clicking Joint War (and Third-Party War / Research Agreement) in the inventory now pushes a target/war-type/tech list; selecting one adds it to the offer; Escape backs out.
- **Diplomatic Favor in deal view (Gathering Storm, implemented 2026-05-31, verified in game).** Favor is a GS-only deal item (`DealItemTypes.FAVOR`) added by the `_Expansion2` rider; the CAI deal view predated it, so two gaps were fixed: (1) `RefreshInventoryTree` had no Favor category — added `PopulateFavorCategory` (after Gold, before Luxury) mirroring vanilla `PopulateAvailableFavor`: gated by `DealItemTypes.FAVOR` existence + `not m_state.isDemand` (favor isn't demandable) + `sidePlayer:GetFavor() > 0`, label "Diplomatic Favor <balance>", activates the global `OnClickAvailableOneTimeFavor(side, DEFAULT_ONE_TIME_FAVOR=1)`; (2) `GetDealItemLabel`/`GetAmountHeader` had no FAVOR branch, so favor already in a deal (e.g. AI-proposed) read as a blank/silent row — added branches reading `LOC_DIPLOMATIC_FAVOR_NAME` + `GetAmount()`. All FAVOR references guard on `DealItemTypes.FAVOR ~= nil` so base/XP1 are untouched. Accumulated strategic resources verified already-correct (return as ordinary `RESOURCES` lump-sum entries, reuse the XP2 `OnClickAvailableResource`). No new CAI loc tags. **In-game test passed (GS):** offer favor from the inventory (only when you have favor and it's not a demand), confirm an AI favor offer reads "Diplomatic Favor N", and editing a favor amount shows the favor header.
- **Secret Society overview row (Ethiopia / Secret Societies mode, implemented 2026-05-31, verified in game).** Unlike the XP1/XP2 alliance/emergency/world-congress content (which gets intel *tabs*), the Ethiopia pack only adds an overview *row* via `DiploScene_RefreshOverviewRows` (`DiplomacyActionView_SecretSocietyRow`), so CAI surfaces it as one more leaf in `AddOverviewChildren`, not a new reader/tab. Gated by `IsSecretSocietiesActive()` (`GameCapabilities.HasCapability("CAPABILITY_SECRETSOCIETIES")`) + a defensive `GetSecretSociety` method check, so non-Ethiopia games add nothing and `AddOverviewChildren` (non-self only) is the natural home. Rebuilt from `ms_SelectedPlayer:GetGovernors():GetSecretSociety()` → `GameInfo.SecretSocieties[hash].Name` / `LOC_SECRETSOCIETY_DIPLO_{NONE,UNKNOWN}_NAME` (+ `_UNKNOWN_DESCRIPTION` tooltip), header `LOC_SECRETSOCIETY`. Replicates vanilla's awareness quirk (awareness checked against the *selected* player's own governors, so the real name shows once a leader joins — "Unknown" branch is effectively dead) for screen parity, per user decision. No new CAI loc tags (reuses the mode's keys). **In-game test passed:** with Secret Societies enabled, select a leader who has joined a society (real name shows), one who hasn't ("None"), and confirm nothing appears in a non-Secret-Societies game.
- **DealView uses a `TabControl`** (My offer / Their offer pages). Each page contains its offers `Tree`, inventory `Tree`, and the deal-actions `List` — the actions live *inside* each page (after inventory) so a plain Tab from the inventory tree lands on them via ordinary in-page nav (a sibling list beside the TabControl is unreachable; the framework's Tab cycle keeps focus inside the strip/page, and a root-sibling `ReleaseTabAtBoundary` experiment was tried and reverted — the nav model isn't built for it). Actions are deal-wide, so both pages' lists mirror the same vanilla controls and are rebuilt together by `RebuildActionsList`; FocusKeys are side-suffixed (`diplo:deal:action:*:<side>`). On rebuild, if a list is focused the cursor is moved to its first child (the leader-line text item) *silently* — speech is owned by the announcement queue below. A demand hides the relevant side's offers tree via `HiddenPredicate`. Inline amount `EditBox` is wrapped in a Transparent host `Panel` (its own AlwaysEdit Esc binding returns false and `OnHandleInput` stops at the first match, so Esc must bubble to a parent to close); agreement selector is a pushed `List` (Esc bubbles from its option buttons to the list). Transients push with inherited priority.
- **Deferred announcement queue (fixes gift double-speak; adds offer-change reports).** Vanilla mutates the working deal in bursts (optimistic pre-propose pass, then the AI's settled response a frame or two later), so announcing on every `UpdateDealStatus`/`PopulatePlayerDealPanel` was noisy/contradictory. All leader-line text and offer add/remove changes are now queued and flushed on the first quiet frame via `ContextPtr:SetUpdate` (a dirty-flag waits one tick with no new change, then `ClearUpdate` + flush), so we speak the value vanilla settled on. Leader text is deduped against `lastSpokenLeaderText`. `RefreshOffersTree` diffs each side's offer items (by deal-item ID → label) against a stored snapshot and queues `Added/Removed to/from your/their side: a, b` (gated by `m_state.dealReady` so the initial build's pre-existing items aren't read out); this covers user- *and* AI-driven adds/removes. Flush order: side changes first, then leader line, via `SpeakLines(lines, true)`. Cleanup: `DestroyRoot` calls `CancelFlush` (`ClearUpdate` + clear queues). New loc tags `LOC_CAI_DIPLOMACYDEAL_{ADDED,REMOVED}_{YOUR,THEIR}_SIDE`. The equalize button label/tooltip still mirror vanilla live (`GetText`/`GetToolTipString`) verbatim — the "what would it take? / our side" vs actual-side mismatch is vanilla's own predictive label and is intentionally left as-is.
- **Bug-fix pass (implemented 2026-05-30, verified in game).** Four diplomacy fixes: (1) **Sub-menu cross-contamination** — all stmt sub-lists share the `g_ActionListIM` instance pool, so harvesting a later sub-menu (Casus Belli) `ResetInstances()`es the very controls an earlier one (Ask For Promise = `LOC_DIPLOMACY_DISCUSS`, holds the `KEEP_PROMISE_*` actions) captured, making "Ask For Promise" read back the live "Declare War" button. `CreateActionSubMenu` now snapshots each child's label/tooltip/disabled as values at build time instead of reading the recycled control live. (2) **Leader reselect on subtree entry** — the leader-row `focus_enter` no longer bails on non-leaf (`w:IsFocused()`) and compares against the live `ms_SelectedPlayerID`, so arrowing back up into an expanded leader's intel children reselects that leader (intel readers key off `ms_SelectedPlayerID`, so otherwise they'd render the other leader's data). (3) **Empty conversation focus** — new `IsConversationActive()` (conversation mode *and* list non-empty) gates both panel `HiddenPredicate`s, so Escaping out of a demand (which flips to conversation mode with a cleared list) returns focus to the overview instead of an empty conversation list. (4) **Demand inventory read-only** — `RefreshInventoryTree` sets `m_state.inventoryReadOnly` (`IsDemandFromOther()` or the hidden-offer side), captured by `CreateInventoryItem` and folded into its `DisabledPredicate`, so you can't add items to your own side on a demand you make (the other side's inventory stays editable).
- **Bug-fix pass 2 (implemented 2026-05-30, verified in game).** Follow-ups to pass 1, mostly UI-manager-level: (5) **Focus returns to the overview position, never the leaders tree, after a conversation** — the ActionView `SelectPlayer` OVERVIEW branch no longer calls `SyncSelectedLeaderRow` on conversation return. Order is now: `fromRow` (leave) → different leader (`SyncSelectedLeaderRow`, the only leader-row case) → same leader with `not IsFocusVisible()` (focused leaf's subtree went hidden, i.e. the conversation list) → `mgr:SetFocus(m_ui.overviewPanel)`. Focusing the transparent overview panel descends the cached `_lastFocusedKey`/`_lastFocusedChild` chain (`Nav.DefaultChild`), so it lands back on the action that launched the conversation (or its submenu), falling back to the first row only if that position is gone. A make-demand round trip leaves focus untouched (still visible on the actions list). `IsFocusVisible()` walks the focused leaf's ancestor chain for any hidden node. (6) **Suppress the actions-focus restore during conversation/cinema transitions, screen-side — NOT by silencing the API.** Vanilla `SelectPlayer` rebuilds the statement list (→ `RebuildActionsList`) and only *afterwards* calls `SetConversationMode`/`ApplyStatement` (see `DiplomacyActionView.lua:1818` then `:1825`, and `OnDiplomacyStatement:2812/2816`), so the actions list restored+spoke before the conversation list got focus — and when a picked Ask-For-Promise sub-option dissolved its submenu the restore fell to the *speaking* positional path and landed on Casus Belli. Fix: `m_state.suppressActionsFocus` (set for `CAI_CONVERSATION_MODE`/`CAI_CINEMA_MODE` around `orig`, reset after) makes `RebuildActionsList` pass a **nil capture** so every `RestoreFocus` no-ops for that pass; the conversation list / cinema line is then the only thing that speaks. DEAL mode is intentionally excluded — the triggering Make Deal/Demand button survives the rebuild, so its restore is a silent FocusKey match that also preserves the spot to return to. `mgr:RestoreFocus` default behavior is unchanged (FocusKey match silent; positional + first-child speak). (7) **`RestoreFocus` stops at collapsed expandables** — the positional `path` walk breaks at any node whose `IsExpanded == false` instead of descending, so a captured position inside a since-collapsed submenu/tree-item lands on the node rather than silently re-opening it. (8) **SubMenu mirrors TreeItem expand/collapse** — `SubMenuWidget:GetDefaultChild`/`GetEntryChild` now also require a remembered focus child (`_lastFocusedChild`/`_lastFocusedKey`), so a bare `Expand(true)` (BuildFocusPath ancestor open, seeding) no longer auto-enters; and `EnterFirstChild` is a no-op once expanded, so a Right/Enter bubbling from a child can't re-enter and yank focus back to the first child.
- **Bug-fix pass 3 (implemented 2026-05-31, verified in game).** Two open-into-conversation focus fixes. (9) **Open-into-conversation lands on the conversation list, not the hidden leaders tree.** An AI-initiated request (e.g. allow an embassy in your capital) runs vanilla `OnDiplomacyStatement` → `SetConversationMode` + `ApplyStatement` *before* `OnShow` (vanilla's own OnShow note confirms this), so by push time the conversation container is shown + its list populated but the overview panel is hidden. `PushRootFocusingSelected` only special-cased cinema and otherwise focused the *selected leader's row* — which sits inside the hidden `overviewPanel`. Root cause beyond this screen: `SetFocus`/`BuildFocusPath` only skip hidden widgets while *descending* to a leaf; they do **not** reject an explicit target whose *ancestor* is hidden (programmatic focus trusts the caller). So focusing the leader row drove focus straight into the hidden tree. Fix kept at the screen level (mirroring the cinema branch): `PushRootFocusingSelected` now checks `IsConversationActive()` and pushes with `focus = m_ui.conversationList` when the conversation is the live view, before the leader-row fallback. Did **not** add an ancestor-visibility guard to the manager (would change focus semantics for every screen). (10) **Conversation rebuilds always reset to the first item.** `RefreshConversationPanel` was doing `CaptureFocusKey` → `RestoreFocus`, which restored the *positional* slot of the reply just picked (conversation reply buttons carry no `FocusKey`, so the capture only matched by index). A rebuild always presents a fresh leader response, so focus belongs on the first item (the response text). Fix: dropped the capture/restore; after `ClearChildren` it nils the list's `_lastFocusedKey`/`_lastFocusedChild` entry-descent cache so `FocusConversationIfReady`'s `SetFocus(m_ui.conversationList)` descends via `DefaultIndex` → first visible child. The existing guards (root must be top, list must be populated) still prevent stealing focus when a deal screen is open or the demand routed straight to the deal. **In-game test passed:** (a) AI request conversation opens reading the response text + reply choices, not the leaders tree; (b) picking a reply that yields a follow-up response reads the new response first, then arrow down into the new choices; (c) cinema intro still lands on the cinema panel and opening the screen yourself still lands on the selected leader row.
- **Focus-model rules learned (durable):** `RestoreFocus` by FocusKey is *silent*; positional `path` restore and first-visible-child fallback both re-`SetFocus` and *re-speak* (the positional walk additionally stops at collapsed expandables). A screen that hands focus elsewhere right after a rebuild should pass a **nil capture** to skip the restore for that pass, not lean on a silent restore. So all rebuilt rows that should restore silently need a `FocusKey` (statement actions `diplo:action:*`, deal actions `diplo:deal:action:*`, offers `diplo:offer:*`, leaders `diplo:leader:*`, side buttons `diplo:deal:side:*`). View panels are `Transparent` (no bare "panel" / duplicate-title speech). Conversation focus is deferred until the list has content (driven from both `SetConversationMode` and `RefreshConversationPanel`, whichever runs second) so the label/choices are fresh and demands don't focus an empty list. Leader-row selection sets a `selectingFromRow` guard so `SelectPlayer` doesn't re-focus (double-speak) the row the user just navigated to; same-leader returns from a deal leave focus on the action (e.g. Make Deal) rather than jumping to the leader.

## DeclareWarPopup migration (implemented 2026-05-31, verified in game)

`DeclareWarPopup_CAI.lua` rebuilt on the new UI manager (full rewrite off the deleted old API). Durable facts:

- **Expansion include chain** (like DiplomacyActionView/GovernmentScreen): re-includes `DeclareWarPopup_Expansion2` / `_Expansion1` / base via `Civ6Common`'s `IsExpansion2Active`/`IsExpansion1Active`. Required because the popup is a full `ReplaceUIScript` (no wildcard) and the expansions override `DeclareWar` (golden-age / retribution / ideological war session strings) and `OnShow` (XP2 grievances-instead-of-warmonger consequence) — a bare base include would silently drop all of that.
- **Only `Targets` + `WarmongerStack` are ever populated** — confirmed by full decompiled grep that vanilla and both expansions never fill the other four XML containers (`DefensivePact`/`CityState`/`TradeRoute`/`Deals`); they stay hidden. CAI still reads all five stacks behind a live `IsHidden`+children guard for forward-compat, but in practice only the warmonger/grievances line and the per-target civ rows appear.
- **Dialog via `mgr.WidgetHelpers.MakeGeneralDialog(titleFn, buttonRow, contentRows, 1)`** (Advisor pattern). **Title** is the generic header `LOC_DECLARE_WAR_HEADER` ("Declare war") via `Locale.Lookup` (the vanilla header label has no control ID). **Content** is two flat `StaticText` rows (no List wrapper): row 1 folds the live advisor message (`Controls.Message`, "...start a war with:") together with the comma-joined target civ names; row 2+ is the consequence line(s) — warmonger penalty (base) or grievances (XP2) — read live from the consequence stacks. (Only `WarmongerStack` is ever populated; the rest read behind a live guard for forward-compat.) Buttons Yes-then-No bound to `Controls.Yes`/`Controls.No`; default index 1 = Yes (Declare War fires on Enter). Activation calls `native:DoLeftClick()` so vanilla's registered eLClick callbacks run intact (Yes → `confirmCallbackFn` incl. WMD/ICBM custom callbacks → OnClose; No → OnClose) rather than CAI re-implementing `DeclareWar`.
- **Lifecycle:** `OnShow`/`OnClose` wraps + `SetShowHandler`/`SetHideHandler`; `m_opening` guard prevents the show handler double-building during `OnShow`. Close uses `mgr:RemoveFromStack(id)`. Note vanilla's `OnClose`/`LocalPlayerTurnEnd` callbacks were registered with the *original* OnClose at `Initialize()` time, so the No button / turn-end auto-close tear down the CAI dialog via the hide handler (`SetHide(true)` → `RemoveDialog`), not via the OnClose wrap. Input wrap returns `true` on mgr-consume with the `IsDialogActive()`/`ContextPtr:IsHidden()` guard; Escape stays vanilla-owned. **In-game test passed:** base / XP1 / XP2 rulesets; single-target (unit-attack) vs multi-target (joint war); city-state target (XP2 grievances suzerain/envoys text); WMD/ICBM strike confirm path.

## CivicsTree + TechTree migration (implemented 2026-05-28)

Both trees are rewritten on the new UI manager. Notable shape changes from the old implementation:

- **Details follow the chooser pattern.** Per-civic / per-tech row tooltip is one comma-joined line (cost, turns, progress, description, boost, obsoletes inline, unlocks inline, awards inline). Detail children are exactly four buckets: **Unlocks** (named entries with descriptions + per-entry Shift+Enter Civilopedia jump — no counted wrapper node, no "No unlocks" placeholder), **Prerequisites**, **Leads to**, **Full path** (only when researchable and the path has >1 step). Each bucket is omitted entirely when empty.
- **Queue is a flat `List` of `Button`s,** not a Tree. Current civic/tech prefixed with `LOC_CAI_*_CURRENT`; queued rows show position via `FormatRowLabel`. Activate jumps to the main-tree node; Shift+Enter opens Civilopedia.
- **Filter is a `List` of `Button`s** between the queue and the main tree (replacing the old DropdownMenu). Activating a non-None filter calls the vanilla `OnFilterClicked` so vanilla state matches CAI's, rebuilds the main tree filtered, and pushes a transient `List` of result Buttons. The result list's `destroy` event always resets both vanilla and CAI filter to None — covers both pick-and-go and Escape paths. Picking a result pops the results list and jumps to the now-revealed main-tree node.
- **Reference link "Jumping to" feedback.** All ref links (prereqs, leads-to, full path, queue rows, filter result rows) funnel through a shared `JumpToCivic` / `JumpToTech` helper that calls `mgr:SetFocus(target, { announce = false })` then `SpeakLines({ "Jumping to <name>" } + mgr:BuildAnnouncement(path, #path), true)` so the jumping line interrupts ongoing speech and the natural focus speech queues behind it.
- **Awards inline (civics only).** The XP1 Governor title (`MODIFIER_PLAYER_ADJUST_GOVERNOR_POINTS`), XP2 Favor (`MODIFIER_PLAYER_ADD_FAVOR`), and base-game Envoys-awarded (`MODIFIER_PLAYER_GRANT_INFLUENCE_TOKEN`) modifiers are surfaced as a comma-joined inline `Awards: …` line at the end of the row tooltip — sourced from `TechAndCivicSupport_BuildCivicModifierCache()`, matched against `g_ExtraIconData` for parity with vanilla's icon strip. No separate detail bucket (these have no descriptions or Civilopedia entries). The chooser tooltip got the same Awards line for parity with vanilla, which already shows the icons on its node row. Verified across base / Australia / XP1 / XP2 IconLoaders — these three modifier types are the only registered entries.
- **DoLeftClick where vanilla provides a click callback.** Enter on a tech/civic row prefers `g_uiNodes[type].NodeButton:DoLeftClick()` (fall back to `OtherStates`, then direct `RequestPlayerOperation` exclusive) so vanilla's `SetCurrentNode` runs — picks up any vanilla / mod hooks that wrap it. Ctrl+Enter still routes directly because vanilla's append branch is gated on a local `m_shiftDown` we can't safely toggle. Both paths trail with a CAI `Speak(LOC_CAI_*_QUEUE_ADDED, count, totalCost)` summary.
- **Shared helpers** lifted into `inGameHelpers_CAI.lua`: `UNLOCK_DESC_TABLES`, `GetUnlockDescription(typeName)`, `GetCivicUnlockObjects(kData)` (flat array), `GetTechUnlockObjects(kData)` (`{ Unlocks, Reveals }` split), `GetAwardNames(modifierList)`, `GetCivicAwardsText` / `GetTechAwardsText`, `CreateUnlockChild(mgr, unlock, idPrefix)`. The local duplicates in `CivicsChooser_CAI.lua` and `ResearchChooser_CAI.lua` were deleted. The legacy counted-node builders (`AddCivicUnlocksNode`, `AddMakesObsoleteNode`, `AddTechUnlocksNode`, `AddTextDetailNode`, `AddCountedListNode`, `GetCivicUnlockNames`, `GetTechUnlockNames`, `SplitTooltipLinesWithoutUnlocks`, `SplitTooltipLinesWithoutSpecialLists`, plus the `IsUnlocksHeader` / `IsMakesObsoleteHeader` / `StartsWithIconBullet` helpers feeding them) are gone — no callers remain.
- **New loc tags** in `cai_text_ui.xml`: `LOC_CAI_CIVICS_TREE_JUMPING`, `LOC_CAI_TECH_TREE_JUMPING`, `LOC_CAI_CIVICS_TREE_FILTER_RESULTS`, `LOC_CAI_TECH_TREE_FILTER_RESULTS`, `LOC_CAI_CIVIC_AWARDS_HEADER`, `LOC_CAI_TECH_AWARDS_HEADER`. The legacy counted-unlocks / obsoletes / "no unlocks" loc tags are now orphaned but not removed in this pass.

## Grid widget rework + CivicsTree/TechTree grid view (implemented 2026-05-28)

- **`CAIWidget_Grid.lua` uses a three-level hierarchy** Grid → Column → Tier → item cell. A **Column** is a labeled group that speaks only its header (`SpeechSettings = { Role=false, Position=false }`) and owns its cells through tiers, so the manager's focus-divergence machinery announces the column header automatically only when focus crosses into a new column. A **Tier** is a `Transparent` side-by-side sub-column (`width` = tier count). An **item cell** is any widget, stacked vertically. Navigation lives on `GridWidget`, reading the live leaf via `Manager:GetFocusedWidget()`: Up/Down within tier, Left/Right across the flattened tier list while preserving vertical index, Home/End tier edge, Ctrl+Home/End grid edge, and Ctrl+Left/Right previous/next column. API: `AddColumn`, `AddItem`, `GetColumnWidget`, `GetTier`, plus the tier-1 grid helpers `AddRow`, `GetCell`, `SetCell`, `RemoveRow`, and `ClearRows`.
- **CivicsTree gained a grid view + toggle.** `m_viewMode` ("grid" default / "tree"). Both views persist in the panel; the inactive one is hidden via predicate so nav skips it. (Initial focus when no civic is in progress lands on the active view; `PushPanel` resolves it by mode.) Panel child order is now queue → filter → main tree (hidden in grid mode) → grid view (hidden in tree mode) → unlocks list (grid mode, hidden when empty) → government edit → **change-view button (always last)**. Grid view: eras = columns, tiers = distinct `kEntry.Column` groups within an era (sorted, cells by `UITreeRow`); cells are `Button`s reusing `FormatRowLabel`/`FormatRowTooltip` and `FocusKey = "civic:<type>"`, with the same activate (set-current) / Ctrl+Enter (append) / Shift+Enter (pedia) bindings as tree nodes but no expandable detail children. A sibling unlocks `List` rebuilds on each cell's `focus_enter` (described unlocks only, via `CreateUnlockChild`). `m_civicsByType` split into `m_treeCivics` / `m_gridCivics`; `GetActiveCivicWidget` resolves by mode so `JumpToCivic` (queue rows, filter results, ref links) lands in the active view. Filter open/reset and `CivicCompleted` now call `RebuildCivicsViews()` (rebuilds both). Loc tags: `LOC_CAI_CIVICS_TREE_GRID` (screen-specific label) plus the shared `LOC_CAI_TREE_SWITCH_TO_GRID` / `_SWITCH_TO_TREE` / `LOC_CAI_TREE_TIER`.
- **Inline prereqs + leads-to in the row tooltip (both views).** `FormatRowTooltip` now appends `Required civics: …` and `Leads to: …` right before the `Unlocks: …` segment. Applies to tree rows too (so they show inline AND as the existing expandable detail children — by request). New loc tags `LOC_CAI_CIVIC_PREREQS_HEADER`, `LOC_CAI_CIVIC_LEADS_TO_HEADER`.
- **Tree view now nests civics under tier nodes.** `RebuildMainTree` groups each era's civics by tree `Column` (the prereq tier, same grouping as the grid view) into `TreeItem` tier nodes (`FocusKey "tier:<eraType>:<colVal>"`, label `Tier N` via shared `LOC_CAI_TREE_TIER`). Structure is now era → tier → civic → [detail nodes]. Tiers are created with `IsExpanded = true` (silent) so expanding an era reveals their civics directly, and each era has an `expanded` listener that force-re-opens all its tiers (silent direct `IsExpanded = true`) — forward-compat for a planned recursive collapse that would otherwise leave tiers closed on era re-expand. Main tree `SearchDepth` bumped 2 → 3 so type-to-find still reaches civic names through the extra level.
- **Unrevealed civics are neutered for parity, but keep their topology (both views).** Vanilla `CivicsTree.lua` PopulateNode hides per-node detail for `not live.IsRevealed`: name → `LOC_CIVICS_TREE_NOT_REVEALED_CIVIC`, generic node tooltip (no cost/turns/desc/boost/obsoletes/unlocks/awards), unlock stack hidden, right-click Civilopedia gated on `live.IsRevealed`. **However `AllocateUI` draws the prereq/leads-to connector lines for every node unconditionally** (loop over `item.Prereqs`, no reveal check; nothing hides them later) — so the tree topology is visible even for unrevealed nodes. CAI matches: `GetCivicName` returns the not-revealed string when `kLive and not kLive.IsRevealed` (labels, ref links, jump text, and prereq/leads-to entry names show "Not revealed" instead of leaking identity); `FormatRowLabel` returns just that name; `FormatRowTooltip` gates cost/turns/progress/desc/boost/obsoletes/unlocks/awards behind a `revealed` flag but **always** emits the prereqs + leads-to lines; `AddCivicDetailChildren` gates only the Unlocks bucket on revealed (Path bucket is already `CanResearch`-gated) and keeps Prereqs/Leads-to; table `RebuildUnlocksList` stays empty when unrevealed; all Shift+Enter Civilopedia bindings (tree node, table cell, queue row, filter result) gained a `not IsCivicRevealed(capturedType)` guard (ref links already gated via `IsDisabled`). Activation/Ctrl+Enter were already blocked through `CanResearch`. `IsRevealed` comes from `pPlayerCultureManager:IsCivicRevealed(item.Index)` when that method exists (some modes/expansions), else true.
- **Unrevealed prereq/leads-to entries carry their tree location.** Since the name is hidden, prereq/leads-to entries that point to an unrevealed civic show "<era>, Tier N Not revealed" (era omitted when it matches the listing civic's era) so the connector-line destination is still conveyed. `BuildStaticMaps` precomputes `m_civicTierByType` (a civic's 1-based tier = rank of its Column among its era's distinct Columns, unfiltered). Helpers `UnrevealedLocationPrefix` / `GetRelatedCivicLabel` (per-entry, used for the focusable ref-link rows in tree details) / `FormatRelatedCivicNames` (tooltip: revealed civics by name, unrevealed grouped by location — single → "<era>, Tier N Not revealed", multiple in one tier → "Tier N: Not revealed, Not revealed"). `IsCivicHidden` (true only when `kLive.IsRevealed == false`) drives the hidden check there, distinct from `IsCivicRevealed` (false also when data missing) used for the disable/pedia guards.
- **Jump feedback names the diverging era/tier (tree mode only).** `JumpToCivic` (used by ref links, queue rows, filter results) folds the target's tier + era into the spoken `Jumping to <civic>[, Tier N][, <era>]` line, but only for the parts that differ from where focus currently is. Source/target era+tier are found by walking the widget's `Parent` chain for the `era:`/`tier:` FocusKeys (`GetEnclosingEraTier`); comparison is by widget identity so Tier 1 of a different era still counts as a change. Jumps from outside the tree (queue/filter) report full era+tier. Table mode keeps plain `Jumping to <civic>` because the column header's natural (non-`IgnoreWhenNotFocused`) focus speech already announces the era on divergence.
- **TechTree got the full CivicsTree port (grid view, tiers, unrevealed handling, jump feedback).** `TechTree_CAI.lua` mirrors every CivicsTree change 1:1, adapted to the tech domain (no government/awards/obsoletes; unlocks come from `GetTechUnlockObjects(...).Unlocks`, reveals stay tooltip-only; `GetAllianceText` line preserved). Same structure: `m_viewMode` "grid" default; `m_treeTechs`/`m_gridTechs` split with `GetActiveTechWidget`; grid view (eras=columns, tiers=Column-groups) + sibling unlocks `List` + last-child change-view toggle; tree view nests techs under `Tier N` nodes (auto-expanded + era `expanded` re-open listener); `SearchDepth` 2→3; unrevealed techs neutered via `LOC_TECH_TREE_NOT_REVEALED_TECH`/`_TOOLTIP` (vanilla `TechTree.lua` PopulateNode 781/826/828/895/957, same rule as civics; lines still drawn so prereqs/leads-to kept); `m_techTierByType` + `IsTechHidden`/`GetRelatedTechLabel`/`FormatRelatedTechNames` for located unrevealed entries; jump feedback names diverging era/tier in tree mode. Tooltip prereq/leads-to use `LOC_CAI_RESEARCH_PREREQS_HEADER`/`_LEADS_TO_HEADER` (distinct text "Required technologies"/"Leads to"); grid label `LOC_CAI_TECH_TREE_GRID` ("Technology grid").
- **Tech tree tiers are now prereq-based, not cost-based (fixed 2026-05-28).** The base Civ VI tech tree lays out columns by COST (`Eras.xml` `TechTreeLayoutMethod="Cost"`; vanilla `TechTree.lua` `LayoutNodeGrid` makes one column per distinct in-era cost), so reading `kEntry.Column` put no-prereq techs like Sailing/Astrology (cost 50) in tier 2 alongside Writing (cost 50) while Pottery/Mining/Animal Husbandry (cost 25) sat in tier 1. The civics tree lays out by prereq (`CivicsTree.lua` pushes each civic one column past its in-era prereqs) and its CAI tiers read correctly off `kEntry.Column`. To match, `TechTree_CAI.lua` now computes its own prereq column via `ComputeTechColumn` (`column = 1 + deepest in-era prereq chain`, cross-era prereqs reset to 1 — same formula as vanilla's unused `"PREREQ"` layout method) stored in `m_techColumnByType`, and all five former `kEntry.Column` reads (tier ranking in `BuildStaticMaps`, `RebuildMainTree`, `RebuildTableView`, `OpenFilterResults` sort) route through it. Vanilla's `kEntry.Column` is left untouched so the on-screen tree stays correct for a sighted hotseat player. Result: Sailing/Astrology/Pottery/Mining/Animal Husbandry are all tier 1; their leads-to (Writing, Masonry, Shipbuilding, etc.) are tier 2.
- **Loc tags consolidated (no duplicate text).** The tier label and the two switch-view labels are identical across both trees, so they now share `LOC_CAI_TREE_TIER` / `LOC_CAI_TREE_SWITCH_TO_GRID` / `LOC_CAI_TREE_SWITCH_TO_TREE`; per-screen `_CIVICS_TREE_TIER`/`_SWITCH_*` and `_TECH_TREE_TIER`/`_SWITCH_*` were removed. Tags whose text genuinely differs (table label, prereq/leads-to headers) stay per-screen. (Pre-existing exact-duplicate tags like `LOC_CAI_*_TREE_UNLOCKS`/`_FILTER`/`_QUEUE_LIST` predate this work and were left as-is.)
- **Verified in-game (both trees):** grid/tree view toggle, tier navigation (Left/Right across tiers, Up/Down within, Ctrl+Left/Right across eras), era-header-on-column-change announce, unlocks list tracking, jump landing in the active view, and era→tier→item nesting with type-to-find all confirmed working.

## ProductionPanel migration (implemented 2026-05-27)

`ProductionPanel_CAI.lua` (2206 lines) is a no-back-compat rewrite for the new UI manager. Old API surface to delete: `mgr:CreateUIWidget`, widget types `"TabBar"`/`"Tab"`/`"Treeview"`/`"TreeviewItem"`, field callbacks (`OnClick`/`OnFocusEnter`/`IsHidden`/`IsDisabled`/`GetLabel`/`GetTooltip`/`IsExpanded`/`OnToggleExpanded`), `node._caiFocusKey` + hand-rolled `CaptureFocusStateForBody`/`FindFocusPathByKey`/`RestoreOrResetBodyFocus`/`BuildPathWithIndex`/`SetFocusIndexPath`/`SetFocusedChild`/`SetDefaultIndex`, `mgr:HasWidget`, multi-body `IsHidden`-toggle tab fake, `AddInputBinding` (singular).

**Includes**: `include("ProductionPanel")` then conditionally `include("ProductionPanel_Babylon_Heroes")` when Babylon is active (Mod GUID `1B28771A-C749-434B-9053-D1380C553DE9`). No XP1/XP2 ProductionPanel replacement exists — confirmed via game install scan. Babylon's only override is `RightClickProductionItem` (redirects Hero Devotion projects to the Hero unit pedia); our `InvokeRightClickPedia` wrap already routes through it.

**Layout** (Panel + TabControl + four TabPages):
- Page "Production" (TAB_PRODUCTION): current-production TreeItem (FocusKey `current`, Delete = remove from queue), then Tree "Available" with category TreeItems Districts / Wonders / Projects / Units (FocusKey `cat:<key>`); production tab merges nested buildings under Districts. Units with Corps/Army have child rows (FocusKey `item:prod:unit-corps:<hash>` etc.).
- Page "Purchase with Gold" (capability-gated `CAPABILITY_GOLD`): Tree with Districts / Buildings / Units.
- Page "Purchase with Faith" (capability-gated `CAPABILITY_FAITH`): same.
- Page "Queue": List of queue Button rows (FocusKey `queue:<index>`, Delete = remove, Shift+Up/Down = move). Current-production Button at the top when active.
- TabControl swap via `SetActivePageById`; vanilla `OnTabChange*` wraps call into it; `OnVanillaListModeChanged` reverse-routes.

**Item row** (TreeItem with `FocusKey "item:<mode>:<class>:<hash>[:corps|army]"`, `Label`/`Tooltip` functions, `DisabledPredicate`, `HiddenPredicate`, `On("activate")` → vanilla left-click action; `AddInputBindings` for Shift+Enter pedia and Ctrl+Enter queue-this-item). Drop the `CreateActionNode` indirection.

**Recommendation suffix** in label: replace boolean `m_state.recommended[hash]` with `{ hash → advisorType }` map sourced from `pCity:GetCityAI():GetBuildRecommendations()` and routed through shared `GetRecommendedPart(kData, disabled)` so labels read `Knight, Recommended by Conquest advisor` — same wording as the chooser migration.

**Tooltip vs details — no duplication rule**:
- Tooltip (comma-joined single line, chooser format `Name, 25 production, 5 turns, 30 percent, description, ...`):
  - Repair/already-built status
  - Cost: `<n> production` / `<n> gold` / `<n> faith` (use new `LOC_CAI_PRODUCTION_COST_*` tags matching `LOC_CAI_RESEARCH_COST` shape)
  - Turns left (production tab) or affordability (purchase tabs, append `Cannot afford` flag)
  - Accumulated progress percent when partial (`LOC_CAI_RESEARCH_PROGRESS` style — reuse "{1_Percent} percent")
  - Maintenance per turn
  - Strategic-resource upkeep (`-1 Oil per turn`) — new bucket; not currently extracted
  - One-line short description (LOC_*_DESCRIPTION first sentence)
  - Headline stats (units: combat/ranged+range/movement; buildings/districts/wonders: housing, amenities, citizen slots, GP points; projects: amenities-while-active / yield conversion)
  - Adjacency *headline* when short (≤ 2 lines)
  - Failure reasons (always — they explain disabled state)
- Details (TreeItem children, each category appears only when non-empty AND not already in tooltip):
  - Description (full prose) — only when longer than the one-line short used in tooltip
  - Bonuses — full yield/citizen-yield/GP/adjacency/housing/amenities list, only when count > headline threshold (e.g. >2 lines or a district with many adjacency rules)
  - Requirements — full prereq list: tech/civic prereq, district prereq, mutually-exclusive list, terrain (river/lake/coast/mountain), adjacent district/improvement/resource, religion-founded requirement with founder religion name
  - Reveals / Unlocks — for projects and wonders that grant follow-on capabilities
  - When all detail buckets are empty (basic worker/scout type rows), the TreeItem has zero children — focus speaks tooltip and Tab moves on. Drop `PRODUCTION_TOOLTIP_PREVIEW`, the mixed top-level description/cost/maintenance/failures rows currently in `AddDetailChildren`, and the `LOC_CAI_PRODUCTION_STATS_*` count tags (stats are tooltip-only now).
- `NewDetail()` becomes two-section: `{ status, cost, turnsOrAffordability, progressPct, maintenance, strategicUpkeep, descriptionShort, stats, adjacencyHeadline, failures }` (tooltip-only) and `{ descriptionFull, bonuses, requirements, unlocks }` (details-only).

**Focus restore on rebuild**: every row gets a `FocusKey`; each rebuild becomes the standard 3-line `mgr:CaptureFocusKey` / `ClearChildren` / `mgr:RestoreFocus` idiom. Delete the entire 160-line custom capture/restore block. Queue move: mutate target row's `FocusKey` to `queue:<targetIndex>` before `RestoreFocus`.

**Re-announce on game events**: `Events.CityProductionChanged` / `CityProductionUpdated` / `CityProductionQueueChanged` rebuild active body, then if focused widget's `FocusKey` starts with `item:`/`current`/`queue:` call `mgr:Refocus()`.

**Lifecycle**: `LuaEvents.ProductionPanel_Open`/`_Close`/`_ListModeChanged` keep current routing. Close path uses `mgr:RemoveFromStack(PANEL_ID)` (NOT `Pop`) per `feedback_mgr_mirrors_vanilla_flow`. Input handler wrap returns true on mgr consume per `feedback_input_handler_consume`, with the `if mgr:GetTop() ~= m_ui.panel then return false end` guard.

**Missing-from-vanilla items to add during rewrite** (currently not surfaced):
- `kItem.Repair` flag → explicit `Repair needed` in label/tooltip (current code reads `LabelText:GetText()` which strips the `[NEWLINE]` suffix).
- Strategic-resource per-turn consumption for units (XP1/XP2 `UnitConsumption.ResourceMaintenanceAmount`).
- Wonder-completed / "Already built" marker (vanilla's `CompletedArea` for wonders already in the city).
- In-progress production progress for non-current items already partly built (`ComposeProductionCostString`).
- Religion name on `RequiresReligion` buildings (vanilla's `ReligionIcon` tooltip).
- Project victory-stage context (space race, diplo victory) via `ToolTipHelper` lookup.
- District unique-replacement note when a civ's UD replaces a base district.

**Detail-bucket builders to reshape**: `BuildUnitDetail` / `BuildBuildingDetail` / `BuildDistrictDetail` / `BuildProjectDetail` keep their GameInfo accesses but emit into the new tooltip-only vs details-only split. `ExtractFailureReasons` stays (parses `[COLOR:Red]` blocks). `FormatDetailBriefTooltip` becomes `FormatTooltip(detail)` returning the chooser-style comma-joined string. `AddDetailChildren` only emits TreeItems for the four details-only buckets that are non-empty.

Reusable as-is from current file: data-extraction half (NewDetail buckets, per-class detail builders, GameInfo accesses, ExtractFailureReasons), all vanilla function wraps (`PopulateGenericItemData`, `PopulateList`, `PopulateWonders/Projects/Units/DistrictsWithNestedBuildings/DistrictsWithoutNestedBuildings`, `View`, `OnCorpsToggle`, `OnTabChange*`, `OnCityPanelChoose*`, `OnCityPanelPurchase*Open`, `OnProductionOpenForQueue`, `BuildBuilding`/`ZoneDistrict`/`BuildUnit`/`BuildUnitCorps`/`BuildUnitArmy`/`AdvanceProject`/`Purchase*` speech wraps), recommendation refresh, instance-by-hash capture, category-list capture.

## DiplomacyRibbon migration (implemented 2026-06-05, verified in game)

`DiplomacyRibbon_CAI.lua` adds accessibility for the top-right leader ribbon HUD. Partial replacement following the standard expansion include chain (base / XP1 / XP2). Wraps vanilla `UpdateLeaders` via `WrapFunc` so the CAI list auto-rebuilds whenever vanilla refreshes its leader entries.

- **Layout:** a single `List` widget pushed via Ctrl+P (toggle). Each met leader is a `MenuItem` with `FocusKey "leader:<playerID>"`. Local player listed first, then met-order. A World Congress `MenuItem` at the bottom when XP2 + capability + era are met (`FocusKey "congress"`). Escape binding closes the list.
- **Label:** leader name, relationship status (Allied/Denounced/At War/Friendly/Unfriendly — only shown when non-neutral, mirroring vanilla pip visibility rules from `DiplomacyRibbonSupport.lua`), "(you)" for local player, "active turn" when applicable.
- **Tooltip (always spoken regardless of vanilla RibbonStats display mode):** Capital, Score, Favor (XP2 + diplomatic victory only), Military, Science, Culture, Gold, Faith — comma-joined via `JoinNonEmpty`.
- **Activate:** leader items call vanilla `OnLeaderClicked(playerID)` (opens DiplomacyActionView / lite mode during congress). Congress item fires `CongressButton_ResumeCongress` or `CongressButton_ShowCongressResults` LuaEvents matching vanilla button behavior.
- **Input actions:** `DiplomacyRibbonOpenList` (Ctrl+P) toggles the list; `DiplomacyRibbonOpenWorldCongress` (F8) opens congress directly when available, gated behind `IsExpansion2Active()` + capability + era.
- **Rebuild:** wraps `UpdateLeaders` so all vanilla event triggers (diplomacy meet/war/peace, player defeat/restore, turn begin/end, etc.) automatically refresh the CAI list with `CaptureFocusKey`/`RestoreFocus`.
- **In-game test passed:** Ctrl+P opens/closes list; leaders read name + relationship + stats; Enter opens diplomacy; congress item appears when era reached; F8 opens congress; list rebuilds on new meetings/war/peace with focus preserved.

## GovernorPanel migration (implemented 2026-06-06, refined 2026-06-07, verified in game)

`GovernorPanel_CAI.lua` adds accessibility for the governor roster screen. Partial replacement with expansion include chain (`GovernorPanel` resolves to XP1 or XP2 via VFS).

- **Layout:** Panel with a governor Tree (titles summary row + TreeItem per governor, earned promotions as children) and a sibling promotions view (Grid for normal governors, List for secret society governors) that rebuilds on governor focus change.
- **Live vanilla text:** panel title follows the XML header loc key `LOC_GOVERNORS_TITLE` and no longer appends title counters. The summary row uses live `Controls.GovernorTitlesAvailable` / `GovernorTitlesSpent`; governor rows prefer captured live vanilla row controls (`GovernorName`, `GovernorTitle`, `GovernorStatus`, `GovernorStatusDetails`, stat labels, button tooltips) with GameInfo fallbacks.
- **Secret society reads:** live normal-governor stat controls are skipped for `IsCannotAssign` governors so Secret Society rows do not speak establish-turn / loyalty-pressure tooltips.
- **Promotion summaries:** governor row tooltips append earned promotion/title names only. Available promotions are omitted from the row tooltip; descriptions are read from the expandable earned-promotion child items.
- **Hidden secret titles:** the Secret Society promotion list includes hidden future titles. Hidden labels use the tree unrevealed `Unknown` loc text instead of vanilla `???`; hidden tooltips still use `GetPromotionHiddenDescription()`. Hidden normal-governor promotion grid cells remain skipped.
- **Secret promotion tooltips:** Secret Society promotion-list item tooltips speak only the promotion description, or the hidden description for unrevealed items. Prerequisite and leads-to relationship text is suppressed for Secret Societies; normal governor promotion grid tooltips still include those relationships.
- **Focus / promotion visibility:** first open focuses the titles summary; focusing the summary clears the selected-governor index so the promotion grid/list hides. Governor focus restores still use `FocusKey` capture/restore on rebuild.
- **Promotion focus restore:** Changing the focused governor clears the promotion grid/list focus memory so entering the promotion view starts fresh. Same-governor refreshes preserve promotion focus, and confirmed promotion operations restore focus to the promoted item after the event refresh.
- **Promotion grid geometry:** Base Ability column + Level 1 + Level 2 columns; rows within each level correspond to vanilla Column 0/1/2, hidden spacers for empty slots. Secret society promotions use a linear List instead.
- **Promotion status/disabled state:** Promotion widgets use a live disabled predicate: read-only screen, base abilities, already-earned promotions, unavailable promotions, and hidden Secret Society future titles cannot activate. Labels speak status as appropriate: Base Ability, Earned, Available, Locked, Unrevealed, or Read-only. Available promotions still open the confirmation dialog.
- **Activation / row disabled state:** Governor rows have a live disabled predicate matching Enter behavior. World Congress read-only mode disables row activation; unappointed rows enable only when the player can appoint; appointed normal governors enable only when assignable / not neutralized; appointed Secret Societies are readable but disabled because they have no assign action.
- **Promotion dialog:** `MakeGeneralDialog` confirmation for earnable promotions, spending a governor title.
- **Event feedback:** Local normal-governor appointment speaks `{Governor} appointed`; local Secret Society appointment speaks `{Society} joined`. Promotion confirm performs a live `CanEarnPromotion(governorHash, promotionHash)` check and immediately speaks `{Governor} gained title {Promotion}` before closing the dialog/requesting the operation; the `GovernorPromoted` event is used only to restore focus after refresh.
- **Assignment confirmation no-governor preview:** When the selected city has no current governor, the confirmation dialog now honors vanilla's hidden preview controls and speaks only `No Governor Assigned` for the current-governor line, without stale identity-pressure or turns-to-establish tooltips.
- **Lifecycle:** wraps Open/Close/Refresh/OnInputHandler/OnShutdown via WrapFunc; re-registers ContextPtr handlers after wraps. Input handler checks `mgr:GetWidgetById(PANEL_ID)` (not just top == panel) so promotion dialogs pushed on top still route through the manager.
- **Modinfo:** File entry + ReplaceUIScript (LuaContext=GovernorPanel, LoadOrder=100).
- **Loc tags:** 24 new `LOC_CAI_GOVERNOR_*` tags in `cai_text_ui.xml`.
- **In-game test passed:** open governor panel from LaunchBar and confirm focus lands on titles summary with promotions hidden; navigate tree (summary, governors, earned promotions); focus normal governors to see the table and secret society governors to see the list; confirm live row text matches vanilla controls; activate unappointed/appointed governors; enter on earnable promotion for dialog; test XP2 read-only during World Congress.

## Session Notes

- StagingRoom frontend accessibility implemented (2026-07-03): `UI/frontEnd/Multiplayer/StagingRoom.lua` is a full vanilla-copy replacement with a single CAI panel pushed from `OnShow()` and removed only around vanilla dequeue functions. The panel exposes player slots, ready/start, writable chat entry, chat target, a chat-history list of static-text entries, read-only game summary, copy join code, Play By Cloud End Game and Quit Game, a persistent friends list, and Game Setup. Editable slots are submenus; non-editable slots are menu items; slot labels append ready plus human/AI/observer/open/closed state. Follow-up fixes from user testing: ready/start changes now announce only the changed control instead of refocusing the panel; slot tooltips were trimmed to non-redundant details; Add Player moved to the bottom of the player list; team dropdown options now mirror vanilla `SetupTeamPulldown(...)` by including existing teams plus one empty new-team entry; slot summaries now derive civ/leader, team, and color from live player state instead of icon-only control text; the slot-type child dropdown now speaks its selected explanation and no longer includes swap, instead showing a disabled `Choose slot type` dummy option when no real slot types are available; swap is now a separate slot child button that calls vanilla `OnSwapButton(playerID)` under vanilla-style eligibility; real multiplayer chat delivered to the local client is now auto-spoken once, non-interrupting, using the same accessible line recorded in CAI chat history, including chat sent by the local player; vanilla lobby system chat lines for player connected, disconnected, host migrated, and kicked are now auto-spoken once as well; `/help` is now mirrored into CAI chat history as a local-only chat line and auto-spoken with multi-line speech, and the chat input tooltip falls back to vanilla's `LOC_CHAT_HELP_COMMAND_HINT`; the leader child dropdown now matches Advanced Setup labels/tooltips; the color child dropdown now mirrors vanilla's manual color alternate list instead of generic parameter values; and a copied Advanced Setup leader-tooltip dependency bug was fixed locally by defining the unique-type grouping helpers inside StagingRoom. Refresh behavior was then tightened to reduce lag: CAI no longer full-rebuilds the player list from wrapped `UpdateAllPlayerEntries`, `UpdatePlayerEntry`, or `PlayerInfoChanged_SpecificPlayer`. Instead it queues one deferred CAI player-list rebuild per frame from higher-level vanilla paths (`BuildPlayerList`, `OnPlayerInfoChanged`) plus the non-player-info slot presentation cases (`OnModStatusUpdated`, countdown start/stop), CAI's own player-parameter setter now also queues through that same deferred path, and the persistent friends list now rebuilds on vanilla friend updates plus player/game-state refreshes that affect invite eligibility. The chat area now follows the requested order by placing chat history after the target dropdown, and the old read-only history edit box is gone. Follow-up fix: when slash commands such as `/w` change vanilla's chat target, CAI now silently updates the target dropdown selection only when the target actually changed, avoiding redundant reselection when the CAI dropdown itself initiated the target change. `ConfirmKick.lua` is now registered as a frontend full replacement with no CAI widgets; its installed handler routes to the CAI UI manager first, then directly mirrors the vanilla Escape close path without calling the original old-signature `InputHandler`. File, frontend import, `ReplaceUIScript`, localization tags, and `docs/game-api.md` notes are added. In-game test passed: reach StagingRoom from hosted/joined multiplayer, confirm slot rows now speak civ/leader, team, difficulty, and color; verify slot-type child focus reads the explanation tooltip, never auto-selects swap, and shows `Choose slot type` when no real slot types are available; verify the separate Swap button appears for eligible target slots and toggles the swap request; verify sent local chat, chat from another player, and player connected/disconnected/kicked/host-migrated system lines auto-speak once; type `/help` and verify command help appears in the CAI chat history list and speaks once; type `/w` in chat input and verify the CAI target dropdown updates exactly once to the whisper target; focus chat input and verify the tooltip says to type `/help` for command help; verify color dropdown opens and changes color correctly; verify leader child labels/tooltips match Advanced Setup; confirm repeated option changes no longer cause heavy lag or multiple audible refreshes; confirm ConfirmKick Escape closes; confirm focus and Escape/back behavior, navigate/edit slots, ready/start, send chat and switch target, read summary, copy join code, verify PBC end/quit visibility/actions, verify the friends list stays visible and each friend submenu exposes the correct actions, and use Game Setup to return without leaving the session.
- StagingRoom feedback follow-up implemented (2026-07-03): human-target Swap buttons now expose `Swap, on` / `Swap, off`, speak the new state immediately when toggled, and still use plain `Swap` for non-human instant swaps. Successful swaps are inferred from the local player ID changing across vanilla player-info refreshes; CAI then speaks `Swap successful` and moves focus to the new local slot only when focus was already somewhere inside the player-slot list. Ready-state changes now announce `{player} ready` / `{player} unready`. The CAI ready button keeps the action label as `Ready` / `Unready`; during active countdowns its tooltip now includes the live countdown number, and if that button is focused each countdown refresh speaks only the number. Friend-submenu invite activation now speaks `Invite sent`. Additional in-game retests passed: verify successful human and non-human swaps, confirm focus lands on the new slot only when the current focus was inside the slots list, confirm human swap buttons update between `on` / `off` correctly without duplicate chatter, confirm focused ready-button countdown ticks speak only the number while the tooltip includes the countdown context, confirm ready/unready announcements for local and remote players, and confirm friend-list invite actions speak once and still update the friend row state.
- StagingRoom vanilla-hints audit completed (2026-07-04). Durable findings were added to `docs/game-api.md`: vanilla start-blocker hints are split across the centered start banner/ready control tooltips, per-row `StatusLabel` text and tooltips, and inline invalid markers on leader/game-setup controls. Current CAI coverage appears good for the global ready/start blocker text and basic row status text, but likely gaps remain for row-status tooltips, inline invalid markers on dropdown choices, and per-parameter invalid tooltips inside the CAI read-only game summary. Vanilla has no dedicated "remove slot" action beyond changing slot status (typically to `Closed`); true disappearance of slots comes only indirectly from map-size / max-major-player changes.
- StagingRoom parity pass implemented (2026-07-04): CAI slot, ready-button, and dropdown tooltips now use explanation-only deduping so text already present in the focused label/value is not repeated in the tooltip. Slot focus again exposes vanilla row-status explanations without repeating slot summary text. Leader and generic parameter option lists now mirror vanilla invalid markers on option labels, while their closed-control tooltips stay explanation-focused. The color duplicate warning now lives on the CAI color control tooltip via the live vanilla warning icon tooltip. Dropdown option lists refresh from live vanilla state on focus so label/tooltip content stays current. The slot-type CAI dropdown now disables itself when there are no real slot actions even though it still keeps a dummy placeholder entry internally. Durable findings added to `docs/game-api.md`: vanilla Kick visibility is host + taken/observer + not-self + non-hotseat, and vanilla slot-type pulldowns disable when `ItemCount < 1`. In-game test passed: confirm slot and ready tooltips no longer repeat label text, verify invalid reasons appear on leader/parameter options and not redundantly in their closed-control tooltips, verify color-collision hint is on the color control tooltip, verify slot-type dropdown is disabled when only the dummy placeholder remains, and recheck vanilla-aligned Kick visibility/behavior on human, observer, AI, open, and closed rows.
- StagingRoom dropdown selection follow-up (2026-07-04): the first parity pass refreshed CAI dropdown options on `focus_enter`, which caused a just-committed selection to be overwritten briefly by stale vanilla state when focus returned from the option list to the closed dropdown. The refresh hook now runs immediately before `Open()` instead, so the widget keeps its internal committed selection right away after Enter while still opening against fresh vanilla options.
- StagingRoom dropdown tooltip follow-up (2026-07-04): after the selection-timing fix, closed dropdown tooltips could still lag because some CAI dropdowns were computing their tooltip from stale vanilla control tooltips even though the CAI widget value had already updated. StagingRoom now computes dropdown tooltips from CAI/game-state models instead of live vanilla control tooltip strings for Slot Type, Team, Leader, Color, generic player-parameter dropdowns, and chat target options. Color tooltips and color option tooltips now derive their conflict warning directly from the currently selected or hypothetical color state rather than waiting for the vanilla warning icon tooltip to refresh.
- StagingRoom parity follow-up (2026-07-04): CAI slot row labels/tooltips now hide ready/not-ready state only in hotseat, matching the user's requested scope. Normal network staging keeps ready state in slot labels/tooltips, while hotseat uses the simplified slot label because readiness is not a per-slot interaction there. The CAI ready action is now hotseat-aware: in hotseat it uses the live start-banner text as its label instead of `Ready`/`Unready`. The CAI slot-type child dropdown now mirrors vanilla filtering more closely by applying the same hotseat-only gating as `PopulateSlotTypePulldown()` and still excluding swap because CAI exposes swap as a separate action. In-game retest passed: confirm network slot rows still speak ready/unready in normal multiplayer, confirm hotseat slot rows do not, confirm hotseat ready action speaks `Start Game` or the live blocker text instead of `Ready`, and confirm slot-type dropdowns still offer AI in valid hotseat/network cases while suppressing options vanilla would hide.
- StagingRoom hotseat-ready announcement follow-up (2026-07-04): CAI’s wrapped `UpdatePlayerEntry(...)` announcement for `{player} ready` / `{player} unready` now skips hotseat entirely. Hotseat uses the center start action rather than multiplayer ready semantics, so clicking Start should no longer speak `Player 1 ready`. In-game retest passed: in hotseat, activate Start and confirm no player-ready announcement is spoken.
- StagingRoom color-tooltip regression fix (2026-07-04): the first CAI-computed color-tooltip pass crashed because `CAI_BuildColorOptions()` called `CAI_GetColorOptionTooltip()` before Lua had defined that local function. The color helper block is now forward-declared correctly, and CAI conflict checks again call vanilla `UI.ArePlayerColorsConflicting(...)` directly. Follow-up correction: the later attempt to make leader/color closed-control tooltips read directly from CAI dropdown widget state was the wrong layer and introduced another runtime error; that screen-specific detour was removed. The fix we are keeping is in `CAIWidget_Dropdown:Commit(...)`, which now updates the committed value before closing/refocusing the dropdown. In-game retest passed: reopen staging room, focus/open the color dropdown, and confirm no runtime error while both the closed-control tooltip and per-option tooltip update immediately after a color choice.
- In-game ChatPanel investigation completed (2026-07-03). Durable findings were added to `docs/game-api.md`: `Assets/UI/Popups/ChatPanel.lua/.xml` is a WorldTracker-mounted HUD card, not a modal, with a scrollable history log, target pulldown, style-owned `ChatEntry` edit box and target icon, expand/contract buttons, drag resize handle, a toggleable player-list side panel, and a kick-vote side panel. Sighted usage is send on Enter, target switching by pulldown or slash commands (`/g`, `/t`, `/w`, `/help`), optional map-pin jump buttons in history, and per-player pulldown actions for kick / friend request / start kick vote, plus invite and join-code rows when the network mode allows them. Proposed CAI shape: preserve vanilla chat send/target mechanics, expose one CAI root with read-only history, writable entry, target dropdown, player-list access, kick-vote dialog support, and map-pin activation; treat visual resize/expand affordances as optional unless testing shows players need explicit control over visible history length. No code implemented yet.
- HostGame multiplayer setup investigation and first implementation completed on 2026-07-02. Durable findings and implementation notes were added to `docs/game-api.md`: `HostGame.lua/.xml` is a full-screen shell setup form with dynamic setup-parameter rows; the CAI replacement exposes those rows as grouped submenus plus sibling action buttons, skips shell tabs, treats game name as the `GAME_NAME` setup parameter row, uses silent checkbox seeding plus `SetValueSetter(...)` to drive the live vanilla checkbox, and calls `DoLeftClick()` directly on live controls because the base control class provides it.
- Espionage UI investigation completed on 2026-06-09. The family is four contexts: `EspionageChooser` (left slide-out travel/mission picker), `EspionagePopup` (mission briefing, abort, and completed-result popup), `EspionageEscape` (escape-route notification modal), and `EspionageOverview` (right-side partial screen with Operatives / City Activity / Mission History tabs). XP1/XP2 only replace `EspionageChooser` and `EspionageOverview` with XP1-named wrappers: chooser destination filtering skips Free Cities, and overview city activity appends city-states; popup, escape, support, and XML layouts are shared. Durable details were added to `docs/game-api.md`. Proposed next implementation: prioritize `EspionageChooser_CAI.lua` because it blocks active spy orders, then `EspionagePopup_CAI.lua`, `EspionageEscape_CAI.lua`, and `EspionageOverview_CAI.lua`, preserving vanilla callbacks and using live controls/functions for activation.
- Trade UI investigation completed on 2026-06-08. There are two distinct "trade" families: diplomacy deals (`DiplomacyDealView_CAI.lua` already exists as a wildcard rider) and trade-route management, which is currently not CAI-covered. The trade-route family is `TradeOverview`, `TradeRouteChooser`, and `TradeOriginChooser`, all registered directly in base `InGame.xml`; no XP1/XP2 replacements were found for the chooser/origin screens, and the only DLC trade-overview replacement found is the Indonesia/Khmer scenario maritime city-state icon tweak. Visually, `TradeOverview` is a tabbed side panel with My Routes / Routes to My Cities / Available Routes, grouped route rows, a my/their benefits toggle, yield/religion/trading-post/path-bonus icons, route distance, and row clicks that select traders or hand off to the route chooser. `TradeRouteChooser` is a left slide-out destination picker with a selected-route preview, dynamic filter pulldown, destination city-card list, TradeRoutes map lens paths, and Begin Route / Cancel confirmation. `TradeOriginChooser` is a simple city list for relocating traders to another origin city. Durable details were added to `docs/game-api.md`. Proposed next implementation: add new-framework CAI replacements for `TradeOverview_CAI.lua`, `TradeRouteChooser_CAI.lua`, and `TradeOriginChooser_CAI.lua`, preserving vanilla callbacks and route lens behavior.
- Unit promotion popup investigation completed on 2026-06-08. Base / XP1 / XP2 all register the same `UnitPromotionPopup` world-popup context and there is no DLC replacement for `UnitPromotionPopup.lua/.xml` in the decompiled UI mirror. Promotions are opened from `UnitPanel`: ordinary promotion classes raise the standalone darkened-screen promotion tree, while units with `NumRandomChoices > 0` use the compact in-panel promotion chooser. Confirmed standard-rules trigger is a Gathering Storm Rock Band (`UNIT_ROCK_BAND`, `InitialLevel="2"`, `NumRandomChoices="3"`). XP1 only includes base `UnitPanel`; XP2 adds unrelated Rock Band / build-adjacent / railroad-upgrade behavior and does not alter promotion flow. Durable API notes were added to `docs/game-api.md`.
- Governors screen investigation completed on 2026-06-06. Governors are a separate popup family, not part of `GovernmentScreen`: `GovernorPanel` is the main horizontally scrolling roster, `GovernorDetailsPanel` is the nested promotion/biography view, and `GovernorAssignmentChooser` is the left slide-out city picker that temporarily turns on the Loyalty lens. Main roster columns show portrait, name/title, turns to establish, identity-pressure stat, status, earned/available promotions, and footer actions (`Details`, `Assign`/`Reassign`, or `Appoint`). Secret Societies are folded into the same roster as special pseudo-governor columns with alternate art, hidden future promotions, and smoke effects in details. XP2's meaningful behavior delta is World Congress read-only gating (`IsReadOnly()` disables appoint/assign/promote while congress is in session); otherwise the screen family is largely the same shape as XP1. Durable API notes were added to `docs/game-api.md`.
- `GovernorPanel_CAI.lua` now reads each governor biography in the governor row tooltip, immediately before earned abilities, using the same `Description` loc key as the vanilla details panel. Promotion-confirm follow-up also removed the manual post-refresh `SetFocus` hack: async promote refresh now seeds the rebuilt promotions container with `mgr:PrepareFocus(...)` and lets normal manager focus restoration return to the promoted row after the dialog pops. In-game test passed: open Governors, move focus across standard and secret governors, confirm the row tooltip order is title/stats/status, then biography, then earned abilities, with no separate biography widget in the root panel, then promote a governor ability and confirm focus returns to the correct promotion without a hard jump or duplicate speech.
- World Congress screen investigation completed on 2026-06-03. Gathering Storm implements it as three separate popup contexts under `decompiled/DLC/Expansion2/UI/Additions/`: `WorldCongressIntro` (intro banner), `WorldCongressPopup` (main voting/review screen), and `WorldCongressBetweenTurns` (post-submit waiting banner). Sighted layout: a fixed left ribbon of world leaders with Favor/grievance info, a large framed center panel of resolution/proposal cards, bottom nav buttons, and a small top launch strip with World Congress / Diplomacy buttons. Stage 4 review has three tabs (`Last Session Results`, `Active Effects`, `Available Proposals`), while in-session flow is two-phase (`vote` then `confirm`). Resolution cards use separate A/B vote widgets plus a target pulldown; discussion/emergency cards use a single support/oppose vote widget or an out-of-session checkbox-style selector for submitting emergencies. Durable API notes were added to `docs/game-api.md`.
- Great People popup investigation completed on 2026-06-02. Base screen is `Assets/UI/Popups/GreatPeoplePopup.lua/.xml`; sighted flow is a horizontally scrolling card strip on the `Great People` tab, a vertical history table on `Previously Recruited`, and mouse-driven per-card actions (`Biography`, recruit-progress expansion, `Recruit`, `Pass`, gold patronage, faith patronage). The popup itself only handles `Escape`; launch/open comes from the Launch Bar, the `ToggleGreatPeople` input action, or `CLAIM_GREAT_PERSON` notifications. Expansion hooks are additive through the wildcard include: XP1 changes patronage tooltips when patronage is disabled for a yield, XP2 makes the popup read-only during World Congress sessions, and Babylon Heroes mode adds a third `Heroes` tab with its own hero-card panel (`GreatPeopleHeroPanel.lua/.xml`). Durable API notes were added to `docs/game-api.md`.
- Previous detailed implementation history and exhaustive verification backlog were intentionally removed during compaction to keep this file small.
- If older reasoning is needed, check `docs/game-api.md` and git history first.
- Religion-screen CAI implementation created on 2026-06-02. `ReligionScreen_CAI.lua` replaces the vanilla tab-strip + inline-editing model with a single browsable tree (one TreeItem per founded religion, each with Summary/Beliefs/Cities/Units/Pantheon categories) plus a conditional SetupButton that pushes modal dialogs for pantheon founding, religion founding (two-step: choose religion + choose beliefs), and belief addition. City rows expand to show follower counts for all founded religions. No expansion conditional include needed (base game is the only Lua file). Registered in modinfo as Files + ImportFiles + ReplaceUIScript. in-game test passed.
- Pantheon chooser investigation completed on 2026-06-02. Base game / XP1 / XP2 all use the same standalone `Assets/UI/Choosers/PantheonChooser.lua` left slide-out panel; no XP1/XP2 replacement exists. Scenario exceptions are `DLC/PolandScenario/UI/ReligionScreen.lua` and `DLC/VikingsScenario/UI/ReligionScreen.lua`, which embed pantheon founding inside the full religion screen, while `ReligionScreen_Indonesia_KhmerScenario.lua` only adjusts locked-belief presentation. Existing CAI coverage is in `src/UI/inGame/ReligionScreen_CAI.lua` via `OpenPantheonDialog()`; there is currently no separate `PantheonChooser_CAI.lua`.
- UI-manager refactor planning pass on 2026-05-24: current architecture is functional but mixed. `UIScreenManager.lua` is acting as factory + stack/focus controller, `widgetTemplates.lua` mixes declarative template defaults with widget-specific behavior, and `widgetTemplateHelpers.lua` has grown into a catch-all for navigation, dialog builders, search, and full edit-box behavior. Likely next step is to split shared container behavior from concrete widget classes and introduce a clearer widget-construction API before adding new composite widgets such as tables.
- `navCursor.lua` zone-change speech was cleaned up so cursor movement now tracks continent and owner-territory changes as stateful zones, speaks the localized continent name directly from `plot:GetContinentType()`, and uses vanilla `LOC_MINIMAP_UNCLAIMED_TOOLTIP` for unclaimed territory instead of the older CAI placeholder.
- Raw cursor sounds now live under `src/sounds/cursor/` with the old `02` suffix removed from filenames. New shared raw-audio manager scaffold lives in `UI/shared/audioManager_CAI.lua`, loads definitions from `CAI_AudioDefinitions` via `DB.ConfigurationQuery`, resolves the mod root from `UIManager:GetFilePath("audioManager_CAI.lua")`, and is attached to `CAIUIScreenManager` lifecycle helpers (`InitializeAudioManager`, `UpdateAudioManager`, `ShutdownAudioManager`). `CAIUIScreenManager:OnUpdate()` now services both search-buffer expiry and audio updates, so MainMenu, IntroScreen, and WorldInput only call the UI manager update hook instead of reaching into the audio object directly. `cursorAudio_CAI.lua` now queues/stops by audio tag (`CURSOR_TERRAIN`, `CURSOR_STINGERS`, `CURSOR_FOG`) instead of maintaining its own local `UI.PlaySound` queue, and it prefers `mgr:GetAudioManager()` for lookups. `CURSOR_WOOSH` is now treated as the fog layer for revealed-but-not-visible plots, matching the old Civ V behavior more closely, and the temporary fake river-crossing cue path was removed until real crossing assets exist. Cursor bed resolution was also brought closer to the Civ V plot-audio rules: mountain now overrides terrain/features via `plot:IsMountain()`, natural wonders do not promote into feature beds, ocean can use its dedicated `CURSOR_OCEAN` file instead of collapsing into coast, and generic water fallback only happens after terrain lookup. `CAIAudioManager:Play(soundId, options)` and `QueueSound(soundId, delaySeconds, options)` now accept an optional options table; current supported flag is `SkipIfPlaying = true`, which suppresses playback if `CAI.IsSoundPlaying(handle)` is already true at the moment the sound would fire. Queued sounds keep the options on the queue item and re-check at dispatch time, which is intended for future notification sounds that should not stack-spam while already active. The audio manager still survives the IntroScreen-to-MainMenu handoff and resolves tag settings generically by convention: `AudioTagEnabled_<TAG>` / `AudioTagVolume_<TAG>`, with fallback from exact tag to parent prefixes (for example `CURSOR_TERRAIN` can inherit from `CURSOR`) and a default volume scalar of `1.0` when no setting exists. In-game test passed: confirm raw sound definitions load in frontend and ingame contexts, mountain plots now fire `CURSOR_MOUNTAIN`, ocean tiles use `CURSOR_OCEAN`, terrain/fog/stinger tags stop and restart correctly, revealed fog plots add the woosh layer while visible plots do not, delayed cursor queues still fire while MainMenu / IntroScreen / WorldInput are active, `SkipIfPlaying` suppresses duplicate plays for immediate and queued calls, and changing tag-based audio settings updates already-loaded sounds without a reload.
- `src/ideHelpers.lua` now documents the shared audio manager for LuaLS/editor completion: `CAIAudioManager`, `CAIAudioRecord`, `CAIAudioDefinitionRow`, `CAIAudioQueueItem`, and `CAIAudioPlayOptions` are annotated, including the `SkipIfPlaying` option on `Play(...)` and `QueueSound(...)`. `UIScreenManager` annotations now also expose `AudioManager` and `GetAudioManager()`.
- NavCursor owner-zone speech now treats the local player as a known owner even though diplomacy `HasMet(...)` is not meaningful for self, and continent-zone speech now uses the CAI loc wrapper `LOC_CAI_NAV_CURSOR_CONTINENT_ZONE` (`The continent of {1_Name}`) instead of speaking the raw continent name.
- NavCursor zone-state updates are now visibility-gated: moving onto an unrevealed / hidden plot does not recompute or overwrite the remembered continent or owner zone, while visible and revealed-fog plots still update normally.
- Hotseat state-isolation investigation completed (2026-07-04). Durable findings were added to `docs/game-api.md`: `navCursor.lua` currently stores one global cursor position plus one global zone-memory set, `WorldInput_CAI.lua` currently owns one global `MessageBuffer` instance, and `RevealAnnouncements_CAI.lua` currently keeps one global reveal debounce/snapshot store even though its logic is local-player sensitive. Recommended direction: introduce a shared per-player CAI session-state store keyed by local player id, swap active cursor/message-buffer/reveal state on `Events.LocalPlayerTurnBegin`, and gate reveal flushing so only the active player's queued lines can flush while their turn is actually active. This avoids leaking player 1 reveals, cursor position, or buffered messages to player 2 in hotseat. No code implemented yet.
- Follow-up design direction (2026-07-04): the preferred shape is not one monolithic shared CAI state object but a reusable helper module, tentatively `PlayerStateManager_CAI.lua`, that each system includes and instantiates for itself. Each subsystem defines its own default state shape and asks the manager for the active player's bucket, so state is isolated both by system and by player. This same helper should cover cursor, message buffer, reveal announcements, and world scanner focus state. Durable hook note: vanilla `PlayerChange.OnOk()` raises `LuaEvents.PlayerChange_Close(Game.GetLocalPlayer())`, which is a good handoff-complete point for deferred turn-start speech.
- Message buffer settings pass completed (2026-07-08). `src/data/settings_CAI.sql` now registers a dedicated `MessageBuffer` settings section with per-category speech toggles for notifications, reveals, combat, movement, chat, and gossip plus a numeric `MessageBufferLimit` edit-box setting. `WorldInput_CAI.lua` now checks the category-specific speech setting in `OnCAIAppendToMessageBuffer(...)` before calling `Speak(text)`, and `MessageBuffer_CAI.lua` now enforces the stored `MessageBufferLimit` dynamically when appending entries.
- World scanner settings pass completed (2026-07-08). `WorldScanner_CAI.lua` now factors current-target resolution and current-item focus into shared helpers, uses the existing `ScannerAutoMoveCursor` setting to move the CAI cursor to the selected scanner target with a non-jump `snap` follow path after scanner speech, and supports a new `ScannerCoordinates` dropdown (`disabled` / `append` / `prepend`) for scanner item announcements. New navigation settings `ScannerAutoFocusValidTargets` and `ScannerAutoFocusActiveLens` default off and now gate whether those dynamic categories auto-focus when they become available or rebuild.
- UI manager search-buffer behavior updated (2026-07-08). `CAIUIScreenManager.lua` now owns explicit `ClearSearchBuffer()` and `TrimSearchBuffer()` helpers. While the type-to-find buffer is non-empty, manager-level Escape clears it and speaks `Search cleared`, and Backspace removes one character; if that empties the buffer it also speaks `Search cleared`. `SearchTimeout <= 0` now means the buffer never auto-expires while appending characters, so only user actions clear it.
- UI manager search-buffer regression fix (2026-07-08). A Lua syntax error in `CAIUIScreenManager.lua` came from using method-call syntax in a presence check (`input:GetKey and ...`). The manager now uses field access for the guard (`input.GetKey and input:GetKey() or nil`), which restores manager initialization and in turn fixes dependent frontend screens like `IntroScreen.lua` that were failing because `ExposedMembers.CAI_UIManager` was nil after the include error.
- UI manager type-to-find backspace follow-up (2026-07-09). Backspace is no longer just a raw string edit on `SearchBuffer`; `CAIUIScreenManager.lua` now resolves the active search-enabled ancestor and re-runs `CAIWidgetHelpers_Search.FindSearchResults(...)` after trimming the buffer, so shortening the query immediately updates focus to the new first match. Clearing to empty still speaks `Search cleared`.
- UI manager type-to-find cleanup (2026-07-09). Backspace handling is now merged into the normal search helper path instead of duplicating match/focus logic in the manager. `CAIWidgetHelpers_Search.lua` owns a shared `ApplyCurrentBuffer(...)` flow used by both `HandleChar(...)` and `HandleBackspace(...)`; `CAIUIScreenManager.lua` just updates the buffer (`AppendSearchChar` / `RemoveSearchChar`) and dispatches Backspace to the focused widget's `OnSearchBackspace()` handler. Lists and Trees now implement that handler directly, so the active search root comes from the same widget path as normal typing.
- Settler-lens expansion analysis was verified against decompiled UI: the base / expansion minimap still colors only the four `Map.GetContinentPlotsWaterAvailability()` buckets, while expansion-only extras are overlay managers layered on top of that lens. `SettlerInfluenceIconManager.lua` adds loyalty-pressure number icons from `Map.GetContinentPlotsLoyalty()`, and `SettlerWarningIconManager.lua` adds flood, volcano, and XP2 coastal-lowland warning icons from environmental APIs.
- Settler-lens loyalty overlay semantics were also confirmed: Firaxis shows the exact loyalty-per-turn value for a hypothetical newly founded city on that plot, not a fixed named bucket set. Any scanner grouping beyond exact value would be CAI-defined behavior.
- The world scanner now exposes the current supported modal lens through an `Active lens` category. Supported first-pass lenses are settler, appeal, continent, owner, government, tourism, and XP2 power. The settler path still presents water availability, exact loyalty-per-turn values when XP1+ is active, and disaster overlays when XP2 is active. Loyalty group ordering is CAI-defined as highest value to lowest.
- Government-lens scanner grouping is now plot-owner aware at the city level: groups are keyed by government + owning civ + owning city so mixed empires do not collapse into one giant government bucket.
- Tourism-lens scanner support now mirrors the vanilla tourism banners with two simple views: `By city` and `By strength`. Strength buckets match Firaxis' own thresholds from `TourismBannerManager.lua` (`>= 16`, `>= 8`, and low otherwise).
- XP2 power-lens scanner support now mirrors the simple vanilla overlay shapes: `Power sources`, `Power range`, `Powered city plots`, and `Unpowered city plots`. Scanner invalidation also listens for `Events.CityPowerChanged` when that event exists.
- Minimap lens investigation is now documented for follow-up scanner work: base modal lenses are religion, continent, appeal, settler, government, owner, tourism, and empire; XP1 adds loyalty; XP2 adds power. The strongest next scanner candidates from confirmed live data are appeal, continent, owner, government, power, and tourism, while empire remains lower confidence because Firaxis implements it mostly through city / district banner detail instead of a dedicated plot-bucket data source.
- City-management investigation confirmed the base ownership split: `CityPanel.lua` owns the shell and toggles, `CityPanelOverview.lua` owns the detailed subpanels, `PlotInfo.lua` owns workable-plot / purchase / swap overlays, and `WorldInput.lua` only owns `CITY_MANAGEMENT` mode routing.
- The next city-management accessibility pass should prioritize structured speech for overview subpanels and a navigable model for `PlotInfo.lua` overlays, since those are the main parity gaps left for sighted city management.
- `CITY_MANAGEMENT` interface-mode accessibility is now routed through `interfaceInfoHelpers_CAI.lua` with a dedicated helper that rebuilds `PlotInfo.lua` state from live `CityManager.GetCommandTargets(...)` results rather than scraping overlay controls.
- The world scanner now has a `City management` category for active in-world city-management mode, grouped into state subcategories such as locked, specialists, worked, swappable, and purchasable tiles.
- `WorldInput_CAI.lua` now has a generalized interface-widget activation fallback: primary and secondary widget actions raise `LuaEvents.CAIInterfaceWidgetPrimaryAction(widgetId, plotId)` and `LuaEvents.CAIInterfaceWidgetSecondaryAction(widgetId, plotId)` when a mode widget does not override them directly.
- `PlotInfo_CAI.lua` now wraps vanilla `PlotInfo` for city management: it listens for the generic interface-widget events when `widgetId == "CAIWorldInputCityManagement"`, routes primary to purchase/manage and secondary to swap based on the current cursor plot state, and mirrors the vanilla affordable-purchase hover coin animation on `LuaEvents.CAICursorMoved`.
- City-management interface speech was tightened to state-only output: it no longer repeats base plot identity from `PlotToolTip_CAI.lua`, uses `Invalid target` for non-actionable plots, says `Unworked`, vanilla worked-citizen text, short specialist counts, `Swappable`, and purchase affordability only when present. `PlotInfo_CAI.lua` now speaks purchase / worker / swap feedback by wrapping the vanilla click handlers directly instead of listening to city change events.
- City-management action feedback now uses a primed pending-action payload plus matching game events instead of click-return data: manage waits for `CityWorkerChanged` and matches only owner + city, purchase waits for `CityMadePurchase` and matches owner + city + purchased plot, and swap waits for `CityTileOwnershipChanged` and matches owner + city before speaking the pending plot's refreshed state.
- `WorldInput_CAI.lua` now follows the base / XP1 / XP2 include chain via `Civ6Common.lua` expansion helpers instead of always including base `WorldInput`. CAI also exposes XP1 `PARADROP` / `PRIORITY_TARGET` and XP2 `BUILD_IMPROVEMENT_ADJACENT` / `MOVE_JUMP` as interface widgets, interface-info targets, and valid-target scanner modes.
- Unit hotkey cleanup policy was refined: only expose unit operations and commands as bindable actions when vanilla marks them `VisibleInUI="true"` in `UnitOperations.xml` or `UnitCommands.xml`.
- Data-file naming convention was tightened: files under `src/data/` should use a `_CAI` suffix to reduce future mod filename collisions, and `.modinfo` references should use those suffixed names.
- CAI keybinding UI now logs raw action loc tags on focus for debugging bad entries, and unit upgrade should be exposed only once through `UNITOPERATION_UPGRADE`, not a duplicate `UNITCOMMAND_UPGRADE` entry.
- Dynamic vanilla unit actions should not be exposed as direct keybindings. Current removed set: build improvements, enter formation, WMD strike, and offensive-spy mission rows that vanilla folds into a single chooser action.
