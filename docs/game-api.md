# Civ VI Game API Reference (CAI Mod)

Documented APIs used by the CAI accessibility mod. Updated as new patterns are discovered.

## Reports consolidation for top-panel economy and strategic resources

- The former `UI_TopPanelYieldInfoList` shortcut (Ctrl+Y) was removed as redundant: F2 already opens the Report Screen on the Yields/Economy tab. Top Panel no longer handles that action or seeds `ExposedMembers.CAIReports.PendingFocusKey`.
- Report Screen's wrapped global `Open()` is the single accessibility choke point for F2, Reports List tab shortcuts, and vanilla callers. The hidden context can load before `ExposedMembers.CAI_UIManager` is populated, so it must refresh and assert the manager reference inside `Open()` rather than permanently capturing nil at file load and silently skipping `PushPanel()`.
- `FormatValuePerTurn()` is defined by TopPanel.lua and is not available merely because Report Screen uses the same player-yield APIs. Report Screen must own the equivalent localized signed-number formatter; calling the absent Top Panel function from the Reports context stops during Science label construction before the panel can be pushed.
- Empire Economy owns the former Top Panel hierarchy: live science and culture rates with their API tooltip breakdowns; Gold balance/net rate with separate income and expense trees; trade-route usage; Faith balance/rate and breakdown; Tourism; Gathering Storm Favor; envoy/influence progress; and nonzero nuclear-device stockpiles. Its collapsed tooltip is a compact snapshot: each yield name plus rate, Diplomatic Favor plus current balance, Envoys plus available count, and Trade Routes plus active/capacity. Detailed Favor rate and influence progress remain on their expandable child rows. Capability checks remain authoritative. Black Death France subtracts the filled Papal slot's 4-Faith upkeep and appends the official upkeep text.
- On Gathering Storm's Resources tab, each strategic resource row uses `GetResourceAmount` / `GetResourceStockpileCap` for its stockpile summary and calculates net flow as extracted accumulation plus imports plus bonus sources, minus unit and power consumption. Reserve, accumulation, and consumption are expandable children using the vanilla resource localization. The existing Report Screen `EntryList` is retained beneath `Detailed sources and commitments`, preserving named cities, deals, city-states, and miscellaneous sources without presenting a second competing resource row.
- Detailed source/commitment rows expose `EntryText`, any meaningful `ControlText`, and the signed `EntryList.Amount` together in one comma-separated StaticText label. The amount is appended on the same line rather than emitted as a separate `[NEWLINE]` segment or widget value; position remains the manager's separate field.
- The Gathering Storm flow API and Report Screen `EntryList` are complementary, not interchangeable. In the observed Coal case, flow APIs report `+19` accumulation as `+16` extracted/improvements plus `+3` City-States and `-5` consumption as `-2` Units plus `-3` Power. Report entries name five owned city sources at `+3` each, plus the same `+3` City-State, `-2` Units, and `-3` Power rows. The named city sources total only `+15`, so the flow API contributes an otherwise-unattributed `+1` extracted amount; conversely, only Report entries identify the five cities. A merged strategic hierarchy should therefore keep the authoritative flow totals, nest named Report sources beneath the matching flow category, synthesize a localized miscellaneous-source residual when the aggregate exceeds the named sum, and suppress duplicate Report rows for City-States, Units, Power, and Reserve. Preserve any Report entries that do not map to a known flow category. Base-game/luxury/bonus resource reports keep their existing entry presentation because they do not have the Gathering Storm strategic-flow hierarchy.
- Gathering Storm strategic resources remain visible when any stockpile, reserve, accumulation, or consumption exists, including a flow-only resource absent from the vanilla report table. Base-game resource rows retain the ordinary Reports source breakdown.

## Safe Mod Keys

Keys confirmed safe to bind in mods without conflicting with game defaults:

- `Keys.VK_TAB` — not bound by default in game
- `Keys.VK_HOME`, `Keys.VK_END` — not bound by default
- `Keys.VK_PRIOR` (Page Up), `Keys.VK_NEXT` (Page Down) — not bound by default
- `Keys.VK_NUMPAD1` through `Keys.VK_NUMPAD9` — hex grid navigation
- `Keys.VK_OEM_2` / `/` — CAI global cursor-to-selection action
- `Keys.VK_SPACE` — safe for checkbox toggle (game uses for other contexts)
- `Keys.VK_RETURN` — safe for button activation
- `Keys.VK_DELETE` — safe for focused, context-specific destructive row actions; bind it only where the focused item can perform that action and put irreversible actions behind confirmation where appropriate
- `Keys.VK_ESCAPE` — standard cancel/close
- `Keys.A` through `Keys.Z` — letter keys (use with IsControl for shortcuts like Ctrl+A)

## Input System

### Input (global)

- `Input.GetActionId(name)` — returns numeric action ID for a named action
- `Input.GetActionCount()` — total number of registered input actions
- `Input.GetActionName(actionId)` — returns LOC key for action name
- `Input.GetActionCategory(actionId)` — returns LOC key for action category
- `Input.GetActionDescription(actionId)` — returns LOC key for action description
- `Input.ShouldShowActionKeybinding(actionId)` — whether this action appears in keybinding UI
- `Input.GetGestureDisplayString(actionId, index)` — human-readable key string for binding (index 0 = primary, 1 = alt); nil if unbound
- `Input.BindAction(actionId, index, gesture)` — binds a gesture to an action
- `Input.ClearGesture(actionId, index)` — clears a binding
- `Input.BeginRecordingGestures(exclusive)` — starts capturing key input for binding; fires `Events.InputGestureRecorded` when captured. Works at engine level below Lua input handlers.
- `Input.StopRecordingGestures()` — stops gesture capture
- `Input.ClearRecordedGestures()` — clears captured gestures
- `Input.SetActiveContext(context)` — sets the active input context (e.g. `InputContext.Startup`)
  - `InputContext` is defined by `Assets/UI/Scripts/InputSupport.lua`; it is not an engine global guaranteed in every Lua context. CAI widgets should normally leave context ownership to the UI manager's GameView/focus lifecycle. Notification Center and Tutorial Goals must not switch to `InputContext.Shell` themselves: doing so duplicates manager behavior and previously caused a nil-index crash in contexts that did not include `InputSupport`.
  - Civ VI Lua globals are context-local. `IsTutorialRunning()` is defined by `Civ6Common.lua`, not by the engine. Vanilla `NotificationPanel.lua` does not include `Civ6Common`, so its wildcard `NotificationPanel_CAI` extension explicitly includes it before `SetTutorialAlwaysReceiveInput()` uses that function. Without the include, Notification Center crashes before being pushed.
- Vanilla English key names in `Assets/Text/en_US/UI_Options_Text.xml` use symbol-only values for 12 `LOC_OPTIONS_KEY_*` tags: underscore, semicolon, the equals key (`LOC_OPTIONS_KEY_PLUS`), comma, minus, period, slash, tilde, left bracket, backslash, right bracket, and quote. CAI overrides these through an English `UpdateText` SQL file so gesture display strings contain spoken names that screen readers announce without an all-symbols verbosity setting.
- CAI hotkey categories in `src/data/hotkey_config_CAI.xml` are intentionally split by task instead of one broad CAI bucket. Current narrow buckets are `CAI_MESSAGE_BUFFER`, `CAI_NAVIGATION_CURSOR`, `CAI_WORLD_SCANNER`, `CAI_SURVEYOR`, and `CAI_INFORMATION`; the existing `CAI_UNIT`, `CAI_GLOBAL`, `CAI_UI`, `CITY`, `CAI_ONLINE`, and `CAI_LENSES` categories remain separate.
- The `CITY` hotkey category should expose only compact city-panel actions, not overview-tab navigation. As of 2026-07-10 the kept CAI city actions are `CityToggleOverview`, `CityManageCity`, `CityPurchaseWithGold`, `CityPurchaseWithFaith`, `CityChangeProduction`, and `CityChangeCitizenYieldFocus`; the old overview-tab actions (`CityOpenBuildings`, `CityOpenLoyalty`, `CityOpenPower`, `CityOpenReligion`, `CityOpenAmenities`, `CityOpenHousing`, `CityOpenCitizens`) plus the separate `CityPurchaseTile` / `CityManageCitizens` entries are intentionally not registered in the hotkey UI.
- For CAI unit hotkeys, keep the hotkey surface aligned with vanilla `VisibleInUI`. If a `UnitOperations.xml` or `UnitCommands.xml` row is hidden there, do not register a CAI `InputActions` row for it and do not assign a `HotkeyId`, or it will appear in the keybinding UI even though vanilla intentionally hides it.
- Avoid exposing duplicate keybinding entries for the same user action. In particular, `UNITOPERATION_UPGRADE` already covers unit upgrading in the keybinding UI, so CAI should not also expose a separate `UNITCOMMAND_UPGRADE` binding row.
- UI-manager base widgets reserve `Shift+F1` for speaking only the focused widget's installed input bindings and `Shift+F2` for speaking the complete manager focus path from root to focused leaf. These are direct widget bindings, not configurable world `InputActions`; input bubbles from the focused leaf so the base action runs on the actual focused widget.
- Empire Reports keeps `UI_TopPanelOpenReports` default-bound to `F2`. Its `UI_TopPanelOpenReportsResources`, `UI_TopPanelOpenReportsCityStatus`, and `UI_TopPanelOpenReportsGossip` actions remain registered and dispatch normally but intentionally have no default gestures. The separate `UI_OpenGlobalResourcePopup` action remains default-bound to `Ctrl+Q`.
- Reports City Status yield tooltips, table columns, and detailed-list sort choices enumerate `GameInfo.Yields()` and read live values through `City:GetYield(yield.Index)`, matching the Production Panel city list. Yield sorts are descending and use localized city name plus city id as deterministic tie-breakers. City Status defaults to a sortable `DataTable`; Alt+2 selects the alternate detailed list and Alt+1 returns to the table. The profile-global internal `UI/ReportsCityStatusViewMode` setting remembers `table` or `list`, with missing and invalid values falling back to table.
- City Status table rows use local city ids as stable keys. Distance is the second column: its cell reads the live navigation-cursor-relative `CAIHexCoordUtils.directionString(...)`, its tooltip gives the numeric hex distance, and its sort key uses the four-coordinate `Map.GetPlotDistance(cursorX, cursorY, cityX, cityY)` overload. The detailed-list Distance sort uses the same value nearest-first. Row labels append Under Siege only when active and append the expansion governor's assigned/traveling state or the absence of a governor; Status and Governor are therefore not separate sortable columns. The Name cell exposes that exact row label so DataTable's exact-fragment deduplication speaks it once during vertical navigation. The table's `row_activate` listener owns city activation, so Enter or Space from every data cell selects the city without changing the cell's `TableCell` role; the transparent action owner uses the localized `TableRow` role for Input Help. Table and list view switches preserve the selected city when it still exists.
- `DataTableWidget:_SortRows()` must branch explicitly on `SortAscending`. The compact expression `ascending and comparison < 0 or comparison > 0` is invalid for this purpose because, when ascending is true, Lua allows either comparison direction to return true. That violates `table.sort`'s ordering contract and makes every ascending table sort inconsistent.
- Every sortable `DataTable` column supplies `sortAscendingDescription` and `sortDescendingDescription` localization tags describing the actual player-facing order. Active headers append semantic wording such as `A to Z`, `nearest first`, `ready first`, or `strongest first` instead of exposing the implementation terms ascending and descending. Companion sort dropdowns are generated from those same column descriptions wherever the alternate view shares both sort directions; fixed-direction dropdowns state their actual direction explicitly.
- `DataTableWidget` reserves Shift+Home and Shift+End for moving to the first and last visible column on the focused row. The same bindings apply to header and data rows; plain Home/End remain first/last data-row navigation in the current column.
- Global Resources preserves vanilla leader activation. CAI creates tree owner rows and table player columns only for the local player or met players. Tree owners attach an `activate` listener, while Enter/Space from a table data cell resolves the focused column; both paths call the included `OnLeaderClicked(playerID)`. Vanilla dispatches `LuaEvents.DiplomacyRibbon_OpenDiplomacyActionView(playerID)` and closes Global Resources.
- `GlobalResourcePopup.PopulateData()` returns one resource record with `name`, `type`, `class`, `kOwnerList`, `isPossessed`, and `total`; each owner entry supplies `playerID`, `amount`, `isMet`, and `isSelf`. The report exposes only possessed Strategic and Luxury records. CAI's table uses those records as rows, reads live known-major player columns as zero when an owner entry is absent, and keeps leader activation column-directed through the included `OnLeaderClicked(playerID)`. In Monopolies mode, the included replacement changes Luxury amounts and totals to controlled-resource data; CAI retains its Mercantilism-gated monopoly/control detail in player cells and tree owner rows.
- Also avoid exposing unit actions whose vanilla UI expands one action id into multiple concrete choices or collapses several DB rows into one chooser. Confirmed examples are `UNITOPERATION_BUILD_IMPROVEMENT`, `UNITCOMMAND_ENTER_FORMATION`, `UNITOPERATION_WMD_STRIKE`, and the offensive-spy mission operations.

### ContextPtr

- To receive input in a LUA context, set a function to be a callback handler.
The handler function should return true if the input was handled.
The handler function should return false if the input was not handled or it was handled but should be considered by other inputs.
Once input is marked as handled (true), no other controls (or contexts) within that root context will receive input. But other controls/contexts within other root contexts will receive a chance to handle the input, despite if it was marked has handled (true) in a different root context.
(NOTE: Root contexts are set via C++, chances are you are working within a single root context.)
Only one input handler callback can be set per context.
There are two types of handlers that can receive input.
Simple Handler
The simple handler will callback when input occurs passing in 3 parameters:
function InputHandler( uiMsg, wParam, lParam )
The uiMsg will be the type of input (keyboard, mouse, pointer, etc…), wParam and lParam will be values that have meaning, based on the type of input that comes in.
To set the handler call SetInputHandler() on the context, passing in the name of function to receive input.
ContextPtr:SetInputHandler( InputHandler );
Example
function InputHandler( uiMsg, wParam, lParam )
if (uiMsg==KeyEvents.KeyDown) then
if (wParam==Keys.VK_ESCAPE) then
OnBack();
return true;
end
end
if (uiMsg==MouseEvents.MouseMove) then        
InspectWhatsBelowTheCursor();
return true;
end
return false;
end
ContextPtr:SetInputHandler( InputHandler );
Extended Handler
The extended handler works almost the same as the simple handler except that it receives a single parameter which is a table of input information:
function InputHandler( inputStruct )
The inputStruct is the same one as "InputStruct" defined in ForgeUI. It allows for detailed querying of the input through various functions.
To set the input handler:
ContextPtr:SetInputHandler(
InputHandler, true );
InputStruct functions include:
Function
Returns
Description
GetFlags
number
Return the low level bit-flags the input system is using.
GetKey
number
Obtain the AppHost key code.
GetMessageType
number
The type of input message contained in this instance of the structure.
Values include:
KeyEvents.KeyDown
KeyEvents.KeyUp
MouseEvents.LButtonDown
MouseEvents.LButtonDoubleClick
MouseEvents.LButtonUp
MouseEvents.MButtonDown
MouseEvents.MButtonDoubleClick
MouseEvents.MButtonUp
MouseEvents.PointerDown
MouseEvents.PointerUp
MouseEvents.RButtonDown
MouseEvents.RButtonDoubleClick
MouseEvents.RButtonUp
GetMouseDX
number
Obtain the horizontal delta for the mouse since the last frame of input.
GetMouseDY
number
Obtain the vertical delta for the mouse since the last frame of input.
GetTouchID
number
The unique ID associate with this touch generating an event.
GetWheel
number
Get mouse wheel value
GetX
number
Horizontal coordinate for this mouse or touch event.
GetY
number
Vertical coordinate for this mouse or touch event.
IsShiftDown
bool
Is the shift key held down?
IsControlDown
bool
Is the control key held down?
IsLButtonDown
bool
Is the left mouse button (or touch equivalent) down?
IsRButtonDown
bool
Is the right mouse button down?
IsMButtonDown
bool
Is the middle mouse button (commonly the mouse wheel) down?
IsAnyButtonDown
bool
Is the left, right, or middle mouse button down?

## Locale

- `Locale.Lookup(tag, ...)` — looks up a localized string by tag, with optional format args
- `Locale.ToUpper(str)` — uppercase
- `Locale.Compare(a, b)` — returns -1, 0, or 1

## Options

### Options (global)

- `Options.GetAppOption(category, key)` — reads an app-level option
- `Options.SetAppOption(category, key, value)` — writes an app-level option
- `Options.GetUserOption(category, key)` — reads a user-level option
- `Options.SetUserOption(category, key, value)` — writes a user-level option
- `Options.SetGraphicsOption(category, key, value, flag)` — writes a graphics option
- `Options.SetAudioOption(group, key, value, flag)` — writes an audio option
- `Options.GetAvailableDisplayAdapters()` — returns table of GPU names
- `Options.GetAvailableDisplayModes()` — returns table of `{Width, Height, RefreshRate}`
- `Options.SaveOptions()` — persists all options to disk
- `Options.RevertOptions()` — reverts to last saved state
- Vanilla `Assets/UI/Options.lua` treats the five audio volume sliders (`MasterVolSlider`, `MusicVolSlider`, `SFXVolSlider`, `AmbVolSlider`, `SpeechVolSlider`) as normalized continuous controls: it seeds them with `slider:SetValue(option / 100.0)` and writes back with `Options.SetAudioOption(..., value * 100.0, 0)`. There is no Lua-side step-count setup for those sliders, so CAI mirrors should use a `0.0..1.0` range rather than integer `0..100` percent steps.
- CAI raw audio is outside the game's normal audio buses, so `CAIAudioManager` reads `Options.GetAudioOption("Sound", "Master Volume")`, normalizes the stored `0..100` value to `0..1`, and uses it as the common base gain for every mod sound. The manager detects live master-volume changes in `Update()` and reapplies the effective gain to loaded handles.

## UI Controls

### Common Control Methods

These are methods on Civ VI's native XML-backed UI controls:

- `ctrl:GetText()` / `ctrl:SetText(str)` — text content
- `ctrl:GetToolTipString()` / `ctrl:SetToolTipString(str)` — tooltip
- `ctrl:DoLeftClick()` — available from the base control class for XML-backed controls; use it directly to preserve vanilla click callbacks when mirroring activation.
- `ctrl:IsHidden()` / `ctrl:SetHide(bool)` — visibility
- `ctrl:IsDisabled()` / `ctrl:SetDisabled(bool)` — disabled state
- `ctrl:IsVisible()` — true if not hidden
- `ctrl:IsSelected()` / `ctrl:SetSelected(bool)` — checkbox/toggle state
- `ctrl:IsChecked()` / `ctrl:SetCheck(bool)` — alternate checkbox API
- `ctrl:GetValue()` / `ctrl:SetValue(float)` — slider value (0.0–1.0)
- `ctrl:GetStep()` / `ctrl:GetNumSteps()` — stepped slider position
- `ctrl:SetStepAndCall(step)` — sets step and fires callback
- `ctrl:RegisterCallback(mouseEvent, fn)` — registers click/hover handlers
- `ctrl:ClearCallback(mouseEvent)` — removes a callback
- `ctrl:CalculateSize()` — recalculates layout
- `ctrl:DestroyAllChildren()` — removes all child instances

### PullDown (ComboBox) Controls

- `ctrl:GetButton()` — returns the button sub-control (has `:GetText()`, `:SetText()`)

### EditBox Controls

- `ctrl:ClearString()` — empties the text
- `ctrl:RegisterCommitCallback(fn)` — fires on Enter
- `ctrl:RegisterStringChangedCallback(fn)` — fires on text change
- `ctrl:GetText()` — current text

### Mouse Events

- `Mouse.eLClick` — left click
- `Mouse.eMouseEnter` — hover enter

## UIManager (global)

- `UIManager:SetClipboardString(str)` — copies text to the system clipboard
- `UIManager:GetFilePath(fileName)` — resolves a VFS-loaded Lua file name to its absolute filesystem path. CAI can use this to derive the current mod root by asking for a CAI-owned Lua file and walking up from `.../UI/...`.

## InstanceManager

Used to create dynamic UI instances from XML templates:

- `InstanceManager:new(instanceName, topControlName, parentControl)` — creates a manager
- `im:GetInstance()` — allocates a new instance, returns control table
- `im:ResetInstances()` — returns all instances to pool
- `im.m_AllocatedInstances` — array of currently allocated instances

## Map & Plot

- `Map.GetPlotByIndex(plotId)` — returns a Plot object
- `plot:GetX()`, `plot:GetY()` — plot coordinates
- `plot:GetContinentType()` returns the numeric `GameInfo.Continents.Index` for the plot, so CAI can read the localized continent name directly from `GameInfo.Continents[index].Description` without the older extra type-map hop.
- `plot:GetOwner()` returns `-1` / no owner for unclaimed plots. For NavCursor-style territory change speech, use vanilla `LOC_MINIMAP_UNCLAIMED_TOOLTIP` for that case instead of a CAI-owned placeholder string.
- `Map.GetGridSize()` — returns map width and height
- `Map.GetPlot(x, y)` — returns a Plot for valid coordinates. On maps where `Map:IsWrapX()` is true, X coordinates wrap east/west, so `x = -1` resolves to the last column. Y coordinates only wrap if `Map:IsWrapY()` is true; normal Civ VI maps do not wrap north/south, so `y = -1` returns nil.
- `Map:IsWrapX()` / `Map:IsWrapY()` — return whether the active map wraps on that axis. Check these before manually wrapping absolute coordinates.
- `plot:IsRiver()` and named-river membership can be true even when the legacy edge booleans `IsNEOfRiver()`, `IsWOfRiver()`, and `IsNWOfRiver()` all report false for that same plot.
- `plot:IsFreshWater()` is the better coarse helper when CAI wants to announce settlement/fresh-water access. Use `LOC_SETTLEMENT_RECOMMENDATION_FRESH_WATER` for that spoken line rather than inferring fresh water from `IsRiver()`.
- CAI's river-edge speech model uses Civ VI's positional ownership flags:
  - this plot's `IsWOfRiver()` means the river is on this plot's `E` edge
  - this plot's `IsNWOfRiver()` means the river is on this plot's `SE` edge
  - this plot's `IsNEOfRiver()` means the river is on this plot's `SW` edge
  - this plot's `W` edge comes from the west neighbor's `IsWOfRiver()`
  - this plot's `NW` edge comes from the northwest neighbor's `IsNWOfRiver()`
  - this plot's `NE` edge comes from the northeast neighbor's `IsNEOfRiver()`
- Diagnostic logs showed `IsRiverCrossingToPlot(...)` can report river relationships even when those legacy booleans return false. Treat that as evidence that Civ VI tracks some river-topology cases outside the old edge-flag model, not as a proven replacement for the intended ownership model.
- The flow getters `GetRiverEFlowDirection()`, `GetRiverSEFlowDirection()`, and `GetRiverSWFlowDirection()` exist, but CAI's current river helper uses only edge-presence checks for direction speech.
- CAI should use the inverted positional helper above for spoken river directions rather than the older literal-edge interpretation.
- `plot:IsRiver()` is still the coarse yes/no helper and is what vanilla `PlotToolTip.lua` uses for the generic "River" line.
- In CAI plot reads, the `W` action now announces fresh water before rivers and owner so players can distinguish broad fresh-water access from actual river-edge directions.
- In Gathering Storm / Expansion 2, `PlotTooltip_Expansion2.lua` populates `data.RiverNames` from `RiverManager.GetRiverName(pPlot)`. CAI should treat that as a named-river collection shape, not as a single lookup argument, and append the computed edge-direction suffix to each named river entry individually.
- Vanilla's settler lens calls `Map.GetContinentPlotsWaterAvailability()`, which returns four explicit plot lists in this order: fresh water, coastal water, no water, and cannot-settle / blocked. `MinimapPanel.lua` colors those arrays directly for the `WaterAvailability` lens, and `ModalLensPanel.lua` / `UnitPanel.lua` supply the localized meanings.
- For Maya (`TRAIT_CIVILIZATION_MAYAB`), vanilla collapses the first three settler-lens meanings into a single "valid settling location" bucket and keeps only the blocked bucket separate.
- The shared decompiled UI still uses that same `WaterAvailability` path with expansions enabled; I did not find an XP1/XP2 branch that adds loyalty or coastal-lowland buckets to `Map.GetContinentPlotsWaterAvailability()`.
- Rise and Fall / Gathering Storm do add extra settlement information, but not by changing the four water-availability color buckets:
  - `DLC/Expansion2/UI/Additions/SettlerInfluenceIconManager.lua` listens for `Hex_Coloring_Water_Availablity`, calls `Map.GetContinentPlotsLoyalty()`, and draws numeric loyalty-pressure icons on top of the active settler lens. Tooltip loc key: `LOC_SETTLER_LOYALTY_WARNING_TOOLTIP`.
    - The settler-lens loyalty overlay is not a named category set like fresh/coastal/no-water/blocked. Firaxis displays the exact `loyaltyVal` returned by `Map.GetContinentPlotsLoyalty()` on each plot, and the tooltip says that nearby cities would apply that many loyalty-per-turn to a new city settled there.
  - `DLC/Expansion2/UI/Additions/SettlerWarningIconManager.lua` also listens for `Hex_Coloring_Water_Availablity` and overlays hazard icons for floodplains, volcano risk, and XP2 coastal lowlands. The coastal warning comes from `TerrainManager.GetCoastalLowlandType(plot)` plus `GameInfo.CoastalLowlands()[...]`, not from the water-availability array.
  - Those overlays are independent of the separate XP1 loyalty lens in `MinimapPanel_Expansion1.lua`, which uses `UILens.SetActive("Loyalty")` and `pCity:GetCulturalIdentity()` pressure waves / flag markers for existing cities.
- CAI now exposes the current supported modal lens through a single dynamic scanner category in `src/UI/InGame/WorldScanner/WorldScannerCategory_activeLens.lua`:
  - active-lens detection is registry-driven through the actual minimap layer state (`UILens.IsLayerOn(...)` for hashes such as `Hex_Coloring_Water_Availablity`, `Hex_Coloring_Appeal_Level`, `Hex_Coloring_Continent`, `Hex_Coloring_Owning_Civ`, `Hex_Coloring_Government`, and XP2 `Power_Lens`), rather than hard-wiring scanner rebuild logic to the settler water layer
  - currently supported scanner-backed lenses are `WaterAvailability`, `Appeal`, `Continent`, `OwningCiv`, `Government`, `Tourism`, and XP2 `Power`
  - the settler implementation remains the richest one so far: subcategories are `Water availability`, `Loyalty`, and `Disasters`
  - appeal uses a single subcategory grouped into breathtaking, charming, average, uninviting, and disgusting
  - continent uses a single subcategory grouped by continent name
  - owning-civ uses stance subcategories `My`, `Neutral`, and `Enemy`, then groups within those by owner
  - government also uses stance subcategories `My`, `Neutral`, and `Enemy`, then groups scanner results by government type. Each item remains a separately zoned government + owning civ + owning city territory and retains that complete label. The reliable city lookup for arbitrary owned plots is `Cities.GetPlotPurchaseCity(plot)` with `Cities.GetCityInPlot(x, y)` as a city-center fallback
  - tourism follows `TourismBannerManager.lua`: it scans only the local player's purchased city plots and keeps only plots where `player:GetCulture():GetTourismAt(plotID) > 0`
  - each vanilla tourism banner is one scanner item under the `Tourism` subcategory. Groups mirror Firaxis' visual strength thresholds: `High` at `>= 16`, `Medium` at `>= 8`, and `Low` otherwise. Items state the tourism value, strength, and international-tourist count and are not duplicated into alternate city and strength views
  - XP2 power uses four simple subcategories derived from `PowerLensManager.lua`: `Power sources`, `Power range`, `Powered city plots`, and `Unpowered city plots`. It rebuilds from local-player cities only, matching the vanilla power lens
  - loyalty groups are keyed by exact loyalty-per-turn value and sorted highest to lowest through a scanner-core subcategory comparator hook
  - disaster groups currently mirror the visual overlay types: flood risk, volcano risk, and coastal lowland 1 / 2 / 3
- Gathering Storm coastal-lowland state is exposed separately through `TerrainManager.GetCoastalLowlandType(plotIndex)` and `GameInfo.CoastalLowlands()`. That is a distinct API path from the settler water-availability lens.
- Minimap modal-lens ownership is split across files:
  - base `Assets/UI/MinimapPanel.lua` toggles `Religion`, `Continent`, `Appeal`, `WaterAvailability`, `Government`, `OwningCiv`, `Tourism`, and `EmpireDetails`
  - XP1 `DLC/Expansion1/UI/Replacements/MinimapPanel_Expansion1.lua` adds `Loyalty`
  - XP2 `DLC/Expansion2/UI/Replacements/MinimapPanel_Expansion2.lua` adds `Power`
- Scanner-friendly lens data paths confirmed during minimap investigation:
  - `Appeal` uses `Map.GetContinentPlotsAppeal()` for five explicit plot buckets: breathtaking, charming, average, uninviting, disgusting
  - `Continent` uses `Map.GetVisibleContinentPlots(continentID)` and `GameInfo.Continents[...].Description`
  - `Government` colors each city's purchased plots via `Map.GetCityPlots():GetPurchasedPlots(city)` and the owner's `player:GetCulture():GetCurrentGovernment()`
  - `OwningCiv` / political colors each city's purchased plots via `Map.GetCityPlots():GetPurchasedPlots(city)` and the owner's player color; XP1 modal-lens key adds a distinct free-city entry
  - `Loyalty` is expansion-only and city-based: `MinimapPanel_Expansion1.lua` uses `city:GetCulturalIdentity():GetConversionOutcome()` plus `GetCityIdentityPressures()` for rising / falling city markers and pressure waves, while settler-lens loyalty uses the separate plot API `Map.GetContinentPlotsLoyalty()`
  - `Power` is XP2-only and lives in `DLC/Expansion2/UI/Additions/PowerLensManager.lua`; it rebuilds from `city:GetPower()` using `GetPlotsCoveredByRegionalPower()`, `GetPlotsProvidingPower()`, `IsFullyPowered()`, and `Map.GetCityPlots():GetPurchasedPlots(city)`
  - `Tourism` uses `Assets/UI/WorldView/TourismBannerManager.lua`; per plot it reads `player:GetCulture():GetTourismAt(plotID)`, `GetTouristsAt(plotID)`, and `GetTourismTooltipAt(plotID)` and creates banners only for owned plots with tourism > 0. CAI mirrors each local-player banner once in the Active Lens scanner, groups it by the vanilla High/Medium/Low animation threshold, and uses the official tourism tooltip as the detailed on-demand explanation. Scanner and Space summaries use the compact order `tourism, strength, international tourists` without a banner header; an explicit lens-information request on a plot without a banner says so
  - `EmpireDetails` does not expose a unique plot-bucket API in `MinimapPanel`; the visible effect is largely city / district banner detail and district-banner reveal, so it is a weaker scanner target than the other modal lenses

## Events

### Engine Events (Events.*)

- `Events.InputGestureRecorded` — fired when gesture recording captures a key combo
- `Events.InputActionTriggered` — fired when a bound input action is triggered
- `Events.InterfaceModeChanged` — fired when interface mode changes (e.g. MOVE_TO)
- `Events.UserAcceptsEULA` — EULA accepted
- `Events.UserConfirmedClose` — user confirmed app exit
- `Events.UserRequestClose` — user requested app exit
- `Events.FrontEndPopup` — frontend popup requested (256 char limit)

### Lua Events (LuaEvents.*)

- `LuaEvents.CAIEndTurn` — CAI custom: triggers end turn
- `LuaEvents.CAICursorMoveTo(plotId, reason)` — CAI custom: unified cursor move request. Reason is `"step"`, `"jump"`, `"select"`, or `"snap"`. Speaks direction when distance > 1. Always fires `CAICursorMoved`.
- `LuaEvents.CAICursorMoveDirection(direction)` — CAI custom: directional cursor step; resolves the adjacent plot and calls `MoveTo` with reason `"step"`.
- `LuaEvents.CAICursorMoved(state)` — CAI custom: emitted after every cursor move. `state` is a table with `fromPlotId`, `toPlotId`, `distance`, `fromX`, `fromY`, `toX`, `toY`, `reason`. Listeners use `state.reason` to decide behavior (e.g. PlotToolTip speaks on `"step"`/`"jump"`, silent on `"select"`/`"snap"`).
- `LuaEvents.CAIWorldTrackerShowChat()` — CAI custom: asks `WorldTracker_CAI.lua` to force the vanilla in-game chat panel visible and re-realized before CAI pushes its own accessibility panel.
- `LuaEvents.MainMenu_ShowAdditionalContent` — opens mods screen
- `LuaEvents.MainMenu_UserRequestClose` — main menu exit request
- `LuaEvents.MainMenu_LaunchError` — game launch error
- `LuaEvents.MultiplayerPopup` — multiplayer popup (no char limit)
- `LuaEvents.FrontEndPopup_CloseConfirmationWithoutAction` — popup closed without action
- `LuaEvents.OnRaisePopupInGame` — raises an in-game popup dialog

## ExposedMembers

Shared global table for cross-context communication:

- `ExposedMembers.CAI_UIManager` — the UIScreenManager singleton
- `ExposedMembers.CAICursor` — the navigation cursor object
- `ExposedMembers.CAICursorOverrides` — cursor coordinate override functions
- `ExposedMembers.CAI_MainGamePanel` — the root in-world CAI game panel created by `WorldInput_CAI.lua`; in-world HUD accessibility layers can attach themselves with `AddChild(...)` instead of pushing a separate screen

## CAI Custom API

### CAI (C++ bridge)

- `CAI.output(text, interrupt)` — sends text to screen reader. Always use `Speak()` wrapper instead.
- `CAI.onCharInput` — callback property. Assign a `function(char)` to receive raw character input. Called by the engine when a character key is pressed (operates alongside the regular input system). Return true to consume.
- `CAI.enableCharInput(bool)` — enables or disables the character input callback.
- `CAI.IsGameWindowFocused()` — returns whether the Civ VI game window is currently focused. CAI raw-audio code can combine this with `Options.GetAudioOption("Sound", "Mute Focus")` to suppress custom sounds while the game is unfocused or minimized.

Configuration persistence note: `CAI.GetConfigValue(section, key, defaultValue)` and `CAI.SetConfigValue(section, key, value)` treat the section and key together as the persisted configuration identity. `CAISettings` passes each `CAI_Settings.Section` as the section and `SettingId` as the key. Moving a row to another section therefore leaves its old value under the former section and makes the new section read the row's default unless an explicit migration is implemented.

The CAI UI tutorial manager stores profile-global completion directly under configuration section `Tutorials` with keys named `Seen_<stable item id>`. Each key stores the current `Tutorials/ResetGeneration`; incrementing that generation makes every older completion record logically unseen without needing all defining screens to be loaded. These completion keys are intentionally not `CAI_Settings` metadata rows and therefore do not appear individually in Mod Settings. The UI settings section contains the default-enabled `ShowTutorials` checkbox and the `ResetModTutorials` action button. Reset emits `LuaEvents.CAISettingsChanged("ResetModTutorials", "reset")`; the tutorial manager increments the generation, speaks localized reset feedback, and leaves Settings open.

Screen tutorials are routed by `CAIUITutorialCatalog` after a matching CAI
stack root is pushed and focused. Contextual subviews remain explicit checks:
Production fires `ProductionQueueOpened` only while its Queue tab is active.
World Congress stage 1 phase 1 fires `WorldCongressResolutionVote`, stage 2
phase 1 fires `WorldCongressProposalVote`, and the stage 4 Available Proposals
page fires `WorldCongressSpecialSessionProposal`. These correspond respectively
to choosing outcome/target/votes for resolutions, voting support or opposition
on an active emergency proposal, and selecting an emergency to propose for a
future special session.

Reports tutorial routing uses the stable `CAIReports_Panel` root ID explicitly.
`ReportScreen_CAI.lua` passes that ID directly to `CreateWidget()`, and the
catalog declares `Roots = { "CAIReports_Panel" }`. The stable
`CAIReports_Tabs` ID is likewise passed directly rather than supplied as a
property override. The `CAIRPT_` prefix remains reserved for generated
transient child-widget IDs and must not be used to match the Reports stack
root. This avoids the former mismatch where runtime logging showed the stable
panel ID being pushed while the catalog expected the transient prefix.

The former Navigation settings section is split into `Cursor`, `WorldScanner`, and `Events`. Cursor owns cursor audio, coordinates, and zone announcements; WorldScanner owns every scanner preference including `ScannerBeaconEnabled`; Events owns visibility-change and unit-movement announcements.

`cursorAudio_CAI.lua` listens for `LuaEvents.CAISettingsChanged`. When `AudioTagVolume_CURSOR` changes, cursor audio reapplies settings to the loaded handles, stops its active tags, and plays `CURSOR_GRASS` as a clean volume preview. The preview uses normal tag playback, so the cursor-audio enabled setting and window-focus mute policy still apply.

### Speak(text, interrupt)

Wrapper for `CAI.output`. Use this for all TTS output.

- `ProcessText(text, tidy)` (in `src/UI/shared/textProcessing.lua`, formerly `ProcessIcons` in `iconsProcessing.lua`) is the single text-filtering entry point for speech. It resolves/strips bracket tokens (`[COLOR]`, `[NEWLINE]`, `[ICON_...]`), transliterates common accented Latin characters and ligatures to ASCII when `Locale.GetCurrentLanguage().Type` identifies an English display language, and finally applies an ASCII-safe whitespace/comma tidy-up. `Speak()` calls it by default; the edit-box `SetText` path calls it with `tidy=false` to preserve multi-line layout. Individual screens must not strip tags or normalize whitespace themselves. All whitespace handling uses explicit ASCII byte sets (`[ \t\r\n]`), never `%s`/`%S`, which are locale-sensitive and corrupt UTF-8 byte `0xA0` under Simplified Chinese.

## Chat panel findings

- `Assets/UI/Popups/ChatPanel.lua` owns four main behaviors: send/edit target handling, incoming chat history, clickable map-pin chat entries, and the player-action / kick-vote area.
- The in-game chat parser path matches staging-room behavior through shared helpers: `ParseInputChatString(...)` handles `/g`, `/t`, `/w`, and `/help`, and `PlayerTargetToChatTarget(...)` converts the player-target model into the `Network.SendChat(...)` tuple.
- `/help` uses two different vanilla outputs:
  - `ChatPrintHelpHint(...)` seeds the one-line hint near the input.
  - `ChatPrintHelp(...)` prints the full help text into chat history.
- Clickable map pins are transmitted as `[pin:<playerID>,<pinID>]`. `OnChat(...)` special-cases that token and resolves the live pin through `GetMapPinConfig(...)`.
- `BuildPlayerList()` is the durable rebuild choke point for the player-actions area. Vanilla chat already calls it for kick-vote changes, player connect/disconnect, host migration, player-info changes, and game-config changes.
- `OnMapPinPopup_RequestChatPlayerTarget()` only needs the current target state; the base screen answers by calling `PlayerTargetChanged(m_playerTarget)`.
- A `ChatPanel` include-after wrapper cannot directly reuse base locals like `m_playerTarget`, so `ChatPanel_CAI.lua` needs its own mirrored target/history state and should reuse the global helper functions instead of trying to mutate the base locals.

### World scanner module layout

- CAI's world scanner now lives under `src/UI/InGame/WorldScanner/`.
- Entry point: `WorldScanner_CAI.lua`
- Shared modules:
  - `WorldScannerCategoryUtils.lua`
  - `WorldScannerCore.lua`
- Category modules:
  - `WorldScannerCategory_cities.lua`
  - `WorldScannerCategory_barbarianCamps.lua`
  - `WorldScannerCategory_improvements.lua`
  - `WorldScannerCategory_terrain.lua`
  - `WorldScannerCategory_waterAvailability.lua`
  - `WorldScannerCategory_resources.lua`
  - `WorldScannerCategory_units.lua`
  - `WorldScannerCategory_validTargets.lua`
  - `WorldScannerCategory_specialMapObjects.lua`
- `WorldScanner_CAI.lua` includes the shared modules directly and then uses `include("WorldScannerCategory_", true)` so all loaded category files with that prefix are pulled in automatically, matching the vanilla late-include pattern.
- `WorldInput_CAI.lua` includes the scanner entry point and dispatches scanner input actions through `Events.InputActionTriggered`.
- `WorldInput_CAI.lua` also drives `RevealAnnouncements_CAI.UpdateVisibility()` through a `ContextPtr:SetUpdate(...)` timer hook so visibility event speech can be throttled without speaking directly inside engine callbacks.
- `RevealAnnouncements_CAI.lua` subscribes to `PlotVisibilityChanged`, `UnitVisibilityChanged`, `ImprovementVisibilityChanged`, `ResourceVisibilityChanged`, `CityVisibilityChanged`, `DistrictVisibilityChanged`, and `LocalPlayerTurnBegin`.
- All visibility callbacks refresh the same 0.5 second quiet-period timer. Plot callbacks still own the reveal/revisit plot-state buffer, while the other visibility callbacks exist so late deferred object events can extend the burst instead of letting the queue flush early.
- Current implementation is a singleton, not a per-player store: `m_firstRevealPlots`, `m_nowVisiblePlots`, `m_knownRevealedPlots`, `m_previousVisibleUnits`, `m_specialImprovementKinds`, and the debounce timer are all shared globals. For hotseat, those buffers must be keyed by the active local player and flush only the active player's queue; otherwise one human can inherit another human's pending reveal speech or visibility snapshot.
- Reveal announcements now mirror the older Civ V-style split:
  - reveal tracks first-reveal plots separately from revisit-visible plots
  - first reveal is detected from a Lua-side `PlayerVisibility:IsRevealed(plot)` snapshot rather than a custom C++ hook
  - the reveal line speaks `<N> tiles revealed` when unexplored plots were involved, otherwise `Revealed`

## Screen Investigation Notes

### Extended Policy Cards mod (Aristos, UUID `382a187f-c8ba-4094-a6a7-0d5315661f33`)

- Ships only a `GovernmentScreen.lua`/`.xml` replacement and depends on Better
  Report Screen. Its sole functional change versus vanilla: when BRS is active it
  computes each policy card's effect via `ExposedMembers.RMA.CalculateModifierEffect("Policy", policyType, ePlayerID, nil, nil)` (first return value is the readable Impact string), appends it to the card's `Draggable` tooltip, and shows it in a new `EffectContainer`/`Effect` label. It changes no data model.
- `RMA.CalculateModifierEffect` is defined by BRS (`RealModifierAnalysis.lua`), not
  by EPC; EPC only ensures `ExposedMembers.RMA` exists and consumes it. It returns
  `Impact, Yields, ImpactToolTip, UnknownEffect`; CAI uses only the first (Impact
  string) and passes `Game.GetLocalPlayer()` for the player id.
- Because CAI's `<ReplaceUIScript LuaContext="GovernmentScreen">` outranks EPC, EPC's
  script never runs when both are active. CAI re-surfaces the effect itself: detect
  with `IsExtendedPolicyCardsActive()` (caiUtils), read RMA in `GetPolicyEffect()`,
  and prepend it to `GetPolicyTooltip()` in `GovernmentScreen_CAI.lua`. When active,
  the policy picker/viewer switches to the table+tree panel in
  `GovernmentScreen_ExtendedPolicyCards_CAI.lua` (pulled by runtime `include()`,
  registered in both the top-level `<File>` list and `<ImportFiles>`).

### Great People Popup

- CAI formats the read-only Great Person biography edit box with the shared
  sentence-based, 75-character-target line splitter. Existing biography
  paragraph boundaries remain boundaries, and long sentences remain intact.

- Base popup files are `decompiled/Assets/UI/Popups/GreatPeoplePopup.lua` and `.xml`.
- The popup is opened from the Launch Bar via `LuaEvents.LaunchBar_OpenGreatPeoplePopup`, from the notification center via `LuaEvents.NotificationPanel_OpenGreatPeoplePopup`, and from the `ToggleGreatPeople` input action handled in `LaunchBar.lua`.
- Base game tabs are `Great People` and `Previously Recruited`. The popup uses `CreateTabs(...)`, but its own input handler only consumes `Escape`; all per-item interaction in the Lua file is wired through mouse callbacks (`Mouse.eLClick`) on the tab buttons and row controls.
- When opened from a `CLAIM_GREAT_PERSON` notification, the popup explicitly selects the `Great People` tab.
- The `Great People` tab is a horizontally scrolling strip of one card per currently available Great Person candidate. If any candidate is currently recruitable, the popup auto-scrolls to the first recruitable card on open or refresh.
- Each Great Person card contains:
  - class name, individual name, era, portrait, passive/active effect list
  - a `Biography` button that swaps the card into a biography view populated from Civilopedia history loc keys
  - a `Recruit progress` section with one collapsed local-player row plus expandable rows for all alive major players
  - per-player progress rows formatted as `current points / recruit cost`, with a progress bar and a tooltip containing that player's per-turn rate for the class
  - contextual action buttons: `Recruit`, `Pass`, `Gold patronage`, `Faith patronage`
  - a red `Cannot Recruit` label when `Game.GetGreatPeople():GetEarnConditionsText(...)` returns a restriction string
- Recruit / pass / patronage actions call `UI.RequestPlayerOperation(...)` and immediately close the popup.
- CAI Great Person tree rows bind Enter to the live Recruit action and Delete to the live Pass action. When Recruit is unavailable, Enter speaks the local player's localized progress row instead. The shortcut availability checks mirror the contextual buttons, including capability and read-only state.
- CAI's Great People tab defaults to a sortable comparison `DataTable`; Alt+2 selects the original detailed tree and Alt+1 returns to the table. The profile-global internal `UI/GreatPeopleViewMode` setting remembers `table` or `tree`, with missing and invalid values falling back to table. Civilization/player ids are stable row keys, while one dynamic column per current timeline entry uses its Great Person class id as stable identity so focus survives replacement of one individual by the next person in that class.
- The first table column identifies each civilization, keeps the local civilization first in natural order, and preserves vanilla's anonymous `Unmet Player` labels. Every Great Person column header contains that person's live name, class, era, passive and active effects, action charges and usage requirements, plus any local earn restriction; biography remains exclusively in the existing read-only edit box. Data cells contain that civilization's `PointsTotal / RecruitCost` and `PointsPerTurn`, and each Great Person header sorts the civilization rows by raw points for that class.
- Great People sort speech uses `A to Z` / `Z to A` for Civilization and `lowest progress first` / `highest progress first` for each class column. The alternate tree dropdown uses the shorter class name with the same direction description, while the table header retains the detailed person header.
- Great People table actions are column-directed rather than row-directed. The table does not register `row_activate`: Enter, Space, and Delete bubble from a data cell to table-level bindings, which resolve `GetFocusedColumn()` and apply Recruit/progress speech or Pass to that column's person. Sortable headers consume Enter/Space before they can bubble, the Civilization column has no person action, and `cell_focus_enter` updates the contextual Recruit, Pass, patronage, and biography controls from the focused Great Person column regardless of the civilization row.
- The `Previously Recruited` tab switches to a vertical history table showing earn date, Great Person, claimant civ/leader, and passive/active ability summaries.
- The popup listens for `Events.GreatPeoplePointsChanged` and refreshes live while open, and closes automatically on hotseat turn end.
- Expansion behavior is additive through the wildcard include at the bottom of `GreatPeoplePopup.lua`:
  - `Expansion1` overrides the gold / faith patronage tooltips when the player is not allowed to patronize with that yield (`IsNoPatronageWith(...)`).
  - `Expansion2` overrides `IsReadOnly()` so the popup becomes read-only during an active World Congress session.
  - `Babylon Heroes` adds a third `Heroes` tab instead of replacing the existing tabs. The screen title/tooltip become `Heroes & Great People`, and the extra tab is driven by `GreatPeopleHeroPanel.lua/.xml`.
- The Babylon `Heroes` tab reuses the same horizontal card metaphor but swaps in hero-specific controls:
  - hero portrait, stats, abilities, commands, status
  - `Look At` button for the current hero or origin city
  - `Civilopedia` button
  - `Faith Recall` button when the claimed local hero is dead and recall is allowed
  - hidden speaks as its own `Hidden: ...` line
  - gone speaks as its own `Gone: ...` line
- CAI's Heroes tab defaults to a sortable `DataTable` and remembers `table` or `list` in the profile-global internal `UI/GreatPeopleHeroesViewMode` setting. Hero-class index is the stable row/focus key. Columns are Name, Status, every field returned by `GetHeroUnitStats()` (Lifespan, BaseMoves, Combat, RangedCombat, and Range), Charges, Passive abilities, and Commands. Numeric columns retain resolved zeroes and sort nil-last; the final two cells contain every localized name and description and sort by entry count. Only discovered heroes are included. Status sorting uses five explicit numeric bands: discovered but unclaimed; recruited by the local player and alive; recruited by the local player and deceased; recruited by another player and alive; and recruited by another player and deceased. Ownership is present in the visible status as `you`, a known civilization, or the anonymous unknown-player fallback, so the sort introduces no hidden distinction. The reverse direction reverses all five bands.
- The alternate Heroes list retains the composite hero label and complete stats/abilities/commands tooltip. It has one list-only sort dropdown generated from the table column metadata and no filter. Table headers and the dropdown share the selected sort. Alt+1 selects the table and Alt+2 selects the list. Enter/Space preserves the vanilla Look At hero/origin-city action, Shift+Enter opens the Hero unit Civilopedia entry, and the existing contextual Recall with Faith button remains after either view.
- `GreatPeopleHeroPanel.lua` runs in the child context loaded by `ContextPtr:LoadNewContext("GreatPeopleHeroPanel", ...)`, so its global `RecallHero(...)` is not callable from a `GreatPeoplePopup_*` wildcard file. A parent-context recall action must reproduce vanilla's request: resolve the hero's current origin city, close through `LuaEvents.GreatPeopleHeroPanel_Close`, look at that city, and send `CityManager.RequestCommand(city, CityCommandTypes.PURCHASE, parameters)` with the hero unit hash, `STANDARD_MILITARY_FORMATION`, and the Faith yield index.

## World Congress popup

- Gathering Storm's World Congress UI is split across three standalone popup contexts under `decompiled/DLC/Expansion2/UI/Additions/`:
  - `WorldCongressIntro.lua/.xml`: a full-screen intro banner with title, two centered body paragraphs, and one `Continue` button. It opens on `Events.WorldCongressStage1/Stage2`, and `Escape`, `Enter`, and the `EndTurn` action all dismiss it into the main congress popup.
  - `WorldCongressPopup.lua/.xml`: the main congress screen. It is a full-screen framed overlay with a fixed left member ribbon (`CongressMembers`), a centered title + Favor total, a large central content frame, bottom navigation buttons, a top launch-bar strip with `World Congress` and `Diplomacy` buttons, and a close button in the top-right corner.
  - `WorldCongressBetweenTurns.lua/.xml`: the between-turns waiting banner shown after the player submits votes in single-player. It shows a vertical list of leader portraits with `Waiting` / `Submitted` status rows and a centered status line until all players finish.
  - Between-turn roster accessibility must not treat the local `WorldCongressStage1/2` callback as proof that the visible rows are gone. Vanilla clears its private stage/player lookup tables on that callback, but the already-created row controls and centered status can remain visible. CAI retains explicit `{ playerID, row }` records for the lifetime of the visible popup, replacing them on `OnShow` and clearing them on actual hide. Player identity comes from `LeaderIcon:GetToolTipString(playerID)`, preserving vanilla's met/unmet and multiplayer naming rules; the row's live `Label` supplies its changing `Waiting` / `Submitted` text.
- Main popup stages and phases:
  - stage 1 = regular session voting
  - stage 2 = special session / emergency voting
  - stage 4 = out-of-session review
  - while in session, the popup uses two phases: phase 1 is vote entry, phase 2 is a summary/confirmation screen before submission
- Sighted stage-1 / stage-2 layout:
  - left column: one portrait tile per alive major civ, wrapping vertically, with Favor and sometimes grievance values beneath the portrait
  - main body: a scrollable stack of framed cards
  - bottom buttons: `Prev`, `Next`, `Pass`, `Submit`, or `Return` depending on stage/tab
  - the active screen title also shows the local player's remaining spendable Diplomatic Favor on the right
- Resolution cards (`ResolutionItem`) are visually rich and stateful:
  - title + icon + description
  - two large A/B outcome slots
  - each outcome has a separate up/down vote widget with live vote count and incremental Favor cost
  - a target selector under the chosen outcome, using either a normal pulldown or a player pulldown with civ + leader icons
  - favored/disfavored player badges showing how many leaders prefer each side
  - a `MoreInfo` button whose tooltip summarizes how this same resolution went the previous time it appeared
  - visual selection state is shown by swapping frame textures and line colors once the player has both chosen an outcome and selected a target
- Discussion / emergency proposal cards (`ProposalItem`, `EmergencyProposalItem`) show:
  - title, description, target leader or proposal-type icon
  - a single vote widget for up/down support during an active session
  - expandable emergency details outside a session
  - in the `Available Proposals` review tab, a checkbox-style selector is used instead of live voting so the player chooses which emergency proposals to submit for a future special session
- Review mode (stage 4) has three top tabs:
  - `Last Session Results`: shows passed/failed resolutions and discussions, with expandable per-player vote breakdowns
  - `Active Effects`: shows currently active resolutions and how many turns remain
  - `Available Proposals`: shows emergencies/special-session proposals the player can currently submit
- Input and launch behavior:
  - the HUD congress button (`CongressButton.lua`) either resumes the live congress session or opens the results/review popup when congress is not in session
  - the main popup's `DiploButton` opens diplomacy directly from the congress overlay
  - clicking leader portraits in the left ribbon opens diplomacy for that leader when met
  - `Enter` advances from phase 1 to phase 2, or submits on the confirmation screen; in review mode it activates the visible submit/return path
  - `Escape` closes the popup; the close button mirrors that behavior
- Unit reveal and hidden speech is snapshot-driven:
  - visibility events only refresh the shared timer
  - flush rebuilds the currently visible foreign-unit set from live unit state
  - reveal units are `current - previous`
  - hidden units are `previous - current`
  - destroyed or captured units are dropped from the hidden line by checking whether they still exist under the previous owner
  - this avoids noisy cross-map `UnitVisibilityChanged` callbacks from speaking units the player never locally saw
- First-reveal payload mirrors the older Civ V reveal model while using Civ VI's visibility APIs:
  - units can speak on first reveal or revisit, but only from the visible-unit snapshot diff
  - cities and resources speak only for first-reveal plots
  - foreign districts and foreign ordinary improvements speak only for first-reveal plots
  - city-center districts are skipped because they are redundant with city announcements
- Reveal-payload skip rules mirror the older mod logic:
  - natural wonders are excluded from the reveal payload
  - barbarian outposts and tribal villages are excluded from the reveal payload
  - own units, own cities, own districts, own improvements, teammates, and unmet foreign players are filtered out
  - foreign districts use live `plot:GetDistrictType()` / `GameInfo.Districts[...]` and skip `DISTRICT_CITY_CENTER` plus internal-only districts when detectable
  - foreign improvements use live `plot:GetImprovementType()` / `GameInfo.Improvements[...]`; ownership comes from `plot:GetImprovementOwner()`
- Gone speech is snapshot-driven for Civ VI's special removable improvements:
  - barbarian outposts are still the `IMPROVEMENT_BARBARIAN_CAMP` improvement with `BarbarianCamp="true"`
  - tribal villages are still the `IMPROVEMENT_GOODY_HUT` improvement with `Goody="true"`
  - both use `RemoveOnEntry="true"` in `decompiled/Assets/Gameplay/Data/Improvements.xml`
  - CAI keeps a Lua-side last-known per-plot snapshot for visible plots and, on revisit, announces the prior special improvement as gone if that plot no longer has the same special improvement
  - unlike the older Civ V-style file, this Civ VI version still cannot bootstrap from a revealed-improvement memory API because I did not find a Civ VI Lua equivalent to `GetRevealedImprovementType(...)`
- Flush grammar is line-oriented rather than queue-category-oriented:
  - reveal speaks either `<N> tiles revealed: ...`, `<N> tiles revealed`, or `Revealed: ...`
  - hidden speaks `Hidden: ...`
  - gone speaks `Gone: ...`
  - repeated labels aggregate to `count + label`
  - reveal payload section order is `Enemy`, `Units`, `Cities`, `Resources`, `Districts`, `Improvements`
- Scanner snapshots are disposable views of authoritative live state; they do not depend on dirty-event coverage. Category cycling rebuilds the complete snapshot, while group and item cycling rebuild only the current category through its live `Scan` or `PlotExtract` source and restores category/subcategory/group/item identity before stepping. A plot-backed category therefore makes one map pass instead of causing `RebuildCategory(...)` to rebuild every plot-backed category.
- Freshly emitted entries are not immediately revalidated: extraction just read their authoritative state, so repeating every `Validate` callback during construction duplicates expensive plot/unit lookups. The current leaf is validated immediately before speech, jump, or direction use; a failed validation prunes that stable item ID from its indexed memberships in `All` and the named subcategory, removes empty groups/subcategories, clamps navigation, and continues until it lands on a valid leaf. Additions and ordinary removals appear naturally through the next authoritative replacement snapshot; pruning covers the post-scan race.
- Category definitions can opt into dynamic one-shot behavior with `BuildOncePerDynamicState = true`. CAI currently uses that for `validTargets` and `waterAvailability`.
- Scanner invalidation is category-scoped where possible: `Events.InterfaceModeChanged` and local unit selection changes rebuild only `validTargets`; the dynamic active-lens category now rebuilds on generic lens toggles plus supporting world events such as plot visibility, city visibility, tile ownership, government, diplomacy, local-player change, city occupation, and XP2 `Events.CityPowerChanged`; `Events.LocalPlayerTurnBegin` clears all built category contents.
- Category definitions can expose a cheap `CanScan(context)` predicate to avoid expensive API calls when a category cannot currently exist, such as `validTargets` outside supported interface modes or `waterAvailability` while the water lens is off.
- Scanner sort distance uses a cached origin captured from the live CAI cursor when the player changes category or subcategory. Item/group navigation and scanner-driven cursor jumps preserve that origin, so inspecting a target cannot reorder the list underneath the player. Spoken direction/distance continues to resolve directly from the current live cursor and is independent of the cached sort distance.
- Scanner distance sorting uses the current CAI cursor plot and `Map.GetPlotDistance(...)`.
- Scanner jump and return use `LuaEvents.CAICursorMoveTo(plotIndex, "jump")`.
- Scanner item focus now has a distinct cursor-follow path from jump: when `ScannerAutoMoveCursor` is enabled, scanner selection moves the CAI cursor with `LuaEvents.CAICursorMoveTo(plotIndex, "snap")` after the item/category announcement, so the spoken direction still reflects the pre-move cursor position while the cursor then follows the selected target for subsequent scans.
- Scanner coordinate speech is setting-driven and mirrors cursor-coordinate behavior: `ScannerCoordinates` uses the same `disabled` / `append` / `prepend` shape, and scanner item announcements build the coordinate text from `CAIHexCoordUtils.coordinateString(x, y)`.
- Full-map scanner categories should iterate plots with `for plotIndex = 0, Map.GetPlotCount() - 1 do` and `Map.GetPlotByIndex(plotIndex)`. Do not use `Map.GetNumPlots()`.
- `WorldScannerCategory_validTargets.lua` appears only in active targeting modes. It gets target items from the neutral `CAIInterfaceTargets` helper in `interfaceTargetHelpers_CAI.lua`, so scanner and Space interface info use the same live target resolution without the scanner depending on `interfaceInfoHelpers_CAI.lua`. It does not attach per-item validation callbacks because the category is rebuilt when target mode or selected unit changes, and re-calling target APIs during scanner core validation is too expensive.
- Auto-focus for dynamic scanner categories is setting-driven. `validTargets`, `activeLens`, `cityManagement`, and `recommendations` declare `AutoFocus = true`, but `WorldScanner_CAI.lua` gates each through its corresponding disabled-by-default navigation setting both when ordering category slots during rebuild and when deciding whether a category-scoped rebuild should steal scanner focus. Interface-mode changes rebuild `cityManagement`; local unit-selection changes rebuild `recommendations` after `CAIRecommendationLogic` refreshes its cache.
- `CAIInterfaceTargets` is now a slim target enumeration/cache helper. It owns target discovery, mode support checks, cache signatures, cached scalar item data, and plot lookup by plot id. It should not own standalone plot-label heuristics beyond the formation-unit special case.
- `CAIInterfaceTargets` caches only scalar target item data: plot ids, unit owner/id, labels, and a plot-id lookup table. Do not cache live `Plot`, `Unit`, `City`, or `District` objects. The cache is cleared on interface-mode and selected-local-unit changes so target APIs such as `UnitManager.GetOperationTargets(... REBASE ...)` are not called on every cursor move.
- Valid Targets plot modes are computed from vanilla `UnitManager.GetOperationTargets(...)`, `UnitManager.GetCommandTargets(...)`, or `CityManager.GetCommandTargets(...)` instead of vanilla `g_targetPlots`. Normal ranged/city/district/air attacks filter for `MODIFIER_IS_TARGET`; WMD, ICBM, coastal raid, deploy, rebase, teleport-to-city, airlift, soothsayer sacrifice, hero target commands, and naval gold raid use the eligible plot list vanilla exposes.
- `FORM_CORPS` and `FORM_ARMY` are unit-target modes, not plot-target modes. Vanilla reads `UnitCommandResults.UNITS` from `UnitManager.GetCommandTargets(selectedUnit, UnitCommandTypes.FORM_CORPS/FORM_ARMY)`, highlights each target unit plot, draws form-corps wave overlays from target unit to selected unit, and activation chooses the valid unit on the cursor plot through the vanilla `FormCorps()` / `FormArmy()` handlers.
- Valid Targets plot labels should go through `ExposedMembers.CAIInfo:RequestPlotInfo(...)` with mode-appropriate requested keys, so plot-tooltip helpers remain the single source of spoken target details. WMD and ICBM targets should request full plot info by passing no keys. Formation scanner items still use the target unit display name and stable owner/id item ids.

### Surveyor world input

- `Surveyor_CAI.lua` is an in-world hotkey module for answering "what is within N tiles of the CAI cursor."
- It is included by `WorldInput_CAI.lua`, uses `CAIHexCoordUtils.plotsInRange(...)`, and keeps its radius as private module-local state clamped from 1 to 5.
- Surveyor reads live game state on each action rather than caching scope results.
- Current default bindings:
  - `Shift+W` grows Surveyor radius.
  - `Shift+X` shrinks Surveyor radius.
  - `Shift+Q` reads summed yields.
  - `Shift+A` reads visible resources.
  - `Shift+Z` reads terrain, features, and elevation.
  - `Shift+E` reads friendly units.
  - `Shift+D` reads visible enemy units.
  - `Shift+C` reads known cities and barbarian camps.
  - `Ctrl+Shift+A` / `Ctrl+Shift+Numpad 4` counts improvements.
  - `Ctrl+Shift+D` / `Ctrl+Shift+Numpad 6` reads visible neutral units.
  - `Ctrl+Shift+Z` / `Ctrl+Shift+Numpad 1` counts tile ownership.
  - `Ctrl+Shift+Q` / `Ctrl+Shift+Numpad 7` counts districts.
  - `Ctrl+Shift+X` / `Ctrl+Shift+Numpad 2` counts revealed land tiles by appeal.
- `PlotReadDistrictBuildings` was removed after its district, building, and great-work bucket was merged into `PlotReadGeography` on `B`. `UnitViewAbilities` remains `Alt+/`, and `WorldTrackerOpenCivicsChooser` remains `Ctrl+C`.
- Surveyor resources should use `plot:GetResourceType()` and `localPlayer:GetResources():IsResourceVisible(resource.Hash)` so invisible strategic resources stay hidden, and count one per plot (like vanilla `ReportScreen.GetCityResourceData`). Do NOT use `plot:GetResourceCount()` for the survey tally: on a revealed-but-fogged plot (which shared team vision brings into range) it returns -1, marshalled to ~65535, producing bogus counts.
- Surveyor appeal follows the vanilla plot-tooltip classification through `plot:GetAppeal()` and the first matching `GameInfo.AppealHousingChanges()` row. It counts non-water revealed plots, sorts appeal bands by descending tile count with localized name as the tie-breaker, omits empty bands, and does not expose appeal when `CAPABILITY_LENS_APPEAL` is unavailable.
- Surveyor units should scan `Units.GetUnitsInPlotLayerID(x, y, MapLayers.ANY)` on revealed plots; enemy-unit speech should additionally require actual visibility through `PlayersVisibility[observer]:IsUnitVisible(unit)`.
- The 2026-07-15 Civ V parity pass added four Civ VI results:
  - Improvements counts revealed improvements by localized name, excluding barbarian camps and tribal villages. It does not count routes, which Civ VI models separately.
  - Neutral units lists actually visible units belonging to met, non-barbarian players who are neither friendly/team units nor at war with the local player.
  - Tile ownership counts every revealed plot under `Yours`, vanilla `Unclaimed`, a known owner's civilization adjective, or localized `Unknown owner` without leaking an unmet identity.
  - Districts counts revealed, non-internal districts by localized name and excludes city centers, matching the World Scanner district definition.
- Civ V Surveyor terrain delegates each plot to `PlotSections.terrainShape.Read(...)`. Hills are additive: an ordinary grass hill contributes `Hills` plus the underlying `Grassland` terrain, unless a feature such as jungle, marsh, oasis, flood plains, or ice suppresses the base terrain. Mountains dominate and contribute only `Mountain`; a natural wonder dominates even the mountain and contributes only its wonder name. Civ V also adds a separate count of river-adjacent tiles.
- Civ VI Surveyor now normalizes elevation-qualified terrain records before aggregation. `GameInfo.TerrainClass_Terrains()` maps an official or modded hill terrain to its class's flat terrain record, so a grass hill contributes `Grassland` plus `Hills` rather than `Grassland (Hills)` plus `Hills`. Ordinary mountains follow Civ V's dominant-shape rule and contribute only `Mountains`, without their elevation-qualified terrain or regular feature-independent base. River counting remains intentionally omitted from the Civ VI terrain result.
- Civ VI feature suppression is derived from `GameInfo.Feature_ValidTerrains()` instead of hard-coding Civ V feature types. Each valid terrain is split into its base class from `GameInfo.TerrainClass_Terrains()` and elevation (`flat`, `hills`, or `mountain`). If every valid terrain shares one base class, the feature uniquely implies that base and Surveyor omits the base-terrain bucket. If every valid terrain shares the same non-flat elevation, the feature uniquely implies that elevation and suppresses the generic elevation bucket. Natural wonders are an explicit dominant case and emit only the wonder name. With official data this suppresses redundant terrain for oasis, marsh, each typed floodplain, and reef; `Volcano` suppresses the generic mountain. Forest, jungle, volcanic soil, and geothermal fissure retain terrain because they span several base classes, while forest/jungle also retain `Hills` where present because they span flat and hill terrain. Ice retains Coast/Ocean because it spans both and that distinction is actionable in Civ VI. A feature with no valid-terrain rows suppresses nothing, providing a safe fallback for custom placement and mods.
- Fresh-water access is a cross-cutting Surveyor terrain bucket. Every revealed plot for which `plot:IsFreshWater()` is true increments the localized `LOC_SETTLEMENT_RECOMMENDATION_FRESH_WATER` bucket, even when a natural wonder or feature suppresses the plot's ordinary terrain-shape tokens. This reuses the same API and label as the navigation cursor's `W` information bucket and does not infer fresh water from river presence.
- The remaining Civ V Surveyor behaviors already have Civ VI equivalents: radius 1-5, yields, visible resources, terrain/features/elevation, friendly units, visible enemy units, cities/barbarian camps, unexplored-tile count, live reads, count-first bucket sorting, and distance/direction instance sorting.
- Civ VI intentionally improves on the Civ V module in several areas: radius is isolated per local player, actions are remappable, output uses Civ VI localization, and unit/city labels use shared owner-aware names (including unit formation suffixes).

### World scanner reveal and object rules

- Terrain scanner emits separate entries for base terrain, feature, and elevation on an ordinary revealed plot. Mountains are the dominant exception: every revealed `plot:IsMountain()` result emits one generic localized Mountains item under Elevation and returns before base-terrain or feature extraction, so the implicit All view and Elevation view each contain one terrain-independent Mountains group with no duplicate mountain plots. Hidden plots are scanned without inspecting their concealed terrain data and placed in a single Unexplored subcategory and group.
- Resources should use `plot:GetResourceType()` together with the local player's `GetResources():IsResourceVisible(resource.Hash)` check so invisible strategic resources stay hidden.
- Improvements use the same live plot getters as vanilla `PlotToolTip.lua`: `plot:GetImprovementType()`, `plot:IsImprovementPillaged()`, `plot:IsRoute()`, `plot:IsRoutePillaged()`, and `plot:GetRouteType()`. Civ VI models routes separately from improvements.
- Decompiled route data confirms `GameInfo.Routes[...]` rows use `RouteType` and `SupportsBridges`. Base-game `ROUTE_ANCIENT_ROAD` has `SupportsBridges="false"`, while medieval / industrial / modern roads do support bridges; Gathering Storm adds `ROUTE_RAILROAD`, which also supports bridges in `DLC/Expansion2/Data/Expansion2_Routes.xml`.
- Barbarian camps and tribal villages are improvements in Civ VI data:
  - `IMPROVEMENT_BARBARIAN_CAMP` has `BarbarianCamp="true"`
  - `IMPROVEMENT_GOODY_HUT` has `Goody="true"`
- Natural wonders are feature rows with `NaturalWonder="true"` in `GameInfo.Features`.
- World scanner category split for natural wonders:
  - `WorldScannerCategory_terrain.lua` should exclude `featureInfo.NaturalWonder` from `Terrain -> Features`
  - `WorldScannerCategory_specialMapObjects.lua` is the intended scanner home for them under `Special map objects -> Natural wonders`
  - in that category's `Validate` callback, compare the live plot feature index with `plot:GetFeatureType()` / `validatePlot:GetFeatureType()`; comparing `validateFeature.FeatureType` against the numeric feature index will silently drop every natural-wonder item
- Non-local city and unit scanner entries should be gated by diplomacy met state via `localPlayer:GetDiplomacy():HasMet(playerID)`.
- Non-local unit scanner entries should also respect actual visibility through `PlayersVisibility[observer]:IsUnitVisible(unit)`.
- For scanner units, prefer player-based unit enumeration, but do not rely on a raw `0 .. PlayerManager.GetWasEverAliveCount() - 1` loop for barbarians. Mirror the `UnitFlagManager` pattern instead: scan normal alive players and also do an explicit `for _, pPlayer in ipairs(Players) do if pPlayer:IsBarbarian() then ... end end` pass, then apply the usual revealed-plot and `IsUnitVisible(unit)` gates.
- Scanner units now use three top-level categories from the same `WorldScannerCategory_units.lua` file: my units, neutral units, and enemy units.
- Foreign religious units use theological-combat relationships instead of ordinary war state. `Unit:GetReligiousStrength() > 0` identifies a live religious combat unit. It remains neutral when `PlayerDiplomacy:GetAllianceType(ownerID)` matches `GameInfo.Alliances["ALLIANCE_RELIGIOUS"].Index`, or when `Unit:GetReligionType()` matches the local player's `PlayerReligion:GetReligionInMajorityOfCities()` result. Every other foreign religious unit belongs under enemy units, including during diplomatic peace. Base-game rulesets legitimately have no `GameInfo.Alliances` table entry, so the alliance lookup must allow that database miss.
- Unit scanner subcategories should stay broad for navigation. CAI now derives them from live unit metadata in this order: barbarians, religious, civilian, support, air, naval, siege, ranged, then melee fallback.
- When classifying religious units from `GameInfo.Units`, do not treat Civ VI gameplay booleans or numeric stats as plain Lua truthy checks. Database booleans arrive as `0` or `1`, and `ReligiousStrength` / charge columns default to `0`; in Lua, `0` is truthy, so scanner logic must check `== 1` / `== true` or `> 0` explicitly.
- Every ordinary scanner category gets an implicit `All` subcategory from `WorldScannerCore.lua`. It is always inserted first and contains every emitted item in that category, grouped through the same `GroupId` / `GroupLabelResolver` path as normal subcategories. The disposable snapshot also indexes each stable leaf ID's `All` and named-subcategory memberships for direct landing-time pruning.
- The former World Scanner Ctrl+F path synchronously rebuilt every category, flattened its leaves, populated a disposable Firaxis `Search.*` context, and called `Search.Optimize()` before pushing the edit box. Commit or cancel destroyed that context, so every open repeated the blocking preparation. Map Search avoided the same open delay because it pushes its panel first, waits for input, and then spreads indexing across 35 plots per frame.
- Civ V Access provides the scanner-search reference design in `CivVAccess_ScannerNav.lua`, `CivVAccess_ScannerInput.lua`, and `CivVAccess_ScannerSearch.lua`. Ctrl+F only pushes a text-capture handler. Enter gathers fresh backend entries and makes one linear Lua pass over each item name using six relevance tiers: start whole-word, start prefix, middle whole-word, middle word-prefix, substring, and ordered word-prefix abbreviation. Identical names become one item with nearest-first instances; items sort by relevance tier and then nearest distance; original scanner categories become result subcategories beneath `All`. No Firaxis `Search.*` context is used.
- Civ VI World Scanner now mirrors that lifecycle and structure. `OpenSearch()` creates and pushes only the edit box. Enter closes it, captures the live cursor as sort origin, rebuilds fresh scanner data, walks each category's authoritative implicit `All` subcategory once, and calls the UI manager helper's public `MatchSearchText(label, query)` classifier on item names. It groups identical names within their original categories, sorts instances nearest-first and groups by tier then distance, builds `All` plus original-category result subcategories, indexes their shared stable leaf ids for live pruning, and injects the frozen synthetic search category. Search history remains available through Page Up/Page Down. Ordinary tokens are rejoined into one Civ V-style ranked positive phrase, while each `--term` token excludes item names matched by the same six-tier classifier. There is no multi-term AND intersection and no arbitrary result cap.
- Scanner category management is implemented without new Civ VI game APIs. The metadata-driven settings system supports action-only `button` rows through `CAISettings.Invoke(...)`: activation persists no fake value and raises `LuaEvents.CAISettingsChanged(settingId, actionValue)`. The in-game-only `Manage scanner categories` action is hidden in front-end settings. Settings follows the Input Help/Civilopedia lookup overlay pattern: a transparent, wrapping, input-trapping Panel is attached to the current manager root, contains and directly focuses the settings Tree, and restores the previously focused widget when destroyed. Escape captures and closes that Panel directly. Settings is not a separate stack root. Unlike those self-contained helpers, the scanner handoff calls Settings APIs from another Lua context, so `GetSettingsOwnerRoot`, `GetSettingsReturnFocus`, and `CloseSettings` rediscover the child Panel through `GetWidgetById(id, true)`. Omitting `recurse=true` searches only the stack roots, returns nil for the child, and leaves the scanner action without a parent. The scanner manager attaches its own wrapping, input-trapping Panel to the same root and prepares its initial focus before `CloseSettings(mgr, false)` destroys Settings without restoring over the replacement. Closing scanner management later focuses the widget that preceded Settings.
- Category layout persists user-wide through CAI configuration using stable category ids, not localized labels or array positions. Loading reconciles saved ids with registered definitions and appends newly introduced built-ins. The transient search-results category is never persisted. The default built-in order follows Civ V where equivalents exist: Cities, Improvements, Recommendations, My Units, Neutral Units, Enemy Units, Resources, then Civ VI Districts and wonders, Special map objects, Terrain, Queued path, Map tacs, City Management, Valid Targets, and Active Lens. Valid Targets and Active Lens are last by design. The former `wonders` saved order and custom-source id normalize to the stable `districts` id.
- The management UI is a focus-stable `Panel` whose children are, in order, the category `List`, Add custom category, and Reset built-in categories. The list contains category `SubMenu` rows whose labels identify each category as Built-in, Contextual, or Custom and append its live Enabled/Disabled configuration state. The state resolver calls `Config.IsEnabled(entry.Id)` rather than using the build-time entry snapshot, so revisiting a row after toggling its checkbox reports the current value. Built-in/contextual rows and source selectors have localized descriptive tooltips; a custom row resolves its tooltip live from the current model as three localized lines: selected source categories with either all or their selected subcategories, included keywords, and excluded keywords. Empty sections say None. Player-facing descriptions say tile and categorized, while Queued path and contextual categories state when they appear. Valid Targets examples include district/wonder placement, ranged attacks, and unit-action tiles; Active Lens examples include Settler, Appeal, Religion, Loyalty, and Power. Shift+Up/Down on the outer row reorders it. Each submenu contains its Enabled checkbox; built-in and contextual rows also contain their declared category-specific settings, Create custom category from this, and Move to default position. Reset built-in order and visibility restores default relative order in the existing built-in slots, enables every built-in, and leaves custom categories unchanged.
- Custom deletion and built-in reset use manager-owned Yes/No Dialogs attached as trapped children of the same owning root as scanner management; neither is pushed onto the manager stack. Opening captures the focused scanner-management control and directly focuses the child Dialog. Cancel/Escape destroys it and restores that control. Delete confirmation rebuilds the category list while the Dialog owns focus, prepares the surviving next/previous category, destroys the Dialog, and directly focuses that neighbor; Add is never selected as a deletion fallback. Reset confirmation rebuilds the reset layout and returns focus to Reset. Adding an include/exclude term appends its remove button in place, silently clears the EditBox, and lets the EditBox deliver its normal commit announcement without rebuilding or refocusing the manager. Removing a term rebuilds with an explicit FocusKey inside the same include/exclude submenu: the term now at the removed index, the previous final term when the removed term was last, or Add term when none remain. Generic root capture/restore is insufficient because its positional fallback can clamp outside a rebuilt submenu after the focused child disappears.
- Custom category storage keeps a monotonic internal numeric id so deleted configuration records cannot collide with later live categories. That internal id is not the visible default-name number. Creation scans current custom names and assigns the first unused localized `Custom N`, so deleting or renaming `Custom 1` makes that visible name available again without changing any surviving category's stable id.
- Recommendations, City Management, Valid Targets, and Active Lens declare `Contextual = true`; their existing auto-focus settings are exposed inside their category rows. Runtime `CanScan` and empty-category handling still skip them when unavailable. Enabled state gates both their normal cycle presence and auto-focus, while the configured order remains authoritative and is never rewritten by auto-focus.
- Civ V-style custom categories have stable ids, editable free-text names, Enabled checkboxes, whole-category or named-subcategory source selectors, and positive/exclusion name terms using the existing six-tier matcher. New customs begin at the front, and duplicating a built-in preselects that category's `All` source. Custom categories project already-built authoritative source snapshots, preserving live visibility gates, validators, grouping, distance sorting, and implicit-`All` deduplication without rescanning the map.
- The core raw-item build contract accepts multiple `SubCategoryIds` for one item. A projected custom item enters the custom category's implicit `All` branch once and every matched selector/term branch. Copied group ids are prefixed by source category and retain the source group label. Validation calls the original source item and its untouched category, subcategory, and group fields; validating a projected item directly is incorrect because its projection fields differ. Ordinary categories build first, custom projections build second, and global Ctrl+F walks authoritative built-in source categories only so custom projections cannot duplicate search hits.
- The registered `districts` definition extracts ordinary districts and delegates wonder extraction to the former Wonders backend. Both use the same relationship subcategories and type grouping; owned pillaged districts retain their additional subcategory. The former Wonders backend is no longer registered as a separate scanner category.
- A universal player-authored rule language should not be the first implementation. Current scanner leaves share identity, label, plot, group, subcategory, and validation behavior, but not a normalized cross-category field schema. Same-plot facts such as terrain elevation and resource type are separate leaves, so a rule like `iron AND hills near my city` requires a plot-level join plus defined semantics for visibility, distance, ownership, AND/OR/NOT nesting, and dynamic categories. A later constrained facet system is possible after category modules expose stable machine-readable attributes; it should start with one source category plus supported facets, not arbitrary cross-object expressions.
- The Cities scanner category reads `ScannerGroupCitiesByCivilization`. Its default enabled state groups cities by owner; when disabled, each city receives a stable group key built from owner ID and city ID. Relationship subcategories are unchanged, and barbarian outposts retain their shared outpost group. `WorldScanner_CAI.lua` listens for `LuaEvents.CAISettingsChanged` and rebuilds Cities immediately when this preference changes.
- `hexCoordUtils_CAI.lua` now owns shared original-capital lookup, relative-coordinate math, wrap-aware spoken hex geometry helpers, and reusable direction/path utilities (`directionString`, `stepListString`, `stepListFromPath`, `unitVector`, `cubeDistance`, `directionRank`, `plotsInRange`).
- `CAIHexCoordUtils.directionString(fromX, fromY, toX, toY)` returns localized `LOC_CAI_HERE` when both coordinate pairs match. Callers should not implement their own same-tile fallback. Movement logging is the exception in behavior, not formatting: it checks known net-zero coordinates before calling the helper so a non-move still produces no movement entry.
- Plot tooltip relative coordinates should call `GetRelativeCoords(plot)` and format the returned `dx, dy` locally; world scanner item speech should call `CAIHexCoordUtils.directionString(cursorX, cursorY, targetX, targetY)` and speak direction text instead of tile distance.
- Map tacs / map pins are read from `PlayerConfigurations[iPlayer]:GetMapPins()` and filtered with `mapPinCfg:IsVisible(localPlayerID)`. Shared label helpers live in `inGameHelpers_CAI.lua`: `BuildMapTacLabel(mapPinCfg)` returns name + icon, `BuildMapTacLabelWithOwner(mapPinCfg, playerID, localPlayerID)` appends the owner name for non-local tacs, `GetVisibleMapTacsAtPlot(plot)` returns visible tacs on a plot, and `GetMapTacIconLabel(iconName)` mirrors `MapTacks.IconOptions(...)` tooltip resolution plus CAI stock-icon localization fallbacks. The world scanner `mapTacs` category builds dynamic subcategories: `My` first, then one subcategory per other visible pin owner. Scanner groups use the item label as their normal group label. `PlotToolTip_CAI.lua` collects visible tack labels into one owner-aware list and speaks it through `LOC_CAI_PLOT_MAP_TACS` as `Map tacks: {list}`.
- Vanilla `MapPinPopup.RequestMapPin(x, y)` obtains the local player's editable pin through `PlayerConfigurations[localPlayerID]:GetMapPin(x, y)`. The same call returns the owned pin already at those coordinates or creates the default pin when none exists. The returned `MapPinConfiguration` can be renamed with `SetName(...)`; deletions use `PlayerConfiguration:DeleteMapPin(pinID)`, and mutations are followed by `Network.BroadcastPlayerInfo()`.
- Vanilla world-map right-click deletion resolves the local player's pin, calls `PlayerConfiguration:DeleteMapPin(pinID)`, broadcasts player info, and plays `Map_Pin_Remove`; it does not ask for confirmation. The accessible Map Pin List binds Delete only on local-player rows and puts that same live deletion sequence behind a manager-owned Yes/No confirmation dialog. The rebindable `CAIDeleteMapTac` world input action defaults to Delete, scans the local player's existing `GetMapPins()` collection for the navigation cursor coordinates, and opens the same confirmation; do not use `GetMapPin(x, y)` for this lookup because that method creates a default pin when none exists. Deleting a pin also clears any CAI bookmark slot that references its ID.
- Map-pin sharing has two distinct multiplayer gates. Vanilla shows the visibility target (`To All`, `To Team`, self, or another human player) for `GameConfiguration.IsAnyMultiplayer()`, but shows `Send to Chat` only when `GameConfiguration.IsNetworkMultiplayer()` is true and the live pin reports `not mapPinCfg:IsPrivate()`. Hotseat therefore has pin visibility but no real-time chat-send button. New pins start with the popup's self-only target and are private. The selected visibility is not written to the pin until `OnOk()` calls `SetVisibility(...)`; `ShowHideSendToChatButton()` reads the live pin rather than the pending target, so changing visibility requires confirming and reopening the pin before the vanilla send button can appear. Once shown, the button is disabled if the current chat target cannot see the pin.
- `MapPinPopup_CAI.lua` mirrors the vanilla multiplayer visibility targets in an accessible `Visible to` dropdown. A selection immediately calls the live pin's `SetVisibility(...)`, broadcasts player info, and calls `ShowHideSendToChatButton()`, so the native and accessible Send-to-Chat controls react without first confirming and reopening the popup.
- Visibility edits are transactional. Opening captures the pin owner/id and committed visibility. Cancel or any non-confirming close restores and broadcasts that committed value; Send to Chat first advances the committed baseline to the live visibility, so a later Cancel retains the visibility used for the sent pin; accessible OK commits the pending visibility. Because base `OnOk()` writes its inaccessible private `g_playerTarget`, accessible confirmation lets base save name/icon and then the teardown restores the CAI-committed visibility if base temporarily overwrote it. Native visual confirmation is explicitly marked so its own target remains authoritative. Accessible Escape now follows `OnCancel()` instead of clicking OK.
- Map-pin bookmark slots persist their pin IDs in local-player configuration values named `CAI_MAP_PIN_BOOKMARK_SLOT_1` through `_10`. `PlayerConfiguration:GetValue(...)` / `SetValue(...)` keeps the mapping player-specific and save-specific alongside the pins. An unset `GetValue(...)` key returns no Lua values, not one `nil`; capture its result in a local before passing it to `tonumber(...)` or another function. Resolve a stored pin ID live with `GetMapPinID(...)`; a missing result means the pin was deleted and the stale slot should be cleared. One pin belongs to at most one bookmark slot. Default actions are `Ctrl+Shift+digit` to assign, `Ctrl+digit` to jump, and `Alt+digit` to speak the live direction from `CAICursor:GetCoords()` to the pin; digit `0` represents slot 10.
- `MapPinListPanel_CAI.lua` binds `ExposedMembers.CAICursor` locally and reads its current position through `CAICursor:GetCoords()` when building each pin's direction label. Do not read cursor implementation fields such as `curX` / `curY` directly.
- `MapSearchPanel_CAI.lua` likewise uses `CAICursor:GetCoords()` for both direction labels and nearest-first result sorting.
- Shared unit naming now lives in `inGameHelpers_CAI.lua`:
  - Civilization ownership prefixes first resolve `GameInfo.Civilizations[playerConfig:GetCivilizationTypeID()].Adjective`. Do not derive an adjective localization tag by rewriting `GetCivilizationShortDescription()`; localization-key naming is not guaranteed. A nonempty database tag is also not guaranteed to have localization: Outback Tycoon declares adjective tags for its four playable colonies and Sydney but defines none of them in any supplied language. Because `Locale.Lookup(...)` returns the raw tag when it is missing, compare the result with the tag and fall back to the localized `GetCivilizationShortDescription()` when they match. This also mirrors vanilla unit-flag ownership labels.
  - `GetUnitFormationSuffix(unit)` is the single formation-suffix helper for live unit objects
  - `GetUnitDataFormationSuffix(data)` is the matching helper for vanilla `UnitPanel` data tables
  - `FormatOwnedUnitDisplayName(unit)` now auto-applies the shared formation suffix when the caller does not pass one explicitly
  - `UnitFlagManager_CAI.lua`, `UnitPanel_CAI.lua`, `WorldScannerCategory_units.lua`, and `RevealAnnouncements_CAI.lua` should use those helpers instead of duplicating corps/army/fleet/armada logic
- A future scanner category can mirror vanilla settler-lens water availability exactly by consuming `Map.GetContinentPlotsWaterAvailability()` and mapping its returned arrays to localized subcategories rather than reverse-engineering lens colors.

### UIScreenManager priority stack

- `UIWidgetPriority` defines shared root-widget priority levels:
  - `Low = -10`
  - `Normal = 0`
  - `High = 10`
- `mgr:Push(widget, priority)` accepts a numeric priority. Nil priority resolves to `Normal`.
- Stack sorting is deterministic: priority wins first, then push order breaks ties. Equal-priority widgets therefore behave like a normal last-in-first-out stack.
- The active root is always `mgr:GetTop()`, which is the last entry after sorting.
- Pushing a lower-priority root does not refocus or reannounce the existing active root when it remains on top.
- `mgr:Pop()` removes the active root. `mgr:Pop(widget)` removes a specific root widget if a screen needs to close a non-active lower-priority root.
- `mgr:SetFocus(...)` and `mgr:SetFocusPath(...)` only apply focus when the target path belongs to the active root, preventing lower-priority screens from stealing focus while a higher-priority modal/popup is active.

## Lua Context Notes

- Included Civ VI Lua panel files expose their top-level functions directly in the current chunk after `include("...")`, so wrappers can usually reassign names like `PopulateWonders = WrapFunc(PopulateWonders, ...)`.
- Do not rely on `_G` or `rawget(_G, ...)` for panel helper discovery in Civ VI UI contexts. These globals are not consistently available/mod-safe in this project; prefer explicit one-by-one capture/wrapping of the functions you need.
- Camera/map movement:
  - `WorldInput.lua` pans the map through `UI.PanMap(panX, panY)` from camera pan input actions and focuses plots through `UI.LookAtPlot(...)` in `SnapToPlot(plotId)`.
  - Minimap clicks call `UI.LookAtPosition(worldX, worldY)`. City and unit panels also call `UI.LookAtPlot(...)` for explicit recenter actions.
  - `WorldView/CameraManager.lua` is combat-specific despite its broad name: it listens to `Events.CombatVisBegin` / `Events.CombatVisEnd`, optionally calls `UI.LookAtPlot(combatMembers.x, combatMembers.y, zoom)` based on gameplay options, saves the previous zoom with `UI.SetRestoreMapZoom(prevZoom)`, and restores it with `UI.RestoreMapZoom()`. It does not manage normal selected unit/city camera sync.
  - Current camera focus can be read with `UI.GetMapLookAtWorldTarget()`, which returns world-space `x, y`. Vanilla `WorldInput.lua` uses it as the drag-start focus, and `Automation_ObserverCamera.lua` converts it to plot coordinates with `UI.GetPlotCoordFromWorld(wx, wy)`.
  - `Events.Camera_Updated` exists. Vanilla uses it for camera/zoom-sensitive world anchors, e.g. `CityPanel.lua` updates the border-growth anchor and `TourismBannerManager.lua` refreshes banner positions. It is not used by vanilla as the primary unit/city selection signal.
  - The event can pass focus and zoom values (`OnCameraUpdate(vFocusX, vFocusY, fZoomLevel)` in `CityPanel.lua`). For selected-object cursor sync, prefer `Events.UnitSelectionChanged`, `Events.CitySelectionChanged`, and explicit CAI/vanilla `LookAtPlot` touch points; use `Camera_Updated` only as a throttled/manual camera-center fallback, probably by reading `UI.GetMapLookAtWorldTarget()` and converting through `UI.GetPlotCoordFromWorld(...)`.
- Tutorial input locking:
  - `TutorialScenarioBase.lua` starts the tutorial with `Input.SetActiveContext(InputContext.Tutorial)`.
  - `TutorialUIRoot.lua` installs `ContextPtr:SetInputHandler(OnInput, true)`, so it receives all input during tutorial flow. Its normal handler only handles debug keys and Escape, but the tutorial system also drives `UITutorialManager` overlays/control filtering.
  - `AdvisorPopup.lua` also installs `ContextPtr:SetInputHandler(OnInputHandler, true)`. When `AdvisorBase` or `MetaBase` is visible, `IsBlockingInput()` returns true, so `OnInputHandler()` consumes most input. Escape is special-cased to fire `LuaEvents.Tutorial_ToggleInGameOptionsMenu()`.
  - `AdvisorPopup.lua` calls `UITutorialManager:SetActiveAlways(isAdvisorVisible)` and `UITutorialManager:EnableOverlay(isAdvisorVisible)` from show/hide handlers. While the advisor is visible, normal UI controls do not get input.
  - `EraCompletePopup` and `AdvisorPopup` can make the era presentation restart repeatedly when they overlap. XP1/XP2 locks the era context at `PopupPriority.High` (1000), while the advisor queues at `PopupPriority.Tutorial` (2000). Each time the higher-priority advisor is lowered or replaced, the still-locked era context becomes visible again and its vanilla `OnShow()` unconditionally plays `UI_Era_Change`, resets `EraPopupAnimation` to the beginning, and plays it. If tutorial processing immediately raises another queued advisor, the contexts alternate and every brief era re-show restarts both the animation and sound. Captured `Lua.log` confirms the cycle as repeated `START EraPopupAnimation` lines interleaved with CAI advisor-dialog remove/push pairs under one era popup lock; the eventual `PopupManager.Unlock` ends it. This is a Firaxis popup-lifecycle interaction exposed by the overlap, not audio playback independently looping.
  - Diplomacy is intended as an exception. `AdvisorPopup.OnDiploSceneOpened()` sets its private `m_isDiplomacyUp` flag and disables `SetActiveAlways` when an already-visible advisor overlaps diplomacy; `IsBlockingInput()` also returns false while that flag is set. `OnDiploSceneClosed()` restores active-always input if the advisor remains visible.
  - There is an ordering hole when an advisor is raised after diplomacy has already opened. `DiploScene_SceneOpened` first runs while no advisor is visible, then `ShowAdvisorPopup()` queues the new popup and its later `OnShow()` unconditionally calls `SetActiveAlways(true)`. No second diplomacy-open event corrects that state. Expansion 1's queueable `ALLIANCE_AVAILABLE` item can follow this exact path when declaring friendship makes an alliance available: the advisor appears below DiplomacyActionView, but tutorial-wide input capture otherwise prevents normal diplomacy navigation.
  - Use the same exemption pattern as EndGameMenu and other screens that must remain operable through tutorial filtering: call `UITutorialManager:AddControlToAlwaysReceiveInput(ContextPtr)` when DiplomacyActionView or DiplomacyDealView opens and `RemoveControlToAlwaysReceiveInput(ContextPtr)` when it hides or shuts down. Each context owns its registration independently, so the deal overlay can close without removing the still-open action view's exemption. Keep the advisor dialog below the diplomacy accessibility root so it becomes active only after diplomacy closes.
  - Detailed tutorial steps call `RaiseDetailedTutorial(item)`, which shows only the tutorial-triggered controls and calls `UITutorialManager:EnableControlsByIdOrTag(...)` for the item `EnabledControls`. Other controls may be blocked by the tutorial overlay even when the advisor popup is hidden.
  - In the production tutorial steps, vanilla production rows are tagged with `UITutorialManager:GetHash(item.Type)` while the scenario enables hashes such as `UNIT_WARRIOR`, `UNIT_BUILDER`, or `BUILDING_MONUMENT`. CAI production rows should read the live vanilla row controls through widget `IsHidden` / `IsDisabled` methods so UI manager navigation and activation respect the current tutorial-filtered control state.
  - ResearchChooser uses the same pattern: vanilla research rows tag `Top` with `UITutorialManager:GetHash(kData.TechType)` and set `Top:SetDisabled(...)`; CAI research rows should cache the returned vanilla row instance and expose live `Top` / `TopContainer` state through widget `IsHidden` / `IsDisabled`.
  - Base tutorial research "Show me" buttons call `LuaEvents.Tutorial_ResearchOpen()` before `LuaEvents.AdvisorPopup_ShowDetails(advisorInfo)`. That means ResearchChooser can open before `RaiseDetailedTutorial(item)` applies enabled-control filtering, so CAI's initial focus should wait for a post-`RaiseDetailedTutorial` signal before pushing the accessibility panel on tutorial opens.
  - Native control callouts shown synchronously inside vanilla `RaiseDetailedTutorial()` are queued until vanilla returns. CAI then emits `CAI_TutorialDetailedControlsReady`, allowing delayed screens such as ResearchChooser to push, before presenting the queued instruction. This prevents the screen's initial focus from interrupting the instruction.
  - The first Mining selection avoids that race because it follows a different scenario chain. The ordinary ActionPanel chooser event fires while `SELECT_RESEARCH_8` is active; `ResearchChooser_ForceHideWorldTracker` completes that item and CAI pushes `CAIResearchChooser_Panel` behind the chained advisors. Later, `SELECT_RESEARCH_A`'s Show Me button calls `Tutorial_ResearchOpen()` and then enters detailed mode, but the accessibility panel already exists. The runtime order is chooser push, advisor transitions, then `TutSelectCityResearch` buffering. Pottery's `SELECT_POTTERY_TECH` instead calls `Tutorial_ResearchOpen()` from its own `OpenFunc` and has no earlier chooser push, exposing the delayed-push/callout race.
  - Tutorial items are data objects loaded by `TutorialUIRoot.LoadItems()`. They define raise listeners, prereqs, advisor text/buttons, advisor-only UI triggers, detailed-mode UI triggers, enabled controls, disabled controls, done listeners, cleanup, and optional chain `NextID`.
  - `TutorialCheck(listenerName)` is the central state machine. It ignores checks while disabled or during the load screen, checks only valid local-player tutorial events, deactivates the active item when its done listener/function matches, then activates unseen eligible items or queues queueable ones.
  - `ActivateItem(item)` sets `m_active`, optionally runs the global pre-activate function and item open function, enables tutorial checks, adds goals, then raises the advisor popup through `LuaEvents.TutorialUIRoot_AdvisorRaise(item.AdvisorInfo)`.
  - Advisor buttons either call `LuaEvents.AdvisorPopup_ClearActive(advisorInfo)` to mark/deactivate/chain, or `LuaEvents.AdvisorPopup_ShowDetails(advisorInfo)` to mark seen and enter detailed mode.
  - `TutorialInput.lua` is a separate lightweight input-filter context. `ActivateInputFiltering()` shows that context through `LuaEvents.TutorialUIRoot_FilterKeysActive()`, and `DisableInputFiltering()` hides it; while active it primarily catches Escape for the tutorial options menu.
  - Native `<Tutorial>` controls do fire `RegisterWhenShown(...)` callbacks for detailed screen pointers. A broad `/InGame/` plus `/TutorialUIRoot/` scan found 60 controls and observed pointers including `TutFoundCityAction`, `TutOpenProduction`, `TutTrainWarriors`, `TutSelectEndTurnAction`, and `TutSelectCityResearch`.
  - Native-control and world-anchor presentation share an item-level exact-text set. CAI records the localized world-anchor text and location first, then suppresses a queued control candidate with the same header/body. `CITY_DEFENSE_B` and `CITY_DEFENSE_C`, which define both same-text `TutMoveToTileAction` and `SetAdvisorCallout(...)` instructions, therefore retain the location-bearing world message only.
  - Native callouts owned by opened screens such as ResearchChooser, ProductionPanel, TechTree, GovernmentScreen, ReligionScreen, GreatPeoplePopup, and TradeRouteChooser are presented as CAI dialogs: the first localized leaf is the dialog title, remaining localized leaves form the body, and `LOC_ADVISOR_BUTTON_OK` dismisses the dialog. The instruction is added silently to Tutorial history so dialog focus is its only immediate speech. Callouts under the map-facing HUD contexts ActionPanel, CityPanel, DiplomacyRibbon, LaunchBar, MinimapPanel, NotificationPanel, PartialScreenHooks, PlotInfo, TopPanel, UnitFlagManager, UnitPanel, WorldInput, and WorldTracker remain ordinary buffered messages. HUD classification uses the native tutorial control IDs as the authoritative fallback because recursive `/InGame` discovery does not reliably preserve the owning context; this covers `TutFoundCityAction`, `TutOpenProduction`, the `TutSelectEndTurnAction*` family, LaunchBar/TopPanel/PartialScreenHooks pointers, and UnitPanel action pointers.
  - English advisor localization can be adapted without copying Firaxis's `AdvisorText.xml`: an in-game `UpdateText` SQL file updates the existing `LocalizedText` rows by `Tag` and `Language = 'en_US'`. `tutorial_advisor_text_CAI.sql` limits its overrides to body tags used by active `TutorialScenarioBase.lua` advisor messages, direct world callouts, and native UI triggers. Headers remain vanilla. The replacements preserve gameplay explanations while routing actions through CAI's selected-object action list, direct bindings, navigation cursor, spoken selection sections, turn-blocker list, and screen trees. Instructions are cumulative: the first applicable item explains a control pattern completely, while later items name only the new target or additional level. Name the actual list, tree, dialog, tab, or action list instead of repeatedly describing it as accessible; retain that word only where semantically necessary, such as the proper name of the Civilopedia's Accessibility Mod section. Screen-owned callouts must identify the actual layout, explain Arrow-key traversal and expansion, mention type-to-find where supported, and state the final Enter or Tab action when that pattern is first introduced. The Technology Tree lesson introduces the shared Focused Widget Reader by explaining its purpose: it lets the player hear detailed information about the focused item one section at a time. `Alt+Up` / `Alt+Down` move to and read the previous / next section, while `Alt+Home` / `Alt+End` jump to and read the first / last section. Later instructions can direct the player to the reader without repeating those bindings. Multi-level flows must name each newly introduced level, such as Unit Action List -> Improvements submenu -> Farm, while the later Quarry step can reuse that vocabulary without teaching list navigation again. The first movement lesson owns the complete quick-movement map: `Alt+Q/E/A/D/Z/C` or `Alt+Numpad 7/9/4/6/1/3` for northwest, northeast, west, east, southwest, and southeast. Mention quick movement later only for adjacent destinations or open-ended exploration; directed multi-tile instructions must use `Alt+M` movement mode. The Barbarian Warrior attack callout explains CAI's two-stage combat confirmation: `Space` reads the live preview without committing, including assessment, predicted damage, strengths, and modifiers; the first `Enter` arms combat and repeats the preview plus `LOC_CAI_MOVEMENT_COMBAT_CONFIRM`, while the second `Enter` commits the attack. Do not tell tutorial players to use `Ctrl+R` for an expected Research Chooser step: `WorldTracker_CAI` deliberately blocks that general chooser hotkey whenever `IsTutorialRunning()` is true, while the permitted Action Panel research action remains available through `Ctrl+Space` or the Turn Blockers list.
  - Tutorial concept instructions should introduce the corresponding live information binding at the lesson where it is directly useful: `R` for Science and research progress, `P` for Culture and civic progress, `F` for Faith, `G` for Gold and trade-route capacity, `Shift+2` for city Production at the Production-cost lesson, `Shift+4` for border-growth progress, `Shift+6` for city population or unit and Trader statistics, `Shift+8` for Trader route information, `B` for city buildings on the navigation-cursor tile, and `X` for a focused tile's movement cost and physical statistics. Space these bindings across their concept lessons rather than accumulating them in one city-yields instruction. The Civics Tree appears before the Technology Tree in `TutorialScenarioBase.lua`; both CAI screens default to a Grid whose era columns contain left-to-right research tiers. Their expandable Tree is an alternate view reached through the final Change View button, so tutorial prose must teach the grid in Civics first and let Technology reuse it.
  - Tutorial message history and the contextual Tutorial Scanner category have different lifetimes. Located callouts remain cumulative message-buffer entries with `category = "tutorial"` and `{x, y}`, but `TutorialUIRoot_CAI.lua` separately publishes only the active world callout as `ExposedMembers.CAI_TutorialWorldAnchor`, containing its localized header and coordinates. It clears that state at item transitions and raises `CAI_TutorialWorldAnchorChanged`; `WorldScannerCategory_tutorial.lua` projects that single live marker using only the callout header. Do not derive current Scanner state from retained message history, because that leaves completed tutorial plots visible. The Scanner introduction belongs on the first movement marker after the Barbarian attack and is intentionally limited to the four hierarchy navigation pairs, `End` for live direction/distance, and `Home` for moving the navigation cursor to the current result.
  - AdvisorPopup and TutorialUIRoot can both receive the same `InputStruct` because Civ VI routes both through its always-active tutorial input path. Advisor Enter synchronously hides the advisor, enters detailed mode, and can push a screen-callout dialog before TutorialUIRoot receives that same key-up; redispatching it activates the new dialog's default OK. Cross-context object markers are not safe because delivery to a second context is inconsistent and Civ VI reuses the Lua `InputStruct` wrapper for later physical events. CAI instead enforces Enter down/up ownership in `CAIUIScreenManager`: the first Enter key-down records the focused leaf, and its key-up is accepted only for that same leaf. The first key-up clears the lifecycle before dispatch, so another context rejects a duplicate after focus changes; the next complete Enter press records its new owner normally. If the owner leaves the active focus path before CAI receives key-up, focus application or widget-destroy pruning cancels the stale lifecycle. Tutorial Setup requires this path because its vanilla handler consumes the movie-skip Enter key-up while `StopMovie()` removes the CAI movie panel; without cancellation, the next leader-button Enter is mistaken for repeat and rejected. This also prevents a callout's OK key-up from activating ProductionPanel, CivicsTree, or GovernmentScreen without delaying the new surface or intercepting unrelated shortcuts.
  - Tutorial Setup's accessible dialog mirrors only the two leader-start controls; it intentionally omits the visual Back control. Each accessible leader action calls the corresponding XML control's `DoLeftClick()` so vanilla remains the sole owner of setup behavior. While the tutorial intro movie is visible, a separate movie panel mirrors End Game Menu's `LOC_CAI_ENDGAME_MOVIE_PLAYING` title and `LOC_CAI_ENDGAME_MOVIE_SKIP` action; activating Skip calls `IntroMovieContainer:DoLeftClick()`, preserving vanilla's registered movie-stop callback.
  - World-anchored unit pointers are a special duplicate. Both `UI/Popups/AdvisorPopup.xml` and the hidden `UI/Automation/Automation_NarrationPopup.xml` define `TutSelectUnit` with `TriggerBy="TutorialSelectUnit"`, so a global native-control scan registers two callbacks and buffers the same world callout twice. The automation copy can also expose inappropriate/raw control-tree text. Do not buffer `TutSelectUnit` through the generic control callback path; use the active item's already-localized `AdvisorInfo.CalloutHeader` / `CalloutBody` once instead.
  - Tutorial scenario `AdvisorInfo.PlotCallback` return types are inconsistent. Most callbacks return a numeric plot index, but some return the `Plot` object from `Map.GetAdjacentPlot(...)` directly. CAI callout-location helpers must accept either a non-negative number resolved by `Map.GetPlotLocation(...)` or a plot-like object resolved through `GetX()` / `GetY()`; comparing an object directly with `< 0` raises a Lua runtime error.
  - In the base tutorial `OPEN_CITY_PANEL` item, the only explicitly enabled target is the city panel `ChangeProductionCheck`, and completion is `CityPanel_ProductionOpen` -> `ProductionPanelViaCityOpen`. This is why normal arrow/Escape navigation can appear stolen until that control is activated.
  - `CityPanel_CAI.OpenOrToggleCityProduction()` normally raises `LuaEvents.CityPanel_ProductionOpen()` directly when the panel is closed. The tutorial overlay can block the visual `ChangeProductionCheck` interaction without changing the control's `IsDisabled()` or `IsHidden()` state, so those live-control checks alone let the CAI action or hotkey open Production during unrelated instructions. Before raising the event, the CAI path must require `IsCAITutorialControlAllowed("ChangeProductionCheck")` and announce the ordinary tutorial opening restriction when denied. Intended city-production items explicitly enable that hash. Action Panel production blockers open through their own vanilla flow and do not pass through this City Panel helper. If Production is already visible, the same helper treats activation as a close attempt and applies `IsCAITutorialScreenCloseAllowed()` instead.
  - The initial `FOUND_FIRST_CITY` item intends the starting Settler to found in place. It completes on any `CityAddedToMap` event and does not validate coordinates, so moving through an accessibility shortcut can found elsewhere and break later fixed-map assumptions. Do not solve this by treating each detailed item's `EnabledControls` as a general unit-action allow-list: Firaxis is inconsistent about listing movement there, notably in `SCOUTS_D2`, and that policy blocks valid Scout, Warrior, Builder, and Settler movement paths. `TutorialUIRoot_CAI` instead calls vanilla `DisableUnitAction("UNITOPERATION_MOVE_TO", "UNIT_SETTLER")` when `FOUND_FIRST_CITY` activates and calls the matching `EnableUnitAction(...)` when it deactivates. This produces the same `action.Disabled` state used by the visual Unit Panel and restores the unit-type restriction immediately after the founding step.
  - The accessible Unit Action List and direct unit-action hotkeys now rely on vanilla's live `action.Disabled` value and no longer apply `IsCAITutorialControlHashAllowed(...)`. Quick movement and final Move To confirmation cannot read UnitPanel's context-local `IsDisabledByTutorial(...)`, so `MovementActions_CAI` reads the same serialized `UI.GetGameParameters():Get("UnitActionRestrictions")` source written by `DisableUnitAction`. It checks both `_ALL` and the selected unit's type for `UNITOPERATION_MOVE_TO`. Thus all three movement paths respect explicit vanilla tutorial action restrictions without interpreting incomplete detailed enabled-control lists.
  - Formation-step unit cycling is affected by a separate persistent vanilla unit-selection lock. The installed/decompiled tutorial has one `LockUnit(...)` call: `FORTIFY_WARRIOR_C.OpenFunc` locks the Warrior until `FOUND_SECOND_CITY_D`, after `SETTLER_FORMATION`. `TutorialUIRoot.CheckLockedUnit()` force-deselects that Warrior after every selection change, making comma/period cycling appear to skip different neighboring units by direction. CAI clears any restored lock when `TutorialUIRoot_CAI` loads and makes `LockUnit` a no-op. This does not bypass tutorial progression: `OnUnitSelectionChanged()` calls `TutorialCheck("UnitSelectionChanged")` before the forced deselection, while separate unit-operation and map-movement restrictions remain responsible for limiting actions.
  - `FIRST_PANTHEON_I` is the tutorial's intended Religion Screen close step. It enables the `ModalControls` hash, raises `TutorialCloseReligionScreenPointer`, and waits for either `ReligionPanelClosed` or `PantheonPanelClosed`, but the overlay does not otherwise deliver input to the Religion Screen context. `TutorialUIRoot_CAI` temporarily registers `/InGame/ReligionScreen` with `UITutorialManager:AddControlToAlwaysReceiveInput` for this item and removes it when the item deactivates, matching the existing Production, Civics Tree, and Government detailed-input treatment.
  - Do not infer that every item using `SetEnabledControls` needs an always-receive registration. In-game evidence confirms that Research Chooser, Technology Tree, and Civics Tree permitted controls already route CAI input correctly. An audit of the active scenario code (excluding Firaxis's block- and line-commented examples) found seven `ChooseProductionMenu` items, all registered to `/InGame/ProductionPanel`: `TRAIN_WARRIORS`, `TRAIN_BUILDER`, `CONSTRUCTING_BUILDINGS_C`, `TRAIN_SETTLER_B`, `TRAIN_SLINGER`, `DISTRICTS_F`, and `CAMPUS_COMPLETE_D`. `CAMPUS_COMPLETE_C` only enables the City Panel control that opens Production; its commented-out alternative chooser implementation is inactive. The remaining special context-input registrations are `CIVICS_TREE_H` for the intended Civics close, `GOVERNMENT_POLICIES_H` for the policies button, and `FIRST_PANTHEON_I` for the intended Religion close. The active Trade Route Chooser interaction items enable the `TradeRouteChooser` root itself. No other active screen item has the same uncovered context-input mismatch.
  - No detailed tutorial item enables City Details (`CityPanel.ToggleOverviewPanel`). While a detailed item is active, its enabled controls are a temporary allowlist. `CityPanel_CAI` now includes `IsCAITutorialControlAllowed("ToggleOverviewPanel")` in the shared action-map availability used by both the `CityToggleOverview` shortcut and City Action List. `CityPanelOverview_CAI.OnShowOverviewPanel(true)` repeats the check as a defensive event boundary and restores the City Panel toggle to false when denied. This is not a tutorial-wide ban: the persistent `Tutorial_ContextDisableItems("CityPanel", ...)` list does not name `ToggleOverviewPanel`, so the helper allows City Details whenever no advisor/detailed item owns input, including later free roam.
  - Reports likewise has no detailed-item permission in the base tutorial. Its ordinary visual route is the Launch Bar Reports hook and is unavailable while another detailed allowlist owns input. CAI adds its own `UI_TopPanelOpenReports` action with F2 (vanilla F2 is `ToggleCityStates`). `TopPanel_CAI` now gates F2, yield Reports, Resources, City Status, Gossip, and Global Resources with `LaunchBar_Hook_Reports` before dispatch and announces the existing tutorial-blocked message when denied. `ReportScreen_CAI.Open()` repeats the check and clears any pending focus request when denied, preventing alternate event callers from bypassing the phase. All paths remain available when the tutorial has no active item; non-opening Top Panel speech remains available throughout.
  - `ExposedMembers.CAI_TutorialState` distinguishes these phases: `HasDetailedItem == true` makes `IsCAITutorialControlAllowed(...)` and its hash variant enforce the published enabled sets; `ActiveItemId ~= nil` with no detailed item returns false because an advisor/noninteractive item owns the phase; both fields nil returns true for normal/free-roam input even though `IsTutorialRunning()` remains true. A missing state remains fail-closed during tutorial initialization. These helpers remain appropriate for CAI-only screen-opening and production-row paths, but no longer gate ordinary unit actions or movement.
  - In the base tutorial `TURN_BASED_B` item, the OK button calls `LuaEvents.AdvisorPopup_ShowDetails(advisorInfo)`. `TutorialUIRoot.OnAdvisorPopupShowDetails()` marks the active item seen and calls `RaiseDetailedTutorial(item)`. Because `TURN_BASED_B` has no `UITriggers`, `RaiseDetailedTutorial()` immediately calls `DeActivateItem(item)`, which chains into `TURN_BASED_C`.
  - `TURN_BASED_C` is the detailed end-turn step: it sets `UITriggers("ActionPanel", "TutorialSelectEndTurn")`, enables `UITutorialManager:GetHash("ActionPanel")`, disables `UITutorialManager:GetHash("ChangeProductionCheck")`, and completes on `LocalPlayerTurnEnd`.
  - Base tutorial detailed steps that show the ActionPanel use `SetUITriggers("ActionPanel", ...)`. Active entries found in `TutorialScenarioBase.lua`: `TURN_BASED_C`, `SELECT_RESEARCH_8`, `SELECT_END_TURN_B`, `SELECT_END_TURN_PRODUCTION`, `SELECT_END_TURN_C`, `SELECT_END_TURN_D`, `SCOUTS_D2`, `SCOUTS_E`, `SELECT_END_TURN_RESEARCH`, and `RESEARCH_IRRIGATION`. `NOTIFICATION_PANEL` uses `SetAdvisorUITriggers("ActionPanel", "TutorialNotificationPointer")` only while the advisor popup is still active. The commented `SELECT_END_TURN_UNIT_ORDERS` block is inactive.
  - `TutorialUIRoot_CAI.lua` emits `LuaEvents.CAI_TutorialActionPanelAllowed(isAllowed)` when detailed tutorial controls are raised or deactivated. `ActionPanel_CAI.lua` should block Space/Ctrl+Space and its blocker-list panel while `IsTutorialRunning()` is true unless the current detailed item has the `ActionPanel` UI trigger.
  - Base tutorial setup calls `SetSlowNextTurnEnable(true)` once. `TutorialUIRoot.SetSlowNextTurnEnable(...)` only broadcasts `LuaEvents.Tutorial_SlowNextTurnEnable(isEnabled)`. `ActionPanel.lua` listens with `OnTutorialSlowTurnEnable(...)`, stores `m_isSlowTurnEnable`, and on every `OnLocalPlayerTurnBegin()` briefly shows `TutorialSlowTurnEnableAnim`, a hidden-by-default `AlphaAnim` with `ConsumeAllMouse="1"`, then hides it again from its animation end callback. This is a mouse click-spam shield, not a persistent ActionPanel enabled/disabled flag.
  - If the game access-violates on the `TURN_BASED_B` OK click with no Lua traceback, the native crash is likely during the `AdvisorPopup_ShowDetails` -> `RaiseDetailedTutorial` -> `DeActivateItem` -> `ActivateItem(TURN_BASED_C)` chain, especially around `UITutorialManager` showing/enabling ActionPanel tutorial controls.
- `TechCivicCompletedPopup.lua` (decompiled/Assets/UI/Popups/) shows tech and civic completions through an internal queue and a UIManager popup:
  - `AddCompletedPopup(player, civic, tech, isByUser)` appends to `m_kPopupData` and calls `UIManager:QueuePopup(ContextPtr, PopupPriority.Low, { DelayShow = true })` on the first entry.
  - `OnShow()` runs `RealizeNextPopup()`, which pops the next `m_kPopupData` entry and dispatches to `ShowTechCompletedPopup(player, tech, quote, audio)` or `ShowCivicCompletedPopup(player, civic, quote, audio)` based on whether `tech` is nil.
  - `TryClose()` fires `LuaEvents.TechCivicCompletedPopup_TechShown` or `_CivicShown`, then either re-enters `RealizeNextPopup()` when more entries remain or calls `Close()` which clears the queue and dequeues from UIManager. Because `TryClose` chains, a single user dismiss can immediately trigger another `Show*Completed*Popup`, so CAI wrappers must pop the previous CAI dialog before each build.
  - `ShowCivicCompletedPopup` is the only path that shows `Controls.ChangeGovernmentButton`. The button label switches between `LOC_GOVT_GOVERNMENT_UNLOCKED` (callback `OnChangeGovernment`) and `LOC_GOVT_CHANGE_POLICIES` (callback `OnChangePolicy`). Both globals call `Close()` themselves after firing `LuaEvents.TechCivicCompletedPopup_GovernmentOpenGovernments` / `_OpenPolicies`.
  - Quote text is written with `Controls.QuoteLabel:LocalizeAndSetText(quote)`, so reading `:GetText()` returns the already-localized string. `Controls.HeaderLabel`, `Controls.ResearchName`, `Controls.CivicMsgLabel`, and `Controls.UnlockCountLabel` are also pre-localized and safe to read live.
  - `OnInputHandler` returns `true` for all input, so CAI's wrapped handler must only delegate to vanilla after `mgr:HandleInput` rejects the event, and only when the CAI dialog is on top of the widget stack.
- `ProductionPanel.lua` queue selection is a single-item pick-up mode, not multi-select:
  - the real selected queue item is local `m_kSelectedQueueItem = { Parent, Button, Index }`
  - `OnItemClicked(parent, button)` selects when `Index == -1`, swaps with the clicked queue index when another item is selected, or deselects when clicking the same item
  - `HighlightButtons(true)` calls `SetSelected(true)` on current production and every valid queue slot, so `control:IsSelected()` means "queue selection mode is active", not "this exact row is selected"
  - `RefreshQueue(...)` starts with `DeselectItem()`, so queue rebuilds intentionally clear vanilla selection
  - build-queue index `0` is current production and indices `1` through `7` are the visible queued items; `ProductionHelper.SwapQueueItem(sourceIndex, destIndex)` accepts any non-negative pair and submits `CityOperationTypes.VALUE_SWAP`, so swapping indices `0` and `1` is the vanilla-supported way to exchange current production with the first queued item
  - CAI queue rows use that same index model for Shift+Up/Down. Current production is index `0`: Shift+Down swaps it with index `1`, Shift+Up reports that it is already first, and Shift+Up on queue index `1` swaps it with current production. Focus follows the moved item by translating the destination queue index back to the visible list index.
- Vanilla `ProductionPanel.lua` recommendation visuals are generic, not advisor-specific:
  - `pSelectedCity:GetCityAI():GetBuildRecommendations()` is read in `View(...)`, but vanilla stores only `BuildItemHash -> BuildItemScore` in `m_kRecommendedItems`
  - `PopulateGenericItemData(...)` checks whether `m_kRecommendedItems[kItem.Hash]` exists and then only calls `kInstance.RecommendedIcon:SetHide(false)`
  - there is no advisor-type lookup, no advisor-specific icon swap, and no recommendation color branch in the base production panel Lua
  - practical implication: any spoken advisor-type recommendation detail in CAI production would be an enhancement, not parity with a distinct vanilla recommendation color
- ProductionPanel CAI open/tab sync:
  - `LuaEvents.ProductionPanel_ListModeChanged(...)` is still the correct CAI sync point for ordinary vanilla tab changes because vanilla emits it directly from `OnTabChangeProduction`, `OnTabChangePurchase`, `OnTabChangePurchaseFaith`, `OnTabChangeQueue`, and `OnTabChangeManager`.
  - For delayed open, `LuaEvents.ProductionPanel_Open()` should be treated only as an "open pending" signal. Vanilla `Open()` fires it before the caller has necessarily selected the final tab.
  - The practical open handshake is therefore: mark CAI open-pending on `ProductionPanel_Open`, wait for the first real `ProductionPanel_ListModeChanged(...)`, then sync the CAI active tab from `m_CurrentListMode` and push the CAI panel.
  - Vanilla `ProductionPanel.OnInterfaceModeChanged(...)` closes the visual panel whenever the new mode is `CITY_MANAGEMENT` or `VIEW_MODAL_LENS`. Its `Close()` raises `LuaEvents.ProductionPanel_Close()` synchronously before the reverse animation eventually hides the context.
  - `ProductionPanel_CAI.OnPanelClosedCAI(...)` retains its CAI root only when the live interface mode is `DISTRICT_PLACEMENT` or `BUILDING_PLACEMENT`. Closes caused by `CITY_MANAGEMENT`, `VIEW_MODAL_LENS`, unit selection, or ordinary close paths remove it immediately. Leaving placement directly for `VIEW_MODAL_LENS` also removes it because `StrategicView_MapPlacement.lua` deliberately suppresses its ordinary production-reopen event for that transition.
  - A second open edge exists during vanilla's reverse animation. `Open()` does nothing until `ContextPtr:IsHidden()` becomes true, so an open request received after `ProductionPanel_Close` but before `OnHide()` finishes can be dropped. Final reconciliation must distinguish a panel that really reopened from one whose close animation finished.
  - Do not defer that reconciliation solely through `ProductionPanel`'s own `ContextPtr:SetUpdate(...)`. Civ VI stops updating this context after vanilla `OnHide()` calls `ContextPtr:SetHide(true)`. On a normal Production-tab district or wonder completion, the confirmation leaves enough time for the close animation to finish; `StrategicView_MapPlacement` then exits with `bWasCancelled=false` and vanilla intentionally does not reopen. Reconcile synchronously when the context is already hidden; use the animation-aware update path only while it remains visible. A cancelled placement or a queue/manager placement reopens the context and must retain the CAI panel and captured focus. The open side must continue waiting for vanilla's final `ProductionPanel_ListModeChanged(...)`, because vanilla selects the destination tab only after showing the context.
  - Every base-game visual close calls the single `Close()` function, directly or through `CloseAfterNewProduction()`. `Close()` is the only path that starts the dismiss animation and it always emits `LuaEvents.ProductionPanel_Close()` when accepted, so that event covers user close/Escape, production and purchase completion, placement entry, city management/modal lens entry, unit selection, city-panel close/purchase-plot requests, city-banner toggling, and refresh failures. Context shutdown is not a gameplay close and bypasses that event; CAI must remove its listeners and root from a re-registered wrapped `OnShutdown` handler.
  - Before a production building, wonder, district, or purchased district enters placement, CAI captures the focused production row through `mgr:CaptureFocusKey(panel)`. If vanilla refreshes the retained panel while the placement interface is still on top, CAI calls `mgr:PrepareFocus(panel, capture.key)` to seed the rebuilt subtree without stealing focus. Cancelling placement therefore returns to the same logical production item; if that item disappeared, normal manager fallback applies.
  - Tutorial-only whole-context input exemptions are detailed-mode state, not activation state. For the seven partial-production items, Civics Tree, and Government policies, CAI installs the required `UITutorialManager` always-receive-input registration after the advisor and any CAI screen-callout dialog have closed; deactivation cancels a pending registration and removes an active one. The exact-input handoff prevents the Enter key-up used to dismiss a dialog from reaching a newly focused Tree and toggling its first category.
  - `CityPanel.lua` treats `Controls.ChangeProductionCheck` as the toggle for the whole production-side panel family, not specifically the production chooser. `OnProductionPanelListModeChanged(...)` sets that check true for both `LISTMODE.PRODUCTION` and `LISTMODE.PROD_QUEUE`.
  - `ProductionPanel.OnCityPanelProductionOpen()` also defaults to the queue tab when `GetQueueSize(m_pCity) > 1` or auto-queue is enabled. Practical implication: a city-panel "change production" activation can land on queue mode first, and if CAI blindly toggles `ChangeProductionCheck` while queue mode already owns that check, the first activation just flips panel state instead of forcing the chooser tab.
  - Because this flow relies on vanilla's own later `m_tabs.SelectTab(...)`, CAI does not need to wrap and re-register the city/notification/tutorial open-entry LuaEvent handlers just to discover the final tab.
  - The CAI `TabControl` emits `value_changed` while its focus transaction is still running. A Queue tutorial raised directly from that listener can therefore receive focus and then immediately lose it when the tab control completes its own page-focus move. Queue tutorial checks must be scheduled through the context's next update and cleared if the Production panel closes; the deferred check is the last focus-affecting operation.
  - Vanilla only shows the shared current-production summary container on the Production tab and the Queue tab. `OnTabChangePurchase()` and `OnTabChangePurchaseFaith()` both hide `Controls.CurrentProductionContainer`, while `OnTabChangeProduction()` and `OnTabChangeQueue()` show it when `m_hasProductionToShow` is true.
  - CAI production refresh hooks should use the real engine events `Events.CityProductionChanged`, `Events.CityProductionUpdated`, and `Events.CityProductionQueueChanged`. The older speculative `CityWorkersChanged` hook is not documented in the IDE helpers and should not be relied on.
  - Vanilla category expanded state lives on the header-pair visibility, not a separate Lua flag: `OnExpand(instance)` hides `instance.Header`, shows `instance.HeaderOn`, and shows `instance.List`; `OnCollapse(instance)` does the reverse and only hides `instance.List` after the collapse animation finishes.
  - For CAI category widgets, treat that header visibility only as an initial-build hint. After the CAI category nodes are created, expand/collapse is ordinary CAI tree state and does not need ongoing sync back to vanilla headers.
  - `View(data)` repopulates more than one production list mode in a single pass. CAI category/header captures therefore need to be keyed by `listMode`; a single shared `wonderList` / `districtList` / `unitList` reference will be overwritten by later populate calls and can make category labels or expand/collapse callbacks point at the wrong vanilla header instance.
- Vanilla district / wonder placement UI:
  - `ProductionPanel.lua` enters placement mode with `UI.SetInterfaceMode(InterfaceModeTypes.DISTRICT_PLACEMENT, tParameters)` for districts and district purchases, or `UI.SetInterfaceMode(InterfaceModeTypes.BUILDING_PLACEMENT, tParameters)` for wonders.
  - Production-build district placement (`ZoneDistrict(...)`) explicitly closes the production panel immediately after entering `DISTRICT_PLACEMENT` by calling `Close()`. This also happens from queue mode; `m_isQueueOpen` stays true because `Close()` does not clear it.
  - Placement exit is routed through `StrategicView_MapPlacement.lua`, which fires `LuaEvents.StrageticView_MapPlacement_ProductionOpen(bWasCancelled)` when leaving district/building placement. `ProductionPanel.lua` reopens on the queue tab whenever `m_isQueueOpen` is still true, reopens on the manager tab whenever `m_isManagerOpen` is true, and in the normal non-queue production path only auto-reopens when placement was cancelled.
  - `PurchaseDistrict(...)` enters `DISTRICT_PLACEMENT` without calling `Close()` internally, but vanilla's purchase-row click callback immediately calls `Close()` after `PurchaseDistrict(...)` returns. CAI activates that live button through `DoLeftClick()`, so purchased-district placement follows the same retained-close and final-reconciliation lifecycle as production-build district/wonder placement.
  - `StrategicView_MapPlacement.lua` owns the in-world placement flow. It highlights owned valid plots as `Placement_Valid`, highlights purchasable valid plots as `Placement_Purchase`, and focuses the currently hovered plot through `RealizeCurrentPlaceDistrictOrWonderPlot()`.
  - Wonder placement uses the same hover-focus path as district placement: `RealizeCurrentPlaceDistrictOrWonderPlot()` unfocuses the previous placement hex and focuses the current one only when the hovered plot is one of the selectable placement hexes.
  - District placement shows more information than wonder placement. `DistrictPlotIconManager.lua` uses `AdjacencyBonusSupport.GetAdjacentYieldBonusString(...)` on each valid district plot and shows:
    - a centered bonus badge (`BonusText`) with the summarized result such as adjacency yields or housing
    - a tooltip on that badge containing the detailed adjacency / housing breakdown from `plot:GetAdjacencyBonusTooltip(...)`
    - an alert icon (`PrereqIcon`) when `requiredText` is returned, with the icon tooltip describing what prerequisite or rule still matters for that placement
    - border adjacency markers around neighboring plots from `AddAdjacentPlotBonuses(...)`, indicating which adjacent terrain, district, wonder, river, resource, etc. is contributing to the bonus
  - `AdjacencyBonusSupport.GetAdjacentYieldBonusString(...)` returns three values for a candidate district plot:
    - a short summary string (`iconString`), normally the net yields or housing
    - a detailed tooltip string (`tooltipText`), built from `plot:GetAdjacencyBonusTooltip(...)`
    - an optional requirement or warning string (`requiredText`)
  - Neighborhood-style districts (`OnePerCity == false`) are a special case: the summary is driven from appeal / housing rather than per-yield adjacency, and the tooltip text is replaced by `LOC_DISTRICT_ZONE_NEIGHBORHOOD_TOOLTIP`.
  - Wonder placement does not build the same bonus badge UI. `DistrictPlotIconManager.lua` only marks which plots are valid or purchasable for the wonder; `BonusText` is cleared and `PrereqIcon` is hidden for wonders.
  - `StrategicView_MapPlacement.ConfirmPlaceWonder(...)` builds the wonder confirmation popup from `LOC_DISTRICT_ZONE_CONFIRM_WONDER_POPUP`, then appends every `CityOperationResults.SUCCESS_CONDITIONS` entry returned by `CityManager.CanStartOperation(...)`. In practice, this means some of the most useful wonder-specific rule text is only visible at confirm time rather than on hover.
  - Wonder and district confirmation both call `CityManager.CanStartOperation(...)` (or `CityManager.CanStartCommand(...)` for purchased districts) with `testVisible=true`, then append every returned `SUCCESS_CONDITIONS` string into the confirmation popup text. Some explanatory placement text therefore appears only at confirm time, especially for wonders.
  - `PlotInfo.lua` filters the purchasable-plot overlay during district / wonder placement so only plots that both can be purchased and can legally host the selected district / wonder receive purchase buttons.
  - `CityPanel.lua` disables the normal city-management toggles during `DISTRICT_PLACEMENT`, hides the growth tile when leaving district or wonder placement, and still shows the growth tile during either placement mode if the pending district / wonder could legally be placed on the city's next-growth plot.
- `ProductionPanel.lua` tab-open sequencing matters:
  - `CreateCorrectTabs()` always initializes vanilla tabs by calling `m_tabs.SelectTab(m_productionTab)` once during setup.
  - Specific city-panel open handlers then switch to the requested tab after `Open()`, for example `OnCityPanelPurchaseGoldOpen()` -> `Open()` -> `m_tabs.SelectTab(m_purchaseTab)` and `OnCityPanelPurchaseFaithOpen()` -> `Open()` -> `m_tabs.SelectTab(m_faithTab)`.
  - The reliable CAI sync point is `LuaEvents.ProductionPanel_ListModeChanged(listMode)`, because the original vanilla tab callbacks always emit it after the real tab switch.
  - CAI should treat `ProductionPanel_ListModeChanged` as the source of truth for the currently active vanilla tab, especially on open where the final vanilla tab choice happens after `Open()`.
  - CAI tab widgets should resolve the live vanilla tab controls (`m_productionTab`, `m_purchaseTab`, `m_faithTab`, `m_queueTab`) instead of assuming `Controls.ProductionTab` / `PurchaseTab` / `PurchaseFaithTab` / `QueueTab` are the active controls, because `CreateCorrectTabs()` may swap in the mini-tab controls before any open handler runs.
- `ProductionManager.lua` is the separate multi-queue UI context opened by `ProductionPanel.lua` Manager tab:
  - `OnTabChangeManager()` calls `OpenManager()`, which broadcasts `LuaEvents.ProductionPanel_OpenManager()`
  - `ProductionManager.lua` listens for `ProductionPanel_OpenManager`, `ProductionPanel_CloseManager`, `ProductionPanel_ProductionClicked`, and `ProductionPanel_CancelManagerSelection`
  - it owns `Controls.FilterPulldown`, `Controls.CityStack`, per-city `CityInstance` rows, per-city `CurrentProductionGrid`, per-city queue slots, and per-city `TrashButton`
  - `SetupFilters()` creates sort entries with `Controls.FilterPulldown:BuildEntry("FilterItemInstance", controlTable)`; each entry button calls `Refresh(sortFunc, name)`
  - built-in sort filters are founding order, city name, and population
  - CAI now folds the non-queue manager affordances into `ProductionPanel_CAI.lua` instead of replacing the separate manager screen: the production-panel CAI root has sibling widgets for the existing production `TabControl`, a local-player city `List`, and a `Sort by` dropdown with founding, name, population, and per-yield options. Focusing a city row calls `UI.SelectCity(city)` only when it is not already selected, letting vanilla city-selection refresh rebuild the active production page.
  - The panel root label resolves the current `UI.GetHeadSelectedCity()` name live. Before the initial push, `mgr:PrepareFocus(cityList, selectedCityFocusKey)` seeds the selected city's stable owner/id `FocusKey` as the list's remembered child, while the push itself still focuses the active production tree. City-row yield tooltips and yield sort options enumerate `GameInfo.Yields()` and read current values through `City:GetYield(yield.Index)`; yield sorts are descending with localized city name as the stable tie-breaker.
  - The production-panel CAI root also owns panel-local `Alt+Left` / `Alt+Right` widget bindings for previous/next city. These step through the same currently sorted city list used by the city `List`, wrap at the ends, and seed the city list's internal remembered focus key through `mgr:PrepareFocus(list, cityFocusKey)` before selecting the city. The next city-list rebuild consumes that pending key instead of restoring a stale captured row, so refresh focus restoration stays aligned with the newly selected city.
  - CAI current-production reads are shared: `GetCurrentProductionItem(city)`, `HasActiveCurrentProduction(city)`, and `ReadCurrentProductionLabel(readTurns, city)` accept an optional city. The selected-city path still reads live vanilla controls for status text, while non-selected city-list rows reuse the same item helper instead of duplicating production-name lookup. Do not use `ipairs(GameInfo.Units)` or similar: those collections are userdata in-game and raise `bad argument #1 to 'pGlobal_ipairs'`.
- `DeclareWarPopup.lua` is the dedicated declare-war confirmation popup used by diplomacy, city-state, Civ6Common, and world-input flows:
  - vanilla open routes are `LuaEvents.DiplomacyActionView_ConfirmWarDialog`, `LuaEvents.CityStates_ConfirmWarDialog`, `LuaEvents.Civ6Common_ConfirmWarDialog`, and `LuaEvents.WorldInput_ConfirmWarDialog`
  - `OnShow(eAttackingPlayer, kDefendingPlayers, eWarType, confirmCallbackFn)` populates the live popup state, including `Controls.Message`, `Controls.Targets`, the visible consequence containers, and the `Yes` callback
  - when `confirmCallbackFn` is nil, vanilla builds a default callback that loops defending players and calls `DeclareWar(...)` for each target
  - visible consequence sections are `WarmongerContainer`, `DefensivePactContainer`, `CityStateContainer`, `TradeRouteContainer`, and `DealsContainer`; hidden sections should be skipped by CAI speech
- `Assets/UI/PartialScreens/CityStates.lua` keeps the send-envoys list and confirm drawer alive in one screen rather than opening a second modal. `OnConfirmPlacement()` submits the pending envoy operations, clears the vanilla pending-change table, and either closes the screen when no envoys remain or leaves the list open for more assignments. CAI mirrors this as a sortable overview `DataTable` followed directly by the vanilla-backed controls. Rebuilds must preserve the selected city-state even while focus is on a following control; otherwise the slider becomes hidden for one tab step because its visibility is keyed off selection, not table focus.
  - Table view is the default and contains the complete comparison data without a separate selected-state details tree. The alternate tree view begins with an Order city-states by dropdown and retains the original all-city-states hierarchy, including its Influence, Bonuses, Quests, and Relationships sections. The final button switches to the other view; `m_ui.panel:AddInputBindings(...)` binds `Alt+1` to table view and `Alt+2` to tree view within the City-States input context. No WorldInput action or bookmark coordination is involved.
  - Shift+Left/Right adjusts pending envoys from any overview data cell or any row/descendant in the alternate all-city-states tree. Ctrl+Enter confirms pending envoy changes from those same contexts.
  - The selected mode is profile-global internal CAI state stored with `CAI.GetConfigValue("UI", "CityStatesViewMode", "table")` and `CAI.SetConfigValue(...)`; only `table` and `tree` are accepted, so an absent or invalid value safely resolves to the default table view. The mode is not a public Mod Settings row.
  - The fixed overview columns are city-state, type, Your relationship, Envoys, current suzerain, Active quests, and Foreign relationships. Foreign relationships is the final fixed column immediately before the dynamic influence columns. Envoys appends the complete live 1-, 3-, 6-envoy, and Suzerain bonus breakdowns immediately after the local envoy count. The table's Suzerain entry uses only `GetSuzerainBonusText(...)`: that vanilla breakdown already contains the unique bonus and current resources, so appending `GetSuzerainUniqueBonusAndResources(...)` would repeat both. The alternate tree retains its existing detail builder. Active quests appends only each quest description after the count; quest titles and rewards remain in the alternate tree. Envoys and quests retain numeric count-based sorting.
  - The tree sort dropdown is generated from the same complete live column array as the table: natural order comes first, followed by both semantically named directions for every fixed and dynamic sortable column. Examples include city-state/type/suzerain A-to-Z, relationship at-war-first or Suzerain-first, fewest/most counts, and lowest/highest influence. It uses each column's exact `sortKey`, direction-description tags, DataTable comparison rules, nil-last behavior, and natural-provider stability. Table-header and tree-dropdown changes update one shared in-memory sort. If a selected dynamic influence column disappears during refresh, both views fall back to natural order and the dropdown focus returns to its root rather than a destroyed option.
  - Foreign relationships combines met non-local major civilizations and met city-states into one cell, with no type grouping. Entries in any of vanilla's three city-state war states come first and are alphabetical; every remaining entry follows alphabetically. The cell excludes the selected city-state's self-entry, and its column sorts rows by the number of visible foreign relationships. `GetRelationships()` records do not retain player ids, but both arrays preserve the corresponding alive-player iteration order while omitting only players whose diplomatic-state index is `-1`. CAI repeats that exact id/state-index walk once to attach stable `PlayerID` values before applying local-player and self-entry exclusions; displayed names are never used as identity.
  - Each met foreign major with positive influence in at least one visible city-state contributes a stable `influence:<playerID>` column whose live value and sort key are that major's envoys in the row's city-state. Positive influence from unmet majors is represented without identities in one `influence:unmet` column: the cell speaks each anonymous count in descending order and the column sorts by their total. The local player's influence is the fixed Envoys column rather than another dynamic influence column. Foreign governors assigned to a city-state are appended only in known-major cells, preserving the same unmet-player privacy boundary as vanilla.
  - Vanilla relationship content is met-only at render time. `ViewRelationships(...)` passes both stored relationship arrays into `RefreshRelationshipStack(...)`, but that function only instantiates entries where `kRelationship.HasMet` is true. CAI relationship children should therefore skip unmet major civs and unmet city-states instead of speaking placeholder `Unmet civ` rows.
  - CAI should also suppress the city-state's self-entry when mirroring the city-state relationship stack. The raw relationship arrays can include the selected city-state itself, but that does not make sense as a spoken relationship row and causes bogus readouts such as `{city-state}, {city-state} is at war with {city-state}`.
  - `OnClose()` hides every consequence container and the context itself, and `Events.LocalPlayerTurnEnd` also closes the popup
  - CAI can safely wrap `OnShow(...)`, `OnClose()`, and `OnInputHandler(...)`, then rebuild a transient dialog from live control text instead of rebinding the popup's Lua event registrations
- `FiraxisLive/My2K.lua` is a front-end popup context hosted under `/FrontEnd/MainMenu/My2K`:
  - Vanilla uses a legacy 3-argument `InputHandler(uiMsg, wParam, lParam)` and `ContextPtr:SetShowHideHandler(ShowHideHandler)`.
  - CAI can replace that input handler with the extended `InputStruct` form after vanilla setup and preserve Escape behavior by checking `input:GetMessageType()`, `input:GetKey()`, `m_bESCEnabled`, and `m_cancelFunction`.
  - Dialog state is selected by `m_currentDialogID` and created by `ShowHideHandler()` through `Create2KMainMenu`, `CreateLoginDialog`, `CreateNewUserDialog`, `CreateUserNameDialog`, `CreateLegalDialog`, `CreateLegalItemDialog`, `CreateMessageDialog`, `CreateLogoutDialog`, and `CreateUnlinkConfirmationDialog`.
  - My2K dialog instances are XML-backed and short-lived through `InstanceManager`; accessible replacements should rebuild from the live `m_currentDialog` instance after each `Create*Dialog()` call.
  - The shared `WidgetTemplateHelpers:MakeGeneralDialog(titleFunc, actionButtons, dlgContent)` helper is the preferred CAI scaffold for these modal dialogs.
- `CityPanel.lua` exposes the live selected-city data table as local `m_kData` inside the included chunk, so a wrapper can read the same live data after wrapping `Refresh()`.
- `CitySupport.lua` exposes `GetCityData(city)`, and `CityPanel.lua` includes `CitySupport`, so `CityPanel_CAI.lua` can request fresh city data without wrapping `Refresh()`.
- `CityManager.GetCity(playerID, cityID)` is available in UI context and can resolve a city for helper-based city info requests.
- `CityPanel_CAI.lua` now extends `ExposedMembers.CAIInfo` with:
  - `CityInfo` helper table. Current single-purpose city-panel helpers are `Name`, `Population`, `Health`, `Growth`, `BorderGrowth`, `Production`, `Housing`, `Religion`, `BuildingsOrLoyalty`, `VisibleYields`, `NormalFocusYields`, `FavoredFocusYields`, and `IgnoredFocusYields`
  - `CityInfoBuckets`, which define the current main city-panel read buckets:
  - summary / tilde: direction from the navigation cursor to the selected city on its own speech line, followed by `Name`, `Population`, `Health`, `Production`, `Growth`
    - `Shift+1`: `Name`, `Health`
    - `Shift+2`: `Production`
    - `Shift+3`: `Growth`
    - `Shift+4`: `BorderGrowth`
    - `Shift+5`: `Religion`
    - `Shift+6`: `Population`, `Housing`, `BuildingsOrLoyalty`
    - `Shift+7`: `VisibleYields`
    - `Shift+8`: `NormalFocusYields`
    - `Shift+9`: `FavoredFocusYields`
    - `Shift+0`: `IgnoredFocusYields`
  - `RequestCityInfo(cityOrCityID, requestedKeys, playerID)` which defaults to the currently selected city when no city is passed
- `CityPanel_CAI.lua` now builds city info from `GetCityData(city)` using the same vanilla loc keys / string assembly patterns as `CityPanel.lua`; it does not read back from UI controls or call `ViewMain(data)`.
- The city-selection speech path is now consistent with manual summary reads: `Events.CitySelectionChanged` requests the same summary bucket instead of a separate summary helper. Its existing `LuaEvents.CAICursorMoveTo(plotId, "select")` call is gated live by the enabled-by-default `AutoMoveCursorToSelectedCity` setting. Direction is not part of the reusable summary bucket: only the `ReadSelectionSummary` input handler prepends the live cursor-to-city direction as its own speech line, so selection-change announcements and other bucket consumers remain unchanged.
- City growth / production helpers can also expose the visible progress-bar state from `GetCityData(city)`:
  - growth: `CurrentFoodPercent`, `FoodPercentNextTurn`
  - production: `CurrentProdPercent`, `ProdPercentNextTurn`
- City panel border-growth speech now uses the city-panel growth tile as parity target: it speaks the target hex direction relative to the city via `CAIHexCoordUtils.directionString(...)` plus the same live culture values vanilla uses for the tile meters (`GetNextPlot()`, `GetCurrentCulture()`, `GetCultureYield()`, `GetNextPlotCultureCost()`, `GetTurnsUntilExpansion()`).
- Main `CityPanel` parity rules currently mirrored by CAI:
  - the old city coords helper was removed from normal city-panel reads
  - border growth is a dedicated helper built from `city:GetCulture()` (`GetNextPlot()`, `GetNextPlotCultureCost()`, `GetCurrentCulture()`, `GetCultureYield()`, `GetTurnsUntilExpansion()`)
  - XP1 / XP2 main-panel first-slot parity uses `IsExpansion1Active()` / `IsExpansion2Active()` from `Civ6Common.lua`; when an expansion ruleset is active CAI speaks current loyalty from `city:GetCulturalIdentity():GetLoyalty()`, otherwise it speaks building count like base vanilla
- `UnitPanel.lua` owns selected-unit panel data and action button construction:
  - `ReadUnitData(unit)` builds the panel data table from the live unit, including name, type, movement, health, charges, promotions, abilities, stats, and `Actions`.
  - `GetSubjectData()` returns the current selected-unit data table cached by `View(data)`.
  - `GetUnitActionsTable(unit)` builds `data.Actions` with vanilla action order, disabled state, tooltip/failure text, callback function, callback void values, and optional sound.
  - Vanilla unit commands and operations use loose `UnitManager.CanStartCommand(...)` / `CanStartOperation(...)` checks to decide whether an action should be visible, then stricter current-executability checks where needed. If the stricter check or tutorial gating fails, the row can still be added with `Disabled = true` and failure reasons appended to `helpString`.
  - `data.Actions.displayOrder.primaryArea` and `secondaryArea` define the normal unit-panel action order. Build actions live in `data.Actions["BUILD"]`.
  - Vanilla does not mix build-improvement actions into the ordinary action stacks. `View(data)` renders `data.Actions["BUILD"]` separately through `BuildActionsStack`, and `RecommendedActionButton` is just a second view over the single `BUILD` entry whose row has `IsBestImprovement = true`.
  - `AddActionToTable(...)` also populates vanilla `m_kHotkeyActions` for actions with `HotkeyId`; CAI should let vanilla continue handling directly bound unit operation / command keys.
  - Combat preview is a live hover pipeline:
    - `OnInterfaceModeChanged(...)` is the attack-preview mode-entry path for `CITY_RANGE_ATTACK` and `DISTRICT_RANGE_ATTACK`. It makes the panel visible, swaps the subject view to the attacker city-center district or district, and calls `OnShowCombat(...)`.
    - `OnInputHandler(...)` calls `InspectWhatsBelowTheCursor()` on every `MouseMove` while the panel is processing input and no unit flag has focus.
    - `InspectWhatsBelowTheCursor()` checks `UI.GetCursorPlotID()` against `m_plotId`; when the cursor enters a new visible plot it calls `InspectPlot(plot)`, otherwise it does nothing.
    - `InspectPlot(plot)` is the main plot-hover preview entry point. It calls `GetCombatResults(...)`, then `ReadTargetData(attacker)`, `CanShowCombat()`, and `OnShowCombat(isValidToShow)`.
    - `OnUnitFlagPointerEntered(playerID, unitID)` is the unit-flag-hover entry point. It simulates direct combat against the hovered defender with `CombatManager.SimulateAttackVersus(...)`, then calls `ReadTargetData(attacker)` and `OnShowCombat(isValidToShow)`.
    - `OnUnitFlagPointerExited(...)` clears flag focus, resets `m_plotId`, and reruns `InspectWhatsBelowTheCursor()` so preview falls back to the plot under the cursor.
    - `GetCombatResults(attacker, x, y)` skips duplicate attacker+plot requests by caching `m_attackerUnit`, `m_locX`, and `m_locY`. It uses `CombatManager.SimulateAttackInto(...)` normally and `SimulatePriorityAttackInto(...)` in `PRIORITY_TARGET`.
    - `ReadTargetData(attacker)` populates `g_targetData` from `m_combatResults` for unit, district, and improvement / plot defenders, including interceptor and anti-air side data when present.
    - `OnShowCombat(showCombat)` is the final display gate. If `m_combatResults` is nil it hides preview; otherwise it renders the attacker/target combat UI from the current live state.
    - CAI combat-preview speech reads visible UnitPanel controls for names, strength numbers, assessment, and modifier text. Since subject/target combat stat icons are image controls, CAI appends localized stat labels for the main attacker/defender strengths by mirroring vanilla's `COMBAT_TYPE` icon-selection logic. Interceptor and anti-air strength sections use only their visible numeric labels. Damage comes from vanilla `GetCombatPreviewResults()` / `CombatResultParameters` rather than health meters, because UnitPanel meter controls are write-only for this purpose.
    - As of 2026-05-18, CAI preview damage speech now uses unsigned dealt-damage values. For walled-city targets, `GetTargetPreviewDamageText()` speaks city damage first and appends wall state in the same target clause: ordinary wall hits append the wall damage amount, while a wall-breaking hit appends `destroys walls` using the same `FINAL_DEFENSE_DAMAGE_TO >= MAX_DEFENSE_HIT_POINTS` check CAI uses for post-combat wall-destruction wording.
    - `Events.Combat(combatResults)` fires after combat resolves and reuses the same hashed `CombatResultParameters` table shape as preview simulation. CAI can read `ATTACKER`, `DEFENDER`, `INTERCEPTOR`, `ANTI_AIR`, `COMBAT_TYPE`, `LOCATION`, `ATTACKER_ADVANCES`, `DEFENDER_CAPTURED`, `DEFENDER_RETALIATES`, `LOCATION_PILLAGED`, `DAMAGE_TO`, `FINAL_DAMAGE_TO`, `DEFENSE_DAMAGE_TO`, `FINAL_DEFENSE_DAMAGE_TO`, `MAX_HIT_POINTS`, `MAX_DEFENSE_HIT_POINTS`, and the nested `ID` payloads from that event without depending on UnitPanel preview visibility.
    - Nested combatant ids in the results table use the same component-id shape UnitPanel reads from `CombatResultParameters.ID`: `id.player`, `id.id`, and `id.type`. Vanilla resolves unit ids with `UnitManager.GetUnit(id.player, id.id)` and district ids with `Players[id.player]:GetDistricts():FindID(id.id)`. Plot-only targets fall back to `CombatResultParameters.LOCATION`.
    - Preview-only text arrays such as `PREVIEW_TEXT_TERRAIN`, `PREVIEW_TEXT_HEALTH`, `PREVIEW_TEXT_OPPONENT`, `PREVIEW_TEXT_MODIFIER`, `PREVIEW_TEXT_ASSIST`, `PREVIEW_TEXT_PROMOTION`, `PREVIEW_TEXT_DEFENSES`, `PREVIEW_TEXT_RESOURCES`, `PREVIEW_TEXT_INTERCEPTOR`, and `PREVIEW_TEXT_ANTI_AIR` are useful for verbose preview breakdowns, but a post-combat summary can ignore them and instead derive concise spoken outcomes from resolved names plus `DAMAGE_TO` / `FINAL_*` / capture-pillaged flags.
  - `src/data/unitOperationConfig_CAI.sql` assigns missing `HotkeyId` values for visible vanilla `UnitOperations` and visible vanilla `UnitCommands`. Unit-command action ids use the `UnitCommand...` prefix to avoid colliding with operation action ids such as `Upgrade`.
  - `UNITCOMMAND_DELETE` is intentionally not assigned through `UnitCommands.HotkeyId`: vanilla already exposes the `DeleteUnit` input action and separately special-cases it in `OnInputActionTriggered`, so adding it to `m_kHotkeyActions` could double-call the delete prompt.
- `UnitPanel_CAI.lua` extends `ExposedMembers.CAIInfo` with `RequestUnitInfo(unitID, requestedKeys, playerID)`, defaults to `UI.GetHeadSelectedUnit()`, and uses the same `ReadUnitData` / `GetSubjectData` data rather than reimplementing unit state.
- UnitPanel keeps `local CAICursor = ExposedMembers.CAICursor` at context scope rather than making the flat-list tooltip own the screen's cursor reference. That tooltip reads the reference through `GetCoords()`, adds the result of `CAIHexCoordUtils.directionString(...)` first, then requests the shared `Summary` bucket and omits its first `UnitName` result because the list-row label already supplies the name. Table cells do not use this summary tooltip. The authoritative summary sequence is `UnitName`, `Activity`, `NextWaypoint`, `Health`, `Moves`, `Stats`, `AdjacentEnemies`, `Charges`, `UpgradeHint`, `Promotions`, and `BuilderRecommendation`; empty stats are omitted. `Stats` and `AdjacentEnemies` therefore precede the level, experience, and promotion details contained in `Promotions`. Unit selection requests movement through the unified `LuaEvents.CAICursorMoveTo(plotId, "select")` path only while the enabled-by-default `AutoMoveCursorToSelectedUnit` setting is enabled. Only the `ReadSelectionSummary` input handler prepends the live cursor-to-unit direction as its own speech line; the reusable `Summary` key and automatic selection speech remain unchanged.
- UnitPanel adjacent-enemy speech scans the six `Map.GetAdjacentPlot(...)` results and counts units, not occupied plots. It follows Surveyor disclosure and relationship rules: the plot must be visible, the unit must pass `PlayersVisibility[observer]:IsUnitVisible(unit)`, and the owner must be barbarian or a met civilization currently at war with the local player. `Shift+0` requests this `AdjacentEnemies` bucket, and the same live count is the final unit-summary field.
- The accessible Units List is a `Panel` labeled with vanilla `LOC_TECH_FILTER_UNITS` (`Units`), with a default sortable `DataTable`, an alternate flat `List`, one shared category filter, a list-only sort dropdown, and a final switch-view button. The table and list labels resolve live to the selected filter label (`All units`, `Military`, `Naval`, `Air`, `Support`, `Civilian`, or `Trade`). It has no category tree and no Category table column. Alt+1 selects table view and Alt+2 selects list view. The panel stores stable `{ PlayerID, UnitID }` records and re-resolves every live label, value, tooltip, state, sort key, and action.
- Units List columns are Name, cursor-relative Direction/Distance, Activity, current Health, Current moves, Maximum moves, Melee strength, Ranged strength, Bombard strength, Religious strength, Anti-air strength, Range, Charges, earned Promotions, and Adjacent enemies. Current health is a bare number rather than the UnitPanel's labeled current/maximum tooltip string. Every resolved numeric value, including zero strength, range, and charges, is displayed as a number. Each strength column reads its matching live `Unit` getter; do not combine unrelated combat stats with `math.max`. Current and maximum movement are independent numeric sort keys. Adjacent enemies reuses the visibility-safe summary counter. The list sort dropdown is generated from these same column definitions and mirrors natural plus the table headers' semantic directions: A-to-Z, nearest/farthest, ready/not-ready, lowest/highest, weakest/strongest, or fewest/most as appropriate. The shared filter offers All, Military, Naval, Air, Support, Civilian, and Trade.
- This independent-strength rule follows Civ V Access's `CivVAccess_MilitaryOverviewAccess.lua`: its Strength cell/sort reads `GetBaseCombatStrength()` directly, its Ranged cell/sort independently reads `GetBaseRangedCombatStrength()`, and `combatNumberCell` returns `tostring(n)` so zero is spoken as zero. Civ VI's equivalent live getters are `GetCombat()` and `GetRangedCombat()`; because Civ VI's vanilla UnitPanel additionally exposes Bombard, Religious, and Anti-Air as distinct stats, CAI applies the same one-getter/one-column rule to `GetBombardCombat()`, `GetReligiousStrength()`, and `GetAntiAirCombat()` rather than dropping or combining them.
- Units List sort keys must not call `ReadUnitData(unit)`. Vanilla `ReadUnitData` also builds the complete unit-action table, promotions, great-person data, trade-route data, abilities, and custom scenario stats, making even one call per row too expensive for a table sort and repeated comparator calls much worse. Charges instead read the same live primitive APIs used by vanilla (`GetBuildCharges`, `GetDisasterCharges`, `GetSpreadCharges`, `GetReligiousHealCharges`, `GetActionCharges`, great-person action charges, park charges, and the Black Death property fallback). Activity reads `UnitManager.GetActivityType`, fortify state, readiness, and the unit definition's Spy/MakeTradeRoute flags directly.
- Both Units List sort paths decorate rows with one computed key before `table.sort`, matching `DataTableWidget:_SortRows`; comparators never invoke a live getter. State refresh rebuilds only the active table or list, and switching modes rebuilds the destination view before focus restoration. A table-header sort only synchronizes the hidden list's dropdown state rather than rebuilding the hidden list.
- Units List Activity sorting adapts Civ V Access's three-band `unitStatusSortKey` to Civ VI's player-facing readiness semantics. Ascending order is Ready (`1`), named activity states (`5`), then Not Ready (`9`); descending reverses that order. Named states are Embarked, Automated, Healing, Sleeping, Skip Turn, Fortified, and queued Moving. Localized status groups break ties, and equal-status rows retain the unit provider's stable order. Crucially, sort rank and displayed Activity use the same `unit:IsReadyToMove()` fallback. Traders on active routes, spies on assignments, ordinary units with no available action, and atypical units with retained movement therefore all remain together under Not Ready instead of receiving contradictory hidden ranks. Conversely, a zero-movement ranged unit that vanilla still considers ready for an action stays with Ready. Sentry, Intercept, and generic `ACTIVITY_OPERATION` are not hidden middle-band signals because CAI does not expose matching player-facing labels for them.
- Explicit Civ VI status text reuses existing localization: vanilla Ready (`LOC_READY_BUTTON`), Not Ready (`LOC_NOT_READY`), Automate, Healing, and Skip Turn, plus existing CAI Embarked, Sleeping, and Fortified. The fallback display uses `unit:IsReadyToMove()`, not raw movement: an ongoing trader can retain movement points but correctly report Not Ready because it is not awaiting player orders. `LOC_CAI_UNIT_ACTIVITY_MOVING` is the only new general activity-status localization.
- A valid `UnitManager.GetQueuedDestination(unit)` result identifies Moving without running the pathfinder. `CAI_GetUnitActivityStatus(unit)` checks that after heal/sleep/hold/fortify but before its `IsReadyToMove()` fallback. Sentry and Intercept are intentionally not spoken: although `InGameText.xml` defines labels for them, vanilla references those labels only from a commented debug block in `UnitFlagManager.lua`, not player-facing UI. Generic `ACTIVITY_OPERATION` likewise falls through to Ready/Not Ready, avoiding an invented Busy label. Neither is assigned a hidden active sort rank. The shared Activity helper feeds selection summaries, Shift+3, flat-list summaries, and the table Activity cell; `CAI_GetUnitActivitySortRank(unit)` supplies the matching three-band sort rank. Detailed queued-path/waypoint speech continues to use the fresh `GetQueuedPathSnapshot` service separately.
- Activity exposes an ordinary numeric DataTable/list sort key: `numericBand * 100 + localizedStatusOrdinal`. Status ordinals are built once by sorting the finite set of displayed activity labels through `Locale.Compare`. Do not concatenate a numeric prefix into a localized string, and do not return a Lua table as a sort value: either can bypass the scalar comparison contract and interleave rows. The numeric key guarantees contiguous bands and contiguous equal-status groups in both the existing DataTable and flat-list comparators.
- Units List table rows and list items bind Ctrl+Enter to re-resolve the unit and send its live plot through `LuaEvents.CAICursorMoveTo(plotId, "jump")`. This leaves the panel open and does not call `UI.SelectUnit`; plain Enter retains select-and-close, table Space activates its row, and Shift+Enter opens the unit type in the Civilopedia. The flat-list item uses the live Summary tooltip without repeating the row name. Following the Reports City Status and City-States table pattern, ordinary table cells announce only their column value: Name is only the unit name, and Direction alone adds a numeric-distance tooltip.
- Combat-result visibility always admits results containing a local-player attacker, defender, interceptor, or anti-air component, even when the location is no longer visible. Other results remain limited to locations currently visible to the local player. The default-enabled Events setting `AnnounceUnownedCombatResults` preserves those visible non-local results; disabling it requires one of the four combat components to belong to the local player. Component ownership comes from `CombatResultParameters.ID.player`, which covers unit, city, and district component ids without resolving a possibly destroyed object after combat.
- UnitPanel bucket output and all comma-based concatenation use `[NEWLINE]` as the separator. Space-based joins remain for values that form one phrase rather than separate information fields.
- `UnitPanel_CAI.lua` is close to Civ VI Lua's per-function register limit because the entire replacement file compiles as one top-level chunk. Keep the required context-scope `CAICursor` reference, but do not add top-level locals merely for separator constants or Units List state/helpers. The table/list implementation uses the collision-safe global `CAIUnitList` namespace so its methods compile as separate functions instead of consuming registers in the replacement chunk. Exceeding the limit reports `Function or expression requires too many registers (too complex)` near the end of the file even when added locals appear much earlier.
- UnitPanel transient cleanup uses early local functions `RemoveUnitList()`, `RemoveSimplePromotionList()`, and `RemoveUnitNamePanel()`. Each conditionally calls `mgr:RemoveFromStack(widgetId)` when its widget reference and manager exist, then unconditionally clears its stored references. Define these functions directly before their first callers; do not forward-declare names and assign function values later.
- `UnitPanel_CAI.lua` handles the shared selection info inputs (`~`, `Shift+1` through `Shift+0`) when a unit is selected and opens a transient action list from the existing `SelectionActions` input.
  - CAI unit action-list labels append bound gestures after `: ` by mapping each vanilla action row's `userTag` hash back to its `UnitOperations.HotkeyId` or `UnitCommands.HotkeyId`, then reading `Input.GetGestureDisplayString(actionId, 0/1)`.
  - The standalone view-abilities action id is `UnitViewAbilities`; keep Lua `Input.GetActionId(...)`, `hotkey_config_CAI.xml`, and default gestures on that exact id. `UI_UnitViewAbilities` is not a registered input action.
  - `CAIDeleteUnit` replaces vanilla's `DeleteUnit` input binding for CAI defaults. It must still call vanilla `OnPromptToDeleteUnit()`, not request deletion directly, so the `CanStartCommand(... DELETE ...)` check, confirmation dialog, `m_DeleteInProgress` guard, and `OnDeleteUnit(unitID)` lens cleanup remain vanilla-owned. The action-list binding display special-cases `UNITCOMMAND_DELETE` to `CAIDeleteUnit` because vanilla intentionally does not assign `UnitCommands.HotkeyId` for delete.
  - Current unit bucket mapping is:
    - `Shift+6` -> unit stats
    - `Shift+7` -> unit abilities
    - `Shift+8` -> special unit info (spy, trader, rock band, great person passive)
    - `Shift+9` -> queued movement path
    - `Shift+0` -> visible enemy units in adjacent hexes
  - `Shift+5` promotion info speaks level / XP progress first, then each earned promotion as `{Name}: {Description}` using `data.CurrentPromotions.Name` / `.Desc`, matching the earned-promotion icon tooltip content sighted players can inspect on the vanilla panel.
  - CAI command-hash lookups should use full DB command keys such as `GameInfo.UnitCommands["UNITCOMMAND_PROMOTE"]`, not short names like `"PROMOTE"`. The selection action list labels promote rows as `LOC_HUD_UNIT_CHOOSE_PROMOTION_TEXT` and adds a synthetic promote row only when live `UnitManager.CanStartCommand(unit, UnitCommandTypes.PROMOTE, true, true)` returns non-empty promotion choices but the cached vanilla action data did not include a promote row.
  - Summary speech reuses the same helper pipeline, inserts upgrade availability before promotions, adds builder recommendation and settler water guide, includes stats only when present, omits abilities, and ends with adjacent enemies.
  - Unit stats should skip combat speech for spies and ordinary civilians, use trade-route name plus land/sea ranges for traders, and keep normal combat/religious stat speech for units that actually expose those stats.
  - Summary-only builder recommendation should prefer the live `RecommendedActionButton` tooltip text, and settler water guide should prefer the live UnitPanel settler-water control tooltips and header text.
- The Units List table/list panel architecture, shared sorting model, category filtering, focus rules, and verification matrix are in `docs/unit-list-view-plan.md`.
- Vanilla shows or refreshes combat preview in these cases:
  - entering city or district ranged-attack mode
  - moving the cursor onto a different visible plot while a valid attacking unit, district, or city attack context is active
  - moving the cursor onto an enemy unit flag while a valid attacking context is active
  - leaving an enemy unit flag, which immediately re-evaluates the plot under the cursor
- Vanilla `WorldInput.lua` attack-related interface modes:
  - `RANGE_ATTACK` executes `UnitOperationTypes.RANGE_ATTACK`; it shows valid target plots on the attack-range lens with attack arcs from the unit, focuses a valid target hex on mouse move, and enables UnitPanel combat preview on hover/flag focus.
  - `CITY_RANGE_ATTACK` and `DISTRICT_RANGE_ATTACK` execute `CityCommandTypes.RANGE_ATTACK`; they show valid target plots on the attack-range lens with arcs from the city center or district, focus valid target hexes on mouse move, and enable UnitPanel combat preview. UnitPanel also swaps the subject display to the attacking city-center district or district on mode entry.
  - `AIR_ATTACK` executes `UnitOperationTypes.AIR_ATTACK`; it highlights valid target plots on `Hex_Coloring_Attack`. UnitPanel records explicit air-attack target plots and can show combat preview for hovered unit, district, improvement, or owned plot targets, including interceptor and anti-air data when present.
  - `WMD_STRIKE` executes `UnitOperationTypes.WMD_STRIKE`; it highlights valid strike plots with attack-range-style target arcs, but UnitPanel `CanShowCombat()` explicitly suppresses combat preview. Selection shows only the targeting lens plus the launch / war confirmation flow.
  - `ICBM_STRIKE` executes `CityCommandTypes.WMD_STRIKE`; it highlights valid strike plots with attack-range-style target arcs and uses the launch / war confirmation flow. Vanilla WorldInput does not pair it with a UnitPanel combat-preview subject path.
  - `COASTAL_RAID` executes `UnitOperationTypes.COASTAL_RAID`; it highlights valid raid plots on `Hex_Coloring_Attack` and may trigger war confirmation. WorldInput itself does not add target details beyond the lens.
  - `InterfaceModeTypes.ATTACK` is allocated in the handler table but has no vanilla enter/leave/mouse mappings in `WorldInput.lua`.
  - Several non-attack modes reuse `CursorTypes.RANGE_ATTACK` or target-plot highlighting (`DEPLOY`, `REBASE`, `TELEPORT_TO_CITY`, `FORM_CORPS`, `FORM_ARMY`, `AIRLIFT`, `SACRIFICE_SELECTION`, hero `KILL_WEAKER_UNIT`, `TRANSFORM_UNIT`, `RESTORE_UNIT_MOVES`, `NAVAL_GOLD_RAID`), but their displayed information is only eligible target plots / movement-style lens feedback, not normal combat-preview data.
- Vanilla suppresses or hides combat preview in these cases:
  - `m_combatResults` is nil
  - `CanShowCombat()` rejects the current mode, currently including `WMD_STRIKE`
  - the selected unit has neither normal combat nor religious strength
  - the hovered plot is not visible to the local player
  - the hovered flag belongs to the local player
  - `UI.IsGameCoreBusy()` prevents simulation
- `Events.UnitOperationAdded`, `Events.UnitOperationDeactivated`, and `Events.UnitOperationsCleared` request a vanilla UnitPanel refresh for the selected unit.
- Normal panel refresh events such as `OnUnitSelectionChanged(...)`, `OnUnitCommandStarted(...)`, `OnUnitOperationAdded(...)`, `OnUnitOperationDeactivated(...)`, `OnUnitOperationsCleared(...)`, and movement-point refreshes update the UnitPanel itself, but they do not independently recompute combat preview unless one of the hover or mode-entry paths above runs again. `OnUnitSelectionChanged(...)` explicitly clears `m_combatResults`.
- `UnitPanel_CAI.lua` implements successful owned-unit immediate-action announcements with the UnitPanel action callback as the intent boundary and the engine start event as the success boundary:
  - `AddActionToTable(...)` wraps the same action callback used by vanilla buttons, CAI's mirrored action list, and `m_kHotkeyActions`. Wrapping the callback supplied to `AddActionToTable(...)` therefore covers all three activation paths without replacing `OnUnitActionClicked(...)` or issuing a duplicate request.
  - Immediate operations without an `InterfaceMode` call `UnitManager.RequestOperation(...)`; immediate commands call `UnitManager.RequestCommand(...)`. These request calls expose no useful success result to UnitPanel Lua.
  - `Events.UnitOperationStarted(ownerID, unitID, operationID)` and `Events.UnitCommandStarted(playerID, unitID, commandID, data1)` provide owner, unit, and action-hash boundaries after the engine accepts or begins many actions. The IDE event helper documents `Events.UnitOperationAdded(playerID, unitID, unknown, operationType)` with four parameters. Vanilla `UnitPanel.lua` misleadingly declares only `OnUnitOperationAdded(player, unitId, hOperation)` because it ignores all operation parameters and only requests a refresh. Runtime tracing establishes that the third argument is the documented unknown value (`0` for HEAL) and the fourth is the numeric `GameInfo.UnitOperations` row index (`13` for HEAL), not the action hash. Resolve `GameInfo.UnitOperations[operationType].Hash` before matching a pending callback intent.
  - It records a five-second pending intent immediately before invoking an eligible wrapped callback. Ordinarily it speaks only when a matching added/start event arrives for the local player's same unit and action hash, then consumes the shared pending intent. An operation that emits both Added and Started therefore speaks once. It expires unmatched intents and clears all pending intents at local turn end so a rejected request or later implicit engine action cannot produce stale speech.
  - Confirmed HEAL behavior: `UNITOPERATION_HEAL` is a visible `INPLACE` database operation and UnitPanel routes it through the generic `OnUnitActionClicked(...)` -> `UnitManager.RequestOperation(selectedUnit, actionHash)` path. Runtime tracing recorded the pending callback under hash `2126026491`, followed about 0.05 seconds later by `UnitOperationAdded(playerID, unitID, 0, 13)`. The initial tracer accepted only three arguments and discarded the useful index; the next attempt incorrectly matched index `13` directly against hash `2126026491`. The permanent handler accepts all four arguments, resolves index `13` through `GameInfo.UnitOperations[13].Hash`, and uses the resulting hash with the ordinary pending-intent matcher. The trace and HEAL-specific fallbacks were removed.
  - `UnitPanel_CAI.lua` is close to Civ VI Lua's per-chunk local-register limit. Temporary diagnostics in this file must not add clusters of top-level local functions or state variables; doing so can produce `Function or expression requires too many registers` at compile time.
  - Result configuration resolves hashes from string-keyed `GameInfo.UnitOperations` / `GameInfo.UnitCommands` rows. Do not construct Lua tables with direct `UnitOperationTypes.X` keys: an unavailable runtime enum becomes nil and causes `table index is nil` while loading UnitPanel. Missing DLC/scenario rows are legitimate and are skipped when the configuration is built.
  - A scenario can leave a string-keyed `GameInfo.UnitOperations` row present while removing its matching entry from `Types`. Such an orphan row has no `Hash`. Alexander does this for `UNITOPERATION_PLANT_FOREST`: `AlexanderScenario_RemoveData.xml` deletes that value only from `Types`, while the base `UnitOperations` row remains because that table has no foreign key to `Types`. Consequently, `GameInfo.UnitOperations["UNITOPERATION_PLANT_FOREST"]` is non-nil but `.Hash` is nil. Result registration must require both the row and `row.Hash`; checking only whether the row exists still raises `table index is nil` when assigning the result lookup. Other data-pruning scenarios can produce the same shape.
  - Use an explicit immediate-action allowlist rather than a category-wide rule or a movement/combat/travel blacklist. Included operations cover stationary orders; building routes and improvements; clearing contamination; converting barbarians; designating parks; harvesting resources; planting/removing features; removing/repairing improvements and routes; removing heresy; retraining; spreading religion; religious healing; and upgrading. Included commands cover wake, stop automation, gift, enter/exit formation, direct Great Person effects, direct district/wonder production effects, wonder harvesting, and petting the dog.
  - Exclude every action with an `InterfaceMode`, plus known UI/confirmation flows including founding a city or religion, evangelizing a belief, launching an inquisition, excavation, promotion, naming, and deletion. Also exclude movement, travel, attacks, pillage, spy missions, cancel-order paths, airlift, and targeted corps/army formation.
  - Improvement/plot result builders cache only database ids before the operation changes the plot, then resolve live localized names when the matching success event arrives. `BUILD_IMPROVEMENT`, `BUILD_ROUTE`, `PLANT_FOREST`, `REMOVE_FEATURE`, `HARVEST_RESOURCE`, `REMOVE_IMPROVEMENT`, `REPAIR`, and `REPAIR_ROUTE` can therefore say results such as `Farm built`, `Woods removed`, `Stone harvested`, or `Farm repaired`. Missing lookup rows log a warning and use a localized generic fallback.
  - Use localized result labels phrased as achieved state rather than merely repeating an infinitive button label. Do not reuse the full action tooltip because it can contain instructions, additional descriptions, and failure reasons rather than a concise result.
  - The pending-intent gate is important for `WAKE`: listening to all matching engine events without proving direct UnitPanel activation could announce implicit wake/state changes caused by some other action.
- Plot info does not call selected-unit info helpers. `worldInfo.lua` keeps a small local plot-unit display-name helper for aggregated plot summaries.
- `UnitFlagManager_CAI.lua` exposes two visible-flag requests. `RequestUnitFlagInfo(playerID, unitID, requestedKeys)` retains the compact default used by cursor movement. `RequestDetailedUnitFlagInfo(playerID, unitID)` is used only by the map `S` unit read and preserves exact-spoken-signature aggregation for matching units on the same tile:
  - The detailed `S` fields are joined with comma-space separators; an aggregated count is localized together with the unit name (`2 Roman Trader`) so no comma separates them. The compact cursor report retains its original spacing.
  - Local-unit details are composed primarily from UnitFlagManager helpers and the live unit: name, full HP, movement fraction, explicit out-of-moves state, embarked/fortified status, and hosted-aircraft capacity/names. It asks `UnitPanel_CAI.lua`'s `RequestUnitInfo(...)` only for its existing stats, level/XP/promotions, and upgrade-tooltip data.
  - Visible foreign non-enemies expose only embarked-prefixed name, damaged HP, and fortified state.
  - Visible enemies expose those foreign-unit details plus live melee combat strength, ranged strength, and attack range. Bombard, anti-air, religious strength, and movement remain excluded.
  - Cursor movement remains on the compact default flag report: aggregated name, queued waypoint, damaged HP, short promotion/levy state, embarked/fortified status, religion, archaeology, aircraft capacity, and scenario markers.
- `UnitFlagManager.lua` / `UnitFlagManager.xml` own the ambient world-map unit flags:
  - A visible foreign unit object exposes live `GetCombat()`, `GetRangedCombat()`, `GetBombardCombat()`, `GetAntiAirCombat()`, `GetReligiousStrength()`, `GetRange()`, `GetMaxMoves()`, and `GetMovesRemaining()` values to UI Lua. `GetRange()` is not exclusively attack range: a noncombat Observation Balloon can return `10`. CAI therefore labels every positive value generically as `Range`, preserving the useful value without claiming an attack capability. Vanilla unit flags do not present those numeric combat, range, or movement values; they present identity and iconographic state plus a damage health bar.
  - Vanilla `UnitPanel` reads those same values from a foreign defender only while building a valid combat preview. `ReadTargetData_Unit()` records combat strengths, attack range, damage, remaining moves, and maximum moves, but the target stat display formats maximum movement rather than remaining movement. A typical ranged defender can therefore show ranged strength, maximum movement, and attack range in the combat preview. This is not a general foreign-unit inspection UI or threat-range overlay.
  - Outside combat preview, a sighted player normally infers an enemy unit's capabilities from its recognizable unit-type flag/icon and can consult that unit type's Civilopedia entry for its base statistics. Promotions and other live modifiers can make effective threat differ from the base entry; vanilla does not provide a persistent exact enemy attack-range visualization.
  - Persistent on-map information is mostly iconographic, not textual: frame style, unit emblem icon, optional health bar, promotion-or-levy badge, corps/army marker, religion badge, hero glow, barbarian attention `!`, and for air-capable hosts an air-capacity counter plus a popup list of stationed aircraft.
  - `CreateUnitFlag(...)` chooses style from the unit role: land combat and air -> military, naval -> naval, support -> support, civilian traders -> trade, religious civilians -> religion, and other civilians -> civilian.
  - `UpdateFlagType()` can override that role frame with embarked or fortified visuals, so the base role is not always what sighted players see.
  - There is no persistent visible name label on the flag itself. Identity/details live in the unit-icon tooltip from `UpdateName()`: civilization short name, multiplayer human player name when relevant, unit name, renamed-vs-type suffix, corps/fleet/army/armada suffix, archaeology home city and artifact owner, religion name, and levy status with turns remaining.
  - The religion badge is separately visible only when `GetReligionType() > 0` and `GetReligiousStrength() > 0`; its tooltip is the religion name.
  - Damage swaps the normal frame/button out for a visible health bar. Thresholds are green at `>= 80%`, yellow at `>= 40%`, and red below that.
  - `UpdatePromotions()` is also doing double duty as a levy/promotion badge: levied military units show a turn icon instead of a promotion count, while normal units show the earned-promotion count only.
  - `UpdateAircraftCounter()` adds host-carrier / host-airfield capacity info that vanilla exposes visually and through the popup list only: current stationed-aircraft count, max air slots, aircraft names, aircraft icons, and ready/not-ready dimming inside that popup.
  - DLC / mode replacements add extra flag-specific data instead of replacing the whole base model:
    - `BarbarianClansMode` adds tribe tooltip/status coverage for tribe display name, bribed turns, incited-against-you source player, incited-by-you target player, and a tribe-status icon path that takes precedence over the normal promotion badge for eligible clan units.
    - `PiratesScenario` reuses the army marker as a special flagship / infamous-pirate marker for `GetMaxDamage() > 100`, and pirate barbarians replace the tooltip text with the Buccaneer description plus unit name.
    - `CivRoyaleScenario` adds a transient combat-preview bar on visible enemy flags and uses Great Person individual icon overrides plus multiplayer player-name tooltip formatting.
    - `PolandScenario` ships its own older `UnitFlagManager.lua`, but for spoken-info parity it matches the same base tooltip and badge categories above rather than adding new scenario-only unit-flag text.
  - `UnitFlagManager_CAI.lua` now chooses its vanilla include dynamically:
    - `RULESET_SCENARIO_CIV_ROYALE` -> `UnitFlagManager_CivRoyaleScenario`
    - `RULESET_SCENARIO_PIRATES` -> `UnitFlagManager_PiratesScenario`
    - `GAMEMODE_BARBARIAN_CLANS = 1` -> `UnitFlagManager_BarbarianClansMode`
    - otherwise -> `UnitFlagManager`
  - CAI unit-flag speech now keeps the ambient buckets short and additive instead of mirroring the full vanilla tooltip text. New helpers cover promotion count or levy turns, religion name, archaeology home city and artifact owner, aircraft capacity as `current/max`, hero / threat / flagship markers, Barbarian Clans short status, and the Pirates Buccaneer label.
  - Carrier / host-aircraft popup contents come straight from the host unit each refresh. `UpdateAircraftCounter()` uses `unit:GetAirSlots()` for capacity, `unit:GetAirUnits()` for the stationed-aircraft rows, writes the counts into `AirUnitInstance.CurrentUnitCount` / `MaxUnitCount`, and rebuilds `UnitListPopup` from that live list.
  - Local human units dim when not `IsReadyToSelect()`. Selection overrides that dimming so the selected unit stays full alpha.
  - Hidden states are layered: non-visible fog state hides the flag; combat visualization temporarily force-hides attacker/defender/interceptor/anti-air flags; turning the relevant lens layer off hides the entire manager context; and stationed air units usually hide their own individual flags on airstrips, aerodromes, and carriers, except intercepting air units keep a visible flag.
  - Same-tile stacking is communicated visually with per-role offsets and duo/trio formation-link graphics. Those link graphics can be suppressed for non-local players when one member of the formation is hidden.
  - Clicking your own visible flag selects the unit when the current interface mode allows it. Clicking a visible enemy flag with a selected local unit can trigger range attack or move-to-attack.
- Vanilla does not appear to provide clean city-panel loc tags for labeling the two percentage values as speech output, so `CityPanel_CAI.lua` uses CAI loc tags only for:
  - `LOC_CAI_CURRENT_PROGRESS`
  - `LOC_CAI_NEXT_TURN_PROGRESS`
- The compact city panel writes each yield checkbox as `[ICON_<Yield>]` plus its signed per-turn value. CAI's shared icon processor already replaces that icon with the localized yield name for speech, so city yield-info helpers must return the icon-bearing value directly and must not prepend `GameInfo.Yields[yieldType].Name`; doing both repeats every yield name.
- `CityPanel_CAI.lua` also listens to `Events.InputActionTriggered(actionId)` and uses an action-id-to-helper map instead of an `if` / `elseif` chain.
- Current city selection hotkey mapping uses the shared city-panel buckets:
  - `ReadSelectionSummary` -> `Name`, `Population`, `Health`, `Production`, `Growth`
  - `ReadSelectionInfo1` -> `Name`, `Health`
  - `ReadSelectionInfo2` -> `Production`
  - `ReadSelectionInfo3` -> `Growth`
  - `ReadSelectionInfo4` -> `BorderGrowth`
  - `ReadSelectionInfo5` -> `Religion`
  - `ReadSelectionInfo6` -> `Population`, `Housing`, `BuildingsOrLoyalty`
  - `ReadSelectionInfo7` -> `VisibleYields`
  - `ReadSelectionInfo8` -> `NormalFocusYields`
  - `ReadSelectionInfo9` -> `FavoredFocusYields`
  - `ReadSelectionInfo10` -> `IgnoredFocusYields`
- `CityPanel_CAI.lua` now appends dynamic ranged-strike rows to the city `SelectionActions` list:
  - availability is gated by `CityManager.CanStartCommand(target, CityCommandTypes.RANGE_ATTACK)` for both the city center and each district owned by the selected city
  - city rows reuse vanilla city targeting flow: `UI.SelectCity(city)` then `UI.SetInterfaceMode(InterfaceModeTypes.CITY_RANGE_ATTACK)`
  - district rows reuse vanilla district targeting flow: `UI.DeselectAll()`, `UI.SelectDistrict(district)`, then `UI.SetInterfaceMode(InterfaceModeTypes.DISTRICT_RANGE_ATTACK)`
  - these rows are CAI-only synthetic menu items, so they centralize strike-capable city and district attacks even though vanilla normally exposes them only on banners / minibanners
- `CityPanel_CAI.lua` also appends dynamic WMD strike rows for strike-capable city districts such as missile silos:
  - availability mirrors vanilla `UpdateWMDBanner()` instead of using `CanStartCommand(...)`: for each supported WMD type, CAI checks local stock through `player:GetWMDs():GetWeaponCount(wmd.Index)` and then calls `CityManager.GetCommandTargets(city, CityCommandTypes.WMD_STRIKE, parameters)` with the district plot as `PARAM_X0` / `PARAM_Y0`
  - launch flow mirrors vanilla `OnICBMStrikeButtonClick(...)`: if already in `ICBM_STRIKE`, switch back to `SELECTION`, then `UI.SelectCity(city)`, `UILens.SetActive("Default")`, and finally `UI.SetInterfaceMode(InterfaceModeTypes.ICBM_STRIKE, parameters)` using the chosen weapon type and district plot
  - CAI additionally jumps the cursor to the firing district plot before entering strike mode so the centralized action list stays spatially grounded
  - `SelectionActions` -> `Tab`
- City yield focus states in `GetCityData(city)` are stored in `data.YieldFilters[yieldType]` and map to:
  - `YIELD_STATE.FAVORED`
  - `YIELD_STATE.IGNORED`
  - any other value = normal
- City yield preferences use a native three-state model:
  - `YIELD_STATE.FAVORED`
  - `YIELD_STATE.IGNORED`
  - neither state = normal
- CityPanel helpers for yield preference changes:
  - `OnCheckYield(yieldType, yieldName)` transitions normal to favored, or favored to ignored depending on the checkbox visual state
  - `OnResetYieldToNormal(yieldType, yieldName)` transitions ignored back to normal
  - `SetYieldFocus(yieldType)` and `SetYieldIgnore(yieldType)` are the lower-level command helpers used underneath
- Compared against vanilla base `CityPanelOverview.lua`, current `CityPanel_CAI.lua` only exposes compact speech for summary, health, production summary, growth summary, housing summary, amenity/building counts, religion follower count, visible yields, and yield-focus state. It does not yet mirror most of the detailed left-panel overview content.
- Vanilla base city panel overview exposes these information groups that CAI does not currently speak in structured form:
  - breakdown tab: district count vs district cap, built districts with pillage state, each built building with pillage state, per-entry yield strings, wonders, and trading-post owners
  - religion tab: pantheon belief, dominant religion, other religions in the city, and dominant-religion belief list
  - amenities tab: city mood, growth/yield effect text, amenity advice, required amenities, and the detailed amenity-source breakdown
  - housing tab: housing growth state, housing advice, and the detailed housing-source breakdown
  - citizens/growth tab: gross food, food consumption, net food, growth threshold, happiness bonus, other growth modifiers, housing multiplier, occupation multiplier, modified food, and total surplus/deficit
  - production tab: current production unit stats and explicit production queue rows
- `CityPanel_CAI.lua` currently opens the same vanilla tabs and toggles through native callbacks / controls, but its spoken info registry in `CityInfo` does not include keys for those detailed overview breakdowns.
- Base city-management ownership split confirmed from the decompiled UI:
  - `CityPanel.lua` owns the compact selected-city shell, the overview toggle, the manage-citizens toggle, the purchase-tile toggle, and the enter/leave transitions for `InterfaceModeTypes.CITY_MANAGEMENT`.
  - `CityPanelOverview.lua` owns the detailed left-side overview tabs and their data views: breakdown, religion, amenities, housing, citizens/growth math, current production detail, and production queue.
  - `PlotInfo.lua` owns the in-world per-plot city-management overlays. In citizen management it exposes workable plots, specialist capacity meters, and locked-citizen icons; in tile purchase it exposes per-plot gold buttons and affordability tooltip state; it also owns tile-swap buttons and the city-yield overlay.
  - `WorldInput.lua` does not own the city-management content itself. It only owns the `CITY_MANAGEMENT` mode routing, including enter/leave handlers, Escape-style key handling through `OnPlacementKeyUp`, and a pointer-up no-op so plot overlays, not world selection, consume interaction while the mode is active.
- Expansion 1 adds loyalty/culture data to the city panel:
  - main panel swaps the first stat from building count to current loyalty in `CityPanel_Expansion1.lua`
  - overview adds a loyalty tab (`CityPanelCulture.lua`) with current/max loyalty, loyalty level, per-turn pressure status, detailed source breakdown, loyalty effects, loyalty advice, diplomatic presence, and assigned-governor details including establishment state / turns
  - XP1 also changes citizens-growth math display to show loyalty growth modifiers instead of the old occupation-only wording, and adds governor-derived amenities in `ViewPanelAmenities`
- Expansion 2 keeps the XP1 loyalty/governor culture tab and further adds a power tab (`CityPanelPower.lua`) with:
  - consumed vs required power totals
  - power status name and description
  - consumed, required, and generated power-source breakdowns
  - power advice
- `CityPanel_CAI.lua` should include the same base / XP1 / XP2 `CityPanel` replacement chain that vanilla would load:
  - base ruleset -> `CityPanel`
  - Expansion 1 active -> `CityPanel_Expansion1`
  - Expansion 2 active -> `CityPanel_Expansion2`
- The city action list can open the expansion overview tabs through the overview context LuaEvents rather than loading those panels itself:
  - XP1 / XP2 loyalty-governor overview -> `LuaEvents.CityPanel_ToggleOverviewLoyalty()`
  - XP2 power overview -> `LuaEvents.CityPanel_ToggleOverviewPower()`
- For the planned city-panel redo, the largest parity gaps are the overview subpanels and the XP1/XP2 dynamic tabs, not the main action buttons.
- `WorldTracker.lua` is a passive in-game HUD stack, not a modal chooser:
  - It builds `ResearchInstance`, `CivicInstance`, `OtherContainer`, `UnitListInstance`, `ChatPanelContainer`, and `TutorialGoals` under `Controls.WorldTrackerVerticalContainer` in that order.
  - The header has `Controls.ToggleAllButton` to collapse/expand the whole tracker and `Controls.ToggleDropdownButton` to open tracker options.
  - The options dropdown contains checkbox controls for chat, civics, research, and unit list visibility. `CheckEnoughRoom()` disables unchecked options and sets `LOC_WORLDTRACKER_NO_ROOM` when there is not enough vertical space.
  - Research and civics panels are shown only when their capability exists and the local player is alive. They call `RealizeCurrentResearch(...)` / `RealizeCurrentCivic(...)` from `TechAndCivicSupport.lua`, which populate title text, icon, turns remaining, progress meter, boost meter/icon/label, unlock icons, and overflow page-turn button.
- `Assets/UI/Popups/ChatPanel.lua/.xml` is the in-world multiplayer chat surface mounted inside WorldTracker's `ChatPanelContainer`, not a standalone modal. Base layout is a compact bottom-left tracker card (`294x118` plus border) with a `ChatLogPanel` scroll area, a target `PullDown` styled as `ChatPullDown`, expand/contract buttons, and a drag-resize handle. In Play By Cloud the normal chat card is hidden and replaced by a minimal `PlayByCloudPanel` that only exposes the show-player-list toggle.
- Sighted chat usage is primarily mouse and keyboard-enter based: type into the `ChatEntry` edit box owned by the `ChatPullDown` style, press Enter to send, switch target with the pulldown (`To All`, `To Team` when applicable, or a human player whisper target), click expand/collapse, or drag the bottom-right resize handle. Target changes also recolor the edit box through `PlayerTargetLogic.lua` (`ChatMessage_Global`, `ChatMessage_Team`, `ChatMessage_Whisper`) and can be changed with slash commands parsed in `ChatLogic.lua` (`/g`, `/t`, `/w`, `/help`).
- Chat history is a bottom-growing stacked log of plain text rows plus special map-pin rows. Normal messages are rendered as `player name: text`, with whisper rows including the target name. Messages containing `[pin:x,y]` become `MapPinChatEntry` rows with a clickable pin-name button that calls `UI.LookAtPlot(hexX, hexY)`. The panel auto-scrolls only once the history exceeds the visible log height, and it seeds the log with `LOC_CHAT_HELP_COMMAND_HINT`.
- Accessible map-pin chat rows retain owner/pin ids and resolve the live pin again on activation, then move the navigation cursor through `LuaEvents.CAICursorMoveTo(plotIndex, "jump")`; camera movement follows through the shared cursor listener. Their message-buffer append includes `{ x = hexX, y = hexY }` as the third `CAIAppendToMessageBuffer` argument, so the ordinary message-buffer move-to action can jump to the shared pin location even after the chat panel closes.
- The adjacent `PlayerListPanel` is a separate toggleable side panel offset to the right of chat. It lists current human / connected players as `PlayerListEntry` pulldowns with a connection-status icon, player name, and a status label from `NetConnectionIconLogic.lua`. Per-player pulldown actions are conditional: host kick, start kick vote, and friend request. The panel may also append an Invite button and an EOS/crossplay join-code row whose text copies to the system clipboard on click.
- Kick-vote flow is another side panel (`KickVotePanel`) using `VoteKickPlayerEntry` rows with a title plus Yes/No buttons. Starting or completing a vote also writes chat-log lines. The local player only sees live vote buttons when they are neither the target nor the initiator.
  - The research/civics `IconButton` opens the matching chooser through `LuaEvents.WorldTracker_OpenChooseResearch()` / `LuaEvents.WorldTracker_OpenChooseCivic()`. Right-click Civilopedia callbacks are added to title/unlock controls outside tutorial mode.
  - If no current research/civic exists, the panel remains visible but the active title/icon are hidden and `UpdateResearchPanel(...)` / `UpdateCivicsPanel(...)` set the title button text to `LOC_WORLD_TRACKER_CHOOSE_RESEARCH` or `LOC_WORLD_TRACKER_CHOOSE_CIVIC`.
  - The unit list is hidden by default. When visible and `CAPABILITY_UNIT_LIST` exists, it rebuilds on unit add/remove/movement/activity events, groups units by broad type, filters by `UnitsSearchBox`, and each row left-clicks `OnUnitEntryClicked(unitID)` to look at and select that unit.
  - Unit rows use live text from `Button:GetText()`, optional tooltip for renamed units, status icon for sleep/skip/fortify, and text/icon color to indicate whether movement remains.
  - World Tracker hides itself during `VIEW_MODAL_LENS`, can be force-hidden/restored by research/civic choosers and tutorials, and in tutorial restore mode the normal collapse/options buttons are temporarily hidden while research/civics are forced visible.
  - CAI access should use world input actions rather than an ambient mirrored widget panel. Current mappings are `WorldTrackerOpenResearchChooser` -> `LuaEvents.WorldTracker_OpenChooseResearch()`, `WorldTrackerOpenCivicsChooser` -> `LuaEvents.WorldTracker_OpenChooseCivic()`, and `WorldTrackerReadSummary` -> live player-state speech.
  - `WorldTrackerOpenResearchChooser` / `WorldTrackerOpenCivicsChooser` should return without raising chooser LuaEvents when the live WorldTracker research/civic `IconButton`, `TitleButton`, or `MainPanel` is hidden/disabled. This lets tutorial and WorldTracker control filtering remain the source of truth instead of adding visibility guards inside the chooser push path.
  - WorldTracker summary speech should not depend on panel visibility. Build research/civic data from `Players[localPlayer]:GetTechs():GetResearchingTech()`, `Players[localPlayer]:GetCulture():GetProgressingCivic()`, `GetResearchData()`, and `GetCivicData()`. Speak only label, turns remaining, and boost status for each. Build unit count from `Players[localPlayer]:GetUnits():GetCount()`.
- Remaining-turns for the ACTIVE research/civic must come from `PlayerTechs:GetTurnsLeft()` / `PlayerCulture:GetTurnsLeft()` (both no-arg, progress-aware). `GetTurnsToResearch(techID)` / `GetTurnsToProgressCivic(civicID)` are from-scratch estimates for not-yet-started items and run one turn high near completion (report "2 turns" on the turn before it finishes). Vanilla's own Tech/Civics trees branch on this: `GetTurnsLeft()` for the current item, `GetTurnsToResearch`/`GetTurnsToProgressCivic` for others. The vanilla ResearchChooser/CivicsChooser and `TechAndCivicSupport.GetResearchData`/`GetCivicData` do NOT branch — they fill `kData.TurnsLeft` (and the row `TurnsLeft` control) from the from-scratch call for every item, including the current one, so any CAI surface reading that value inherits the overestimate. CAI overrides `TurnsLeft` with `GetTurnsLeft()` for the current item in `WorldTracker_CAI` (r/p summaries) and both choosers; the trees already read vanilla's branched value and need no override.
- Vanilla `ResearchChooser.lua` / `ResearchChooser.xml`:
  - `AddAvailableResearch(...)` writes the live row strings onto native controls: `TechName:SetText(...)`, `Top:LocalizeAndSetToolTip(...)`, `NodeNumber:SetText(...)`, `TurnsLeft:SetText(...)`, and boost icon `SetToolTipString(...)`.
  - `RealizeCurrentResearch(...)` writes the active header text through `TitleButton:SetText(...)`, uses the shared turns-left helper to populate `TurnsLeft`, and reuses the same boost icon tooltip path for current research.
  - CAI wrappers should prefer those live control values through `GetText()` / `GetToolTipString()` over rebuilding text with `Locale.Lookup(...)`, except for CAI-only wrapper labels or synthesized state strings that vanilla does not expose on a control.
- `TopPanel.lua` owns the global top-bar yields/resources:
  - `RefreshYields()` creates yield display instances under `Controls.YieldStack`.
  - Science, culture, and tourism use `YieldButton_SingleLabel` with `YieldIconString`, `YieldPerTurn`, and `YieldBacking` tooltip.
  - Gold and faith use `YieldButton_DoubleLabel` with `YieldIconString`, `YieldBalance`, `YieldPerTurn`, and `YieldBacking` tooltip.
  - Yield tooltips come from `ToolTipHelper_PlayerYields.lua`: `GetScienceTooltip()`, `GetCultureTooltip()`, `GetGoldTooltip()`, and `GetFaithTooltip()`. Tourism builds a tooltip from `LOC_WORLD_RANKINGS_OVERVIEW_CULTURE_TOURISM_RATE` plus `player:GetStats():GetTourismToolTip()`.
  - Base `TopPanel.lua` does not register click callbacks on the yield `YieldBacking` buttons. Only `MenuButton` and `CivpediaButton` get click callbacks in the base file.
  - `RefreshResources()` creates `ResourceInstance` rows under `Controls.ResourceStack`, showing only non-bonus, non-luxury, non-artifact resources with amount > 0. In practice this is the strategic-resource strip.
  - Each visible resource label uses text like `[ICON_RESOURCE_IRON] 3` and a tooltip of localized resource name plus `LOC_TOOLTIP_STRATEGIC_RESOURCE`.
  - If resources overflow the available top-bar width, vanilla shows a single `[ICON_Plus]` label whose tooltip lists the hidden resource amounts and names.
  - CAI TopPanel access should use input actions and pushed CAI widgets, not an ambient mirrored UI. Use vanilla loc strings and tooltip helpers directly; do not strip icon/color markup locally.
  - TopPanel player-yield APIs expose totals plus localized tooltip strings, not structured category tables. Known UI methods are `PlayerTechs:GetScienceYieldToolTip()`, `PlayerCulture:GetCultureYieldToolTip()`, `PlayerReligion:GetFaithYieldToolTip()`, `PlayerStats:GetTourismToolTip()`, `PlayerTreasury:GetGoldYieldToolTip()`, and `PlayerTreasury:GetTotalMaintenanceToolTip()`.
- Gold has extra direct numeric APIs for major expense totals: `PlayerTreasury:GetBuildingMaintenance()`, `GetDistrictMaintenance()`, `GetUnitMaintenance()`, `GetWMDMaintenance()`, and `GetMaintDiscountPerUnit()`. `ToolTipHelper_PlayerYields.GetExtendedGoldTooltip()` uses them for its own summary and to infer hostile-Spy costs.
- For the accessible hierarchy, keep `PlayerTreasury:GetTotalMaintenanceToolTip()` authoritative for every displayed Gold expense category and amount. Do not reconstruct category totals or synthesize remainder rows: a live `114` expense case already broke down correctly as `10 Deals + 30 WMDs + 26 Units + 48 Cities`, while manual subtraction that omitted Deals created a false `10 Other city maintenance`. Identify Cities/Units/WMD rows by the invariant localized prefix/suffix produced from their named-value localization tag with two sentinel values; this remains language-independent while ignoring the amount. Attach Reports detail directly beneath the matched vanilla row and leave any mismatch visible for diagnosis.
- `LOC_PLAYER_YIELD_*` source and maintenance strings use named `{Value}` parameters. Lua callers must pass `{ Name = "Value", Value = amount }`; passing the amount positionally leaves the value blank without raising a Lua error.
- Science, Culture, Gold income, and Faith player tooltips have localized aggregate `from Cities` rows, while `GetData()` supplies live cities and `City:GetYield(...)` plus each city's `GetYieldToolTip(...)` detail. Match the aggregate row with its named-value localization, replace any tooltip-provided city children with the Reports-backed city rows, and retain every non-city engine source row. Tourism's engine tooltip also exposes an authoritative `from Cities` row, although no tourism-specific localization tag exists; its text matches `LOC_PLAYER_YIELD_CULTURE_FROM_CITIES`. Enrich that existing row instead of appending a second Cities branch. Reports only attributes worked-plot tourism by city, so add the aggregate-minus-city-sum residual as `Other city tourism` beneath the same row.
- Gold deal detail in `m_caiDealData` maps to the named-value `LOC_PLAYER_YIELD_GOLD_FROM_DEALS` and `LOC_PLAYER_GOLD_COST_FROM_DEALS` rows. Nest incoming and outgoing deal instances there instead of keeping a competing top-level Diplomatic Deals group. WMD detail can be reconstructed from `GameInfo.WMDs().Maintenance` times `PlayerWMDs:GetWeaponCount(index)` and enriches the matching engine WMD category.
- Vanilla Reports `Building Expenses` is not buildings-only: it iterates `kCityData.Buildings` for ordinary building maintenance and `kCityData.BuildingsAndDistricts` for district maintenance, then combines both in one total. Gathering Storm's replacement `CitySupport.lua` changes the building record from raw `GameInfo.Buildings.Maintenance` to `CityBuildings:GetBuildingMaintenance(type)`; in a live test those records were zero despite maintained buildings, so CAI must not use them as its building-detail source. Enumerate every `GameInfo.Buildings()` row for each city's `CityBuildings:HasBuilding(index)`, exclude pillaged instances, and use the database `Maintenance` value. This restores type and city-instance detail without altering the vanilla Cities parent.
- Tooltip indentation can represent more than one source depth. Parse the leading whitespace as a hierarchy instead of treating every indented line as a child of the last root row; this preserves nested vanilla breakdowns not explicitly enhanced by Reports data.
  - Yield detail access is a `Treeview`: each top-level yield node is the yield name plus value/rate preview. Use vanilla `LOC_HUD_REPORTS_PER_TURN` to append localized "per turn" text to any single rate value before composing larger labels. Outer yield nodes do not expose the full vanilla tooltip because the tree contains the formatted breakdown. Gold is split into vanilla `LOC_TOP_PANEL_GOLD_INCOME` and `LOC_TOP_PANEL_GOLD_EXPENSE` category nodes with localized tooltip detail lines beneath them. Science, culture, faith, and tourism use localized tooltip breakdown lines as category/detail nodes. CAI uses tooltip indentation/line structure for nesting and does not parse English text.
  - `ReportScreen.lua` city-income data is built from `GetCityData(pCity)` plus `GetWorkedTileYieldData(pCity, pCulture)`. District rows come from `data.BuildingsAndDistricts`: each district exposes its own per-turn yields plus an `AdjacencyBonus` table and per-building rows from the district plot. Great works are attached separately through `GetGreatWorksForCity(pCity)`. In a Report tree, non-actionable leaves stay `StaticText`, while every actionable leaf and every expandable row is a `TreeItem`; buttons remain appropriate only in non-tree containers such as the City Status list. District tooltip totals should aggregate the district row's own yields plus adjacency, building, and attached great-work yields.
  - Report component destinations are recoverable even though `GetCityData` omits plot coordinates from its district/building/wonder records. District plot indices come from `City:GetDistricts():Members()` matched by `GameInfo.Districts[district:GetType()].DistrictType`. Building and wonder plot indices come from scanning `Map.GetCityPlots():GetPurchasedPlots(city)` and matching each `CityBuildings:GetBuildingsAtLocation(plotIndex)` result through `GameInfo.Buildings`. Concrete city/component labels append the live cursor-relative result of `CAIHexCoordUtils.directionString(...)`, matching Map Pin List. Activation must call `Close()` before `LuaEvents.CAICursorMoveTo(plotIndex, "jump")` so panel-close speech cannot interrupt the destination announcement. Maintenance type headers can represent multiple plots, so only their concrete city-instance children have destinations.
  - `City:GetYieldToolTip(...)` embeds concrete component leaves through `LOC_CITY_YIELD_FROM_DISTRICTS_TOOLTIP` and `LOC_CITY_YIELD_FROM_BUILDINGS_TOOLTIP` beneath aggregate rows produced by `LOC_CITY_YIELD_FROM_DISTRICTS_SUMMARY_TOOLTIP` and `LOC_CITY_YIELD_FROM_BUILDINGS_SUMMARY_TOOLTIP`. Preserve parent links while parsing the indentation hierarchy, then match a concrete component by its localized name only when one of those corresponding summary rows is an ancestor. Test longer localized names first so an overlapping name such as Palace cannot claim a Forbidden Palace row. The non-destructive decorator appends the component location and attaches activation while preserving any original tooltip children. Do not reconstruct the mixed named/positional component localization call or use an unrestricted component-name substring: the former does not reliably reproduce the engine string, while the latter would incorrectly match `Campus Research Grants` as Campus.
  - Vanilla Reports groups Unit Expenses by `UnitType .. MilitaryFormation`, retaining only name, count, and total maintenance. Rebuild the same key while enumerating the local player's live units to attach stable unit IDs beneath each aggregate type row. A concrete unit label resolves `Players[playerID]:GetUnits():FindID(unitID)` to append its live cursor-relative location; activation resolves it again, closes Reports, then jumps to its current plot. Format each instance with shared `GetUnitFormationSuffix(unit)` plus `FormatOwnedName(nil, name, suffix)` so Corps/Army/Fleet/Armada children retain the formation shown by their parent. Its per-instance maintenance is the grouped total divided by the same-key count; all members share type, formation, and the player's per-unit discount. Aggregate unit-type rows remain expandable because they may represent several plots.
  - Empire Economy's collapsed summary rounds rates to one decimal before localization. Expanded Science, Culture, Gold-net, and Faith labels must pass the same `Round(value, 1)` result to their signed formatter; feeding raw floating-point values can make the expanded label differ from the summary by `0.1`.
- `CivilopediaScreen.lua` / `CivilopediaSupport.lua` / `CivilopediaScreen.xml` define the in-game encyclopedia popup:
  - Vanilla opens it through `LuaEvents.ToggleCivilopedia()` or `LuaEvents.OpenCivilopedia(sectionOrSearch, pageId)`. The latter accepts either a direct section/page request or a search term and then navigates to the first result.
  - The sighted layout has five main regions:
    - a top horizontal section-tab strip with icon buttons for the major Civilopedia categories
    - a left search box plus vertical page list for the current section
    - optional collapsible page-group headers inside that page list
    - a breadcrumb/history strip with explicit Back and Forward buttons
    - a large scrollable article pane on the right
  - The article pane supports both a full-width chapter layout and a two-column layout. Two-column pages typically put text chapters on the left and portraits, quote panels, and stat boxes on the right.
  - Section tabs and page rows are mouse-first in vanilla. The active section/page is shown selected and disabled. Page-group headers toggle collapse/expand in place.
  - Search is live type-ahead. `SearchEditBox` calls `Search.Search("Civilopedia", str)` on text change, fills a separate result popup list, and Enter commits the current query by reopening the Civilopedia on the first hit.
  - Civilopedia maintains its own history trail in `m_kPageTrail`, with clickable breadcrumb buttons and input actions `CivilopediaBack` / `CivilopediaForward`.
  - The base context input handler only special-cases `Escape` to close. Vanilla does not provide a richer keyboard reading/navigation model for article content.
  - Article content itself can contain interactive navigation:
    - `HookupIcon(...)` wires many icon buttons to related Civilopedia pages when icon data includes a search target
    - right-column quote panels may expose a Play button that calls `UI.PlaySound(audio)`
    - technology, civic, unit, government, religion, and similar layouts use icon links for prerequisites, unlocks, upgrades, replacements, uniques, and related entries
  - Content is data-driven from `Assets/Gameplay/Data/Civilopedia.xml`: sections define top tabs, page groups define left-list buckets, pages and page queries populate entries, and page layouts map into Lua templates such as `Simple`, `Technology`, `Unit`, `Government`, `Feature`, and `Leader`.
  - CAI adds a collision-safe `CAI_ACCESSIBILITY_MOD` section through `CivilopediaSections`, using the existing Concepts icon and `SortIndex=-10` so it appears immediately before the base `CONCEPTS` section at index 0. Static pages use the stock `Simple` layout, so no additional Civilopedia UI replacement is required.
  - The section contains three ungrouped reference pages plus `CAI_SCREEN_TUTORIALS` and `CAI_UI_WIDGETS` page groups. Screen tutorials remain one page per catalog entry. UI Widgets instead has three family pages—Leaf, Value, and Container widgets—with each of the 19 included widgets represented by a titled chapter. `MenuItem` and the internal `HorizontalList` do not receive chapters.
  - Widget-family pages add collision-safe `CivilopediaPageLayouts` rows that reuse the stock `Simple` script template. Their ordered widget chapters come from `CivilopediaPageLayoutChapters`; per-page `CivilopediaPageChapterHeaders` rows use each widget title; and `CivilopediaPageChapterParagraphs` hold that widget's audited text. `CivilopediaSupport.CacheData()` resolves all three tables, and `CivilopediaPage_Simple.lua` renders each non-page-title header through `AddChapter(...)`.
  - Screen tutorial articles reuse the catalog's exact title and content localization tags. The introduction reuses the Main Menu tutorial's title and content. Key-binding, search/type-ahead, family-page, and widget reference text lives in `AccessibilityPediaText_CAI.xml`; its widget inventory is limited to concrete widgets exposed by in-game CAI screens. Do not document abstract widget bases or the internal HorizontalList tab-strip implementation as player-facing controls.
  - These rows belong in an in-game gameplay-database action, not `UpdateText`. `scripts/Generate-TutorialCivilopedia.ps1` reads the Lua-local `SCREEN_DEFINITIONS` and `CONTEXT_DEFINITIONS` arrays and combines them with its audited guide/widget inventories to regenerate `src/data/tutorial_civilopedia_CAI.xml`. It automatically follows later tutorial catalog additions and rejects missing ids, titles, content, or duplicate tutorial ids.
  - CAI implication: model Civilopedia as a document browser with separate accessible structures for section tabs, page tree/search results, history, article chapters, and related-entry links. A flat single-speech dump would miss too much of the sighted interaction model.
- `navCursor.lua` owns the CAI map cursor:
  - Public movement uses two LuaEvents: `CAICursorMoveTo(plotId, reason)` for absolute moves and `CAICursorMoveDirection(direction)` for hex stepping.
  - `reason` is `"step"` (adjacent hex), `"jump"` (scanner/search), `"select"` (unit/city selection), or `"snap"` (post-move/initial placement).
  - `CAICursor:MoveTo(plotId, reason)` is the single internal entry point. It updates coordinates and zones, speaks direction on `"jump"` and `"select"`, and fires `LuaEvents.CAICursorMoved(state)` with a state table (`fromPlotId`, `toPlotId`, `distance`, `fromX`, `fromY`, `toX`, `toY`, `reason`).
  - `Map.GetAdjacentPlot(x, y, direction)` returns `nil` when a directional step leaves a non-wrapping map edge. `CAICursor:MoveDirection(...)` retains the current plot and announces `LOC_CAI_NAV_CURSOR_MAP_EDGE` in that case; it does not fire `CAICursorMoved` because no move occurred.
  - Listeners use `state.reason` to decide behavior: PlotToolTip speaks on all reasons except `"select"`. Direction speech is reason-based (`"jump"` and `"select"`), not distance-based.
  - Zone tracking announces continent, owner, territory (XP2 deserts/mountains/seas/lakes/oceans), volcano (XP2), natural wonder, and national park on first entry only.
  - Query-style access remains available through the WorldInput `UI` hijack: `UI.GetCursorPlotID()` and `UI.GetCursorPlotCoord()`.
  - Current implementation stores one global cursor position plus one global zone-memory set (`curX`, `curY`, `lastOwnerZone`, `lastContinentZone`, `lastTerritoryZone`, `lastVolcanoZone`, `lastNationalParkZone`). For hotseat, that state should be split per local player so turn handoff can restore the right plot and zone history before any focus-driven speech resumes.
  - Current default cursor input actions are `CAICursorMoveNorthWest`, `CAICursorMoveNorthEast`, `CAICursorMoveWest`, `CAICursorMoveEast`, `CAICursorMoveSouthWest`, and `CAICursorMoveSouthEast`, bound by default to `Q/E`, `A/D`, `Z/C` with numpad `7/9`, `4/6`, `1/3` as alternate bindings.
- `CAICursorJumpToSelection` is a global world input action bound to `/`. It checks `UI.GetHeadSelectedUnit()` first, then `UI.GetHeadSelectedCity()`, resolves the object's plot with `Map.GetPlot(x, y):GetIndex()`, and raises `LuaEvents.CAICursorMoveTo(plotIndex, "jump")`.
- `CAICursorJumpToCapital` is a navigation-cursor world input action bound to `Ctrl+S`. It resolves the local player's capital through `Players[Game.GetLocalPlayer()]:GetCities():GetCapitalCity()`, converts the city coordinates to a plot index, and raises `LuaEvents.CAICursorMoveTo(plotIndex, "jump")` without changing the current city or unit selection.
  - CAI city selection replacements use `WorldSelectPreviousCity_CAI` (`[`), `WorldSelectNextCity_CAI` (`]`), and `WorldSelectCapitalCity_CAI` (`\`). They mirror vanilla `WorldInput.lua`: previous/next call `UI.SelectPrevCity(UI.GetHeadSelectedCity())` / `UI.SelectNextCity(UI.GetHeadSelectedCity())`; capital resolves the local capital city and calls `UI.SelectNextCity(capital)`. Use CAI-owned name/description locale keys for these replacement actions; reusing vanilla `LOC_OPTIONS_HOTKEY_GLOBAL_*` labels conflicts in the keybinding UI.
- `WorldInput_CAI.lua` wraps vanilla `WorldInput.lua`:
  - CAI installs all `UI` table overrides in one `InstallUIOverrides()` section. Current overrides are `UI.GetCursorPlotID()` and `UI.GetCursorPlotCoord()`.
  - CAI keeps the vanilla `Events.LoadScreenClose` boundary by wrapping `OnLoadScreenClose(...)`; the main game view widget is created and pushed only after the load screen closes.
  - CAI input action entries are records with `Type` and `Action`. Use `Type = "Started"` for repeat-style inputs such as cursor movement, and `Type = "Triggered"` for one-shot inputs such as path info or interface primary action.
  - CAI registers separate dispatchers for `Events.InputActionStarted` and `Events.InputActionTriggered`, while vanilla `WorldInput` keeps its own subscriptions.
  - Interface-mode-specific action records override shared action records for the same action id.
  - `InterfaceInfo` is the Space-bound world action. `WorldInput_CAI.lua` dispatches it to `SpeakActiveInterfacePlotInfo(...)`, which resolves the CAI cursor plot and calls the active `InterfaceInfoHelpers[UI.GetInterfaceMode()]` function.
  - Shared interface widget activation now has a generic event fallback: when the active interface widget does not override `InterfaceWidgetPrimaryAction` or `InterfaceWidgetSecondaryAction`, `WorldInput_CAI.lua` raises `LuaEvents.CAIInterfaceWidgetPrimaryAction(widgetId, plotId)` or `LuaEvents.CAIInterfaceWidgetSecondaryAction(widgetId, plotId)` using the current CAI widget id and the plot id under the CAI cursor.
  - Current default ActionPanel bindings are `ReplaceEndTurn_CAI` on `Ctrl+Space`, `ActionPanelSpeakTurnBlockers` on `Shift+Space`, and `ActionPanelOpenTurnBlockers` on `Ctrl+Shift+Space`; plain Space is reserved for interface-specific information.
  - CAI targeting widgets cover `RANGE_ATTACK`, `CITY_RANGE_ATTACK`, `DISTRICT_RANGE_ATTACK`, `AIR_ATTACK`, `WMD_STRIKE`, `ICBM_STRIKE`, and `COASTAL_RAID`. Return delegates to the matching vanilla execution handler and Escape delegates through vanilla `OnPlacementKeyUp(...)`.
  - Vanilla camera movement has two main paths: continuous camera panning through `UI.PanMap(panX, panY)` / `ProcessPan(...)`, and plot-centering through `SnapToPlot(plotId)` -> `UI.LookAtPlot(plot)`.
  - CAI cursor follow listens to `LuaEvents.CAICursorMoved(x, y, plotId)`, resolves the plot with `Map.GetPlotByIndex(plotId)`, and calls `UI.LookAtPlot(plot)` when the plot is valid. This follows the vanilla snap-to-plot pattern without selecting units/cities or changing interface mode.
  - Do not use `UI.PanMap(...)` for CAI cursor follow; it is designed for analog/held camera pan state and does not naturally target a discrete cursor plot.
- `LoadScreen.lua` is the shell-to-game loading context under `UI/FrontEnd/LoadScreen`:
  - It starts with `Input.SetActiveContext(InputContext.Loading)` and only installs its Lua input handler after `Events.LoadGameViewStateDone`.
  - `Events.LoadScreenContentReady` fires when player configuration/game data is ready enough to populate the loading screen; vanilla `OnLoadScreenContentReady()` fills the leader/civilization presentation.
  - `Events.LoadGameViewStateDone` fires when the game view is ready; vanilla sets `m_isLoadComplete = true`, hides the loading message, shows the Begin/Continue button, switches to `InputContext.Ready`, registers button callbacks, and subscribes to `Events.InputActionTriggered`.
  - The close/continue path is `OnActivateButtonClicked()`: unloads loading textures, raises `Events.LoadScreenClose()`, stops Dawn of Man speech/menu music, dequeues the load-screen popup, switches input to `InputContext.World`, resets active lens if needed, and performs Play-By-Cloud follow-up notification checks.
  - Escape, `StartGame`, and `StartGameAlt` only continue when `m_isLoadComplete` is true. Before that, input should not force the game forward.
  - Resync loads, multiplayer games, World Builder editor loads, and automation auto-start can bypass the visible Begin/Continue button by directly calling `OnActivateButtonClicked()` after game-view state is ready.
  - Visual layout is: full-screen black/background root, fallback/loading text, leader background image, leader portrait area, central colored banner, civilization icon/name, era line, leader/challenge name, leader/challenge text, feature/ability stack, loading message, and final Begin/Continue button.
  - Content sources are `Network.GetLocalPlayerID()` with hotseat fallback, `PlayerConfigurations[playerID]`, `GameInfo.LoadingInfo[leaderType]`, `GameInfo.Leaders`, `GameInfo.Eras`, `UI.GetSaveGameMetaData()` for saved-game era, `Challenges` loading texts, and `GetLeaderUniqueTraits()` / `GetCivilizationUniqueTraits()` from `Civ6Common.lua`.
  - Dawn of Man audio is separate from screen-reader output. CAI should not rely on it as the accessible announcement, and should avoid talking over it unless the user explicitly requests load-screen speech.
  - Suggested CAI access is a `LoadScreen.lua` wrapper imported/replaced in the front-end action set. Wrap `OnLoadScreenContentReady()` to capture/live-read populated control text and build a transient `Dialog` or `Panel`; wrap `OnLoadGameViewStateDone()` to add/enable the Begin/Continue button and speak readiness; wrap `OnActivateButtonClicked()` or `OnHide()` to pop/destroy the load-screen widget before the world view is initialized.
  - Keep the existing `WorldInput_CAI.lua` `Events.LoadScreenClose` boundary unchanged. Load-screen accessibility should end before `CAI_MainGamePanel` is pushed.
- `NotificationPanel.lua` owns the ambient right-side notification rail:
  - It creates generic `ItemInstance` rows under `Controls.ScrollStack` and tracks active entries in local `m_notifications[playerID][typeName]`.
  - Each visual notification instance has `MouseInArea`, `MouseOutArea`, `IconBG`, `IconBGInvalidPhase`, `Icon`, `CountImage`, `DismissStackButton`, expanded `TitleInfo`, `Summary`, `LeftArrow`, `RightArrow`, `PagePipStack`, and `Pages`.
  - Notification data should come from the live `Notification` object via `NotificationManager.Find(playerID, notificationID)`. Useful methods include `GetMessage()`, `GetSummary()`, `GetTypeName()`, `GetType()`, `GetIconName()`, `GetGroup()`, `GetCount()`, `GetEndTurnBlocking()`, `CanUserDismiss()`, `IsVisibleInUI()`, `IsValidForPhase()`, `IsLocationValid()`, `GetLocation()`, `IsTargetValid()`, `GetTarget()`, `Activate(true)`, and `GetValue(key)`.
  - `NotificationManager.SendNotification(playerID, notificationType, text, desc, mapX, mapY)` accepts the numeric notification type id, not the string type name. The tutorial code path proves that `notification:GetType()` can be compared directly against `GameInfo.Notifications["NOTIFICATION_FOO"].Hash`, so resolving custom notification ids through `GameInfo.Notifications[...]` is a safe runtime path when a `NotificationTypes.*` constant is uncertain.
  - Vanilla only creates notification rail entries when the notification is visible in UI, has a displayable icon, is the first visual entry for that type/stack, and is not an end-turn blocker. End-turn blockers are normally displayed by `ActionPanel`; CAI handles them outside the notification center.
  - `SetNotificationText(...)` sets the expanded title from `Locale.Lookup(pNotification:GetMessage())` and summary from `Locale.Lookup(pNotification:GetSummary())`.
  - Left-click activation calls `pNotification:Activate(true)`, which later raises `Events.NotificationActivated`; vanilla type-specific activate handlers then open the correct screen or look at/select the target.
  - Right-click dismiss calls `NotificationManager.Dismiss(...)` only when `pNotification:CanUserDismiss()` is true.
  - Stacked notifications share one visual row by type name. `LeftArrow` / `RightArrow` move the active index and either call `LookAtNotification(...)` or, for `COMMAND_UNITS`, select previous/next ready unit. The count badge's hidden `DismissStackButton` right-click dismisses every dismissible notification in that stack.
  - Wrong-phase notifications keep a visual row but left-click does nothing, right-click can dismiss, and the icon background switches to `IconBGInvalidPhase`.
  - Type-specific vanilla activation handlers include research/civic choosers, production, government/policies, raze city, pantheon/religion/artifact/great people/envoys, espionage escape route, continent lens, boost/completed popups, relic popup, PBC popups, user-defined notification activation, city ranged attack, command units, and default look-at/select behavior.
  - `RegisterHandlers()` explicitly assigns all nine `NotificationTypes.USER_DEFINED_*` entries both default handlers and `OnUserNotificationActivate`, which raises `LuaEvents.NotificationPanel_UserNotificationActivate(playerID, notificationID)`. These slots are convenient, but CAI can also register its own custom notification hashes in `g_notificationHandlers[...]` through the notification-panel wildcard include path.
  - `GetHandler(notificationType)` falls back to `NotificationTypes.DEFAULT` when a type has no explicit registration. That means a database-added notification type can still get generic rail UI, but it will not automatically get a custom Lua activation hook unless CAI or vanilla registers one.
  - Vanilla notification add audio is dispatched centrally in `OnNotificationAdded(...)`, not inside most `Add` handlers. The flow is `local handler = GetHandler(pNotification:GetType())`, then `handler.Add(pNotification)`, then `if handler.AddSound ~= nil and handler.AddSound ~= "" and m_isLoadComplete then UI.PlaySound(handler.AddSound) end`.
  - Because many built-in notification types already have explicit handler tables from `RegisterHandlers()` but no `AddSound`, changing only `NotificationTypes.DEFAULT.AddSound` would not cover them. The reliable global fallback hook is to wrap `OnNotificationAdded(...)` and play a default sound such as `ALERT_NEUTRAL` when the resolved handler's `AddSound` is nil or empty.
  - CAI keeps that fallback outside the base `Add` handlers so custom handlers like `OnMetCivAddNotification(...)`, which already play their own special sound before calling `OnDefaultAddNotification(...)`, can be excluded. It additionally requires the notification to pass the notification center's own availability predicate: visible, not dismissed or expired, and backed by a live vanilla rail instance. This intentionally makes fallback audio narrower than vanilla `AddSound` behavior so only notifications represented in the CAI notification center receive the neutral fallback.
  - A missing notification-handler `AddSound` does not prove the notification is otherwise silent. `NOTIFICATION_DISCOVER_GOODY_HUT` has no `AddSound` but its tribal-village discovery cue is produced outside `NotificationPanel.lua`; CAI therefore excludes `NotificationTypes.DISCOVER_GOODY_HUT` from neutral fallback audio, alongside `PLAYER_MET`.
  - `WorldView/CityBannerManager.lua` owns the floating map banners for cities and city-linked districts:
  - It creates two full city-center variants: `TeamCityBanner` for the local player and `OtherCityBanner` for everyone else, plus mini-banners for aerodromes, missile silos, encampments, and other districts.
  - Core city-center visuals for both variants are city name, population number, population meter, defense strength, district and outer-defense health bars when relevant, majority-religion or pantheon icon, and player-color styling. Foreign-city banners also show the owner's civilization icon.
  - The name line can carry situational markers: capital icon, trading-post icon, disabled trading-post icon, under-siege icon, occupied icon, insufficient-housing icon, insufficient-amenities icon, and city-state quest icon. The quest tooltip lists each active quest from that city-state.
  - Local-player city banners additionally show production directly in the banner: current production icon, progress meter, turns-left label, tooltip text, and a clickable production button. Foreign-city banners keep some of those controls in XML but vanilla hides the owner-only production visuals.
  - Population always shows as a large number. For the local player only, the banner also shows turns until growth or starvation, color-codes that turns label, and adds a tooltip with growth, stagnation, or starvation details plus food surplus.
  - Defense info comes from the city-center district: defense strength number, garrison hit points, optional outer-defense hit points, and color-coded health bars. A city-range-strike button appears only for the local player when the city can currently perform a ranged strike.
  - Religion has two layers. The normal banner can show a majority-religion icon or pantheon icon with a tooltip. When the religion lens is active, an attached religion panel appears under the banner with religion icons present in the city, conversion-turns status, a detailed follower list with pressure values, outgoing pressure, and a follower pie chart.
  - CAI no longer requires that religion lens panel to be visibly open for banner speech. `CityBannerManager_CAI.lua` now falls back to live `city:GetReligion()` data for conversion turns, follower-pressure lines, and outgoing pressure when the vanilla religion detail panel is hidden.
  - `AerodromeBanner` shows stationed aircraft as `current/max`, a capacity tooltip, and a dropdown list of air units with icon, uppercase name, and dimmed styling for units that cannot move. When the plot is only revealed, the count bar is hidden and the dropdown is disabled.
  - `WMDBanner` is local-player-only and shows nuclear and thermonuclear stockpile counts plus strike buttons that are enabled only when the silo has valid targets.
  - `EncampmentBanner` shows district defense strength, district hit points, optional outer-defense hit points, and a district ranged-strike button when usable by the local player.
  - `DistrictBanner` is normally force-hidden and becomes visible through the city-details or empire-details lens. When shown it displays the district or wonder icon, an under-construction overlay if incomplete, and a tooltip with the district or wonder name plus description.
  - CAI now ignores that lens-only force-hide for `BANNERTYPE_OTHER_DISTRICT` when resolving banner info reads. For accessibility reads, generic district name/type/construction/description can be spoken even with the lens off, as long as the underlying mini-banner instance exists and the plot is not fully hidden by fog.
  - Expansion 1 (`DLC/Expansion1/UI/CityBanners/CityBannerManager.lua`) keeps the base city-center and mini-banner inventory, but adds loyalty and governors to city-center banners:
    - A governor status widget in `CityStatusStack` with governor portrait/fill art, tooltip, turns-left label, and ambassador count.
    - A loyalty flyout under the city banner with a compact bar and an expanded panel. The compact panel shows owner civ icon, most-influential civ icon, a loyalty pressure icon, and a loyalty fill meter.
    - The expanded loyalty panel adds a loyalty percentage label plus per-source breakdown widgets for population pressure, governors, happiness, other modifiers, city-state bonus, and free-city bonus, and an `IdentityBreakdownStack` of detailed influence lines.
    - Expansion 1 also replaces the old center-row production display with `CityStatProduction` in the city status stack for city-center banners, alongside the population and governor widgets.
  - Expansion 2 (`DLC/Expansion2/UI/CityBanners/CityBannerManager.lua`) keeps the Expansion 1 loyalty/governor city-center additions and adds:
    - Two new improvement mini-banner types: `BANNERTYPE_MOUNTAIN_TUNNEL` and `BANNERTYPE_QHAPAQ_NAN`, each rendered as a small icon-only improvement banner with the improvement-name tooltip.
    - A new `CityDetailsEffects` stack and `CityDetailEffect` instances at the top of the city banner. Firaxis marks this in code as the XP2 difference for the power menu.
    - New `CityInfoType` and `CityInfoCondition` instance managers in the city info row, including rising/falling condition overlays. These are the extra icon/condition slots Expansion 2 adds around the city name for city detail summaries.
  - Click behavior is important for accessibility parity: clicking your own city banner selects the city, clicking a met major civ banner opens diplomacy, clicking a met city-state banner opens the city-state panel, clicking during trade-route mode sets that city as the destination, and clicking local mini-banners selects their district or triggers unit or strike actions.
  - CAI extends vanilla through the wildcard include file `src/UI/inGame/CityBannerManager_CAI.lua` and exposes `ExposedMembers.CAIInfo:RequestCityBannerInfo(requestedKeys)` for on-demand cursor reads.
  - The banner reader tracks the current cursor plot from `LuaEvents.CAICursorMoved`, resolves the active live city or mini-banner for that plot, and only speaks banner-backed information.
  - Dedicated city-banner bucket actions are `CityBannerReadIdentityStatus`, `CityBannerReadGrowthInfluence`, `CityBannerReadReligion`, and `CityBannerReadDiplomacy`.
  - The input-action bucket mapping is table-driven in `CityBannerManager_CAI.lua`, with separate `city` and `district` key lists so bucket order and banner-type variants can be edited in one place.
  - Base data defines `KIND_NOTIFICATION` rows in `Gameplay/Data/Notifications.xml` under both `Types` and `Notifications`. The base game already reserves `NOTIFICATION_USER_DEFINED_1` through `NOTIFICATION_USER_DEFINED_9` there, all grouped as `USER`.
  - CAI access uses `NotificationPanel_CAI.lua` through vanilla's `include("NotificationPanel_", true)` wildcard extension path rather than a full `ReplaceUIScript`.
  - `Ctrl+N` opens a transient `Panel` notification center through the `NotificationPanelOpenList` input action. The panel owns a two-page `TabControl`: Notifications and Message buffer.
  - The Notifications page contains the current-notifications tree. Single notifications are direct tree leaves; grouped notification types become expandable parent nodes with one child per live notification.
  - The Message buffer page orders its controls as message list, category dropdown, then Copy buffer. The dropdown uses the buffer's full category cycle (`all`, `notification`, `reveal`, `combat`, `gossip`, `movement`, and `chat`) and updates the buffer's shared active filter. Closing and reopening the Notification Center preserves that filter and restores the matching dropdown selection.
  - `MessageBuffer_CAI.lua` exposes `MessageBuffer.GetActive` as `ExposedMembers.CAI.GetMessageBuffer`. UI consumers use the returned buffer's `GetCategories()`, `GetEntries(filter)`, `GetFilter()`, and `SetFilter(filter)` methods instead of reading private fields.
  - Each buffer entry stores a stable per-buffer id and the `Game.GetCurrentGameTurn()` on which it was appended, matching the turn-number model used by gossip records.
  - Location text uses `CAIHexCoordUtils.directionString(cursorX, cursorY, targetX, targetY)`, the same live navigation-cursor-relative direction string used by World Scanner entries. List rows are ordered as message, location when present, then turn. `SpeakEntry()` and the manual previous/next/first/last actions speak message followed by that live location string, but omit turn.
  - Non-location list entries are `StaticText`; entries with valid x/y locations are `Button` widgets. Activating a location row closes the Notification Center and passes that exact entry to `MessageBuffer:JumpToEntryLocation(entry)`, which uses the same `LuaEvents.CAICursorMoveTo(plotIndex, "jump")` path as the message-buffer move-to input action.
  - The message list defaults to its newest matching row on first entry. Stable focus keys let later rebuilds retain the current row with `mgr:CaptureFocusKey(...)` / `mgr:RestoreFocus(...)`. The open list rebuilds when `LuaEvents.CAIAppendToMessageBuffer` fires.
  - Copy buffer reads `GetEntries()` with the current filter, converts Civ VI newline/icon tokens into clipboard-friendly text, and copies message followed by turn through `UIManager:SetClipboardString(...)`. Clipboard lines deliberately omit location; players select the `all` category before copying when they want the entire buffer.
  - Leaf `Enter` / `Space` calls `pNotification:Activate(true)` when valid for the current phase.
  - Leaf `Delete` calls `NotificationManager.Dismiss(...)` when `CanUserDismiss()` is true.
  - Group `Delete` dismisses all dismissible notifications in that group.
  - Notification leaf focus keys retain the live notification id as `notification:<id>`.
  - `NotificationPanel_CAI.lua` captures the base `OnNotificationAdded(...)` / `OnNotificationDismissed(...)` globals before base `Initialize()` runs, then replaces those globals so base `LateInitialize()` registers the wrapped versions.
  - The wrapped add/dismiss paths preserve vanilla behavior, then rebuild the open Notifications tree while restoring its stable focus key.
  - CAI ties notifications back to vanilla's own `GetNotificationEntry(playerID, notificationID)` lookup and requires the returned entry to have a live `m_Instance`, matching the actual vanilla rail. This intentionally excludes end-turn blockers, which ActionPanel should handle.
  - Rebuilding the tree filters out notification ids that no longer have a vanilla rail instance, plus `IsDismissed()` / `IsExpired()` when those optional methods are available, so stale ids still returned by `NotificationManager.GetList(...)` are not reintroduced.
  - Activating a leaf closes the notification center before calling `pNotification:Activate(true)`, so vanilla action/camera behavior runs after CAI focus is gone.
  - CAI tutorial goals now use custom notification types `NOTIFICATION_CAI_TUTORIAL_GOAL_ADDED` and `NOTIFICATION_CAI_TUTORIAL_GOAL_COMPLETED`, resolved in Lua with `DB.MakeHash(...)`, sent with direct text via `NotificationManager.SendNotification(...)`, and activated through custom `g_notificationHandlers[...]` entries that open the tutorial goals list.
  - Vanilla `LookAtNotification(...)` syncs the CAI cursor through `LuaEvents.CAICursorMoveTo(plotId, "jump")` when a notification has a valid location or target, except while the CAI notification center is open. Browsing the tree should not move the cursor.
  - `NotificationPanel_CAI.lua` also speaks new rail notifications from `Events.NotificationAdded` as `LOC_CAI_NOTIFICATION_ALERT` with the notification summary, and retains any resolved coordinates only as the message-buffer location payload for explicit jump-to-location commands.
  - Notification add speech is de-duplicated by notification id until dismissal because Civ VI can surface the same notification id through the add path more than once.
  - Vanilla `UnitCaptured.lua` registers `Events.UnitCaptured` only when `GameConfiguration.IsAnyMultiplayer()` is false. In single-player, a capture involving the local player opens an in-game `PopupDialog`; any multiplayer configuration suppresses that popup because capture information is expected through the notification bar.
  - `Events.UnitCaptured(currentUnitPlayerID, unitID, owningPlayerID, capturingPlayerID)` identifies the capturing player in its fourth argument. `UnitCaptured_CAI.lua` retains the vanilla popup path, gives its context an extended CAI input handler, and in multiplayer appends `LOC_CAPTURE_UNIT` to the notification message-buffer category only when `capturingPlayerID == Game.GetLocalPlayer()` and the prior owner is someone else.
- Vanilla `ActionPanel.lua` / `ActionPanel.xml`:
  - Main purpose is the bottom-right end-turn/action button plus end-turn blockers. It reads blockers from `NotificationManager.GetFirstEndTurnBlocking(playerID)` and `NotificationManager.GetAllEndTurnBlocking(playerID)`.
  - Known blocker types are mapped in global `g_kMessageInfo` to localized message text, tooltip text, icon name, and optional sound. Covered blockers include units/stacked units, research, civic, policy slot, government change, raze city, production, pantheon/religion/belief, envoys, great person, spy escape route/dragnet priority, and artifact choice.
  - Primary controls are `EndTurnButton`, `EndTurnButtonLabel`, `EndTurnText`, and `CurrentTurnBlockerIcon`. The icon uses `SetIcon(icon)` and `EndTurnText` is truncated through `TruncateStringWithTooltip(...)`.
  - Extra blocker controls are `TurnBlockerButton2`, `TurnBlockerButton3`, and `TurnBlockerButton4`. Overflow blockers are built from `TurnBlockerInstance` rows under `OverflowStack`, opened by `OverflowCheckbox`.
  - Vanilla deduplicates ActionPanel blocker buttons by blocker type through `BlockerIsVisible(blockerType)`. Unlike the notification rail, ActionPanel does not expose left/right cycling between individual notifications of the same type.
  - The count badge `CountImage` / `Count` shows when there are two or more notifications of the active blocker type, but activating the button still calls `DoEndTurn(blockerType)`, which uses `NotificationManager.FindEndTurnBlocking(blockerType, playerID)` rather than a specific notification id.
  - Left-clicking the main button calls `DoEndTurn()`. Left-clicking a secondary/overflow blocker calls `DoEndTurn(blockerType)`. Right-clicking the main button only dismisses the city ranged attack notification in the special city-ranged-attack state.
  - `DoEndTurn(...)` can unready a multiplayer turn, select next ready unit, select a ranged-attack city and enter `InterfaceModeTypes.CITY_RANGE_ATTACK`, request `ActionTypes.ACTION_ENDTURN`, or raise `LuaEvents.ActionPanel_ActivateNotification(pNotification)` for generic blockers.
  - The panel has special non-notification states for auto-end-turn units with moves remaining and city ranged attacks, controlled by user options `AutoEndTurn` and `CityRangeAttackTurnBlocking`.
  - Waiting states set `EndTurnText` and tooltip to please-wait, waiting-for-player, Play-By-Cloud uploading/uploaded, or autoplay text. Multiplayer can include other active human player names and the unready-turn tooltip.
  - The era display uses `EraContainer`, `EraIndicator`, `EraToolTipArea1`, `EraToolTipArea2`, and `EraPipInstance`; `PopulateEraData()` builds pips from `GameInfo.Eras()`, and `RealizeEraIndicator()` rotates the indicator to the local player's era.
  - Turn timer display uses `TurnTimerContainer`, `TurnTimerMeter`, `TurnTimerLabelBG`, and `TurnTimerLabel`; `OnTurnTimerUpdated(...)` sets progress, active/inactive color, tooltip, countdown text, and low-time tick sounds.
  - Observer mode shows `ObserverButtonLabel` and `EndObserverModeButton`, both raising `LuaEvents.ActionPanel_EndObserverMode()`.
  - Input paths include `Input.GetActionId("EndTurn")`, `Events.InputActionTriggered`, and `ContextPtr:SetInputHandler(OnInputHandler, true)`. Enter triggers normal end turn; Shift+Enter forces `ActionTypes.ACTION_ENDTURN` with `REASON = "UserForced"` when no tutorial is running.
  - CAI `ActionPanel_CAI.lua` wraps `OnInputActionTriggered` with `WrapFunc(...)` after including vanilla and does not add its own input-action event subscription. `ActionPanel.lua` registers `OnInputActionTriggered` later from `LateInitialize()`, so the wrapped global is what vanilla registers. Adding another `Events.InputActionTriggered.Add(...)` from the wrapper can double-fire the action.
  - CAI replaces the context input handler with a manager-only handler via `ContextPtr:SetInputHandler(OnCAIActionPanelInputHandler, true)`. It routes input through `ExposedMembers.CAI_UIManager:HandleInput(...)` and otherwise returns false, intentionally avoiding vanilla `OnInputHandler` because vanilla maps Enter / Shift+Enter directly to end-turn actions.
  - `Events.EndTurnBlockingChanged` is the blocker change event, but vanilla's `OnEndTurnBlockingChanged(...)` does not set the spoken action text. The main button tooltip is assigned from `OnRefresh()` via `Controls.EndTurnButton:SetToolTipString(...)`, so CAI wraps and reinstalls the refresh handler with `ContextPtr:SetRefreshHandler(OnRefresh)`, then reads the live `EndTurnButton` tooltip after refresh. Changed-only automatic speech uses `SpeakTurnBlockers` while the local player's turn is active and `SpeakBetweenTurnsMessage` while it is inactive. `ActionPanelSpeakTurnBlockers` forces the same current tooltip on demand regardless of those settings. Speech remains gated by `ContextPtr:IsHidden()` and, during tutorials, `LuaEvents.CAI_TutorialActionPanelAllowed`.
  - Hotkey-triggered end turn must respect live `EndTurnButton` / `EndTurnButtonLabel` disabled state before calling vanilla `OnInputActionTriggered(...)`. During tutorials, it must also respect the current detailed item's ActionPanel UI trigger state from `LuaEvents.CAI_TutorialActionPanelAllowed`; the tutorial can block ActionPanel interaction through `UITutorialManager` without necessarily making the vanilla end-turn button report disabled to CAI. Also respect `TutorialSlowTurnEnableAnim` visibility because vanilla uses it as a short click-spam shield at turn begin.
  - CAI turn blocker list id is `CAIActionPanelTurnBlockerList`, opened by default with `Ctrl+Shift+Space`. It uses the vanilla primary action plus one deduplicated row per blocker type. Enter on a row calls `DoEndTurn()` or `DoEndTurn(blockerType)`, matching vanilla button behavior. Escape removes the list from the CAI stack.
  - Tutorial-only controls include `TutNotificationPointer`, many `TutSelectEndTurnAction*` callouts, and `TutorialSlowTurnEnableAnim`.
- Vanilla `DedicationPopup.lua` / `DedicationPopup.xml` (XP1 / XP2 additions):
  - Each dedication row is a `SelectCheck` `GridButton` under `Controls.CommemorationsStack` with two readable child labels inside its detail stack: `MomentCategory` and `MomentBonuses`.
  - `CreateCommemoration(...)` fills `MomentCategory` from `CategoryDescription` and fills `MomentBonuses` from the age-appropriate bonus description text, appending the normal-age quest line for heroic ages when `IsPlayerAlwaysAllowedCommemorationQuest(...)` is true.
  - The CAI popup should keep reading the live `MomentBonuses` control text for tooltip speech, but if that control contains a heading line followed by bonus lines, trim only the first line so the checkbox label does not immediately repeat the dedication heading before the actual bonus text.
- Vanilla `GovernmentScreen.lua` / `GovernmentScreen.xml`:
  - The default keybinding is `ToggleGovernment`, bound to `LOC_OPTIONS_KEY_F7` in `Configuration/Data/InputConfiguration.xml`. `LaunchBar.lua` handles the action by calling `OnOpenGovernment()`, which raises `LuaEvents.LaunchBar_GovernmentOpenMyGovernment()` or `LuaEvents.LaunchBar_GovernmentOpenGovernments()` depending on current government/civic state.
  - The screen is a low-priority popup queued with `UIManager:QueuePopup(ContextPtr, PopupPriority.Low, { RenderAtCurrentParent=true, InputAtCurrentParent=true, AlwaysVisibleInQueue=true })`.
  - The top tab strip has up to three tabs: `ButtonMyGovernment`, `ButtonPolicies`, and `ButtonGovernments`. My Government and Policies are hidden until the player has a current government. Tab labels change between view/change wording based on `IsAbleToChangeGovernment()` and `IsAbleToChangePolicies()`.
  - My Government shows the current government card on the left/top area: government name, slot/stat summary, government art, inherent bonus, current accumulated/legacy bonus progress, and a scrollable heritage bonus stack. Below/alongside it are the same four active policy rows used by the Policies tab.
  - Policies shows four horizontal active policy rows: Military, Economic, Diplomatic, and Wildcard. Each row has a row label, count badges, category icon, watermark, optional empty-state text, and active policy card instances in `StackMilitary`, `StackEconomic`, `StackDiplomatic`, or `StackWildcard`.
  - The policy catalog appears to the right of the policy rows. It is a parchment panel with a horizontal scroll panel (`PolicyScroller` / `PolicyCatalog`) containing `PolicyCard` instances, plus filter tab buttons in `FilterStack`, back/forward filter scroll buttons, a hidden fallback pulldown, and bottom action buttons `ConfirmPolicies` / `UnlockPolicies`.
  - Policy cards are `140x150` draggable visual cards with title, description, colored type background, optional new icon, and tooltip. Drag/drop is the primary mouse flow; double-clicking a catalog card calls `AddToNextAvailRow(cardInstance)`, and double-clicking an active row card removes it.
  - Active policy row card rebuild is owned by `RealizeActivePoliciesRows()`. It destroys row children, rebuilds each occupied or empty slot, registers drag/drop/double-click/Civilopedia callbacks, updates row counters, and updates the confirm/unlock button state.
  - The Governments tab is a horizontal scroller (`GovernmentScroller`) with a parchment background. Government cards are grouped into columns by total policy-slot count, with vertical era-divider labels rendered between columns. Each government card shows name, stats, art strip, mini slot icons, inherent bonus, accumulated bonus/progress, selected overlay, locked overlay, and prerequisite civic icon/progress when locked.
  - Vanilla communicates government tier through that chart layout rather than card text. `RealizeGovernmentsPage()` groups cards by total slots (Chiefdom 2, Tier 1 governments 4, Tier 2 governments 6, Tier 3 governments 8, and Gathering Storm Tier 4 governments 10) and inserts Roman-numeral dividers I–IV between the columns. Rise and Fall and later government database rows also carry explicit `Tier="Tier1"` through `Tier="Tier4"`, but `GovernmentScreen.lua` does not read that field; Chiefdom has no numbered tier. Historical Moments use the explicit tier data independently of the screen.
  - Accessible government-row labels state the numbered tier. Prefer the row's explicit `Tier` and its `GovernmentTiers.Sorting` value when available; base-game rows fall back to the same total-slot grouping used by vanilla. Chiefdom remains unnumbered.
  - Government selection is driven by `OnGovernmentSelected(governmentType)`, which opens a vanilla `PopupDialogInGame` confirmation. Accepting calls `RequestChangeGovernment(...)`, then usually switches to the Policies tab.
  - Vanilla also permits activating the already-active government card. `OnGovernmentSelected(...)` does not exclude the current government before calling `GetAnarchyTurns(...)`, so it can display the normal previously-adopted-government anarchy warning even though `RequestChangeGovernment(...)` is a same-government request and the engine leaves the current government in place. The warning alone therefore does not establish that anarchy began; confirm the live state with `IsInAnarchy()` / `GetAnarchyEndTurn()`. Actual anarchy applies when changing to a previously adopted different government, and `CanChangeGovernmentAtAll()` prevents further government changes until it ends.
  - During actual anarchy, `PlayerCulture:GetCurrentGovernment()` returns `-1`, the same sentinel seen before Code of Laws unlocks governments. Any government summary must check `IsInAnarchy()` first: in anarchy, identify the state with `LOC_GOVERNMENT_ANARCHY_TURNS` and `GetAnarchyEndTurn() - Game.GetCurrentGameTurn()`; only use `LOC_GOVERNMENT_DOESNT_UNLOCK` when there is no current government and the player is not in anarchy.
  - Policy confirmation is driven by `OnConfirmPolicies()`. It builds clear/add slot lists from `m_ActivePoliciesBySlot`, calls `RequestPolicyChanges(clearList, addList)`, resets the dirty flag, and closes the screen after confirmation.
  - Escape closes through `Close()`. Enter is consumed by `OnInputHandler()`; when the policies tab confirm overlay is visible and policies can be changed, Enter calls `OnConfirmPolicies()`.
  - Tab activation should use the vanilla tab callback path, not direct calls to `SwitchTabTo...()`. `m_tabs` is local to `GovernmentScreen.lua`, but `CreateTabs.AddTab(...)` stores the same select path on `Controls.Button...["CallbackFunc"]`; calling that callback changes vanilla visual selection and rebuilds the vanilla body. CAI tab widgets should switch tabs from `OnFocusEnter` so left/right arrow movement through the tab bar changes the active tab.
  - Vanilla tab text is set in `RealizeTabs()` on `Controls.ButtonGovernments` / `Controls.ButtonPolicies` with `SetText(Locale.Lookup(...))`. CAI should read the live control text rather than duplicating that label logic. For GovernmentScreen tab buttons, `GetTextControl():GetText()` may expose the visible text if direct `GetText()` is empty. For vanilla-populated controls such as `ModalScreenTitle`, `ModalScreenClose`, tab buttons, confirm buttons, and unlock buttons, do not add a CAI localization fallback; read `GetText()` / text subcontrols / `GetToolTipString()` from the control.
- Vanilla registers `Mouse.eMouseEnter` hover sounds with `UI.PlaySound("Main_Menu_Mouse_Over")` for its government tabs, `ConfirmPolicies`, `PolicyPanelCheckbox`, unlock buttons, and filter navigation buttons. CAI should mirror the relevant exposed widgets by playing the same sound from `OnFocusEnter` for tabs, Confirm Policies, View All Policies, Unlock Policies, and Unlock Governments.
- Vanilla policy dragging has two distinct feedback cues: `OnStartDragFromCatalog(...)` and `OnStartDragFromRow(...)` play `UI_Policies_Card_Take`, while `OnDropFromCatalog(...)` and `OnDropFromRow(...)` always play `UI_Policies_Card_Drop`. The accessible slot picker treats its lifetime as the equivalent drag: opening plays the take cue, and successful selection or Escape/cancellation plays the drop cue. Delete remains the equivalent of vanilla double-click removal and does not add drag sounds.
- Government-card information parity requires the full `BonusAccumulatedTooltip` and the progress meter shown when a locked government's prerequisite is the currently progressing civic. Civic progress uses `GetCulturalProgress(...) / GetCultureCost(...)` rounded to the same whole-percent form used by the Civics tree. In base rules, vanilla renders `BonusFlatAmountPreview` as the percentage beside `BonusAccumulatedText` and puts the detailed formula in `BonusAccumulatedTooltip`; accessible output should combine those into one detailed legacy-bonus line rather than reading the short and full descriptions consecutively. `BonusFlatAmountPreview` is not an additional bonus. Rise and Fall removes `CAPABILITY_GOVERNMENTS_LEGACY_BONUSES`, hides the percentage/quill presentation, and reuses the value as the numeric amount behind the already-described passive effect.
- Vanilla marks policies unlocked by the civic completed this turn with `NewIcon`; Rise and Fall additionally marks newly available Dark Age policies on the era-start turn. Dramatic Ages adds a Golden-policy filter and Golden card art but its shipped `PopulateLivePlayerData(...)` override repeats the Dark-Age-new test rather than marking Golden policies specially. Accessible picker/catalog row labels append localized `New` for the completed-civic case and the official Dark-Age era-start case; their tooltips independently identify Dark- and Golden-Age eligibility.
- Vanilla government cards support Civilopedia opening through right-click. Accessible government list rows map this to Shift+Enter, matching accessible policy rows.
- Vanilla `ConfirmPolicies` changes its live text between `GetConfirmPoliciesText()` and the assign-all-policies warning when slots remain empty, and disables the control when `m_isPoliciesChanged` is false or any slot is empty. The accessible button always uses `GetConfirmPoliciesText()` because `Assign all policies` is misleading as an action label; while disabled, its tooltip enumerates every empty slot as a separate localized blocker using its category-local ordinal, for example `Must fill slot 1 for Economic Policies`, or states that there are no policy changes to confirm when all slots are filled.
- In base rules, a government's legacy effect consists of its active-government flat amount plus an incrementing amount. The incrementing amount grows at the modifier's game-speed-scaled interval and remains as heritage after leaving that government; the flat amount does not carry over. For example, base Oligarchy starts at +20% unit experience and adds +1 percentage point every five standard-speed turns. Rise and Fall and Gathering Storm delete the legacy-bonus capability and the accumulating modifiers: the former accumulated-bonus description becomes an ordinary fixed passive that applies only while the government is active. Expansion 2's government data explicitly notes that accumulated bonuses no longer accumulate and `BonusType` is retained for UI rather than gameplay.
- Government bonus presentation must therefore avoid repeating the same database effect through its description, full tooltip, and raw flat amount. In expansion rules, do not announce a separate `Flat bonus`; use the passive description. In base rules, prefer the detailed legacy formula over the redundant short description, distinguish earned heritage from that fixed starting amount, and omit zero values. An accumulated or carried heritage value is a percentage point amount, but vanilla's heritage row visually separates that number from its effect; accessible output must recombine the government's `BonusAccumulatedText`, the percentage, and `LOC_GOVT_HERITAGE_BONUS_PREV`, for example `Discount on Faith purchases. Current accumulated bonus: 20%. From previous turns under Theocracy.` Use the live vanilla section labels: base localizes them as `Inherent bonus` and `Legacy bonus`, while Rise and Fall overrides the same tags to `Major bonus` and `Minor bonus`. Only expose current and carried heritage when `CAPABILITY_GOVERNMENTS_LEGACY_BONUSES` is enabled; otherwise the old generic legacy explanation leaks into expansion games even though that mechanic is disabled. Collapse consecutive `[NEWLINE]` tokens in government stats so XP2's intentional paragraph spacing does not become an empty spoken element.
- `GovernmentScreen_PiratesScenario.lua` is a functional Relic-screen override, not presentation-only. Under `RULESET_SCENARIO_PIRATES` it pauses effects, raises `RelicScreen_RelicScreenOpened`, forces a Policies open, relabels the screen and active wildcard row as Relics, allows relic changes once per turn without the ordinary civic/free-change gate, keeps the confirm button visible with Relic-specific text, disables the ordinary confirmation popup, removes policy filters, and resumes effects on close. Any full `GovernmentScreen` replacement must select this wrapper before expansion/base variants; otherwise Pirates receives ordinary Government-screen behavior.
- CAI accessibility should wrap this screen rather than replacing its game logic. The accessible government screen intentionally exposes only the Policies and Governments tabs; vanilla My Government can still exist internally, but a CAI My Government open state maps to Governments, where the selected row carries the current-government summary and the separate Policies page carries the active cards.
  - CAI Policies is slot-first: four expandable row categories contain one child per live policy slot. Slot data can be rebuilt from `Players[Game.GetLocalPlayer()]:GetCulture():GetNumPolicySlots()`, `GetSlotType(slotIndex)`, and `GetSlotPolicy(slotIndex)`, mirroring vanilla `PopulateLivePlayerData(...)`; this is more reliable than capturing only filled card instances because empty cards do not pass through `RealizePolicyCard(...)`.
  - CAI policy assignment uses Enter on a focused slot to push a filtered policy `Treeview`. Picking an unused legal policy calls `SetActivePolicyAtSlotIndex(slotIndex, policyType)` for the exact slot. Delete on a filled slot calls `RemoveActivePolicyAtSlotIndex(slotIndex)`. Dedicated policy movement is intentionally omitted; replace/remove/reassign covers the needed gameplay result without exposing vanilla drag/drop movement as its own workflow.
  - CAI slot widgets should keep stable widget identity during pending policy edits. Store a small pending slot-policy mirror for labels/tooltips and suppress CAI body rebuilds while calling vanilla `RealizePolicyCatalog()` / `RealizeActivePoliciesRows()` after assignment/removal. Rebuilding from live player culture during an unconfirmed edit reads committed policy state and can make removal or replacement appear to fail.
  - CAI slot and government activation should use the default key-up binding path. Put item actions on the focused item itself so the manager handles them before input bubbles to the generic tree Return toggle or vanilla handler.
  - CAI View All Policies is a pushed read-only categorized policy `Treeview`; policy items expand to detail children. It replaces the inaccessible policy-list checkbox access from vanilla My Government and does not assign or remove policies.
- CAI Governments is a flat `List` of `Button` rows. Each row owns Enter activation through `OnGovernmentSelected(governmentType)`, Shift+Enter Civilopedia access, and one complete tooltip containing slot mix, inherent and passive/legacy bonuses without duplicating their raw modifier amount, the full explanation, stats, locked prerequisite and live civic progress, plus current-government legacy progress and carryover values in base rules when selected.
- Government-list refreshes use stable `FocusKey` values plus `CaptureFocusKey(...)` / `RestoreFocus(...)`, so wrapped vanilla realization and shared `RefreshAllData()` paths can update live labels/tooltips without losing the focused government.
- After `OnAcceptGovernmentChange()`, vanilla calls `m_tabs.SelectTab(Controls.ButtonPolicies)`. CAI should mirror this by switching/rebuilding to the Policies tab after the accepted government change, since the available policy slots can change.
- `OnAcceptGovernmentChange()` does not repopulate `m_ActivePolicyRows` by itself. A wrapper that rebuilds accessible policy slots from live culture data must refresh through `RefreshAllData()` or `PopulateLivePlayerData(...)` after the accepted government change; otherwise the screen can keep the old slot layout until close/reopen, which also leaves Confirm Policies disabled because CAI still sees an unfilled old/new slot mix.
- CAI policy confirmation / unlock buttons should not maintain a parallel action-state model here. Read label/hidden/disabled state directly from the live vanilla controls such as `Controls.ConfirmPolicies`, `Controls.UnlockPolicies`, and `Controls.UnlockGovernments`/`Controls.UnlockGovernmentsContainer`, because vanilla updates them in place while the screen stays open.
- After `SetActivePolicyAtSlotIndex(...)` / `RemoveActivePolicyAtSlotIndex(...)`, CAI can safely rebuild its own Policies body only if it preserves the current slot mirror. Rebuilding that body from committed live culture data during unconfirmed edits will drop the pending slot arrangement even though vanilla's local `m_ActivePoliciesBySlot` is already correct.
- GovernmentScreen CAI should keep its tab bodies alive for the lifetime of the open screen instead of destroying and recreating them on every refresh. Build the Governments list and Policies tree once on open, then refresh only their changing children in place.
- Vanilla uses `RefreshAllData()` as the shared dirty-data path for multiple GovernmentScreen engine events, including `GovernmentChanged`, policy unlock/change events, and local-player turn/state updates. CAI should therefore refresh the active accessibility tree from the wrapped `RefreshAllData()` path as well, not only from explicit tab/page callbacks.
- Policies-side category rows are structurally static. Keep the four category `TreeviewItem` nodes alive, and on refresh call `:ClearChildren()` only on each row node before repopulating its slot widgets from the live culture data or pending slot-policy mirror.
- Governments-side refresh clears and repopulates the root list, preserving focus by each row's `gov:<GovernmentType>` key.
- GovernmentScreen CAI refreshers should be reactionary and render-only. `RefreshPoliciesTree()` and `RefreshGovernmentsList()` should never call `RefreshAllData()` or otherwise initiate upstream vanilla data refresh; wrappers and direct CAI actions own the data-sync step, then call the render refresh.
- The only CAI-specific refresh guard still needed for this screen is the internal vanilla policy-control refresh guard used around `RealizePolicyCatalog()` and `RealizeActivePoliciesRows()`. This protects local pending CAI slot edits from being overwritten by an immediate live-state resync during vanilla control realization.
- The sensitive open-path timing is the CAI `Push()`/focus step, not panel-object creation by itself. Building the CAI panel before vanilla `OnOpenGovernmentScreen(...)` is fine because CAI widget labels are getter-based; the problematic sequence is pushing the panel before the CAI body exists, which causes an initial tab-bar focus bounce before focus moves into the body. Prefer: build panel if needed, run vanilla open/tab selection, build CAI body from the final selected tab, then push and focus the body once.
- Some live Civ VI button controls, especially nested `GridButton` layouts such as `UnlockGovernments`, may expose their visible text only on child controls. CAI control-text helpers should fall back to recursively checking child controls for nonempty live text before giving up.
- When CAI changes tabs programmatically, also update the CAI tab bar's `DefaultIndex` and `FocusedChild` to the selected tab widget. Otherwise Shift+Tab back from the body can restore the tab bar's stale focused child, e.g. Governments after vanilla has moved to Policies.
  - Important local tables such as `m_tabs`, `m_kPolicyCatalogData`, `m_kPolicyCatalogOrder`, `m_ActivePolicyRows`, and `m_kUnlockedGovernments` are not visible to a replacement wrapper after `include("GovernmentScreen")`. Capture populated government instances by wrapping `RealizeGovernmentInstance(governmentType, inst, ...)`; use public vanilla helpers such as `GetPolicyFromCatalog(policyType)`, `IsPolicyTypeLegalInRow(rowIndex, policyType)`, `IsPolicyTypeActive(policyType)`, and `IsPolicyAvailable(culture, policyHash)` for policy availability and derived data.
- Vanilla `CivicsChooser.lua` / `CivicsChooser.xml`:
  - The chooser is a left slide-out tray opened by `LuaEvents.ActionPanel_OpenChooseCivic` and `LuaEvents.WorldTracker_OpenChooseCivic`; it closes through `OnClosePanel()`, `LaunchBar_CloseChoosers`, the title/current-civic button, the icon button, the close button, or after choosing a civic.
  - Opening hides the visual WorldTracker through `LuaEvents.ResearchChooser_ForceHideWorldTracker()` and closing restores it through `LuaEvents.ResearchChooser_RestoreWorldTracker()`.
  - The top current-civic panel shows the active or most recently completed civic: title button, civic icon, progress meter, boost meter/icon/label, turns-left label, recommended icon, and unlock icons with overflow paging.
  - Below the current-civic panel is `OpenTreeButton`, which raises `LuaEvents.CivicsChooser_RaiseCivicsTree()` and closes the chooser.
  - Available civic rows are `CivicListInstance` instances under `CivicStack` inside `ChooseCivicList`. Each row has `TopContainer`, clickable `Top`, `TechName`, progress/boost meters, civic icon, boost icon/label, turns-left text, queue badge/number, unlock icon stack, overflow page-turner, and optional recommended icon.
  - `GetData()` reads live local player culture data from `Players[playerID]:GetCulture()`, current civic from `GetProgressingCivic()`, queue from `GetCivicQueue(...)`, and recommendations from `GetGrandStrategicAI():GetCivicsRecommendations()`.
  - `View()` sorts civics alphabetically by localized name, routes current/last-completed civic to `RealizeCurrentCivic(...)`, and calls `AddAvailableCivic(...)` for selectable rows. Repeatable current civics also appear in the available list.
  - `AddAvailableCivic(...)` builds row controls, sets tutorial tags from `UITutorialManager:GetHash(kData.CivicType)`, wires left-click to `OnChooseCivic(kData.Hash)`, wires right-click Civilopedia outside tutorial mode, disables rows when `GetCultureYield() <= 0`, and shows queue/recommended badges.
  - Choosing a civic calls `UI.RequestPlayerOperation(Game.GetLocalPlayer(), PlayerOperations.PROGRESS_CIVIC, parameters)` with `PARAM_CIVIC_TYPE` and exclusive insert mode, plays `Confirm_Civic`, then closes the tray if expanded.
  - The data shape from `GetCivicData(...)` includes `ID`, `Hash`, localized `Name`, `CivicType`, `ToolTip`, `Cost`, normalized `Progress`, `TurnsLeft`, boost fields, repeatable flag, current/last-completed flags, queue position, and recommendation fields.
  - CAI can largely mirror `ResearchChooser_CAI.lua`: include vanilla `CivicsChooser`, wrap `View`, `AddAvailableCivic`, `RealizeCurrentCivic`, `OnInputHandler`, `OnChooseCivic`, and `OnClosePanel`, then expose an accessibility `Panel` with a read-only current/queue tree, an available civics tree, Open Civics Tree, and Close.
  - Unlike ResearchChooser, vanilla CivicsChooser has no base-tutorial special reorder handler. It does use tutorial tags/disabled state, so CAI rows should keep a mapping from civic hash to native row instance and read live `TopContainer` / `Top` hidden-disabled state.
- Vanilla diplomacy flow:
  - First contact / greeting uses `Popups/LeaderView.lua` plus `LeaderView.xml`, not `DiplomacyActionView.lua`.
  - `LeaderView.xml` is minimal: `LeaderText`, `DeclareWarButton`, and `GoodbyeButton`. `ShowFirstMeetingLeader(...)` sets `LeaderText` to `LOC_LEADER_SCREEN_GREETING`, shows the leader scene with `Events.ShowLeaderScreen(...)`, and opens the popup for either participant in `Events.LeaderPopup` / `Events.DiplomacyMeet`.
  - In the current base file, first meet only exposes the greeting text and Goodbye flow. The declare-war button exists in XML but `ShowFirstMeetingLeader(...)` does not unhide it.
  - The same lightweight popup is also reused for some direct leader popups such as war declaration / refuse peace, with `LeaderText` changed to `LOC_DECLARE_WAR` or `LOC_REFUSE_PEACE`.
  - Talking to an already met leader routes out of `LeaderView.lua` into the full diplomacy screen through `OnTalkToLeader(playerID)`, which is subscribed to `LuaEvents.CityBannerManager_TalkToLeader` and `LuaEvents.DiploPopup_TalkToLeader`.
  - The repository contains `src/UI/inGame/LeaderView_CAI.lua`, but the current `src/CivViAccess.modinfo` does not register a `ReplaceUIScript` for `LuaContext` `LeaderView`. In the current build, first-meet / war / refuse-peace `LeaderView` popups therefore bypass CAI entirely unless that registration is restored.
  - The CAI wrapper includes vanilla `LeaderView`, keeps the leader scene active underneath, and pushes a transient CAI `Dialog` built with `WidgetTemplateHelpers:MakeGeneralDialog(...)`.
  - The CAI dialog reads only live vanilla control text: title and body from `Controls.LeaderText:GetText()`, plus visible button labels from `GoodbyeButton` / `DeclareWarButton`.
  - The CAI dialog should be rebuilt from the `LeaderView` context show/hide lifecycle rather than by rebinding the popup events. That avoids depending on the order of include-time `Events.*` registrations inside `LeaderView.lua`.
  - CAI wraps `OnContinue()` and removes the CAI dialog before calling the vanilla close path.
  - CAI wraps `OnDeclareWar()`, removes the CAI dialog before the vanilla action, then rebuilds a fresh dialog from the updated live popup state because vanilla `OnDeclareWar()` does not close the popup.
  - `LeaderView.lua` registers its events during `include("LeaderView")`, so post-include function reassignment is not a reliable hook for meet-popup open paths. The safer hook is the context becoming visible, plus direct wrapping of local close/action functions such as `OnContinue()` and `OnDeclareWar()`.
  - `LeaderView` cleanup must remove the specific CAI dialog by id through `mgr:RemoveFromStack(...)`; plain `mgr:Pop()` can close the wrong root if another accessibility popup is currently above it.
  - CAI installs a context input handler for `LeaderView` that routes input through the UI manager only while the popup is visible and the CAI dialog is the active top widget.
  - CAI also installs a hide handler so external popup hides clean up the CAI dialog even if the flow bypasses `OnContinue()` / `OnDeclareWar()`.
  - The full diplomacy hub is `DiplomacyActionView.lua`. `OnOpenDiplomacyActionView(otherPlayerID)` initializes the screen, populates a vertical diplomacy ribbon of met leaders, selects the target leader, and opens in `OVERVIEW_MODE`.
  - Verified open routes into `DiplomacyActionView` are `LuaEvents.DiplomacyRibbon_OpenDiplomacyActionView`, `LuaEvents.TopPanel_OpenDiplomacyActionView`, `LuaEvents.CityBannerManager_TalkToLeader`, `LuaEvents.DiploPopup_TalkToLeader`, and the lite variant `LuaEvents.DiplomacyActionView_OpenLite`.
  - `TopPanel_OpenDiplomacyActionView` is raised by `PartialScreenHooks.lua` `OnOpenDiplomacy()` with no explicit player id, so the default top-panel open path falls through to the diplomacy screen's local-player/self selection behavior.
  - `DiplomacyRibbon.lua` opens the full diplomacy hub when a met leader portrait is clicked. It always shows the local player first, then met major leaders ordered by met turn.
- The diplomacy overview body is split into two areas: top-level statement/action buttons on the left and an intel panel on the right. `PopulatePlayerPanel(...)` calls `AddStatmentOptions(...)` and `AddIntelPanel(...)`.
- For CAI, the accessible overview can be safely reshaped into two persistent widgets without replacing vanilla game logic: one leaders tree that owns self/intel detail nodes, and one action list that mirrors the root action list plus submenu stacks.
  - Top-level actions come from the parsed `GREETING` diplomacy statement. `GetInitialStatementOptions(...)` groups actions by `GameInfo.DiplomaticActions[...].UIGroup`, turning `DISCUSS` and `FORMALWAR` into submenu buttons and leaving other valid actions on the root list.
  - Verified direct action/session entries in `OnSelectInitialDiplomacyStatement(...)` include `CHOICE_MAKE_PEACE`, `CHOICE_MAKE_DEAL`, `CHOICE_VIEW_DEAL`, `CHOICE_MAKE_DEMAND`, `CHOICE_VIEW_DEMAND`, `CHOICE_DENOUNCE`, `CHOICE_DIPLOMATIC_DELEGATION`, `CHOICE_DECLARE_FRIENDSHIP`, `CHOICE_RESIDENT_EMBASSY`, `CHOICE_OPEN_BORDERS`, and promise-demand actions for spying, settling nearby, converting cities, and digging artifacts.
  - War choices are a submenu. Verified supported casus belli entries are surprise, formal, holy, liberation, reconquest, protectorate, colonial, and territorial war.
- Conversation sessions use `CONVERSATION_MODE` or `CINEMA_MODE` depending on the leader animation. `VOICEOVER_SUPPORT` currently includes `KUDOS`, `WARNING`, `DECLARE_WAR_FROM_HUMAN`, `DECLARE_WAR_FROM_AI`, `FIRST_MEET`, `DEFEAT`, and `ENRAGED`.
- Voiced diplomacy can open hidden in `CINEMA_MODE` first. In that path `SelectPlayer(..., CINEMA_MODE, ...)` only sets `m_cinemaMode = true` while hidden, `OnShow()` plays the cinema presentation, and later `ToggleCinemaMode()` reveals the real response popup again. There is no single universal "conversation opened" helper in base Lua that covers both normal conversation and all close paths. `SetConversationMode()` is a real open point for normal conversations, but cinema reveal and session close still fan out through separate mutators such as `ToggleCinemaMode()`, `SelectPlayer(..., OVERVIEW_MODE, ...)`, deal/demand apply handlers, and `OnDiplomacySessionClosed(...)`. Any CAI wrapper that assumes one global open hook, or that reconstructs state after the fact instead of following the actual statement/apply lifecycle, is likely to drift.
- The intel panel has tabs for Overview, Gossip, Access Level, and Relationship. The Relationship tab is omitted for human-controlled targets.
- When mirroring the intel panel into CAI tree nodes, keep the first-level tab widgets persistent and do not hide non-selected leaders' child nodes in CAI. Leader focus already triggers a real vanilla selection change plus refresh, which should resolve focus and live detail content naturally. Also do not force leader/tree expansion from selection state; user collapse/expand state should stay under CAI tree control.
- In a replacement `DiplomacyActionView` wrapper, the local intel `InstanceManager` objects and per-row local instance tables are not directly safe to read by name after `include("DiplomacyActionView")`. Prefer capturing live instance tables from vanilla callbacks that receive them, such as `PopulateIntelOverview(overviewInstance)`, `OnActivateIntelGossipHistoryPanel(gossipInstance)`, `OnActivateIntelAccessLevelPanel(accessLevelInstance)`, and `OnActivateIntelRelationshipPanel(relationshipInstance)`. The overview sub-row instances themselves are still created inside local helper functions, so if you need those exact row controls you must either capture them at creation time or rebuild just that sub-data from stable game APIs.
- Intel tabs do NOT all share a single creation chokepoint. The base tabs (overview/gossip/access/relationship) are built by `AddIntelTab(tabPanelIM, tooltip, header, icon)`, but the expansion tabs are added by separate adders that bypass it: `AddIntelAlliance`/`AddIntelEmergency` (Expansion1 replacement) and `AddWorldCongressInfoTab` (Expansion2) each call the lower-level `CreateTabButton()` and `GetTabAnchor()` + `ContextPtr:LoadNewContext(...)`, loading the tab's content into its own context. So wrapping `AddIntelTab` only captures the base tabs. Conditions for the DLC tabs: Alliance shows for any selected player NOT on your team; Emergency shows only when `AreThereEmergenciesForPlayer(selectedID)` (a begun emergency involving both you and them); World Congress is added unconditionally but its whole tab bar is hidden before `GlobalParameters.WORLD_CONGRESS_INITIAL_ERA`. DLC adders that should hide bail before `GetTabAnchor`/`SetHide(true)`, so they don't linger as live tabs.
- To enumerate ALL intel tabs (base + DLC) generically from a wrapper, walk the live tab bar after the panel build instead of wrapping creation: iterate `ms_IntelPanel.IntelTabButtonStack:GetChildren()` and skip `IsHidden()` (pooled/stale) buttons. `ms_IntelPanel` itself is a global and is reachable; the panel/anchor/button instance managers are file-locals and attached Lua fields (`m_HeaderText`/`m_ButtonInstance`) do NOT survive `GetChildren()` (cf. the `LastTenTurnsStack` note below) — only `.CData`-backed control methods are reliable on `GetChildren()` results.
- **Do NOT pre-resolve each tab's header/panel by clicking through them.** `button:DoLeftClick()` is NOT silent: although `ShowPanel(...)` itself plays no sound, `DoLeftClick` fires the Button control's own native press SoundData (from its XML `Style`), so clicking every tab on every panel build produces an audible click-storm. Resolve a tab lazily instead — only when the user actually navigates into it: `DoLeftClick` that one button, then read its title from `ms_IntelPanel.IntelHeader:GetText()` (equals the tab's `Locale.ToUpper("LOC_..._REPORT_*")`, usable as a reader key) and grab the now-visible child of `ms_IntelPanel.IntelPanelContainer` as its panel.
- **Gate the panel switch on live `button:IsSelected()`.** `ShowPanel` calls `SetSelected(true)` on the shown tab's button and `false` on the rest (DLC anchors set `m_ButtonInstance`/`m_HeaderText`, so this works for them too), and `AddIntelPanel` ends with `ShowOverviewPanel()` so Overview is the shown/selected tab after every (re)build. So a tab is already displayed iff its `button:IsSelected()` — skip the `DoLeftClick` in that case to avoid a redundant click (e.g. focusing Overview right after a leader select, or re-entering the tab you're already on). Only an actual tab change should click.
- The base readers' typed instances are captured eagerly: vanilla calls `PopulateIntelOverview`/`OnActivateIntelGossipHistoryPanel`/`OnActivateIntelAccessLevelPanel`/`OnActivateIntelRelationshipPanel` inside `AddIntelPanel` (right after each `AddIntelTab`), NOT on tab click. So wrapping those to stash the instances works without driving any tab clicks.
- Selecting yourself in `DiplomacyActionView` switches to a self-view instead of the normal action/intel panel: the right-side player panel is hidden, the large leader/civ header stays visible, and the body becomes a scrollable "Features & Abilities" list built from leader/civilization unique abilities, units, and buildings.
- For CAI text on this screen, prefer live vanilla controls and vanilla loc keys over CAI-owned replacements wherever the base screen already exposes the text. Useful direct sources include `Controls.PlayerNameText`, `Controls.CivNameText`, `Controls.LeaderResponseName`, and `ms_IntelPanel.IntelHeader`. Keep CAI localization only for wrapper-owned group labels or synthesized states that vanilla never names on a control.
  - Overview rows are built in this order: recent gossip count, access level, current government, agendas, active agreements, our relationship with them, and their relationships with other met leaders.
  - Overview gossip is only a summary count for the last turn: `LOC_DIPLOMACY_GOSSIP_ITEM_COUNT` or `LOC_DIPLOMACY_GOSSIP_ITEM_NONE_THIS_TURN`.
  - Overview access level shows current diplomatic visibility icon plus `GameInfo.Visibilities[iAccessLevel].Name`.
  - Overview government shows the target's current government, anarchy with turns remaining, or `LOC_DIPLOMACY_GOVERNMENT_NONE`.
  - Overview agendas show the historical agenda for AI leaders, then either revealed random agendas or a hidden-agenda count depending on current visibility. Human targets hide this row.
  - Overview agreements can show delegation, embassy, defensive pact, open borders in either direction, research agreement, and joint war.
  - Overview "our relationship" shows the selected leader's diplomatic state toward us, with special tooltip handling for denouncements and declared friendship turns remaining.
  - Overview "other relationships" shows non-neutral relationships the selected leader has with other met major players, filtered through `DiplomacyRibbonSupport.lua` rules. Valid displayed states include allied, declared friend, denounced, war, and for AI also unfriendly/friendly.
  - Overview Secret Society row (Ethiopia / Secret Societies mode, gated by `GameCapabilities.HasCapability("CAPABILITY_SECRETSOCIETIES")`): vanilla `DiplomacyActionView_SecretSocietyRow.Refresh(selectedID)` reads `Players[selectedID]:GetGovernors():GetSecretSociety()` (a hash, or `-1` for none). Indexes `GameInfo.SecretSocieties[hash].Name` for the known name; `LOC_SECRETSOCIETY_DIPLO_NONE_NAME` when `-1`; and `LOC_SECRETSOCIETY_DIPLO_UNKNOWN_NAME` + `_DESCRIPTION` (tooltip) when unaware. Row header label is `LOC_SECRETSOCIETY`. Quirk: the awareness check `IsAwareOfSecretSociety(hash)` is run against the SELECTED player's own governors (the computed `pLocalPlayer` is unused), so a leader is always aware of their own society and the real name is shown on screen whenever they have joined one — the "Unknown" branch is effectively dead. CAI reconstructs from this same API (the addon is a separate context whose Controls are not reachable) and replicates the awareness quirk for screen parity, surfacing the row as a leaf in the overview reader (`AddOverviewChildren`), not as a separate intel tab.
  - The Access Level tab shows active visibility sources, the info shared at the current level, the info unlocked at the next level, and advisor suggestions for new visibility sources.
- The Gossip tab shows visible gossip from the last 100 turns, split into "last ten turns" and older items, with a "new" indicator for this turn or last turn. If there is no recent gossip, the first section shows `LOC_DIPLOMACY_GOSSIP_ITEM_NO_RECENT`.
- The Relationship tab shows the selected AI leader's diplomatic state toward us, a relationship bar/icon, sorted positive and negative modifier reasons from `GetDiplomaticModifiers(...)`, and advisor suggestions for improving or worsening relations.
- In CAI wrappers, do not rely on `LastTenTurnsStack:GetChildren()` or `RelationshipReasonStack:GetChildren()` to recover per-row data by field name after vanilla instance-manager rebuilds. The stable sources are `Game.GetGossipManager():GetRecentVisibleGossipStrings(...)` for gossip rows and `selectedPlayerDiplomaticAI:GetDiplomaticModifiers(ms_LocalPlayerID)` for relationship-reason rows; live control text remains appropriate for section headers and advisor text blocks.
- Root diplomacy actions are rendered as one vertical button list plus an optional sublist. `DISCUSS` and `FORMALWAR` actions become submenu buttons, and opening a submenu adds an explicit Cancel button that returns to the root list.
- In CAI, root diplomacy actions map cleanly to a `List` containing `Button` rows for direct actions and `SubMenu` rows for grouped actions. Submenu expansion should rebuild its button children from the current live vanilla sub-option stack, and submenu collapse should mirror `ShowOptionStack(false)`.
- Conversation close is still fragmented in base Lua. Besides overview-return and deal/demand close paths, CAI should also clear its conversation dialog and cached bindings on concrete goodbye/session-close paths such as `CHOICE_EXIT` activation and `OnDiplomacySessionClosed(...)`.
  - `DiplomacyDealView.lua` is the separate make-deal / make-demand screen. `DiplomacyActionView` opens it through `LuaEvents.DiploPopup_ShowMakeDeal(ms_OtherPlayerID)` or `LuaEvents.DiploPopup_ShowMakeDemand(ms_OtherPlayerID)`.
  - Sighted players normally reach `DiplomacyDealView` from the full diplomacy hub, not directly from the map. `DiplomacyRibbon.lua` opens `DiplomacyActionView` from leader portraits, `PartialScreenHooks.lua` opens the diplomacy hub from the top panel, and `DiplomacyActionView.lua` then switches from a `MAKE_DEAL` or `MAKE_DEMAND` statement into the separate deal screen by hiding its own overview/conversation containers and raising the `DiploPopup_ShowMakeDeal` / `DiploPopup_ShowMakeDemand` Lua events.
  - Available trade inventory groups are gold, luxury resources, strategic resources, agreements, cities, other players, great works, and captives.
  - In the CAI deal inventory, quantity-bearing rows such as `Iron x60` or `Diplomatic Favor 42` should not repeat the same name again as a tooltip when the tooltip adds no extra detail; otherwise focus speech becomes redundant.
  - Diplomatic Favor is a Gathering Storm-only deal item (`DealItemTypes.FAVOR`), added by the `DiplomacyDealView_Expansion2` wildcard rider, not the base script. It is a bare-amount lump-sum item like gold (no value-type name), traded via the global `OnClickAvailableOneTimeFavor(player, amount)` with a default add amount of `1`. Vanilla `PopulateAvailableFavor` shows it only when `player:GetFavor() > 0` and `not ms_bIsDemand` (favor is not demandable). On-screen amount is the side's current favor balance. `GameInfo`/loc: `LOC_DIPLOMATIC_FAVOR_NAME`, icon `ICON_YIELD_FAVOR`. Because favor carries no `GetValueTypeNameID()`, a generic deal-item label/header path renders it blank+silent — FAVOR needs an explicit branch reading `LOC_DIPLOMATIC_FAVOR_NAME` + `GetAmount()`. Accumulated/stockpiled strategic resources (also GS) need no special handling: they come back from `GetPossibleDealItems(..., DealItemTypes.RESOURCES, ...)` as ordinary `RESOURCES` entries (`MaxAmount` = stockpile, `duration == 0` lump sum) and reuse the XP2-overridden global `OnClickAvailableResource`.
  - The current deal area can show resources, agreements, cities, great works, and captives. City deal entries can expand for detailed city contents.
  - Deal action buttons include `EqualizeDeal`, `AcceptDeal`, `DemandDeal`, `RefuseDeal`, and `ResumeGame`, with visibility depending on whether the current screen is a deal, a demand, or a pending viewed proposal.
  - Visually, `DiplomacyDealView.xml` is a full-screen modal sheet with a tall parchment/banner panel on the left half of the screen, an animated leader speech bubble at the top, and a small animated yield strip in the top-right for science, culture, faith, and gold. Inside the main trade panel, the content is split into two mirrored columns: left for the local player and right for the other player.
  - The body is vertically divided into two mirrored sections. The upper section is the current offer area, labeled `My Offer` and `Their Offer`; the lower section is the inventory area showing what each side can add. A draggable resize handle sits between them, so sighted players can drag the divider to give more height to offers or inventory.
  - Inventory items are presented as icon tiles or icon-plus-text rows grouped under headers. Gold and resources are mostly compact horizontal icon rows. Agreements, cities, great works, and captives use vertical category blocks with collapsible headers. When collapsed, the category turns into a minimized icon strip rather than disappearing completely.
  - Offer items are interactive review rows rather than plain text. Left click edits an item's amount or parameter when the item supports it, right click removes the item, and a dedicated remove button is also shown on the row. For AI-proposed items the screen can also show a small "don't ask again" marker button for unacceptable items.
  - CAI offer-diff announcements need three cases from the per-side offer snapshot: removed items, changed-in-place items (same deal item ID, different spoken label such as amount or target), and newly added items. Speak them in that order so mutations are heard as removals/changes before additions.
  - `DLC/KublaiKhan_Vietnam/UI/Replacements/DiplomacyDealView_KublaiKhanVietnam_MODE.lua` does not add new deal-item types or interactions. Its deal-view impact is limited to Monopolies & Corporations product great works: it overrides `GetGreatWorkIcon(...)` to swap in the product resource icon and overrides `GetGreatWorkTooltip(...)` to append the product's `GameInfo.ResourceIndustries().ResourceEffectTExt` line. CAI deal-view great-work tooltip sites should therefore follow vanilla's own pattern and call `GetGreatWorkTooltip(greatWorkDesc, GreatWorksSupport_GetBasicTooltip(...))` directly, so DLC/mode overrides stay in the path.
  - Deal-view tooltip rule: prefer the same vanilla tooltip functions and primary-row tooltip logic before building CAI text. Current relevant vanilla paths are `MakeCityToolTip(...)`, `GetParentItemTransferToolTip(...)`, `GetGreatWorkTooltip(...)`, and the primary-row rules in `PopulateAvailable*` / `PopulateDeal*` (for example: available agreements with no duration have no main tooltip, captives have no main tooltip, city-child great works use plain `GreatWorksSupport_GetBasicTooltip(...)`, and invalid available resources append the red no-cap-room message instead of reusing the normal tooltip).
  - Vanilla localization keys matter here too: the per-item unacceptable marker text is `LOC_DIPLO_DEAL_UNACCEPTABLE_ITEM_TOOLTIP` (`Offer blocking deal`), not `LOC_DIPLOMACY_DEAL_UNACCEPTABLE`, and the stop-asking affordance text already exists as `LOC_DIPLO_DEAL_MARK_UNACCEPTABLE`. Prefer those vanilla loc keys over CAI-owned substitutes.
  - `OnClickAvailableAgreement(player, agreementType, agreementTurns)` does NOT add an item for `DealAgreementTypes.JOINT_WAR`, `THIRD_PARTY_WAR`, or `RESEARCH_AGREEMENT` — it calls `ShowAgreementOptionPopup(...)` to pick the target/war-type/tech first, and only `OnSelectAgreementOption(agreementType, turns, value, parameters, fromPlayerID)` then adds the item. All other agreement types (and `ALLIANCE`) add directly on click; their value, where any, is chosen later via the offer item's value-edit. So a CAI layer that only wraps the click + offer-edit path will silently no-op on joint/third-party war and research agreements (the item never gets created, and the vanilla option popup is mouse-only). The accessible fix is to detect those three types at inventory-click time and push the same option list the offer-edit selector builds (`GetPossibleDealItems(from, to, AGREEMENTS, agreementType, pForDeal)` → rows with `ForType`/`Parameters.WarType`, activated via `OnSelectAgreementOption`).
  - Editing an item opens a centered darkened-overlay popup inside the same screen. For gold/resources it shows the item icon, amount field, left/right arrow buttons, and a confirm/back button. For agreement items it instead shows a scrollable option list, such as alliance type, research target, or joint-war target.
  - City items are visually richer than other rows. They can show a collapse/expand button that reveals child detail icons inside the row's own detail grid, so sighted players can inspect the city's bundled contents before deciding whether to keep it in the trade.
  - The leader-dialog bubble at the top is reused as deal feedback text. It changes to reactions such as unfair deal, invalid deal, gift, acceptable proposal, equalize failed, and demand flavor text, so sighted players get immediate conversational feedback while modifying the deal.
  - Human interaction on this screen is strongly mouse-oriented in vanilla. Players click inventory items to add them, click or arrow-adjust existing deal items to edit values, right click or hit remove to take items back out, click header chevrons to collapse deal categories, drag the central resize handle, and use the bottom action buttons to propose, accept, demand, refuse, cancel, or resume.
  - Keyboard handling in base Lua is minimal here. `DiplomacyDealView.lua` explicitly handles Escape only, routing it to popup close, Resume Game, or Refuse/Cancel depending on state. Everything else is expected to go through ordinary control focus/click behavior rather than a screen-specific keyboard navigation layer.
- Trade route UI family:
  - Base `InGame.xml` registers three direct contexts: `TradeOverview`, `TradeRouteChooser`, and `TradeOriginChooser`. The base game has no wildcard include for these contexts. I found no XP1/XP2 replacements for the route chooser or origin chooser in the decompiled mirror; the only DLC replacement found was `DLC/Indonesia_KhmerScenario/UI/Replacements/TradeOverview_Indonesia_KhmerScenario.lua`, which includes base `TradeOverview` and only adds the maritime city-state icon branch.
  - `TradeOverview.lua/.xml` is the trade-routes side panel opened from `PartialScreenHooks` through the `ToggleTradeRoutes` input action or the launch-bar trade hook. It is unlocked once the local player's outgoing route capacity is greater than zero and requires `CAPABILITY_TRADE_VIEW`.
  - Visually, `TradeOverview.xml` is a tall right-side style partial panel with a dark title/header frame, three tab buttons across the top (`My Routes`, `Routes to My Cities`, `Available Routes`), a header row with active/capacity text where relevant, origin/destination column labels, a central `BenefitsButton` that toggles whether route rows show my benefits or their benefits, and a vertical scroll body of section headers plus route rows.
  - `TradeOverview` route rows are compact 482x78 list buttons. Each row shows an origin-to-destination label, origin and destination civilization/city-state icons, a benefactor arrow pointing to the side whose benefits are being displayed, a horizontal yield strip, optional religion-pressure icon/value, optional trade-route bonus icon, trading-post indicator, route distance in turns, and on the My Routes tab a trade-route status icon. The row tooltip is the detailed yield-source breakdown returned by `TradeSupport.GetYieldsForRoute(...)`.
  - `TradeOverview` tabs rebuild different models: `My Routes` groups the local player's outgoing routes by destination player and appends unused-route rows; `Routes to My Cities` groups foreign incoming routes by origin player; `Available Routes` scans every local origin city against every known destination city with `Game.GetTradeManager():CanStartRoute(...)`.
  - Interaction in `TradeOverview` is mostly mouse clicks: tab buttons switch sections; `BenefitsButton` toggles my/their benefits; route rows with live trader units select and center that trader; available-route rows from idle traders also raise `LuaEvents.TradeOverview_SelectRouteFromOverview(destinationPlayerID, destinationCityID)` to open/select that destination in `TradeRouteChooser`; unused-route rows either select an idle trader with `Choose Route` or show a disabled `Produce Trade Unit` row.
  - `TradeRouteChooser.lua/.xml` is the left slide-out destination picker for `InterfaceModeTypes.MAKE_TRADE_ROUTE`. Opening it sets `LuaEvents.TradeRouteChooser_SetTradeUnitStatus("LOC_HUD_UNIT_PANEL_CHOOSING_TRADE_ROUTE")`, activates the `TradeRoutes` lens, rebuilds possible destination cities from the selected trader's origin city, draws every filtered route path on the map lens, and optionally preselects the last completed route or a route requested by `TradeOverview`.
  - Visually, `TradeRouteChooser.xml` has a 310px left panel. The top preview card shows the selected destination city banner, trading-post and route-bonus icons, quest icon for city-state trade-route quests, route distance, two resource boxes for what the destination receives and what the origin receives, and `No benefits` text when applicable. The lower panel has a pulldown filter and a vertical scroll list of destination city cards. A bottom confirmation drawer appears for a selected route, with `Begin Route` and `Cancel`.
  - Destination rows in `TradeRouteChooser` mirror small city banners: selected brace, city name colored by owner, trading-post icon, route-bonus icon, city-state quest icon, distance, and a yield/religion strip. Row tooltip is the origin-side route yield breakdown. Clicking a row selects it, moves the camera to the destination city with `UI.LookAtPlotScreenPosition(...)`, refreshes the map route lens, and raises `LuaEvents.TradeRouteChooser_RouteConsidered()`.
  - `TradeRouteChooser` filters are built dynamically into `DestinationFilterPulldown`: all routes, each reachable civilization/city-state group, city-states, and one entry per yield type. Yield filters sort by descending yield value. Filter choice rebuilds only the destination stack and route-path lens, not the origin city.
  - Confirming a route calls `UnitManager.RequestOperation(selectedTrader, UnitOperationTypes.MAKE_TRADE_ROUTE, { X0/Y0 = destination city, X1/Y1 = trader plot })` after rechecking `UnitManager.CanStartOperation(...)`, then returns to `InterfaceModeTypes.SELECTION` and plays `START_TRADE_ROUTE`. Escape closes the chooser and restores the default lens.
  - `TradeOriginChooser.lua/.xml` is a much simpler left slide-out used for `InterfaceModeTypes.TELEPORT_TO_CITY` when the selected unit is a trade unit. It lists the local player's other cities that pass `UnitManager.CanStartOperation(...)` for the active operation. Clicking a city requests that operation with destination `PARAM_X/PARAM_Y`, returns to selection mode, plays `Unit_Relocate`, and closes.
  - `TradeSupport.lua` is shared by `TradeOverview` and `TradeRouteChooser`. `GetIdleTradeUnits(playerID)` identifies trade units with no matching outgoing route. `GetYieldsForRoute(originCity, destinationCity, bReturnDestinationYields)` combines route yields, path/trading-post yields, modifier yields, international yield multipliers, and majority-religion pressure into `kYieldValues`, `TooltipText`, `HasPathBonus`, `MajorityReligion`, and `ReligionPressure`.
  - Accessibility-shape implication: `TradeOverview` should be a CAI `Panel` containing a `TabControl`, a benefits toggle button, and per-tab `Tree`/`List` content grouped by player/city-state/unused routes. `TradeRouteChooser` should be a separate CAI surface with a destination filter `Dropdown`, a destination `List`, a read-only selected-route summary, and explicit `Begin Route` / `Cancel` buttons. `TradeOriginChooser` can be a small transient `List` of cities. All three should preserve vanilla callbacks by wrapping `Open`/`Close`/`Refresh`/selection functions and activating live controls or vanilla functions rather than issuing operations from duplicated UI state except where vanilla itself already does so.
- Diplomacy context-extension mechanics (important for CAI load strategy):
  - `DiplomacyDealView.lua` ends with `include("DiplomacyDealView_", true)` — a **wildcard host**: it pulls in every loaded file whose name starts with `DiplomacyDealView_`. `Initialize()` is called on the line *after* the wildcard include (`DiplomacyDealView.lua:3179` vs `:3177`), so a wildcard-included file runs while all vanilla functions exist but before handlers are registered. The file's own comment says a new `DiplomacyDealView_*` file must NOT `include("DiplomacyDealView")`. CAI therefore ships `DiplomacyDealView_CAI.lua` as a wildcard rider (InGame `<ImportFiles>` File, no `ReplaceUIScript`): it only reassigns globals and lets vanilla's later `Initialize()` register them. `ms_bIsDemand` and the `OnClickAvailable*` / `OnValueEditButton` / `OnSelectAgreementOption` callbacks are true globals, reachable from the rider.
  - `DiplomacyActionView.lua` has **no** wildcard include, so it must be extended via `ReplaceUIScript`. The expansions register their own `ReplaceUIScript` on the same `DiplomacyActionView` context (`Expansion1.modinfo` criteria=Expansion1 → `DiplomacyActionView_Expansion1.lua`; `Expansion2.modinfo` criteria=Expansion2 → `DiplomacyActionView_Expansion2.lua`, which chain-includes the XP1 file). A CAI `ReplaceUIScript` that `include("DiplomacyActionView")` (base) would clobber all expansion logic on XP1/XP2. Fix: branch on `IsExpansion2Active()` / `IsExpansion1Active()` and re-include the matching variant (same approach as `GovernmentScreen_CAI`).
  - `AddIntelTab(tabPanelIM, buttonTooltip, headerText, buttonIcon)` is the single chokepoint that creates every intel tab — base overview/gossip/access/relationship and all DLC tabs. It returns the panel instance; `inst:GetTopControl()` is the panel control, and it caches `m_ButtonInstance` / `m_HeaderText` on that control. Wrapping `AddIntelTab` captures the full live tab set generically (the `headerText` arg is the already-`Locale.ToUpper`'d display string). `AddIntelPanel(rootControl)` is the per-selected-player rebuild that runs `PopulateIntelPanels` → the `AddIntel*` adders.
  - DLC intel additions are separate contexts that populate reserved containers in response to `LuaEvents.DiploScene_RefreshTabs(playerID)` and `LuaEvents.DiploScene_RefreshOverviewRows(playerID)`, both raised synchronously *during* the intel build (XP1's `PopulateIntelPanels` override raises RefreshTabs; base `AddIntelOverview` raises RefreshOverviewRows). XP1 adds the Alliance and Emergency tabs (`DiplomacyActionView_AllianceTab` / `_EmergencyTab`); XP2 adds the World Congress tab (`_WorldCongressTab`); the Ethiopia/Secret-Societies mode adds an overview row via `_SecretSocietyRow`.
  - The base intel tab order is stable: `AddIntelOverview()` creates the Overview tab first, then Gossip, Access Level, and, only for non-human selected players, Relationship. XP1/XP2 append Alliance, Emergency, and World Congress after that base set. The overview tab button tooltip is `LOC_DIPLOMACY_INTEL_OVERVIEW_COLON_TOOLTIP`, which is a reliable identifier when CAI wants to suppress the overview node but still keep vanilla tab selection in sync.
  - Agenda sourcing differs by ruleset. Base overview uses `selectedPlayer:GetAgendaTypes()` and removes the first entry as the historical agenda before applying visibility gating. The XP2 override instead uses `selectedPlayer:GetAgendasAndVisibilities()` and checks each returned entry's `Visibility` against the local player's current access level. For GS-safe parity, prefer the XP2-style `GetAgendasAndVisibilities()` path when present and fall back to the base `GetAgendaTypes()` path otherwise.
- Vanilla `InGamePopup.lua`:
  - `PopupDialogInGame:Open()` raises `LuaEvents.OnRaisePopupInGame(id, options)`, which is handled by the separate `InGamePopup` context. Government confirmation dialogs therefore do not receive input through `GovernmentScreen`'s input handler.
  - Vanilla `InGamePopup` uses the old `InputHandler(uiMsg, wParam, lParam)` signature and registers it with `ContextPtr:SetInputHandler(InputHandler)`. CAI replacement should wrap that handler, call `mgr:HandleInput(input)` first, then preserve vanilla Escape/Enter behavior by calling the original handler with `input:GetMessageType()` and `input:GetKey()`. Register the wrapper with `ContextPtr:SetInputHandler(InputHandler, true)`.
- CAI `Treeview` / `TreeviewItem` widgets:
  - `Treeview` provides shared Left/Right/Return bindings. Right expands the focused node or moves to its first child; Left collapses the focused node or moves to its parent; Return toggles the focused node.
  - Screens should not add their own expand/collapse bindings. Use `TreeviewItem.OnToggleExpanded` to synchronize vanilla visual state when expansion changes.
  - `TreeviewItem:GetValue()` announces `LOC_CAI_TREEVIEW_EXPANDED`, `LOC_CAI_TREEVIEW_COLLAPSED`, and `LOC_CAI_TREEVIEW_ITEM_COUNT` for expanded nodes.
- CAI interface preview helpers:
  - `interfaceInfoHelpers_CAI.lua` is the shared helper module for interface-specific plot previews.
  - `interfaceTargetHelpers_CAI.lua` owns neutral active-target resolution used by both interface info and the world scanner.
  - The legacy movement path helpers remain there unchanged for callers such as `UnitPanel_CAI.lua`.
  - Vanilla movement-path behavior lives in `decompiled/Assets/UI/WorldInput.lua`, centered on `RealizeMovementPath(showQueuedPath)`. That function is primarily a renderer for sighted feedback, not a text-summary API.
  - Lifecycle and entry points for vanilla move-path visuals:
    - entering `InterfaceModeTypes.MOVE_TO` calls `OnInterfaceModeChange_MoveTo()` -> `RealizeMovementPath()`
    - while already in move mode, mouse move calls `OnMouseMoveToUpdate()` -> `RealizeMovementPath()`
    - while in normal selection mode, holding right mouse to quick-move calls `OnMouseSelectionUnitMoveStart()` / `OnMouseSelectionMove()` -> `RealizeMovementPath()`
    - touch pathing calls `OnTouchSelectionStart()` / `OnTouchSelectionUpdate()` or `OnTouchMoveToUpdate()` -> `RealizeMovementPath()`
    - selecting a unit with a queued destination calls `OnUnitSelectionChanged(..., isSelected=true, ...)` -> `RealizeMovementPath(true)`
    - cancelling movement clears the active preview, then restores queued-path preview if the selected unit still has one
    - leaving move mode calls `OnInterfaceModeChange_MoveToLeave()` -> `ClearMovementPath()`
  - Queued-order ownership:
    - Vanilla Lua clearly knows about already queued movement through `UnitManager.GetQueuedDestination(unit)`, but the actual act of making a move become queued is not exposed as a Lua branch in the decompiled UI.
    - `WorldInput.lua` move confirmation paths such as `OnMouseSelectionUnitMoveEnd()` and `OnMouseMoveToEnd()` simply call `MoveUnitToCursorPlot(unit)` with no `Shift` or queue-specific condition.
    - `MoveUnitToCursorPlot()` in `WorldInput.lua` delegates to `MoveUnitToPlot()`, and `MoveUnitToPlot()` / `RequestMoveOperation()` in `Civ6Common.lua` submit the same normal `UnitManager.RequestOperation(unit, UnitOperationTypes.MOVE_TO, tParameters)` request used for ordinary movement.
    - The only move modifiers Lua supplies there are combat / fog related (`ATTACK`, `MOVE_IGNORE_UNEXPLORED_DESTINATION`, or `NONE`); no exposed `UnitOperationMoveModifiers` value represents `queue movement`.
    - Practical implication: sighted queued movement is a real engine-level state that Lua can read back and render, but the input rule that turns a move order into a queued order is likely handled below Lua rather than by a visible `if Shift then queue` script branch.
  - Early-exit conditions:
    - no path is shown if `UI.IsMovementPathOn()` is false or `UI.IsGameCoreBusy()` is true
    - no path is shown with no selected unit
    - no path is shown for `IgnoreMoves` units
    - no path is shown when `UI.GetCursorPlotID()` is not a valid plot
  - `RealizeMovementPath(...)` computes or consumes these concrete data points:
    - end plot: current cursor plot, or `UnitManager.GetQueuedDestination(unit)` when `showQueuedPath=true`
    - full path data from `UnitManager.GetMoveToPathEx(unit, endPlotId)`, notably `plots`, `turns`, `obstacles`, `entrancePortals`, and `exitPortals`
    - tutorial / constrained-path invalidity from `IsPlotPathRestrictedForUnit(pathInfo.plots, pathInfo.turns, unit)`
    - fog-of-war state by checking every plot in the path against `PlayersVisibility[localPlayer]:IsVisible(plotId)`
    - enemy presence on the destination plot by scanning visible units in that plot
    - implicit ranged-attack targeting by checking `UnitManager.GetOperationTargets(unit, UnitOperationTypes.RANGE_ATTACK)` for the hovered end plot
    - swap-with-unit validity by testing `UnitManager.CanStartOperation(unit, UnitOperationTypes.SWAP_UNITS, nil, params)`
  - What a sighted player is told by the movement preview:
    - The preview is almost entirely visual. Vanilla does not build a spoken or tooltip summary string here.
    - Path validity category is conveyed by lens family:
      - `MovementGood`: ordinary valid movement path
      - `MovementQueue`: queued path for an already issued order
      - `MovementBad`: restricted / invalid path, including tutorial-forbidden destinations
      - `MovementFOW`: otherwise valid path that enters fog or mid-fog
    - Origin / destination are shown with dedicated path-end variants such as `_Origin` and `_Destination`.
    - Turn count is conveyed visually by numbered markers added with `UI.AddNumberToPath(turnNumber, plotId)`. Markers are placed at each turn break and at the final path plot. This is not a per-step numeric breakdown.
    - Non-turn-break intermediate plots are shown as ordinary pips (`..._Pip`).
    - Obstacles along the path, such as river crossings, are shown with `..._Minus` markers between the obstacle plot and the following plot.
    - If the cursor target is a valid same-turn ranged attack for the selected unit, vanilla does not show a movement path at all. It instead highlights the target hex in the attack-range lens and focuses that hex.
    - If the cursor target is a valid unit-swap destination, vanilla shows a special two-plot move path with turn `1` markers on both ends.
    - If no multi-plot path exists:
      - same tile: only a `MovementGood_Destination` marker on the current plot
      - different invalid tile: only a `MovementBad_Destination` marker on the target plot
    - If a visible enemy unit is on the destination and arrival is this turn, vanilla hides the normal destination marker. For later-turn arrivals, the destination marker is still shown.
    - Mountain-tunnel travel is drawn as segmented path pieces using `exitPortals` / `entrancePortals`; the function comments note there are no special portal-entry/exit variations yet.
  - Non-visual feedback in this path system is minimal:
    - when a path first becomes multi-turn (`lastTurn == 2` and the previous preview was 1 turn), vanilla plays `UI_Multi_Turn_Movement_Alert`
    - when confirming a move after at least one realized turn count, vanilla plays `UI_Move_Confirm`
    - `RealizeMovementPath(...)` itself does not generate text, tooltip content, or combat-preview strings
  - Design implication for CAI:
    - vanilla preview exposes a small set of stable semantic facts: valid / queued / restricted / fog, implicit ranged attack vs movement, visible enemy-at-end affecting marker visibility, turn-break count, obstacles, swap, and portal segmentation
    - many current CAI movement sentences are interpretations layered on top of those visuals, not direct vanilla wording. A clean redesign should treat the items above as the real vanilla source model and decide separately what spoken abstraction best serves blind play.
- CAI movement speech now treats `UnitManager.GetMoveToPathEx(...)` as the source for path structure and uses the final `pathInfo.turns[#pathInfo.turns]` entry as the arrival turn. Do not use `#pathInfo.turns` as the turn count; that is the number of path nodes.
- `GetMoveToPathEx(...).turns` values are one-based turn labels, matching vanilla `WorldInput.lua`: `1` means the current turn, `2` means the next turn, and so on. They are not elapsed-turn counts. Speech phrased as a relative duration must therefore use `max(0, finalTurnLabel - 1)`; directly speaking label `2` as `2 turns` is an off-by-one error.
  - Implicit ranged-attack detection must run before movement-failure diagnosis, not only after a successful path build. Otherwise hostile cursor targets for ranged units can fall through to bogus move-failure speech like `Blocked by unit` instead of the intended attack-preview branch.
  - Mirror vanilla `RealizeMovementPath(showQueuedPath)` branch order when classifying preview state: queued-destination override, invalid-plot bail, swap test, `GetMoveToPathEx(...)`, implicit ranged-attack check only on real multi-plot paths, then queue/restricted/FOW/good lens selection. Keep the separate fallback branch for `#pathInfo.plots <= 1`: off-unit targets are the no-path `MovementBad` case, while the current unit tile is the same-tile case.
  - Vanilla has two different `MovementBad` shapes that CAI must not collapse together. A restricted path still has a real route, counters, and path details; a no-path target has only a bad destination marker. Speech should only announce enemy-at-end, delayed-combat, and route geometry for branches that actually have a real path.
  - Movement path direction speech should use `CAIHexCoordUtils.stepListFromPath(...)` with `{ x, y }` path nodes. Civ VI vanilla uses `IsVisible(...)` to choose the `MovementFOW` lens, but speech must distinguish revealed fog from unexplored tiles: use `IsRevealed(...)` for the route-geometry cutoff and only say `Then unexplored` after the first unrevealed path tile.
  - If active `MOVE_TO` interface info would immediately resolve as combat against an at-war visible unit, city, or district, CAI requests `LuaEvents.CAISpeakCombatPreview()` and suppresses movement speech. Queued-path reads keep speaking queued movement information instead of firing combat preview.
  - `MovementActions_CAI.lua` now owns shared move-target activation for both `MOVE_TO` primary action and the adjacent quick-move hotkeys. It analyzes destination plot ids directly with `BuildMovementPathInfo(...)`, arms one pending `{ owner, unitId, targetPlotId }` combat confirmation only for immediate move-to melee combat, silently syncs the CAI cursor to the target, then commits through the same vanilla `OnMouseMoveToEnd()` path used by mouse move confirmation.
  - Quick-move hotkeys are selection-driven, not cursor-driven. They resolve adjacency from the selected unit's live plot with `Map.GetAdjacentPlot(...)`, reuse the same movement-failure speech path as move mode, and only require a second press when the exact same unit and target plot are still immediate combat on a fresh re-analysis.
  - Quick-move hotkeys reject otherwise valid adjacent paths when `pathInfo.arrivalTurn > 1`. They announce `Not enough movement` and return before `OnMouseMoveToEnd()`, preventing the engine from queueing that move for a later turn. Normal `MOVE_TO` activation still permits multi-turn orders.
  - When `GetMoveToPathEx(...)` fails and CAI synthesizes a bad-path result with no real path nodes, movement diagnostics must still read the unit's live `unit:GetPlotId()` as the origin. Using `pathInfo.plots[1]` in that case can silently replace the true start plot with the destination plot and break embark-tech failure diagnosis for coastal water targets.
- Runtime dumps confirm `GetMoveToPathEx(...)` exposes only the keys vanilla uses (`plots`, `turns`, `obstacles`, `entrancePortals`, `exitPortals`). `MovementCost_CAI.lua` therefore replays the directed edges of that engine-selected route to calculate MP cost and arrival remainder. It reads active abilities and promotions through the unit, interprets their movement modifiers through `UnitAbilityModifiers` / `UnitPromotionModifiers`, and combines live plot terrain/feature cost with roads, bridge support, rivers, shore transitions, portals, ZOC, and the full-movement adjacent-tile rule. The pathfinder's per-node turn labels remain authoritative for turn boundaries.
  - Movement cost and nonzero arrival remainder are appended only by `BuildMoveToInterfaceInfo(...)`, so they are spoken while moving the cursor in `MOVE_TO` interface mode and by that mode's explicit interface-info read. Shared movement activation and queued-path speech do not add them.
  - Formation movement must use `GetFormationMovesRemaining()` / `GetFormationMaxMoves()` when `GetFormationUnitCount() > 1`. Civilian/support ZOC exhaustion must use live `unit:IgnoresZOC()` rather than only inspecting the subject unit's modifiers, because the engine method includes ZOC immunity inherited from a formation escort.
  - `MovementCost_CAI.lua` records each local unit's plot on `Events.LocalPlayerTurnBegin`. For every later `pathInfo.turns` boundary it treats `plots[i - 1]` as the next turn's starting waypoint, removes terrain-conditional movement earned at the real current-turn start from each formation member's live maximum, and reapplies the bonus for the waypoint. Supported data-backed start conditions are `MODIFIER_PLAYER_UNIT_ADJUST_CLEAR_TERRAIN_START_MOVEMENT` (Heavy Chariot / War-Cart family and Maryannu family amounts come from modifier arguments) plus Bà Triệu's Woods/Rainforest/Marsh movement, including the friendly-territory doubled amount. A formation uses the lowest recalculated member maximum.
  - 2026-05-19 comparison against Civ V Access `CivVAccess_PathDiagnostic.lua` / `CivVAccess_UnitControlMovement.lua`: that mod does not settle for a generic move failure. It re-runs the engine pathfinder with progressively relaxed flags, then names causes such as blocked borders / would declare war, friendly stacking, at-war enemy blocker, no embark tech, no deep-water tech, mountain or natural wonder, no naval connection, cannot attack from land, cannot attack from water, and cannot travel to land.
  - Civ VI does not expose a Civ V-style discriminative path API. In confirmed Lua/UI surfaces we have only the `GetMoveToPathEx(...)` structure, `UnitManager.GetOperationTargets(...)` for implicit ranged attacks, `UnitManager.GetReachableZonesOfControl(...)`, `CombatManager.IsAttackChangeWarState(...)`, plot terrain/water queries, visibility checks, and generic `UnitManager.CanStartOperation(...)` booleans.
  - Civ VI's generic `UnitManager.CanStartOperation(unit, operationType, plotOrParams, returnResults)` signature can return `UnitOperationResults.FAILURE_REASONS` for many unit operations when `returnResults=true`, and vanilla `UnitPanel.lua` appends those localized strings for disabled actions. However, direct probing showed that per-destination `MOVE_TO` preview does not populate useful `FAILURE_REASONS`, `ACTION_NAME`, or `ADDITIONAL_DESCRIPTION` data through the tested `CanStartOperation(...)` call shapes, so CAI should not rely on that path for movement diagnostics.
  - CAI adds movement hints from other confirmed vanilla systems: for non-queued movement preview only, visible ZOC is detected by intersecting `pathInfo.plots` with `UnitManager.GetReachableZonesOfControl(unit, true)`. CAI tracks two internal facts from that set: any non-destination path node means `intersects zone of control`, while a destination plot means `ends at zone of control`. Spoken output is intentionally mutually exclusive: destination ZOC takes priority, otherwise CAI speaks intersection. War-start warning still follows `CombatManager.IsAttackChangeWarState(unit:GetComponentID(), x, y)`.
  - CAI now splits movement preview verbosity by caller: ordinary move-mode cursor reads from `GetActiveInterfacePlotInfo(...)` keep the compact summary only, while explicit `Space` reads through `SpeakActiveInterfacePlotInfo(...)` add the step-by-step path line plus fog / unexplored path lines. Compact cursor reads should still keep failure, arrival turn, obstacle, tunnel, ZOC, war-start, combat-at-destination, and blocked-state speech.
  - Detailed path-step speech now honors turn breaks in both places that expose the full path. Explicit move preview splits visible steps into per-turn segments using `pathInfo.turns`, while queued-path speech splits cached visible steps at queued waypoint markers; both join segments with localized `then`.
  - Normal movement previews should not speak generic embark/disembark requirements at all. Access or tech requirements belong only to the failure diagnostic path, for example `Requires: <tech> technology` on an invalid target.
  - Delayed combat speech should be driven directly by hostile destination state plus arrival turn, not by a route-text fallback. If the destination will trigger combat after movement, CAI should say only `Encounters combat this turn` or `Encounters combat in n turns`; do not fall back to `Enemy at destination` for delayed hostile contact.
  - Immediate movement combat confirmation must use the same engine-backed target test as vanilla `UnitPanel`: call `CombatManager.SimulateAttackInto(attackerComponentID, nil, x, y)`, take the returned defender component and combat type, then require `CombatManager.CanAttackTarget(attackerComponentID, defenderComponentID, combatType)`. Foreign plot ownership, an at-war owner, or the presence of a district is not sufficient because ordinary territory and some districts are not attackable targets.
  - Current practical parity judgement versus Civ V:
    - can match directly now: blocked visible unit, impassable terrain / mountain, land-vs-water attack incompatibility, sea unit cannot travel to land, ZOC entry, war-start warning, embark/disembark along a valid route, fog vs unexplored, swap, queued path, arrival turn, and combat-at-end preview suppression
    - can now approximate in CAI from start/target plot state: blocked foreign territory, unit-specific embark tech (`TECH_SAILING` for builders, `TECH_CELESTIAL_NAVIGATION` for traders, `TECH_SHIPBUILDING` for other land units), and `TECH_CARTOGRAPHY`-gated ocean travel when the destination plot itself is ocean once embark is already unlocked
    - confirmed gameplay data: `Technologies.xml` uses `EmbarkUnitType="UNIT_BUILDER"` on `TECH_SAILING`, `EmbarkUnitType="UNIT_TRADER"` on `TECH_CELESTIAL_NAVIGATION`, `EmbarkAll="true"` on `TECH_SHIPBUILDING`, and `CARTOGRAPHY_GRANT_OCEAN_NAVIGATION` on `TECH_CARTOGRAPHY`
    - cannot currently match from confirmed Civ VI Lua alone: Civ V-style closest-reachable direction beyond adjacent plots, natural-wonder-specific blocker naming, and naval no-water-connection diagnosis
  - `BuildMovementResultSpeech(unit, targetX, targetY, turnsToArrival)` formats post-move result text for later event handlers. It compares the unit's live position to the original target; on arrival it uses `unit:GetMovementMovesRemaining()` as Civ VI's display movement value, so no Civ V-style `MOVE_DENOMINATOR` conversion is needed. If the unit stopped short, caller-supplied `turnsToArrival` controls whether CAI says `Stopped short, n turns till arrival` or bare `Stopped short`.
  - Runtime movement tracing confirms `Events.UnitMoved(playerID, unitID, x, y, locallyVisible, stateChange)` fires once for each traversed hex, followed by one `Events.UnitMoveComplete(playerID, unitID, x, y)` at the operation boundary. `locallyVisible` is populated per step; the extra `visibleToLocalPlayer` and `unitState` parameters declared by `TutorialUIRoot.lua` are nil in normal runtime callbacks. `UnitMoveLog_CAI.lua` collects only locally visible adjacent steps, formats exact continuous paths with `CAIHexCoordUtils.stepListString(...)`, and flushes one persistent Movement-category entry on `UnitMoveComplete`. Hidden coordinates are never cached or used to connect visible segments.
  - Discontinuous relocation is split across event families. Runtime testing confirms Observation Balloon airlift and trader city transfer raise `Events.UnitTeleported(playerID, unitID, x, y)` with the destination and no `UnitMoved`/`UnitMoveComplete` pair. Bomber rebase instead raises one `UnitMoved` destination followed by `UnitMoveComplete`, even when the origin is non-adjacent. A movement log must therefore consume both streams, compare `UnitMoved` endpoints with its cached last-visible position, and classify a non-adjacent single endpoint as relocation rather than formatting it as a traversed path. Vanilla `UnitFlagManager.lua` and `SelectedUnit.lua` consume `UnitTeleported` to refresh the unit at its destination.
  - Civ VI's UI `UnitMoved` event does not provide the origin coordinates. `UnitOperationStarted(playerID, unitID, operationID)` also provides no coordinates, but fires early enough to offer a candidate pre-move snapshot through the live unit. The movement log captures that snapshot only for owned units or units currently reported visible by `PlayersVisibility[observer]:IsUnitVisible(unit)`; it also captures visible formation members. `unit:GetFormationUnits()` is not usable in these UI contexts. Shared `GetFormationUnitsOnPlot(unit)` in `inGameHelpers_CAI.lua` instead enumerates `Units.GetUnitsInPlotLayerID(unit:GetX(), unit:GetY(), MapLayers.ANY)` and matches `GetFormationID()` after confirming `GetFormationUnitCount() > 1`; both the movement logger and movement-cost simulation use it. The operation snapshot is preferred over the persistent last-visible location when the first `UnitMoved` step arrives. Initialization and local turn start also seed every currently visible unit, including explicitly enumerated barbarians, so a unit already on screen does not need a visibility-transition event before its next move. This timing still requires runtime validation across AI, queued, rebase, swap, and formation operations.
  - Matching Civ V's announcement fallback, a discontinuity, relocation, or movement with no clean adjacent steps uses `CAIHexCoordUtils.directionString(...)` to announce net displacement whenever a safe visible origin is known. Explicit teleports use the operation snapshot or the prior visible position the same way, and a known net-zero displacement produces no entry. If the origin was hidden or never observed, CAI reports generic movement/relocation rather than exposing or inventing it. Routine trader movement remains unannounced but still advances the internal visible-position cache so a later city transfer does not inherit a stale origin.
  - `UnitMoveLog_CAI.lua` keeps its position, visibility, in-flight movement, and pending-speech state per local observer through `PlayerStateManager`. In hotseat, entries append silently to that observer's persistent message buffer. The separate pending-speech queue becomes eligible on the observer's next `Events.LocalPlayerTurnBegin` and is spoken after `LuaEvents.PlayerChange_Close(playerID)`, keeping it behind the handoff/password prompt. Delivery clears only pending speech, never historical buffer entries.
  - Movement-log speech filters are Events-section dropdowns for eligible own, teammate, hostile-major, neutral-major, city-state, and barbarian movement. Each offers `military`, `civilian`, `both`, and `none`; a unit is military when its live combat or ranged-combat strength is positive, otherwise civilian. All faction defaults are `none` except barbarians, which default to `both`. The dropdowns combine with the existing Message Buffer movement-speech master setting and never prevent storage. Invalid legacy checkbox values fall back to the new per-setting default in both the settings UI and movement logger. Ordinary trader route shuttling is excluded, while trader city relocation through `UnitTeleported` remains logged as civilian movement.
  - Owned direct path movement is excluded from the movement log's announcement path. `MovementActions_CAI` pending moves already create an immediate movement-result buffer entry, so the new logger omits its duplicate record. Other directly ordered move-to or swap movement is still stored silently with its observed path. `UnitOperationStarted` marks selected-unit `MOVE_TO` and `SWAP_UNITS` operations, including formation members, and the marker is consumed by the resulting `UnitMoved` event. Relocations such as rebase, airlift, trader city transfer, deploy, teleport, and spy travel remain eligible for announcement even when directly ordered. Queued continuations and automated movement have no fresh direct-path marker, so they also remain eligible for the owned-unit announcement filter.
  - Completed movement records are deferred until the next WorldInput update so formation-member completion events can be consolidated. Members group only when owner, formation ID, origin, destination, visibility/relocation state, and compressed path all match. Their localized labels use `LOC_CAI_AND`: two units format as `A and B`; three or more format as `A, B, and C`. A mixed military/civilian formation remains eligible under either matching announcement filter while producing only one buffer entry and one spoken line.
  - `EventSubs_CAI.lua` owns orphan event subscriptions that do not naturally belong to a screen override. It currently listens to `Events.UnitMoveComplete(playerID, unitID, x, y)` for the local player only, finds the unit, checks `UnitManager.GetQueuedDestination(unit)`, re-runs `UnitManager.GetMoveToPathEx(unit, queuedPlotId)` when a queued destination remains, and passes the final turn entry to `BuildMovementResultSpeech(...)` before speaking the result.
  - `UnitWaypoints_CAI.lua` is a WorldInput-owned on-demand path service. It does not retain Lua pathfinder results. Each request resolves either the supplied player/unit ids or the selected unit, reads its live queued destination, calls `UnitManager.GetMoveToPathEx(...)`, and derives a complete snapshot from that result.
  - A snapshot contains path entries, path and waypoint lookups, destination, next waypoint, the raw one-based arrival turn label, and the elapsed turns until arrival. Waypoints are end-of-turn stop plots derived by marking `plots[i - 1]` when `turns[i]` increases. The current plot is never marked, and the destination is always treated as the final waypoint.
  - `GetQueuedPathSnapshot(playerID, unitID)` is the preferred API when a caller needs more than one path fact. Compatibility getters also rebuild on demand. Plot and scanner membership queries use the selected unit, while unit panel and unit flag requests pass the actual unit identity.
  - `Events.UnitSelectionChanged` and `Events.UnitMoveComplete` only request a queued-path World Scanner category rebuild; they no longer populate or refresh Lua path state.
  - `UnitPanel_CAI.lua` uses the same fresh snapshot for queued-path steps and arrival speech. `GetNextUnitWaypoint(playerID, unitID)` lets unit flags announce `Next waypoint: ...` for local queued units regardless of selection.
  - `PlotToolTip_CAI.lua` consumes `IsWaypointPlot(plotId)` through a `waypoint` plot-info helper and can announce `Waypoint` for matching plots.
  - `WorldScannerCategory_waypoints.lua` represents the selected unit's live queued path as a dynamic `Queued path` scanner category with `Full path` and `Waypoints` subcategories. Scanner item labels use the current `RequestPlotInfo(plotId, requestedKeys)` contract so the category reuses normal plot naming instead of custom label logic. The July 11 plot-tooltip refactor removed the older `(plot, requestedKeys, explicitPlotId)` form; stale three-argument calls pass `nil` as the effective plot id and therefore return `No plot` for every item. The waypoint and valid-target scanner callers have both been migrated to the two-argument form.
  - `WorldScannerCategory_cityManagement.lua` now exposes the active `CITY_MANAGEMENT` overlay as a scanner category when that interface mode is active and either the citizen-management lens or purchase lens is on.
    - It rebuilds from the shared city-management helper instead of caching live controls.
    - Current subcategories are `locked`, `specialists`, `worked`, `available`, `swappable`, `purchasable`, and `tooExpensive`.
    - Scanner item labels reuse the same full spoken line as `InterfaceInfo` for that plot so Space reads and scanner reads stay aligned.
  - `GetActiveInterfacePlotInfo(plot)` dispatches by `UI.GetInterfaceMode()` through `InterfaceInfoHelpers[...]`.
  - `SpeakActiveInterfacePlotInfo(plot)` is the one-shot speech wrapper for the Space-bound `InterfaceInfo` action. Helpers remain plain functions and return either a string, a list of strings, nil, or `false` when explicit speech was handled by side effect.
  - Current supported preview modes include `MOVE_TO`, `CITY_MANAGEMENT`, `DISTRICT_PLACEMENT`, `BUILDING_PLACEMENT`, `RANGE_ATTACK`, `CITY_RANGE_ATTACK`, `DISTRICT_RANGE_ATTACK`, `AIR_ATTACK`, `DEPLOY`, `REBASE`, `TELEPORT_TO_CITY`, `FORM_CORPS`, `FORM_ARMY`, `AIRLIFT`, `SACRIFICE_SELECTION`, `KILL_WEAKER_UNIT`, `TRANSFORM_UNIT`, `RESTORE_UNIT_MOVES`, and `NAVAL_GOLD_RAID`.
  - Ranged, city ranged, district ranged, and air attack helpers call `LuaEvents.CAISpeakCombatPreview()` and return `false`; combat preview speech remains owned by `UnitPanel_CAI.lua`.
  - `CITY_MANAGEMENT` interface info is now rebuilt from the same live `CityManager.GetCommandTargets(...)` data that `PlotInfo.lua` uses instead of reading overlay controls:
    - citizen management uses `CityCommandTypes.MANAGE` and reads `PLOTS`, `CITIZENS`, `MAX_CITIZENS`, and `LOCKED_CITIZENS`
    - tile swapping uses `CityCommandTypes.SWAP_TILE_OWNER`
    - tile purchase uses `CityCommandTypes.PURCHASE` plus live `city:GetGold():GetPlotPurchaseCost(plotId)` and local treasury gold
    - speech intentionally does not split compact vs explicit modes; passive preview and explicit `Space` both say the same full plot-state line and omit unavailable states
  - `PlotInfo_CAI.lua` now replaces `PlotInfo` and keeps vanilla overlay logic, but listens for the generic `CAIInterfaceWidgetPrimaryAction` and `CAIInterfaceWidgetSecondaryAction` Lua events.
    - It reacts only when `widgetId == "CAIWorldInputCityManagement"`.
    - Primary resolves to purchase first when the purchase lens is active on that plot, otherwise citizen manage; secondary resolves explicitly to tile swap.
    - It wraps vanilla `OnClickCitizen(...)`, `OnClickPurchasePlot(...)`, and `OnClickSwapTile(...)` only to prime a pending action payload with owner, city id, and clicked plot id.
    - Result speech is then driven by matching game events: `CityWorkerChanged(ownerPlayerID, cityID)` for manage, `CityMadePurchase(owner, cityID, plotX, plotY, purchaseType, objectType)` for purchase, and `CityTileOwnershipChanged(owner, cityID)` for swap.
    - Vanilla uses both a 2-arg and a 4-arg `OnCityWorkerChanged` helper signature in different UI files, but CAI's pending manage feedback should treat the event as owner + city only and then speak the pending plot's refreshed state.
    - Purchase matching should agree on owner, city, and purchased plot id. Swap matching should agree on owner and city, then speak the pending plot's refreshed state.
    - It also listens to `LuaEvents.CAICursorMoved` and reuses vanilla `OnSpinningCoinAnimMouseEnter(...)` to play the purchase coin animation when the CAI cursor lands on an affordable purchase plot.
  - District placement preview reuses vanilla `AdjacencyBonusSupport.GetAdjacentYieldBonusString(...)` for the short bonus summary, detailed tooltip text, and requirement/warning text, while CAI computes owned-valid versus purchasable-valid state locally from `CityManager.GetOperationTargets(...)` and `CityManager.GetCommandTargets(...)`.
  - Wonder placement uses valid owned plots from `CityManager.GetOperationTargets(...)` and valid purchasable plots from `CityManager.GetCommandTargets(...)` filtered through `plot:CanHaveWonder(...)`.
  - District and wonder placement deliberately leave the navigation cursor unrestricted and do not move it to the selected city center on mode entry. Vanilla does not spatially lock map inspection in these modes, and surrounding plots can matter to adjacency, requirements, and orientation. Existing valid-target navigation and live placement information remain responsible for efficient selection and action validation.
    - ordinary buildings do not enter a map-placement interface; `InterfaceModeTypes.BUILDING_PLACEMENT` is the wonder-placement flow
    - district and wonder placement can legally target an unowned plot when `CityCommandTypes.PURCHASE` returns it and `plot:CanHaveDistrict(...)` / `plot:CanHaveWonder(...)` accepts it; vanilla labels that target `Placement_Purchase` and purchases it as part of placement
  - The navigation-cursor boundary applies only to `InterfaceModeTypes.CITY_MANAGEMENT` and follows the selected city's live interaction envelope rather than plot ownership alone:
    - tile-purchase mode necessarily exposes unowned `CityCommandTypes.PURCHASE` targets
    - citizen management exposes workable/specialist plots through `CityCommandTypes.MANAGE`, while `CityCommandTypes.SWAP_TILE_OWNER` can expose a plot currently assigned to a neighboring city; such a plot is outside the selected city's current footprint but still belongs to the local empire
    - base data sets `CITY_MAX_BUY_PLOT_RANGE` to 3, while passive culture acquisition can reach `PLOT_INFLUENCE_MAX_ACQUIRE_DISTANCE` 5; local-player ownership is too broad because it includes other cities, while the selected city's current plot assignment alone is too narrow because it excludes purchase and swap targets
    - use plots assigned to the selected city as the traversal backbone and add live manage, purchase, swap, and scenario-specific action plots as explicit exceptions
    - CAI enforces the envelope at `CAICursor:MoveTo(...)`, the common step/jump choke point, so scanner jumps, bookmarks, and other cursor moves cannot bypass the lock; a rejected move retains the current plot and announces the localized city-management boundary
    - live selected-city membership is checked with `Cities.GetPlotPurchaseCity(plot)` so a newly purchased tile remains admitted even while `Map.GetCityPlots():GetPurchasedPlots(city)` is briefly stale; action plots come from the authoritative city-management helper
    - `WorldInput_CAI.lua` invalidates and revalidates the scope on interface-mode entry, selected-city changes, purchases, worker changes, tile-owner swaps, and local-player changes; if the current cursor is outside the new envelope, it moves to the selected city center with the internal `scope` move reason
  - Non-attack targeting/destination helpers use shared `CAIInterfaceTargets` target resolution. Space says `Valid` plus the plot target label, `Invalid target` when the CAI cursor is not on an eligible plot, and for `FORM_CORPS` / `FORM_ARMY` appends `formation target` after the target unit name.
  - `PlotToolTip_CAI.lua` now treats `PlotInfo5` as a general interface preview slot rather than a movement-only slot, and automatic cursor speech includes the same interface preview lines when the active mode supplies them.
- CAI cursor-move plot speech now distinguishes revealed fog from both live-visible plots and unexplored plots. Use `PlayersVisibility[observer]:IsRevealed(plot)` together with `:IsVisible(plot:GetIndex())`: revealed plus not visible should speak the CAI-owned `Fog` helper, while unrevealed plots still fall back to the terrain-name helper's fog-of-war behavior.
- Interface-mode and active-lens plot information must use the same local-observer reveal gate before building any details. For an unrevealed plot, `GetActiveInterfacePlotInfo(...)` returns no details so cursor movement reaches its existing `LOC_MINIMAP_FOG_OF_WAR_TOOLTIP` plot-name fallback, while the explicit Interface Info action speaks that localization directly. Spectator observers retain unrestricted plot information.
- NavCursor zone-state updates should use that same visibility gate. When the cursor lands on an unrevealed plot, do not recompute or overwrite the remembered continent / owner zone state; only visible or revealed plots should update those zone trackers.
  - `PlotToolTip_CAI.lua` now selects its vanilla include by active rules content: base `PlotToolTip`, `PlotTooltip_Expansion2` when `IsExpansion2Active()` is true, `PlotToolTip_BarbarianClansMode` when `GameConfiguration.GetValue("GAMEMODE_BARBARIAN_CLANS") == 1`, and `PlotTooltip_Expansion2_BarbarianClansMode` when both are active.
  - `ExposedMembers.CAIInfo:RequestPlotInfo(plot, requestedKeys, optionalPlotId)` returns a flat `string[]`. Existing callers can keep passing the explicit `plot` object. Callers that need info for a specific target plot without depending on the current cursor plot can pass `nil` for `plot` and the target plot id as `optionalPlotId`. Individual plot info helpers may return either one string or a list of strings; the request path must flatten helper tables before concatenating speech. This matters for `interfaceInfoHelpers_CAI.lua`, whose movement and placement previews are multi-line.
  - `PlotToolTip_CAI.lua` also owns one-shot plot read actions through `Events.InputActionTriggered`. The current action ids are `PlotReadUnits`, `PlotReadYieldRiverOwner`, `PlotReadStats`, `PlotReadRelativeCoords`, and `PlotReadGeography`; each action builds a requested-key list and then routes through the same `RequestPlotInfo(...)` / helper pipeline used by cursor speech. The geography bucket appends district, building, and great-work information after all geography keys.
  - City-center plot data satisfies both title-helper shapes: `data.IsCity == true` with a valid `DistrictType` activates `cityDistrictTitle`, while the same plot's valid `DistrictID` and `DistrictType` also activate `districtTitle`. The geography bucket therefore requests `districtTitle` only when `data.IsCity ~= true`; city centers use `cityDistrictTitle`, while non-city districts retain `districtTitle` and its pillaged/construction suffixes.
  - `PlotReadStats` intentionally uses separate `movement`, `defense`, and `appeal` helpers instead of a bundled physical-info bucket. `relativeCoords` is the one helper allowed to speak when plot visibility gates would otherwise suppress normal tooltip data.
  - Current default bindings in `src/data/hotkey_config_CAI.xml`: `S` / `NP_5` -> `PlotReadUnits`, `W` / `NP_8` -> `PlotReadYieldRiverOwner`, `X` / `NP_2` -> `PlotReadStats`, `Shift+S` / `Shift+NP_5` -> `PlotReadRelativeCoords`, and `B` -> `PlotReadGeography`. Surveyor grow/shrink use `Shift+W` / `Shift+X`.
  - `WorldInput_CAI.lua` exposes both move mode and district placement mode through CAI `InterfaceMode` widgets; district placement uses the same Escape / primary-action widget pattern as move mode, but routed to `OnMouseDistrictPlacementCancel()` and `OnMouseDistrictPlacementEnd()`.
  - `WorldInput_CAI.lua` also exposes targeting widgets for `RANGE_ATTACK`, `CITY_RANGE_ATTACK`, `DISTRICT_RANGE_ATTACK`, `AIR_ATTACK`, `WMD_STRIKE`, `ICBM_STRIKE`, `COASTAL_RAID`, `DEPLOY`, `REBASE`, `TELEPORT_TO_CITY`, `FORM_CORPS`, `FORM_ARMY`, `AIRLIFT`, `SACRIFICE_SELECTION`, `KILL_WEAKER_UNIT`, `TRANSFORM_UNIT`, `RESTORE_UNIT_MOVES`, and `NAVAL_GOLD_RAID`. Return delegates to the matching vanilla execution function, and Escape delegates through vanilla `OnPlacementKeyUp(...)`.
  - Base-game interface modes that still do not have CAI world-input widgets fall into two groups:
    - World-input-light modes whose useful information lives elsewhere:
      - `MAKE_TRADE_ROUTE` - `WorldInput.lua` only changes cursor; route choice and route details live in `Choosers/TradeRouteChooser.lua` plus related city-banner trade selection hooks.
      - `PLACE_MAP_PIN` - `WorldInput.lua` only changes cursor and commits placement; the user-facing flow lives in `Popups/MapPinListPanel.lua`.
      - `SPY_CHOOSE_MISSION` and `SPY_TRAVEL_TO_CITY` - `WorldInput.lua` only resets cursor/lens; chooser content lives in `Choosers/EspionageChooser.lua` and `Popups/EspionagePopup.lua`.
      - `FULLSCREEN_MAP` - visible content lives in `FullscreenMapPopup.lua`.
      - `VIEW_MODAL_LENS` - world input only routes clicks; active lens labels and state live in `MinimapPanel.lua`, `Panels/ModalLensPanel.lua`, and other HUD listeners.
      - `CITY_SELECTION` - `WorldInput.lua` only resets cursor; the mode is entered by `Panels/CityPanelOverview.lua` and coordinated with `Panels/ModalLensPanel.lua`.
      - `CINEMATIC` and deprecated `NATURAL_WONDER` - world input only toggles fixed tilt / cursor state; actual content lives in cinematic popups such as `NaturalWonderPopup.lua`, `WonderBuiltPopup.lua`, and `ProjectBuiltPopup.lua`.
    - Modes with little or no normal gameplay payload in world input:
      - `SELECTION` - ambient/default world mode. Useful speech is already split across CAI cursor reads, plot tooltip, scanner, surveyor, unit panel, city panel, and notification/HUD readers rather than a dedicated world-input widget.
      - `DEBUG` and `WB_SELECT_PLOT` - debug / World Builder only.
  - Expansion world-input replacements add interface modes that CAI currently does not mirror:
    - Expansion 1 adds `PARADROP` and `PRIORITY_TARGET` in `DLC/Expansion1/UI/Replacements/WorldInput_Expansion1.lua`. Expansion 2 includes that XP1 replacement layer too.
    - Expansion 2 adds `BUILD_IMPROVEMENT_ADJACENT` and `MOVE_JUMP` in `DLC/Expansion2/UI/Replacements/WorldInput_Expansion2.lua`.
    - Unlike city management, these four expansion modes are true world-input-owned target-selection modes. Their visible payload is built directly by the world-input replacement itself from `UnitManager.GetCommandTargets(...)` or `UnitManager.GetOperationTargets(...)` plus placement / movement overlays, so CAI should eventually add first-class interface widgets for them rather than delegating them to another popup or panel.
- CAI widgets require an id at construction: `mgr:CreateUIWidget(id, type, props)`.
  - Use stable semantic ids or focus keys for widgets that need direct lookup, for example `yield:economy`.
  - Use `mgr:GenerateWidgetId("CAIScreenWidgetType")` for repeated rows/items that must be unique but do not need stable lookup.
  - Widget ids are expected to be globally unique among live widgets.
  - `widget:GetId()` returns the widget id assigned at construction.
  - `widget:GetChildById(id, recurse)` returns a direct child by id, or recursively searches descendants when `recurse` is true.
  - `mgr:GetWidgetById(id, recurse)` returns the live widget or nil, searching from the active stack root backward.
  - `mgr:RemoveFromStack(id)` closes and destroys a pushed stack-root widget by id.
  - `mgr.CAISettings.ValidateWidgetIds = true` enables attachment-time duplicate-id warnings for debugging without adding normal runtime lookup-table state.
- `CivilopediaScreen.lua` / `CivilopediaSupport.lua` (decompiled/Assets/UI/Civilopedia/):
  - The stock Civilopedia is an in-game-only UI context. Base and expansion `InGame.xml` attach `<LuaContext ID="Civilopedia" FileName="CivilopediaScreen" Hidden="1"/>`, while `FrontEnd/MainMenu.xml` has no Civilopedia child context. A frontend mod can add a copied/custom context to a full `MainMenu.xml` replacement and queue it like the other frontend popups, but importing `CivilopediaScreen.lua` alone cannot instantiate a screen.
  - The larger frontend boundary is data, not rendering. The Civilopedia tables (`CivilopediaSections`, pages, groups, layouts, chapters, queries, and search terms) are declared in `Gameplay/Data/Schema/01_GameplaySchema.sql`, and the stock page scripts query the gameplay `GameInfo`/`DB` tables. They are not part of the frontend configuration schema used by `<FrontEndActions><UpdateDatabase>`. The stock open path also gates on `GameCapabilities.HasCapability("CAPABILITY_CIVILOPEDIA")`, which assumes an active ruleset.
  - Consequently, a complete, automatically ruleset- and mod-aware stock Civilopedia cannot be moved to the main menu by a small UI replacement. A limited frontend Civilopedia is feasible by supplying a frontend-owned data model and bypassing/adapting the capability gate; CAI's custom `Simple` accessibility articles are a particularly suitable subset because they do not need gameplay-object page queries. A full frontend encyclopedia would be a substantial custom browser or a maintained mirror of gameplay schema/data, and would not naturally reflect the finally selected gameplay mods/ruleset before game creation.
  - Data model: `_Sections`, `_PagesBySection[sid]`, `_PageGroupsBySection[sid]`. Public accessors `GetSections()`, `GetPages(sid)`, `GetPageGroup(sid, gid)`, `GetPage(sid, pid)`, `GetCurrentPage()` (returns `sid, pid`). `_CurrentSectionId` / `_CurrentPageId` are file-locals; always go through `GetCurrentPage()`.
  - Entry points: `OnOpenCivilopedia(sectionId_or_search, pageId)` opens or searches and ends with `UIManager:QueuePopup(ContextPtr, PopupPriority.Civilopedia)`; `OnClose()` calls `UIManager:DequeuePopup(ContextPtr)`. `LuaEvents.OpenCivilopedia` and `LuaEvents.ToggleCivilopedia` register the original open functions in `Initialize()`, and `Controls.WindowCloseButton:RegisterCallback(Mouse.eLClick, OnClose)` captures the original close function there too. If CAI wraps `OnOpenCivilopedia` / `OnClose` after `include("CivilopediaScreen")`, remove and re-add the `LuaEvents.OpenCivilopedia` listener and re-register the close button callback so the wrapped functions become the live open/close path.
  - Navigation: `NavigateTo(SectionId, PageId)` is the single re-render point for both the chapter stacks and the right-column quotes/icon lists. Wrap it to refresh CAI state on every page change; the same wrap fires whether the user clicked vanilla tabs or activated a CAI tree node.
  - History: vanilla breadcrumb state lives in file-local `m_kPageTrail`, so CAI cannot read the trail table directly from a wrapper file. Mirror the trail in CAI by wrapping `NavigateTo(...)` for append/truncate behavior and `NavigateToPageTrailIndex(index, bUpdateScroll)` for current-index changes, then call the vanilla `NavigateToPageTrailIndex(...)` from accessible history buttons so activation still uses the native path.
  - Readable text: text reaches the article through `SetPageHeader`, `SetPageSubHeader`, `AddFullWidthChapter` / `AddFullWidthHeader` / `AddFullWidthParagraph` / `AddFullWidthParagraphs`, `AddLeftColumnChapter` / `AddLeftColumnHeader` / `AddLeftColumnParagraph` / `AddLeftColumnParagraphs`, `AddLeftColumnHeaderBody`, and `AddLeftColumnIconHeaderBody`. Each takes a localized text key (often a `LOC_…`). Wrapping these and capturing `Locale.Lookup` of each argument is more reliable than walking `Controls.PageChaptersStack` / `LeftColumnStack` instance children.
  - Quotes: `AddQuote(quote, audio)` renders into `_RightColumnQuoteManager`. When `audio` is a non-empty string the instance's `PlayQuote` button is shown and registered to call `UI.PlaySound(audio)`. CAI captures both in the `AddQuote` wrap and exposes activation through a Button widget whose `OnClick` plays the audio.
  - Related links: `HookupIcon(icon_data, icon_control, button_control)` is the single point that wires every clickable related-concept icon (used by stat boxes' `AddIconLabel` / `AddIconList` and by `Do_AddIconHeaderBody`). When `icon_data` is a table with a non-nil `search_term` at index 3 the button's `eLClick` runs `CivilopediaSearch(search_term, 1)` then `NavigateTo(result.SectionId, result.PageId)`. CAI captures `{tooltip = icon_data[2], search_term = icon_data[3]}` and reproduces the click behavior on a Button widget in the article list.
  - Promotion relationship exception: the technology and civic layouts represent prerequisite/progression rows with `AddIconLabel({ icon, name, type }, name)`, so `HookupIcon` receives the target type and both vanilla and CAI can navigate. `CivilopediaPage_UnitPromotion.lua` instead localizes prerequisite names into strings and emits bullet-prefixed `AddLabel(...)` rows; `CivilopediaPage_GovernorPromotion.lua` does the same for both prerequisites and `Leads to Titles`. Those rows contain no target type or `search_term`, so vanilla renders non-clickable text and CAI faithfully captures static text. The promotion pages themselves are registered in the Civilopedia (`UnitPromotionType` / `GovernorPromotionType` are their page ids), so CAI can enhance these relationships by preserving the database type in the layout data or by deriving a title-to-page target explicitly; this is not a missing-page or failed-search issue.
  - Capture-buffer reset: clear text/quote/link buffers at the top of the `NavigateTo` wrap (before `orig`), because vanilla calls the text-add and `HookupIcon`/`AddQuote` functions during `RefreshPageContent` inside `orig`.
  - Section / page label fields: `section.TabName` (label, tooltip), `group.TabName`, `page.Title` (full title used for crumbs), `page.TabName` (short tab text), `page.PageGroupId` (may be nil for top-level pages). Not every page belongs to a group.
  - Stat boxes: `AddRightColumnStatBox(title, populate_method)` (line 1641) builds a stat box and calls `populate_method(stat_box)` once. `stat_box` is a local table built fresh per call, with methods `AddSeparator`, `AddHeader(caption)`, `AddLabel(caption)`, `AddSmallLabel(caption)`, `AddIconLabel(icon, caption)`, `AddIconNumberLabel(icon, value, caption)`, `AddIconList(icon1..icon4)`. These methods cannot be wrapped globally — wrap `AddRightColumnStatBox` itself and replace `stat_box[name]` with a shim that records and delegates before invoking the user populate. The user populate must still run; do not run it twice.
  - `AddIconLabel` / `AddIconList` internally call `HookupIcon`, so a global `HookupIcon` wrap will see every stat-box icon. If you also capture related links through `HookupIcon`, suppress that capture (via a state flag) for the duration of the wrapped stat-box populate so icons inside stat boxes don't duplicate into a global Related list.
  - Vanilla's so-called full-text `Civilopedia` search context is not an article-body index. `PopulateSearchData()` calls `Search.AddData(context, sectionId .. "|" .. pageId, title, "", terms)`, so it indexes the page title plus explicit aliases from `CivilopediaPageSearchTerms` / `CivilopediaPageSearchTermQueries`; its description field is empty. CAI Ctrl+F queried this context before the enhanced title-and-body replacement was implemented.
  - CAI's enhanced context uses `sectionId .. "|" .. pageId` as the search record id, the localized stripped `page.Title` as the title field, and captured localized body text as the description field. This preserves vanilla article-title searching and the engine's title-first relevance while adding body matches. Every cached page gets a record even when its body is empty. Section/group metadata remains available without becoming searchable. Civilopedia Ctrl+F result labels follow the existing pedia-result format but start with the article: `page.Title`, section `TabName`, then optional page-group `TabName`.
  - There is no universal article-body database column. `CivilopediaPageLayoutChapters` plus `GetChapterBody(...)` cover configured history/concept paragraphs, but layout scripts also add primary descriptions, unique abilities, and other dynamic text from their own `GameInfo` queries. Complete indexing must collect what each `PageLayouts[template](page)` emits rather than attempting to duplicate every layout's database logic.
  - The shipped base, XP1, XP2, and Babylon page layouts route their output through shared helpers (`SetPageHeader`, `SetPageSubHeader`, `AddChapter`, `AddLeftColumnChapter`, `AddHeaderBody`, `AddRightColumnStatBox`, portraits, and quotes) and do not directly access `Controls`, `ContextPtr`, UI events, or `UIManager`. This permits a headless indexing pass: temporarily substitute capture/no-op helpers, execute each cached page's layout, collect paragraph/body arguments while excluding header/title arguments, then restore every helper even if a layout fails. A fake stat-box object can run its populate callback if stat values should count as body text.
  - Some layouts are game-state-sensitive. Technology and civic layouts read active randomized-tree nodes and local-player reveal state, and Hero Unit reads the live Heroes manager. A body index built for the current session should preserve those visibility rules and be rebuilt when the Civilopedia data/ruleset context is recreated. Unknown mod-added layouts may call helpers or controls outside the shipped contract, so isolate per-page failures and fall back to generic `GetPageChapters(...)` / `GetChapterBody(...)` text for that page.
  - Exact initialization order is `CacheData()`, `RefreshSections()`, then vanilla `PopulateSearchData()`. `CacheData()` creates and localizes every section/page/group record, configured chapter override, and page-layout mapping, but it does not call any `PageLayouts[...]` renderer; article bodies therefore have not been emitted at that point.
  - `CivilopediaScreen_CAI.lua` includes `CivilopediaScreen`, whose final statement calls `Initialize()`. When that include returns, the page caches, all wildcard-included layout functions, and vanilla search context already exist. CAI can define its headless capture helpers and immediately build/optimize a separate body context at the bottom of its replacement file. This moves the one-time collection cost to UI-context initialization instead of the first Ctrl+F use; it does not remove the cost, so startup impact still needs measurement.
  - Civ VI's Civilopedia UI context does not expose `_G`; attempting `_G[name]` raises `attempt to index a nil value`. A late headless pass must save the known global render-helper functions explicitly, assign the capture/no-op functions directly by name, then explicitly restore each saved reference. The late pass does not need reflection or a separate event because it already runs after `include("CivilopediaScreen")` returns from vanilla initialization.
  - Vanilla registers the current `Shutdown` function with `ContextPtr:SetShutdown(Shutdown)` inside `Initialize()`. If CAI wraps `Shutdown` after `include("CivilopediaScreen")` to destroy its additional search context, it must call `ContextPtr:SetShutdown(wrappedShutdown)` again; merely replacing the global function does not change the callback reference already registered by vanilla.
  - `Search.Search(...)` result entries use `[1]` for the record id, `[2]` for the title with the matched term highlighted, and `[3]` for an ellipsized description snippet with the match highlighted. Tech Tree documents and populates all three fields. Vanilla Civilopedia calls `[3]` page content “NYI” only because its own `Search.AddData(...)` passes an empty description. If CAI stores article body text as the description, `[3]` directly provides a match preview suitable for a search-result tooltip while CAI supplies the normal unhighlighted article/section/group label.
  - CAI `SearchPanelWidget:SetResults(...)` already accepts `entry.tooltip` and creates a `MenuItem` whose focus speech includes the tooltip, so a Civilopedia result can speak its article-first label followed by the body preview without changing the result widget type. Multi-term AND search must keep one clean preview per result: retain the first non-empty term excerpt instead of concatenating near-duplicate excerpts for every query term.
  - Custom-handler multi-term search must not use the normal `MAX_RESULTS * 3` intermediate cap. Each whitelist term is queried separately before intersection; a broad one-character term can have the correct page below that cap, producing a false negative such as `nuclear device T` failing while the narrower `nuclear device Ty` succeeds. Custom multi-term handlers request up to 2000 intermediate hits, then the SearchPanel applies its normal final 50-result cap.
  - Multi-term intersection must also restore deterministic relevance after collecting results from the key map. Preserve each hit's per-term engine rank, sum those ranks by result id, then sort the final intersection by: exact full `searchTitle` match, title prefix/full-phrase match, summed engine rank, localized label. Without this final sort, Lua `pairs(...)` order can place a body match such as Power Plants before the exact `Nuclear Device Types` article-title match.
  - The SearchPanel now has two query modes. Its default `"terms"` mode retains CAI's term-by-term AND and `--term` exclusion behavior for maps, trees, and lists. `"raw"` mode passes the complete edit-box string to a custom handler (or its own `Search.*` context) exactly once and does not apply CAI term parsing. Civilopedia uses raw mode for its title-and-body context so the engine evaluates the whole phrase, retains its native phrase/title relevance, and returns one coherent description preview.
  - Search context creation exposes highlight wrappers and the ellipsis marker, but no snippet-length parameter. Civilopedia retains the full stripped body by record id and creates its own longer preview around the queried term (180 characters of context on each side, adjusted to spaces to avoid cutting words/UTF-8 bytes), falling back to engine result `[3]` when it cannot locate the term locally.
  - The UI-manager Ctrl+I lookup uses the same vanilla `Search.Search("Civilopedia", query)` context as the Civilopedia live search. Vanilla constructs `page.Title` by taking the first available `TITLE` localization key in `FindPageTextKey(...)` order (page `TextKeyPrefix` variants, section/page variant, generic page variant), then falling back to the page database `Name` and finally `PageId`; `PopulateSearchData()` localizes and strips tags from that title before indexing it with explicit page aliases. Lookup result `[2]` is therefore the authoritative localized article title and should be processed for speech rather than reconstructed from `PageId`. Ctrl+I runs `ProcessText(...)` and removes balanced parenthetical spans before splitting an ordinary widget label on commas or colons and before submitting every ordinary or plot-derived query, so icon-only labels become searchable concepts, prefixed labels such as `Technology completed: Pottery` expose their article title, and qualifiers such as `(France)` do not become part of the lookup; terms that normalize to empty are skipped. The same parenthetical removal is used only for result comparison, while the displayed article title remains intact. Because native search is live type-ahead and returns prefix/substring candidates, Ctrl+I retains a result only when its processed title equals the processed query under `Locale.ToUpper(...)`; aliases and partial-title hits do not qualify. Multiple exact concepts remain independent searches whose results are de-duplicated in concept order and capped at 20 overall.
- Focus seeding on a deep `TreeviewItem` requires expanding its ancestor tree items first: `TreeviewItem.GetDefaultChild` returns nil when `IsExpanded` is false (`src/UI/uiManager/widgetTemplates.lua:878-883`), so `mgr:SetFocus(leaf)` will not land on a leaf inside collapsed ancestors. Walk `node.Parent` up to the Treeview root and call `:Expand()` on each ancestor `TreeviewItem` before `SetFocus`.
- CAI's custom `Accessibility Mod` data now includes a `Gameplay` page group ahead of `Screen Tutorials` and `UI Widgets`. The eleven page rows match the level-three headings under README `Gameplay`; level-four README sections become titled chapters within their owning article. Because the stock `Simple` renderer obtains chapter order from `CivilopediaPageLayoutChapters`, each gameplay article has its own `CAIGameplay...` layout with `ScriptTemplate="Simple"` rather than sharing a union layout that could expose unrelated empty chapters. `scripts/Generate-TutorialCivilopedia.ps1` is the source of truth for the group, page, layout, chapter-header, and paragraph rows; player-facing prose remains localized in `AccessibilityPediaText_CAI.xml`.
- The key-bindings reference likewise uses a dedicated `CAIKeyBindings` Simple layout. It contains introductory and remapping chapters followed by one chapter for each of the eleven `InputCategories` in `hotkey_config_CAI.xml`. Online owns the `UI_OpenChatPanel` action and its default Control+Slash gesture; Lenses currently has no action row because the accessible lens list belongs to `CAI_UI`. Default-binding prose is audited against the action rows rather than generated from displayed gesture strings, because those strings can change after the player remaps them.
- The accessible lens list intentionally omits the Empire lens. Cursor plot information, banner readouts, and ordinary Scanner categories already expose its useful city, district, wonder, worked-territory, and ownership information; presenting it again as a lens would duplicate those paths. The Lenses article still includes an Empire chapter so the omission is explicit rather than appearing accidental.
- Reveal announcements batch newly explored tiles and newly visible foreign cities, resources, districts, improvements, and units. They distinguish units becoming hidden from objects known to be gone, avoid duplicating natural-wonder and special-improvement discoveries, append to the `reveal` message-buffer category, and respect the turn-start, while-moving, and outside-turn settings. Hotseat outside-turn announcements wait for the affected player's turn.

- `ReligionScreen.lua` / `ReligionScreen.xml` (decompiled/Assets/UI/):
  - Mainline base, Rise and Fall, and Gathering Storm all register the same `ReligionScreen` context. Base `Assets/UI/InGame.xml`, XP1 `DLC/Expansion1/UI/InGame.xml`, and XP2 `DLC/Expansion2/UI/Replacements/InGame.xml` each declare `<LuaContext ID="ReligionScreen" FileName="ReligionScreen" Hidden="1"/>`; there is no separate XP1/XP2 religion-screen replacement in the decompiled UI mirror.
  - Mainline base, Rise and Fall, and Gathering Storm also keep the same standalone `PantheonChooser` context. `Assets/UI/InGame.xml` registers `<LuaContext ID="PantheonChooser" FileName="PantheonChooser" Hidden="1"/>`, and there is no separate XP1/XP2 pantheon-chooser replacement in the decompiled UI mirror.
  - Opening path is split across three vanilla entry points:
    - `LaunchBar.lua` opens `PantheonChooser` instead of `ReligionScreen` when the local player can found a pantheon and has none yet; otherwise it raises `LuaEvents.LaunchBar_OpenReligionPanel()`.
    - `NotificationPanel.lua` routes both choose-pantheon and choose-religion notifications into the religion flow (`OpenPantheonChooser` for pantheon, `OpenReligionPanel` for religion).
    - `PantheonChooser.lua` closes after founding the pantheon, then immediately raises `LuaEvents.PantheonChooser_OpenReligionPanel()` so the full religion screen opens next.
  - `PantheonChooser.lua` / `PantheonChooser.xml` (decompiled/Assets/UI/Choosers/) is a separate compact left slide-out chooser:
    - `PantheonChooserSlideAnim` slides a 495px-wide panel in from the left.
    - Top panel: decorative religion frame plus either the generic `Choosing a Pantheon` title or a selected-belief summary card.
    - Body panel: one scrollable vertical stack of pantheon belief buttons; each row is a large card with icon, uppercase belief name, and wrapped description.
    - Bottom drawer: `Found this Pantheon` confirm button plus `Reselect Pantheon` clear-selection button.
    - Interaction model: single-select only. Clicking a belief highlights it and reveals the confirm drawer; clicking the selected summary card or the reset button clears the selection; Escape or the close button closes the chooser.
    - Confirm path: `ConfirmPantheon()` submits `PlayerOperations.FOUND_PANTHEON`, closes the chooser, then raises `LuaEvents.PantheonChooser_OpenReligionPanel()` to open the full religion screen.
  - The religion screen is a single stateful popup that swaps whole-page containers on one shared context. `ViewMyReligion()` is the state router and chooses among:
    - `WorkingTowardsPantheon()`
    - `WorkingTowardsReligion()`
    - `ChooseReligion()`
    - `SelectPantheonBeliefs()`
    - `SelectReligionBeliefs()`
    - `ConfirmPantheonBeliefs()`
    - `ConfirmReligionBeliefs()`
    - `ViewReligion(religionType)`
    - `ViewAllReligions()`
  - XML top-level sections are:
    - `WorkingTowards`: progress/instructions before founding
    - `SelectBeliefs`: choose beliefs for a pantheon or first-time religion founding
    - `AddBeliefs`: add beliefs to an existing founded religion
    - `ChooseReligion`: icon-grid religion picker plus optional custom-name edit box
    - `ViewReligion`: one religion detail page with beliefs, pantheon, unit icons, and city table
    - `ViewAllReligions`: scrollable multi-card summary of every founded religion
  - Tab strip behavior:
    - Tabs are rebuilt every open from live religion state.
    - First tab is always the local player's current religion/pantheon state (`My Religion` or `My Pantheon`).
    - One tab is added for each founded non-pantheon religion other than the player's own.
    - Final tab is `All Religions` only when at least one religion has been founded.
    - When tabs do not fit, middle religion tabs collapse to icon-only buttons and expose the religion name by tooltip instead of visible text.
  - `ViewReligion(...)` is the main sighted-information page. It shows:
    - Religion identity block: large icon, religion name, founder civ, holy city, dominant-city count
    - Unit icon strip: all religious-strength units the local player can own/build in the current ruleset, including scenario-added units; the count badge shows how many are owned, and alpha dimming indicates none owned
    - Pantheon summary block: pantheon icon plus full belief description
    - Beliefs list: unlocked beliefs first, then gray `Locked belief` placeholders up to `NUM_MAX_BELIEFS` (4)
    - Cities table: city name, follower counts for each founded religion, and the active pantheon belief description for that city
    - City filter pull-down with two modes: cities following the selected religion, or cities where the religion is merely present
  - `ViewAllReligions()` is not interactive beyond scrolling; it renders one card per founded religion with religion icon, founder, dominant-city count, and a compact list of equipped beliefs.
  - `ChooseReligion()` uses a grid of clickable religion icons (`ReligionOption` instances). Selecting one updates the big preview icon/title on the left and may enable a custom-name `EditBox` when `RequiresCustomName` and `CAPABILITY_RENAME` are both true.
  - Belief-picking behavior:
    - Available beliefs are displayed as large button rows with icon, uppercase name, and description.
    - Clicking a belief disables its source row and adds it to the selected-beliefs list.
    - When the player has no equipped religion beliefs yet, vanilla forces the first pick to be a follower belief before broader religion beliefs become available.
    - Confirm/reselect buttons swap in only after the required number of beliefs is selected.
    - Pantheon founding is single-belief-only in both the standalone chooser and the religion-screen pantheon state.
  - Input/lifecycle:
    - Escape closes the popup.
    - The screen is a low-priority popup queued at the current parent, not a full hard modal over everything.
    - `Events.BeliefAdded`, `PantheonFounded`, and `ReligionFounded` refresh the screen, except during the short post-confirm blocking-state window guarded by `m_isConfirmedBeliefs`.
  - Scenario exceptions:
    - `DLC/PolandScenario/UI/ReligionScreen.lua` and `DLC/VikingsScenario/UI/ReligionScreen.lua` each ship their own full `ReligionScreen.lua` copy, including pantheon-selection states (`SelectPantheonBeliefs`, `ConfirmPantheonBeliefs`) and `FOUND_PANTHEON` handling inside the full-screen religion UI rather than the standalone chooser.
    - `DLC/Indonesia_KhmerScenario/UI/Replacements/ReligionScreen_Indonesia_KhmerScenario.lua` is only a thin wrapper around base `ReligionScreen` that overrides `AddLockedBeliefs`; it does not replace the pantheon-founding interaction model.
  - Accessibility-shape implication: this is fundamentally a tabbed multi-state information-and-picker screen, not one flat list. CAI should preserve the state model and expose explicit widgets for tab strip, current page body, religion icon grid, belief lists, and the per-city follower matrix rather than flattening everything into one monolithic reader.
  - CAI's current `GetSetupState()` deliberately exposes only founding a religion after a pantheon exists and adding beliefs to an existing religion. It never returns the legacy in-screen `PANTHEON` state, because mainline pantheon selection belongs to the standalone `PantheonChooser`. Religion Screen tutorials must therefore begin with religion founding or enhancement and must not instruct players to select a pantheon there.

- `GovernorPanel.lua` / `GovernorDetailsPanel.lua` / `GovernorAssignmentChooser.lua` (decompiled/DLC/Expansion1/UI/Additions/ and decompiled/DLC/Expansion2/UI/Additions/):
  - Governors are not part of `GovernmentScreen`. They are a separate popup family opened from `LaunchBar.OnOpenGovernors()` through `LuaEvents.GovernorPanel_Open()` / `_Close()`, tracked by `LaunchBar`'s `isGovernorPanelOpen` flag, and treated as one of the mutually exclusive major popups.
  - There is no base-game governor screen context in `Assets/UI/InGame.xml`; governors are expansion-era content. The screen family consists of:
    - `GovernorPanel`: the main horizontally scrolling governor roster.
    - `GovernorDetailsPanel`: an inline child context declared inside `GovernorPanel.xml` (`LuaContext ID="DetailsPanel"`), used for the promotion tree / biography view.
    - `GovernorAssignmentChooser`: a separate popup used when assigning or reassigning a governor to a city.
  - Main roster layout (`GovernorPanel.xml`):
    - Full-screen tiled governor background with top header `LOC_GOVERNORS_TITLE`.
    - Two summary counters near the top: available governor titles and spent governor titles.
    - One horizontal `ScrollPanel` of tall governor columns (`GovernorInstanceStack`), each about `234x680`.
    - Each standard governor column shows portrait, name plaque, title, turns-to-establish stat, loyalty / identity-pressure stat, a short status block, earned promotions list, available promotions list, and footer buttons for `Details` plus `Assign` or `Appoint`.
    - Assigned governors swap to the "column on" art; unassigned governors stay on the darker "column off" art. Neutralized governors show a large neutralized overlay plus a dimmed fade layer.
  - Secret-society pseudo-governors are integrated into the same roster ahead of normal governors. The code identifies them with `IsCannotAssign(governorDef)` and uses the alternate `SocietyGovernorInstance` art (`Secret_Column_BackgroundTile`, `Secret_NamePlaque`, `SocietyIcon`). They never expose assignment, only details / appoint / promote.
  - Main roster interaction:
    - `Appoint` triggers `PlayerOperations.APPOINT_GOVERNOR`.
    - `Assign` opens `GovernorAssignmentChooser` through `LuaEvents.GovernorAssignmentChooser_RequestAssignment(...)`.
    - `Details` opens `GovernorDetailsPanel` through `LuaEvents.GovernorDetailsPanel_OpenDetails(...)`.
    - The screen auto-opens or refreshes on governor-related notifications (`NOTIFICATION_GOVERNOR_APPOINTMENT_AVAILABLE`, `_OPPORTUNITY_AVAILABLE`, `_PROMOTION_AVAILABLE`, `_IDLE`, and Secret Society level-up).
    - Closing with Escape / close button calls `PlayerRequestClose()`, which marks `localPlayer:GetGovernors():SetTitleConsidered(true)` if the player had not yet considered the title prompt.
  - Details view layout (`GovernorDetailsPanel.xml`):
    - Left profile card: selected portrait, name/title plaque, scrollable biography text, establish-turn / identity-pressure stats, governor status lines, and an assign / reassign / appoint button for normal governors.
    - The biography region is a plain `ScrollPanel` containing `GovernorBioLabel`; `GovernorDetailsPanel.Refresh()` fills it directly from `Locale.Lookup(pGovernorDef.Description)`, so CAI can mirror biography text from the governor definition in row tooltips instead of scraping promotion controls.
    - Right promotion area (`PromotionAnchor`): a large promotion panel with either a 3-column tree for standard governors or a vertical list for secret societies.
    - Bottom button row: `Back` when nothing is queued, `Confirm` when a promotion is currently selected.
  - Standard promotion-tree behavior:
    - Base ability is shown separately at the top.
    - Non-base promotions are positioned by `GovernorPromotions.Column` and `Level`, producing a fixed 3x3 visual tree with prerequisite connector art (`ReqLinesLeft`, `ReqLinesRight`, `ReqLinesDown`).
    - Clicking an earnable promotion only selects it first (`m_SelectedPromotion`); confirming the choice is a second explicit step via the `Confirm` button or Enter key.
    - Already-owned promotions are disabled and shown in a completed visual state; locked promotions are disabled; available promotions stay clickable unless the screen is read-only.
    - Vanilla uses `playerGovernors:CanEarnPromotion(governorHash, promotionHash)` as the individual promotion clickability test. The UI does not separately expose "missing prerequisite" versus "cannot afford a governor title right now"; both collapse into the disabled / locked branch when `CanEarnPromotion` is false.
  - Secret-society promotion behavior:
    - Uses a simple vertical list rather than the 3x3 branch tree.
    - Hidden promotions use `GetPromotionHiddenName()` / `GetPromotionHiddenDescription()` until prerequisites are met.
    - Hidden future promotions are marked with `FireFX_SecretSocietySmoke`.
  - Assignment chooser layout and flow (`GovernorAssignmentChooser.xml` / `.lua`):
    - A left slide-out panel (`AssignmentChooserSlideAnim`) opens over the world.
    - Top panel shows the selected city, the incoming governor summary, and when relevant the outgoing governor summary.
    - Bottom panel is a scrollable list of city rows. Each row mirrors a city-banner card, showing city name, capital marker, current / projected identity pressure, governor icon if one is already present, and establish turns.
    - On open, the chooser activates the Loyalty lens and usually moves the camera to the governor's currently assigned city or to a newly selected city. Closing restores the default lens unless the player is already in `VIEW_MODAL_LENS`.
    - Selecting a city updates the top panel preview only; actual assignment requires pressing `Confirm`.
    - Confirm sends `PlayerOperations.ASSIGN_GOVERNOR`. If the target city already has one of the local player's governors, vanilla shows a yes/no replacement confirmation first.
  - XP1 versus XP2:
    - XP1 and XP2 both ship the full governor panel family under their own DLC folders.
    - The biggest XP2-only behavior difference is read-only gating during World Congress. `GovernorPanel_Expansion2.IsReadOnly()` returns `Game.GetWorldCongress():IsInSession()`, and the XP2 details panel receives that state so appoint / assign / promote affordances disable cleanly during congress.
    - Secret Society support is already present throughout these later governor-panel files (roster art, icon, hidden-promotion smoke, notification hash), so CAI should treat that as part of the modern governor experience whenever the associated content is active.
  - Accessibility-shape implication: this is a three-stage flow, not one flat screen:
    - Stage 1: governor roster across columns.
    - Stage 2: per-governor promotion / biography details.
    - Stage 3: city assignment chooser with world-camera / loyalty-lens context.
    - A good CAI integration should preserve those stages as separate push/pop surfaces, keep normal versus secret-society governors distinct, expose promotion trees structurally, and speak assignment consequences before confirmation instead of flattening everything into one long list.
  - CAI's Governor roster defaults to a sortable `DataTable`; Alt+2 selects the retained detailed tree and Alt+1 returns to the table. `UI/GovernorPanelViewMode` remembers `table` or `tree`, with absent or invalid values falling back to table. Both views use governor definition indexes as stable row keys and share one sort. The tree-only `Order governors by` dropdown exposes natural order plus both directions for Name, semantic Status, establishment speed, identity pressure, and earned-promotion count. Natural order preserves vanilla's Secret Society appointed/candidate groups before ordinary appointed/candidate groups. Status sorting uses the displayed state bands Established, appointed Secret Society, establishing, needs assignment, neutralized, available for appointment, and unavailable; remaining establishment or neutralization turns refine the applicable bands. Establishment speed reads `PlayerGovernors:GetTurnsToEstablish(governorHash)`, identity pressure reads `GameInfo.Governors.IdentityPressure`, and unassignable Secret Society governors return nil for both so the shared `DataTable`/tree comparators leave them last in either direction. The two stat headers scan the captured vanilla rows for `TransitionStrengthLabel` and `IdentityPressureLabel` tooltips—the same sources used by tree-row speech—and fall back to `LOC_GOVERNOR_TRANSITION_STRENGTH_TOOLTIP` and `LOC_GOVERNOR_IDENTITY_PRESSURE_TOOLTIP`. Earned-promotion counts exclude the automatic base ability; the cell starts with the numeric count and then names and describes every owned non-base promotion. Only the Name cell exposes the governor title, biography, and base ability as its tooltip, preventing repeated tooltip speech while moving across the other columns. The active table header and tree dropdown use the same semantic descriptions: A-to-Z/Z-to-A, active/unavailable first, fastest/slowest first, lowest/highest first, or fewest/most first. Focusing either roster view continues to rebuild the existing promotion grid/list, and row activation preserves vanilla appoint or assign/reassign behavior.
  - `GovernorAssignmentChooser_CAI.lua` now uses a Pantheon-style CAI overlay for stage 3:
    - the CAI chooser itself is only a `List` of city rows, not a mirrored preview panel
    - row labels speak just city name plus capital marker
    - row tooltips read the live vanilla row controls for current governor, identity-pressure before/after, establish turns, and disabled reasons
    - activating a CAI row drives the live vanilla row button's `DoLeftClick()` instead of calling `OnSelectCity(...)` directly, so vanilla keeps ownership of camera movement, loyalty-lens activation, and preview rebuild
    - the CAI confirmation dialog is populated from the live top preview controls after vanilla selection updates them
    - when the preview's current-governor slot is the vanilla no-governor placeholder, `AddGovernorInstance(..., nil, nil, ..., true)` hides `IdentityPressureContainer` and `TurnsToEstablishIcon`; CAI must honor those hidden states so stale identity-pressure / establish-turn tooltips are not spoken after `LOC_GOVERNOR_ASSIGNMENT_NO_GOVERNOR`
    - confirmation drives the live vanilla `ConfirmButton:DoLeftClick()`; dialog-only reselect/escape is CAI-local because vanilla has no equivalent sub-step control

## Interface Modes

- `InterfaceModeTypes.MOVE_TO` — unit movement mode
- `UI.GetInterfaceMode()` — returns current interface mode
- `UI.SetInterfaceMode(mode)` — changes interface mode

## Unit Promotion Popup

- Vanilla registers `UnitPromotionPopup` as a `WorldPopups` context in base, XP1, and XP2 `InGame.xml`, all pointing to `FileName="UnitPromotionPopup"`. No XP1/XP2 `UnitPromotionPopup.lua/.xml` replacement exists in the decompiled UI mirror; the expansions only replace/wrap `UnitPanel`.
- Opening is driven by the unit panel's promote action. `UnitPanel.GetUnitActionsTable(...)` adds the promote action only when `UnitManager.CanStartCommand(pUnit, UnitCommandTypes.PROMOTE, true, true)` returns a non-empty `UnitCommandResults.PROMOTIONS` list.
- `UnitPanel.ShowPromotionsList(tPromotions)` has two paths:
  - if `GameInfo.Units[unitType].NumRandomChoices > 0`, vanilla shows the compact in-panel `Controls.PromotionPanel` list, with one button per available random promotion choice;
  - otherwise vanilla raises `LuaEvents.UnitPanel_PromoteUnit()`, which opens the standalone `UnitPromotionPopup`.
  - `UnitPromotionPopup.lua` rebuilds a full promotion tree from `GameInfo.UnitPromotions()` filtered by the selected unit's `GameInfo.Units[pUnit:GetUnitType()].PromotionClass`, plus prerequisite rows from `GameInfo.UnitPromotionPrereqs()`. It overlays earned/current promotions, available promotions returned by `CanStartCommand`, and locked future promotions in one tree.
  - Promotion identities are mixed by design: prerequisite relationships use `UnitPromotionType` strings, while `UnitCommandResults.PROMOTIONS`, `UnitExperience:GetPromotions()`, and `UnitCommandTypes.PARAM_PROMOTION_TYPE` use numeric `GameInfo.UnitPromotions` indexes. When CAI builds a local model, relationship sorting must use that local model, not the global/current `m_model`, because `m_model` is not assigned until after `BuildPromotionModel()` returns.
  - `UnitPromotionPrereqs` rows are individual unlock edges, not an all-prerequisites list. Some branches intentionally have reciprocal same-tier rows, such as Heavy Cavalry `Marauding <-> Rout`, so CAI should speak these as `Unlocked by` / `Unlocks` rather than `Prerequisites` / `Leads to`. Keep relationship lists comma-separated for concise scan/readback.
  - `UnitPanel.View(data)` displays already-earned promotions as small `EarnedPromotionInstance` icons in `Controls.EarnedPromotionsStack`. Each icon receives a tooltip containing the promotion name plus `Locale.Lookup(promotion.Desc)` from `data.CurrentPromotions`, so sighted players can inspect earned promotion descriptions from the unit panel even when the full promotion popup is not available.
  - Standalone popup layout (`UnitPromotionPopup.xml`): darkened full-screen backdrop; centered framed panel with close button; header `LOC_HUD_UNIT_CHOOSE_PROMOTION_TEXT`; top-left combat/movement stat icons assembled from live unit values; top-right XP label; scrollable promotion tree. Each node is a 212x106 promotion card containing icon, uppercase promotion name, and truncated description. Completed promotions use `Promotion_ButtonCompleted`; unearned cards use `Promotion_Button`; available cards are enabled, locked/future cards are disabled. Prerequisite connector lines are solid blue when both endpoint promotions are owned, dashed otherwise.
- Standalone interaction: click an enabled promotion card to immediately call `UnitManager.RequestCommand(pUnit, UnitCommandTypes.PROMOTE, { [UnitCommandTypes.PARAM_PROMOTION_TYPE] = ePromotion })`; there is no confirmation step. Escape or the close button closes the popup. It also closes on city selection change, hotseat turn end, and `LuaEvents.UnitPanel_HideUnitPromotion()`.
- Compact random-choice promotion panel layout (`UnitPanel.xml`): a 300x287 HUD frame anchored above the unit panel action area, with header `LOC_HUD_UNIT_CHOOSE_PROMOTION_TEXT`, vertical scroll list, and small close button. Each row is a rectangular button with promotion tier roman numeral, name, and description. Clicking a row immediately requests the same `UnitCommandTypes.PROMOTE` command.
- The compact random-choice promotion panel is not a tree: it only lists the available promotion ids returned by `UnitManager.CanStartCommand(... PROMOTE ...)`, does not build `UnitPromotionPrereqs`, and has no prerequisite / unlock connector lines. Vanilla row visuals are a gray 32x32 `PromotionsSmall` badge area with a roman-numeral tier label (`I` through `V`) over it, followed by localized promotion name and localized description text.
- `UnitPanel_CAI.lua` wraps `ShowPromotionsList(tPromotions)` and calls vanilla first. If vanilla actually shows `Controls.PromotionPanel` for a `NumRandomChoices > 0` unit, CAI pushes a transient list mirrored from the live vanilla `Controls.PromotionList` rows. Each CAI row reads `PromotionTier:GetText()` plus `PromotionName:GetText()` as the label, reads `PromotionDescription:GetText()` as the tooltip, and activates with `PromotionSlot:DoLeftClick()` so the vanilla registered left-click callback remains the command path. `HidePromotionPanel()` and unit selection changes remove the CAI list.
- The veteran rename panel is also UnitPanel-owned. `ShowNameUnitPanel()` shows `Controls.VeteranNamePanel` and calls `RandomizeName()`. `RandomizeName()` writes the generated localized name into `Controls.VeteranNameField` and stores the command payload in `m_FullVeteranName`. Manual edits must call `OnEditCustomVeteranName()` after writing `VeteranNameField`, because `OnConfirmVeteranName()` reads `m_FullVeteranName` before requesting `UnitCommandTypes.NAME_UNIT`. `UnitPanel_CAI.lua` mirrors this panel with a transient CAI panel: edit box text is bound to `VeteranNameField` through `SetValueSetter`, Enter on the `AlwaysEdit` edit box bubbles to a panel binding that clicks `ConfirmVeteranName`, the `OnConfirmVeteranName` wrapper commits the CAI edit before vanilla reads `m_FullVeteranName`, randomize clicks `RandomNameButton:DoLeftClick()` and resyncs from the vanilla field, Escape clicks `VeteranNamingCancelButton`, and `HideNameUnitPanel()` is the only close/remove path for the CAI panel.
- Confirmed standard-rules trigger: Gathering Storm Rock Bands (`UNIT_ROCK_BAND`) define `InitialLevel="2"`, `NumRandomChoices="3"`, and `PromotionClass="PROMOTION_CLASS_ROCK_BAND"` in `DLC/Expansion2/Data/Expansion2_Units.xml`. Buying a Rock Band and opening its initial promotion action should show the compact in-panel list instead of the standalone tree.
- XP1 `UnitPanel_Expansion1.lua` is only `include("UnitPanel")`. XP2 `UnitPanel_Expansion2.lua` includes XP1 and adds Rock Band / adjacent-build / railroad-upgrade behavior, but does not alter the promotion popup or promote command flow.

## Espionage UI

- Vanilla registers four main espionage UI contexts from `InGame.xml`: `EspionageOverview`, `EspionageChooser`, `EspionagePopup`, and `EspionageEscape`. Shared behavior lives in `EspionageSupport.lua`; `EspionageViewManager.lua` only stores the current city being viewed for espionage-side-panel purposes.
- DLC replacements found in XP1 and XP2 are only `EspionageChooser_Expansion1.lua` and `EspionageOverview_Expansion1.lua`; both are also shipped under `DLC/Expansion2/UI/Replacements/` with the same XP1 filename. No DLC replacement was found for `EspionagePopup.lua/.xml`, `EspionageEscape.lua/.xml`, `EspionageSupport.lua`, or the XML layouts for chooser/overview.
- `EspionageChooser` is a left slide-out chooser with two modes:
  - `SPY_TRAVEL_TO_CITY`: destination picker. Rows are city-banner cards with city name/capital marker, total travel plus establish time, district icons, and district-scroll arrows when the district strip is longer than the visible slots. Selecting a row moves the camera to the city, shows a top selected-city banner, shows a non-clickable possible-missions preview, and exposes `Confirm Placement` / `Cancel`. Confirm sends `UnitOperationTypes.SPY_TRAVEL_NEW_CITY` with `PARAM_X/PARAM_Y`, unless the selected city is the local city the spy already occupies, in which case vanilla switches to mission-chooser mode for counterspy selection.
  - Same-current-city destination rows still display `UnitManager.GetTravelTime(m_spy, city)` / `GetEstablishInCityTime(...)` in vanilla `AddDestination` and `UpdateCityBanner`. The current-city special case is not applied until `OnConfirmPlacement()`, so the row can misleadingly say travel time even though confirming it opens counterspy mission selection instead of moving the spy.
  - `SPY_CHOOSE_MISSION`: mission picker for the city where the spy currently sits. Enemy-city offensive missions are rows with mission icon, localized operation name, operation detail text, target district, turn count, and success chance. Disabled offensive missions are still listed when `CanStartOperation(..., returnResults=true)` provides failure reasons, with those reasons shown in red. Local-player cities list counterspy target rows by district and activate immediately with `SPY_COUNTERSPY`.
- In base rules, `EspionageChooser.RefreshDestinationList()` adds revealed local-player cities plus revealed non-minor player cities. XP1/XP2 override that function to skip Free Cities explicitly while preserving the same broad destination shape. `EspionageOverview_Expansion1.RefreshCityActivity()` calls the base city-activity refresh and then appends city-state cities.
- `EspionagePopup` is a centered parchment popup. In mission briefing state it shows mission title/icon, objective, duration, possible outcome percentages, and bottom `Accept Mission` / `Cancel` buttons. Accept calls `UnitManager.RequestOperation(m_spy, m_operation.Hash)`; the operation target is already established by the chooser selection path. In abort state it shows current mission details, turns remaining, `Abort Mission` / `Cancel`, and abort calls `UnitManager.RequestCommand(m_spy, UnitCommandTypes.CANCEL)`. Listening Post and Counterspy aborts skip the popup and cancel immediately. In mission-completed state it shows success/failure outcome, rewards such as promotion-ready and loot, consequences such as relationship damage or killed/captured agent, and may show `Renew Mission` for Listening Post or Counterspy if the mission can still be started at the target.
- Vanilla `EspionagePopup.Open()` calls `UIManager:QueuePopup(ContextPtr, ...)` before `Refresh()`. Showing the context can therefore run a replacement's `ContextPtr:SetShowHandler(...)` while `MissionTitle` still contains the XML placeholder `$Mission Title$`; only the following `Refresh()` writes the localized title and rows. A CAI replacement must use one post-refresh dialog build path. Building from the show handler, a wrapped `Refresh()`, and a wrapped `Open()` produces three consecutive pushes; if each rebuild first calls `RemoveFromStack`, the user hears two close/reopen cycles, with the placeholder on the first opening and duplicate populated content on the next two.
- AdvisorPopup queues at `PopupPriority.Tutorial`, above EspionagePopup's `PopupPriority.Low`; `UIManager:QueuePopup()` can therefore leave the espionage context hidden while `Open()` continues through `Refresh()`. After `Refresh()` has populated the controls, CAI builds and pushes its dialog at Low priority even while the native context is obscured. The CAI manager retains that lower-priority root on its own stack and naturally promotes it when the queued Tutorial dialogs leave. Keeping `Open()` as the single post-refresh build point still avoids the former placeholder and duplicate pushes.
- `EspionagePopup.xml` control inventory:
  - Primary live text/detail controls: `MissionTitle`, `MissionObjectiveLabel`, `MissionDurationLabel`, `MissionOutcomeLabel`, `MissionOutcomeDescription`, `SpyPromotionLabel`, `SpyPromotionDescription`, `SpyLootRewardLabel`, `SpyLootRewardDescription`, `RelationshipDamageTitle`, `RelationshipDamageDescription`, `LostAgentTitle`, `LostAgentDescription`, `RenewableMissionDetails`, `MissionDistrictName`, `TurnsToCompleteLabel`, `ProbabilityLabel`.
  - Visibility containers: `MissionObjectiveContainer`, `MissionOutcomeContainer`, `MissionRewardsContainer`, `SpyLootGrid`, `MissionConsequencesContainer`, `LostAgentGrid`, `MissionDurationContainer`, `PossibleOutcomesContainer`, `RenewableMissionContainer`, `ProbabilityGrid`.
  - Instance-driven possible-outcome rows are created under `OutcomeStack` from `OutcomeLabelInstance` (`OutcomeLabel`) and `OutcomePercentInstance` (`Top`, `OutcomePercentNumber`, `OutcomePercentLabel`).
  - User-action controls: `AcceptButton`, `RenewButton`, `AbortButton`, `CancelButton`, `MissionSucceedButton`, `MissionFailureButton`. There is no close button; Escape calls `Close()`.
- `EspionageSupport.RefreshMissionStats(...)` is the shared source for displayed mission stats: `UnitManager.GetTimeToComplete(...)`, `UnitManager.GetResultProbability(...)` summed across `ESPIONAGE_SUCCESS_UNDETECTED` and `ESPIONAGE_SUCCESS_MUST_ESCAPE`, color-coded probability thresholds, and target district resolution from operation result plots or city center fallback.
- `EspionageChooser_CAI.lua` must not pair recomputed CAI missions with `Controls.MissionStack:GetChildren()[i]`. Vanilla mission rows are created through `AddCounterspyOperation`, `AddAvailableOffensiveOperation`, and `AddDisabledOffensiveOperation`; CAI captures rows from those wrappers and stores the live mission button. Activation should call the captured button's `DoLeftClick()` so vanilla's registered counterspy/offensive/disabled callbacks remain the source of truth.
- `EspionageChooser_CAI.lua` should treat `Refresh()` as the mode synchronization choke point. After vanilla refreshes, read live vanilla visibility (`MissionPanel` visible means mission mode; `DestinationPanel` visible means destination mode, with interface mode only as fallback), rebuild CAI widgets, and move focus only when that derived mode changes. Do not special-case `OnConfirmPlacement()` for same-city spy assignment; vanilla already switches from destination mode to mission mode inside that flow.
- Keep destination and mission navigation as separate CAI widgets. The destination view is a `Tree` of city rows with read-only possible-mission children. The mission view is a sibling `List` of actionable mission rows. Their hidden predicates are driven by the derived vanilla mode, so refreshes and edge-case mode switches cannot leave mission rows mixed into the destination tree. The parent panel label must also be derived from the same mode state, so the screen context changes from destination chooser text to mission chooser text when focus moves across a vanilla mode switch.
- The implementation-audited tutorial must describe those modes separately rather than claiming the destination tree and mission list are consecutive controls. Destination rows expose city/civilization identity, capital state, travel plus establishment timing, mission count, and completed districts; expanding a row exposes read-only computed mission previews. Activating the city opens CAI's Confirm Spy Placement dialog, whose Confirm uses vanilla placement and whose Reselect/Escape returns to the tree. Confirming the spy's current local city changes to mission mode for Counterspy selection.
- In mission mode, the panel reports live Gain Sources state and exposes only the mission `List`. Captured enabled foreign mission buttons open the separate vanilla `EspionagePopup` mission-confirmation dialog in briefing state, with Accept Mission and Cancel, while disabled rows remain readable with failure reasons. Captured Counterspy buttons identify their target district and submit the counterspy operation immediately without opening that confirmation popup. Tutorial text must preserve this different activation behavior.
- `EspionageOverview` is a right-side partial screen opened through `PartialScreenHooks_OpenEspionage`. It has three tabs:
  - Operatives: local spies grouped visually as idle/awaiting assignment, active missions with progress bar/turns remaining/details/target district, captured spies with capturing civilization and `Ask for Trade`, and off-map travelling spies with travel progress/destination.
  - City Activity: revealed cities with a clickable city banner, complete non-wonder district icons, source-boost icon when active, and an overlaid spy marker on districts where one of the local player's spies is active. Clicking a city banner looks at the city. XP1/XP2 append city-states here.
  - Mission History: captured enemy operatives that can be traded for, plus up to 10 recent missions with spy name/rank, mission name, turns since completion, success/failure status, killed/captured status, outcome description, mission icon, and target district icon. Trade buttons create a locked `DealItemTypes.CAPTIVE` and request a diplomacy deal session.
- `EspionageEscape` is a small modal opened from notification activation through `LuaEvents.NotificationPanel_OpenEspionageEscape(...)`. It shows city, agent, loot, pursuer, and four escape-route buttons. Buttons 1-3 are disabled if the target district is not present in the city; each enabled button shows turn count and submits `PlayerOperations.SET_ESCAPE_ROUTE` with `PARAM_DISTRICT_TYPE`. Escape closes without choosing.
- `EspionageEscape.lua` registers `LuaEvents.NotificationPanel_OpenEspionageEscape.Add(OnOpen)` inside `Initialize()`, so a CAI replacement that wraps `OnOpen` after `include("EspionageEscape")` must remove the original listener and add the wrapped function. The route button callbacks can remain vanilla; CAI activation should use `ButtonN:DoLeftClick()`.
- `EspionageEscape.xml` control inventory:
  - Detail controls: `PanelHeader`, `CityHeader`, `AgentLabel`, `AgentDetails`, `LootLabel`, `LootDetails`, `PursuitLabel`, `PursuitDetails`, `ChoiceHeader`.
  - Route controls: `Button1`/`Label1`, `Button2`/`Label2`, `Button3`/`Label3`, `Button4`/`Label4`.
  - Lifecycle/animation controls: `PopupGenericBlocker`, `PopupAlphaIn`, `PopupSlideIn`, `PopupDialog`, `EscapeWindow`.
- Accessibility-shape implication: treat espionage as four surfaces, not one monolith. The chooser should be the highest-priority gameplay target because it blocks spy orders. Mirror destination rows, mission rows, and mission briefing with CAI widgets that activate live vanilla controls/functions. The overview can be a tabbed read/action panel. The completion/abort popup can be a dialog-style panel with read-only detail sections plus live buttons. The escape popup can be a small dialog with one row per route, disabled states and reasons preserved.

## City Overview Panel

- Vanilla city details are split across two UI contexts:
  - `CityPanel.lua/.xml` is the compact selected-city HUD panel anchored at the bottom right. It shows city name, civ icon/health rings, buildings-or-loyalty summary, religion followers, amenities, housing, growth meter, production card/meter, per-turn yield focus checkboxes, next/previous city buttons, and checkboxes for overview, manage citizens, purchase tile, change production, gold purchase, and faith purchase.
  - `CityPanelOverview.lua/.xml` is the left slide-out "City Details" panel. `CityPanel.Initialize()` raises `LuaEvents.CityPanel_OpenOverview()` so the context is loaded at startup, and `CityPanelOverview` stays hidden until opened.
- Base overview visuals:
  - parchment/blue left panel with city-name header, close button, rename button/edit box for owned cities, a vertical tab/icon rail, and a scrollable content area.
  - Base tabs are Citizens/Health, Buildings, and Religion when `CAPABILITY_CITY_HUD_RELIGION_TAB` is available.
  - Citizens/Health tab shows three stacked subpanels: amenities mood and effects, housing status and source breakdown, and citizen growth math.
  - Buildings tab shows district count, built districts with nested built buildings, wonder list or no-wonders message, and trading-post list or no-trading-posts message. District/building/wonder rows have tooltips from `ToolTipHelper`.
  - Religion tab shows pantheon belief, dominant religion follower count, dominant-religion beliefs with descriptions, other religions in the city, and a color-key list for founded religions. Selecting the tab activates the Religion lens.
  - Production details exist in the same base overview file: `ViewPanelProductionNow` reads current production, unit stats, and description; `ViewPanelQueue` builds numbered queue entries. These panels are hidden in `HideAll()` but are still part of the context.
- Base overview interaction:
  - `ToggleOverviewTab(tabButton)` opens the panel if needed, selects the tab if different, or closes the panel if the same tab is selected again for the same city.
  - `CityPanelOverview` listens to `LuaEvents.CityPanel_ToggleOverviewCitizens`, `CityPanel_ToggleOverviewBuildings`, and `CityPanel_ToggleOverviewReligion`.
  - Close button, Escape, or right-click closes the overview, resets the active lens to Default, and sets interface mode to `SELECTION`.
  - `RenameCity()` sends `CityManager.RequestCommand(city, CityCommandTypes.NAME_CITY, params)`. The city object does not expose the submitted name synchronously; the authoritative completion signal is `Events.CityNameChanged(playerID, cityID)`. An accessible rename editor must remain focused after submitting a changed nonblank name and close only when that event matches the pending owner/city, so focus restoration reads the updated live name rather than the pre-command name. Blank or unchanged submissions require no command and may close immediately.
  - Base tab selection sets `CityDetails` lens for local cities and `EnemyCityDetails` plus `LuaEvents.ShowEnemyCityDetails(owner, cityId)` for enemy/espionage city views. Religion tab sets `Religion`.
  - The compact `CityPanel` summary stat buttons call older event names (`CityPanel_ShowBreakdownTab`, `CityPanel_ShowReligionTab`, `CityPanel_ShowAmenitiesTab`, `CityPanel_ShowHousingTab`, `CityPanel_ShowCitizensTab`), while the overview file registers the `CityPanel_ToggleOverview*` events. Existing CAI code already uses the registered `CityPanel_ToggleOverview*` family for direct tab hotkeys.
- Rise and Fall / XP1:
  - `CityPanel_Expansion1.lua` includes base `CityPanel` and changes the compact first stat row from Buildings to Loyalty, using `city:GetCulturalIdentity():GetLoyalty()`. It also deselects a selected city when `Events.CityLoyaltyChanged` fires for that city.
  - `CityPanelOverview_Expansion1.lua` includes base overview, overrides growth math for the XP1 rule path, adds governor amenity loss to the amenities breakdown, loads `CityPanelCulture` into `PanelDynamicTab`, and adds a Loyalty/Governor dynamic tab with icon `ICON_STAT_GOVERNOR`.
  - Selecting the XP1 dynamic tab shows `CityPanelCulture`, activates the Loyalty lens, and raises `LuaEvents.CityPanelTabRefresh()`.
  - `CityPanelCulture.lua/.xml` shows loyalty current/max/status, loyalty pressure tooltip/icon, potential transfer owner, detailed identity-source breakdown, total loyalty-per-turn line, loyalty effects, advisor text, diplomatic influence by civilization, and assigned governor identity/effects/establishment state. It listens to `CityPanelTabRefresh`, governor events, city selection, city loyalty changes, and enemy-city overview.
- Gathering Storm / XP2:
  - `CityPanel_Expansion2.lua` includes XP1 and overrides `DisplayGrowthTile()` so World Congress resolutions can suppress culture border growth display.
  - `CityPanelOverview_Expansion2.lua` includes XP1 overview and, when `CAPABILITY_LENS_POWER` exists, loads `CityPanelPower` into `PanelDynamicTab`, adds a Power dynamic tab with icon `ICON_STAT_POWER`, listens to `LuaEvents.CityPanel_ToggleOverviewPower`, activates the Power lens, and raises `LuaEvents.CityPanelTabRefresh()`.
  - `CityPanelPower.lua/.xml` shows consumed/current power, required power, power status and description, consumed/required/generated breakdown lines from `city:GetPower()`, advisor text, and a power-lens key. It listens to `CityPanelTabRefresh`, city selection, and enemy-city overview.
- Accessibility-shape implication:
  - Treat CityPanel compact actions and CityPanelOverview details as separate but linked surfaces. The compact city panel should continue to expose fast summary buckets and action hotkeys; the overview should get its own new-framework CAI mirror that follows vanilla open/close/tab/lens behavior.
  - CAI's combined city-management action belongs on the compact city panel as a single input action (`CityManageCity`) that toggles the same combined citizen-management / tile-purchase flow the city action list exposes. Do not reintroduce separate `CityPurchaseTile` or `CityManageCitizens` hotkey entries for that surface.
  - Best first mirror is a `Panel` containing a `TabControl` with pages for Citizens, Buildings, Religion, XP1 Loyalty/Governor, and XP2 Power. Each page should be rebuilt from live vanilla city data and/or the same API calls used by the vanilla panel, with `FocusKey` rows and manager capture/restore.
  - Buildings should be a `Tree`: districts as expandable nodes, buildings as children, plus separate Wonder and Trading Post groups. Religion and Loyalty can also be trees/lists because most content is read-only structured rows. Power is a list grouped into status, consumed, required, generated, and lens key. Citizens should be grouped sections for amenities, housing, and growth math.
  - Activation should preserve vanilla. Tab activation should call the vanilla `LuaEvents.CityPanel_ToggleOverview*` event or live tab button path, not only swap CAI pages, so lenses and close-on-same-tab behavior remain in sync. Close should follow vanilla close/Escape rather than popping CAI only. Rename should mirror the live edit box or call the same `CityCommandTypes.NAME_CITY` path after committing user text.
  - Prefer live control text/tooltips when vanilla rows are visible, especially district/building/wonder tooltips, religion belief descriptions, loyalty/power status labels, and advisor text. Where vanilla content is built from data without stable controls, use the same APIs documented above and localized vanilla loc keys.

## Frontend picker control bindings

- MultiSelectWindow, CityStatePicker, and LeaderPicker create each sighted item from a button plus a `Selected` checkbox. Both controls register the same click callback, which updates the selection table and explicitly sets the checkbox state.
- Accessible checkbox mirrors must seed their state with `SetChecked(liveCheckbox:IsChecked(), true)` and write through `SetValueSetter`. When the desired state differs, the setter should call `liveCheckbox:DoLeftClick()` so the registered vanilla callback remains authoritative.
- Front-end buttons should likewise prefer the live control's `DoLeftClick()` path. Dropdown widgets should use `SetValueSetter` to perform their selection logic directly; do not simulate a click on the sighted pulldown.
- CityStatePicker initializes its native count slider during `ParameterInitialize`, before showing the picker. The accessible panel is built later by the context show handler, so its slider must configure the live parameter minimum, maximum, step sizes, and current value when the widget is created as well as on later parameter initialization. Follow the standard slider order: `SetMin`, `SetMax`, `SetStepSize`, `SetPageStep`, then `SetValue(current, true)`.

## Frontend Scenario Setup

- `MainMenu.OnScenarioSetup()` resets `GameConfiguration` to defaults, clears `RULESET` so setup logic can choose an eligible default, sets the load-game server type to `SERVER_TYPE_NONE`, and queues the deferred `ScenarioSetup` context. The main-menu Scenarios button is enabled only when configuration data contains an `IsScenario = 1`, `SupportsSinglePlayer = 1` ruleset.
- CAI's Main Menu submenu mirror no longer excludes the `OnScenarioSetup` entry. The accessible Single Player submenu builds its Scenarios button from the same live vanilla instance as every ordinary submenu row, inheriting `UpdateScenariosButton` visibility, the localized label/tooltip, disabled state, focus highlighting, and activation through `control.OptionButton:DoLeftClick()`.
- The shipped PC build has one base `Assets/UI/FrontEnd/ScenarioSetup.lua/.xml` pair and no DLC-specific `ScenarioSetup` Lua/XML replacement. Scenario content changes the ruleset data, supported values, players, maps, portraits, and descriptions rather than replacing this UI context.
- The screen is one full-screen shell popup with two mutually exclusive internal views. Its persistent header is Restore Defaults on the left, `Choose Scenario` in the center, and Back on the right; Start Game remains centered along the bottom. Escape/Back returns from the advanced view to the basic view first, then dequeues the popup from the basic view.
- Basic-view visual layout is a three-column composition. The left side is scenario/leader artwork, the narrow center is the setup form, and the right side repeats the scenario background behind its long description. The center form contains Scenario and Civilization pulldowns, conditionally visible Difficulty, Game Speed, Map Type, and Map Size pulldowns, any additional visible `BasicGameOptions` / `BasicMapOptions` parameters, and an Advanced Options button at the bottom.
- The Ruleset parameter is filtered to values whose configuration row has `IsScenario`. Opening its pulldown shows scenario names and descriptions. Merely moving the mouse over a scenario entry while the list is open calls `RefreshScenarioData(...)`, previewing that entry's portrait/background and long description without committing it; clicking commits through the setup-parameter object and broadcasts the game configuration. The selected ruleset's description also refreshes the right panel after normal parameter updates.
- The Civilization pulldown is the local player's `PlayerLeader` selector built by `SetupLeaderPulldown(...)`. Its rows combine leader/civilization text with leader and civilization icons. Hovering uses the shared player-info tooltip/placard path, exposing leader/civilization abilities and unique items. Scenario configuration can restrict different player slots to different leader sets, including locked scenario participants.
- Basic dynamic parameter vocabulary is boolean checkbox, text/integer edit box, integer-range slider with number display, and ordinary multi-value pulldown. Fixed pulldowns and multi-value controls are hidden when only one value exists; sliders are hidden when their range collapses; every control follows the live parameter's visible/enabled state. All user changes call `SetParameterValue(...)` and `Network.BroadcastGameConfig()`.
- Activating Advanced Options hides the basic three-column view and reveals a two-column editor. The left Players panel contains the local leader/civilization pulldown, one leader/civilization pulldown plus X remove button for each nonlocal AI slot, and Add AI when below the map's maximum major-player count. Remove is available only above the map's minimum; Add searches for a closed slot, marks it computer/major-civ, and rebuilds player parameters.
- The advanced right panel is a scrollable dynamic parameter form with visible headings Options, Victory Conditions, and Advanced Options. `GameSetupLogic.GetControlStack(...)` routes `BasicGameOptions`, `GameOptions`, `BasicMapOptions`, `MapOptions`, and (because this XML has no `GameModeParameterStack`) `GameModes` into the primary Options stack; `Victories` and `AdvancedOptions` use their named stacks. The shared driver sorts each visible stack after refresh and hides an empty stack, although the ScenarioSetup XML headings themselves have no IDs for independent hiding.
- Start is optimistically enabled after each parameter refresh, then disabled with `LOC_SETUP_PARAMETER_ERROR` when `GetGameParametersError()` reports a game error or `LOC_SETUP_PLAYER_PARAMETER_ERROR` when any participating player reports an error. Unlike `AdvancedSetup.xml`, Scenario Setup has no `ConflictPopup`; validation is surfaced through the disabled Start button tooltip and invalid leader-option presentation. Start preserves vanilla by setting the `Scenario Start` entry method and calling `Network.HostGame(SERVER_TYPE_NONE)`; World Builder uses its corresponding entry method and 2D render view.
- Restore Defaults calls `GameConfiguration.SetToDefaults()`, regenerates seeds, and rebuilds players/parameters. Parameter changes and `FinishedGameplayContentConfigure` refresh the dynamic form. Hiding releases game/player parameter drivers and clears the cached ruleset portrait/description data.
- `UI/frontEnd/ScenarioSetup.lua` is implemented as a full frontend replacement and registered in both the mod's top-level `<File>` list and the `CAIFrontEnd` `<ImportFiles>` action. Its CAI block sits after the vanilla `Initialize` definition and immediately before the final call, so wrapping `Initialize` is not overwritten by the definition. No DLC include branch is needed.
- The CAI root is a `Panel` containing a two-page `TabControl`. The basic page is a `List` ordered as Scenario, Civilization, Difficulty, Game Speed, Map Type, Map Size, then additional basic parameters. The advanced page is a `List` with Players, Options, Victory Conditions, and Advanced Options `SubMenu` groups; `GameModes` is folded into Options. Tab changes directly synchronize the native basic/advanced containers and `m_AdvancedMode`, matching CAI Advanced Setup without rebuilding either accessible page or restoring focus.
- Basic and advanced game parameters use Checkbox/Dropdown/Slider/EditBox widgets and stable `basic:param:*` / `advanced:param:*` focus keys. Scenario dropdown options append the configuration database's long description to their tooltip, providing the information-equivalent of sighted hover preview without committing or changing the vanilla portrait merely because CAI option focus moved. Map remains a dropdown. Difficulty omits its selected-value and option tooltips because they duplicate the already spoken value/label; any invalid reason remains in that spoken label.
- Leader dropdowns reuse the Advanced Setup player-info pattern: labels are `leader, civilization`; tooltips contain civilization/leader abilities and grouped unique items; invalid reasons are appended to both option labels and tooltips. Scenario Setup does not expose color controls. Inside Players, every slot is a direct civilization dropdown labeled by player ordinal; Delete removes a nonlocal AI when removal is legal, and Add AI follows the live vanilla button visibility.
- Stateful widgets seed silently and write through live setup/player parameter objects followed by `Network.BroadcastGameConfig()`. Checkbox setters prefer the live vanilla checkbox click path when available. Restore Defaults and Start delegate through their live vanilla buttons. Start exposes the live disabled tooltip plus localized game/player error details when the error object supplies them; no conflict dialog is invented.
- Actual parameter/player refreshes capture the panel focus key, rebuild both pages from live data, and restore focus. Tab switching performs no rebuild, restore, or refocus. The panel is built and pushed after vanilla `OnShow()` has rebuilt setup state and removed on `OnHide()` and invite processing. Escape/Back closes Scenario Setup from either tab; only tab activation changes the basic/advanced view.
- Disabled EditBox parameters remain readable but their inactive Up/Down and Home/End bindings bubble through the shared UI manager to the containing List. The base dispatcher uses the binding's `BubbleWhenDisabled` flag for this case; Enter and editing commands remain consumed while the field is unavailable.
- In-game test matrix: each installed single-player scenario; scenario-option focus versus commit; long-description speech; restricted/locked leaders and invalid reasons; parameters that appear/disappear after ruleset changes; fixed-value controls skipped; direct player dropdowns and Delete/Add AI at minimum/maximum limits; basic/advanced tab announcements and focus entry; Escape closing from both tabs; Restore Defaults; disabled Start reasons; successful scenario launch; and focus restoration through actual dynamic refreshes.

## Frontend Mods / Additional Content

- The Main Menu route is `Additional Content` -> `Mods`. `MainMenu.OnMods()` first calls `GameConfiguration.SetToDefaults()`, queues the deferred `ModsContext` at `PopupPriority.Current`, and closes the Main Menu. `FrontEndPopup.OnLaunchError()` can also reach the same screen through its `View Mods` action and `LuaEvents.MainMenu_ShowAdditionalContent`.
- The shipped screen is the base-game `Base/Assets/UI/FrontEnd/Mods.lua` plus `Mods.xml`; there are no DLC replacements. It is a front-end/shared full-replacement target, so CAI must copy the complete vanilla Lua file to `src/UI/frontEnd/Mods.lua`, add accessibility immediately before the final `Initialize()` call, and register it in both the top-level file list and `CAIFrontEnd` imports.
- Visually, the screen is a centered 1024-pixel-wide, dark navy translucent shell panel. At taller resolutions it displays the Civilization VI logo in a 116-pixel header block; at roughly 768-pixel height it hides the logo and gives that space to the content. A decorated title bar says `Mods` and places a `Back` button at the upper right.
- Two 300-by-41 shell tabs sit below the title: `Installed` and, on Steam builds, `Subscriptions`. The Steam tab is hidden when `Steam` is absent or its app ID is zero.
- The Installed page is split into two columns. Its narrow left column contains, from top to bottom, a mod-group pull-down with small add/delete buttons, centered `Enable All` and `Disable All` buttons, a live search edit box, a sort pull-down, `Official Content` and `Community Content` checkboxes, and a vertically scrolling mod list. Its wider right column is a blue framed detail pane with a watermark until a mod is selected.
- Each visual mod row is a 60-pixel button. It shows an official-content, community-content, or World Builder map icon; the mod title; and an enabled/disabled label. Unowned content and unacknowledged incompatible content append red warnings to the title. The row tooltip is the mod teaser.
- Selecting a visual row does not enable or disable it. It highlights the row and fills the right pane. That pane shows content type, title, GUID/ID, a map source filename when applicable, compatibility warning, description, author, special thanks, creation date, ownership for official content, whether it affects saved games, single-player support, multiplayer support, and dependency names. The bottom contains either `Enable` or `Disable`; incompatible content also shows the `Ignore compatibility warning` checkbox.
- The official-content `Ownership` value is `Modding.GetModInfo(handle).Allowance`, an entitlement check rather than an author/owner name: Yes means the current platform account is allowed to use that paid/free DLC, while No produces the `OwnershipRequired` enable/disable failure. Vanilla hides the row for community mods and official entries where `Allowance` is nil.
- `Modding.GetModAssociations(handle)` returns dependencies, references, and blocks as three separate values, but vanilla's visible details pane renders only the first dependency list; CAI deliberately mirrors that list. Most ordinary official DLC manifests declare expansion compatibility through `<References>`, so no Dependencies branch is expected for them. True scenario packages do declare `<Dependencies>` and should expose the branch—for example Australia Scenario depends on the Australia civilization pack, Poland Scenario on the Poland pack, and War Machine Scenario on Gathering Storm.
- Group choices come from `Modding.GetModGroups()`, localized and sorted by `SortIndex` then name. Choosing one calls `Modding.SetCurrentModGroup()` and refreshes the screen. Creating a group opens a centered modal with one edit box and Cancel/Create; `Modding.CreateModGroup(name, currentGroup)` clones the current group's state. Delete is disabled for protected groups. Activating it has no vanilla confirmation: the screen switches to the first other sorted group and immediately calls `Modding.DeleteModGroup(currentGroup)`.
- Both content filters default checked. Listings marked `ShowInBrowser="AlwaysHidden"` are excluded, as is unowned content in a final-release build. Search updates on every typed character, indexes localized name plus teaser through the engine `Search` context, and highlights matches in the displayed name. Sort choices are Name and Enabled; both hard-pin Rise and Fall and Gathering Storm ahead of other entries.
- `Enable All` and `Disable All` operate only on the currently filtered result set, including the search query and official/community checkboxes. Bulk enable first keeps only entries for which `Modding.CanEnableMod(handle, true)` returns `OK`. If compatibility warnings are active, one dialog confirms all incompatible entries and offers a checkbox to ignore their future warnings. Bulk disable passes every filtered handle to `Modding.DisableMod()`.
- Single-mod enable first checks `Modding.CanEnableMod(handle)`. If dependencies are missing it retries `CanEnableMod(handle, true)` so the button can describe dependencies that will be included. Activation calls `Modding.EnableMod(handle, true)`. An unacknowledged incompatibility opens a Yes/No confirmation. Single-mod disable uses `Modding.CanDisableMod(handle)` and exposes dependency/ownership failures in the disabled button tooltip before calling `Modding.DisableMod(handle)`.
- The Subscriptions page is one full-width scrolling list plus a bottom `Browse Workshop` button. Each 80-pixel subscription card shows its title, status, last-updated value, a green download-progress fill while downloading, and context-dependent buttons: View when the Steam overlay is available, Update when needed, otherwise Unsubscribe. Statuses are sourced from `Modding.GetSubscriptionDetails()` and `Modding.GetSubscriptionDownloadStatus()` and are refreshed from the context update loop. Browse is hidden in multiplayer and disabled when the platform overlay is unavailable. Vanilla unsubscribe calls `Modding.Unsubscribe(subscriptionId)` and immediately hides the existing visual instance; it deliberately does not call `RefreshSubscriptions()`, because Steam's asynchronous `GetSubscriptions()` result can still contain the removed ID briefly.
- Vanilla keyboard handling is limited. Escape closes the group-name modal if visible, otherwise exits the screen. Enter commits a nonempty group name. Search and group-name fields accept typing. No explicit keyboard traversal exists for the tabs, pull-downs, filters, mod rows, detail actions, or subscription cards; sighted players primarily click, scroll, and use the mouse wheel. Closing calls `GameConfiguration.UpdateEnabledMods()` before dequeueing the context, which makes the chosen group state authoritative for subsequent configuration.

CAI implementation:

- Build one manager `Panel` labeled from `LOC_MODS_TITLE`, containing a `TabControl`. Add Installed unconditionally and Subscriptions only when the live vanilla tab is visible. Accessible tab changes call `DoLeftClick()` on `Controls.InstalledTab` / `Controls.SubscriptionsTab`; the registered vanilla callbacks remain the sole owners of visual state, search reset, selection, and subscription refresh.
- The Installed page order is the installed-mod `Tree`, sort `Dropdown`, Official and Community filter `Checkbox` widgets, the selected mod's conditional Ignore Warnings checkbox, group `Dropdown`, Create Group, Delete Group, Enable All, and Disable All. There is no accessible Back widget; Escape retains vanilla `HandleExitRequest()`. The sort dropdown has the contextual label `Sort by`, but its two option labels are only `Name` and `Enabled`. Filter labels use the XML's localization tags directly because the native checkbox controls return empty text.
- Represent each visible mod as one expandable `TreeItem` keyed by its stable mod handle. Its label includes live name, enabled/disabled state, content type, and ownership/compatibility warning; its tooltip is only the teaser. Right/Left expands or collapses details. Enter invokes the currently selected vanilla Enable or Disable button instead of toggling expansion. On `focus_enter`, call `SelectMod(handle)` only when selection changed so the sighted details pane follows screen-reader focus without rebuilding the list.
- Expanded mod details expose compatibility, description, ID/map filename, author, special thanks, creation date, official-content entitlement under vanilla's `Ownership` label, saved-game effect, single-player/multiplayer support, and true dependencies. References and blocks are not rendered because vanilla's details pane omits those two association classes. An unavailable Enter action speaks the live vanilla Enable/Disable disabled tooltip, which contains associated mod names; if vanilla supplies no tooltip, CAI maps every known `CanEnableMod` / `CanDisableMod` failure identifier to its localized message and retains a generic localized fallback.
- Standard compatibility confirmations require no Mods-specific mirror because CAI's shared `PopupDialog.lua` replacement converts every standard `PopupDialog` automatically. Do not silently acknowledge compatibility warnings.
- Create Group shows the vanilla animated popup for visual parity and pushes a manager-owned `MakeGeneralDialog` like Save Game's nonstandard delete dialog. Its CAI `EditBox` synchronizes the live `ModGroupEditBox`; immediately after vanilla opens, call `Controls.ModGroupEditBox:DropFocus()` so keyboard input reaches CAI. Cancel and Create run the live vanilla button paths. Delete Group preserves vanilla's immediate deletion behavior and exposes the live descriptive tooltip rather than inventing a confirmation.
- The Mods tree installs a custom `SetSearchQueryHandler` following Tech/Civics. It queries vanilla's already-populated `g_SearchContext`, returns localized mod labels and teaser tooltips, and focuses the stable installed row when a result is activated. This preserves vanilla name-plus-teaser indexing and CAI's shared Ctrl+F overlay/history.
- Wrap `RefreshListings()` and `RefreshModGroups()` after the vanilla definitions. Rebuild affected accessible controls with `mgr:CaptureFocusKey(root)` / `mgr:RestoreFocus(root, capture)` and stable handle/group focus keys. Enabling/disabling restores the same mod row, verifies the live state transition, and announces `<mod name> Enabled/Disabled`. Failed individual actions speak the complete live failure tooltip. Delete Group announces `<group name> deleted`. Immediate bulk actions announce their changed count or that none changed; bulk-enable compatibility dialogs receive no premature success line.
- Add the Subscriptions page only when the live vanilla tab is available. Build it as a non-expandable `List` keyed by subscription ID. Each row label is only the mod title; its tooltip contains status and last-updated text. Enter invokes Update when available, otherwise View in Steam Workshop when available; Ctrl+Enter invokes View whenever available. Delete invokes the live Unsubscribe action, removes that ID optimistically from the current `g_Subscriptions` screen model, rebuilds with next/previous stable-key focus, and announces `Unsubscribed from <mod name>`. Do not immediately call `RefreshSubscriptions()` after unsubscribe: that races Steam's asynchronous backend and recreates the deleted row until a second attempt. Retain pending unsubscribed IDs across later refreshes and suppress them while Steam still returns them; clear each pending marker once the backend omits that ID. Browse Workshop follows the List immediately in tab order. Do not announce byte-level download progress every update tick.
- Install an extended input-struct handler that lets `mgr:HandleInput(input)` run first and otherwise preserves vanilla Escape behavior. Civ VI can invoke `OnShow()` before and again from vanilla `PostInit()`. The accessible open path therefore gates on completed post-initialization and funnels all build/push work through one idempotent `CAI_OpenPanel()` chokepoint; later context shows use the same function. Every close/dequeue path removes the CAI panel and any child dialog before calling the original.
- Static verification should cover vanilla-body parity, top-level and `CAIFrontEnd` registration, no literal user-facing strings, stable mod/subscription focus keys, all callback/API paths, and absence of direct `CAI.output`. In-game testing should cover one initial panel push; hide/reopen; no-mods and no-subscriptions states; many mods; both sorts and both labeled filters; live name/teaser search; group create/switch/delete; individual and bulk enable/disable feedback; dependencies, duplicate versions, missing ownership, incompatibility confirmation/whitelisting; maps and official/community content; Steam overlay enabled/disabled; downloading/update/unsubscribe transitions; Escape from the group dialog and root; Escape applying enabled mods; and the launch-error `View Mods` route.

## Frontend Credits screen

- The Main Menu route is `Additional Content` -> `Credits`. `MainMenu.OnCredits()` queues the non-deferred `CreditsScreen` child context at `PopupPriority.Current`.
- The shipped PC build has one base `Base/Assets/UI/FrontEnd/CreditsScreen.lua/.xml` pair and no DLC Lua/XML replacements. The installed Lua is byte-identical to `decompiled/Assets/UI/FrontEnd/CreditsScreen.lua`. CAI implements it as a front-end/shared full replacement at `src/UI/frontEnd/CreditsScreen.lua`; the complete vanilla body remains exact before the accessibility block, and the file is registered in both the top-level `<File>` list and `CAIFrontEnd` imports. No `<ReplaceUIScript>` or custom XML is needed.
- Visually, `BGBlock` fills the screen. A disabled, scrollbar-free 1024-pixel-wide `ScrollPanel` occupies the full height at screen center. Its `SlideAnim` moves a centered vertical stack upward after one screen-height initial spacer. The bottom-left stack contains a 250x32 shell-style Back button and, above it, a centered 250x24 blue package pull-down. The pull-down is hidden if the configuration database supplies only one roll.
- Credit markup is one line per item: `[1]` major title, `[2]` minor title/section, `[3]` heading or role, `[4]` ordinary entry/name, `[i]` image, and an empty payload for vertical spacing. Major titles are large ivory small caps, minor titles are gold small caps, headings are medium gray, and entries are smaller white text. All shipped English `[i]` lines use `TechTree_Meter_Done` as a decorative section divider; they carry no meaningful alternative text.
- `OnShow()` queries `DB.ConfigurationQuery("SELECT * FROM Credits ORDER BY SortOrder ASC")`, builds the pull-down from each row's localized `DisplayName`, then calls `DisplayCredits(1)`. Shipped rows are Base Game, Rise and Fall, Gathering Storm (all default sort order 0), New Frontier Pass (30), and Leader Pass (40), subject to the installed/loaded configuration packages.
- `DisplayCredits(index)` updates the pull-down button, destroys and regenerates all visual instances from `Locale.Lookup(credits.Credits)`, starts the roll at the beginning, and chooses animation speed from the generated stack height. When a roll completes, `OnCreditScrollComplete()` advances to the next database row and wraps from the last to the first.
- Sighted interaction is deliberately sparse. Mouse users choose a roll from the pull-down or click Back; changing a roll restarts it from the beginning. There is no scrollbar and the scroll panel is disabled, so mouse-wheel/manual scrolling is unavailable. Space pauses/resumes the animation. Enter or Escape closes the screen on key-up. The undocumented `Alt+Shift+T` turbo and `Alt+Shift+R` reverse paths are developer/debug controls, not player-facing features. The context input handler returns `true` for every input and assigns no keyboard focus to the native pull-down, so CAI must register a manager-first `InputStruct` handler before falling back to vanilla or Enter on an accessible widget will close the screen.
- The accessible layer retains the visual roll and exposes a stable virtual structure: a Panel labeled from `LOC_MAIN_MENU_CREDITS`, a package Dropdown backed by live `CreditsData`, and a searchable one-level Tree for the selected localized roll. Escape is the accessible close path; do not mirror the visual Back button as a widget. It parses with vanilla's CRLF/LF behavior, ignores blank spacing and decorative images, presents `[1]` as top-level title text, and `[2]` as ordered outer section nodes. Inside each section, every `[3]` role/heading and its following `[4]` contributors become one StaticText label joined by `[NEWLINE]`; there are no nested role TreeItems. Malformed or out-of-order contributor runs are likewise combined into their nearest section.
- Localized credit records may end with XML indentation consisting only of tabs, and some localized payloads use ordinary or non-breaking spaces for visual spacing. Vanilla renders those as blank space. Normalize UTF-8 non-breaking spaces to ordinary spaces and trim every accessible payload before classifying it; whitespace-only text must be decorative or it produces a focusable empty label announced as `""`.
- The accessible dropdown calls the final `DisplayCredits(index)`, keeping visible and virtual selections synchronized. `DisplayCredits` rebuilds the Tree with stable source-line focus keys and resets automatic narration. `Resize` recalculates narration positions while retaining the current animation position. `OnClose` removes the CAI root before vanilla dequeues the popup.
- `SlideAnim:RegisterAnimCallback()` supplies live normalized animation progress. CAI assigns each source line a visual-height weight matching its vanilla style, scales those weights to the final live stack height, and gives each nonblank/nonimage line a progress threshold where its center crosses the vertical center of the viewport. While the roll is playing and focus is outside the Tree, each crossed line is sent once through `Speak(..., false)`, preserving source order and allowing the screen reader to queue it. Space pauses both roll and narration, clears queued roll speech, plays `Play_UI_Click`, and announces `Credits paused`; Space on a manually paused roll plays the same cue, announces `Credits resumed`, and resumes from the current progress. Entering the Tree stops the animation; leaving resumes it only if tree focus caused the pause, so a prior manual Space pause is preserved. Manager-first input routing prevents Enter on accessible widgets from triggering vanilla's global close.
- Civ VI's UI Lua parser rejects a call statement whose receiver is a parenthesized `or` expression, such as `(currentSection or tree):AddChild(child)`, with `ambiguous syntax (function call x new statement)`. Assign the selected parent to a local first, then call `parent:AddChild(child)`. Standard Lua Language Server accepts the rejected form and therefore does not catch this compatibility error.
- Static verification covers vanilla-body parity, modinfo registration, localization and parser behavior for all five rolls, decorative-image suppression, monotonic narration thresholds, stable focus keys, manager-first input routing, and absence of direct `CAI.output`. In-game tests should cover one/many available packages, package selection and narration restart, spoken line order at the viewport center, Space pause/resume, no narration while stopped, Tree focus pausing and leaving resuming only a tree-caused pause, section/role/name order, collapsed-tree search and large rolls, Enter/Escape behavior, reopening after close, screen resize without repeated narration, natural end-of-roll rollover, and Main Menu focus return.

## Frontend Multiplayer UI

- Current `MainMenu.lua` exposes multiplayer through the main-menu submenu (`OnMultiPlayer -> ToggleOption`) rather than the older standalone `MultiplayerSelect` popup. The submenu rows are Play By Cloud, Internet, Unified PC/Crossplay, LAN, Hotseat, and DLC scenario matchmaking rows for Civ Royale and Pirates when available. The legacy `MultiplayerSelect.lua/.xml` still exists and offers Standard -> Internet/LAN plus Hotseat, but it is not the active option-table path.
- `LobbyTypes.lua` maps the frontend mode string to engine server/game/lobby types: Internet uses `SERVER_TYPE_INTERNET` / `GameModeTypes.INTERNET` / `LOBBY_INTERNET`; LAN uses `SERVER_TYPE_LAN`; Hotseat uses `SERVER_TYPE_HOTSEAT`; Play By Cloud uses `SERVER_TYPE_FIRAXIS_CLOUD`; Crossplay uses `SERVER_TYPE_CROSSPLAY`.
- Selecting Internet, LAN, Play By Cloud, or Crossplay raises `Lobby.lua`. Hotseat raises `HostGame.lua` directly. Civ Royale and Pirates use intro popups when not seen, then call matchmaking and show `JoiningRoom.lua`.
- `Lobby.lua/.xml` is a full-screen shell game browser with logo/header, Back and Refresh/Stop Refresh buttons, optional Play By Cloud shell tabs (`My Games`, `Completed Games` with unseen-complete variant), optional Join Code button, a sortable game list, bottom buttons (`Join Game` / `Play Game`, `Load Game`, `Create Game`), and a collapsible friends panel.
- Lobby listing rows are generated by `ListingButtonInstance` and have columns for game name, ruleset, map, game speed, official/community content icons, and player count. Rows show faded/version-mismatch coloring and row tooltips for started games, loading saves, version mismatch, current PBC turn, unseen completed games, turn owner, players, and mod ownership/download status. A row click selects it; double-click selects and joins. Bottom Join is disabled for version mismatch and changes to `Play Game` for personal Play By Cloud games.
- Lobby column headers are sort buttons (`SortbyName`, `SortbyRuleSet`, `SortbyMapName`, `SortbyGameSpeed`, `SortbyModsHosted`, `SortbyPlayers`). Play By Cloud uses offset scrolling and backend browse modes, so the UI does not locally resort those offset-scrolled results. Reaching scroll ends may request the previous/next PBC page.
- Lobby actions preserve engine flow: refresh calls `Matchmaking.RefreshGameList()`, Create Game raises `LuaEvents.Lobby_RaiseHostGame()`, Join calls `Network.JoinGame(serverID)`, Join Code opens a `PopupDialog` edit box then calls `Network.JoinGameByJoinCode(code)`, Load Game opens `LoadGameMenu` after setting server type/game mode, and Back closes dialogs, calls `Network.LeaveGame()`, and dequeues the lobby.
- A visible Lobby listing exposes its target mod configuration before joining through `serverEntry.EnabledMods`, preserved as `listing.EnabledMods`. `Modding.GetModsFromConfigurationString(...)` returns entries with at least the target `ModId`, display `Name`, and optional Steam `SubscriptionId`. Vanilla already classifies each entry with `Modding.GetModHandle(mod.ModId)` and `Modding.IsJoinGameAllowed(mod.ModId)`; an absent handle with a nonempty subscription id is shown as downloadable, while an absent handle without one is shown as missing. This makes a pre-`Network.JoinGame(serverID)` missing-content gate feasible for selected game-list rows without changing game configuration first.
- The multiplayer auto-download option is the numeric user option `Options.GetUserOption("Multiplayer", "AutoModDownload")`: `1` is enabled and `0` is disabled. Vanilla exposes this option only on Steam (`Network.GetNetworkPlatform() == NetworkPlatform.NETWORK_PLATFORM_STEAM`); other platforms do not support the mod auto-download feature in the Options UI.
- A pre-join failure cannot reuse `Modding.GetLastModErrorString()` verbatim because that backend error string is populated only after an attempted join/content-configure failure. The Lobby must build a localized missing-content message from the parsed target mod entries, then raise `LuaEvents.MultiplayerPopup(message, "LOC_GAME_ABANDONED_MOD_MISSING_TITLE")` while CAI is still enabled. The existing `JoiningRoom` missing-mod path uses the same popup title with `Modding.GetLastModErrorString()` after `KickReason.KICK_MOD_MISSING`.
- This preflight is limited to join paths that have a resolved game-list entry. Join Code calls `Network.JoinGameByJoinCode(code)` directly, and invite/matchmaking processing does not expose the target `EnabledMods` configuration to Lobby Lua before joining; those paths cannot perform the same comparison with the documented frontend APIs. The engine-side `JoiningRoom` failure handling remains necessary for them and for races where a listing changes after refresh.
- Non-list join details: `Network.JoinGameByJoinCode(code)` accepts only the opaque code; Epic/friend invites are accepted through `FriendsInstance:AcceptInvite(inviteID)`; Play By Cloud notifications call `Matchmaking.AcceptCloudGameInvite(matchID)`; scenario matchmaking configures the locally known scenario mod and calls `Network.MatchMake()`, after which the engine chooses the target. `Events.BeforeMultiplayerInviteProcessing` and `Events.MultiplayerJoinRoomAttempt` have no Lua parameters in the shipped callers/IDE event signatures. The latter is notification-only and is used by `FrontEnd.lua` merely to queue `JoiningRoom`; no documented cancel/defer hook or join-code/invite-to-listing resolver exists.
- A possible experimental interception point for non-list joins is `Events.MultiplayerJoinRoomComplete`, before `MultiplayerJoinGameComplete` / `FinishedGameplayContentConfigure`. At that point Lua could inspect `GameConfiguration.GetEnabledMods()` and leave the session before further configuration if it contains the complete remote requirement set. Shipped Lua does not establish that timing or data completeness, especially for mods absent locally, so this must be instrumented and tested before relying on it. It cannot satisfy a strict "before attempting to join the room" requirement.
- The dependable non-list mitigation is therefore a deferred failure popup owned entirely by `JoiningRoom`; generic `FrontEndPopup` behavior is unchanged. Terminal room/matchmaking/abandon handlers capture the same vanilla popup text and title, hide JoiningRoom, and call `Network.LeaveGame()` without raising `LuaEvents.MultiplayerPopup` yet. On the pending `Events.LeaveGameComplete`, CAI calls `Modding.EnableMod(caiHandle, true)` and `GameConfiguration.UpdateEnabledMods()`. Only after the resulting successful `FinishedGameplayContentConfigure` event does JoiningRoom raise the captured popup, ensuring CAI settings such as `SpeakRole` exist before `FrontEndPopup` pushes its accessible dialog. Individual room failures/abandonments during active matchmaking retain vanilla behavior because they are nonterminal and matchmaking continues searching. Shipped `Mods.lua` confirms `Modding.EnableMod` takes a mod handle; `Modding.IsModEnabled` takes the GUID and is useful for verifying recovery state. Its optional `true` flag resolves/includes required dependencies: vanilla retries `CanEnableMod(handle, true)` after `MissingDependencies`, shows the returned dependency list, and then calls `EnableMod(handle, true)`. Compatibility warnings are handled separately.
- `HostGame.lua/.xml` is the multiplayer setup screen. It is a full-screen shell form with the Civ VI logo at the top, a centered `LOC_MULTIPLAYER_HOST_GAME` header, Back on the header right, Restore Default on the header left, an Additional Content button above the setup panel, shell tabs, a large bordered scroll panel, and a bottom button row for Load Game, Load Config, Save Config, and Confirm Settings.
- The scroll panel is divided into four visible parameter sections backed by separate stacks: Map Options (`PrimaryParametersStack`, for `BasicGameOptions`, `GameOptions`, `BasicMapOptions`, and `MapOptions`), Game Modes (`GameModeParameterStack`), Victory Conditions (`VictoryParameterStack`), and Advanced Options (`SecondaryParametersStack`). `GameSetupLogic.lua` sorts children after each refresh with `o.Utility_SortFunction(...)` and hides empty parameter stacks/headers when possible. In HostGame XML the Game Modes header has an ID, but Victory/Advanced section headers are anonymous; accessible grouping should therefore use the known stack order/loc keys rather than only header IDs.
- Host setup uses `GameSetupLogic.lua` / `PlayerSetupLogic.lua` drivers for dynamic setup parameters. The stable widget vocabulary is boolean checkbox, text/number edit box, integer range slider with number display, pulldown, and button-driven picker. Boolean rows click to toggle and broadcast config. Edit boxes commit text/int/uint values and broadcast config. Sliders broadcast on step change and show the current number in `NumberDisplay`. Pulldowns rebuild entries from `parameter.Values`; the selected button text is the current value name and the selected button tooltip is the value description when available. Generic multi-value arrays are disabled pulldowns, but HostGame overrides array parameters into picker buttons where possible.
- Play By Cloud exposes an integer-range HostGame parameter whose minimum is `2`. CAI constructs every mirrored slider with an initial internal value of `0`, then applies its bounds before silently seeding the live parameter value. `SliderWidget:SetMin(2)` previously clamped `0` to `2` through a non-silent `SetValue`, causing every HostGame parameter refresh to speak the bare value `2` even though the slider was not focused. Shared slider bound changes now clamp silently; user input remains non-silent.
- HostGame overrides specific setup drivers: `CityStates` opens `CityStatePicker`; `LeaderPool1` / `LeaderPool2` open `LeaderPicker`; other array parameters open `MultiSelectWindow`. These picker buttons show summary text such as Nothing, Everything, or Custom count according to `parameter.UxHint == "InvertSelection"`, keep the parameter name in `StringName`, and use `parameter.Description` as the tooltip. Picker results return through `LuaEvents.*_SetParameterValues` / `CityStatePicker_SetParameterValue`, update `g_GameParameters`, and call `Network.BroadcastGameConfig()`.
- Sighted picker flow: `MultiSelectWindow` shows a title, description, selectable item checklist with icon/name, focused-item detail pane, Select All, Select None, Confirm, and Close/Escape. `CityStatePicker` is similar but also has a city-state count slider/number, a sort pulldown, count warning, and disables Confirm until enough city-states are selected. `LeaderPicker` is similar but shows leader/civ ability details for the focused row and has a preset pulldown for All, None, and No Wins. Clicking a row or its checkbox toggles selection; Confirm writes selected values and closes; Escape/Close cancels. CityStatePicker Close also restores the original city-state count.
- HostGame lifecycle/actions: `OnRaiseHostGame()` defaults the game config for the current multiplayer lobby mode, clears `RULESET`, and queues the screen. `OnEnsureHostGame()` queues without defaulting for an existing session. `OnShow()` rebuilds player parameters headlessly, refreshes setup parameters, hides Additional Content and Confirm when already in a network session, hides Load Game unless Hotseat and not in session, and realizes shell tabs. When in a session, shell tabs are Game Setup plus Staging Room, and the Staging Room tab raises `LuaEvents.HostGame_ShowStagingRoom()`.
- HostGame exit/confirm behavior: Back/Escape raise `LuaEvents.Multiplayer_ExitShell`; if already in a network session, Back first shows a `PopupDialog` quit warning and `CheckLeaveGame()` may call `Network.LeaveGame()` before dequeueing. Confirm ensures `GAME_NAME` is non-empty, warns with `LOC_CITY_STATE_PICKER_TOO_FEW_WARNING` if the selected/excluded city-states leave fewer available city-states than `CityStateCount`, and then calls `Network.HostGame(serverType)` through `HostGame(serverType)`.
- `UI/frontEnd/Multiplayer/HostGame.lua` CAI frontend replacement is implemented as a full vanilla-copy override with an accessibility block before `Initialize()`. The CAI shape deliberately omits shell tabs and exposes one panel with a setup-options list plus sibling action buttons. The list contains submenus for Map Options, Game Modes, Victory Conditions, and Advanced Options, populated from live `g_GameParameters.Parameters` and grouped by the same parameter groups as the vanilla stacks. Game name is not a separate XML edit box; it is the dynamic text setup parameter (`GAME_NAME`) and appears in the options list as an `EditBox`. Checkbox rows seed state from the live vanilla checkbox when available with `SetChecked(value, true)` and use `SetValueSetter(...)` to click the live vanilla checkbox only when CAI's requested value differs, preserving the vanilla toggle/broadcast path without the old `value_changed` inversion pattern. Array rows click the live picker button or raise the vanilla CityStatePicker/LeaderPicker/MultiSelectWindow fallback; sliders, dropdowns, and edit boxes write through `g_GameParameters:SetParameterValue(...)` and broadcast config. Action buttons click live Load Game, Load Config, Save Config, Restore Default, Additional Content, and Confirm Settings controls; Staging Room is exposed as an action only during an existing network session and raises `LuaEvents.HostGame_ShowStagingRoom()`.
- `AdvancedSetup.lua` shares the same `PlayerSetupLogic.lua` validation sources as HostGame/StagingRoom, but the vanilla surfacing differs: `UI_PostRefreshParameters()` clears the Start button tooltip/disabled state, disables Start with `LOC_SETUP_PARAMETER_ERROR` when `GetGameParametersError()` is non-nil, then checks every participating player with `GetPlayerParameterError(playerID)` and, on any hit, disables Start again, sets the Start tooltip to `LOC_SETUP_PLAYER_PARAMETER_ERROR`, and unhides `Controls.ConflictPopup`. `ConflictPopup` itself is static XML (`LOC_SETUP_PLAYER_PARAMETER_ERROR`, body `LOC_SETUP_PLAYER_PARAMETER_ERROR_POPUP`, confirm `LOC_CONTINUE`). Player-leader pulldowns still inherit the same `value.InvalidReason` data from `PlayerSetupLogic.lua`: the selected caption and each entry append the invalid reason in red in vanilla, so accessibility mirrors should expose those invalid reasons on both the closed selected value and the option list, not only via the global Start blocker.
- Advanced Setup parameter-parity audit (2026-07-17): the shipped `src/UI/frontEnd/AdvancedSetup.lua` body is an exact copy of the installed vanilla `Base/Assets/UI/FrontEnd/AdvancedSetup.lua` through the end of vanilla `Initialize()`; CAI is appended before the final call. Vanilla and CAI both cover the complete shipped game-parameter group set: `BasicGameOptions`, `GameOptions`, `BasicMapOptions`, `MapOptions`, `GameModes`, `Victories`, and `AdvancedOptions`. The supported visual domains also match vanilla's complete vocabulary: bool, int/uint/text, `IntRange`, ordinary values, and arrays, with the City-States, leader-pool, and generic multi-select pickers.
- The Advanced page clears and rebuilds its parameter children from current `g_GameParameters.Parameters` after every refresh. Basic-page widgets retain stable parameter ids and explicitly resynchronize checkbox, slider, dropdown, and inactive edit-box values. CAI also wraps vanilla `UI_DestroyParameter(...)`: if a custom ruleset/map/mod removes a dynamic `BasicGameOptions`, `BasicMapOptions`, or `GameModes` id entirely, the corresponding accessible widget is destroyed and its caches are cleared before a later instance can reuse the vanilla control.
- Vanilla's `Map` factory does not use the `CreateGame_MapType` pulldown even though that unused control exists in `AdvancedSetup.xml`. In the Basic view, `CreateSimpleMapPopupDriver(...)` updates `MapSelectButton` inside the visible `CreateGame_MapSelectContainer`; in the Advanced view, `CreateButtonPopupDriver(..., OnMapSelect)` creates another button that opens the same `MapSelect` popup. CAI mirrors both paths: Basic visibility follows `CreateGame_MapSelectContainer`, and the Advanced Map row is a button that clicks the live vanilla advanced button, preserving the accessible MapSelect screen, generated/static filtering, descriptions, and vanilla write/broadcast callbacks.
- Vanilla `SetupLeaderPulldown(...)` resets `PlayerColorAlternate` to `0` after every leader change and disables each pulldown through `CheckExternalEnabled(...)` plus the single/empty-values check. CAI now applies the reset to local and AI leader choices and disables AI dropdowns when the live `PlayerLeader` parameter is disabled or has at most one value. These dropdowns use `SetValueSetter(...)`, with silent selection used only for initialization.
- Removable AI player dropdowns in Advanced Setup and Scenario Setup bind Delete on key-up with the shared input-help description `LOC_CAI_KB_DELETE_PLAYER` (`Delete player`). Keep the description on both screen-local bindings so focused-binding help does not announce an unlabeled Delete command.
- Parameter-level invalidity is distinct from invalid individual values. CAI composes a live parameter tooltip from the selected value or parameter description, then appends `parameter.InvalidReason` and the selected `value.InvalidReason` when present. Checkboxes, arrays, sliders, edit boxes, fixed/dynamic dropdowns, Map buttons, and closed leader controls therefore expose the applicable local explanation while the Start action continues to expose the global blocker. Individual dropdown and leader options retain their option-specific invalid reasons.
- Advanced section visibility mirrors vanilla without consulting accessible children from the section's own hidden predicate. Players follows `Controls.PlayersSection`; Options, Game Modes, Victories, and Advanced Options follow `PrimaryParametersStack`, `GameModeParameterStack`, `VictoryParameterStack`, and `SecondaryParametersStack`, whose visibility vanilla already calculates from their live children. Calling `sub:GetVisibleChildren()` from `sub`'s hidden predicate recurses through each child's ancestor-hidden check and freezes the UI. Basic game-mode visibility follows the live checkbox rather than its persistent instance root.
- Advanced accessibility rebuilding has two entry points with one dirty flag: `GameParameters_UI_AfterRefresh(...)` is the sole configuration/player refresh choke point and rebuilds immediately only while Advanced is active; the Advanced tab activation builds only when a refresh marked it dirty while inactive. Do not also wrap `RefreshPlayerSlots()`: vanilla ends that function by calling `GameSetup_RefreshParameters()`, so its work already reaches the after-refresh choke point and a wrapper duplicates the player/parameter rebuild chain.
- Array summaries say Everything for a nil inverted selection and Nothing for a nil non-inverted selection, matching vanilla for both shipped and mod-provided array parameters.
- `JoiningRoom.lua/.xml` is a simple interstitial for joining, matchmaking, content configuration, and mod-download progress. Visually it is a shell background box with a centered shell window, header `LOC_MULTIPLAYER_JOINING_ROOM_TITLE`, one centered `JoiningLabel`, and a bottom-centered `CancelButton`. There is no game list, player list, chat, ready button, or choice set on this screen.
- `JoiningRoom` sighted interaction is minimal: click `Cancel` or press Escape to call `HandleExitRequest()`, which runs `Network.LeaveGame()` and dequeues the popup. Everything else is passive status feedback driven by network/content events.
- `JoiningRoom` status text changes: on show it says matchmaking (`LOC_MULTIPLAYER_MATCHMAKING`) when `Network.IsMatchMaking()` is true, otherwise joining room (`LOC_MULTIPLAYER_JOINING_ROOM`); `MultiplayerJoinRoomComplete` may change remote clients to joining host (`LOC_MULTIPLAYER_JOINING_HOST`); `MultiplayerJoinGameComplete` changes to configuring content (`LOC_MULTIPLAYER_CONFIGURING_CONTENT`) while waiting for content configure; `ConnectedToNetSessionHost` says connecting to players (`LOC_MULTIPLAYER_CONNECTING_TO_PLAYERS`); local `ModStatusUpdated` while downloading mods shows `LOC_MODS_SUBSCRIPTION_DOWNLOAD_PENDING` plus `[Icon_AdditionalContent]remaining/required`.
- `JoiningRoom` transitions to `StagingRoom` only after `Events.MultiplayerJoinGameComplete` and, for remote clients, successful `Events.FinishedGameplayContentConfigure`. The transition raises `LuaEvents.JoiningRoom_ShowStagingRoom()` before dequeuing the popup, so `StagingRoom` can queue itself without exposing the lobby beneath for a frame.
- `JoiningRoom` failure/abandon paths raise `LuaEvents.MultiplayerPopup(...)`, call `Network.LeaveGame()`, and dequeue the popup. Covered failures include room full, game started, too many matches, generic join failed, matchmaking failed, kicked, host lost/refused, no room, version mismatch, mod error, missing mod, and match deleted. Matchmaking mode ignores join-room-failed/abandoned events so matchmaking can continue searching.
- `UI/frontEnd/Multiplayer/JoiningRoom.lua` CAI frontend replacement is implemented as a full vanilla-copy override with an accessibility block before `Initialize()`. It builds a status `StaticText` bound to live `Controls.JoiningLabel` and a Cancel `Button` that clicks the live vanilla `CancelButton` or falls back to `HandleExitRequest()`, then assembles them with `mgr.WidgetHelpers.MakeGeneralDialog(titleFn, buttons, contentRows, defaultIndex)` like other CAI dialogs. The status line has no tooltip; it speaks from its label only. The dialog opens focused on the status text so the initial status is spoken by normal focus speech. Status-changing handlers (`OnJoinRoomComplete`, `OnJoinGameComplete`, `OnModStatusUpdated`, and `OnConnectedToNetSessionHost`) announce the status widget only when the live label differs from the previous spoken status. Exit/transition/failure/invite paths remove the CAI dialog before vanilla dequeues the context or raises follow-up UI. The replacement also installs an extended input handler so the UI manager receives frontend dialog input while Escape keeps the vanilla leave-session behavior.
- `StagingRoom.lua/.xml` is the main player lobby. The top has Back, optional Play By Cloud End Game/Quit Game buttons, title, and Game Setup/Staging Room shell tabs. The primary panel is a table-like player list with headers for Players, Team, Civ/Leader, Difficulty, Ready, and Kick. The lower right is Chat; the lower left is either Game Summary or Friends, selected by tabs.
- Staging player rows expose slot type/player identity, team pulldown, color pulldown, leader/civ pulldown with leader/civ icons and warning icon for color conflicts, difficulty pulldown, ready status, kick button, add-player button for the first closed slot when allowed, and hotseat edit button. Slot type options include open/closed/AI/human-required behavior plus swap options in non-hotseat, non-PBC, non-matchmaking sessions when allowed.
- Staging row data is not all readable from visible button text. The leader selector is a split leader/civ pulldown created by `SetupSplitLeaderPulldown(...)`; the selected spoken label that best matches Advanced Setup is `leader name, civilization name`, and the durable tooltip source is the same `GetPlayerInfo(...)`-driven leader/civ ability build used in `AdvancedSetup.lua` (`BuildLeaderTooltip`). Team and color selected states are largely icon-only in StagingRoom, so accessibility should derive them from live player state rather than `GetText()`: team from `PlayerConfigurations[playerID]:GetTeam()` plus the launched-game single-member-team-to-None special case, and color from `PlayerColorAlternate` plus `UI.GetPlayerColorValues(...)`.
- Staging color selection is not a normal parameter-values pulldown. Vanilla manually rebuilds `ColorPullDown` entries inside `UpdatePlayerEntry()` by iterating alternates `0..3`, checking `UI.GetPlayerColorValues(playerColor, j)`, setting `m_teamColors[playerID]` for collision checks, and writing `parameters:SetParameterValue(parameters.Parameters["PlayerColorAlternate"], j)`. Accessibility wrappers should mirror that manual option build instead of trusting `PlayerColorAlternate.Values`.
- Staging team pulldowns are also rebuilt manually by `SetupTeamPulldown(...)`: always add `None`, append existing non-`NO_TEAM` teams from `GetTeamCounts(teamCounts)`, then append one empty team slot with the first unused numeric ID so players can create a new team.
- Staging permissions are live: hosts can modify non-human slots and kick remote humans; players can modify themselves until ready; ready players lock their slot-type and player-value controls; in-progress games hide or disable many setup controls; matchmade games prevent slot edits; hotseat allows broader local changes and hides network-only ready/chat/kick behavior.
- Staging ready/start area is a large central banner/check button. Network games use a ready checkbox when not counting down, and a numbered launch/ready/wait countdown button during countdown. Hotseat uses it as Start Game. The Start label explains blockers such as not enough players, invalid teams, map-size slot errors, mod downloads, duplicate leaders, unfilled required humans, Play By Cloud remote-ready restrictions, or player parameter errors.
- Hotseat enters through `MainMenu.OnHotSeat()`, which sets `MPLobbyTypes.HOTSEAT` and raises `HostGame`; it does not go through the lobby browser. `HostGame` is still the same setup form, but `Load Game` is shown only for Hotseat when not already in session. After `Network.HostGame(serverType)`, the same `StagingRoom` context is reused with hotseat-specific visibility and validation.
- Hotseat staging differs from network staging mostly by hiding network surfaces and widening local slot editing. `InitializeReadyUI()` hides the ready column and countdown art, `ShowHideReadyButtons()` always shows the big `ReadyButton` container instead of the ready checkbox, `ShowHideChatPanel()` hides chat entirely, `RealizeInfoTabs()` omits the Friends tab, Kick is always hidden, and `PopulateSlotTypePulldown()` allows the hotseat-only `Human` slot type while suppressing swap.
- Hotseat player names are auto-seeded from `LOC_HOTSEAT_DEFAULT_PLAYER_NAME`, then renumbered by `UpdateAllDefaultPlayerNames()` for untouched default-name humans. Active human rows swap their normal player-card control for `HotseatEditButton`, which opens `EditHotseatPlayer`.
- Hotseat launch validation is split awkwardly. `UpdateReadyButton_Hotseat()` computes banner text and disabled state from cached `g_hotseatNumHumanPlayers`, `g_hotseatNumAIPlayers`, team validity, map-size validity, and duplicate-leader checks. But `OnReadyButton()` still calls `Network.LaunchGame()` unconditionally whenever `GameConfiguration.IsHotseat()` is true. So the button text is meant to be different in hotseat mode, but it is only advisory UI; if the cached blocker state goes stale or the button remains clickable, the click path itself does not revalidate before launching.
- `g_hotseatNumHumanPlayers` and `g_hotseatNumAIPlayers` are refreshed inside `UpdatePlayerEntry_Hotseat()` rather than at click time. That makes a stale hotseat blocker label/tooltip plausible after slot edits or rebuild timing: the Start banner can lag behind the actual launchable state even though the click path launches immediately.
- Vanilla staging-room launch-blocker hints appear in three distinct places:
  - the centered start banner (`StartLabel`) plus ready control tooltips (`ReadyButton`, `ReadyCheck`, and the local row `ReadyImage`) show the current global blocker or countdown state. For players-connecting and mods-not-ready blockers, the tooltip is expanded with one line per affected player name.
  - each player row's right-side `StatusLabel` shows per-slot readiness or slot-specific problems such as mod download state, invalid-for-map-size, empty human-required slot, unsupported player-count warning, and any `GetPlayerParameterError(playerID)` reason appended in red. Some of those rows also carry a more explicit tooltip (`LOC_INVALID_SLOT_MAP_SIZE_TT`, `LOC_INVALID_SLOT_HUMAN_REQUIRED_TT`, unsupported tooltip text).
  - individual setup selectors can also carry inline invalid markers: the leader/civ pulldown's selected caption and dropdown entries append `value.InvalidReason` in red when a choice is invalid, and generic game-setup parameter controls prepend `parameter.InvalidReason` to the control tooltip.
- Vanilla staging-room parameter-error gating is two-layered: `UpdateReadyButton()` surfaces the first game/player parameter blocker globally by repainting `StartLabel` red and disabling the ready controls, while `UpdatePlayerEntry()` also appends the specific player-parameter error text onto the affected row's `StatusLabel`. This means the player can usually see both the global "cannot start" state and which row/selector caused it.
- Slot removal in vanilla staging is state-based, not structural. The slot pulldown supports `Open`, `AI`, `Closed`, hotseat-only `Human`, and `Swap`; `OnAddPlayer()` only reopens the first closed slot. There is no dedicated "delete/remove slot" action in `StagingRoom.lua`. The only way slots disappear entirely from the room is indirectly through `g_currentMaxPlayers` / map-size changes (`MapConfiguration.GetMaxMajorPlayers()` and related events), which makes higher player IDs non-displayable or invalid for the current map size.
- Vanilla kick-button visibility is narrower than simple slot editability: `UpdatePlayerEntry()` shows Kick only when the local player is the game host, the target slot status is `SS_TAKEN` or `SS_OBSERVER`, the target is not the local player, and the session is not hotseat. Inactive/open/closed/AI rows hide Kick even if other slot controls remain visible.
- Vanilla slot-type pulldowns explicitly disable when they have zero real options (`pullDown.ItemCount < 1`), even before the broader ready/permissions disable pass runs. Any CAI mirror that keeps a placeholder entry for accessibility should still disable the dropdown itself when there are no real slot actions available.
- Staging chat is hidden for Hotseat, Play By Cloud, or when chat is unavailable. Otherwise it has a target pulldown and edit box; committing the box sends chat. `/help` is handled entirely client-side by `ParseInputChatString(...)` returning `printHelp=true`; vanilla then calls `ChatPrintHelp(...)`, which writes localized help directly to the visible chat stack without sending network chat or calling `OnChat(...)`. `ResetChat()` similarly calls `ChatPrintHelpHint(...)` to print the localized command-help hint. Friends/invite controls use `Network.GetFriends()` helpers and are hidden when invites are not allowed. Join code can be shown in the game-summary panel for PBC/Crossplay/EOS Internet and clicking the text copies it to clipboard.
- `EditHotseatPlayer.lua/.xml` is a modal for hotseat player name/password setup. It has name, password, password-verify edit boxes, mismatch status, Accept, and Cancel. Accept only enables when passwords match; it calls `LuaEvents.EditHotseatPlayer_UpdatePlayer(playerID)`.
- `EditHotseatPlayer` mutates live `PlayerConfigurations[playerID]` while the dialog is open: name changes write immediately via `SetHotseatName(...)`, password edits write immediately via `SetHotseatPassword(...)` when the two password boxes match, and a mismatch temporarily clears the stored hotseat password to `""`. Cancel restores the captured initial name/password before closing. Accept does not push a second copy of the values; it only raises `LuaEvents.EditHotseatPlayer_UpdatePlayer(playerID)` so `StagingRoom` refreshes that row.
- `UI/frontEnd/Multiplayer/EditHotseatPlayer.lua` now has a CAI frontend full replacement. The CAI mirror is a modal `MakeGeneralDialog` with three `AlwaysEdit` fields bound to the live vanilla edit boxes through `text_changed` so name/password writes stay immediate, matching vanilla's live `PlayerConfigurations` mutation. The old mismatch status label is intentionally not exposed as a separate CAI widget; when mismatch text is visible, it becomes the Accept button tooltip instead.
- `PlayerChange.lua/.xml` is the in-game hotseat player-handoff popup. It has two modes: a turn-start prompt that sets `TitleText` to the local player's name and optionally requires a hotseat password before Start Turn, and a turn-end `Please Wait` mode driven by `bPlayerChanging` where the dialog box itself is hidden and only `PlayerChangingText` remains visible. `ShowTurnControls()` is the live choke point for that state switch; it clears the password field, toggles `PasswordStack`, enables/disables `OkButton`, and calls `Controls.PasswordEntry:TakeFocus()` when a password is required.
- Vanilla hotseat handoff gives CAI two useful boundaries: `Events.LocalPlayerTurnEnd` for freezing or deferring per-player state, and `Events.LocalPlayerTurnBegin` for restoring the newly active player's state. `PlayerChange` sits between those events visually, but the CAI state swap itself should key off the player-turn events, not the popup lifecycle.
- `PlayerChange.OnOk()` raises `LuaEvents.PlayerChange_Close(Game.GetLocalPlayer())` immediately before the popup is dequeued. That is a good post-accept hook for deferred CAI speech that should wait until the handoff prompt is dismissed.
- `UI/inGame/PlayerChange_CAI.lua` now mirrors `PlayerChange` through a CAI dialog rebuilt from wrapped `ShowTurnControls()`. Prompt mode exposes Start Turn, Save Game, and Menu plus a masked `AlwaysEdit` password field when `PasswordStack` is visible; wait mode exposes the wait text and Menu. The password edit writes through `text_changed` to the live vanilla `Controls.PasswordEntry`, calls vanilla `OnPasswordEntryStringChanged(...)` for enablement parity, uses Enter commit to call vanilla `OnPasswordEntryCommit()`, and disables commit-on-focus-leave so tabbing away does not auto-start the turn. Right before the CAI dialog is pushed, it drops focus from the live `Controls.PasswordEntry` to prevent the native edit box from trapping interaction under the CAI dialog.
- `ConfirmKick.lua` uses `PopupDialog` for host kick confirmation or vote-kick reason selection (AFK, griefing, cheating, cancel). The CAI frontend replacement is a full vanilla-copy override with no CAI widget mirror; its accessibility block installs an extended input-struct handler after `Initialize()` that lets the CAI UI manager handle input first, then directly mirrors the vanilla Escape close path without delegating to the original old-signature `InputHandler`. `ConfirmExit.lua` is a simpler accept/cancel dialog for exit/kick flow. Crossplay login is a message dialog around EOS login state and raises `LuaEvents.EnterCrossPlayLobby()` when login succeeds.
- `PBCNotifyRemind.lua/.xml` is a Play By Cloud notification setup reminder with Options, Accept, and a Do Not Remind checkbox persisted to `Options.SetUserOption("Interface", "PlayByCloudNotifyRemind", ...)`.
- `UI/frontEnd/Multiplayer/PBCNotifyRemind.lua` CAI frontend replacement should mirror it as a simple dialog: title from `Controls.RemindTitle`, body from `Controls.RemindText`, a `Checkbox` bound to the live `DoNotRemindCheckbox` via `IsChecked()` + `DoLeftClick()`, and Options / OK buttons that click the live vanilla buttons. Show/hide is driven by the popup's `ShowHideHandler`, and the extended input handler should let the CAI manager handle Enter/Space/Tab while Escape still closes through `OnAccept()`.
- Civ Royale and Pirates intro popups are multi-page visual tutorials with logo, illustration, description, optional scrollable details, Previous, Next, and Close. CAI changes the final Next control into Close, hides Previous on that page, and removes the matchmaking event call, so the final page exposes only Close.
- CAI's accessible Multiplayer submenu deliberately exposes the Civ Royale and Pirates Help controls as direct buttons and omits both matchmaking controls because scenario matchmaking disables CAI. Each button uses the live Help tooltip (`View a quick tutorial on...`) as its label instead of adding a generic Help child beneath a scenario submenu.
- The front-end Civ Royale tutorial is the separate `CivRoyaleIntro` context hosted by `Base/Assets/UI/FrontEnd/MainMenu.xml`; it is not the in-game `ExpansionIntro` context. CAI supplies `UI/frontEnd/Multiplayer/CivRoyaleIntro.lua` as a full front-end replacement: the vanilla file is retained and the accessibility block is inserted before the final `Initialize()` call. Its dialog mirrors the live description/detail controls and Previous/Next controls; the final page hides Previous and relabels Next as Close. Escape preserves vanilla close/seen behavior, and `JoiningRoom_Showing` removes both the vanilla popup and CAI dialog.
- Accessibility-shape implication: treat frontend multiplayer as four primary surfaces: MainMenu multiplayer submenu, Lobby browser, HostGame setup, and StagingRoom. Lobby should be a tab/list browser with sortable column reads, join-code dialog, friends list, and live row tooltips. HostGame can mirror the existing setup parameters with a form-like panel and picker dialogs. StagingRoom is the highest-density surface and should likely use a table or tree/list hybrid for player rows plus separate chat, game-summary, friends, and ready/start widgets, preserving all vanilla callbacks (`Network.JoinGame`, `Network.HostGame`, `Network.BroadcastPlayerInfo`, `Network.LaunchGame`, live pulldown callbacks, and modal dialogs).
- `UI/frontEnd/Multiplayer/Lobby.lua` CAI frontend replacement is implemented as a full vanilla-copy override with an accessibility block before `Initialize()`. The CAI panel exposes a games `Tree`, sort dropdown, sort-direction dropdown, refresh button, Join Code, Load, Host/Create, and a persistent friends `List`. PBC lobbies wrap the tree in a `TabControl` whose pages mirror the vanilla shell tabs and switch by clicking the vanilla tab button. Game-row focus selects the vanilla row only when it is not already selected; game-row activate selects and clicks the vanilla Join/Play button when available, so there is no separate CAI Join button. The CAI Join Code button mirrors the live vanilla Join Code button and opens the same `PopupDialog` path that commits through `Network.JoinGameByJoinCode(...)`. Disabled join reasons are included in the row tooltip, but row status such as loading-save/current-turn remains in the label only to avoid repeated speech. Rows expand to player names sourced from the live Members tooltip when available; vanilla does not create visual player rows and the only known source is `serverEntry.Players`, so some lobby types may expose only the player count. Rows also include official/community additional-content groups. Content groups with entries expand to individual mod/game-mode rows and use the group tooltip for owned/required counts; empty groups have no children and use `None` as the tooltip. Vanilla Lua only treats Play By Cloud as a true batch-update source: `IsGameListBatchUpdating()` returns true for PBC only, while Internet and Crossplay both receive trickled `MultiplayerGameListUpdated` entries. CAI therefore tracks its own refresh session from `RebuildGameList()` / `MultiplayerGameListClear` through `MultiplayerGameListComplete`, keeps the tree on a stable `Refreshing game list` placeholder for that whole window, and suppresses incremental rebuilds until the completion event. The focused empty-state row uses different `FocusKey` values for refreshing vs no-results (`empty:refreshing` / `empty:none`) so the tree manager treats `Refreshing game list` and `No games found` as distinct focus states and re-announces when the empty label changes under focus. Live add/remove updates after refresh still rebuild the tree. The persistent friends list mirrors the ChatPanel player-list shape: one always-visible `SubMenu` row per friend, with row speech including the live status and submenu children rebuilt from `BuildFriendActionList(...)`. PBC previous/next page requests are bound to panel-local `Alt+Left` / `Alt+Right` and call the vanilla backend offset refresh path.
- `UI/frontEnd/Multiplayer/StagingRoom.lua` CAI frontend replacement is implemented as a full vanilla-copy override with an accessibility block before `Initialize()`. The CAI panel is pushed from wrapped `OnShow()` only when it is not already on the manager stack, and it is removed only in wrappers around vanilla dequeue chokepoints: `OnHandleExitRequest`, `OnLeaveGameComplete`, `OnBeforeMultiplayerInviteProcessing`, and `OnGameSetupTabClicked`. The root shape is a single panel: player-slot `List`, ready/start action, writable `AlwaysEdit` chat entry that sends via `SendChat` on commit, chat-target `Dropdown`, chat-history `List` of static-text entries, read-only game-summary `EditBox`, Copy Join Code, Play By Cloud End Game and Quit Game, persistent friends `List`, and Game Setup. Player slots are editable `SubMenu` rows when any live slot control is visible/enabled; otherwise they are read-only `MenuItem` rows. Slot labels append ready status plus human/AI/observer/open/closed status, while slot tooltips now compute civ/leader, team, difficulty, and color from live player parameters and game state rather than from often-empty icon-only controls. The slot-type child dropdown uses the selected slot-type explanation as its tooltip, filters out vanilla's swap pseudo-option, and falls back to a disabled `Choose slot type` dummy option when no real slot types are available. Swap is exposed as a separate CAI button that calls vanilla `OnSwapButton(playerID)` under the same non-hotseat/non-PBC/non-matchmaking eligibility used by vanilla. The CAI copy-join-code action mirrors the live join-code text and now falls back to a CAI `Copy the join code to the clipboard.` tooltip when vanilla leaves the join-code text control without one. Real multiplayer chat delivered to the local client is recorded in the CAI chat history and spoken once, non-interrupting, from the wrapped `OnChat` path, including chat sent by the local player; vanilla lobby system chat lines for player connected, disconnected, host migrated, and kicked are also auto-spoken once. CAI also wraps `ChatPrintHelp(...)` so `/help` output is mirrored into CAI chat history as a local-only chat line and auto-spoken with multi-line `SpeakLines(...)`; the chat input tooltip falls back to `LOC_CHAT_HELP_COMMAND_HINT` when the live edit box has no tooltip. The history widget order follows the user's requested flow: chat input, chat target, then history, and the history rows are individual `StaticText` widgets rather than one read-only edit buffer. The leader child dropdown reuses the Advanced Setup `leader, civilization` labels plus leader/civ ability tooltip pattern, and the color child dropdown mirrors vanilla's manual color-option build. Submenu children preserve vanilla mechanics through `OnSlotType`, `OnTeamPull`, player parameter writes, live hotseat edit/kick/add-player callbacks, and `DoLeftClick()` where available. The persistent friends list matches the Lobby/ChatPanel submenu pattern: one always-visible `SubMenu` row per friend, with per-friend action children rebuilt from `BuildFriendActionList(...)` and refreshed on vanilla friend updates plus staging player-state rebuilds that can change invite eligibility.
- Current CAI staging-room hotseat coverage now includes the hotseat edit modal itself: the player-slot submenu still opens `EditHotseatPlayer` through the live vanilla modal path, but the modal is now mirrored by CAI and refreshes off `Realize()` so it stays in sync whether `StagingRoom_SetPlayerID` fires before or after the popup becomes visible.
- Shared UI-manager edit-box note: `EditBoxWidget` now exposes `CommitOnFocusLeave` and defaults it to `true` for writable `AlwaysEdit` parity. StagingRoom chat, `UI/inGame/ChatPanel_CAI.lua`, and the CAI end-game chat in `UI/inGame/EndGameMenu_CAI.lua` all set it to `false` so leaving the input field does not auto-send or auto-commit partially typed chat; Enter remains the explicit send path.
- `UI/inGame/EndGameMenu_CAI.lua` now mirrors the same structural chat pattern as `ChatPanel_CAI.lua` / staging room rather than the older history-editbox + Send-button layout. The CAI end-game chat keeps its own mirrored player-target state, syncs initial state from the live vanilla end-game pulldown label, writes target changes back through `ValidatePlayerTarget(...)`, `UpdatePlayerTargetPulldown(...)`, `UpdatePlayerTargetEditBox(...)`, `UpdatePlayerTargetIcon(...)`, and `PlayerTargetChanged(...)`, and rebuilds its history list from the live vanilla `Controls.ChatStack` on incoming chat, `/help`, and open. This keeps end-game chat behavior native while exposing the newer CAI input -> target -> history navigation order.
- End-game vanilla-parity audit (2026-07-26): the accessible Results tab carries the live ribbon result, victory type, winner/team or defeated civilization/player, and winner blurb; Ranking reproduces every `HistoricRankings` threshold, the player's inserted score/quote, and the resulting historic-leader statement; the live action mirrors preserve replay movie, Historic Moments, history export plus its shared accessible `PopupDialog`, Royale Observe, Main Menu, hotseat Next Player, and One More Turn. Scenario wildcard additions and `IsValidGraphDataSetToShow(...)` filters continue to govern the same controls and replay datasets.
- Vanilla Replay selects one visible `GameSummary` dataset, graphs every coalesced value from `GameConfiguration.GetStartTurn()` through the current turn for every eligible participant, preserves missing-value line breaks, and lets the user show/hide individual player lines (minor civilizations default hidden). Those legend checkboxes only call `pDataSet:SetVisible(...)`; they do not change replay data or gameplay.
- Two smaller semantic gaps remain. Vanilla marks a locally winning player/team with `LocalPlayerRim` / `LocalPlayerArrow`, but the accessible Results row does not explicitly append a localized "you" indicator (the Victory/Defeat ribbon usually makes the outcome inferable). In multiplayer chat, vanilla distinguishes team messages from global messages only through `ChatMessage_Team` color; the end-game CAI history scrapes the rendered text, so it does not announce that team-channel identity. Whisper messages retain their explicit target name. Leader portraits and decorative result/graph colors do not add unique mechanics, but the portrait's leader identity is also not named by CAI.
- Implemented accessible Replay design (2026-07-26): one Tree contains player nodes, each player contains only turns for which at least one allowed replay dataset has a non-nil value, and each turn contains the complete alphabetically sorted set of recorded dataset/value leaves. Data comes from the same visible, populated datasets and final `IsValidGraphDataSetToShow(...)` scenario filter as vanilla, using `GameSummary.CoalesceDataSet(...)` over the complete initial-to-final turn range. Numeric leaves use locale formatting with up to two decimal places instead of flooring the final value. Players with no recorded values and empty turns are omitted; a wholly empty replay uses vanilla's no-graph-data string. Stable player/turn/dataset focus keys and subtree capture/restore protect focus across rebuilds.
- The accessible Replay deliberately has no player checkboxes. All players with data remain available as collapsed top-level nodes, so reproducing vanilla's purely visual line-visibility toggle would only add filtering controls without exposing information or behavior.
- Hall of Fame game-detail graphs use a different persisted frontend API but can expose a player -> changed turn -> changed value hierarchy. `HallofFame.GetGraphs(gameId, ruleset)` returns graph tables with `MinX`, `MaxX`, `XUnit`, and dataset values indexed by turn. Game-scoped player datasets identify the owning player through `ds.ObjectId`; player-scoped city datasets belong to the graph context and their object records expose `PlayerObjectId`; game-scoped religion objects use the same owner link. For each dataset, CAI retains the first recorded value and then only values unequal to the last non-nil recorded value. A turn node is created only when at least one dataset changed for that player, and it contains only those changed messages. City and religion messages retain the entity name, values use locale formatting without integer flooring, empty turns/players are omitted, and stable object/turn/dataset focus keys protect rebuild position. The complete change tree remains eagerly constructed; do not replace it with lazy expansion. Read-only verification against the live Hall of Fame database confirmed that `GameObjects` exposes `PlayerObjectId`, all existing city/player/religion datasets have a resolvable owner path, and change filtering materially reduces the largest stored game's repeated samples.
- Hall of Fame parent/details lifecycle (2026-07-28): queuing `GameSummaries_GameDetails` at `PopupPriority.Current` hides the parent `GameSummaries` context. Vanilla `GameSummaries.OnHide()` destroys its instances and cache; when Details dequeues, vanilla `OnShow()` rebuilds the cache, calls `SelectRuleset(1)`, and calls `SelectTab(1)`. `SelectRuleset(1)` rebuilds History and `History_PopulateGames()` clears `g_SelectedGameId`. Vanilla therefore resets the ruleset to the first/default ruleset, returns its visual tab to Overview, and clears the selected history game. Its sort function and reverse-direction globals are not part of `DumpCache()` and persist. CAI keeps its Hall of Fame panel alive while Details is open, so every vanilla refresh path must synchronize that retained panel: wrapped `PopulateAvailableRulesets()` rebuilds the CAI options, wrapped `SelectRuleset()` silently selects the live ruleset and rebuilds Overview, wrapped `History_PopulateGames()` silently selects the live sort method and rebuilds History, and wrapped `SelectTab()` silently activates the same CAI page. Dropdown listeners are attached once during panel construction rather than on every options refresh. Details removes its CAI root with `RemoveFromStack(id, false)`, suppressing the obsolete restored History announcement; the synchronous vanilla `SelectTab(1)` refresh then moves and announces the final Overview focus directly. This makes the retained CAI state follow vanilla's complete return reset without duplicate callbacks, stale controls, or an intermediate History focus.
- Additional staging-room CAI behavior: for human-target swaps, the separate Swap button now reflects the local pending handshake state as `Swap, on` / `Swap, off`, speaks that state immediately when toggled, and still uses plain `Swap` for instant non-human swaps. Successful swaps are inferred from the local player ID changing across `OnPlayerInfoChanged(...)`; CAI then speaks `Swap successful` and moves focus to the new local slot only if focus was already somewhere inside the CAI player-slot list. Ready-state changes now announce `{player} ready` / `{player} unready`. The CAI ready button keeps the action label as `Ready` / `Unready`, but when a countdown is active its tooltip prepends the live countdown number and, if the button is currently focused, countdown refresh ticks speak the number only. Friend-submenu invite activation now also speaks `Invite sent`.
- Staging-room backend team IDs are zero-based. Player-facing CAI labels keep `TeamTypes.NO_TEAM` unchanged, but format real and newly available teams with `Locale.Lookup("LOC_WORLD_RANKINGS_TEAM", teamID + 1)` so they display as Team 1, Team 2, and so on.

## Sound

- `UI.PlaySound(soundKey)` — plays a UI sound effect
  - `"Main_Menu_Mouse_Over"` — standard hover sound

## Modding

- `Modding.IsModInstalled(modGuid)` — checks if a mod is active
- `Modding.IsModActive(modGuid)` — used by `Civ6Common.IsExpansion1Active`/`IsExpansion2Active`; `caiUtils.IsBetterTradeScreenActive()` uses it with the Better Trade Screen UUID `8d4fa23a-ef43-440c-8422-2bec11f8f5d7`.
- `Modding.CheckRequirements(mods, saveType)` — validates mod compatibility

## Better Trade Screen (mod) integration

Better Trade Screen (astog, UUID `8d4fa23a-ef43-440c-8422-2bec11f8f5d7`) is a full rewrite of the three vanilla trade contexts. It does not use `ReplaceUIScript`; its `<ImportFiles>` overrides the vanilla files under the same VFS leaf names (`TradeRouteChooser`, `TradeOverview`, `TradeOriginChooser`) plus a new shared `TradeSupport.lua`. So a CAI `include("TradeRouteChooser")` resolves to the BTS rewrite when the mod is active. The CAI trade `_CAI.lua` files branch on `IsBetterTradeScreenActive()`: when true they `return` after including a mod-specific layer (`TradeRouteChooser_BetterTradeScreen_CAI.lua`), which must be registered in both the top-level `<File>` list and `<ImportFiles id="CAIInGame">` because it is pulled by a runtime `include()`.

BTS API differences from vanilla (all break the vanilla wraps):
- Route chooser populates via `AddRouteToDestinationStack(routeInfo)`, not `AddCityToDestinationStack(city)`; there is no `GetOriginCity()` or `GetYieldsForRoute()`.
- Overview uses `AddRouteInstanceFromRouteInfo(routeInfo)`, `AddChooseRouteButtonInstance`, `AddProduceTradeUnitButtonInstance` (vanilla names without the `Instance` suffix are gone) and adds a group-by pulldown.
- Origin chooser `AddCity(cityID:number)` takes a city id, not a city table.

Shared `TradeSupport.lua` globals reused by the CAI layer (all non-`local`):
- `SORT_BY_ID` (FOOD=1..FAITH=6, TURNS_TO_COMPLETE=7, ORIGIN_NAME=8, DESTINATION_NAME=9), `SORT_ASCENDING=1`, `SORT_DESCENDING=2`, `START_INDEX`/`END_INDEX`.
- `InsertSortEntry(id, order, settings)` / `RemoveSortEntry(id, settings)` mutate a passed sort-settings table; `SortTradeRoutes(routes, settings)` returns a sorted copy (or the original when settings is empty).
- `GetYieldsForOriginCity(routeInfo, buildTooltip)` / `GetYieldsForDestinationCity(...)` return `(values, tooltips)` arrays keyed `START_INDEX..END_INDEX`. `GetAdvancedRouteInfo(routeInfo)` returns `pathLength, trips, turnsToComplete`. `GetRouteHasTradingPost(routeInfo)`.
- Automation: `AutomateTrader(traderID, isAutomated[, sortSettings])`, `CancelAutomatedTrader(traderID)`, `IsTraderAutomated(traderID)`. `RequestTradeRoute()` reads the live `Controls.RepeatRouteCheckbox`/`Controls.FromTopSortEntryCheckbox` and the selected destination.

CAI sort design: because BTS keeps its sort state in a file-`local` (`m_SortBySettings`) we cannot reach, the CAI layer owns its own settings table, mutates it directly with `InsertSortEntry`/`RemoveSortEntry`, and re-sorts the captured route list with `SortTradeRoutes` — no dependence on the visual sort bar or shift chording. The exposed sort keys are the six yields, `TURNS_TO_COMPLETE`, and `DESTINATION_NAME` (`ORIGIN_NAME` is omitted since the chooser has a single origin); `SortTradeRoutes`/`ScoreRoute` handle string score keys (descending via `invert_string`), so name sorting honors the direction button like the numeric keys. The "repeat the best route by your sort" automation is exposed as a panel button (not a checkbox): rather than binding to the inaccessible `m_SortBySettings`, it passes the CAI-owned `BuildCaiSortSettings()` to `AutomateTrader(traderID, true, sortSettings)`, so the trader keeps re-picking the top route by the CAI sort.

## Better Balanced Game (mod) integration

Better Balanced Game (iElden, primary UUID `cb84075d-5007-4207-b662-c35a5f7be240`; also historical `...be217` codenaugh and `...be231` beta) is ~98% gameplay data (`sql/`, `lang/`, `AddGameplayScripts`) — CAI reads live game state, so BBG's balance changes flow through unchanged. It has a handful of UI touchpoints, four of which register a `ReplaceUIScript` on a context CAI also claims (single-winner conflict); the `decompiled/` copy is BBG **7.4.6** (newer than the 4.8.3 build earlier notes described, which had fewer registered UI files):
- `EndGameMenu`: BBG rides the vanilla `include("EndGameMenu_", true)` wildcard (adds a victory style) and overrides `EndGameMenu.xml` (adds a domination ribbon). CAI's `EndGameMenu_CAI.lua` rides the same wildcard, so both coexist; BBG's XML keeps all vanilla control IDs CAI reads. No conflict, no change needed.
- `TradeRouteChooser`: BBG's `compatibility/traderoutechooser.lua` is exported via `<ImportFiles>` only, **never** bound to the context with a `ReplaceUIScript` — BBG leaves this screen to vanilla. No conflict, no change needed.
- `ProductionPanel`: BBG's `productionpanel_bbg.lua` overrides `GetData()` — ~95% a verbatim copy of vanilla, plus three splices: `MonkOverride()` removes the Warrior Monk from the normal build list when the `BBGTS_MONK_NERF` config is on (Monk becomes faith-purchase-only), and `BBGCanProduce()`/`BBGDeductDynamicDistrictPrereq()` let Water Mill/Palgum qualify on an Aqueduct/Bath/Dam (not just a river city-center) with a dynamic prereq district. The Water Mill logic is gated behind a local `bWatermil = false` in the shipped copy (dormant); only the Monk filter is live. Integrated exactly like WorldRankings below: vendored copy at `UI/Replacements/BetterBalancedGame/ProductionPanel_BetterBalancedGame_CAIBase.lua`, `ProductionPanel_CAI.lua` includes it when BBG is active. CAI wraps `View`/`PopulateList`/`PopulateGenericItemData` (not `GetData`), so it simply reads BBG's data — no ordering conflict.
- `TradeOverview`: BBG's `tradeoverview_bbg.lua` overrides only `ViewAvailableRoutes()` to regroup the Available Routes tab (city-states vs. civs by influence eligibility); no new data fields. Vendored copy at `UI/Replacements/BetterBalancedGame/TradeOverview_BetterBalancedGame_CAIBase.lua`, `TradeOverview_CAI.lua` includes it when BBG is active (ahead of the Better Trade Screen branch; BBG's base lacks BTS's `AddRouteInstanceFromRouteInfo`, so the BTS guard stays false and the vanilla-based CAI path runs). CAI already wraps `CreatePlayerHeader`/`CreateCityStateHeader`/`AddRoute`, which BBG's `ViewAvailableRoutes` calls by global name (late-bound), so the accessibility layer captures BBG's grouping. `compatibility/tradeoverview.lua` (BBG's other TradeOverview `ReplaceUIScript`) is a plain vanilla Firaxis copy, **not** the Better Trade Screen mod — no collision with CAI's `TradeOverview_BetterTradeScreen_CAI` path.
- `WorldRankings`: the original conflict. BBG registers a `ReplaceUIScript` on the same context CAI claims, and only one LuaReplace wins per context. CAI wins, so BBG's `worldrankings_bbg.lua` never loads as a context. **A runtime `include("worldrankings_bbg")` cannot reach BBG's file**: cross-context `include()` resolves only against files exported via `<ImportFiles>`, and BBG's `<ImportFiles>` block lists only `endgamemenu_bbg.lua` — `worldrankings_bbg.lua` is in BBG's `<Files>` (plain VFS) and its losing `<ReplaceUIScript>` only. (A top-level `<File>` entry alone does **not** make a Lua file include-able from another mod's context; that was the original bug — the failed include left every base WorldRankings global nil, so the first unguarded `WrapFunc` crashed the context and the screen would not open.) Resolution mirrors the CivRoyale/scenario `_CAIBase` vendor pattern: CAI ships a **verbatim copy** of BBG's file at `UI/Replacements/BetterBalancedGame/WorldRankings_BetterBalancedGame_CAIBase.lua`, registered in CAI's own `<Files>` + `<ImportFiles id="CAIInGame">`, and `WorldRankings_CAI.lua` (after detecting BBG via `Modding.IsModActive` over the three IDs, ahead of the expansion branches) does `include("WorldRankings_BetterBalancedGame_CAIBase")`. The vendored copy still does BBG's own base/XP2 split internally. Re-copy when BBG updates that file.

What BBG adds to WorldRankings and how CAI accounts for it (all in `WorldRankings_CAI.lua`, gated on `m_isBBG`):
- BBG adds a **Traditional Domination** custom victory tab (`VICTORY_TRADITIONAL_DOMINATION`), rendered through the shared GenericView. CAI's `AddTab`/`AddExtraTab`/`BASE_AddTab` wraps already capture it and route it to `RebuildGenericTree`.
- Its tab label is the **button tooltip key** `LOC_TOOLTIP_TRADITIONAL_DOMINATION_BUTTON`, not `row.Name` — so `IdentifyGenericVictoryTypes` needs a bespoke branch (same shape as the diplomatic special case) to resolve `tabDef.victoryType`; without it the tab has no victory type and its generic data never binds.
- BBG renders rows through its own `PopulateTradDomInstance` / `PopulateTradDomTeamInstance`, **not** the vanilla `PopulateGenericInstance`/`...TeamInstance` that CAI's capture wraps — so those wraps are mirrored onto the `PopulateTradDom*` globals (guarded; they exist only under BBG) to harvest each row's progress-% Details and the tile-count tooltip. The tab is driven by `ViewTraditionalDomination` (not `ViewGeneric`), which is wrapped for `BeginGenericCapture`/`EndGenericCapture` + `ViewSync`.
- BBG's Details already merge domination progress with requirement text, so for this victory type `CreateGenericPlayerRow` skips CAI's own requirement recomputation (which would drop the percentage and duplicate the requirement text) and defers to the captured Details.
- The required domination threshold lives in BBG's header (`TRADITIONAL_DOMINATION_LEVEL` + `LOC_WORLD_RANKINGS_TRADITIONAL_DOMINATION_DESC_LAST`); CAI appends it to the tab's advisor leaf to match. All BBG loc keys resolve at runtime since BBG ships them in every language — no new CAI tags.
- Table view mode: the tab has no `tableKind`, so it gracefully falls back to the generic tree in both modes, same as any non-diplomatic custom victory.

## Game State

- `GameConfiguration.GetGameState()` — returns current state
- `GameStateTypes.GAMESTATE_PREGAME` — pre-game state constant

## Network

- `Network.LeaveGame()` — leaves current network session
- `Network.LoadGame(saveData, serverType)` — loads a save file
- `Network.GetFriends()` — returns friends API object
- `Network.GetNetworkPlatform()` returns a `NetworkPlatform` enum; observed values are `NETWORK_PLATFORM_STEAM` and `NETWORK_PLATFORM_EOS` (Epic Online Services). Vanilla uses it in 15+ front-end contexts (MainMenu, Lobby, StagingRoom, MultiplayerSelect, My2K, PBCNotifyRemind). NOTE: it reports the active network transport, not the installed store build — vanilla's own Lobby computes `eosAllowed = (GetNetworkPlatform() == EOS) and IsUsingInternetGameList()`, so a Steam copy returns EOS while browsing the crossplay list. Do NOT use it to detect "is this the Epic build"; prefer feature detection (`if UI.SomeFn then`), which is what the CAI Epic guards use.

## Epic Games Store build differences

The Epic build ships an OLDER game version than Steam and omits the online 2K Challenge / Game-of-the-Month system. This matters because the mod ships Steam-based full copies of front-end/shared screens but replaces only the Lua context (not the XML), so on Epic the Steam Lua runs against Epic's older XML/runtime. Findings from diffing the Epic base `UI/` folder against the Steam `Base/Assets/UI` install:

- Every **in-game** replaced screen is byte-identical Epic vs Steam. Only front-end/shared full-copy screens diverge.
- `MainMenu.xml` on Epic omits ALL Challenge carousel controls (`ChallengeStack`, `ChallengeIndicatorStack`, `ChallengeContainer`, `ChallengeScroll`, `ChallengeLButton`, `ChallengeRButton`) and both instance templates (`CarouselEntry`, `CarouselEntryIndicator`). The Steam-based Lua's `InstanceManager:new(..., Controls.ChallengeStack)` at load and its per-frame `OnUpdate` carousel block index nil controls → aborts the menu. CAI gates all carousel logic behind `m_hasChallengeCarousel = Controls.ChallengeStack ~= nil`. (Separate pre-existing Epic issue: Epic's MainMenu.xml also omits the `<MakeInstance Name="PopupDialog"/>`, handled by the `Controls.PopupRoot` guard that builds the instance via `ContextPtr:BuildInstance`.)
- The `Challenges` Lua global's presence on Epic is unconfirmed; guard it (`Challenges ~= nil`) or gate on challenge data (`g_CurrentRuleset.ChallengeIds`, which is nil on Epic).
- Three newer Lua APIs are used by Steam's Lua but avoided by Epic's vanilla (Epic uses older equivalents), so likely absent on Epic — feature-detect and fall back: `UI.GetNetworkVersion` → `UI.GetAppVersion`; `Events.SetGameEntryMethod` (launch telemetry) → skip; `HallofFame.GetLocalizedVictoryTypeText(name)` → `LocalizeAndSetText(name)`.

## City Banner XP1 / XP2

- `Civ6Common.lua` exposes `IsExpansion1Active()` and `IsExpansion2Active()`, and CAI can include it directly in in-game UI helpers when behavior needs to branch on Rise and Fall / Gathering Storm.
- Base `CityBannerManager.lua` stores local-city growth and production speech data on top-level controls such as `m_Instance.CityPopulation`, `m_Instance.CityProduction`, `m_Instance.CityPopulationMeter`, and `m_Instance.CityProductionMeter`.
- Expansion 1 and Expansion 2 move those reads into stat-row instance managers instead: `m_StatPopulationIM` allocates a `CityStatPopulation` row whose tooltip lives on `FillMeter`, and `m_StatProductionIM` allocates a `CityStatProduction` row whose tooltip lives on `Button` and whose progress meter lives on `FillMeter`. CAI hotkey reads must support both layouts.
- Expansion 1 and Expansion 2 also move governor info into `m_StatGovernorIM`, with governor detail tooltips on each row `FillMeter`.
- Expansion 1 and Expansion 2 shift city-state quest / trading-post status and city effect icons into instance-manager rows: `m_DetailStatusIM` for status icons such as quests and trading posts, and `m_DetailEffectsIM` for effects such as siege, occupied, housing, amenities, and XP2 power.
- `CityBannerManager` expansion loyalty data can be rebuilt from live `city:GetCulturalIdentity()` calls without the loyalty lens. Useful methods are `GetLoyalty()`, `GetMaxLoyalty()`, `GetLoyaltyPerTurn()`, `GetPotentialTransferPlayer()`, `GetPlayerIdentitiesInCity()`, and `GetIdentitySourcesBreakdown()`.
- Expansion loyalty mini-panel control names are stable enough for CAI tree labels/tooltips: `LoyaltyInfo.LoyaltyPercentageLabel`, `PopulationTop`, `GovernorTop`, `Happiness`, `OtherTop`, `CityStateTop`, `FreeCityTop`, and `IdentityBreakdownStack`.
- GS power banner text can be mirrored from `city:GetPower()` with vanilla loc keys. Firaxis uses `GetFreePower()`, `GetTemporaryPower()`, `GetRequiredPower()`, `IsFullyPowered()`, and `IsFullyPoweredByActiveProject()` together with `LOC_CITY_BANNER_POWERED_CITY`, `LOC_CITY_BANNER_POWERED_CITY_FROM_ACTIVE_PROJECT`, and `LOC_CITY_BANNER_UNPOWERED_CITY`.

# CAI plot-based raw audio

- `CAI_AudioDefinitions.IsPositional` marks raw sounds that must be played through `CAIAudioManager:PlayAtPlot(...)` / `QueueSoundAtPlot(...)`; ordinary `Play(...)` / `QueueSound(...)` reject them so a plot-based handle cannot reuse stale plot parameters. `SCANNER_BEACON` resolves `UI/scanner_beacon.wav` under the `BEACONS` tag and is the first such definition. `CAI_AudioDefinitions.RelativePath` values are resolved beneath the `SOUNDS_ROOT` folder (`"sounds"`), a variable in `audioManager_CAI.lua`, so definition paths no longer include the `sounds/` prefix.
- Plot-based playback deliberately does not use the DLL spatialization system. The audio manager never enables sound spatialization and never sets source/listener position, direction, velocity, or attenuation model. There is no spatial initialization lifecycle. `WorldInput_CAI.lua` includes `hexCoordUtils_CAI` directly for the in-game plot math, while the shared audio manager and load-screen sounds continue initializing normally.
- `CAIHexCoordUtils.displacement(...)` applies nearest-map-wrap folding and odd-row half-column correction. `plotAudioParameters(...)` maps that displacement to ordinary per-handle properties using the Civ V beacon model: pan is `clamp(dcol / 12, -1, 1)`; pitch is one semitone per row capped at one octave through `2 ^ (clamp(drow, -12, 12) / 12)`; volume fades linearly from 1 to 0 over the requested maximum `Map.GetPlotDistance(...)`, defaulting to 30 hexes.
- `PlayAtPlot(soundId, sourcePlotOrId, options)` accepts a Plot or plot id. `options.ListenerPlot` may supply a Plot/id scan origin; otherwise the manager resolves the current CAI cursor live. `options.MaxDistance` overrides the 30-hex default. Active plot sounds recompute ordinary pan, pitch, and volume from live plot ids during the manager update. Queued plot sounds cache ids, not Plot objects, and resolve the default cursor listener at dispatch time.
- Unless `SkipIfPlaying` is explicitly requested, `PlayAtPlot(...)` stops an already-playing handle, applies the new plot's ordinary pan/pitch/volume after that stop, and then calls `PlaySound(...)`. The order matters because stopping may reset per-handle parameters.
- Effective plot volume is the game's live master-volume gain multiplied by the sound's base gain, live tag-setting volume, and current plot-distance gain. `SetSoundVolume(...)` changes the per-sound base gain, so later master, tag, or cursor updates preserve all four components.
- World Scanner item focus plays `SCANNER_BEACON` at the current item's plot, falling back to the current group's plot when needed. Direct item/group cycling, category focus, subcategory focus, search-result injection, and configured auto-focus categories share the same focus helper. Playback captures the current CAI cursor plot as an explicit listener before `ScannerAutoMoveCursor` can snap the cursor to the result, preserving the directional cue instead of immediately recentering it. Silent focus restoration during ordinary rebuild/resort does not replay the beacon.
- `ScannerBeaconEnabled` is a World scanner checkbox setting, enabled by default and sorted before the other scanner settings. `AudioTagVolume_BEACONS` is a 0–100 slider immediately after it and controls the inherited volume of the `BEACONS` audio tag. The shared scanner beacon helper reads the enabled setting live, so disabling it suppresses item-focus beacons, End-key beacon replays, and volume previews without suppressing scanner speech.
- When `AudioTagVolume_BEACONS` changes, World Scanner reapplies audio settings and previews `SCANNER_BEACON` with the source and listener both at the current cursor plot. This produces centered pan, neutral pitch, and full distance gain so the preview demonstrates only the newly selected tag volume.
- `CAIWorldScanner:SpeakCurrentDirection()` is the scanner End-key repeat path. After successfully speaking the current item's direction, it calls the same positional beacon helper so End repeats both the verbal and audio direction. An unavailable/plotless current item retains the existing unavailable speech without playing a beacon.

# CAI navigation wrap audio

- `CAIWidgetHelpers_Navigation.Navigate` can determine a real wrap by comparing the resolved visible-child index with the focused starting index. It emits `navigation_wrap` only after a wrapped focus move succeeds.
- `TabControlWidget:NextPage` / `PreviousPage` emit the same event when Ctrl+Tab page cycling crosses the first/last page boundary. Left/Right tab-strip wrapping uses the generic container path.
- `UIScreenManager:CreateWidget` installs the default event listener and plays raw audio definition `UI_MENU_WRAP` at half the live `UI_NAVIGATION` tag volume; widgets remain responsible only for reporting the navigation event.
- The non-widget world scanner reports category, subcategory, group, and item boundary crossings to the same `UIScreenManager:HandleNavigationWrap(...)` policy method. Initial group or item selection from index `0` is not a wrap.

## World scanner city grouping and apparent distance loss

- The Cities category does not apply a cursor-distance cutoff. It scans every plot revealed to the local observer and validates city centers with `Cities.GetCityInPlot(x, y)`.
- `ScannerGroupCitiesByCivilization` defaults to enabled. Enabled uses `GroupId = "player:" .. ownerID` and the owning-player label; disabled uses `GroupId = "city:" .. ownerID .. ":" .. cityID` and the city label. Groups and their items remain nearest-first relative to the current cursor.
- Cities are classified only by the implicit All subcategory and the relationship subcategories My, City-States, Neutral, and Enemy. Barbarian outposts use a separate subcategory and retain one shared outpost group.

## World scanner zones

- Civ V Access represents a contiguous geography or unexplored region as one scanner instance containing a list of plot indices. Six-neighbor union-find clustering builds the regions, a stable region id is derived independently from the current representative, and the representative is re-resolved to the member nearest the cursor before direction speech or a jump. Unexplored validation also removes cells that have become revealed and drops the instance when none remain.
- Civ VI implements this through `WorldScannerZoneUtils.lua`. Zone items retain their complete `ZonePlotIndices`, a stable component id based on the lowest member plot id, and an optional live member validator. Category construction and `RefreshCategorySort(...)` select the member nearest the captured sort origin; the common current-item boundary reselects from the live navigation cursor and updates the leaf before speech, beacon, auto-move, End, or explicit jump.
- The zone contract survives implicit All duplication, explicit subcategories, custom-category projection, and search cloning because those leaves retain the source item and its member list. Live resolution prunes invalid members and removes an empty zone through the scanner's existing stale-item path.
- Every scanner item backed by `ZonePlotIndices` uses the shared tile-count label policy. Ordinary regions append only the number of currently revealed members as `<N> tiles explored`; unexplored components use `<N> tiles unexplored`. Regions whose complete shape is official player information opt into `<revealed> of <total> tiles explored`; currently this is used by the Pirates search/path regions and Civ Royale safe-zone/Red Death/fallout regions, whose visual overlays disclose the whole region regardless of exploration. Geography adds its revealed count before disambiguating duplicate landmass or ocean labels, so a different explored size breaks the tie before the fixed direction-from-home suffix and final ordinal fallback. It never exposes the hidden engine-area total.
- Geography landmass and ocean zones append `, fully revealed` (localized `LOC_CAI_WORLD_SCANNER_ZONE_FULLY_REVEALED`) once the whole engine area is charted. Civ VI exposes contiguous-connectivity `Area` objects (the analog of Civ V's `Area`, distinct from geological `GetContinentType()`): `Plot:GetArea()` / `Plot:GetAreaID()` (Gameplay + UI context) and `Area:GetPlotCount()` (UI context) returns the area's total tile count including fogged tiles; `Area:IsWater()` splits land from water areas. There is no Civ VI equivalent of Civ V's `Area:GetNumRevealedTiles`, but it is unnecessary: a geography zone is a connected cluster of revealed plots inside one area, so `#zoneCells == plot:GetArea():GetPlotCount()` is an exact fully-revealed test (a fully revealed contiguous area cannot be fog-split, so it forms a single cluster equal to the whole area). The total is only compared, never spoken, so it leaks no hidden count. `ZoneUtils.IsAreaFullyRevealed` / `ZoneUtils.WithFullyRevealedSuffix` implement this; `revealedOfTotal` zones (Pirates, Civ Royale) instead gate the suffix on `revealed >= total` members. Note: no shipped Firaxis UI file calls `GetArea`/`GetPlotCount` (only map-gen scripts do), but the IDE helpers annotate them UI-context.
- Geography has a Natural disasters subcategory (Gathering Storm only, gated on `IsExpansion2Active()`). Active hazards are read per revealed plot during the shared map pass — matching `PlotToolTip.lua`, which gates its storm/drought/eruption details on `IsRevealed` (not visibility), so anything the plot tooltip shows the scanner shows — and grouped into zones: storms by `GameClimate.GetActiveStormIDAtPlot(x,y)`, droughts by `GetActiveDroughtIDAtPlot(x,y)`, erupting volcanoes per plot. `GameClimate.GetActiveStormTypeAtPlot(plot)` / `GetActiveDroughtTypeAtPlot(plot)` return a `GameInfo.RandomEvents` index (or -1) whose `.Name` is the localized disaster name; `GetDroughtTurnsAtPlot(plot)` gives drought turns. Volcanoes are listed per revealed plot regardless of state via `MapFeatureManager.IsVolcano(plot)` (permanent map feature), with status resolved as erupting (`IsVolcanoErupting(plot)`) > active (`IsActiveVolcano(plot)`) > inactive, and `GetVolcanoName(plot)` supplying the name when the volcano has one. Note: the IDE helpers for `IsVolcano`/`IsActiveVolcano`/`IsVolcanoErupting`/`GetVolcanoName` omit the `Plot` argument, but vanilla `PlotTooltip_Expansion2.FetchData` passes `pPlot` to each — the plot arg is required. These mirror the vanilla plot-tooltip/`MapSearchPanel_Expansion2` data set, so no non-vanilla info is exposed. Within the subcategory, items are split into one group per hazard type (`GroupId` `disasters:storms` / `disasters:droughts` / `disasters:volcanoes`) via `GroupOrderBySubCategory[SUBCATEGORY_DISASTERS]`, so storms, droughts, and volcanoes are not interleaved. Disaster items set `ZoneTileCountEmbedded` (no tiles-explored suffix) and a `ZoneValidatePlot` that re-checks reveal plus live disaster/volcano state so moved/expired disasters prune between rebuilds (a volcano stays valid while its plot is revealed).
- `WorldScannerCategoryDefinition.EndExtract` is the post-extraction/finalization hook. Terrain and Geography collect classified plot ids during the shared map pass and partition them afterward; active-lens scanners partition their authoritative `Scan(...)` results directly.
- Geography clusters only revealed plots by six-neighbor adjacency, separately for land and non-lake water. It does not group by Civ VI `GetContinentType()` or reveal the engine's complete continent plot lists: disconnected revealed patches remain separate until the player reveals their connection.
- Unexplored zones match Civ V's grouping and presentation semantics: all unrevealed plots participate regardless of land/water or hidden terrain type, six-neighbor adjacency produces one instance per connected fog component, the minimum member plot id supplies the stable component suffix, and the actionable representative is the surviving member nearest the live cursor. They appear under Terrain/Base with their current hidden-tile count. Before use, live resolution removes newly revealed members, refreshes that count, and prunes an empty instance; a rebuild is authoritative for splits and merges.
- Terrain retains Civ V's independent Fresh Water subcategory. Every revealed plot whose live `plot:IsFreshWater()` result is true appears once under the localized fresh-water group, including mountain or feature plots that also appear in another Terrain subcategory. These remain individual plot instances rather than connectivity zones because they answer the precise “where can I access fresh water?” query.
- The existing Continent active-lens scanner can zone `Map.GetVisibleContinentPlots(continentId)` by continent id plus revealed adjacency. Separate visible patches of the same named continent remain separate instances until exploration connects them, while each instance retains the official continent label.
- The Political / `OwningCiv` lens can zone revealed owned plots by owner id plus adjacency. Adjacent territory of one civilization becomes one instance; disconnected islands, colonies, and enclaves remain separate instances. Existing met-player masking and My/Neutral/Enemy classification remain unchanged.
- The Government lens must not zone only by government type, because adjacent civilizations using the same government would merge and lose their owner/city identity. Use the semantic identity—government, owner, and purchasing city—as the zone-classification key, then split that set by adjacency. Scanner groups may still use government type: each group then contains the separately zoned government/civilization/city items without merging their plots.
- Lens-zone validation should prune members whose revealed state or live classification changed and remove an empty component. Newly revealed territory, ownership transfers, government changes, and component merges/splits remain authoritative on the next active-lens rebuild; nearest-target resolution remains live between rebuilds.
- Power-lens regional coverage uses `city:GetPower():GetPlotsCoveredByRegionalPower()` as its authoritative source. Revealed covered plots are partitioned by city and six-neighbor adjacency, so overlapping ranges from different cities retain their city identity while each connected range becomes one nearest-target zone. Live validation re-resolves the city through `CityManager.GetCity(ownerID, cityID)` and removes plots no longer covered by that city's regional power.
- Gathering Storm exposes official named-place territories through `Territories.GetTerritoryAt(plotIndex)`. `MapLabelManager.lua` groups plots by `territory:GetID()`, reads `territory:GetName()`, and classifies the territory with `GetTerrainClass()`, `IsLake()`, and `IsSea()`. Its named territory layers cover deserts, mountain ranges, lakes, oceans, and seas; `Expansion2_NamedPlaces.xml` supplies `NamedMountains`, `NamedDeserts`, `NamedLakes`, `NamedSeas`, and `NamedOceans` plus civilization-specific name pools.
- Civ V Geography's “ocean” is not a named region: it is each connected component of revealed non-lake water, and lakes are excluded entirely. Gathering Storm water labels instead partition `TERRAIN_CLASS_WATER` territories into lakes, seas, and oceans. A single connected body can therefore contain an ocean territory plus several named sea territories, and `MapLabelManager.lua` may repeat one very large water-territory label at several positions. Official water-territory names cannot safely replace a connected Geography zone's identity one-for-one; either retain connected bodies with generic labels, compose their revealed territory names, or expose named-water territories as a separate scanner view.
- Civ V Terrain separately emits every revealed water plot as its individual base terrain: Coast and Ocean use their ordinary terrain descriptions, while a coast-terrain plot with `plot:IsLake()` is relabeled Lake. Civ VI Terrain follows the same per-plot model. Its special case relabels `TERRAIN_COAST` plus `IsLake()` as Lake, retains non-lake Coast, and lets `TERRAIN_OCEAN` use its official terrain name. This intentional overlap answers a different query from Geography's connected water-body zones.
- Geography retains the connected revealed non-lake-water zone as its actionable identity. Under Gathering Storm, its label is composed from the unique revealed non-lake water territories in that zone: use all official ocean names in localized order when any are present, otherwise use all official sea names, and fall back to localized `Ocean` when no named territory is available. Named seas are intentionally omitted when the same connected zone already exposes an ocean name so global water connectivity does not produce an impractically long label. Outside Gathering Storm, the territory API is never touched and every zone uses the generic label.
- Mountain ranges can therefore use the same scanner-zone target resolver while preferring Gathering Storm's authoritative territory membership and localized `GetName()` value. Defensively partition a territory's revealed mountain plots by adjacency before emitting scanner instances, so a malformed or modded disconnected territory cannot produce a noncontiguous jump zone. In rulesets without the Gathering Storm territory API, fall back to adjacency-clustered revealed mountain plots with a localized generic range label.
- Gathering Storm does not define or display named landmass territories. Its continent label path is separate: `RefreshContinents(...)` gets every plot for a `GameInfo.Continents` id, partitions disconnected regions, and prints the official continent name on sufficiently large regions. Civ VI continents are geological types and can divide one connected landmass; the same continent id can also have disconnected visible patches. A continent label must not be presented as the landmass's authoritative name.
- Prefer descriptive landmass labels over bare ordinals. Build them only from revealed information: mark the component containing the player's capital/start anchor as `Home landmass`; append the revealed Civ VI continent names represented in the component (for example, `Home landmass, Africa and Asia` or `Landmass, Africa`); and use a fixed capital/start-relative direction only when two components would otherwise have the same label. Numbering should be a final collision fallback, not the primary identity. Labels may legitimately gain a continent name or merge when exploration reveals more of the component.

## World scanner category-switch performance

- `CAIWorldScanner:CycleCategory(...)` calls `self:Rebuild()` before finding the next available category. This recreates every category slot and calls `Core.BuildAllCategories(...)`; a category switch therefore rebuilds the complete scanner rather than only the destination category.
- The shared plot pass invokes every eligible plot extractor separately. With the current eight plot-extractor definitions, a fully revealed 6,000-plot map still makes about 48,000 extractor invocations, but extraction is protected once per category rather than wrapping every individual invocation in `pcall`.
- The full rebuild snapshots plot references and reveal state into parallel arrays in one map pass. Every extractor reuses that snapshot, and terrain receives the cached reveal flag for its hidden-plot split. This avoids repeated visibility calls without caching scanner results between rebuilds.
- Category construction still creates separate leaves/groups for implicit `All` and explicit subcategories, but every raw item is now validated once before either view is built.
- Distance and resolved-label sort keys are computed once per validated raw item. Item and group comparators perform pure-Lua key comparisons instead of calling `Map.GetPlotByIndex(...)`, coordinate getters, `Map.GetPlotDistance(...)`, and `Locale.Lookup(...)` at `O(n log n)` comparison frequency.
- Cursor-driven resorting recomputes distance once per unique plot in the current category, updates the duplicated leaves, and then uses the same pure-Lua comparators.
- Each extracted item commonly allocates a table and a closure-backed `Validate` function. Rebuilding all categories discards and recreates those objects, and construction then creates additional leaf, group, and subcategory tables. The resulting allocation and garbage-collection pressure grows with revealed plots and late-game objects.
- Non-plot categories also run during the same rebuild. Unit scanning is shared within one scanner context across My, Neutral, and Enemy categories, but still enumerates every alive player's units and validates the filtered results. Other categories independently query map tacs, waypoints, recommendations, and any currently active dynamic mode or lens.
- Project decision: category switching must continue rebuilding the complete scanner; do not introduce category caching or dirty-category state.
- The full-rebuild optimizations above preserve current category contents, validation, nearest-first ordering, and per-category extraction failure isolation. They do not introduce category caches or dirty state.
# CAI shared dropdown focus restoration

- Dropdown option rows use the widget-owned focus key `<dropdown focus key or id>:option:<index>`, preferring the dropdown's stable `FocusKey` over its runtime `Id`. This distinction matters in `HostGame.lua`, where parameter dropdowns have generated widget IDs on every rebuild but stable `param:<parameter id>` focus keys; using the generated ID makes refresh restoration miss and fall back by path, which can announce a bare numeric option label. Building any focus path to a dropdown descendant silently opens its dropdown ancestor, matching the manager's existing behavior for descendants of collapsed trees and submenus. During `CaptureFocusKey` / `RestoreFocus`, this preserves the open dropdown and option position across passive parent-list refreshes such as staging-room player-state updates and Play By Cloud HostGame parameter refreshes.
- Dropdown commit is distinct from passive refresh: it must write through the selected value before closing so the closed control reads live state, but the setter may synchronously rebuild while focus remains on an option. During `Commit(...)`, `FocusRestoreKeyOverride` makes capture prefer the outer dropdown's stable key while the setter and `value_changed` handlers run. The rebuild therefore restores to the closed replacement dropdown instead of reopening the option row; ordinary passive refresh still restores inside an open dropdown.
## EventPopup behavior and accessibility

- `Assets/UI/Popups/EventPopup.lua` is the base context for data-driven scenario events. There are no DLC Lua replacements for this context in the decompiled game files, so a partial `EventPopup_CAI.lua` replacement can include `EventPopup` directly.
- Requests arrive through either `Events.EventPopupRequest` or `LuaEvents.EventPopupRequest`. Requests for another player are ignored; valid requests are shown at `PopupPriority.Medium`, and later requests are deep-copied into an internal FIFO queue until the current popup closes.
- A popup always exposes its data-row title, optional request/data-row description, request/data-row effects, and optional image caption. One-shot events show Continue plus optional policy, building, unit, and free-form effect unlock icons. Choice events hide Continue and show `ChoiceA` / `ChoiceB`; each choice may have its own unlock icons.
- Each unlock icon's authoritative readable text is the tooltip passed through `AddUnlockIcon(...)`. Type-based unlocks obtain that tooltip from `ToolTipHelper.GetToolTip(typeName, Game.GetLocalPlayer())`; free-form effect unlocks pass their supplied text directly. CAI captures those exact tooltip strings while vanilla populates the current popup instead of reconstructing their displayed descriptions.
- Choice activation sets zero-based `ResponseIndex`, sends `EVENT_POPUP_RESPONSE`, resets the choice controls, and closes. Escape and Return close one-shot events but deliberately do nothing in choice mode. The accessible choice dialog must therefore have no dialog-wide default Enter action; Enter activates a choice only while that choice button has focus.
- `OnClose()` dequeues the current popup and immediately calls `ShowNextQueuedPopup()`. Accessible cleanup must occur before that transition so the next queued event replaces the old CAI dialog without leaving a stale manager-stack entry.
- The decompiled official content contains A/B `EventPopup` definitions only in the Black Death scenario. Its four choice events are `Sick in Body and Soul` when plague is active in at least half the player's cities, `Physician, Heal Thyself` after at least 10 total plague deaths, `Is This God's Judgement?` after at least 30 deaths and again after the scenario counter reaches 15 more deaths, and `The End of the World` after at least 45 total deaths. The first, third, and fourth choices expose free-form effect tooltips; `Physician, Heal Thyself` exposes a Plague Doctor versus Flagellant unit unlock. Australia uses the same popup only for Continue-style exploration and political events, and no other decompiled DLC Lua constructs `ChoiceAText` / `ChoiceBText`.

## Vanilla Gathering Storm World Congress voting flow

- `DLC/Expansion2/UI/Additions/WorldCongressIntro.lua` opens a blocking introduction on `Events.WorldCongressStage1`. The visible action is `Begin Voting`; clicking it dequeues the intro and fires `LuaEvents.WorldCongressIntro_ShowWorldCongress`, which opens `WorldCongressPopup` at stage 1, phase 1.
- A normal-session ballot is a two-phase popup. Phase 1 calls `PopulateResolutions()` and displays each resolution as one card with outcome A and outcome B, each with its own `VotingWidget`, followed by a target pulldown for the currently selected outcome. The voting widget's up control adds a vote and its down control removes one; these are not for/against controls. Clicking the first add-vote control selects A or B and reveals that outcome's target pulldown. Switching outcomes clears the former target and zeroes the other outcome's votes.
- The player must give every resolution at least one vote and select both an outcome and a target. `CanMoveToNextPhase()` keeps `Next` disabled until all three conditions are true for every resolution. The tooltip says `You must vote on all submitted Proposals.`
- Player targets use a leader/civilization pulldown; non-player targets use an alphabetically displayed pulldown of the resolution's possible targets. The stored target remains the game-core index rather than the displayed sort index.
- The first vote on a resolution is free. Additional add/remove controls show the incremental Diplomatic Favor cost/refund; the popup displays remaining Favor at the top and the total resolution cost in the resolution category heading.
- Clicking `Next` moves to phase 2, where `PopulateSummary()` presents the chosen outcomes, targets, and votes for review. `Previous` returns to the editable ballot. `Submit` calls `OnAccept()`, sends one `WORLD_CONGRESS_RESOLUTION_VOTE` operation per voted resolution, then `WORLD_CONGRESS_SUBMIT_TURN`, requests end turn, and disables submission.
- Outside hotseat, submission closes the ballot and opens `WorldCongressBetweenTurns`, which shows every Congress member as waiting/submitted. After resolution processing, `OnWorldCongressResults(...)` reopens `WorldCongressPopup` at stage 4 in results/review mode; its tabs include Last Session Results, Active Effects, and Available Proposals when special-session proposals exist.
- The accessible World Congress action tutorials are contextual rather than screen-root tutorials: stage 1 phase 1 explains resolution outcome, target, and vote entry; stage 2 phase 1 explains support/oppose voting on special-session proposals; stage 4 Available Proposals explains selecting an emergency to submit for a future session. Checks are deferred to the next context update after a body rebuild or tab change so the tutorial is the final focus-affecting operation. Closing the popup clears any pending check.
- CAI intentionally keeps one pending target per resolution hash rather than per outcome because vanilla populates both outcome pulldowns from the same `PossibleTargets` list and submits only one `PARAM_RESOLUTION_SELECTION`. The CAI target dropdown remains available before outcome/vote selection, its setter changes only pending CAI state, and the CAI Next action writes all valid pending targets into the captured vanilla resolution choices immediately before invoking vanilla Next. Switching A/B therefore preserves the pending target even though vanilla's `OnVoteResolution(...)` clears its own target when the outcome changes.
- The CAI phase-1 Next button cannot mirror `Controls.NextButton:IsDisabled()` while targets are pending outside vanilla. Its live disabled predicate and tooltip validate the CAI outcome and pending target for every normal resolution; it does not add a separate normal-resolution vote blocker because committing an outcome automatically supplies the free first vote. Actionable special-session proposals still validate their required explicit vote. On activation Next commits pending targets, calls vanilla `UpdateNavButtons()`, then clicks the now-valid vanilla Next control.
- World Congress `ResolutionItem` instances all expose the XML root ID `Root`; it is not a unique instance identity. CAI resolution tree rows therefore use the resolution definition hash for `FocusKey` (`res:<hash>`). Using `instanceRoot:GetID()` produces duplicate `res:Root` keys, causing cached tree re-entry to resolve to the first resolution regardless of the previously focused row.
- World Congress stateful widgets follow the shared value-binding contract. Outcome and target dropdowns use `SetValueSetter(...)` for their pending CAI state; silent `SetSelectedIndex(..., true)` is initialization only. Emergency-proposal checkboxes silently seed from `SelectBox:IsSelected()`, bind changes through `SetValueSetter(...)`, and click vanilla only when its live selection differs from the requested value. Do not use `value_changed` as a substitute for a backing setter or retain empty `value_changed` handlers.
- Committing outcome A or B through the CAI resolution dropdown first updates CAI's selected outcome, then clicks that outcome's live vanilla up-vote control when its vote count is zero. Vanilla clears the other outcome while switching, so this commits the resolution's free first vote without duplicating vanilla vote state; recommitting an outcome that already has votes must not add a paid vote. The placeholder option uses vanilla `LOC_WORLD_CONGRESS_TT_NO_VOTING_DOWN`, the same localized first-vote guidance shown on the initial resolution vote button.

## UI-opening input-action eligibility

- CAI's `UI_CAIOpen*` actions delegate to vanilla toggle actions only after checking the same live gate used by their owning HUD context. A failed open must call `Speak(Locale.Lookup(...))`; silently returning leaves a screen-reader user unable to distinguish an unavailable screen from a broken binding.
- `LaunchBar.lua` gates Technology and Civics by capability. Government, Religion, Great People, and Great Works additionally use live launch-button visibility; the Government button is disabled during anarchy and its tooltip contains the remaining-turn reason. Great People, Great Works, and Espionage also honor the vanilla `DISABLE_*_HOTKEY` global parameters.
- Government unlocks after the local culture component reports at least one unlocked policy. Religion unlocks after Faith becomes available. Great Works unlock when an owned city building contains a Great Work; vanilla keeps that screen unlocked after the first discovery. Therefore CAI should use the live LaunchBar control state rather than independently caching displayed or historical values.
- `PartialScreenHooks.lua` unlocks City-States after `Players[local]:GetDiplomacy():HasMet(...)` succeeds for an alive minor, Trade Overview after `GetOutgoingRouteCapacity() > 0`, and Espionage after `GetSpyCapacity() > 0`. Each also has its corresponding game capability gate.
- `UI_CAIOpenCityStates` is handled directly by the `PartialScreenHooks_CAI` `InputActionStarted` wrapper, alongside the other partial-screen actions. After the live contact check, call included `CheckCityStatesUnlocked(localPlayer)` to update vanilla's private cache, then call the actual `OnToggleCityStates()` open function and preserve the click sound.
- The Gathering Storm World Congress ribbon button requires `CAPABILITY_WORLD_CONGRESS` and `Game.GetEras():GetCurrentEra() >= GlobalParameters.WORLD_CONGRESS_INITIAL_ERA`. `GameInfo.Eras[GlobalParameters.WORLD_CONGRESS_INITIAL_ERA].Name` supplies the localized configured era name for failure speech.
- The WorldTracker research/civic chooser hotkeys are blocked while `IsTutorialRunning()` is true and while the corresponding live tracker controls are hidden or disabled. Minimap map search and lens opening are blocked without a local player, during district placement, or in World Builder; lens and map-pin controls add their own capability/visibility/disabled gates. Realtime in-game chat requires the Chat feature and network multiplayer.

## InGame root and accessible quick save/load

- Gathering Storm's `DLC/Expansion2/UI/Replacements/InGame.lua` is the authoritative `InGame` root context. Its header states that mods cannot partially replace this engine-attached context, so CAI imports a full copy as `UI/inGame/InGame.lua` through the `CAIInGame` import action rather than using `ReplaceUIScript`.
- Vanilla handles `PauseMenu` and `QuickSave` through `Events.InputActionTriggered` and `OnInputActionTriggered(actionId)`. CAI removes that exact listener after vanilla initialization and registers one `Events.InputActionStarted` listener which delegates ordinary action ids back to the original function.
- The accessible action is `SaveGame_CAI`, deliberately beginning with `S` so it sorts after vanilla `QuickSave`, which begins with `Q`, when both use `F5`. It checks `CanLocalPlayerSaveGame()` before delegating to `OnInputActionTriggered(Input.GetActionId("QuickSave"))`, and speaks localized success or failure feedback through `Speak()`.
- `LoadGameMenu.lua` owns vanilla `QuickLoad`: its `OnInputActionTriggered(actionId)` checks `CanLocalPlayerLoadGame()`, queries the local quick-save slot with `UI.QuerySaveGameList(...)`, validates required mods in `OnQuickLoadQueryResults(...)`, and delegates the actual load to `Network.LoadGame(...)`. CAI must call this original handler rather than duplicate that asynchronous flow.
- The accessible load action is `ReloadGame_CAI`, deliberately beginning with `R` so it sorts after vanilla `QuickLoad`, which begins with `Q`, when both use `F6`. `LoadGameMenu.lua` replaces its own `InputActionTriggered` listener with an `InputActionStarted` listener. An unavailable load speaks localized failure feedback; an available load pushes a low-priority CAI Yes/No dialog. Yes closes the dialog and calls the original `OnInputActionTriggered(m_QuickloadId)`. The dialog removes itself from the manager stack on `focus_leave`, No, or Escape.

## Cultural victory World Rankings data

- Vanilla `GatherCultureData()` returns alive major players grouped by team. Each player row contains `PlayerID`, `NumStaycationers` from `PlayerCulture:GetStaycationers()`, `NumVisitingUs` from `GetTouristsTo()`, the required foreign-tourist target derived as one more than the largest opposing domestic-tourist count, and `TurnsTillCulturalVictory` when `GetTurnsUntilVictory()` exists.
- Despite the generated Civ VI IDE helper naming its argument `plotID`, `PlayerCulture:GetTouristsFrom(playerID)` takes another player id in World Rankings and returns how many of that civilization's tourists are visiting the owning player. `GetTouristsFromTooltip(playerID)` returns the matching tourism-per-turn, lifetime-tourism, and modifier breakdown. The base World Rankings uses both calls on the local player's culture component for every other alive major player.
- The accessible Culture tree retains team nesting only when a team has multiple alive major players. Teams are sorted by their best member's cultural-victory progress and their live label reports that best progress; players within a team are sorted by individual progress. Singleton players remain top-level rows. Non-local players are leaves whose tooltip contains domestic tourists and a positive turns-to-victory estimate when available. Only the local player's row expands, including when nested under a team, with one flat leaf per other alive major civilization containing its masked leader/civilization identity, tourists visiting the local player, and the authoritative `GetTouristsFromTooltip(...)` breakdown. Unmet identities must remain `LOC_DIPLOPANEL_UNMET_PLAYER` rather than exposing configuration names.
- The expanded vanilla Culture header exposes the main victory explanation plus separate definitions of domestic and visiting tourists. Its three visual column headers also have tooltips explaining domestic tourists, tourists visiting us from that civilization, and total visiting tourists versus the victory target. CAI's advisor leaf joins the main victory explanation with the separate domestic- and visiting-tourist definitions; explicit row labels make the shorter column explanations redundant.
- Gathering Storm cultural-dominance summaries (`we dominate`, `they dominate us`) are produced by `GetCulturalVictoryAdditionalSummary(...)` for the Overall view, not by `ViewCulture()`. Overall also exposes each player's total Tourism-per-turn and Culture-per-turn values. These may be useful Culture-tree enhancements, but they are not missing parity with vanilla's Culture tab. Opening vanilla Culture still activates the `TourismWithUnits` lens; CAI preserves that behavior because its wrapper calls the included `ViewCulture()` before synchronizing the accessible page.
- CAI's Score, Science, Culture, Domination, Religion, Diplomatic, generic/custom, and ordinary Overall victory trees all preserve multi-player team nesting, with singleton players presented directly. Overall uses a victory -> ranked team/singleton -> team-member hierarchy. Its ranking mirrors `PopulateOverallInstance(...)`: team victory progress first, then average primary tiebreak, average secondary tiebreak, highest member generic score, and team id. Each team/singleton row exposes its ordinal place and normalized victory-progress percentage. Known player tooltips contain the localized primary and distinct secondary tiebreak summaries plus `AdditionalSummary`; Culture therefore includes Tourism per turn, Culture per turn, and Gathering Storm cultural dominance. Unmet player rows expose only `LOC_DIPLOPANEL_UNMET_PLAYER`, not hidden tiebreak data. Multiplayer human names and local player/team indicators are stated in text instead of relying on vanilla's visual icon treatments. Scenario-specific Overall branches retain their official narrative/objective presentations rather than forcing the ordinary hierarchy onto incompatible scenario layouts.
- Final ordinary-screen parity implementation (excluding scenarios) retains all substantive vanilla values in Overall, Score, Science, Culture, Domination, Religion, and Diplomatic. The Score advisor prepends `LOC_WORLD_RANKINGS_SCORE_CONDITION` with `Game.GetMaxGameTurns()` before the score explanation. Religion uses vanilla's full `LOC_WORLD_RANKINGS_RELIGION_CONVERT_SUMMARY` wording for both civilizations and teams. Included `ViewCulture()` / `ViewReligion()` calls preserve Tourism and Religion lens activation and reset behavior.
- Science milestone leaves use vanilla `GetTooltipForScienceProject(...)` to expose Spaceport status on the first milestone and prerequisite-technology plus individual-project completion on every applicable milestone. The Gathering Storm advisor includes `LOC_WORLD_RANKINGS_SCIENCE_REQUIREMENT_FINAL` with the configured light-year target. Its light-year progress row is present only after Exoplanet Expedition completion, matching the fifth vanilla meter's gate.
- Science ranking progress mirrors vanilla's per-player progress-data path: for the satellite and Moon Landing stages, a city's project cost enters the denominator only when that city has nonzero progress or the globally completed project supplies full progress; base Mars and Gathering Storm Mars/Exoplanet include each city cost in the denominator. Per-project completion is retained across the city scan so tooltips and the local next-step line identify the first genuinely unfinished project.
- Detailed Score, Science, Culture, Domination, Religion, Diplomatic, and generic/custom rows textually expose vanilla's visual local-player/team state as `You` / `Your team`; known human players also include their multiplayer player name. Reference-only children such as captured-from, converted-civilization, and tourist-source rows retain the ordinary civilization label so the relationship reads naturally.

## World Rankings comparison tables

- World Rankings uses one shared persisted `UI/WorldRankingsViewMode` selection. Table mode applies to every structured standard tab; tree mode applies to every tab. Narrative scenario Overall pages and arbitrary generic/custom victory tabs remain tree-only and identify that fallback while table mode is selected.
- Every table row is one actual competitor keyed by team id. A singleton team uses its civilization label; a multi-civilization team uses its team label, with its member civilizations in the Competitor tooltip. There is no separate Team or Members column. Metric cells expose the authoritative team result, and their tooltips contain the per-civilization breakdown without revealing unmet identities.
- Overall columns use `Game.GetVictoryProgressForTeam(...)` and the same ordered progress, averaged primary tiebreak, averaged secondary tiebreak, highest-member generic score, and team-id ranking as the tree. Each enabled structured victory becomes one dynamic column displaying exact place and normalized progress.
- Score totals sum member `PlayerScore` values exactly as vanilla `GatherScoreData()` does. Active scoring-category columns are dynamic and sum each displayed member's live `GetCategoryScore(...)`. The table prefers the same captured `PopulateScoreInstance(...)` / `PopulateScoreTeamInstance(...)` records as the tree, preserving scenario display filters such as Nubia's invasion-player suppression; direct `GatherScoreData()` is only the fallback when no captured presentation is available.
- Science mirrors vanilla `PopulateScienceTeamInstance(...)`: one coherent member progress record supplies the visible team meters. Members compare lexicographically by the first three project-stage percentages, then player id; Gathering Storm retains that inherited three-stage leader choice even though the selected record also exposes Exoplanet distance and speed. The table never combines milestones or travel values from different teammates. Every milestone tooltip retains `GetTooltipForScienceProject(...)` for each member.
- Culture uses the same best-member ratio and required-tourist tiebreak as `GatherCultureData()`. All visible Culture values come from that one member, while cell tooltips compare every member. The local competitor's Visiting tourists tooltip also retains the authoritative per-source `GetTouristsFrom(...)` and `GetTouristsFromTooltip(...)` detail.
- Domination uses `TotalCapturedCapitals`, member original-capital state, and `Game.GetVictoryProgressForTeam("VICTORY_CONQUEST", teamID)`. Religion uses the deduplicated team `ConvertedCivs` and `ReligionTypes` sets returned by `GatherReligionData()`. Diplomatic Victory displays the highest member's point count, matching CAI's existing `GetBestDiploScore(...)`, alongside authoritative team victory progress; the leading member's vanilla point tooltip remains available.

## Alexander scenario World Rankings replacement

- `DLC/AlexanderScenario/AlexanderScenario.modinfo` registers exactly one `ReplaceUIScript`: Lua context `WorldRankings` is replaced by `UI/Replacements/WorldRankings_AlexanderScenario.lua` when `RULESET_SCENARIO_ALEXANDER` is active. `ARXManager.lua` is imported but is not a UI-context replacement CAI needs to merge.
- The scenario also imports its own `UI/WorldRankings.xml`. That layout replaces the normal Overall contents with live controls `Controls.Alexander1` through `Controls.Alexander8`; the remaining standard ranking containers still exist in the XML.
- `WorldRankings_AlexanderScenario.lua` includes base `WorldRankings`, overrides `ViewOverall()`, and overrides `ShouldCheckCustomVictories()` to return `false`. Its Overall function populates the eight controls with the Alexander quote, scenario rules, the live count of enemy cities, and the live score value `owned cities * 5`.
- CAI's `CAI_WorldRankings` replacement has `LoadOrder 100`, while Alexander's replacement declares no load order, so CAI is the final owner of the shared context. CAI must explicitly compose the scenario behavior; two independent replacements cannot both supply their Lua overrides.
- The correct CAI include chain is ruleset-first: when `GameConfiguration.GetRuleSet()` equals `RULESET_SCENARIO_ALEXANDER`, include `WorldRankings_AlexanderScenario`; otherwise retain the existing XP2 / XP1 / base chain. `GameConfiguration.GetRuleSet()` is available here; do not add a `GameConfiguration.GetValue("RULESET")` fallback. The scenario wrapper already includes base `WorldRankings`, so CAI must not include base a second time.
- CAI's accessible Overall rebuild must also branch for Alexander. The first implementation read `Controls.Alexander1` through `Controls.Alexander8` after the included scenario `ViewOverall()` ran, but in-game testing produced eight present yet blank accessible rows: the imported controls existed while `GetText()` returned empty in CAI's winning replacement context. Do not use those controls as CAI's text source.
- Build three `StaticText` rows from the same authoritative inputs used by `WorldRankings_AlexanderScenario.lua`, joining their components with `[NEWLINE]`. Row one contains scenario display lines 2, 3, and 4 (`LOC_ALEXANDER_SCENARIO_WORLD_RANKING_1`, `_2`, `_3`). Row two contains lines 5 and 6 (`_4`, `_5`). Row three contains lines 7, 8, and 1: live enemy cities (`_6`), live player-zero city score (`_8`), then `LOC_ALEXANDER_ENDGAME_RANKING_LEADER_QUOTE`. For `_6`, count cities owned by alive players whose id is greater than zero; for `_8`, count player-zero cities and multiply by five. Evaluate both counts together from `PlayerManager.GetAlive()` when the third row label is read rather than caching displayed values. Standard tabs that the scenario leaves available can continue using the existing CAI rebuild functions.
- Static integration checks should confirm the ruleset branch precedes expansion selection, the Alexander include is registered by the scenario's `ImportFiles`, all eight scenario localization tags are present once across the three accessible rows in the required order, the third row uses the live city-count helper once for both values, and `ShouldCheckCustomVictories()` remains the included scenario override. In-game testing must open World Rankings in Conquests of Alexander, verify the three grouped Overall rows and current city/score values, verify no duplicate custom-victory tab appears, and smoke-test every remaining tab and close/reopen focus behavior.

## Australia scenario UI replacements

- `DLC/AustraliaScenario/AustraliaScenario.modinfo` activates for `RULESET_SCENARIO_AUSTRALIA` and registers three `ReplaceUIScript` actions: `PartialScreenHooks_AustraliaScenario.lua`, `TechTree_AustraliaScenario.lua`, and `WorldRankings_AustraliaScenario.lua`. None declares a load order. CAI's load-order-100 replacements therefore win all three contexts and must explicitly compose the scenario files before adding accessibility behavior.
- The scenario `ImportFiles` action makes all three replacement Lua files available to wildcard `include(...)` calls. It also imports `UI/WorldRankings.xml`, `UI/ARXManager.lua`, and `UI/CivicsTreeIconLoader_AwardSettler.lua`. `WorldRankings.xml` supplies the scenario Overall controls and a shorter score-row layout. `ARXManager.lua` replaces the optional Logitech ARX companion-display implementation through VFS name resolution and does not require CAI integration. The civic icon loader is discovered by the base Civics Tree, Civics Chooser, and World Tracker wildcard include and adds numbered settler-award icons/tooltips.
- `PartialScreenHooks_AustraliaScenario.lua` includes base `PartialScreenHooks` and replaces `CheckSpyCapacity`, `OnDiplomacyMeet`, and `CheckCityStatesUnlocked` with no-ops. This permanently suppresses vanilla espionage and City-States hook unlocking. The scenario database also deletes `CAPABILITY_CITY_STATES_VIEW` and `UNIT_SPY`, but CAI must not rely only on those data consequences: its direct accessible espionage action bypasses vanilla's private unlock flag and should explicitly report the screen unavailable for the Australia ruleset. The CAI include chain should select the Australia wrapper before XP2 / XP1 / base so vanilla hook behavior remains authoritative.
- `TechTree_AustraliaScenario.lua` includes base `TechTree`, caches and overrides `GetEraArtXOffset`, passes `363` as a third argument to the base calculation, then calls `AllocateUI()` / `Resize()`. In the current shipped base file `GetEraArtXOffset(instArt, eraData)` accepts only two arguments, so Lua ignores the scenario's third value and the returned offset is numerically unchanged; the early reallocation also occurs before the normal context `OnInit` rebuild. The wrapper appears to be retained legacy layout code and adds no accessible content, but CAI should still select it before the expansion/base chain to compose the official scenario behavior rather than depend on that implementation accident.
- `WorldRankings_AustraliaScenario.lua` includes base `WorldRankings`, sets `SIZE_SCORE_ITEM_DETAILS` to `69`, replaces `ViewOverall`, and replaces `GatherScoreData`. `GatherScoreData` includes only alive major human players, preventing AI scores from appearing in this economic race. CAI's Score tree already consumes `GatherScoreData()`, so selecting the scenario wrapper automatically preserves this filtering.
- Australia's Overall view contains the localized scenario description; in single-player it lists all eight difficulty thresholds from Settler `200` through Deity `900`, followed by the local player's current difficulty and corresponding required score; and in both single- and multiplayer it contains the Outback title and effects. Multiplayer hides the entire difficulty/threshold block. As with Alexander, CAI should build accessible rows from the authoritative localization tags and live configuration rather than reading scenario layout controls after its winning replacement loads.
- The World Rankings include chain is ruleset-first: Australia, then Alexander, then XP2 / XP1 / base. The accessible Overall rebuild has a dedicated Australia branch. Its first two rows are the scenario description and Outback explanation. The final row derives the live single-player target from `GameInfo.Difficulties`, `PlayerConfigurations[Game.GetLocalPlayer()]:GetHandicapTypeID()`, and the scenario's `{200,300,400,500,600,700,800,900}` thresholds. That current-game row is an expandable `TreeItem`; its eight children run Deity through Settler and each say only `<difficulty name>, <required score>`, avoiding repetition of `Difficulty:` on every row. Multiplayer omits this complete final current-game/threshold tree and retains the two explanation rows. No CAI modinfo entry or copied scenario file is required because the official scenario already imports the wrapper and layout files.
- `CivicsTreeIconLoader_AwardSettler.lua` handles `MODIFIER_PLAYER_GRANT_UNIT_IN_CAPITAL` by `ModifierId`: `CIVIC_AWARD_ONE_SETTLER` uses `LOC_CIVIC_SCENARIO_CROWN_COLONY_DESCRIPTION`, while `CIVIC_AWARD_TWO_SETTLERS` uses `LOC_CIVIC_SCENARIO_GOLD_RUSH_DESCRIPTION`. CAI's shared `GetAwardNames(...)` must recognize those two modifier ids explicitly; the generic modifier cache's single `ModifierValue` is not usable here because later `UnitType`, `Amount`, and `AllowUniqueOverride` arguments overwrite one another.
- The Settler modifiers are civic rewards, not technology rewards. Penal Colony, Crown Colony, Pastoral Leases, Federation, and The Great War each grant one Settler; Gold Rush grants two. Nothing should appear for these rewards in the Technology Tree. Verify them in the Civics Tree or Civics Chooser instead.
- Australia also supplies Continue-style rows to the ordinary data-driven `EventPopup`; it does not replace that UI context. CAI's existing `EventPopup_CAI.lua` integration already covers those events.
- Static verification should confirm all three ruleset-first include branches, explicit accessible espionage suppression, human-only Score data through the included `GatherScoreData`, presence and multiplayer omission of every Overall component, both settler award modifier ids, scenario imports, and `git diff --check`. In-game testing should cover accessible and sighted Tech Tree layout, unavailable City-States and Espionage actions, single-player and multiplayer Overall contents, human-only Score rows, both settler-grant civic descriptions in the chooser/tree/tracker, Australia event popups, remaining World Rankings tabs, and close/reopen focus.

## Black Death scenario UI replacements and composition plan

- `DLC/BlackDeathScenario/BlackDeathScenario.modinfo` activates for `RULESET_SCENARIO_BLACKDEATH`. The installed modinfo under the Steam Civ VI DLC directory has the same SHA-256 hash as the decompiled project copy (`B9013A98CD658017C505A124F26458878B24FF5EDD5950DD66B25E288754FE45`), so the decompiled declarations below match the currently installed game.
- The scenario registers eleven `ReplaceUIScript` actions and none declares a load order: `ActionPanel`, `ARXManager`, `UnitPanel`, `CityPanel`, `WorldRankings`, `GovernmentScreen`, `MinimapPanel`, `ModalLensPanel`, `PlotInfo`, `PlotToolTip`, and `TopPanel`. CAI's load-order-100 replacements win nine of these contexts; CAI does not replace `ARXManager` or `ModalLensPanel`. As with Australia and Alexander, every shared context must explicitly compose the scenario before adding accessibility behavior.
- The scenario's `ImportFiles` action exposes the wrappers for `ActionPanel`, `UnitPanel`, `CityPanel`, `WorldRankings`, `GovernmentScreen`, `PlotInfo`, `PlotToolTip`, and `TopPanel`, so those CAI files can use a ruleset-first `include("..._BlackDeathScenario")` branch before their expansion/base selection. `MinimapPanel_BlackDeathScenario.lua` and `ModalLensPanel_BlackDeathScenario.lua` are direct replacements but are not in `ImportFiles`. For a non-imported replacement that CAI must compose, follow the established Byzantium/Gaul GovernmentScreen pattern: copy the official wrapper into `UI/Replacements/<source DLC>`, add it to the top-level `<Files>` list, register it through a criteria-scoped `<ImportFiles>` action with no load order, then include that VFS copy from the CAI replacement. The official `ModalLensPanel` remains active because CAI does not replace its context and therefore needs no copied import.
- `ActionPanel_BlackDeathScenario.lua` includes `ActionPanel_Expansion1`, overrides `OnRefresh()`, delegates to `BASE_OnRefresh()`, and then hides the era/age score, threshold, countdown, animation, and tooltip controls. A CAI ruleset-first include preserves the sighted scenario panel. The accessible `ActionPanelSpeakEraAge` action should honor the same suppression instead of exposing a UI feature the scenario deliberately removes.
- `UnitPanel_BlackDeathScenario.lua` includes base `UnitPanel`, adds five `EXECUTE_SCRIPT` actions through `GetUnitActionsTable()`—Study, Minister, Pacify, Purge, and Holy Fervor—and adds the scenario's remaining-charge property to the visual stats list. CAI's action menu already consumes the returned vanilla action table and invokes each stored callback, so including the wrapper should expose all five commands without reimplementing them. CAI's Charges summary reads ordinary `ReadUnitData` charge fields, not the scenario property used by `FilterUnitStatsFromUnitData`; it needs a Black Death property fallback so the remaining scenario charges are spoken too. The wrapper also adds only presentation animations for the five activation reasons.
- `CityPanel_BlackDeathScenario.lua` includes base `CityPanel` and makes `PurchaseTileCheck` available only to England. CAI's combined city-management action already reads that control's live visibility, so including the wrapper preserves the civilization gate.
- `PlotInfo_BlackDeathScenario.lua` turns England's purchase-plot mode into Coerce. It lists only plague-bearing plots, charges 10 Gold, applies `CoerceTurns = 10` plus the current start turn, shows remaining coerced turns for an already affected plot, and refreshes when plot properties change. CAI must include this wrapper so activation delegates to the scenario's `OnClickPurchasePlot`. The shared city-management helper currently derives purchase candidates and costs from `CityManager.GetCommandTargets()` and `city:GetGold():GetPlotPurchaseCost()`, which does not describe Coerce; its Black Death branch must instead mirror the scenario's plague/coercion properties and live 10-Gold affordability before announcing or enabling a plot.
- `PlotToolTip_BlackDeathScenario.lua` includes `PlotTooltip_Expansion2`, adds `CoerceTurns` to fetched plot data, and appends the localized remaining-turn label for an England-owned coerced plot. A ruleset-first include supplies that field to `PlotToolTip_CAI.lua`; CAI must still expose it through a dedicated `coerceTurns` information helper because the accessible helper pipeline does not consume vanilla's `GetDetails()` list. The helper mirrors the official visibility, local ownership, and England checks and uses `LOC_PLOTINFO_COERCED_TURNS_LABEL`. It appears immediately after `fallout` in the default plot information, automatic cursor-move keys, and `PlotReadStats` bucket.
- `WorldRankings_BlackDeathScenario.lua` includes base `WorldRankings` and adds `g_victoryData.VICTORY_BLACKDEATH_SCENARIO`. Its progress text reports completed civics and technologies against the scenario endpoints (`CIVIC_MIDDLE_CLASS` and `TECH_INVENTION`), and its score is the greater of the two completion fractions. It also replaces the sighted Overall-instance population code. Once the wrapper is selected before Australia, Alexander, and expansion/base files, CAI's existing custom-victory discovery and generic victory views can consume the added `g_victoryData` entry; a separate hard-coded Overall branch should not be necessary unless in-game testing shows scenario-specific presentation missing.
- `GovernmentScreen_BlackDeathScenario.lua` is a 2,734-line full replacement based on the base screen, paired with the scenario's imported `GovernmentScreen.xml`; it is not an Expansion 2 extension. CAI must select it before Dramatic Ages / expansion variants so Lua and XML remain compatible. France's trait adds a third wildcard slot; scenario code treats that third wildcard/core slot index 2 as the Papal slot, displays its localized name and description, charges 4 Faith per turn while filled, and deliberately permits incomplete governments. CAI's generic policy-slot widgets must identify that live slot for France and append the Papal name/upkeep description to both empty and filled slot speech. Confirmation should continue mirroring the live vanilla `ConfirmPolicies` state, which preserves the scenario's empty-slot allowance. The wrapper's standalone `OnFaithChanged()` is never registered anywhere in the shipped DLC and contains an undefined `kPlayer`; the gameplay script, not this dead UI function, performs the per-turn Faith deduction and clears/notifies when upkeep cannot be paid.
- `TopPanel_BlackDeathScenario.lua` includes `TopPanel_Expansion2`, subtracts the filled Papal slot's 4-Faith upkeep from France's displayed per-turn Faith, and appends the localized upkeep line to the Faith tooltip. Selecting the wrapper preserves the sighted yields and makes its `GetFaithTooltip()` available to CAI, but CAI's direct Faith speech and yield-tree value currently call `GetFaithYield()` themselves and must apply the same live subtraction. This wrapper also includes `BlackDeathScenario_PopupHandler`; losing the wrapper loses scenario event-response plumbing, not just TopPanel presentation.
- `BlackDeathScenario_PopupHandler.lua` maps user-defined notification activation to `LuaEvents.EventPopupRequest`, then maps `Events.EventPopupResponse` to a networked `PlayerOperations.EXECUTE_SCRIPT` request whose `OnStart` is `EventPopupChoice`. CAI's notification center already activates the vanilla rail handler, which raises the expected user-notification Lua event, and `EventPopup_CAI.lua` already delegates choice/Continue clicks to vanilla. Loading the official handler through the TopPanel composition is therefore the missing link; CAI should not duplicate this network flow.
- `MinimapPanel_BlackDeathScenario.lua` creates the Plague lens toggle, maintains the `PlagueHighlight` overlay from each plot's `Plague` property on a 0–10 grayscale, handles the scenario `LensPlague` action (default Insert), and raises `LuaEvents.OnPlagueLensOn`. `ModalLensPanel_BlackDeathScenario.lua` listens for that event and displays the localized plague legend. CAI imports and includes a copy of the official Minimap wrapper, with one small getter exposing its scenario-created button to the accessible lens list. The accessible Plague entry activates the official `LuaEvents.OnViewPlagueLens` path. CAI's `InputActionStarted` wrapper removes the included `OnInputActionTriggered` listener only after late initialization and delegates ordinary actions to the included handler, allowing Insert to reach the official scenario implementation.
- `CityBannerManager_BlackDeathScenario.lua` and `CityBannerManager.xml` are imported rather than registered as a replacement. Expansion city banners wildcard-include `CityBannerManager_*`, so the official plague-indicator instance and method extensions compose automatically with `CityBannerManager_CAI.lua`. CAI reads the visible `banner.m_StatPlagueIM` tooltip through its dedicated `blackDeathIndicator` information key. That key belongs only to the city Identity and status bucket, whose `CityBannerReadIdentityStatus` action is bound to number 1; it is not folded into the generic status collector or repeated in another bucket.
- `EndGameMenu_BlackDeathScenario.lua` is also imported and is loaded by base `include("EndGameMenu_", true)`. It adds the Black Death victory ribbon, background, movie, sound, and color style automatically; CAI does not need to copy or register it. The scenario's ARX manager is confined to the optional Logitech companion display and requires no CAI integration.
- Use ruleset-first includes for the eight wrappers already imported by the scenario. For a wrapper omitted from the scenario's `ImportFiles`, copy and import it through CAI as described above instead of manually reproducing its behavior. Retain the official direct `ModalLensPanel`, and add accessibility-specific live-data handling only for Coerce, Papal-slot speech/upkeep, scenario charges, the Plague lens entry, and city-banner plague status.
- A safe implementation order is: (1) compose TopPanel and event plumbing; (2) compose UnitPanel, CityPanel, PlotInfo, and PlotToolTip plus the Coerce/charge speech gaps; (3) compose GovernmentScreen and Papal slot/upkeep speech; (4) import/include the non-imported Minimap wrapper and expose its Plague lens; (5) compose ActionPanel and WorldRankings; (6) verify wildcard CityBanner and EndGame behavior. In-game testing should cover all four playable civilizations, all five scenario unit commands including disabled reasons and charge changes, England Coerce targeting/cost/duration, France's third wildcard slot and 4-Faith upkeep/automatic clearing, plague tooltip/banner/lens behavior and Insert remapping, all four A/B plague events through notification activation and response, Black Death victory progress/end-game presentation, hidden era controls/action, every remaining World Rankings tab, and close/reopen focus behavior.
- CAI implementation 2026-07-17 follows that composition plan. The eight scenario-imported shared contexts select their Black Death wrappers before expansion/base files. The copied Minimap wrapper is registered in CAI's top-level file list and in a `BlackDeathScenario`-scoped import action with no load order, then selected first by `MinimapPanel_CAI.lua`. Its official Plague toggle, overlay, `LensPlague`, and `OnViewPlagueLens` behavior remain authoritative; CAI adds only the button getter and accessible list entry. The `InputActionTriggered` listener is removed only after the included late initializer registers it, and ordinary `InputActionStarted` actions delegate to the included scenario handler.
- The Plague lens is a custom overlay, not one of the engine lens layers consumed by the ordinary `Events.LensLayerOn/Off` scanner path. `MinimapPanel_CAI.lua` therefore publishes authoritative `ExposedMembers.CAIPlagueLensActive` state plus `LuaEvents.CAIPlagueLensChanged` from the included scenario's on/off and refresh functions, and the World Scanner rebuilds its Active lens category from that event. Black Death implements plague effects through Civ VI's Fallout manager; the official PlotToolTip already announces `LOC_TOOLTIP_PLOT_CONTAMINATED_TEXT` as localized `PLAGUE-STRICKEN: N turns`. Do not add a second active-lens plot helper. The scanner reads the same `Game.GetFalloutManager():GetFalloutTurnsRemaining(plotIndex)` source and official localization, enumerates only revealed positive-duration plots, groups them from highest to lowest, and validates the live duration before focusing an item.
- Black Death city-management state is scenario-specific. England's active purchase layer enumerates `Map.GetCityPlots():GetPurchasedPlots(city)`, retains only plots whose live `Plague` property is positive, reads `Coerce` with `RefreshObjectState` when available, uses the scenario's 10-Gold cost, and exposes coerced plots separately from affordable and too-expensive targets. Accessible activation still calls the included scenario `OnClickPurchasePlot`; success immediately announces the localized 10-turn coerced state while the networked property operation completes.
- The accessible Government policy tree recognizes France's core slot index `RULES.PapalSlotIndex - 1`, prepends the localized Papal slot name, and exposes its upkeep description whether empty or filled. TopPanel uses one shared live Faith-yield helper for direct speech and the yield tree, subtracting `RULES.PapalSlotUpkeep` only while France's Papal slot is filled. Unit charge speech reads the scenario's `Charges` / `MaxCharges` properties, and the number 1 city-banner Identity and status bucket reads the visible `m_StatPlagueIM` indicator tooltip.
- The scenario GovernmentScreen is based on an older base revision and does not define the later global extension points `GetPolicyFromCatalog()` or `IsReadOnly()`. Accessibility code must not call either unconditionally. Its policy fallback should reproduce the official catalog record from `GameInfo.Policies[policyType]` plus `GameInfo.Types[policyType].Hash`, localizing `Name` and `Description`; its read-only fallback is false because the scenario has no World Congress policy-lock state. The scenario does retain `IsAbleToChangePolicies()`, `IsPolicyTypeActive()`, `IsPolicyTypeLegalInRow()`, `SetActivePolicyAtSlotIndex()`, and `RemoveActivePolicyAtSlotIndex()`.

## Nubia scenario UI replacements and composition plan

- `DLC/NubiaScenario/NubiaScenario.modinfo` activates for `RULESET_SCENARIO_NUBIA` and registers exactly one formal `ReplaceUIScript`: Lua context `WorldRankings` is replaced by `UI/Replacements/WorldRankings_NubiaScenario.lua`. The replacement declares no load order, so CAI's load-order-100 `WorldRankings_CAI.lua` wins and must explicitly compose the scenario wrapper. The installed modinfo and all four relevant UI files have the same SHA-256 hashes as the decompiled copies; the installed modinfo hash is `80215E115BD7147BF96DB4B36DE7084453D08F0F59F7DA60F8F0B16A993763F2`.
- The scenario also imports `TechFilterFunctions_NubiaScenario.lua`, `EndGameMenu_NubiaScenario.lua`, `ARXManager.lua`, the World Rankings wrapper, and `WorldRankings.xml`. The XML remains the active layout through the scenario import and requires no CAI copy. ARXManager targets only the optional Logitech companion display and does not need accessibility composition.
- `WorldRankings_NubiaScenario.lua` includes base `WorldRankings`, hides Overall through `ShouldShowOverall()`, forces Score to remain available through `ForceShowScore()`, and replaces the custom-victory data/view path. `GatherGenericData()` counts each alive major player's `BUILDING_TEMPLE_AMUN` buildings into `PlayerScore` and sums them into `TeamScore`; the custom `ViewGeneric()` sorts by team score and displays progress as `<count>/7`. The scenario victory requirement confirms that seven Temples to Amun is the target.
- Nubia's World Rankings deliberately suppress singleton players whose id is greater than 1 in both Score and custom-victory views. Player ids 0 and 1 are Egypt and Nubia; ids 2 through 7 are the six scheduled invasion civilizations. CAI must preserve that filter instead of exposing invasion-player rows that the official scenario hides. Team rows with more than one member retain the official behavior.
- The correct CAI include chain is ruleset-first: when `GameConfiguration.GetRuleSet()` is `RULESET_SCENARIO_NUBIA`, include `WorldRankings_NubiaScenario`; otherwise continue through the other scenario, expansion, and base branches. The wrapper already includes base World Rankings, so it must not be followed by a second base include. No modinfo entry or copied scenario file is needed because the scenario already imports the wrapper.
- Including the wrapper is necessary but not sufficient for accessible parity. CAI's generic victory row currently announces the player and requirements but not `playerData.PlayerScore`. For `VICTORY_NUBIA_SCENARIO`, the accessible rebuild must sort by live `TeamScore`, retain the official main-player filter, and announce the live Temple count against the target of seven. The accessible Score rebuild must apply the same official singleton-player filter. The forced Score tab should retain the scenario's localized `LOC_WORLD_RANKINGS_SCORE_DETAILS`, which explains that score determines victory level but cannot itself win the scenario.
- `TechFilterFunctions_NubiaScenario.lua` is loaded automatically by the base `TechFilterFunctions.lua` wildcard include used by both Tech Tree and Civics Tree. It adds `CIVIC_BOARDGAMES_ATHLETICS` to the Units filter because that civic grants a Great General. CAI should not copy or manually reproduce it; verify that the accessible Civics Tree Units filter includes Boardgames and Athletics after the World Rankings composition change.
- `EndGameMenu_NubiaScenario.lua` is loaded automatically by base `include("EndGameMenu_", true)` and adds the style for `VICTORY_NUBIA_SCENARIO`. CAI's EndGameMenu replacement extends the original context after that wildcard path and needs no Nubia-specific include or copy. Verify the Nubia victory ribbon, background, movie, sound, color, final score/ranking, and accessible end-game controls.
- Nubia sends its introduction, mercenary, invasion, high-priestess, and named-general messages through the ordinary data-driven `EVENT_POPUP_REQUEST` flow. It does not replace EventPopup. CAI's existing `EventPopup_CAI.lua` should expose their localized title, description, effects, image caption, and Continue control without scenario-specific code. Nubia also deletes `UNITOPERATION_PLANT_FOREST`; the existing UnitPanel guard for scenario-pruned operation rows covers the known load hazard.
- A safe implementation order is: (1) add the Nubia ruleset-first World Rankings include; (2) add the Temple-count/sort and main-player filtering to the accessible custom-victory view; (3) apply the official main-player filter to accessible Score; (4) statically verify the wildcard TechFilter and EndGameMenu paths plus EventPopup and UnitPanel coverage. In-game testing should cover no Overall tab, the default Temple-victory tab, live Egypt/Nubia Temple totals from 0 through 7, ordering changes as temples are built, absence of the six invasion players, the forced Score explanation and final-score rows, Boardgames and Athletics under the Civics Units filter, several scenario event popups, conquest and Temple victories, end-game presentation/controls, remaining available tabs, and close/reopen focus.
- CAI uses generic custom-victory presentation capture instead of adding a ruleset branch to `RebuildGenericTree` for every scenario. After the selected official wrapper is included, CAI wraps its global `PopulateGenericInstance` and `PopulateGenericTeamInstance` functions. While the official `ViewGeneric(victoryType)` renders, it captures the exact ordered player/team records actually populated, their stable ids, the scenario-returned data tables, and the nonempty visible text/tooltips written to their instance controls. The CAI tree is built from that normalized capture immediately after the original view returns. This preserves wrapper-owned filtering, ordering, labels, scores, and requirements; Nubia's `PlayerID < 2` condition and `Score` text therefore flow through without a Nubia-only accessible rebuild.
- Keep `Game.GetVictoryRequirements(...)`, `GameEffects` requirement text/state, `GameInfo.Victories` description, and player/team name helpers as the semantic fallback when a populated instance exposes no useful presentation text. Add conventional structured fallbacks for `PlayerScore` / `TeamScore`, but do not announce arbitrary data-table field names because Civ VI provides no labels or schema for scenario-defined fields. Preserve `FocusKey` from team/player ids rather than captured display text.
- The capture cannot guarantee compatibility with every third-party victory. Civ VI defines no structured custom-victory UI contract beyond overridable Lua functions: a mod may replace `ViewGeneric` completely, use local population functions, or paint unrelated custom controls directly. `ExposedMembers.CAIWorldRankings.RegisterGenericVictoryAdapter(victoryType, adapter)` covers those exceptions. `adapter.GetRows(victoryType, capturedRows)` returns replacement normalized rows or nil to use the structured fallback; adapter failures and invalid return values are logged and fall back to the captured rows. Official wrappers that use the vanilla global population hooks require no adapter; custom Overall replacements remain a separate presentation problem.
- Capture runs only during the active original `ViewGeneric` call, clears the model before each render, and rebuilds the accessible tree afterward with focus capture/restore. It retains captured strings and the immediately returned data records, not vanilla instance controls that InstanceManager may recycle. Refresh reruns the authoritative official view/capture path before changed progress is announced.
- The same authoritative capture is applied to `PopulateScoreInstance` and `PopulateScoreTeamInstance`. Accessible Score rows therefore inherit scenario-owned filtering and order while retaining CAI's expandable score-category children. This automatically preserves Nubia's exclusion of singleton invasion players and Australia's human-only Score data.
- CAI implementation 2026-07-17 selects `WorldRankings_NubiaScenario` before expansion/base files. The custom-victory capture exposes the wrapper's live `<Temple count>/7` text and Egypt/Nubia filtering; the Score capture preserves its equivalent official filtering. No Nubia file copy, modinfo registration, or new localization is required.
- `HAS_7_TEMPLES` has no `VictoryProgress` requirement-text mapping, so `GameEffects.GetRequirementTextKey(requirementID, "VictoryProgress")` legitimately returns nil. Never pass that nil into `GameEffects.GetRequirementText`, whose second argument must be a string. Skip the structured requirement child when the key is absent and retain the captured official `<Temple count>/7` row plus victory description as the accessible presentation.

## Indonesia/Khmer scenario UI replacements and composition plan

- `DLC/Indonesia_KhmerScenario/Indonesia_KhmerScenario.modinfo` activates for `RULESET_SCENARIO_INDONESIA_KHMER` and registers four formal `ReplaceUIScript` actions: `CityStates`, `TradeOverview`, `ReligionScreen`, and `WorldRankings`. None declares a load order, so CAI's load-order-100 replacements win and must explicitly compose all four official wrappers. The installed and decompiled modinfo files have the same SHA-256 hash, `7EB60F5CCDBCCD15FF778B68814A14D2EBB8D15747B7705EC54F466DC05C7B38`.
- The same scenario action imports all four wrappers, `WorldRankings.xml`, `CivilopediaPage_Resource.lua`, `EndGameMenu_Indonesia_KhmerScenario.lua`, and `ARXManager.lua`. It also lists `UI/GreatWorksOverview.lua`, but that file is absent from both the installed Windows DLC and the decompiled copy; this is a stale manifest entry, not scenario behavior that CAI must reproduce. ARXManager targets only the optional Logitech companion display.
- The correct include rule for each formal replacement is ruleset-first. `CityStates_CAI.lua`, `TradeOverview_CAI.lua`, `ReligionScreen_CAI.lua`, and `WorldRankings_CAI.lua` should include their corresponding `*_Indonesia_KhmerScenario` wrapper when the active ruleset matches, then skip the ordinary expansion/base branch because each official wrapper already includes its base file. No scenario file copy and no CAI modinfo registration are needed because the DLC already imports every wrapper and its World Rankings XML.
- `CityStates_Indonesia_KhmerScenario.lua` adds the scenario's `MARITIME` city-state type. It supplies the Maritime icon atlas, localized type name and tooltip, recognizes leaders inheriting from `LEADER_MINOR_CIV_MARITIME`, and returns the scenario-specific 1/3/6-envoy bonuses. CAI's accessible city-state tree already calls the overridable `GetCityStateType`, `GetTypeName`, and `GetBonusText` functions, so selecting this wrapper should make the Maritime type and all three bonuses accessible without a parallel ruleset branch. The sighted panel also retains its official Maritime icon.
- `TradeOverview_Indonesia_KhmerScenario.lua` overrides `GetCityStateIcon()` so routes to scenario Maritime city-states use `ICON_CITYSTATE_MARITIME`. This is a sighted icon-only change; CAI's accessible route names and yield details do not depend on the icon. The shipped function refers to an undefined `leader` variable in its direct-leader comparison, but all playable Maritime city-state leaders inherit from `LEADER_MINOR_CIV_MARITIME`, so its `leaderInfo.InheritFrom` path covers the scenario data. Preserve the official wrapper rather than duplicating this function.
- `ReligionScreen_Indonesia_KhmerScenario.lua` makes `AddLockedBeliefs()` a no-op because every scenario religion is founded by the gameplay script with a preset pantheon plus three preset religion beliefs. The pantheon is stored and displayed separately; `religion.Beliefs` therefore legitimately contains three items. Selecting the wrapper preserves the sighted screen's lack of locked slots, but CAI's independent `BuildBeliefsSection()` currently hardcodes a maximum of four and adds a false fourth `Locked belief slot` with the ordinary Apostle evangelization tooltip. Under `RULESET_SCENARIO_INDONESIA_KHMER`, the accessible belief count must use three as its total and must not synthesize locked rows. The already-founded scenario religion should expose no setup or evangelization flow.
- Civ VI exposes no database or gameplay API for a religion's maximum belief-slot count. Base `ReligionScreen.lua` declares `NUM_MAX_BELIEFS = 4` as a private local and uses it only inside the overridable global `AddLockedBeliefs(religion)`. Do not duplicate that constant in CAI or add per-ruleset slot totals. After the active official/mod wrapper populates a selected religion, treat the live children of `Controls.ViewReligionBeliefs` as authoritative: `AddUnlockedBeliefs()` creates the unlocked instances and the active `AddLockedBeliefs()` implementation creates or suppresses locked instances. Build the accessible count and locked rows from that live presentation while continuing to derive unlocked belief names/descriptions from `religion.Beliefs`. This automatically gives four total slots in vanilla and three in Path to Nirvana, and also follows compatible mods that preserve the standard Religion Screen stack/population contract. No solution can guarantee parity with every arbitrary mod: a full replacement may use unrelated custom controls or local-only population functions. When the standard stack is unavailable, the safe fallback is to expose the known unlocked beliefs without inventing locked slots; a bespoke adapter is required for a custom presentation that wants more.
- `WorldRankings_Indonesia_KhmerScenario.lua` overrides `ViewOverall()` and fills ten custom `IndonesiaKhmer1` through `IndonesiaKhmer10` controls from the imported scenario XML. The rows are static scenario rules: unavailable systems, promoted/trainable religious units, unique city-state suzerain behavior, 300 religious pressure from envoys, traders not creating roads, and the three scoring sources. The last three scoring explanations use the official example values: 1 point per follower, 20 points per foreign converted city, and 1 point per Faith per turn.
- World Rankings therefore needs two layers of composition. Include the official wrapper first so its layout and sighted Overall behavior survive. Then add a `RULESET_SCENARIO_INDONESIA_KHMER` branch to CAI's `RebuildOverallTree()` that exposes the same ten localized rows in the wrapper's exact displayed order (`1, 2, 3, 4, 10, 5, 6, 7, 8, 9`) and uses the same formatting arguments (`1,1`, `20,20`, and `1,1`). The standard score view remains authoritative: the gameplay script updates `ScoringScenario1` from followers, `ScoringScenario2` from foreign cities following the religion, and `ScoringScenario3` from live Faith yield; the scenario scoring database maps them to the visible Score categories with multipliers 1, 20, and 1. CAI's existing official Score population capture and expandable category rows should consume these values without an Indonesia/Khmer-specific score implementation.
- `CivilopediaPage_Resource.lua` is a same-name wildcard replacement loaded by base `include("CivilopediaPage_", true)`, which still runs through `CivilopediaScreen_CAI.lua`. It changes the Cloves and Cinnamon suzerain source from Zanzibar to scenario Palembang and omits ordinary resource placement rows. This should compose automatically; do not copy or directly include it from CAI.
- `EndGameMenu_Indonesia_KhmerScenario.lua` is loaded automatically by base `include("EndGameMenu_", true)` alongside `EndGameMenu_CAI.lua`. It styles the scenario's Score victory as a Religious victory. Scenario data supplies five historic rankings at 0/250/500/750/1000 points and disables One More Turn. CAI's generic end-game results/ranking controls should consume that live base presentation without a scenario branch.
- A safe implementation order is: (1) add the ruleset-first includes to CityStates, TradeOverview, ReligionScreen, and WorldRankings; (2) add the ten-row accessible World Rankings Overall branch; (3) suppress CAI's ordinary fourth locked-belief row and use a three-belief total for this ruleset; (4) statically verify that Score capture still exposes only the three scenario categories and that Civilopedia/EndGame wildcard files remain loaded; (5) test the scenario in game. The test matrix should cover all four Maritime city-states and their 1/3/6-envoy bonuses, a trade route involving a Maritime city-state, all seven preset religions with a separate pantheon plus three religion beliefs and no locked/setup controls, the two grouped Overall rules rows in official line order, live follower/foreign-city/Faith score categories and totals, Palembang as the Cloves/Cinnamon suzerain source, Score-victory religious styling, all five historic ranking thresholds, disabled One More Turn, remaining available tabs, and close/reopen focus.
- CAI implementation 2026-07-17 selects the four official Indonesia/Khmer wrappers before ordinary expansion/base files. CityStates' accessible tree consumes the wrapper's live Maritime type and envoy-bonus functions, while TradeOverview and ReligionScreen preserve their official sighted behavior. World Rankings exposes the ten localized scenario-rule lines in the wrapper's exact order as two accessible rows joined with `[NEWLINE]`: displayed lines 1-6 are the rules row and displayed lines 7-10 are the scoring row. The standard captured Score presentation remains authoritative for live scenario categories. Civilopedia and EndGame continue composing through their existing wildcard hosts; no scenario file copy, modinfo entry, or new localization is required.
- CAI's religion belief presentation now reads the visible children of `Controls.ViewReligionBeliefs` whenever the requested religion is the live selected sighted view. Recycled InstanceManager children are excluded through `IsHidden()`, and the count is never allowed below the structured unlocked count. Both the section summary and synthesized locked rows use that live total. If the requested religion is not currently selected, the standard stack is unavailable, or the sighted religion view is hidden, CAI falls back to the unlocked `religion.Beliefs` count and creates no speculative locked rows. This yields 3 of 3 with no locked row in Path to Nirvana and retains vanilla's ordinary 3 of 4 plus locked row when the official screen creates it.

## Poland scenario UI replacements and composition plan

- `DLC/PolandScenario/PolandScenario.modinfo` activates for `RULESET_SCENARIO_POLAND`. Its only formal `ReplaceUIScript` replaces the `WorldRankings` context with `UI/Replacements/WorldRankings_PolandScenario.lua` and declares no load order. CAI's load-order-100 `WorldRankings_CAI.lua` therefore wins and must explicitly include the official wrapper for this ruleset. The scenario also imports `ReligionScreen.lua`, `UnitFlagManager.lua`, the World Rankings wrapper and XML, and `ARXManager.lua`.
- `WorldRankings_PolandScenario.lua` includes base `WorldRankings`, changes only the Overall view, and preserves the ordinary Score view. Its imported XML supplies `ViennaInfo1` and `ViennaInfo2`. The first control contains, in order, Vienna history paragraph 1, Vienna history paragraph 2, and Ottoman Janissary history paragraph 1, separated by double newlines; the second contains Ottoman Janissary history paragraph 2. The last paragraph states the scenario's essential Vienna-survival objective.
- Add `RULESET_SCENARIO_POLAND` to the ruleset-first include chain in `WorldRankings_CAI.lua`, before expansion/base selection. Add a Poland branch to `RebuildOverallTree()` with one stable `StaticText` row containing all four official paragraphs in order, joined with `[NEWLINE]`. Use the four existing localization tags and `JoinLines`; no new player-facing localization is needed. The existing authoritative Score capture should remain unchanged.
- `ReligionScreen.lua` is an imported same-name full replacement, so CAI's existing `include("ReligionScreen")` should resolve it automatically when the Poland scenario action is active. The official file suppresses player religion founding and belief-addition flows, creates Catholicism or Orthodoxy from gameplay script data, exposes both religions, and hardcodes the scenario maximum to two founded religions because `GREAT_PERSON_CLASS_PROPHET` is deleted. It still renders four belief instances: two preset beliefs plus two locked rows.
- CAI must not infer setup availability independently in this scenario. `ReligionScreen_CAI.lua:GetSetupState()` should return nil for `RULESET_SCENARIO_POLAND`, preserving the official read-only scenario behavior even if engine belief counters look pending. The accessible `All Religions` summary must report a maximum of two for this ruleset; the generic `Map_GreatPersonClasses` lookup returns zero because the scenario deletes the Prophet class. Continue using the live visible belief-instance count for belief totals and locked rows, so the official two-of-four presentation remains authoritative.
- `UnitFlagManager.lua` is also an imported same-name full replacement. `UnitFlagManager_CAI.lua` already calls `include("UnitFlagManager")` for ordinary rulesets, so the official scenario manager should compose automatically and retain its invasion-unit colors. The official wrapper exports `GetTribeName(index)`, mapping scenario tribe indices 0 through 9 to the localized Teutonic Knights, Rus, Horde, Moldavian, Ottoman, and Swedish faction names used by the sighted flag tooltip.
- Add an accessible Poland invasion-faction detail to CAI unit-flag information. Resolve it from the included official `GetTribeName(unit:GetBarbarianTribeIndex())` rather than duplicating the index table. Include the nonempty result in default and requested unit-flag details with a stable semantic key. This is separate from Barbarian Clans mode: Poland does not enable that mode, so the current `barbarianClan` helper never exposes these names.
- The scenario's early-warning and invasion-arrival messages use ordinary `EVENT_POPUP_REQUEST` records (`SCENARIO_POLAND_EARLY_WARNING` and `SCENARIO_POLAND_INVASION_HERE`) with their complete localized message in `EventEffect`. `EventPopup_CAI.lua` should expose them without Poland-specific code. Verify the warning's invader, turns, and target city and the arrival's invader name, plus repeated/queued warnings.
- Scenario Setup is data-driven: three playable nobles, fixed 60 turns, fixed Duel map, Score-only victory, and leader/civilization abilities and unique items. The existing accessible Scenario Setup should consume these records without a new screen replacement. LoadScreen should consume `LOC_LOADING_INFO_POLAND_SCENARIO` through its existing live presentation.
- Vienna falling triggers `DEFEAT_VIENNA`; reaching the fixed turn limit uses ordinary Score victory. The scenario supplies thirteen historic end-game thresholds from 0 through 1000. No EndGame UI file is imported, so CAI's generic end-game controls should expose the defeat, winner/score, and historic ranking without a scenario branch. World Rankings Score should expose the three playable nobles and live category details through the existing score capture.
- Scenario Great People, writings, portraits, buildings, units, beliefs, Civilopedia queries, and city-state data are database additions or removals with no dedicated UI wrapper beyond the files above. Existing Great People, Great Works, Production, Civilopedia, city-state, religion, unit, and city accessibility paths should consume the live data. Treat these as regression coverage rather than new scenario branches unless an in-game mismatch is found.
- Safe implementation order: (1) compose the Poland World Rankings wrapper and add its merged Overall row; (2) make Religion Screen setup read-only and correct the two-religion maximum; (3) expose official invasion-faction names from UnitFlagManager; (4) run static include/order/localization and modinfo checks; (5) execute the scenario test matrix.
- In-game matrix: Scenario Setup with all three nobles and fixed settings; LoadScreen narration; World Rankings Overall row order and Score details; Catholic and Orthodox religion views, two-of-four belief presentation, no setup/add-belief controls, and `2/2` founded religions; Teutonic, Rus, Horde, Moldavian, Ottoman, and Swedish unit faction names through cursor/unit-flag reads; early-warning, arrival, and queued event popups; new scenario units/buildings/Great People/Great Works/Civilopedia pages; Vienna survival through turn 60 and Vienna-loss defeat; all thirteen end-game ranking thresholds; multiplayer/hotseat player filtering if supported; close/reopen and focus restoration; ordinary base-game World Rankings, Religion Screen, unit flags, and Barbarian Clans regressions.
- CAI implementation 2026-07-17 selects `WorldRankings_PolandScenario` before other scenario/expansion/base files and exposes all four official Vienna/Janissary paragraphs as one stable Overall row joined with `[NEWLINE]`. Religion setup is always read-only under `RULESET_SCENARIO_POLAND`, and the founded-religion summary uses the official maximum of two while belief totals still follow the live scenario Religion Screen. UnitFlagManager adds a `polandInvasion` information helper and default key; both ordinary and detailed unit reads call the included official `GetTribeName()` for visible barbarian invasion units. No scenario files, modinfo registrations, or new localization tags are required.

## Vikings scenario UI replacements and composition plan

- `DLC/VikingsScenario/VikingsScenario.modinfo` activates for `RULESET_SCENARIO_VIKINGS`. The installed and decompiled manifests have the same SHA-256 hash, `6B5F05148CD513726456690EA9FAC96A027E3158987A75E393C3A618B73FDE06`. It registers two formal replacements with no load order: `PartialScreenHooks_VikingsScenario.lua` and `WorldRankings_VikingsScenario.lua`. CAI's load-order-100 replacements therefore win both contexts and must explicitly compose the official wrappers. The scenario also imports its same-name full `ReligionScreen.lua` and its custom `WorldRankings.xml`.
- Add a ruleset-first branch to `PartialScreenHooks_CAI.lua` that includes `PartialScreenHooks_VikingsScenario` before the expansion/base chain. The wrapper deliberately keeps Espionage and City States locked by overriding `CheckSpyCapacity`, `OnDiplomacyMeet`, and `CheckCityStatesUnlocked`. Scenario data also deletes `CAPABILITY_CITY_STATES_VIEW`, `CAPABILITY_TOP_PANEL_ENVOYS`, and the Government Screen diplomacy filter. CAI's City States action should continue reporting the capability-based unavailable message, while its Espionage action needs an explicit Vikings suppression equivalent to Australia's because the Vikings data does not delete an Espionage capability.
- Add `WorldRankings_VikingsScenario` to the ruleset-first include chain in `WorldRankings_CAI.lua`. Its imported layout has three Overall controls. Accessible Overall should expose three stable rows matching those controls: `LOC_VIKING_SCENARIO_DESCRIPTION`; `LOC_VIKING_SCENARIO_RELIGION_DESCRIPTION`; and the three scoring explanations joined in official order with the wrapper's exact numeric arguments: `(25, 10, 50, 25, 10)`, `(1000, 500, 300, 100)`, and `(50, 50, 1, 10, 1, 5)`. The ordinary Score view remains authoritative and should continue through CAI's captured score population.
- Vikings scoring retains City, Population, Great Person, Trade, and Pillage line items with scenario multipliers, plus `ScoringScenario1` for Vinland Admiral discovery awards, `ScoringScenario2` for religion-dependent raiding/settling awards, and `ScoringScenario3` for the final-turn Viking Gold/Faith bonus. The existing Score tree reads the live category totals and should require no scenario branch. The gameplay script announces individual awards only through `Game.AddWorldViewText`, which has no existing CAI capture path; persistent totals remain available on demand in World Rankings. Treat immediate score-floater speech as a separate enhancement rather than duplicating scenario scoring rules inside the UI integration.
- The scenario's imported `ReligionScreen.lua` is a same-name full replacement, so CAI's ordinary `include("ReligionScreen")` should resolve it automatically; do not copy it or add a named include that does not exist. The official screen forces `ViewMyReligion()` into read-only review, because turn-one gameplay assigns Odin pantheons to Denmark, Norway, and Sweden, Catholicism to Rome, Islam to Al-Andalus, and preset pantheons to the remaining Christian powers. CAI must return no setup state under `RULESET_SCENARIO_VIKINGS` even if generic belief counters suggest founding or adding beliefs. The founded-religion summary should use the scenario's intended maximum of two, Catholicism and Islam. Continue using the live visible belief-instance count so those two founded religions retain the official two-unlocked/two-locked presentation, while a playable Viking's own row remains a pantheon-only view.
- Scenario Setup is data-driven: only Denmark/Cnut, Norway/Harald, and Sweden/Olof are human-playable; Al-Andalus, Byzantium, and Francia occupy locked AI slots; the Standard map, six major players, eighteen scenario city-states, and 60-turn Score victory are fixed by configuration. LoadScreen consumes the shared `LOC_LOADING_INFO_VIKING_SCENARIO`. No scenario EventPopup definitions or UI replacements exist.
- The scenario deletes `UNITOPERATION_TELEPORT_TO_CITY` and `UNITOPERATION_PLANT_FOREST`; CAI's existing UnitPanel result-registration guard for scenario-pruned rows covers the known load hazard. Great People are data-driven Generals and Admirals, including the five Vinland-capable Admirals; Tech/Civic, Government, Production, Unit, City, Trade, Great People, Civilopedia, diplomacy, and end-game screens have no additional Vikings UI wrapper. Validate their scenario data through existing accessible paths instead of adding ruleset branches unless testing exposes a mismatch.
- Safe implementation order: (1) wait for the pending Poland World Rankings/Religion regressions because Vikings changes the same shared files; (2) compose the Vikings PartialScreenHooks and World Rankings wrappers; (3) add the three accessible Overall rows; (4) make Religion Screen read-only with a two-religion maximum; (5) run static ruleset/include/tag/argument and scenario-pruned-operation checks; (6) execute the in-game matrix.
- In-game matrix: Scenario Setup for all three playable leaders and the three locked AI slots; fixed map/player/turn settings and LoadScreen narration; unavailable City States and Espionage actions; World Rankings three-row Overall order and all live Score categories/totals; Odin pantheon-only views for all three Viking civilizations; Catholic and Islamic religion rows with two-of-four beliefs, no setup/add-belief controls, and `2/2` founded religions; faith-pressure changes and religion-dependent scoring; Vinland discovery with first/second/third/later Admiral awards; city/population, Great Person, trade, pillage, raiding/settling, and turn-60 Gold/Faith score updates; scenario units, buildings, governments/policies, techs/civics, Great Generals/Admirals and activation requirements, Civilopedia pages, diplomacy/war state, final Score victory and end-game controls; close/reopen focus; and ordinary-game PartialScreenHooks, World Rankings, Religion Screen, and UnitPanel regressions.
- CAI implementation 2026-07-17 selects the official Vikings PartialScreenHooks and World Rankings wrappers before expansion/base files. The accessible Espionage action reports unavailable, while City States continues through the scenario-deleted capability. World Rankings exposes the official description, religion rule, and three joined scoring explanations as three stable Overall rows and retains the generic live Score capture. Religion setup is always read-only, the founded-religion summary uses the official maximum of two, and belief totals continue following the imported scenario screen's live instances. No scenario files, modinfo registrations, or new localization tags were required.

## War Machine scenario UI replacements and composition plan

- `DLC/WarMachineScenario/WarMachineScenario.modinfo` activates for `RULESET_SCENARIO_WARMACHINE` and registers four formal replacements with no load order: `ActionPanel_WarMachineScenario.lua`, `TopPanel_WarMachineScenario.lua`, `WorldRankings_WarMachineScenario.lua`, and `ARXManager_WarMachineScenario.lua`. CAI's load-order-100 replacements therefore win the first three relevant contexts and must explicitly compose their official wrappers. ARX has no CAI accessibility context to merge. The installed and decompiled manifest and three relevant wrapper files have matching SHA-256 hashes; the manifest hash is `1338F8B1ABA806D0BDE2652C3925B15458A6A339189E030F48CDB47895D2B104`.
- Add ruleset-first branches to `ActionPanel_CAI.lua`, `TopPanel_CAI.lua`, and `WorldRankings_CAI.lua`, before the Black Death and expansion/base branches. Each official War Machine file already includes its required underlying UI (`ActionPanel_Expansion2`, `TopPanel_Expansion2`, or its own full World Rankings implementation), so CAI must not include an expansion/base file a second time. No scenario file copy or CAI modinfo registration is needed because the scenario imports all three wrappers and its World Rankings XML.
- `ActionPanel_WarMachineScenario.lua` overrides `OnRefresh()` after including Expansion 2. It deliberately calls `BASE_OnRefresh()` and hides Era/Age controls, animations, score, thresholds, and timer-warning art because the scenario deletes `CAPABILITY_ERAS`. CAI's existing `OnRefresh` wrapper can remain outside this official override and continue speaking the live end-turn tooltip. Replace the current Black-Death-only Era/Age hotkey exception with a capability check: when `CAPABILITY_ERAS` is absent, speak the existing localized unavailable message and do not inspect `Game.GetEras()`.
- `TopPanel_WarMachineScenario.lua` overrides `RefreshTurnsRemaining()` and is the authoritative source for the scenario's normalized `current/40` display and its daily calendar from August 4 through September 12, 1914. CAI's on-demand turn/time/date action already reads `Controls.Turns` and `Controls.CurrentDate`, so composing the wrapper preserves the correct values. The turn-begin announcement should call `RefreshTurnsRemaining()` and read those same live controls instead of combining raw `Game.GetCurrentGameTurn()` with a possibly stale date.
- `WorldRankings_WarMachineScenario.lua` is a full older World Rankings implementation paired with an imported XML whose Overall view contains only `WarMachine1`, `WarMachine2`, and `WarMachine3`. Its official `ViewOverall()` fills them, in order, from `LOC_WARMACHINE_SCENARIO_NAME`, `LOC_WARMACHINE_SCENARIO_ERA_INDUSTRIAL_DESCRIPTION`, and `LOC_VICTORY_WARMACHINE_SCENARIO_TT`. The first value is only the scenario title and duplicates the surrounding context, so CAI intentionally omits it. The accessible Overall view contains two stable `StaticText` rows: the scenario description followed by the France/Germany objective.
- Unlike the current base/expansion World Rankings files, the War Machine full replacement declares `TAB_SCORE`, `TAB_OVERALL`, `TAB_SCIENCE`, `TAB_CULTURE`, `TAB_RELIGION`, and `TAB_DOMINATION` as private locals. CAI cannot read those values after `include(...)`. Define CAI-owned localized tab labels with the ordinary exported globals as the first choice and the corresponding `LOC_WORLD_RANKINGS_*_TAB` lookups as fallbacks, then use those CAI labels for captured-tab mapping, rebuild dispatch, and view synchronization. This preserves ordinary wrappers and prevents nil tab mappings under War Machine.
- Only `VICTORY_SCORE` remains enabled. The scenario renames it to War Machine Victory and makes `CATEGORY_SCENARIO1` / `LINE_ITEM_CONTROL_OF_PARIS` the only scoring category with a nonzero multiplier. The gameplay script initializes France's `ScoringScenario1` value to `1`; Germany's capture of Paris ends the game through the victory requirement rather than a UI-side score calculation. CAI's existing official Score population capture should expose the live player/team ordering, score, and Control of Paris detail without a War Machine-specific score implementation.
- `EndGameMenu_WarMachineScenario.lua` is an addition loaded automatically by base `include("EndGameMenu_", true)`, the same wildcard host that imports `EndGameMenu_CAI.lua`. It changes the Score victory style to the War Machine movie and time ribbon. Scenario data disables One More Turn for every victory/defeat, gives France victory at the turn limit, gives Germany victory on capturing the Paris plot, and gives Germany a time defeat when the limit expires. Preserve the wildcard composition; do not copy or directly include the addition. Although five historic-ranking localization pairs ship, the scenario deletes every `HistoricRankings` database row and adds none, so CAI should correctly omit the Ranking tab rather than exposing those unused strings.
- War Machine is multiplayer-only (`SupportsSinglePlayer=0`), so exclusion from the single-player Scenario Setup list is correct. Its accessible launch path is the ordinary Host Game flow, including Hotseat: fixed 40 turns, fixed WWI Western Front map, France and Germany in the two selectable participant slots, and Britain and Belgium in locked slots. The existing data-driven HostGame and StagingRoom accessibility should expose ruleset choice, leaders, player state, validation, and launch without a scenario-specific frontend branch.
- Remaining content is data-driven. The ruleset removes culture/civics, governments, religion, trade, espionage, governors, Great Works, climate, World Congress, most lenses, and many ordinary units/buildings, while retaining six scenario technologies, a compact military roster, railroad-building Military Engineers, four generic Great Generals, permanent alliances and wars, and Luxembourg as a Trade city-state with France initialized as suzerain. Existing capability checks and live Tech Tree, Production, Unit, City, Great People, City States, Civilopedia, diplomacy-status, combat, and cursor paths should consume this data. The existing UnitPanel hash guard is required because the scenario prunes operations/commands such as `UNITOPERATION_PLANT_FOREST`, `UNITOPERATION_CLEAR_CONTAMINATION`, and `UNITCOMMAND_ACTIVATE_GREAT_PERSON` before re-adding only the supported command types.
- Safe implementation order: (1) add the ruleset-first Action Panel, Top Panel, and World Rankings includes; (2) generalize Era/Age unavailability to `CAPABILITY_ERAS`; (3) make turn-begin speech refresh and read the official Top Panel controls; (4) add the two-row accessible Overall branch; (5) statically verify include order, imported-file availability, localization order, wildcard End Game composition, capability gating, and absence of CAI modinfo changes; (6) run the planned in-game matrix.
- In-game matrix: create/launch Internet or Hotseat games; verify France/Germany choice and locked Britain/Belgium slots; LoadScreen player/civilization details; correct turn `1/40` through `40/40` and August/September dates on demand and at turn start; end-turn blocker speech with no Era/Age data; two World Rankings Overall rows with no redundant title; Score with live Control of Paris data and multiplayer filtering; France's turn-limit victory, Germany's Paris-capture victory, Germany's time defeat, War Machine end-game styling, no One More Turn, and no Ranking tab; six-tech tree including repeatable Industrial Warfare; military production, formations, Great General aura, engineers/railroads/forts/airstrips, aircraft, combat modifiers, city capture, Luxembourg/suzerainty, Civilopedia, and all capability-disabled actions; close/reopen and hotseat player handoff; ordinary Expansion 2 Action Panel, Top Panel, World Rankings, and End Game regressions.
- CAI implementation 2026-07-17 selects the official War Machine ActionPanel, TopPanel, and WorldRankings files before other scenario/expansion/base files. Era/Age reads follow `CAPABILITY_ERAS`; turn-start speech refreshes and reads the official turn/date controls; World Rankings uses CAI-owned tab-label fallbacks and exposes the description and objective as two Overall rows while omitting the redundant scenario-title row and retaining captured live Score data. EndGame remains automatic through its wildcard host. No scenario files, modinfo entries, or new localization tags were added.

## Civ Royale / Red Death UI replacements and CAI integration

- `DLC/CivRoyaleScenario/CivRoyaleScenario.modinfo` activates for `RULESET_SCENARIO_CIV_ROYALE`, is multiplayer-only (`SupportsSinglePlayer=0`), and registers thirteen formal replacements: `ARXManager`, `ActionPanel`, `UnitFlagManager`, `UnitPanel`, `ChatPanel`, `DiplomacyRibbon`, `FullscreenMapPopup`, `InGameTopOptionsMenu`, `MinimapPanel`, `SaveGameMenu`, `TopPanel`, `WorldTracker`, and `WorldInput`. It also adds the `ExpansionIntro` and `LeaderIcon` contexts and imports wildcard/addition files for `EndGameMenu`, `NotificationPanel`, the World Tracker global-ability instance, and the safe-zone/storm tracker panel.
- CAI's nine overlapping formal contexts select the Royale implementation first: `ActionPanel`, `ChatPanel`, `DiplomacyRibbon`, `InGameTopOptionsMenu`, `MinimapPanel`, `TopPanel`, `UnitPanel`, `WorldInput`, and `WorldTracker`. `UnitFlagManager_CAI.lua` already selected `UnitFlagManager_CivRoyaleScenario`. CAI does not replace `ARXManager`, `FullscreenMapPopup`, or `SaveGameMenu`, so Firaxis retains those directly. `EndGameMenu_CivRoyaleScenario.lua` and `NotificationPanel_CivRoyaleScenario.lua` are wildcard additions and continue composing automatically.
- Civ VI `include()` can resolve only files exported by an active `ImportFiles` action; listing a script under the DLC's top-level `Files` and using it as `LuaReplace` does not make it includeable by CAI. The Royale action exports `ActionPanel`, `ChatPanel`, `UnitPanel`, and `WorldInput`, but does not export the `DiplomacyRibbon`, `InGameTopOptionsMenu`, `MinimapPanel`, `TopPanel`, or `WorldTracker` replacement scripts. Attempting to include those five names fails silently and leaves their expected globals nil. CAI therefore carries normalized-exact, collision-safe `*_CivRoyaleScenario_CAIBase.lua` copies of those five installed Firaxis scripts under `UI/Replacements/CivRoyaleScenario`, registers each in both the top-level file list and `CAIInGame` imports, and includes those names. All copied non-imported Firaxis replacements live under `UI/Replacements/<source DLC>` rather than beside authored in-game CAI scripts. Preserve exact parity with the installed sources when updating them. The files include their own base where applicable; Royale `TopPanel` is standalone.
- `ActionPanel_CivRoyaleScenario.lua` preserves ordinary end-turn behavior for living players and raises `ActionPanel_ObserverModeTurnBegin` instead when the local player has been eliminated. Compose it before CAI's existing refresh wrapper so observer turns refresh the scenario Top Panel rather than running a dead player's normal action refresh.
- `UnitPanel_CivRoyaleScenario.lua` is essential gameplay composition. It adds the scenario command table to `GetUnitActionsTable`: Scientist shielding, Prepper traps, Alien cloaking, Cultist sacrifice, and Mutant radiation-spread toggling. It intentionally keeps the Wanderer Road Vision, Pirate Burn Treasure Map, and Edgelord Grieving Gift commands out of the per-unit panel because those are global abilities. It also exposes radiation/trap charges, paired formation-unit data, faction portrait variants, scenario heal/WMD restrictions, and combat-preview forwarding to unit flags. CAI's accessible action list already consumes the final `data.Actions` entries and their callback/disabled tooltip, so selecting the wrapper should expose the five per-unit commands with minimal special code. Add explicit accessible state text for the Mutant toggle if the final tooltip does not communicate active/inactive state.
- `WorldTracker_CivRoyaleScenario.lua` adds the three faction-wide actions: Wanderer Road Vision, Pirate Burn Treasure Map, and Edgelord Grieving Gift. It supplies live availability, cooldown, charges, tooltip, command callback, and selected interface mode. CAI reports the applicable ability's official description, availability/remaining turns, and charges at the end of turn information. Under Royale, the existing `UI_OpenWorldCrisisTracker` action (Ctrl+W by default) activates the live global ability instead of opening the unavailable emergency tracker. Wanderer and Pirate abilities reproduce the official `UnitManager.RequestCommand(... EXECUTE_SCRIPT, PARAM_NAME=EventName)` path; Grieving Gift selects the official default unit only when no head unit is selected and enters its official interface mode. Disabled activation speaks the official live cooldown/charge explanation.
- “Global” means player-owned rather than independent of units. The World Tracker retrieves the player's first unit with `UnitManager.GetUnit(localPlayerID, nil)` and uses it as the carrier for scripted command requests. Road Vision makes every previously revealed plot temporarily visible, is active for one turn, then recharges for three turns and becomes ready four turns after activation. Burn Treasure Map immediately chooses a new valid treasure location and recharges for five turns. Grieving Gift starts with two charges, stores at most two, marks a valid target with `DeferredGiftOwner` so the fake supply drop appears on the following turn, and consumes one charge. Spending from the full two-charge state starts a five-turn recharge; when it completes, the turn update adds two charges capped at two, so the stock returns to full at once.
- The game does not publish a global notification, announcer line, or opponent-status card when another player activates a global ability. Road Vision and Burn Treasure Map only call `Game.AddWorldViewText(...)` at the carrier unit's plot with `Visibility = RevealedState.VISIBLE` and no player target, so their activation text is merely a visible-map flyover that an opponent can witness when that plot is currently visible; it is not an everywhere announcement. A successful Grieving Gift emits no flyover at all—the success text is commented out—and later presents the gift to other players as an ordinary supply drop without revealing its owner or activation. Its invalid-target flyover is explicitly targeted back to the activating owner. CAI must not announce remote global-ability activation generally because that would expose information vanilla withholds.
- The official global-ability card has four meaningful accessible states: absent for factions without a global action; `Available`; Road Vision `Active, N turns`; and `Recharging, N turns`. Grieving Gift also displays `current/max` charges and remains usable with one charge while its replenishment timer runs; it disables only at zero charges. The cooldown meter is visual duplication of the live last-used turn and duration/debounce values. CAI should announce the localized name, state, charges when present, and the official explanatory/disabled tooltip, and should refresh after `UnitCommandStarted`, after leaving Grieving Gift's interface mode, and at ordinary World Tracker refreshes.
- `WorldInput_CivRoyaleScenario.lua` registers the `GRIEVING_GIFT` interface mode and converts the chosen cursor plot into `ScenarioCommand_GrievingGift`. Compose it before CAI's World Input layer, then expose the mode through the existing interface-widget, interface-info, valid-target scanner, and primary-action paths. Validate targets with the final scenario `IsSelectionAllowedAt(...)`; do not recreate its target rules.
- `MinimapPanel_CivRoyaleScenario.lua` is also essential gameplay composition. It instantiates `CivRoyaleScenario_Announcer` and manages safe-zone borders/fills, Red Death fallout, supply drops, city ruins, raider camps, Pirate treasure, Prepper traps, Edgelord gifts/fake drops, Mutant fallout, and Zombie hunger-wave overlays. It also refreshes these from turn, phase, notification, improvement, WMD-fallout, defeat, and victory events. Select this wrapper before CAI's lens layer. Extend scanner/surveyor access from the official live plot properties, improvements, fallout manager, and visibility rules; visual overlay colors alone are not an accessible data source.
- The announcer is localized prerecorded scenario voice-over, not a visual overlay and not TTS. Its Minimap-side initialization subscribes to faction-specific opponent eliminations, local defeat, safe-zone appearance/shrink, every WMD detonation, local victory, and loss of a local Settler to fallout. It also has a 4% turn-end chance of a flavor quote, with at least five turns between random quotes. Composing the official Minimap wrapper preserves this audio automatically; CAI should not duplicate it, though corresponding live notifications/status speech remain independently useful.
- Overlay disclosure rules are gameplay-significant. The current safe-zone hexes and ordinary Red Death fallout are global. Mutant-created fallout is globally visible to Mutants but shown to other factions only while the plot is currently visible. Real supply drops and another player's Grieving Gift are presented alike as supply drops; the owner alone sees their placed/deferred gifts as fake or incoming gifts. City ruins and raider camps require the plot to be revealed or visible. Only Preppers receive the improvised-trap overlay. Pirate treasure uses only the local Pirate player's private `TreasurePlotIndex`. Only Zombies receive hunger waves: for each Zombie unit, the UI points toward the closest otherwise unseen enemy unit within seven hexes. CAI cursor/scanner work must reproduce these exact disclosure rules and must not expose private trap, gift, treasure, Mutant-fallout, or hidden-unit information to the wrong faction.
- The geometric map center is not a gameplay target. The initial safe-zone center is chosen randomly from eligible plots away from non-wrapping map edges, preferring sufficiently large landmasses. Every later center is randomly selected within the center-shift radius that keeps the smaller new zone entirely inside the current zone. The current `SafeZoneX` / `SafeZoneY` therefore move and can differ substantially from the map center. Sighted disclosure is based on the current safe-zone boundary/fill on the world view and minimap plus the location-bearing Safe Zone Appeared/Changed notification. Accessible direction, distance, and jump behavior should use the current safe-zone center or nearest safe-zone boundary, not the static map center.
- Cursor and scanner integration does not read `UILens` colors. `CivRoyaleMapInfo_CAI.lua` classifies one live plot for one local player from safe-zone center/radius properties, `Game.GetFalloutManager()`, plot improvements/properties, player faction, and official visibility. Automatic cursor movement still speaks only the changed three-state zone name, while the cursor-move plot-tooltip bucket identifies the exact live center plot as `Safe zone center`. The B-bound `PlotReadGeography` bucket—not the X statistics bucket—uses a separate detailed read: a safe plot says `Safe zone`, the cursor-relative direction to the live safe-zone center, then the official turns-until-shrink/minimum-size status; ordinary Red Death says `Red Death` followed by the same official category and damage text as T; an outside plot says only `Outside of safe zone`. Ordinary Red Death retains precedence over safe-zone membership, and no path announces a clear-of-Red-Death phrase. The dedicated Red Death scanner category exposes the live safe-zone center as a separate group under the Safe zone subcategory, collapses safe-zone, ordinary Red Death, and disclosed Mutant fallout plots into connected regions, and lists private Pirate treasure, faction-visible traps/gifts, and Zombie hunger targets. Supply drops, city ruins, and raider outposts are deliberately omitted from the Royale-specific cursor/scanner object source because the game's ordinary tribal-village handling already exposes them. All Royale-only consumers share the same disclosure implementation.
- `TopPanel_CivRoyaleScenario.lua` replaces ordinary yields with normalized turn/date/time, turns until the ring shrinks, safe-zone phase, current fallout damage, owned Nuclear/Thermonuclear/Hail Mary devices, and a button selecting the first Settler standing in harmful fallout. CAI's existing turn/time action and turn-start announcement read the refreshed official fields, then append the countdown/minimum state, phase and damage, nonzero WMD counts, endangered-Settler warning, and global-ability information. Standard yield/report/Civilopedia actions remain governed by capabilities.
- Royale status is live game state rather than a stored UI model. `NextSafeZoneTurn - Game.GetCurrentGameTurn()` is the countdown; `StartSafeZoneTurn` is the duration used to draw its progress; `SafeZonePhase` starts at zero before the first zone; and `Game.GetFalloutManager():GetFalloutDamageOverride()` is the current per-turn Red Death damage. Successive safe-zone delays are 10, 10, 8, then five 6-turn phases, then 4 turns for all later phases. Damage progresses from 10 in the pre-zone/first phase through 15, 20, 25, 30, 35, and finally 40 for later phases. When `NextSafeZoneTurn == -1`, the zone has reached minimum size and the countdown is hidden. The separate Settler warning scans the local player's Settlers and selects the first one standing in fallout; Mutant-created fallout is safe for a Mutant Settler and does not trigger it. Top Panel status refreshes after load, local-player turn start/change, unit movement, WMD-count changes, restored visual state, hotseat handoff, and observer-turn refresh.
- User decision 2026-07-24: do not create a separate Royale status readout. The existing Top Panel “speak turn/time/date” output under `RULESET_SCENARIO_CIV_ROYALE` preserves its ordinary turn/date/time fields, then appends safe-zone countdown or minimum-size state, phase and damage, nonzero WMD counts, and the harmful-fallout Settler warning. The local faction's global-ability name/state/remaining turns/charges and official explanatory tooltip are last. Ctrl+W activates that global ability in Royale.
- Implemented accessible additions include Ctrl+W global-ability activation, Grieving Gift interface-mode navigation/confirmation/cancellation and valid targets, eliminated-player ribbon entries, Royale observer-chat filtering/labels, both the front-end matchmaking tutorial and in-game Royale intro, End Game Observe, Mutant radiation-spread active/inactive state, and spatial cursor/scanner coverage.
- The DLC ships and imports `WorldTrackerPanel_CivRoyaleScenario.lua/.xml`, a compact ring/storm display using `NextSafeZoneTurn`, `StartSafeZoneTurn`, `SafeZonePhase`, and `Game.GetFalloutManager():GetFalloutDamageOverride()`. No `AddUserInterfaces` entry, `<LuaContext>` host, or other reference to that filename exists in the installed DLC, so it appears orphaned and should not be explicitly integrated unless an in-game trace proves that Civ VI instantiates imported XML by another mechanism. The authoritative values already remain available from the scenario Top Panel and game state.
- `DiplomacyRibbon_CivRoyaleScenario.lua` keeps eliminated opponents in the ribbon, marks them eliminated, preserves player names in multiplayer tooltips, and stops dead-player turn animations. After composing it, make CAI's accessible ribbon enumerate `PlayerManager.GetWasEverAliveMajors()` under Royale rather than only `GetAliveMajors()`, with live alive/eliminated state and official player/faction identity masking.
- `ChatPanel_CivRoyaleScenario.lua` keeps observers out of living-player chat, labels observer senders after elimination, shows faction names, preserves disconnected observer entries, and clears kick-vote instances on entering observer mode. CAI's `OnChat` wrapper can wrap this final implementation; audit the CAI player list and stored accessible chat line so observer visibility, faction name, and player name match the final scenario rules.
- `InGameTopOptionsMenu_CivRoyaleScenario.lua` adds the How to Play button that opens the Royale three-page `ExpansionIntro`. CAI also replaces the `ExpansionIntro` context, so `ExpansionIntro_CAI.lua` must select the Royale intro under this ruleset instead of base `ExpansionIntro`; its current generic dialog can then read the official two description controls and Previous/Next/Close buttons. The ordinary expansion-specific “do not show again” checkbox does not exist in the Royale XML and must not be assumed.
- `FullscreenMapPopup_CivRoyaleScenario.lua` changes revealed minimap tooltips for wasteland, Red Death, safe zone, city ruins, raider camps, and supply drops. `SaveGameMenu_CivRoyaleScenario.lua` uses the faction/leader plus normalized turn in default filenames. CAI does not conflict with either context; retain them and test only.
- `LeaderIcon_CivRoyaleScenario.lua` supplies multiplayer player-name tooltips and is consumed by the official Diplomacy Ribbon wrapper. `UnitFlagManager_CivRoyaleScenario.lua` adds faction portraits/player names and a transient enemy combat-preview health bar; CAI already composes it. `ARXManager_CivRoyale.lua` only serves Logitech ARX and needs no accessibility integration.
- `NotificationPanel_CivRoyaleScenario.lua` is loaded by the base Notification Panel wildcard. It validates changing Pirate buried-treasure notifications, dismisses supply-drop/gift notifications when the improvement disappears, and assigns the centered `UI_Royale_Ring_Warning` sound only to `USER_DEFINED_2` (Safe Zone Changed) and `USER_DEFINED_3` (Safe Zone Appeared). `ShowSafeZone()` sends one of those location-bearing notifications at the start of each new safe-zone phase, during turn processing; this is the occasional non-positional mechanical warning heard between turns. Ordinary per-turn danger-ring contraction and supply drops do not use this sound. CAI's Notification Center should consume the resulting live notifications normally and preserve their location activation/dismissal rules.
- `EndGameMenu_CivRoyaleScenario.lua` is loaded by the End Game wildcard. It supplies Royale victory/defeat styles, limits replay graphs to combat/destruction data, adds Observe while multiple players remain, and supports leaving/re-entering the result screen around observer mode. Observe calls `Close()`, destroying CAI's panel, while the later final-result path enters the scenario's observer-specific `OnShow`: that branch calls `OnShowEndGame()` / `OnInfoTab()` and deliberately bypasses `BASE_OnShow()` and therefore `ShowComplete()`. A CAI wrapper that repushes only from `ShowComplete` will leave the visible final result screen inaccessible. Wrap the final `OnShow` before `Initialize()` registers it and, only for a visible Royale local observer with no built CAI panel, restore always-receive-input state and rebuild/push the panel. No scenario-specific World Rankings replacement exists because the ruleset deletes the World Rankings and Score display capabilities.
- The ruleset retains only `CAPABILITY_DISPLAY_NORMALIZED_TURN` from its scenario capability file and deletes ordinary civics/tech trees and choosers, Civilopedia, diplomacy, faith/religion, governments, trade, espionage, reports, score/World Rankings, climate, World Congress, standard top-panel yields/resources, and most lens UI. Existing CAI hotkeys must use capability gates and announce unavailable rather than dereferencing removed APIs or opening empty panels.
- Required in-game verification covers all five per-unit faction commands, all three global-ability status variants, Grieving Gift targeting entered through the official visual control, overlay/announcer refresh, every scanner disclosure rule, eliminated-player observer/chat lifecycle, intro paging, End Game Observe, and ordinary base/XP1/XP2/Black Death regressions for the shared contexts.
- In-game matrix: launch through Internet/LAN/Hotseat with each of the ten factions; verify custom and global faction abilities, cooldowns/charges/toggle state and valid targeting; ring countdown, phase, minimum size, fallout damage and warning audio; safe-zone/fallout/supply-drop/ruin/camp/treasure/trap/gift/fake-drop/mutant/zombie map objects with visibility masking; endangered Settler selection; all three WMD counts and Hail Mary cooldown; linked formations and faction portraits; notifications and location jumps; chat before/after elimination; eliminated ribbon entries; Observe, observer turns, end-game return, and remaining-player victory; How to Play pages; save filename and fullscreen-map tooltips; capability-disabled CAI actions; close/reopen/focus behavior; and ordinary-game regressions for Action Panel, Chat, Diplomacy Ribbon, pause menu, Minimap, Top Panel, Unit Panel, World Input, World Tracker, Expansion Intro, Notification Center, Unit Flags, and End Game.

## Tech and civic boost popup

- The boost event's `iTechProgress` / `iCivicProgress` value is the progress before the boost. After a completion boost, `PlayerTechs:GetResearchProgress(index)` or `PlayerCulture:GetCulturalProgress(index)` can already return `0` because the completed item is no longer progressing. Use `PlayerTechs:HasTech(index)` / `PlayerCulture:HasCivic(index)` as the authoritative completion state; completed boosts end at 100% and use the vanilla localized completion description.

## Research and civic chooser relationships

- Chooser rows can derive their outgoing tree relationships by reversing `GameInfo.TechnologyPrereqs()` (`PrereqTech` -> `Technology`) or `GameInfo.CivicPrereqs()` (`PrereqCivic` -> `Civic`). Use the local player's `IsTechRevealed(index)` / `IsCivicRevealed(index)` result before speaking each destination name; unrevealed destinations use the same localized `Not revealed` label as their corresponding tree.
- The full Technology and Civics Tree replacements should build accessible graph edges from the final composed vanilla `g_kItemDefaults[itemType].Prereqs` rather than independently querying database tables. That table is populated after the active base/expansion/scenario replacement is included and therefore matches the nodes and prerequisite relationships actually owned by the live screen. Exclude `PREREQ_ID_TREE_START`; reverse edges are `prerequisite -> dependent`. Add graph nodes in deterministic era, prerequisite-column, `UITreeRow`, then type order so multiple incoming/outgoing choices remain stable.
- Civ V Access's `NavigableGraph` retains the relationship set produced by the last edge move. Descending to a dependent seeds all dependents of the departed prerequisite; ascending seeds all prerequisites of the departed dependent. It has one explicit exception: when ascending lands on a root, it replaces that incoming set with the complete root set. Civ VI Graph must preserve that exception, so Pottery -> Writing -> Left to Pottery lets Up/Down traverse the other roots, while Mining -> Masonry -> Construction -> Left to Masonry lets Up/Down traverse Construction's other prerequisite, Horseback Riding.
- Technology and Civics Trees persist their three-mode selection independently through profile-global `UI/TechTreeViewMode` and `UI/CivicsTreeViewMode` settings. Valid values are `grid`, `graph`, and `tree`; missing or invalid values fall back to `grid`. The Grid, Graph, and Tree containers on each screen use the same main-list label, while the widget role identifies the active structure.
- Graph navigation must not treat unrevealed items as hidden. Vanilla still draws their connectors, and the existing tree disclosure policy exposes their era/tier location while masking their name and details. Temporary filter results may narrow the inactive graph while the modal result list owns focus, but closing results restores the unfiltered graph before jumping to the chosen node; Graph must never traverse through an omitted node to synthesize an edge.

## Tutorial control and input mirroring

- Tutorial callouts have two distinct definition paths. `TutorialItem:SetAdvisorCallout(...)` localizes and stores `AdvisorInfo.CalloutHeader`, `AdvisorInfo.CalloutBody`, and `AdvisorInfo.PlotCallback`; `AdvisorPopup.ShowAdvisorPopup(...)` copies those values into its world-anchor callout controls. Other pointers are static `<Tutorial TriggerBy="...">` controls distributed across screen XML, and some contain only an arrow.
- Civ VI controls expose `RegisterWhenShown(callback)`, `GetChildren()`, `GetType()`, and `GetText()`. Central traversal from `/InGame/` successfully receives shown callbacks for static screen-control callouts. CAI registers controls whose runtime type is `Tutorial`, except `TutSelectUnit`; it reads only non-empty leaf-descendant text, deduplicates equal leaf strings, and buffers a control/text combination once per active tutorial item.
- Advisor and detailed screen pointers still converge at `UITutorialManager:ShowControlsByID`: `AdvisorPopup.ShowAdvisorPopup(...)` uses `advisorData.UITriggers` with `false`, while `TutorialUIRoot.RaiseDetailedTutorial(...)` uses the item's detailed `UITriggers` with `true`. This does not prove that their displayed text shares one data source; static screen labels remain defined in their screen XML.
- `TutorialUIRoot_CAI` uses a hybrid buffer path. Static screen pointers come from their native shown controls. `TutSelectUnit` is excluded because AdvisorPopup and Automation_NarrationPopup both define it; explicit world anchors instead come once from `item.AdvisorInfo` after `RaiseDetailedTutorial(...)`. A non-nil `PlotCallback` distinguishes `SetAdvisorCallout(...)` data from every item's default placeholder header/body. Header and body are already localized, are joined into one entry, and carry a map location resolved from either a numeric plot index or a Plot object. The dedicated `tutorial` category and default-enabled `SpeakMessageBufferTutorials` preference remain.
- `TutorialUIRoot.RaiseDetailedTutorial(item)` is the point where Firaxis exposes the interactive phase. It shows every `item.UITriggers` entry, enables every `item.EnabledControls` value through `UITutorialManager:EnableControlsByIdOrTag`, applies `item.DisabledControls`, and then enables the overlay. CAI publishes the active detailed item and its enabled ID/hash set through `ExposedMembers.CAI_TutorialState`; CAI-only screen hotkeys must consult that set. This mirrors the tutorial overlay without globally swallowing manager input or changing vanilla control state.
- During the tutorial, CAI's replacement pause handling may open the options menu only when `Input.GetActiveContext() == InputContext.World`. Shell-context screens retain their own Escape behavior.
- The tutorial explicitly allows closing the technology tree through `TechTreeModal`, the civics tree through `CivicsTreeModal`, and the Pantheon/Religion flow through `ModalControls`. `IsCAITutorialScreenCloseAllowed(...)` reads the published active phase and enabled-control sets: outside the tutorial or with no active item it returns true; an advisor/non-detailed item returns false; a detailed item returns true only when one of the supplied vanilla close-control IDs/hashes is enabled. With no supplied control it remains false until free roam. This avoids hard-coding tutorial item names and keeps Research/Civics Choosers, Government, Production, World Rankings, Great People, Diplomacy, and Trade Route Chooser open while their instructions own them. Screen handlers let nested Escape behavior such as closing a filter, picker, confirmation dialog, conversation, or search run first, then apply the policy before Escape can reach the full-screen vanilla close path. A rejected full-screen close calls `AnnounceCAITutorialScreenCloseBlocked()` and speaks the dedicated closing message; it must not reuse the opening-hotkey failure localization.
- ResearchChooser and CivicsChooser have no tutorial close-control exception. Their vanilla `OnInputHandler` delegates Escape to the slide animator, but CAI must withhold that call while a required chooser instruction is active because the scenario enables only the required research/civic row. With no active tutorial item, the shared policy returns true and Escape reaches the vanilla animator normally.
- The tutorial `MOVE_WARRIOR`, `MOVE_WARRIOR_B`, `MOVE_WARRIOR_C`, and `MOVE_SETTLER` items explicitly enable `UnitOperationTypes.MOVE_TO` under `UnitFlagManager`. In Move To mode, CAI confirms the destination through `InterfaceWidgetPrimaryAction` on `Events.InputActionTriggered`, not through a widget Enter binding. Civ VI can invoke WorldInput's raw handler for that physical Enter release without delivering a corresponding raw key-down to the manager. Enter lifecycle protection must therefore apply only when the focused widget path owns a matching Enter binding; consuming unmatched Enter releases globally prevents the movement action from firing.
- `LuaEvents.Tutorial_DisableMapSelect(isDisabled, exceptionHexIds)` stores either nil, an empty list, or a sequential list of selectable plot IDs. Vanilla `WorldInput.IsSelectionAllowedAt(plotId)` linearly checks that list. CAI mirrors the event in `interfaceInfoHelpers_CAI.lua` and rejects accessible plot interactions outside the same allowed set.
- Tutorial movement restriction handlers are registered only by `EventSubs_CAI.lua`. `interfaceInfoHelpers_CAI.lua` defines state and handlers but must not subscribe itself, because World Input includes both modules.
- The tutorial production UI exposes only the choose-production tree. CAI omits its city list, sorting, purchase, and queue surfaces in tutorial mode. Production item hashes may be filtered only while a detailed tutorial item actually supplies enabled controls. Founding the second city opens Production through the normal city-founded flow while the scenario advances through advisor-only `FOUND_SECOND_CITY_C` / `FOUND_SECOND_CITY_D`; neither item has `UITriggers` or `EnabledControls`. Treating the absence of a detailed item as an empty production allow-list disables every row and is incorrect. In that state, CAI must defer to the live vanilla row disabled state.
- Production opened from the Action Panel can outlive the step which opened it. For example, `SELECT_END_TURN_PRODUCTION` completes on `ProductionPanelOpen`, immediately advancing to the advisor-only `SHOULD_TRAIN_BUILDER`, and then to the detailed `TRAIN_BUILDER` choice. The open panel therefore remains tutorial-owned even though the Action Panel step is already inactive. User cancellation through Production's Escape handler or close button uses the shared tutorial screen-close policy and is allowed again during free roam. Do not gate `Close()` itself: successful production selection uses `CloseAfterNewProduction()` -> `Close()`, and must still dismiss the panel while the choice item is active.
- A context passed to `UITutorialManager:AddControlToAlwaysReceiveInput(ContextPtr)` must be paired with `RemoveControlToAlwaysReceiveInput(ContextPtr)` on every close/shutdown path. Notification Panel does this only while its CAI panel is open and its input handler delegates to the CAI manager only while that panel is the stack root.
- Warrior actions remain restricted after the tutorial appears to open into free play because of the installed Firaxis scenario. Initialization disables Skip Turn and Delete globally plus Fortify, Heal, and Cancel for `UNIT_WARRIOR`. `POTTERY_TECH_SELECTED_C` later disables `UNITOPERATION_MOVE_TO` for the warrior. `FORTIFY_WARRIOR_B` enables Fortify and Heal, but its cleanup disables Heal and Cancel again. `FOUND_SECOND_CITY_D` eventually calls `UnlockUnit()` and re-enables the warrior's formation-exit command, but never restores Move, Heal, or Cancel; no later scenario item does so. Vanilla `UnitPanel.lua` puts this tutorial result in each action record's `Disabled` field and disables its visual button from that same field; `UnitPanel_CAI.lua` reads the unchanged `currentAction.Disabled` value. `TutorialUIRoot_CAI` repairs the omission from its `FOUND_SECOND_CITY_D` activation hook by calling the vanilla `EnableUnitAction` helper for Move, Heal, and Cancel. Calling the helper is required because it updates both persisted tutorial restriction state and the Unit Panel event listener. Tutorial-wide Skip Turn, Delete, and automation restrictions remain unchanged.

## Great Works Overview and Showcase parity audit

- The installed and decompiled base `GreatWorksOverview.lua` / `GreatWorkShowcase.lua`, Expansion 2 overview override, Babylon Heroes overview/showcase additions, and Kublai Khan/Vietnam Monopolies overview/showcase additions have matching SHA-256 hashes. These are the complete official Great Works UI variants in the installed game. Indonesia/Khmer's manifest-only `UI/GreatWorksOverview.lua` remains absent from both copies and is not an additional implementation.
- Both base screens are wildcard hosts. CAI's files are imported at load order 50 after the official additions and wrap the resulting globals, so Expansion 2 auto-theming, Heroes slot/tooltips/showcase handling, and Monopolies Product icons/tooltips/showcase handling remain loaded. The CAI picker uses vanilla `CanMoveWorkAtAll` and `CanMoveGreatWork`, preserving art lock turns, the full-artifact-museum rule, slot compatibility, and two-way swap compatibility.
- Accessible Product rows use the active Monopolies `GetGreatWorkTooltip` override and resolve their label's live corporation name from the Product resource. This preserves the corporation, Product yields/tourism, creation data, and `ResourceIndustries.ResourceEffectTExt` benefit. Showcase continues reading the final vanilla controls, preserving both Product corporation names and Heroes creator overrides.
- `GetValidSlotIndices()` applies Expansion 2's `GetGreatWorkSlotType(...) >= 0` rule and requires a live `GreatWorkSlotTypes` row. Both hierarchy modes and the move picker consume that filtered list, so ruleset-pruned or mod-invalid slots are skipped consistently.
- Vanilla `MoveGreatWork` keeps destination city/building in private locals for its themed-building completion/progress sounds. CAI records the pending source/destination ids around its operation request and temporarily wraps the exported destination getters only while the matching `GreatWorkMoved` refresh runs. This restores the official sound path without copying or mutating vanilla's private drag instances.
- Gallery Previous/Next moves focus to the details widget after vanilla updates the controls. The newly selected work is therefore spoken and becomes the navigation position regardless of whether cycling came from a button, Left/Right, or Alt+Left/Alt+Right.
- CAI's aggregate yield summary uses the same localized-name sort as vanilla, including Tourism in the ordered set.
- The `Open()` wrapper verifies both a local player and the visible vanilla context after calling the original, so a rejected direct/debug open cannot push an empty CAI panel.
- `GreatWorksGroupByBuildings` is a non-database CAI config value stored directly under section `UI`, with string default `"true"`, following the established internal-setting pattern in `CAI_logging.lua`. The screen keeps a live boolean initialized from `CAI.GetConfigValue("UI", "GreatWorksGroupByBuildings", "true")` and writes changes through `CAI.SetConfigValue`; it does not pass the key through metadata-driven `CAISettings`, `CAI_Settings`, `settings_CAI.sql`, or the global settings screen. Its screen-local checkbox is the Great Works panel's last child. Checked builds `building -> city -> slot` in the overview and `building -> city -> work` in the move/swap picker; unchecked builds `city -> building -> slot/work`. Slot/work focus keys and all move/view eligibility are shared by both hierarchies.
- In building-first mode, the outer building already supplies the building name. City-node tooltips therefore omit that repeated name while retaining per-city theming status and providing yields/tourism.
- Great Works resolves each slot-owning building instance to its plot by scanning the owning city's `Map.GetCityPlots():GetPurchasedPlots(city)` results and matching `CityBuildings:GetBuildingsAtLocation(plotIndex)`, the same authoritative path used by Reports. In city-first mode the slot-owning building row appends its live navigation-cursor-relative direction; in building-first mode the slot-owning city row does so because it represents that building instance. Enter on either row sends `LuaEvents.CAICursorMoveTo(plotIndex, "jump")` without closing the screen; the attached `activate` listener intentionally makes Enter an action while Left/Right retain tree expansion and collapse.
## Climate Screen vanilla parity audit

- The installed Gathering Storm `ClimateScreen.lua` / XML and Gran Colombia & Maya `ClimateScreen_GranColombia_Maya.lua` / XML exactly match the copies under `decompiled/`; no other official Climate Screen Lua variants are installed.
- Vanilla Event History gates a non-global event on revelation of `kEvent.CurrentLocation`, but displays the location from `kEvent.StartLocation`. CAI uses that same split: `CurrentLocation` decides visibility, while the spoken historical location and cursor jump use `StartLocation`.
- When the current random event is a `SEA_LEVEL` event, vanilla hides the ordinary current-event card and shows separate `New` indicators on Polar Ice and Sea Level. CAI carries the two official recent-update tooltip strings into the corresponding accessible rows.
- Vanilla current-event affected-city entries pair each city name with a civilization icon whose tooltip contains the owning civilization. CAI speaks each visible affected city as city plus owning civilization.
- Vanilla's CO2-by-civilization rows use `CivilizationIcon:SetLeaderTooltip(playerID)`. CAI speaks leader plus civilization for the local contribution and every met contributor, while retaining unmet-player masking. The numeric footprint, local-player contribution, per-resource contributions, and global resource totals match vanilla.
- A revealed, non-global current event exposes `CurrentLocation`; its accessible row appends the live navigation-cursor-relative direction and is an actionable `TreeItem`. A visible non-climate Event History row exposes `StartLocation`; its row appends the same live direction and is a `Button`. Enter sends `LuaEvents.CAICursorMoveTo(plotIndex, "jump")` without closing the Climate Screen. Global live events, unrevealed live events, and locationless climate-phase history rows remain non-actionable and do not expose a cursor direction.
- Although the Gran Colombia & Maya manifest formally replaces Climate Screen for Expansion 2, CAI must include `ClimateScreen_GranColombia_Maya` only when `GAMEMODE_APOCALYPSE` is active. Firaxis does not make its forest-fire localization tags available to the in-game locale database outside the mode; composing the wrapper there makes its controls and speech expose raw `LOC_CLIMATE_FOREST_FIRE*` tags. This conditional is intentional and must not be generalized to DLC ownership or ruleset alone. CAI's accessible forest-fire row remains additionally gated on both `GameClimate.GetFirePercentChance` and `RANDOM_EVENT_FOREST_FIRE_TRIGGERED`, while its comet-strike damage interpretation is reconstructed directly.
- CAI exposes all textual/numeric vanilla content: current weather identity/location/direction/effects/losses, climate phase and all seven phase tooltips, CO2 and temperature factors, world settings, all available forecast categories including mode-gated forest fire, polar ice, sea-level totals and forecasts, both CO2 breakdowns and last-turn consumption tooltips, and reverse-chronological event history. Vanilla's pie charts, colors, icons, phase-meter fill, wave position, and event art are visual encodings of data CAI already provides textually rather than additional numeric data.

## Pirates scenario

- `DLC/PiratesScenario/PiratesScenario.modinfo` activates for `RULESET_SCENARIO_PIRATES`. It is multiplayer-only (`SupportsSinglePlayer=0`), requires no teams, lasts a fixed 60 turns, uses Online speed and the Industrial era, and supports four human pirate players plus four locked AI colonial powers. The maps are the generated Pirate Islands map at Small through Huge and the fixed Large Caribbean map. The accessible launch path is the ordinary Host Game flow, including Hotseat; it should not be added to the single-player Scenario Setup list.
- The four playable factions are Dread Pirate, Privateer, Swashbuckler, and Hoarder. Their defining active actions are respectively Walk the Plank, Bring Her Home, Tack into the Wind, and Chain Shot. The scenario also adds shared Visit Tavern, Careening, Capture Boat, Shore Party disembark/embark, and Bury Treasure unit commands. The final `UnitPanel_PiratesScenario.lua` command table is authoritative for visibility, availability, disabled explanations, callbacks, and entry into target modes; CAI must consume that table rather than recreate its rules.
- The scenario adds Sloop, Brigantine, Galleon, and Shore Party units, ship-specific promotion classes, crew values on ships, more-than-100-health flagships/infamous pirates, buried and floating treasure improvements, treasure maps and fleets, infamous pirates, city tavern/sacking cooldowns, and privateer patron ports. Gold is both purchasing power and crew morale: after two turns at zero gold, non-flagship units begin mutinying. Relics are implemented as fourteen otherwise-unresearchable civics paired with fourteen military policy cards; finding a relic grants its civic, and GovernmentScreen is repurposed as a once-per-turn relic loadout screen.
- Victory is the highest Infamy at the end of 60 turns, with ordinary score categories zeroed and three scenario categories enabled: Treasure, Infamous Pirates, and Fighting. `VICTORY_DEFAULT` also supports a last-pirate-standing finish. Pirates lose when they have no units; colonial powers use a separate normal defeat that includes cities. One More Turn is disabled. World Rankings is removed, but the scenario Diplomacy Ribbon displays each pirate's total and the three live component scores.
- The ruleset adds only `CAPABILITY_DISPLAY_NORMALIZED_TURN` and deletes ordinary science/culture trees and choosers, Civilopedia, diplomacy, religion/faith, Great Works/Great People, trade, espionage, Reports, World Rankings, climate, World Congress, most lenses, plot yields, standard top-panel yields, and many ordinary content rows. CAI must retain capability gates and must not infer that GovernmentScreen is unavailable merely because ordinary culture/civics are removed: Pirates deliberately reuses it for relics.
- The manifest formally replaces thirteen Lua contexts: `GovernmentScreen`, `LaunchBar`, `TechCivicCompletedPopup`, `InGameTopOptionsMenu`, `TopPanel`, `UnitFlagManager`, `UnitPanel`, `MinimapPanel`, `PlotToolTip`, `DiplomacyRibbon`, `WorldInput`, `WorldTracker`, and `ARXManager`. CAI replaces the first twelve and intentionally leaves Logitech-only `ARXManager` alone.
- Three additional scripts are imported for base wildcard hosts rather than registered as formal replacements. `CityBannerManager_PiratesScenario.lua` adds live Tavern and Sacked indicators and their cooldown tooltips. `NotificationPanel_PiratesScenario.lua` changes scenario notification icons, immediately opens relic discoveries, maintains treasure/infamous-pirate notifications, and suppresses initial forced-war spam. `EndGameMenu_PiratesScenario.lua` supplies scenario victory art and limits replay graphs to score, combats, gold, and destroyed units. CAI's existing imported `CityBannerManager_CAI.lua`, `NotificationPanel_CAI.lua`, and `EndGameMenu_CAI.lua` should continue wrapping the final wildcard-composed functions; do not directly include the scenario additions.
- `ExpansionIntro.lua/.xml` is a new four-page How to Play context opened by `InGameTopOptionsMenu_PiratesScenario.lua`. Its pages cover the scenario, crew, treasure, and scoring; it has Description, optional Description2, Previous, Next, and Close controls and no `DontShowAgain` checkbox. `ExpansionIntro_CAI.lua` must select the Pirates addition under this ruleset and treat it like Royale's checkbox-free layout.
- The Main Menu uses a separate base-game `PiratesIntro` context from `Base/Assets/UI/FrontEnd/Multiplayer`, launched by `LuaEvents.MainMenu_ShowPiratesIntro`; it is not the in-game `ExpansionIntro`. CAI supplies a same-name full front-end replacement whose vanilla body is retained before the accessibility block. Its four accessible pages mirror the live description/detail controls and Previous/Next controls; the final page hides Previous and relabels Next as Close without raising `LuaEvents.PiratesIntro_StartMatchMaking`.
- `GovernmentScreen_PiratesScenario.lua` opens directly to Policies, hides My Government, renames the screen and policy controls to Relics, removes policy filters and confirmation dialogs, restricts loadout changes to once per turn, and pauses effects while open. CAI must include it before building its widgets and use a Pirates-specific single relic tab/tree/picker while retaining the official `IsAbleToChangePolicies`, slot, drag/drop, confirm, and close paths. Do not expose the ordinary Governments tab or government-change flows.
- The only scenario government is `GOVERNMENT_PIRATE_KING`, with three `SLOT_WILDCARD` slots. All fourteen relic policies are declared as `SLOT_MILITARY`; vanilla wildcard legality is what permits every unlocked relic in every slot. The accessible Pirates screen should therefore flatten the three live wildcard slots into `Relic slot 1..3`, use `IsPolicyTypeLegalInRow` for assignment, and list only the local player's unlocked relic policies. Do not expose the misleading ordinary Wildcard/Military category labels, locked relics, government selection, policy filters, Civilopedia shortcuts, or the generic four-category policy tree.
- `LaunchBar_PiratesScenario.lua` uses the government button/free-policy flag for newly found relics and stops flagging it after the relic screen has been opened. Selecting it before CAI preserves the existing accessible government hotkey as the relic-screen entry path.
- `TechCivicCompletedPopup_PiratesScenario.lua/.xml` turns civic completion into the relic-found popup: official relic name, effect, flavor text, sound, and Change Relics button. CAI must select the scenario wrapper and read `ResearchName`, `RelicInfoLabel`, and `QuoteLabel`; its generic unlock enumeration alone is insufficient.
- `TopPanel_PiratesScenario.lua` keeps the normalized turn controls but replaces the date with `1621 + elapsed turn`, displayed as an AD year. CAI's turn/time/date action and turn-start announcement should include this wrapper and continue reading the live `Controls.CurrentDate`.
- `UnitPanel_PiratesScenario.lua/.xml` is essential gameplay composition. It adds ten scenario command groups to the final action table, prevents pillaging one's own treasure, exposes Crew before ordinary unit stats, hides naval Rest and Repair, hides the promotion banner during combat preview, and removes duplicate colonial combat modifiers. CAI's existing action-list adapter should expose the execute-script actions automatically once this wrapper is selected. Its interface-mode actions still require WorldInput integration, and Crew must be confirmed in accessible unit-stat output.
- Pirates UnitPanel hides naval Rest and Repair only in its overridden `AddActionButton()`, after the action table has been built. CAI reads the final action table independently of those visual instances, so it must explicitly omit `ICON_UNITOPERATION_HEAL` for `DOMAIN_SEA` units or it will expose an action Firaxis intentionally removed. Crew is stored at `unit:GetProperty(g_unitPropertyKeys.Crew)`; CAI must read that live property rather than assuming the scenario's visual `SpreadCharges` stat has propagated into `UnitData`.
- `WorldInput_PiratesScenario.lua` registers six custom target modes: Capture Boat, Shore Party, Shore Party Embark, Walk the Plank, Bring Her Home, and Chain Shot. Each mode computes `g_targetPlots` through the official scenario validators and sends the official `UnitManager.RequestCommand(... EXECUTE_SCRIPT ...)` payload on selection. Compose it before CAI WorldInput, register all six hashes in CAI's interface-widget map, expose the final `g_targetPlots` through valid-target navigation, and activate through the scenario's final click handler or an exactly equivalent command call. Do not reconstruct target legality.
- The scenario populates `g_targetPlots` inside its interface-mode-enter handler. CAI's generic target cache can be built earlier in that same transition, while the table is still empty, even though the later World Scanner snapshot and click handler see the valid targets. Mark Pirates target modes as live custom targets and rebuild their plot lookup for plot-detail requests; otherwise Shore Party can be actionable and listed by the scanner while the plot tooltip incorrectly says `Invalid target`.
- `MinimapPanel_PiratesScenario.lua` is essential gameplay composition. It draws private treasure-map search zones, global infamous-pirate search zones, treasure-fleet routes, buried/floating treasure effects, and relic direction waves. English Pointer points each local naval unit toward its closest otherwise unseen enemy within range; Dowsing Rod points toward nearby unpillaged treasure not owned by the local player. Those hidden-target disclosures are official relic benefits, but CAI must expose them only while the corresponding relic policy is active and under the same ownership/range rules.
- Treasure-fleet routes are live projected paths from active colonial treasure fleets toward the scenario's Europe exit plot. A fleet spawns at a valid colonial city with two treasure Galleons and three Brigantine guards, then follows a scripted operation toward that exit; route tags are recomputed from the surviving gold ships. The route is global scenario information, while the units still obey visibility, so a visible route can legitimately contain no currently visible ships. It is intended as interception information: pirates can patrol or wait along it, but the path can move or change as fleets advance.
- The Pirates manifest does not export `MinimapPanel_PiratesScenario.lua` through its `ImportFiles` action, even though it formally installs it as a `LuaReplace`. CAI therefore cannot include it by name. Follow the established Royale pattern: keep a normalized-exact, collision-safe copy under `src/UI/Replacements/PiratesScenario/`, register it in the top-level file list and `CAIInGame` imports under the Pirates criterion, and include the copied name from `MinimapPanel_CAI.lua`. Preserve exact parity with the installed source.
- `PlotToolTip_PiratesScenario.lua` adds official details for a local treasure-map search zone, an infamous-pirate search zone, treasure-fleet routes, and the owner of buried/floating treasure. Select it before CAI's plot layer, then carry those final details into cursor speech and the World Scanner. Treasure maps are local-player properties; treasure ownership and relic-based hidden-target information must not leak to other players.
- CAI's plot layer already calls final `FetchData(plot)`, so selecting the Pirates wrapper automatically populates `TreasureSearchTooltip`, `InfamousPirateTooltip`, `TreasureFleetTooltip`, and `TreasureOwnerTooltip`. The missing work is to register CAI plot-info helpers for those four fields and add them to the cursor/geography buckets; do not duplicate the scenario's property searches inside `PlotToolTip_CAI.lua`.
- Relic sensor scanner rows identify the naval source unit and give the source-to-target tile distance; the scanner's trailing compass components remain the target's position relative to the navigation cursor. Use the four-coordinate `Map.GetPlotDistance(unitX, unitY, targetX, targetY)` overload for spoken sensor distance. The scenario Minimap code's two-plot-index overload produced a plot-sized number in this UI context during the first in-game test.
- Treasure-map and infamous-pirate search-zone membership must use that same four-coordinate distance form. Using the scenario's two-index form in the World Scanner caused known search zones to collect no plots in the UI context.
- `DiplomacyRibbon_PiratesScenario.lua/.xml` restricts the ribbon to pirate players, forces it visible, shows multiplayer player plus faction identity, and adds Treasure, Infamous Pirate, Fighting, and total score. CAI must select it before its accessible ribbon builder and add the three scenario category values rather than exposing only generic score.
- CAI's ordinary ribbon enumerates all alive majors and sorts through local diplomacy. Pirates deletes diplomacy and the alive-major set also contains the four colonial powers. Under Pirates, build the accessible list from alive players for which the final scenario `IsPiratePlayer(playerID)` is true, keep the local pirate first, avoid relationship/met-turn queries, and make the rows informational rather than trying to open the absent diplomacy view. Resolve the three category indexes once from `GameInfo.ScoringCategories.PrimaryKey` values `CATEGORY_SCENARIO1..3`, then read each player's live `GetCategoryScore(index)`.
- Pirates player configurations can return localization tags from `GetPlayerName()` even in the multiplayer ribbon. Pass that value through `Locale.Lookup()` before speech; human-entered names remain unchanged while scenario defaults resolve correctly.
- `WorldTracker_PiratesScenario.lua/.xml` adds the Crew Morale strip: gold balance, net gold per turn, gold tooltip, profitable/stable state, turns until mutiny, active mutiny, and remaining mutable unit count. CAI must select it before its tracker layer and provide an on-demand accessible morale/status readout driven by the same live treasury, `LastHadGoldTurn`, two-turn delay, and non-flagship unit count. It should refresh on treasury, unit, policy, local-turn, and local-player changes.
- Crew Morale belongs in CAI's existing T turn/time/date readout, parallel to the other scenario status appended there for Civ Royale. Read the live gold balance, net gold per turn, `LastHadGoldTurn`, two-turn delay, and non-flagship unit count each time T is used. Pirates does not repurpose Ctrl+W; composing the official WorldTracker still preserves its visual refresh subscriptions.
- User decision after the first in-game wording test: Crew Morale on T should be compact—`Crew morale`, `{balance} gold`, `{net} per turn`—followed by the scenario's original profitable, stable, mutiny-countdown, total-mutiny, or empty-fleet string. Do not expand the countdown into depletion/grace/game-end explanations, and do not reuse the icon-bearing visual title.
- `UnitFlagManager_PiratesScenario.lua` is already selected by `UnitFlagManager_CAI.lua`. It marks high-health flagships and infamous pirates with the army marker and changes barbarian-unit tooltips to the Buccaneer description plus live unit name. Verify composition; no new selector work is needed.
- `CityBannerManager_PiratesScenario.lua` already composes through the wildcard, but CAI's `GetStatusList()` currently ignores the added controls. Under Pirates, append the live tooltip from visible `VisitTavernIcon` and `SackedIcon` controls to the existing identity/status bucket. Do not recompute their cooldowns; the final scenario banner has already populated the authoritative localized tooltip.
- New Infamous Pirate notifications deliberately pass `nil` as `ParameterTypes.LOCATION`. They carry `g_notificationKeys.InfamousSearchZoneID`; the scenario activation handler resolves that ID against `Game:GetProperty(g_gamePropertyKeys.InfamousPirateSearchZones)` and calls `UI.LookAtPlot()` on the zone center. CAI must wrap that custom activation and send the same center to `LuaEvents.CAICursorMoveTo`, because the generic `notification:IsLocationValid()` bridge cannot handle it. The Pirates World Scanner represents the surrounding search region, not the hidden pirate's exact tile.
- Pirates World Scanner labels are functional names, not Firaxis flavor lines. It has exactly two scenario subcategories: Treasure contains treasure-map search areas, treasure-fleet paths, Dowsing Rod signals, and revealed treasure locations; Enemy activity contains infamous-pirate search areas and English Pointer signals, which can point to any unseen enemy unit despite the relic's ship wording. The official `LOC_PIRATES_PLOT_TOOLTIP_*` flavor remains plot detail only.
- In the default World Scanner order, the contextual Red Death and Pirates categories immediately follow Geography, ahead of Queued path and the remaining interaction categories. Persisted player customization remains authoritative until the layout is reset or a category is moved to its default position.
- Scanner selection follows the item with the CAI cursor. On an unrevealed scenario target, the ensuing cursor announcement can replace the scanner line with `Uncharted area`; therefore cursor-move and B geography output must put the matching clear Pirates object/signal name before fog status. The Pirates `FetchData()` wrapper also uses the unreliable two-index distance overload for treasure-map membership, so CAI plot helpers must fall back to `CAIPiratesMapInfo`'s four-coordinate checks when the official flavor field is missing.
- `InGameTopOptionsMenu_PiratesScenario.lua` adds the How to Play button and opens the Pirates Expansion Intro. Select it before CAI's pause-menu layer. Its XML has no `CivIcon`, so CAI's game-details builder must tolerate that expected absence rather than dereference it.
- Integration should be staged in dependency order: first compose every official wrapper and the Pirates Expansion Intro while adding the Minimap exact copy; second adapt relic GovernmentScreen and relic completion; third expose UnitPanel commands plus all six WorldInput target modes; fourth add morale, three-part scoring, treasure/infamous/fleet plot information, and relic direction targets; finally run multiplayer/hotseat scenario and ordinary-game regression matrices. Do not add a separate Pirates `ReplaceUIScript`; update CAI's existing replacement files so there remains one winning script per context.
- Implemented 2026-07-25: every overlapping CAI context selects the official Pirates script first; the omitted Minimap replacement is preserved as a collision-safe normalized copy. GovernmentScreen is a three-slot relic interface, relic completion exposes the scenario text, UnitPanel reads Crew and hides naval repair, and WorldInput supports all six target modes through the official target set and command payloads. T appends live Crew Morale. The ribbon lists pirate players with all three Infamy categories, plot speech carries the four official Pirates fields, city banners expose Tavern and Sack state, and a contextual World Scanner category contains the known search zones, fleet routes, revealed treasure, and policy-gated English Pointer/Dowsing Rod signals. Both the in-game Expansion Intro and separate Main Menu Pirates Intro are accessible.
- Required in-game verification covers all four factions; all shared and faction unit actions, disabled explanations, cooldowns, and target selection; relic discovery/loadout/change-once-per-turn behavior; crew values and every morale transition; treasure map, buried/floating treasure, treasure fleet, infamous pirate, English Pointer, and Dowsing Rod disclosure; tavern and city-sacking cooldowns; all four intro pages; live AD year; all three score categories; notifications; both defeat types and turn-limit victory; Hotseat identity/focus behavior; and base/XP1/XP2/Black Death/Royale regressions for every shared context.
- Base on-rails tutorial compatibility audit (2026-07-28):
  - `TutorialScenarioBase.lua` contains 246 active `TutorialItem` definitions after removing Lua comments. Of these, 244 are reachable from 52 raise-event roots through `NextID`; `CONSTRUCTING_BUILDINGS_D` and `CONSTRUCTING_BUILDINGS_E` are active but unreachable because `CONSTRUCTING_BUILDINGS_C` has no `NextID` and neither later item has a raise event.
  - The active bank has 57 detailed items using `SetUITriggers(...)`, 13 advisor-only pointer items using `SetAdvisorUITriggers(...)`, and 176 items without a native UI trigger.
  - `TutorialGoals_CAI.lua` still uses the deleted rework APIs `mgr:CreateUIWidget(...)` and direct `FocusedChild` mutation. Opening the accessible goal list from its hotkey or a goal notification will fail even though goal mirroring and notifications can run before that point.
  - `TutorialUIRoot_CAI.NotifyActionPanelAllowed()` currently enables CAI Action Panel input from an active item's declared `UITriggers` during `ActivateItem()`, before detailed mode is raised. End-turn input can therefore execute beneath the still-open advisor. Permission must depend on the current detailed item/state, not merely the active definition.
  - TutorialUIRoot receives Escape before screen contexts. Its passthrough list covers the CAI Technology and Civics Tree roots, but not `CAIPantheon_Panel` or `CAIWorldRank_Panel`. `FIRST_PANTHEON_I` waits for a religion/pantheon close event, and `CAMPUS_COMPLETE_J` raises on `WorldRankingsClosed`; both chains can be stranded when Escape opens the pause menu instead of reaching the CAI screen.
  - CAI World Input mirrors Firaxis's unit-type, hex, and constrained-destination movement restrictions, but its plot interaction directly calls `UI.SelectUnit()`/`UI.SelectCity()` without consulting vanilla World Input's local `m_kTutorialPermittedHexes`. It can bypass `Tutorial_DisableMapSelect` during the rail.
  - World Input registers the CAI tutorial restriction handlers twice: `interfaceInfoHelpers_CAI.lua` subscribes directly, and `EventSubs_CAI.lua` subscribes the same functions again. State is mostly idempotent, but duplicate add/remove events can log false data errors and temporarily duplicate hex restrictions.
  - The seven partial Production Panel items are exactly the seven IDs covered by `TutorialUIRoot_CAI`'s always-receive-input production hooks. Civics close and Government policy-tab selection also have targeted full-context hooks. The general `tutorialActivatedIds` routing table remains empty despite its input-handler comment and requires in-game verification.
  - `TutorialUIRoot_CAI.lua` includes only base `TutorialUIRoot`, not the XP1/XP2 replacement that extends non-player-turn listener eligibility. The base RAILS bank does not use those expansion listener names, but this remains a replacement-composition gap.
  - Full item/surface matrix and prioritized playthrough: `docs/tutorial-scenario-compatibility-audit.md`.

## Better Report Screen (BRS) mod integration

Build spec: `docs/brsSupport.md`. BRS (Infixo, UUID `6f2888d4-79dc-415f-a8ff-f9d81d7afb53`) and CAI both `ReplaceUIScript` the `ReportScreen` context; CAI's higher LoadOrder wins, so BRS's `reportscreen.lua` never runs and its data model (nine `View*Page`/`GetData*` functions, module globals like `m_kCityData`, `m_kResourceData`, `m_kPolicyData`, `m_kCurrentDeals`) is absent under the vanilla path. Plan: detect BRS at runtime (`IsBetterReportScreenActive()` in `caiUtils.lua`) and hand off to a dedicated `ReportScreen_BetterReportsScreen_CAI.lua` variant, mirroring `TradeOverview_BetterTradeScreen_CAI.lua`. BRS source is vendored under `decompiled/mods/BetterReportsScreen/` for reference.

- Shared game-state readers (§8 #6): `inGameHelpers_CAI.lua` now hosts the unit-record helpers (`BuildLocalUnitRecords`/`ResolveUnitRecord`/`UnitRecordFocusKey`/`CategorizeUnit`, plus `SelectUnitRecord`/`JumpToUnitRecord`/`OpenUnitRecordCivilopedia` which take an injected teardown callback) and the city-state readers (`GetCityStateData`/`GetAllCityStatesData`/`GetRelationshipsWithPlayerIDs`/`FillBonuses`). `UnitPanel_CAI` (Ctrl+U list) and `CityStates_CAI` delegate to them; the report Units and Minor tabs will reuse them.
- **Cross-context gotcha (Minor tab):** the city-state readers depend on vanilla globals defined only in the `CityStates` partial screen — `GetRelationships`, `GetCityStateType`, `GetQuests`, `GetBonusText`, `GetSuzerainBonusText` (in `decompiled/Assets/UI/PartialScreens/CityStates.lua`). These are present when the readers are called from the CityStates context but NOT in the ReportScreen context. Before the report Minor tab calls `GetCityStateData`, the report variant must make those vanilla helpers resolvable in-context (e.g. include the CityStates partial or vendor the needed helpers); the `~= nil` dispatcher guard does not cover this because it is a call-time global miss, not a load-time one. Resolve and verify in `Lua.log` when building the Minor tab.

## Quick Deals (mod) integration

Quick Deals (wltk, UUID `5aceed03-8639-4a81-8cbf-03f54d543502`, vendored at `decompiled/mods/QuickDeals/`) adds a launch-bar popup (`OpenQDPopup` action) that queries every met AI at once for their best gold offer. It has five UI contexts registered via `AddUserInterfaces` (context name = XML basename): `qd_dealpopup` (shell: a `TabSupport` tab bar over `TabContentContainer`), and `qd_popuptab_sale`/`qd_popuptab_purchase`/`qd_popuptab_exchange` (each ChangeParents itself into the shell's `TabContentContainer` on the `QD_PopupShowTab` LuaEvent). A separate gameplay-context engine (`qd_offerautomator.lua`) silently opens `DiplomacyManager` sessions with each AI and INSPECT/EQUALIZEs working deals via `DealManager`, driven by `Events.DiplomacyStatement`; it returns offers to the tabs through `QD_EndAIOfferFetch`/`QD_EndAIGoldExchange`/`QD_EndMultiTurnGoldUpdate`. Quick Deals exports every `ui/*.lua` via `<ImportFiles>` so other mods can `include()` them.

- **Detection:** `IsQuickDealsActive()` in `caiUtils.lua`.
- **DiplomacyActionView collision:** Quick Deals also `ReplaceUIScript`s `DiplomacyActionView` (LoadOrder 99999) only to wrap `OnDiplomacyStatement`/`LateInitialize` and close its popup + silent sessions on a surprise statement. CAI's LoadOrder (999999999) wins, so `DiplomacyActionView_CAI.lua` branches its base include to `include("diplomacyactionview_qd")` when QD is active (it re-includes the correct vanilla variant itself and installs QD's wraps), then CAI wraps on top.
- **Hotkey:** `ModCompatibilityConfig_CAI.sql` rebinds `OpenQDPopup` from the bare `D` key (QD's `InsertOrIgnore` default) to Ctrl+D (`LOC_OPTIONS_KEY_CONTROL+LOC_OPTIONS_KEY_D`).
- **Accessibility architecture (shell-owns-all):** CAI wins the four popup contexts. `qd_dealpopup_CAI.lua` (shell) builds ALL widgets (one context = one input handler, no cross-context widget trees) and renders from a model that the three provider files (`qd_popuptab_*_CAI.lua`) publish into `ExposedMembers.CAIQuickDeals[tabKey]`, signalled by `LuaEvents.CAIQD_Changed(tabKey, kind)`. Providers `WrapFunc` their own context's populate functions to capture Quick Deals' per-offer data (the offer table is passed to `PopulateAIOffer(offer, control)`, so wrapping it captures the full offer without needing QD's file-locals) and expose action closures (accept, view deal, increase/decrease 30-turn gold, convert-all, edit amount, add-one/add-ten, remove) that run in their home context and call QD globals (`OnAcceptDeal`/`OnShowDealDetails`/`OnUpdateMultiTurnGold`/`AddResourceToOffer`/etc.). Filters drive fetches by firing the public `QD_StartAIOfferFetch`/`QD_StartAIGoldExchange` with CAI-owned filter state rather than mutating QD's base-file locals. Closures and table refs pass across contexts (Quick Deals itself threads control instances through its own LuaEvents, confirming references survive cross-context dispatch).

## Movie/cinematic subtitles (SubtitleContext) — dead end

`decompiled/Assets/UI/SubtitleContext.xml` is a pure-XML context (`Name="SubtitleContext"`, one `Label ID="SubtitleText"`) with no vanilla Lua, and nothing in the UI Lua references it. Attempted a same-base-name companion Lua imported via both `<ImportFiles>` blocks with a debug `Speak` on load: **it never fires**. The context is not instantiated during normal play (intro/menu tested), so a companion Lua cannot bind and this approach is a dead end. Do not retry it. Captions are driven entirely by the native movie player from the `.srt` files under `Assets/UI/Subtitles/<lang>/`.

## Great work images and the showcase

- `GreatWorkShowcase.lua` picks the image by `GreatWorkObjectType`: writing and music set the shared `WRITING` / `MUSIC` textures; artifacts set `ARTIFACT_<n>` with `n = ((id - 1) % 25) + 1`; relics call `SetIcon("ICON_GREATWORK_RELIC_<id>", 256)`; everything else sets the texture named by the type minus its `GREATWORK_` prefix. `HandleCustomGreatWorkTypes` is the DLC hook: the Babylon Heroes replacement handles `GREATWORKOBJECT_HERO` with `ICON_<GreatWorkType>` (twelve hero symbols have their own icon, all hero epics share atlas index 12), and the Vietnam Monopolies replacement handles `GREATWORKOBJECT_PRODUCT` with `ICON_MONOPOLIES_AND_CORPS_RESOURCE_<RESOURCE>` after stripping `GREATWORK_PRODUCT_` and the `_N` suffix, so a resource's five products share one icon.
- Secret Societies relics are `GREATWORK_RELIC_25` to `_48` (names `LOC_GREATWORK_VOIDSIGNERS_RELIC_<n>_NAME`, n skips 12). Their icons come from `ICON_ATLAS_ETHIOPIA_RELICS_MODE` with only three cells: index 0 for 25 to 32, 1 for 33 to 40, 2 for 41 to 48.
- The showcase textures are packed in `Platforms/Windows/BLPs/UI/UI_GreatWorksArt.blp` per ruleset (also used by the Mac build) and icon atlases in the sibling `Icons.blp`. `CIVBLP` is a Firaxis reflection-serialized `ForgeUI::TexturePackage`; pixels are raw RGBA8, either a sub-rectangle of an atlas page or a table of unique NxN blocks plus an index map. `scripts/Extract-GreatWorksBlp.py` decodes both. Paintings are stored at display size, about 483 px tall, inside their gold frames; every sculpture is a painted cut-out of the statue alone on a transparent background, with no pedestal or setting.
- CAI keys the spoken description off the captured `GreatWorkType` and `GreatWorkObjectType` in `GreatWorkShowcase_CAI.GetDescriptionTag()`; the key scheme is documented in `docs/great-work-descriptions.md`.


## Leader diplomacy scene images

- The diplomacy screen is an animated 3D leader model (`Leader_<Name>.xlp`, referenced from each ruleset's `ArtDefs/Leaders.artdef` via `Leader_BLP_Entry`) drawn in front of painted 2D parallax layers referenced by `Leader_Background_BLP_Entry`. No flat image of the whole screen ships.
- The layers live in `Platforms/Windows/BLPs/UI_LeaderScenes.blp` per ruleset/DLC (Mac build too, under `Civ6.app/Contents/Assets`), named `<LEADERTYPE minus LEADER_>_1` (far, 960x505), `_2` (mid, 960x505), `_3` (foreground, 1920x1010), plus `_4` entries that all point at one shared near-black vignette page. Personas with their own scene (Eleanor England/France, Kublai China/Mongolia, every Leader Pass persona) have their own layer set. Scenario leaders share one backdrop per scenario in a package layout the extractor does not parse.
- `LeaderFallbackImages.blp` (per ruleset/DLC, referenced from `ArtDefs/FallbackLeaders.artdef`) holds `FALLBACK_NEUTRAL_<NAME>`: a 2D RGBA8 render of the leader alone on transparency, 1024 or 1080 px tall, in the neutral idle pose. This is what the diplomacy screen draws when leader scenes are set to low quality, so cut-out plus layers reproduces the low-quality screen. Some names differ from the LeaderType (`CATHERINE`, `PHILLIP`, `ROOSEVELT`, `CURTAIN`, `MBANDE`, `QINALT`, `BOLIVAR`, `ROOSEVELT_RR`, `MENELIK_II`, ...); `scripts/Extract-LeaderImages.py` carries the alias table and writes cut-outs, layers, and composites for all 82 leaders CAI describes. The Base fallback package parses with the great works extractor; the DLC ones use a different data-package layout, so the script locates `BLP::TextureEntry` records by scanning for plausible format/size/offset tuples.
- Props tied to animations (Gandhi's walking stick, Lautaro's rapier, Robert the Bruce's stool) are not in the idle cut-out. The fandom wiki's `<Leader> loadscreen (Civ6).png` files (1920x1080) show the same model in an animated pose; the wiki API (`api.php?action=parse&prop=wikitext`) works with curl even though page fetches are blocked, and 51 of the 77 leader pages carry a trivia line naming the scene location.

## macOS (Aspyr) build

The Mac port and its native layer are documented in `mac/README.md`. The facts below are the ones a Lua screen author needs.

- `UI.GetAspyrAppVersion` exists only on the Aspyr macOS build; `IsMacBuild()` in `caiUtils.lua` is the shared check. Aspyr patched nine vanilla Lua files (`ASPYR MOD BEGIN` / `ASL_BEGIN` markers); partial `_CAI` replacements inherit those edits, and the four full-copy files (`MainMenu.lua`, `Options.lua`, `PopupDialog.lua`, `My2K.lua`) branch on `m_isAspyrMacBuild`.
- The Mac build writes no `Lua.log`; the native layer writes `print` output and runtime errors to `~/Library/Logs/CAI/cai_native.log`.
- `Automation.GetTime()` advances only once per second on the Aspyr build (observed: every audio-manager sound queued with a 0.1 s delay started at the same fraction of a second, 0.1 to 1 s late). Never use it for a CAI timer or delay; call `GetMonotonicTime()` from `caiUtils.lua`, which reads the native `CAI.GetTime()` on the Mac and `Automation.GetTime()` on Windows. The two clocks have different origins, so compare only values from the same function.
- Option arrives as Alt (`IsAltDown`). Command arrives as its own key, `Keys.VK_LWIN` (118), with normal key-down and key-up events and no modifier flag; a key pressed with Command held arrives as the plain key, and Command stacks with Shift. `InputStruct:GetFlags()` is Shift 4, Control 8, Alt 16. `IsCommandDown()` in `caiUtils.lua` gives the live state (native `CAI.IsCommandDown`, falling back to `VK_LWIN` tracking from `UIScreenManager:HandleInput`).
- Mac key rule: a binding with `IsControl = true` on one of the four arrow keys also matches with Command held and is spoken as Command (`LOC_CAI_KEY_COMMAND`), because Ctrl+arrow is taken by Mission Control on macOS. Implemented through `IsMacCommandKey` in `caiUtils.lua`, used by `UIWidget:OnHandleInput` and `FormatBinding` only, so DataTable and Grid column jumps and EditBox word movement need no per-widget change. `PathOwnsEnterBinding` is untouched because Enter is never a Command key. Alt is spoken as Option (`LOC_CAI_KEY_OPTION`) through `GetAltKeyName`.
- Combinations macOS takes first and that no binding may use: Cmd+Tab, Cmd+Space, Cmd+Q, Cmd+H, Cmd+M, Cmd+W, Cmd+backtick and the screenshot keys. Aspyr remapped nothing to Command; the shipped Mac defaults are the Windows ones (Ctrl+F map search, Ctrl+comma and Ctrl+period in the Civilopedia).
- Option+arrow key-down events occasionally do not reach Lua while the key-up always does; plain and Shift+arrow are reliable. Do not bind Option+arrow on key-down.
- Every key press, modifiers included, stops speech natively before the key reaches Lua, the way a screen reader does on Windows (`keyboard.mm`; VoiceOver through prism is left to stop itself). Lua needs no `CAI.Silence()` on key-down for this, and speech a screen starts on key-up or later is unaffected.
- Character input: on the Mac typed characters are queued natively and drained by `UIScreenManager:PollCharInput` through `CAI.PollCharInput()`, once per frame from the contexts with an update hook and on every key-up in `HandleInput`; on Windows the DLL calls the registered handler directly and that function is absent.
- A front-end context's `ContextPtr:SetUpdate` callback stops firing while a popup queued above it is shown: `MainMenu.lua`'s `OnUpdate` ran on the main menu and stopped for as long as `AdvancedSetup` and the leader picker were up (Mac, 2026-09-03). Per-frame work that must continue under popups cannot rely on the parent context's update hook.
